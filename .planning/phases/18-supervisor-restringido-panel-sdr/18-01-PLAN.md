---
phase: 18-supervisor-restringido-panel-sdr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.js]
autonomous: true
requirements: [PH18-SC1, PH18-SC2, PH18-SC4]
must_haves:
  truths:
    - "Un supervisor scoped (visibleSetterIds no vacío) recibe en team-performance solo sus setters visibles y teamAverages calculado solo sobre ellos"
    - "Un supervisor scoped que pide ?setter=<oculto> o pega a un lead/llamada de setter oculto recibe 403, no resultado vacío"
    - "Un supervisor scoped recibe 403 en telnyx/balance, real-costs, events, pool-summary, pool-setter-breakdown, command"
    - "script-effectiveness, recent-responses y GET /api/auth/users devuelven al supervisor scoped solo data de sus setters visibles (+ su propio user en /users)"
    - "Un supervisor SIN visibleSetterIds (ausente o []) tiene comportamiento 100% idéntico al actual (cero regresión)"
    - "Admin puede setear visibleSetterIds vía PATCH /api/auth/users/:id y vía el flujo invite/accept-invite; el campo persiste en auth.json y se refleja en publicUser"
  artifacts:
    - path: "index.js"
      provides: "Helpers _visibleSetterIds / _filterSettersVisible / _setterIsVisible + scoping aplicado en todos los endpoints supervisor-accesibles + auth plumbing del campo visibleSetterIds"
      contains: "globalThis.__phase18"
  key_links:
    - from: "req.auth.user.visibleSetterIds"
      to: "_visibleSetterIds(authUser)"
      via: "helper central invocado en cada handler tocado"
      pattern: "_visibleSetterIds\\(req\\.auth\\.user\\)"
    - from: "publicUser(user)"
      to: "frontend /api/auth/me"
      via: "visibleSetterIds expuesto en la proyección"
      pattern: "visibleSetterIds"
---

<objective>
Implementar el scoping server-side (RBAC real) del rol `supervisor` por subconjunto de setters visibles, más el plumbing de auth para gestionar el campo `visibleSetterIds`.

Purpose: Un supervisor scoped (ej. ve solo Judith/Roxana/Nadine) no debe poder acceder a NINGUNA data de setters fuera de su lista ni siquiera pegando a la API a mano. Un supervisor sin la lista sigue viendo todo (cero regresión).

Output: `index.js` con helpers centrales `_visibleSetterIds`/`_filterSettersVisible`/`_setterIsVisible`, expuestos en `globalThis.__phase18`, y el filtro aplicado en cada endpoint supervisor-accesible del audit; campo `visibleSetterIds` aceptado/persistido en invite/accept-invite/PATCH users y reflejado en `publicUser`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-supervisor-restringido-panel-sdr/18-CONTEXT.md
@.planning/phases/18-supervisor-restringido-panel-sdr/18-RESEARCH.md

<interfaces>
Helpers/patrones existentes que el executor DEBE reutilizar (no reinventar):

- `_buildUserSetterMap()` (index.js:5303) → devuelve Map userId→setterId desde auth.json.
- `_callSetterId(entry, lead, userMap)` (expuesto en globalThis.__metricsAudit) → atribución de una entry de callLog al setter que LLAMÓ (usar para métricas de llamadas).
- Para LEADS/CALENDAR usar `lead.assignedTo` / `entry.setterId` directo (NO `_callSetterId`).
- `publicUser(user)` (index.js:218) → proyección devuelta en /api/auth/me, /login, /api/auth/users, /accept-invite.
- `getEffectiveAuth(req)` (index.js:519) → { role, setterId, isImpersonating }. Extender para incluir visibleSetterIds.
- `requireRole(...roles)` (index.js:~496) → gate binario por rol; el scoping va DENTRO del handler tras pasar el gate.
- Patrón existente en ~15 endpoints: `if (role === 'setter') { filtrar por authSetterId }`. Agregar rama `else if (visibleSet) { filtrar por visibleSet }` — NUNCA reemplazar la rama de setter.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helpers centrales + auth plumbing del campo visibleSetterIds</name>
  <read_first>
    - index.js:200-240 (publicUser)
    - index.js:519-540 (getEffectiveAuth)
    - index.js:5300-5315 (_buildUserSetterMap + globalThis.__metricsAudit — patrón de exposición)
    - index.js:1838-1920 (POST /api/auth/invites + POST /api/auth/accept-invite)
    - index.js:5690-5760 (PATCH /api/auth/users/:id)
    - index.js:1645-1680 (GET /api/auth/users)
  </read_first>
  <action>
    1. Definir 3 helpers cerca de `_buildUserSetterMap` (índice ~5303), EXACTAMENTE con estas firmas:
       - `function _visibleSetterIds(authUser)`: si `!authUser || authUser.role !== 'supervisor'` → `return null`. Sino `const ids = Array.isArray(authUser.visibleSetterIds) ? authUser.visibleSetterIds.filter(Boolean) : []`; si `ids.length === 0` → `return null`; sino `return new Set(ids)`.
       - `function _filterSettersVisible(setters, visibleSet)`: si `!visibleSet` → `return setters`; sino `return (setters || []).filter((s) => visibleSet.has(s.id))`.
       - `function _setterIsVisible(setterId, visibleSet)`: `return !visibleSet || visibleSet.has(setterId)`.
    2. Exponer: `globalThis.__phase18 = { _visibleSetterIds, _filterSettersVisible, _setterIsVisible };`
    3. `publicUser` (218): agregar al objeto devuelto `visibleSetterIds: Array.isArray(user.visibleSetterIds) ? user.visibleSetterIds : []`.
    4. `getEffectiveAuth` (519): agregar `visibleSetterIds: (req.auth?.user?.visibleSetterIds) || []` al objeto devuelto (campo nuevo, no rompe consumidores).
    5. `PATCH /api/auth/users/:id` (admin-only): si el body trae `visibleSetterIds`, validar que sea array; filtrar a solo ids que existan en `data.setters` (map `s.id`); guardar `user.visibleSetterIds = <lista validada>`. Ignorar si no viene en el body (no borrar).
    6. `POST /api/auth/invites` (admin): aceptar `visibleSetterIds` opcional del body; si `role === 'supervisor'` y es array, validar contra `data.setters` y guardarlo en el objeto `invite`. Para otros roles, no guardar.
    7. `POST /api/auth/accept-invite`: al crear el user, copiar `visibleSetterIds: invite.visibleSetterIds || []` (mismo patrón que `setterId: invite.setterId || ''`).
    8. NO tocar la rama de `role === 'setter'` en ningún lado.
  </action>
  <acceptance_criteria>
    - `grep -n "globalThis.__phase18" index.js` devuelve 1 línea con `_visibleSetterIds, _filterSettersVisible, _setterIsVisible`.
    - `grep -c "function _visibleSetterIds" index.js` == 1.
    - `grep -n "visibleSetterIds" index.js` incluye una ocurrencia dentro del cuerpo de `publicUser` (entre línea 218 y ~240).
    - PATCH /api/auth/users/:id filtra visibleSetterIds contra data.setters (existe `.filter(` sobre setter ids en ese handler).
    - `node --check index.js` pasa sin error.
  </acceptance_criteria>
  <verify>
    <automated>node --check index.js</automated>
  </verify>
  <done>Helpers definidos y expuestos; visibleSetterIds fluye por publicUser/getEffectiveAuth y se persiste en invite/accept-invite/PATCH users con validación contra setters existentes.</done>
</task>

<task type="auto">
  <name>Task 2: Scoping en endpoints de métricas y listas agregadas</name>
  <read_first>
    - index.js:8929-9075 (GET /api/setters/team-performance — perSetter + teamAverages)
    - index.js:5319-5360 (GET /api/setters/cold-call-metrics)
    - index.js:8793-8875 (GET /api/setters/performance)
    - index.js:5411-5430 (GET /api/setters/team/:id/calls-today) y 5221-5240 (quota)
    - index.js:5971-6025 (GET /api/setters/objection-analytics)
    - index.js:5132-5140 (GET /api/setters) y 8368+ (GET /api/setters/stats)
    - index.js:13589-13660 (GET /api/telnyx/metrics), 12609-12640 (cold-call-effectiveness), 12977-12990 (telnyx/calls/recent)
    - index.js:13167-13205 (GET /api/training/calls — patrón TRAINING_EXCLUDED_SETTERS a extender)
    - index.js:1567+ (GET /api/auth/online)
    - index.js:1645-1680 (GET /api/auth/users)
    - index.js:12747-12800 (GET /api/telnyx/script-effectiveness — loop de callLog por lead)
    - index.js:6073-6090 (GET /api/setters/recent-responses + _registerLeadResponse en 7225 — las entries traen setterId)
  </read_first>
  <action>
    En cada handler, tras `requireRole`, obtener `const visibleSet = _visibleSetterIds(req.auth.user);` (o leer `eff.visibleSetterIds` si el handler ya usa getEffectiveAuth — convertir a Set con misma semántica vacío=null). Aplicar:
    - `team-performance` (8929): filtrar `data.setters` con `_filterSettersVisible(..., visibleSet)` ANTES del `.map()` que construye `perSetter` (~8988), para que `teamAverages` (~9059) se calcule SOLO sobre el subconjunto. Filtrar `alerts` a setters visibles.
    - `cold-call-metrics` (5319): si viene `?setter=<X>` y `visibleSet && !visibleSet.has(X)` → `return res.status(403).json({error:'Setter fuera de tu visibilidad'})`. Si `?setter=` vacío: en el loop de leads/calls agregar `if (visibleSet && !visibleSet.has(_callSetterId(entry, lead, userMap))) continue;` (métricas de llamadas → `_callSetterId`).
    - `performance` (8793): filtrar `allLeads` por `visibleSet` (usar `lead.assignedTo`) cuando no hay `setterFilter`; filtrar el array `setters` devuelto con `_filterSettersVisible`; si `setterFilter` puntual y `!visibleSet.has(setterFilter)` → 403.
    - `calls-today` (5411) y `quota` (5221): tras el chequeo existente de setter, agregar `if (visibleSet && !visibleSet.has(setterId)) return res.status(403).json({error:'Setter fuera de tu visibilidad'})`.
    - `objection-analytics` (5971): en el loop, `if (visibleSet && !visibleSet.has(_callSetterId(entry, lead, userMap))) continue;` antes de acumular en bySetter/byCountry/byTag.
    - `/api/setters` (5132): devolver `_filterSettersVisible(data.setters, visibleSet)`.
    - `/api/setters/stats` (8368): filtrar `leads` por `lead.assignedTo` en visibleSet cuando no hay authSetterId; filtrar el array `setters` de la respuesta con `_filterSettersVisible`.
    - `telnyx/metrics` (13589) y `cold-call-effectiveness` (12609): filtrar el pool de calls por `_callSetterId(...)` en visibleSet antes de agregar; filtrar `bySetter`.
    - `telnyx/calls/recent` (12977): en el `if (role==='setter')` existente, agregar rama `else if (visibleSet)` que filtra por `_callSetterId(...)` en visibleSet.
    - `training/calls` (13172): replicar la lógica de `TRAINING_EXCLUDED_SETTERS` pero en modo INCLUIR: `if (visibleSet && !visibleSet.has(lead.assignedTo)) continue;` y mismo chequeo sobre `userSetterId[c.by]`.
    - `auth/online` (1567): construir map userId→setterId (usar `_buildUserSetterMap`) y filtrar `allUsers` a: el propio user (`u.id === req.auth.user.id`) + users con `setterId` en visibleSet.
    - `GET /api/auth/users` (1645): para supervisor scoped, filtrar `data.users` a: el propio caller (`u.id === req.auth.user.id`) + users cuyo `u.setterId` esté en visibleSet (FILTRAR, no 403 — otras UIs consumen este endpoint). Admin y supervisor sin lista: sin cambios.
    - `telnyx/script-effectiveness` (12747): en el loop de `Object.values(settersData.leads)` → `lead.callLog`, agregar `if (visibleSet && !visibleSet.has(_callSetterId(c, lead, userMap))) continue;` antes de acumular en `stats` (métricas de llamadas → `_callSetterId`, consistente con telnyx/metrics y cold-call-effectiveness). Construir `userMap` con `_buildUserSetterMap()` solo si visibleSet existe.
    - `recent-responses` (6082): las entries del buffer traen `setterId` (`_registerLeadResponse`, index.js:7225). Filtrar `newResponses` con `.filter(r => _setterIsVisible(r.setterId, visibleSet))` antes de responder.
    - Zero-regression: cuando `visibleSet === null`, ninguna rama nueva debe ejecutarse (comportamiento idéntico al actual).
  </action>
  <acceptance_criteria>
    - `grep -c "_visibleSetterIds(req.auth.user)\|eff.visibleSetterIds\|visibleSet" index.js` >= 12 (uno por endpoint tocado).
    - En team-performance el filtro `_filterSettersVisible` aparece ANTES del cálculo de `teamAverages` (verificable leyendo el bloque 8929-9075).
    - cold-call-metrics y calls-today devuelven `status(403)` para setter fuera del scope (existe `.status(403)` en ambos handlers).
    - training/calls usa `visibleSet.has(lead.assignedTo)` en el filtro de inclusión.
    - El handler de script-effectiveness (bloque tras index.js:12747) contiene `_callSetterId` y `visibleSet` (grep del bloque lo confirma).
    - El handler de recent-responses contiene `_setterIsVisible(r.setterId` (o equivalente con visibleSet) sobre `newResponses`.
    - El handler de GET /api/auth/users contiene un filtro por `visibleSet` sobre `data.users` que preserva al propio caller.
    - `node --check index.js` pasa.
  </acceptance_criteria>
  <verify>
    <automated>node --check index.js</automated>
  </verify>
  <done>Todos los endpoints de métricas/listas agregadas filtran por visibleSet usando la fuente correcta (assignedTo para leads, _callSetterId para llamadas); ?setter=<oculto> devuelve 403; agregados se computan sobre el subconjunto visible; script-effectiveness, recent-responses y GET /api/auth/users también filtrados.</done>
</task>

<task type="auto">
  <name>Task 3: Guards de ownership por lead/calendario + bloqueo 403 de endpoints financieros/pool</name>
  <read_first>
    - index.js:5908-5965 (GET /api/setters/leads y /leads/sin-wsp)
    - index.js:7162-7210 (GET /leads/:id/contact-status, PATCH /leads/:id) y 7373 (followup), 7676 (asistencia)
    - index.js:9549-9600 (GET /api/setters/calendar, POST, /calendar/enriched)
    - index.js:13391-13400 (POST /api/telnyx/calls/:leadId/transcribe) y 13219 (training detail /calls/:leadId/:callIdx)
    - index.js:6853-6910 (pool-summary, pool-setter-breakdown), 12053 (telnyx/balance), 12212 (real-costs), 13678 (telnyx/events), 9094 (command)
  </read_first>
  <action>
    1. Guards de ownership por lead (usar `lead.assignedTo`): en cada uno de `GET /leads/:id/contact-status` (7162), `PATCH /leads/:id` (7177), `PATCH /leads/:id/followup` (7373), `PATCH /leads/:id/asistencia` (7676), agregar como `else if` de la rama de setter existente:
       `const visibleSet = _visibleSetterIds(req.auth.user); if (visibleSet && !_setterIsVisible(lead.assignedTo, visibleSet)) return res.status(403).json({error:'Lead fuera de tu visibilidad'});`
       colocado DESPUÉS de resolver `lead` y ANTES de mutar/devolver.
    2. Leads/calendar list (usar `assignedTo`/`entry.setterId`): en `GET /leads` (5908) y `/leads/sin-wsp` (5925) filtrar por `visibleSet` cuando no hay `authSetterId` (setter real); en `GET /calendar` (9549) y `/calendar/enriched` (9593) filtrar `entry.setterId` por visibleSet cuando aplica; en `POST /calendar` (9556) validar el `setterId`/lead destino contra visibleSet (403 si oculto).
    3. Transcribe (13391) y training detail (13219): agregar el mismo guard `_setterIsVisible(lead.assignedTo, visibleSet)` → 403 para supervisor scoped.
    4. Bloqueo entero (403) para supervisor scoped en endpoints financieros/pool/gestión — al inicio del handler, DESPUÉS de `requireRole('admin','supervisor')`, agregar:
       `const visibleSet = _visibleSetterIds(req.auth.user); if (visibleSet) return res.status(403).json({error:'No disponible para supervisor con setters restringidos'});`
       Aplicar en: `pool-summary` (6853), `pool-setter-breakdown` (6903), `telnyx/balance` (12053), `telnyx/real-costs` (12212), `telnyx/events` (13678), `command` (9094).
    5. Zero-regression: `visibleSet === null` (supervisor sin lista, admin, setter) nunca dispara los guards nuevos.
  </action>
  <acceptance_criteria>
    - `grep -c "Lead fuera de tu visibilidad\|No disponible para supervisor con setters restringidos" index.js` >= 7.
    - Los 6 endpoints financieros/pool (pool-summary, pool-setter-breakdown, telnyx/balance, real-costs, events, command) contienen `if (visibleSet) return res.status(403)`.
    - PATCH /leads/:id contiene un guard `_setterIsVisible(lead.assignedTo` que devuelve 403.
    - El guard nuevo aparece como rama adicional, no reemplaza la rama `role === 'setter'` (grep confirma que la rama de setter sigue presente en esos handlers).
    - `node --check index.js` pasa.
  </acceptance_criteria>
  <verify>
    <automated>node --check index.js</automated>
  </verify>
  <done>Leads/calendario/transcripciones de setters ocultos devuelven 403 para supervisor scoped; endpoints financieros/pool/command devuelven 403 entero para supervisor scoped; supervisor sin lista y admin sin cambios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supervisor scoped → API | Un supervisor autenticado con visibleSetterIds intenta leer/operar data de setters fuera de su lista pegando a la API directo (no solo UI) |
| admin → PATCH users | Solo admin puede escribir visibleSetterIds de cualquier user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | Endpoints de métricas agregadas (team-performance, cold-call-metrics, telnyx/*) | mitigate | Filtrar la fuente ANTES de promediar/agregar; agregados computados solo sobre visibleSet |
| T-18-02 | Information Disclosure | Inferencia por 403-vs-vacío | mitigate | `?setter=<oculto>` devuelve 403 explícito (consistente con patrón role==='setter'), no vacío silencioso |
| T-18-03 | Information Disclosure | Endpoints financieros/pool no filtrables (balance, real-costs, events, pool-summary, command) | mitigate | Bloqueo entero 403 para supervisor scoped (más simple y seguro que filtrar) |
| T-18-04 | Elevation of Privilege | PATCH /api/auth/users/:id escribiendo visibleSetterIds | accept | Ya es admin-only vía requireRole('admin'); se agrega validación de que los ids existan en data.setters |
| T-18-05 | Tampering | Supervisor scoped editando lead ajeno (PATCH /leads/:id) | mitigate | Guard `_setterIsVisible(lead.assignedTo)` → 403 antes de mutar |
</threat_model>

<verification>
- `node --check index.js` pasa tras cada task.
- El plan 2 (tests/supervisor-scope.test.js) valida el comportamiento end-to-end; este plan solo debe dejar el backend listo y sintácticamente válido.
- Zero-regression manual: revisar que ninguna rama `role === 'setter'` fue reemplazada (solo se agregaron `else if`/guards adicionales).
</verification>

<success_criteria>
- Helpers `_visibleSetterIds`/`_filterSettersVisible`/`_setterIsVisible` definidos y en `globalThis.__phase18`.
- Campo `visibleSetterIds` aceptado/validado/persistido en invite, accept-invite, PATCH users; expuesto en publicUser y getEffectiveAuth.
- Todos los endpoints del audit del RESEARCH filtran o bloquean para supervisor scoped; comportamiento idéntico al actual cuando visibleSet es null.
- `node --check index.js` limpio.
</success_criteria>

<output>
After completion, create `.planning/phases/18-supervisor-restringido-panel-sdr/18-01-SUMMARY.md`
</output>
