---
phase: 18-supervisor-restringido-panel-sdr
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified: [tests/supervisor-scope.test.js]
autonomous: true
requirements: [PH18-SC1, PH18-SC2, PH18-SC5]
must_haves:
  truths:
    - "El test verde prueba que un supervisor scoped recibe solo sus setters visibles en team-performance/cold-call-metrics/setters"
    - "El test prueba que un supervisor scoped recibe 403 en lead de setter oculto y en telnyx/balance"
    - "El test prueba que un supervisor SIN visibleSetterIds y un admin siguen viendo todo (regresión)"
    - "El test prueba que PATCH visibleSetterIds es admin-only y valida ids contra setters existentes"
  artifacts:
    - path: "tests/supervisor-scope.test.js"
      provides: "Suite de scoping del supervisor siguiendo el patrón training-privacy"
      contains: "supervisor-scope"
  key_links:
    - from: "tests/supervisor-scope.test.js"
      to: "index.js endpoints scoped"
      via: "supertest + cookie de login real"
      pattern: "visibleSetterIds"
---

<objective>
Suite de tests que valida el scoping server-side del rol supervisor implementado en el plan 18-01.

Purpose: Garantizar (verificable en CI) que un supervisor scoped no puede ver data de setters fuera de su lista en ningún endpoint, que un supervisor sin lista y un admin no sufren regresión, y que la gestión de visibleSetterIds es admin-only y validada.

Output: `tests/supervisor-scope.test.js` verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-supervisor-restringido-panel-sdr/18-RESEARCH.md
@tests/training-privacy.test.js
@tests/team-performance.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixture de auth + setters con setters visibles y ocultos</name>
  <read_first>
    - tests/training-privacy.test.js (setup: tmpData, DATA_DIR, API keys="", pre-popular auth.json/setters.json antes de import, login por cookie)
    - tests/team-performance.test.js (fixture de 3+ setters con distintos patrones; user role supervisor sin visibleSetterIds; rangos explícitos from/to)
    - index.js:5303 (_buildUserSetterMap — cómo se mapea userId→setterId, para armar el fixture coherente)
  </read_first>
  <action>
    Crear `tests/supervisor-scope.test.js`. Setup (copiar patrón de training-privacy.test.js):
    - `process.env.NODE_ENV='test'`, `process.env.DATA_DIR=tmpData` (os.tmpdir()).
    - Setear `process.env.OPENAI_API_KEY=''`, `process.env.MERCURY_API_KEY=''`, `process.env.QWEN_API_KEY=''` (string vacío, NUNCA `delete` — regla #121).
    - Pre-popular ANTES de `await import('../index.js')`:
      - `auth.json` con: 1 admin (`user_admin`), 1 supervisor scoped (`user_sup_scoped`, role 'supervisor', `visibleSetterIds: ['setter_a','setter_b']`), 1 supervisor sin scope (`user_sup_all`, role 'supervisor', SIN visibleSetterIds), y opcionalmente users linked a setters. Passwords con el helper de hash que usen los otros tests (o el shape `{salt,hash}` que espera login — replicar de training-privacy).
      - `setters.json` con 3 setters: `setter_a`, `setter_b` (visibles) y `setter_c` (oculto), cada uno con algunos leads en el MAP `leads` (recordar: leads es objeto keyed por id, NO array) con `assignedTo` y algún `callLog[]` con `by` y outcomes que cuenten como connect/conversation, para poder aseverar métricas.
    - Helper de login que devuelva la cookie `gs_session` (extraer de `set-cookie[0].split(';')[0]`), uno por cada user.
    - Si se testean rangos de tiempo, usar `from`/`to` explícitos (patrón team-performance.test.js), no `period=day`.
  </action>
  <acceptance_criteria>
    - `tests/supervisor-scope.test.js` existe e importa `../index.js` DESPUÉS de escribir los fixtures.
    - `grep -c "= ''" tests/supervisor-scope.test.js` incluye las 3 API keys (no hay `delete process.env`).
    - `grep -c "delete process.env" tests/supervisor-scope.test.js` == 0.
    - Fixture incluye `setter_a`, `setter_b`, `setter_c` y `visibleSetterIds: ['setter_a','setter_b']`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/supervisor-scope.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>Fixture y helpers de login listos; el archivo importa index.js con el entorno de test correcto (API keys vacías, DATA_DIR temporal).</done>
</task>

<task type="auto">
  <name>Task 2: Casos de scoping, regresión y gestión admin</name>
  <read_first>
    - .planning/phases/18-supervisor-restringido-panel-sdr/18-RESEARCH.md (sección "Test nuevo sugerido" — los 9 casos)
    - .planning/phases/18-supervisor-restringido-panel-sdr/18-01-PLAN.md (endpoints y códigos exactos: 403 messages, campos)
  </read_first>
  <action>
    Escribir los casos (supertest + cookie del user correspondiente):
    1. `user_sup_scoped` GET `/api/setters/team-performance` → `perSetter` contiene solo setter_a y setter_b (NO setter_c); aserción numérica: `teamAverages` computado solo sobre a+b (comparar contra el cálculo hecho a mano con esos 2).
    2. `user_sup_scoped` GET `/api/setters/cold-call-metrics` (sin ?setter) → dials/connects agregan solo llamadas de a+b (aserción de un total exacto contando el fixture).
    3. `user_sup_scoped` GET `/api/setters/cold-call-metrics?setter=setter_c` → 403.
    4. `user_sup_scoped` GET `/api/setters` y `/api/setters/stats` → `setters[]` no incluye `setter_c`.
    5. `user_sup_scoped` PATCH `/api/setters/leads/<lead_de_setter_c>` → 403.
    6. `user_sup_scoped` GET `/api/telnyx/balance` → 403.
    7. `user_sup_scoped` GET `/api/auth/online` → no incluye users linked a setter_c.
    7b. `user_sup_scoped` GET `/api/auth/users` → incluye su propio user y users con setterId en {setter_a,setter_b}, NO incluye el user linked a setter_c; admin GET → todos.
    7c. `user_sup_scoped` GET `/api/telnyx/script-effectiveness` → los `used` de cada script cuentan SOLO llamadas de setter_a/setter_b (fixture: entries de callLog con `channel:'telnyx_webrtc'` + `scriptIdsUsed` en leads de setter_b Y de setter_c; aserción numérica exacta del `used` esperado). Admin ve el total completo.
    7d. Speed-to-lead: admin PATCH `respondio:true` sobre un lead de setter_c y otro de setter_a → `user_sup_scoped` GET `/api/setters/recent-responses?since=<antes del PATCH>` devuelve solo la entry con `setterId:'setter_a'`; admin recibe ambas.
    8. Regresión: `user_sup_all` (sin visibleSetterIds) GET `/api/setters/team-performance` → incluye los 3 setters; admin GET idem → 3 setters. Confirma cero regresión.
    9. Gestión: admin PATCH `/api/auth/users/user_sup_scoped` con `{visibleSetterIds:['setter_a']}` → 200 y persiste; con un id inexistente `['setter_zzz']` → se filtra (no persiste el inválido); `user_sup_scoped` intentando PATCH sus propios visibleSetterIds → 403 (endpoint es admin-only).
  </action>
  <acceptance_criteria>
    - La suite tiene al menos 12 `it(`/`test(` casos cubriendo los puntos anteriores (incl. auth/users, script-effectiveness y recent-responses).
    - `npx vitest run tests/supervisor-scope.test.js` termina con todos los tests PASS (exit 0).
    - Aserciones incluyen al menos una comparación numérica exacta de teamAverages/dials (no solo `.not.toContain`).
    - Existe un caso que aserta 403 en team-performance/leads para setter oculto y un caso admin 200 sin filtrar.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/supervisor-scope.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>Los 12 casos verdes; scoping (incl. script-effectiveness, recent-responses, auth/users), regresión (supervisor sin lista + admin) y gestión admin-only validados.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/supervisor-scope.test.js` → todos PASS.
- No hay `delete process.env` en el archivo (API keys seteadas a '').
</verification>

<success_criteria>
- Suite verde con >=12 casos cubriendo scoping (incl. script-effectiveness, recent-responses, auth/users), 403 de setter/financieros ocultos, regresión de supervisor sin lista + admin, y gestión admin-only de visibleSetterIds.
</success_criteria>

<output>
After completion, create `.planning/phases/18-supervisor-restringido-panel-sdr/18-02-SUMMARY.md`
</output>
