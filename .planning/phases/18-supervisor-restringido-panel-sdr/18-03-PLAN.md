---
phase: 18-supervisor-restringido-panel-sdr
plan: 03
type: execute
wave: 2
depends_on: ["18-01"]
files_modified: [public/app.js, public/index.html]
autonomous: false
requirements: [PH18-SC3, PH18-SC4]
must_haves:
  truths:
    - "El admin puede editar visibleSetterIds de un supervisor tildando setters en un modal del Centro de Comando"
    - "En el flujo de invitación, al elegir role=supervisor aparece un selector de setters visibles y se manda en el body del invite"
    - "Un supervisor scoped aterriza en view-team (panel de rendimiento) al loguearse, no en view-calls"
    - "A un supervisor scoped el sidebar le oculta las vistas que darían 403 (pool/Distribución, Centro de Comando, Online, financieros de Centralita)"
  artifacts:
    - path: "public/app.js"
      provides: "Modal de edición de visibleSetterIds, selector en invite, ruteo de home scoped, ocultamiento de sidebar scoped"
      contains: "visibleSetterIds"
    - path: "public/index.html"
      provides: "Cache-buster bumpeado + contenedor(es) para el selector de setters visibles si hace falta"
      contains: "app.js?v="
  key_links:
    - from: "currentUser.visibleSetterIds"
      to: "sidebar hide + home routing"
      via: "chequeo currentUser.role==='supervisor' && visibleSetterIds.length"
      pattern: "visibleSetterIds"
---

<objective>
Frontend del supervisor scoped: gestión desde el Centro de Comando (editar/crear con setters visibles tildados), home = panel de rendimiento pro (view-team + Cold Call Funnel), y ocultamiento de las vistas que darían 403.

Purpose: Cerrar el loop de gestión (admin edita la lista) y experiencia (el supervisor scoped ve solo lo suyo, aterriza en su panel). El backend (plan 18-01) ya devuelve data filtrada, así que los dropdowns/tablas quedan correctos automáticamente; este plan agrega el editor, el ruteo y el hide del sidebar.

Output: `public/app.js` + `public/index.html` con cache-buster bumpeado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-supervisor-restringido-panel-sdr/18-CONTEXT.md
@.planning/phases/18-supervisor-restringido-panel-sdr/18-RESEARCH.md

<interfaces>
- `publicUser` (plan 18-01) ahora devuelve `visibleSetterIds: []` en /api/auth/me → `currentUser.visibleSetterIds` disponible en el frontend.
- app.js:381-385 → loop que aplica `data-roles` (`el.classList.add('hidden')` si el rol no está permitido). Punto de extensión para el hide scoped.
- app.js:10755 → `const _defaultMenuItem = document.querySelector('[data-target="view-calls"]')` + `.click()` → home default. Punto de ruteo del home scoped.
- app.js:9208 `loadUsersPanel()` → tabla de usuarios; acciones inline con `window._changeUserRole` (usa prompt(), app.js:9423), `window._resetUserPassword`, `window._deleteUser`.
- app.js:9304 `_showOnboardingDetail` → patrón de overlay fixed con document.createElement + innerHTML (reusar para el modal de setters visibles).
- app.js:9530 handler de `inviteUserBtn` → arma body de POST /api/auth/invites.
- Preferencia del user: UI minimalista, SIN emojis decorativos (memoria ui-minimalista-sin-emojis).
- Cache-buster actual: app.js `v=20260711e`, style.css `v=20260710a` (index.html líneas 14 y 3339).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editor de setters visibles (modal en tabla de usuarios + selector en invite)</name>
  <read_first>
    - public/app.js:9208-9320 (loadUsersPanel + _showOnboardingDetail como patrón de modal)
    - public/app.js:9423-9460 (window._changeUserRole — patrón prompt actual a complementar)
    - public/app.js:9525-9560 (inviteUserBtn handler)
    - public/index.html:1843-1886 (sección .admin-users-panel + form de invitación con #invite-role, #invite-user-btn)
  </read_first>
  <action>
    1. Modal "Setters visibles" (solo para users con `role==='supervisor'`): en la columna Acciones de la tabla de usuarios (`loadUsersPanel`, gate `meIsAdmin`), agregar un botón "Setters visibles" que abre un overlay (reusar el patrón de `_showOnboardingDetail`: fixed, createElement + innerHTML). El overlay lista TODOS los setters (fetch a `/api/setters` — admin los recibe todos) como checkboxes, pre-tildando los que están en `user.visibleSetterIds`. Botón Guardar → `PATCH /api/auth/users/<id>` con `{visibleSetterIds: [ids tildados]}` → refrescar la tabla. Texto de ayuda: "Vacío = ve todos los setters." Sin emojis decorativos.
    2. Selector en invite: en el form de invitación, agregar un contenedor de checkboxes de setters (oculto por default) que se muestra cuando `#invite-role` cambia a `supervisor` (listener `change`). En el handler de `inviteUserBtn` (9530), si `role==='supervisor'`, agregar `visibleSetterIds: [ids tildados]` al body del POST.
    3. Estilos: reutilizar clases existentes del design system; no inventar CSS nuevo salvo utilitarios mínimos. Si NO tocás style.css, no lo bumpees.
  </action>
  <acceptance_criteria>
    - `grep -c "visibleSetterIds" public/app.js` >= 3 (modal render, guardar PATCH, invite body).
    - El botón/acción de setters visibles solo se renderiza cuando `u.role === 'supervisor'` (condición verificable en el código de loadUsersPanel).
    - El PATCH del modal apunta a `/api/auth/users/` con `visibleSetterIds` en el body.
    - El selector de invite se togglea con un listener sobre `#invite-role`.
    - Sin emojis nuevos en los labels/botones agregados.
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('fs').readFileSync('public/app.js','utf8').includes('visibleSetterIds') || process.exit(1)"</automated>
  </verify>
  <done>Admin puede tildar setters visibles al crear (invite) y al editar (modal en la tabla); el backend recibe y persiste la lista.</done>
</task>

<task type="auto">
  <name>Task 2: Home scoped en view-team + ocultamiento de sidebar + cache-buster</name>
  <read_first>
    - public/app.js:378-386 (loop data-roles)
    - public/app.js:10752-10760 (ruteo de home default view-calls)
    - public/index.html:14 y 3339 (cache-buster style.css / app.js)
    - public/index.html:2555 (view-team) y sidebar items view-pool/view-command/view-online
  </read_first>
  <action>
    1. Home scoped: en el bloque de ruteo default (~app.js:10755), ANTES del `_defaultMenuItem.click()`, agregar:
       `if (currentUser?.role === 'supervisor' && currentUser?.visibleSetterIds?.length) { document.querySelector('[data-target="view-team"]')?.click(); } else { <ruteo actual a view-calls> }`.
       El panel view-team + Cold Call Funnel YA consume el backend filtrado (plan 18-01) → los dropdowns se poblarán solo con setters visibles sin tocar `_teamLoad`/`_mypLoad`. Verificar (leyendo, no reescribir) que `_teamLoad` (app.js:14075) renderiza alerts + tabla comparativa; si el Cold Call Funnel per-SDR no está ya embebido en view-team y es barato agregarlo reusando `_mypLoadColdCall`/`.ccm-*`, hacerlo; si es costoso, dejar view-team tal cual (ya cumple: comparativa, alertas, tendencias).
    2. Ocultamiento de sidebar scoped: tras el loop `data-roles` (app.js:381-385), agregar un bloque que, si `currentUser.role === 'supervisor' && currentUser.visibleSetterIds?.length`, oculte (`classList.add('hidden')`) las vistas que darían 403 o exponen data global. Definir `const SCOPED_HIDDEN_VIEWS = ['view-pool','view-command','view-online']` y ocultar cada `[data-target=...]` correspondiente. (Las cards financieras de Centralita ya están dentro de un `<details>` colapsado y esas rutas 403ean en backend; no es necesario ocultarlas si el supervisor scoped no accede a Centralita — si `view-telnyx-config` es supervisor-accesible, agregarla a la lista.)
    3. Cache-buster: bumpear `app.js?v=` en index.html:3339 a `v=20260712a`. Si NO se tocó style.css, dejar su `?v=` como está.
  </action>
  <acceptance_criteria>
    - `grep -c "view-team" public/app.js` incluye la nueva rama de ruteo de home scoped.
    - Existe `SCOPED_HIDDEN_VIEWS` (o equivalente) con al menos `view-pool`, `view-command`, `view-online`, aplicado solo cuando `visibleSetterIds.length`.
    - `grep -o "app.js?v=[0-9a-z]*" public/index.html` devuelve `app.js?v=20260712a`.
    - La rama de home scoped chequea `currentUser.role === 'supervisor'` Y `visibleSetterIds?.length` (no rutea a view-team a supervisores sin lista).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('public/index.html','utf8'); if(!h.includes('app.js?v=20260712a')) process.exit(1)"</automated>
  </verify>
  <done>Supervisor scoped aterriza en view-team; sidebar oculta pool/command/online; cache-buster bumpeado. Supervisor sin lista y admin sin cambios.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Editor de setters visibles (admin), home del supervisor scoped en view-team y ocultamiento de sidebar. Backend (plan 18-01) filtra la data.</what-built>
  <how-to-verify>
    1. Levantar preview (o prod tras deploy) y loguearse como admin.
    2. En Centro de Comando → tabla de usuarios, abrir "Setters visibles" de un supervisor, tildar 2-3 setters, Guardar. Reabrir y confirmar que persistió.
    3. Crear/invitar un supervisor con role=supervisor y tildar setters en el form de invitación.
    4. Loguearse como ese supervisor scoped: confirmar que aterriza en Equipo (view-team) y que la comparativa/dropdowns muestran SOLO los setters tildados.
    5. Confirmar que el sidebar NO muestra Distribución (pool), Centro de Comando ni Quién está conectado.
    6. Confirmar que un supervisor SIN setters tildados sigue viendo todo (aterriza en Llamadas, ve todas las vistas).
  </how-to-verify>
  <resume-signal>Escribí "aprobado" o describí los problemas encontrados.</resume-signal>
</task>

</tasks>

<verification>
- `node -e` checks de cache-buster y presencia de visibleSetterIds pasan.
- Checkpoint humano confirma gestión + home + sidebar del supervisor scoped y la no-regresión del supervisor sin lista.
</verification>

<success_criteria>
- Admin edita/crea visibleSetterIds desde el Centro de Comando (modal + selector en invite).
- Supervisor scoped: home = view-team, sidebar sin pool/command/online, data filtrada.
- Supervisor sin lista y admin: sin cambios visibles.
- Cache-buster app.js bumpeado a v=20260712a (+ style.css solo si se tocó).
</success_criteria>

<output>
After completion, create `.planning/phases/18-supervisor-restringido-panel-sdr/18-03-SUMMARY.md`
</output>
