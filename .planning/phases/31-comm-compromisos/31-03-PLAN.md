---
phase: 31-comm-compromisos
plan: 03
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - public/index.html
  - public/app.js
  - tests/commitment-ui.test.js
autonomous: true
requirements: [COMM-01, COMM-03]

must_haves:
  truths:
    - "Al cerrar una llamada como Interesado, el usuario elige el tipo de compromiso en el MISMO modal — no hay un segundo paso"
    - "Elegir el tipo repropone la fecha según el mapa D-06, y el usuario la puede ajustar con el calendario"
    - "El usuario declara explícitamente quién se comprometió: él o el prospecto"
    - "Desde la ficha del lead se puede cargar y cerrar un compromiso sin marcar ninguna llamada"
    - "Al guardar, el aviso de destino dice que el lead se fue a la sección de compromisos de Hoy, no a un lugar genérico"
  artifacts:
    - path: "public/index.html"
      provides: "controles de compromiso dentro de #call-next-modal + cache-buster bumpeado"
      contains: "call-next-commitment-tipo"
    - path: "public/app.js"
      provides: "bloque [31-03] COMMITMENT-PURE, wiring del modal, bloque Compromiso en la ficha, rama de destino"
      contains: "[31-03] COMMITMENT-PURE"
    - path: "tests/commitment-ui.test.js"
      provides: "cobertura del bloque puro + aserciones de fuente del cableado"
      min_lines: 120
  key_links:
    - from: "openNextStepModal"
      to: "POST /api/setters/leads/:id/call-disposition"
      via: "campo commitment en el body cuando el usuario eligió un tipo"
      pattern: "commitment: \\{"
    - from: "window._callsSetCommitment / window._callsCloseCommitment"
      to: "PATCH /api/setters/leads/:id/commitment"
      via: "fetch con apiUrl + _leadStoreApply + _refreshLeadPanels"
      pattern: "/commitment"
    - from: "_dispoDestination"
      to: "lead.commitment"
      via: "rama nueva antes de interesado"
      pattern: "Hoy → Mis compromisos"
---

<objective>
Dar al usuario las dos vías de carga del compromiso que pide el CONTEXT: dentro
del modal "Próximo paso" que ya existe (D-08 — no un modal nuevo, porque sumar
un segundo paso al cierre de una llamada es exactamente la fricción que hace que
no se use) y desde la ficha del lead, fuera de una llamada (D-09).

Purpose: COMM-01 y COMM-03 solo existen de verdad cuando el usuario puede
declarar tipo y parte en el momento en que lo habla. Sin esto el modelo de 31-01
y los endpoints de 31-02 quedan sin puerta de entrada.
Output: selector de compromiso en `#call-next-modal` que repropone la fecha del
mapa D-06, bloque "Compromiso" editable en la ficha del lead, y el aviso de
destino nombrando la sección real de Hoy.

**Este plan toca `public/`: el cache-buster de `app.js` en `public/index.html`
DEBE bumpearse.** `public/style.css` NO se toca (todo el markup nuevo usa
estilos inline, igual que el markup vecino de estos bloques).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-comm-compromisos/31-CONTEXT.md
@.planning/phases/31-comm-compromisos/31-PATTERNS.md
@.planning/phases/31-comm-compromisos/31-02-SUMMARY.md
@CLAUDE.md

Reglas del proyecto que aplican:
- **Cache-buster**: cambiar `public/app.js` o `public/index.html` OBLIGA a
  bumpear `app.js?v=` en `public/index.html` (hoy `v=20260815d`). Sin excepción,
  aunque sea una línea — el bug real ya se vivió (sección "Cache-busting" de
  CLAUDE.md).
- **Nota #175**: todo control nuevo de la ficha DEBE terminar en
  `_refreshLeadPanels(leadId)`. El modal de Hoy es un DOM aparte de la lista de
  Llamadas; sin ese refresco el usuario no ve el cambio, vuelve a clickear y
  DESHACE lo que acababa de poner.
- **Regla #105**: tras un POST/PATCH exitoso se pasa el `lead` COMPLETO que
  devuelve el server a `_leadStoreApply(leadId, data.lead)` — nunca un patch
  parcial armado a mano.
- **Fix #181b (cinturón + tiradores)**: los modales se reusan entre aperturas;
  reset SIEMPRE al abrir, antes de `classList.remove('hidden')`, y reset también
  en el `finally` del handler. No romperlo.
- **Patrón de 30-03**: toda disposición pasa por
  `_dispoAfterSaved(leadId, { lead, outcome })` para heredar el aviso de destino.
  Si hace falta un mensaje especial se EXTIENDE `_dispoDestination` dentro del
  bloque `[30-03] DISPO-DEST`; nunca se tira un `showToast` de destino a mano.
- **Marca (identidad SCM)**: usar tokens (`--accent-soft`, `--accent-strong`,
  `--warning`, `--danger`, `--text-primary/secondary/tertiary`). Si algún
  elemento lleva fondo de acento SÓLIDO, el texto va con `var(--on-accent)`.
  Teléfonos y fechas numéricas con `var(--font-mono)` +
  `font-variant-numeric: tabular-nums`.
</context>

<interfaces>
<!-- Contrato que este plan expone y que 31-04 consume TAL CUAL. -->

Bloque puro `[31-03] COMMITMENT-PURE` en `public/app.js` (self-contained: se
extrae por marcadores y se evalúa con `new Function`, sin DOM, red ni
almacenamiento persistente). Exporta:

  COMMITMENT_UI_TIPOS       array de { key, label, parte, canal }, en orden de menú
  COMMITMENT_DELTAS_MS      objeto key -> ms (espejo del mapa D-06 del backend)
  _commitmentLabel(tipo)            -> string (etiqueta corta en minúscula)
  _commitmentDefaultParte(tipo)     -> 'yo' | 'prospecto'
  _commitmentDefaultCanal(tipo)     -> 'whatsapp' | 'llamada'
  _commitmentDefaultDate(tipo, now) -> Date
  _commitmentEffectiveEstado(c, nowMs) -> 'pendiente'|'vencido'|'cumplido'|'incumplido'|''

Nombres de las secciones de Hoy (D-10), fijados por este plan y reusados
LITERALES por 31-04 en `_hoyRenderSection`:

  parte 'yo'        -> "Mis compromisos"
  parte 'prospecto' -> "Esperando del prospecto"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Bloque puro, selector de compromiso en el modal "Próximo paso" y rama de destino</name>

  <read_first>
    - `public/app.js` líneas 11279-11312 — el bloque `[30-02] GATE-PURE`
      completo: es el formato EXACTO de marcadores a replicar
      (`// ─── [NN-NN] NOMBRE-PURE: INICIO ───` / `FIN`) y el estilo de los
      helpers de fecha.
    - `public/app.js` líneas 11026-11128 — `openNextStepModal` completo: default
      cargado al abrir, `_dtPickerAttach` con el literal
      `{ getLead: () => _callsLeadsById.get(leadId) }`, los quickpicks, el
      cinturón+tiradores del botón, la forma del `body` del POST y el llamado a
      `_dispoAfterSaved`.
    - `public/index.html` líneas 1573-1598 — el markup completo de
      `#call-next-modal`.
    - `public/app.js` líneas 8582-8707 — el bloque `[30-03] DISPO-DEST` completo,
      en particular la precedencia de ramas de `_dispoDestination` y la forma de
      su valor de retorno `{ vista, cuando, tono, texto }`.
    - `tests/gate-destination.test.js` líneas 1-60 — cómo se extrae el bloque
      DISPO-DEST con `new Function`. **Consecuencia dura: la rama nueva de
      `_dispoDestination` NO puede referenciar ningún identificador declarado
      fuera de los marcadores DISPO-DEST** (rompería el `new Function` de esa
      suite). Solo literales y campos del `lead`.
    - `.planning/phases/31-comm-compromisos/31-02-SUMMARY.md` — el contrato HTTP
      real de `call-disposition` con `commitment`.
  </read_first>

  <files>public/app.js, public/index.html</files>

  <action>
**A. Bloque puro `[31-03] COMMITMENT-PURE` en `public/app.js`.**
Insertarlo inmediatamente DESPUÉS del cierre del bloque `[30-02] GATE-PURE`
(línea 11312) y antes de `_buildCallbackQuickPicks`. Marcadores literales:
`// ─── [31-03] COMMITMENT-PURE: INICIO ───` y `// ─── [31-03] COMMITMENT-PURE: FIN ───`.
El bloque tiene que ser AUTOCONTENIDO (no referenciar `GATE_INTERESADO_DELTA_MS`
ni nada de afuera) porque el test lo evalúa aislado.

Comentario de cabecera obligatorio: estas duraciones ESPEJAN el mapa D-06 del
backend (`COMMITMENT_DELTA_MS_BY_TIPO` en `index.js`) — si una cambia, cambiar
las dos; el test verifica que los valores coincidan leyendo los dos archivos
como texto. Mismo criterio que `GATE_INTERESADO_DELTA_MS`.

Contenido:
  - `COMMITMENT_DELTAS_MS`: `hablar_con_socio` = 5 días, `pensarlo` = 3 días,
    `pedir_presupuesto` = 3 días, `llamar_despues` = 24 horas, `otro` = 3 días.
    `enviar_info` NO va acá (su fecha es "hoy", la resuelve la función).
  - `COMMITMENT_ENVIAR_INFO_MIN_MS` = 1 hora.
  - `COMMITMENT_UI_TIPOS`: array en este orden exacto, con `key`, `label`
    (texto que ve el usuario), `parte` (default) y `canal` (default):
      `enviar_info` / "Le mando info" / `yo` / `whatsapp`
      `pedir_presupuesto` / "Le mando presupuesto" / `yo` / `whatsapp`
      `llamar_despues` / "Lo llamo en la fecha pactada" / `yo` / `llamada`
      `hablar_con_socio` / "Lo habla con su socio" / `prospecto` / `llamada`
      `pensarlo` / "Lo va a pensar" / `prospecto` / `llamada`
      `otro` / "Otro compromiso" / `yo` / `llamada`
    Los 6 `key` y los defaults de parte/canal tienen que coincidir exactamente
    con `COMMITMENT_TIPOS` / `COMMITMENT_DEFAULT_PARTE` /
    `COMMITMENT_DEFAULT_CANAL` del backend.
  - `_commitmentLabel(tipo)`: etiqueta corta en minúscula para textos inline —
    `mandar info`, `mandar presupuesto`, `volver a llamar`,
    `hablar con su socio`, `lo iba a pensar`, `compromiso`. Espeja
    `COMMITMENT_LABELS` del backend.
  - `_commitmentDefaultParte(tipo)` y `_commitmentDefaultCanal(tipo)`: leen de
    `COMMITMENT_UI_TIPOS`, con fallback `yo` / `llamada`.
  - `_commitmentDefaultDate(tipo, now)`: devuelve un `Date`. Para `enviar_info`,
    el fin del día LOCAL del navegador (23:59 del mismo día) o `now` + 1 hora,
    el que sea mayor — es "mandar hoy" de D-06 fila 1. Para el resto,
    `now + COMMITMENT_DELTAS_MS[tipo]`, con fallback a 3 días. Comentario: el
    frontend SIEMPRE manda un `dueAt` explícito, así que el mapa del backend
    queda como red de seguridad; la pequeña diferencia entre el fin de día local
    y el de la TZ de negocio es intencional y sin efecto.
  - `_commitmentEffectiveEstado(commitment, nowMs)`: misma regla que el backend
    — `''` sin compromiso, el estado almacenado si no es `pendiente`, `vencido`
    si es `pendiente` y `dueAt` ya pasó, `pendiente` si no. No muta nada.

**B. Markup en `#call-next-modal` (`public/index.html`).**
Insertar entre el `<p>` explicativo (línea 1581) y el `<label>Atajos</label>`
(línea 1583) un bloque nuevo con el mismo lenguaje visual del modal:
  - `<label>` con el estilo de los otros labels del modal, texto
    "¿Quedó algún compromiso?".
  - `<select id="call-next-commitment-tipo">` con el mismo estilo de input que
    `#call-next-motivo`, con una primera opción de `value=""` y texto
    "Sin compromiso específico", y las 6 opciones restantes con los `value`
    exactos de `COMMITMENT_TIPOS` y los `label` de `COMMITMENT_UI_TIPOS`. Las
    opciones se escriben literales en el HTML (no se generan por JS) para que el
    test las pueda verificar como fuente.
  - `<div id="call-next-commitment-parte">` con `display:none` inicial, que
    contiene un `<label>` "¿Quién se comprometió?" y dos botones
    `type="button"` con `data-parte="yo"` ("Me comprometí yo") y
    `data-parte="prospecto"` ("Se comprometió él"), con el mismo estilo de los
    botones `cb-quickpick` (fondo `var(--bg-surface)` / borde
    `var(--border-subtle)` en reposo, `var(--accent-soft)` / `var(--accent)`
    cuando está activo).
Actualizar también el `<p>` explicativo para que no mienta: hoy afirma que el
lead queda en "Hoy → Interesados"; agregar que si se carga un compromiso, además
aparece en la sección de compromisos de Hoy.

**C. Wiring en `openNextStepModal` (`public/app.js`).**
  - Al abrir (cinturón, ANTES de `classList.remove('hidden')`): resetear el
    `<select>` a `''` y ocultar el bloque de parte, sin `data-parte` activo. El
    modal se reusa entre leads y un compromiso pegado del lead anterior sería un
    dato falso.
  - `onchange` del `<select>`:
      - Si el valor es `''`: volver a la propuesta base
        (`_gateInteresadoDefaultDate(new Date())`), ocultar el bloque de parte y
        limpiar el `data-active` de los quickpicks.
      - Si es un tipo: setear `fechaInput.value` con
        `_toDatetimeLocal(_commitmentDefaultDate(tipo, new Date()))`, llamar
        `_dtPickerSync(fechaInput)` (mismo mecanismo que ya usan los atajos),
        limpiar el `data-active` de los quickpicks, mostrar el bloque de parte y
        preseleccionar `_commitmentDefaultParte(tipo)`.
  - Click en los botones de parte: marcan `data-parte-active="1"` en el elegido
    y lo quitan del otro, y repintan los dos con los estilos de arriba. No tocan
    la fecha.
  - En el `body` del POST: agregar `commitment` SOLO si el `<select>` tiene un
    valor no vacío, con `{ tipo, parte, canal: _commitmentDefaultCanal(tipo),
    motivo, dueAt: dueAtIso }` — el MISMO `dueAtIso` que ya viaja en
    `nextAction`, para que la fecha elegida por el usuario mande sobre el mapa.
    `parte` sale del botón activo, con fallback a `_commitmentDefaultParte(tipo)`.
  - No tocar el resto del handler: `_consumeTelnyxMeta`, `_dispoEnforcementBody`,
    la nota del Power Dialer, `_leadStoreApply(leadId, data.lead)`,
    `_dispoAfterSaved`, `_refreshLeadPanels`, `renderCallsStats`,
    `loadCallsView` y el `finally` del botón quedan idénticos.

**D. Rama de compromiso en `_dispoDestination` (bloque `[30-03] DISPO-DEST`).**
Insertar una rama nueva DESPUÉS de la de `agendado` y ANTES de la de
`interesado`. Corre cuando `lead.commitment` existe y su `estado === 'pendiente'`.
Usa `_dispoNextAt(lead)` (ya vive en el mismo bloque) para la fecha y
`_dispoWhenLabel` para el texto relativo. Devuelve:
  - `parte === 'yo'`: `vista` = `en Hoy → Mis compromisos`, `tono` = `info`,
    texto del estilo «Nombre» sale de la cola — vuelve <cuando> en
    **Hoy → Mis compromisos**.
  - cualquier otro caso: `vista` = `en Hoy → Esperando del prospecto`,
    `tono` = `info`, texto del estilo «Nombre» sale de la cola — vuelve
    <cuando> en **Hoy → Esperando del prospecto**.
  - Si no hay fecha parseable, omitir el "vuelve <cuando>" (misma tolerancia que
    la rama de `interesado`).
**Restricción dura**: esta rama solo puede usar literales y campos del `lead`.
Nada de `_commitmentLabel`, `COMMITMENT_UI_TIPOS` ni ningún identificador de
afuera de los marcadores DISPO-DEST — el `new Function` de
`tests/gate-destination.test.js` no los tendría en scope.
  </action>

  <verify>
    <automated>node --check public/app.js &amp;&amp; npx vitest run tests/gate-destination.test.js tests/gate-next-step-ui.test.js tests/dtpicker-wiring.test.js tests/panel-drag.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "\[31-03\] COMMITMENT-PURE" public/app.js` devuelve exactamente 2
      (INICIO y FIN).
    - Dentro del rango entre esos dos marcadores,
      `grep -c "document\.\|localStorage\|fetch(" ` devuelve 0 (el bloque es puro).
    - `grep -c "call-next-commitment-tipo" public/index.html` devuelve 1 y
      `grep -c "call-next-commitment-parte" public/index.html` devuelve 1.
    - Los 6 `value` de tipo aparecen literales en `public/index.html`:
      `value="enviar_info"`, `value="pedir_presupuesto"`,
      `value="llamar_despues"`, `value="hablar_con_socio"`, `value="pensarlo"`,
      `value="otro"`.
    - `grep -c "Hoy → Mis compromisos" public/app.js` devuelve al menos 1 y
      `grep -c "Hoy → Esperando del prospecto" public/app.js` devuelve al menos 1,
      y ambas ocurrencias caen dentro del rango de marcadores
      `[30-03] DISPO-DEST`.
    - `npx vitest run tests/gate-destination.test.js` pasa: el `new Function`
      sobre el bloque DISPO-DEST sigue evaluando sin `ReferenceError`.
    - `npx vitest run tests/gate-next-step-ui.test.js` pasa (el cinturón+tiradores
      y el ruteo de `answered_interested` siguen intactos).
    - `grep -c "_dtPickerAttach(fechaInput, { getLead: () => _callsLeadsById.get(leadId) })" public/app.js`
      sigue devolviendo 2 (el literal que verifica `tests/dtpicker-wiring.test.js`
      no se tocó).
    - `git diff --name-only` no incluye `public/style.css`.
  </acceptance_criteria>

  <done>
El modal "Próximo paso" ofrece el compromiso como una opción más, repropone la
fecha del mapa D-06 al elegir tipo, manda el objeto en el body, y el aviso de
destino nombra la sección real de Hoy. Ningún test de las Phases 28/30 se rompió.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bloque "Compromiso" en la ficha del lead (D-09 frontend)</name>

  <read_first>
    - `public/app.js` líneas 7770-7790 — arranque de `_callsRenderExpandedPanel`
      (`fups`, `fupSteps`, y el resto de las variables locales).
    - `public/app.js` líneas 7838-7855 — construcción de `_expChips` (los chips
      de la cabecera de la ficha), donde entra el chip de compromiso.
    - `public/app.js` líneas 7971-7990 — la columna derecha de la ficha: el
      bloque "Follow-up programado" completo (markup a replicar) y el bloque de
      notas.
    - `public/app.js` líneas 7686-7710 — `window._callsToggleFollowup`: el
      handler completo a replicar (fetch con `apiUrl`, actualización del cache,
      `_refreshLeadPanels`, toast con feedback explícito, catch con toast de
      error).
    - `public/app.js` líneas 5833-5851 — `_leadStoreApply` (regla #105).
    - `public/app.js` líneas 6132-6159 — `_hoyRefreshFicha` y
      `_refreshLeadPanels` (nota #175).
    - El bloque `[31-03] COMMITMENT-PURE` creado en la Task 1.
    - `.planning/phases/31-comm-compromisos/31-02-SUMMARY.md` — contrato de
      `PATCH /api/setters/leads/:id/commitment`.
  </read_first>

  <files>public/app.js</files>

  <action>
**A. Bloque "Compromiso" dentro de `_callsRenderExpandedPanel`.**
Agregarlo en la columna derecha, INMEDIATAMENTE ANTES del `<h4>` de "Follow-up
programado" (el compromiso es ahora el mecanismo principal; el follow-up viejo
queda debajo como registro). Usar el mismo `class="call-detail-section-title"`
para el encabezado, con texto "Compromiso".

Tres estados de render, decididos con
`_commitmentEffectiveEstado(l.commitment, Date.now())`:

  1. **`pendiente` o `vencido`** — mostrar en una tarjeta con borde izquierdo de
     3px (mismo lenguaje que `_altBlock`, líneas 7873-7876): la etiqueta del
     tipo (`_commitmentLabel`), quién se comprometió en texto claro
     ("Me comprometí yo" / "Se comprometió él"), el canal, la fecha formateada
     con `var(--font-mono)` + `tabular-nums`, y el `motivo` si lo hay. Si está
     vencido, el borde y la fecha van con `var(--warning)` y se agrega el texto
     "vencido"; si no, con `var(--accent)`.
     Debajo, dos botones de cierre con la clase `call-action-btn`:
       - "Cumplido" → `window._callsCloseCommitment(id, 'cumplido')`.
       - Si `parte === 'prospecto'`: "No cumplió" → `..., 'incumplido'`.
         Si `parte === 'yo'`: "Ya no aplica" → `..., 'vencido'`.
       Comentario obligatorio: D-04 reserva `incumplido` para compromisos del
       prospecto — es dato para el scoring futuro, no un reproche; el cierre de
       un compromiso propio que ya no corresponde usa `vencido`.
     Y un botón "Cambiar" que muestra el formulario de carga (el mismo del punto
     3) para reemplazarlo.

  2. **cerrado** (`cumplido` / `incumplido`) — una sola línea compacta con el
     estado, la etiqueta del tipo y la fecha de cierre (`closedAt`), en
     `var(--text-secondary)`. El detalle de historial completo lo agrega el plan
     31-04 (D-11); acá alcanza con que el compromiso cerrado no desaparezca de
     la vista. Debajo, el formulario de carga del punto 3 para cargar el
     siguiente.

  3. **sin compromiso** — un formulario compacto: un `<select>` con
     `id="call-commit-tipo-<leadId>"` poblado desde `COMMITMENT_UI_TIPOS` (misma
     primera opción vacía "Sin compromiso"), un `<select>` con
     `id="call-commit-parte-<leadId>"` con las dos opciones ("Me comprometí yo"
     / "Se comprometió él"), un `<input type="text">` con
     `id="call-commit-motivo-<leadId>"` y `maxlength="200"`, y un botón
     "Guardar compromiso" que llama `window._callsSetCommitment('<leadId>')`.
     Sin campo de fecha: la propone el mapa D-06 del backend. Dejar un hint de
     una línea en `var(--text-tertiary)` diciendo qué fecha va a proponer,
     calculada con `_commitmentDefaultDate`.

Todo dato del lead que se interpole (nombre, motivo, etiquetas, fechas) va
envuelto en `escHtml` — este markup viaja por `innerHTML`.

**B. Chip en la cabecera de la ficha.**
En la construcción de `_expChips`, agregar (después del chip de INTERESADO) un
chip cuando hay compromiso pendiente: texto con la etiqueta del tipo, fondo
`var(--accent-soft)` y borde `var(--accent-strong)`; si está vencido, con
`var(--warning)`. Si el fondo termina siendo acento sólido, el texto va con
`var(--on-accent)`.

**C. Los dos handlers.**
Calcarlos de `window._callsToggleFollowup` (misma estructura de `try/catch`,
mismo uso de `apiUrl`, mismos toasts):

  - `window._callsSetCommitment = async function(leadId)`: lee los tres controles
    por id, avisa con un toast de tipo `warning` y corta si no se eligió tipo,
    hace `PATCH apiUrl('/api/setters/leads/' + leadId + '/commitment')` con
    `{ tipo, parte, motivo }`. Si la respuesta no es `ok`, parsea el JSON y
    muestra el `error` del server en el toast (los 409 de lead terminal tienen
    un mensaje útil). Si es `ok`: `_leadStoreApply(leadId, d.lead)` con el lead
    COMPLETO (regla #105), `_refreshLeadPanels(leadId)`, y un toast `success`
    que diga qué quedó programado y para cuándo.
  - `window._callsCloseCommitment = async function(leadId, estado)`: mismo
    esqueleto, body `{ estado }`. Toast `success` para `cumplido` e `info` para
    los otros dos. Si el server contesta que ya no había compromiso pendiente,
    mostrar ese mensaje y refrescar igual (el panel estaba viendo datos viejos).

**Ambos handlers DEBEN terminar en `_refreshLeadPanels(leadId)`** incluso en el
camino de error de negocio — es la lección de la nota #175: sin refresco del
modal de Hoy el usuario reintenta y deshace lo que acababa de hacer.

**No** llamar `_dispoAfterSaved` ni `_dispoAnnounce` desde acá: esto no es una
disposición de llamada y `_dispoAfterSaved` libera el gate de disposición
obligatoria de la Phase 20 — hacerlo falsearía que la llamada quedó marcada
(mismo criterio que tomó 30-03 con el hold de calendario).

**D. Cache-buster.**
Bumpear `app.js?v=` en `public/index.html` de `v=20260815d` a `v=20260815e`
(línea 3650). `style.css?v=` NO se toca.
  </action>

  <verify>
    <automated>node --check public/app.js &amp;&amp; npx vitest run tests/gate-destination.test.js tests/dtpicker-wiring.test.js tests/panel-drag.test.js tests/app-version.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "window._callsSetCommitment" public/app.js` devuelve al menos 2
      (la definición y al menos un call site en el markup).
    - `grep -c "window._callsCloseCommitment" public/app.js` devuelve al menos 3
      (definición + los dos botones de cierre).
    - `grep -c "_refreshLeadPanels" public/app.js` sube en al menos 2 respecto del
      baseline pre-task, y ambas ocurrencias nuevas están dentro del cuerpo de los
      dos handlers de compromiso.
    - `grep -c "_leadStoreApply(leadId, d.lead)" public/app.js` devuelve al menos 2.
    - `grep -c "_dispoAfterSaved" public/app.js` NO cambia respecto del baseline
      pre-task (los handlers de la ficha no tocan el gate de la Phase 20).
    - `grep -n "app.js?v=20260815e" public/index.html` devuelve exactamente 1
      línea, y `grep -c "app.js?v=20260815d" public/index.html` devuelve 0.
    - `grep -n "style.css?v=" public/index.html` sigue mostrando `v=20260815d`
      (sin cambios).
    - `git diff --name-only` devuelve exactamente `public/app.js` y
      `public/index.html`.
    - Las 4 suites del comando de verify pasan sin fallos.
  </acceptance_criteria>

  <done>
La ficha del lead muestra el compromiso vigente con su fecha y su parte, permite
cerrarlo con los dos botones correctos según la parte, y permite cargar uno
nuevo — todo refrescando la lista de Llamadas Y el modal de Hoy.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests del bloque puro y del cableado del frontend</name>

  <read_first>
    - `tests/gate-next-step-ui.test.js` COMPLETO — el molde exacto: extracción
      del bloque puro por marcadores con `new Function`, aserciones de markup
      sobre `public/index.html`, aserciones de cableado sobre `public/app.js`,
      helper `countOccurrences`.
    - `tests/gate-destination.test.js` líneas 330-350 — cómo se asserta el
      cache-buster con una comparación de string (`>` sobre el baseline), no con
      igualdad.
    - `index.js`, bloque COMPROMISOS — para las aserciones cruzadas de que los
      valores del frontend y del backend coinciden.
    - El bloque `[31-03] COMMITMENT-PURE` y los handlers escritos en las Tasks 1 y 2.
  </read_first>

  <files>tests/commitment-ui.test.js</files>

  <action>
Crear `tests/commitment-ui.test.js` con los dos modos que ya usa el proyecto (no
hay browser ni jsdom): bloque puro extraído por marcadores + aserciones de
fuente. Todos los casos de fecha usan un reloj FIJO, nunca el reloj de la
corrida (nota #163).

**Bloque puro `[31-03] COMMITMENT-PURE`** (extraído con `new Function`,
devolviendo `COMMITMENT_UI_TIPOS`, `COMMITMENT_DELTAS_MS`, `_commitmentLabel`,
`_commitmentDefaultParte`, `_commitmentDefaultCanal`, `_commitmentDefaultDate`,
`_commitmentEffectiveEstado`):
  - `COMMITMENT_UI_TIPOS` tiene exactamente 6 entradas y sus `key` son los 6
    tipos esperados, en el orden documentado.
  - Cada entrada tiene `label` no vacío, `parte` en `{yo, prospecto}` y `canal`
    en `{whatsapp, llamada}`.
  - `_commitmentDefaultDate('hablar_con_socio', reloj)` cae exactamente a
    +5 días; `pensarlo` y `pedir_presupuesto` a +3 días; `llamar_despues` a +24h.
  - `_commitmentDefaultDate('enviar_info', reloj de mediodía)` cae el MISMO día
    local y después del reloj.
  - `_commitmentDefaultDate('enviar_info', reloj de 23:59:30)` devuelve una fecha
    al menos 1 hora en el futuro.
  - `_commitmentDefaultParte` / `_commitmentDefaultCanal` devuelven el default
    del tipo y el fallback (`yo` / `llamada`) para un tipo desconocido.
  - `_commitmentEffectiveEstado`: pendiente futuro → `pendiente`; pendiente
    pasado → `vencido` SIN mutar el objeto (assert explícito de que
    `commitment.estado` sigue siendo `pendiente`); cumplido pasado → `cumplido`;
    `null` → `''`.

**Paridad frontend ↔ backend** (leyendo `index.js` y `public/app.js` como texto):
  - Los 6 `key` de `COMMITMENT_UI_TIPOS` aparecen literales dentro del bloque
    COMPROMISOS de `index.js`.
  - El valor de 5 días del frontend y el `COMMITMENT_SOCIO_DELTA_MS` del backend
    son la misma expresión (`5 * 24 * 60 * 60 * 1000`).
  - Los defaults de parte del frontend coinciden con los del backend para los 6
    tipos (comparación de pares extraídos por regex de las dos fuentes).

**Markup** (sobre `public/index.html`):
  - `#call-next-commitment-tipo` existe dentro del bloque de `#call-next-modal`
    (verificar por el índice de la substring, entre el `<div id="call-next-modal"`
    y su cierre).
  - Tiene 7 `<option>` (la vacía + las 6), con los `value` exactos.
  - `#call-next-commitment-parte` existe y contiene `data-parte="yo"` y
    `data-parte="prospecto"`.
  - El cache-buster de `app.js` es estrictamente mayor que `20260815d`, y el de
    `style.css` sigue siendo `20260815d`.

**Cableado** (sobre `public/app.js`):
  - `openNextStepModal` referencia `call-next-commitment-tipo` y
    `_commitmentDefaultDate`, y llama `_dtPickerSync` al recalcular la fecha.
  - El body del POST de `openNextStepModal` incluye una clave `commitment`.
  - El reset del `<select>` de compromiso ocurre ANTES de
    `classList.remove('hidden')` (comparar índices de substring dentro de la
    función — patrón "cinturón" del fix #181b, ya usado por
    `tests/gate-next-step-ui.test.js`).
  - `window._callsSetCommitment` y `window._callsCloseCommitment` existen y
    ambos cuerpos contienen `_leadStoreApply`, `_refreshLeadPanels` y
    `apiUrl('/api/setters/leads/'`.
  - Ninguno de los dos handlers contiene `_dispoAfterSaved`.
  - `_dispoDestination` contiene las dos vistas nuevas literales
    (`Hoy → Mis compromisos` y `Hoy → Esperando del prospecto`) y ambas caen
    dentro del rango de marcadores `[30-03] DISPO-DEST`.
  - Todo lo que se interpola del lead en el bloque nuevo de la ficha pasa por
    `escHtml`: assertion de que no hay ningún `${l.commitment.motivo}` ni
    `${c.motivo}` sin envolver (buscar las ocurrencias de `motivo` dentro del
    rango del bloque nuevo y verificar que cada una está precedida por `escHtml(`).

Verificación por mutación al terminar: romper temporalmente el `onchange` del
selector (por ejemplo, no llamar `_commitmentDefaultDate`), correr la suite,
registrar los tests en rojo, restaurar y confirmar con `git diff` que no quedó
ninguna marca. Documentar en el SUMMARY.
  </action>

  <verify>
    <automated>npx vitest run tests/commitment-ui.test.js &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/commitment-ui.test.js` pasa con al menos 22 tests y
      0 fallos.
    - `grep -c "Date.now()" tests/commitment-ui.test.js` devuelve 0 en los casos
      de fecha (todos usan un reloj fijo).
    - `npm test` pasa sin fallos, con un total mayor o igual al reportado por
      31-02.
    - `npx vitest run tests/gate-destination.test.js tests/gate-next-step-ui.test.js tests/dtpicker-wiring.test.js tests/panel-drag.test.js`
      pasa sin fallos.
    - `npx vitest run tests/metrics-consistency.test.js` pasa sin editar ese
      archivo.
    - `git diff --name-only` no incluye `public/style.css`.
    - La verificación por mutación quedó registrada en el SUMMARY y
      `grep -n "MUTATION" public/app.js` devuelve 0.
  </acceptance_criteria>

  <done>
El bloque puro y todo el cableado del frontend están cubiertos por tests que
corren contra el archivo REAL, la paridad de valores frontend/backend está
asegurada por assertion, y la suite completa del repo sigue verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos del lead → `innerHTML` | El `motivo` del compromiso es texto libre escrito por el usuario y vuelve del server para pintarse en la ficha y en los chips. |
| navegador → API | El body del POST/PATCH lo arma el frontend, pero la autoridad es el sanitizador del backend (31-01/31-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering (XSS almacenado) | bloque "Compromiso" de `_callsRenderExpandedPanel`, chip de `_expChips`, texto de `_dispoDestination` | mitigate | Toda interpolación de datos del lead pasa por `escHtml`, igual que el resto del panel. Assertion de fuente dedicada en la suite. El texto de destino que llega al banner del Power Dialer ya se escapa ahí (T-30-08, plan 30-03) y este plan no cambia ese camino. |
| T-31-09 | Tampering | `#call-next-commitment-tipo` | accept | El usuario puede editar el `value` del `<select>` desde DevTools, pero el backend valida contra `COMMITMENT_TIPOS` y descarta el payload entero si no matchea (T-31-01). El frontend no es la frontera de confianza. |
| T-31-10 | Repudiation | handlers de la ficha | mitigate | `createdBy`/`closedBy` los pone el server desde `req.auth.user.name`; el frontend no los manda (mismo criterio que la nota #26). |
| T-31-11 | Denial of Service | `_commitmentDefaultDate` | accept | Función pura O(1) sin I/O. Sin superficie de abuso. |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala paquetes; `package.json` no se modifica. |
</threat_model>

<verification>
1. `node --check public/app.js` limpio.
2. `npx vitest run tests/commitment-ui.test.js` verde.
3. Las 4 suites de frontend de las Phases 28/30 (`gate-destination`,
   `gate-next-step-ui`, `dtpicker-wiring`, `panel-drag`) verdes.
4. `npm test` verde.
5. `grep -n "app.js?v=" public/index.html` muestra `v=20260815e`; `style.css?v=`
   sigue en `v=20260815d`.
6. `git diff --name-only` devuelve exactamente `public/app.js`,
   `public/index.html` y `tests/commitment-ui.test.js`.
7. Ninguna task implementó extracción automática de compromisos desde la
   transcripción ni desde el resumen IA (D-12 — fuera de alcance).
</verification>

<success_criteria>
- El modal "Próximo paso" tiene el selector de compromiso y el toggle de parte;
  elegir un tipo repropone la fecha del mapa D-06 y el calendario de la Phase 28
  la puede ajustar.
- El body del POST manda `commitment` con la misma fecha que `nextAction`.
- La ficha del lead muestra, cierra y reemplaza el compromiso, con los dos
  botones de cierre correctos según la parte.
- El aviso de destino nombra `Hoy → Mis compromisos` o
  `Hoy → Esperando del prospecto`.
- Cache-buster de `app.js` bumpeado; `style.css` intacto.
- Suite completa del repo verde.
</success_criteria>

<output>
Crear `.planning/phases/31-comm-compromisos/31-03-SUMMARY.md` al terminar.

Anotar en el SUMMARY, para el plan 31-04:
- Los nombres LITERALES de las dos secciones de Hoy fijados por este plan
  (`Mis compromisos` / `Esperando del prospecto`) — 31-04 los tiene que usar
  idénticos o el aviso de destino va a mentir.
- La superficie exportable del bloque `[31-03] COMMITMENT-PURE` (lista de
  nombres) y en qué línea quedaron los marcadores.
- El valor del cache-buster que dejó este plan, para que 31-04 lo bumpee otra vez.
- Qué quedó SIN verificar en vivo (no hay browser en el entorno): abrir el modal,
  cambiar de tipo y ver la fecha repropuesta; cargar y cerrar un compromiso desde
  la ficha de Hoy y desde el panel expandido de Llamadas. Dejarlo listado como
  checklist de preview/producción.
</output>
