---
phase: 31-comm-compromisos
plan: 04
type: execute
wave: 4
depends_on: ["31-03"]
files_modified:
  - public/app.js
  - public/index.html
  - tests/commitment-hoy.test.js
autonomous: true
requirements: [COMM-03, COMM-04]

must_haves:
  truths:
    - "Desde Hoy el usuario ve, en cualquier momento, qué compromisos propios están pendientes y qué está esperando del prospecto"
    - "Los compromisos propios se muestran primero: son deuda propia"
    - "Un compromiso vencido sigue visible y marcado como vencido — no desaparece justo cuando hay que actuar"
    - "Después de marcar que ya mandó la información, el lead sigue visible como 'esperando respuesta' con la fecha en que se mandó"
    - "El compromiso cerrado queda en el historial del lead con su fecha de cierre"
  artifacts:
    - path: "public/app.js"
      provides: "secciones de compromisos en Hoy, badge por fila, evento de compromiso en la timeline del lead"
      contains: "Mis compromisos"
    - path: "tests/commitment-hoy.test.js"
      provides: "cobertura del bucket de Hoy y del evento de timeline"
      min_lines: 110
  key_links:
    - from: "loadHoyView"
      to: "_commitmentHoyBucket"
      via: "filtro client-side sobre los leads que Hoy ya trae"
      pattern: "_commitmentHoyBucket"
    - from: "_hoyRenderSection"
      to: "opts.rowBadge"
      via: "badge opcional por fila para mostrar el compromiso y su vencimiento"
      pattern: "rowBadge"
    - from: "_renderCallHistory"
      to: "lead.commitment"
      via: "tercer kind de evento en la timeline unificada"
      pattern: "kind: 'commitment'"
---

<objective>
Cerrar COMM-04: que el usuario pueda consultar en cualquier momento a quién le
mandó información, cuándo, y qué compromisos siguen sin resolver — sin una vista
nueva (D-10: dentro de Hoy) y sin una lista aparte de cerrados (D-11: en el
timeline del lead).

Purpose: los planes 31-01/02/03 dejan el compromiso creado y editable, pero un
compromiso que no se puede consultar de un vistazo no resuelve el dolor real
("saber que a esa persona ya le mandé información y le puedo hacer el
seguimiento").
Output: dos secciones nuevas en Hoy agrupadas por parte (las propias primero),
el vencimiento visible en cada fila, y el compromiso cerrado como un evento más
del historial del lead.

**Este plan toca `public/`: el cache-buster de `app.js` DEBE bumpearse otra vez.**
`public/style.css` NO se toca.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-comm-compromisos/31-CONTEXT.md
@.planning/phases/31-comm-compromisos/31-PATTERNS.md
@.planning/phases/31-comm-compromisos/31-03-SUMMARY.md
@CLAUDE.md

Reglas del proyecto que aplican:
- **Cache-buster**: `public/app.js` cambia → `app.js?v=` en `public/index.html`
  se bumpea. El valor que dejó 31-03 está en `31-03-SUMMARY.md`.
- **Nota #147**: Hoy tiene un filtro por SDR (`#hoy-setter-select`) para
  admin/supervisor. Las secciones nuevas se derivan del MISMO array de leads que
  ya trae `loadHoyView`, así que heredan el filtro sin tocar nada.
- **Nota #125**: Hoy es la vista de seguimientos. Los interesados aparecen todos
  los días hasta agendar/descartar; los callbacks manuales solo el día que
  tocan. Las reglas de esas dos secciones NO se tocan en este plan.
- **PATTERNS ⚠️**: un lead puede tener simultáneamente un callback/interés Y un
  compromiso pendiente. Las secciones de compromisos NO participan del `claimed`
  Set — responden una pregunta distinta ("quién me debe algo") y aparecer en dos
  secciones a la vez es correcto, no un duplicado a evitar.
- **Marca**: tokens del Design System; si algún chip lleva fondo de acento
  sólido, el texto va con `var(--on-accent)`. Fechas con `var(--font-mono)` +
  `font-variant-numeric: tabular-nums`.
</context>

<interfaces>
<!-- Contrato que consume este plan, fijado por 31-01 y 31-03. -->

`lead.commitment` (o `null`): { tipo, parte, canal, dueAt, estado, motivo,
callId, createdAt, createdBy, closedAt, closedBy }.

Del bloque `[31-03] COMMITMENT-PURE` (public/app.js): `COMMITMENT_UI_TIPOS`,
`_commitmentLabel(tipo)`, `_commitmentDefaultParte`, `_commitmentDefaultCanal`,
`_commitmentDefaultDate`, `_commitmentEffectiveEstado(commitment, nowMs)`.

Títulos de sección FIJADOS por 31-03 (los usa `_dispoDestination` para el aviso
de destino — tienen que coincidir LITERAL o el aviso miente):
  parte 'yo'        -> "Mis compromisos"
  parte 'prospecto' -> "Esperando del prospecto"

Firma existente a extender (public/app.js:6056):
  _hoyRenderSection(title, leads, accent, hint, dialerMode)
  -> pasa a
  _hoyRenderSection(title, leads, accent, hint, dialerMode, opts = {})
  con `opts.rowBadge` = función `(lead) => string HTML` (opcional; sin ella el
  render queda byte-idéntico al de hoy).

Función nueva a agregar dentro del bloque `[31-03] COMMITMENT-PURE`:
  _commitmentHoyBucket(lead, nowMs) -> 'yo' | 'prospecto' | null
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Secciones "Mis compromisos" y "Esperando del prospecto" en Hoy (D-10)</name>

  <read_first>
    - `public/app.js` líneas 5926-6021 — `loadHoyView` completa: el fetch de
      `sin-wsp?include=callable`, el upsert en `callsLeadsCache`, el `claimed`
      Set, los filtros de `callbacks` e `interesados`, `virgenesCount`,
      `_hoyState`, los KPIs, `totalPend`/`greetEl` y el `secEl.innerHTML` con el
      orden de secciones.
    - `public/app.js` líneas 6056-6100 — `_hoyRenderSection` completa: la fila
      (`hoy-row`), el chip de dueño, el `cbStr` que ya muestra `callbackAt`, el
      `_dispoSelectHTML`, los botones Ficha/Llamar, el botón de Power Dialer por
      sección y el contenedor `hoy-section`.
    - `public/app.js` — el bloque `[31-03] COMMITMENT-PURE` (marcadores y
      helpers), ubicación exacta en `31-03-SUMMARY.md`.
    - `.planning/phases/31-comm-compromisos/31-CONTEXT.md` — D-03, D-06 (fila 1,
      el seguimiento post-envío), D-10.
    - `.planning/phases/31-comm-compromisos/31-03-SUMMARY.md` — títulos literales
      de las secciones y superficie del bloque puro.
  </read_first>

  <files>public/app.js</files>

  <action>
**A. `_commitmentHoyBucket(lead, nowMs)` dentro del bloque
`[31-03] COMMITMENT-PURE`** (para que quede cubierto por el mismo test de bloque
puro y no se duplique la regla). Devuelve:
  - `'yo'` si `lead.commitment` existe, su estado almacenado es `pendiente` y
    `parte === 'yo'`. (Vencido incluido a propósito: el estado almacenado sigue
    siendo `pendiente`; un compromiso vencido tiene que seguir visible — es
    justo cuando más hay que actuar.)
  - `'prospecto'` si el estado almacenado es `pendiente` y
    `parte === 'prospecto'`.
  - `'prospecto'` TAMBIÉN cuando el compromiso propio ya se cumplió y el lead
    quedó esperando respuesta: `commitment.estado === 'cumplido'` Y
    `commitment.parte === 'yo'` Y `lead.nextAction` con
    `origen === 'compromiso'` Y `tipo === 'esperar_respuesta'`. Ésta es la
    respuesta literal a COMM-04 ("a quién le mandé información, cuándo, y qué
    falta responder"): el lead sigue a la vista después de mandar, con la fecha
    de envío. Dejarlo comentado con esa justificación.
  - `null` en cualquier otro caso.
Función PURA: no muta, no toca DOM ni red.

**B. Badge por fila en `_hoyRenderSection`.**
Agregar un sexto parámetro `opts = {}` con `opts.rowBadge`. Si viene, se
renderiza su resultado dentro de la primera línea de la fila, inmediatamente
después del `cbStr` existente. Sin `opts.rowBadge`, el markup tiene que quedar
BYTE-IDÉNTICO al actual — las secciones de Callbacks e Interesados no pueden
cambiar de aspecto por este plan.

**C. Constructor del badge de compromiso** (función nueva fuera del bloque puro,
junto a `_hoyOwnerName`): `_hoyCommitBadge(lead)`. Devuelve un `<span>` con:
  - La etiqueta del tipo (`_commitmentLabel`), escapada con `escHtml`.
  - La fecha del compromiso (`commitment.dueAt`) en formato `DD/MM HH:MM`, con
    `var(--font-mono)` y `font-variant-numeric: tabular-nums`.
  - Si `_commitmentEffectiveEstado(...) === 'vencido'`: fondo/borde con
    `var(--warning)` y el texto "vencido"; si no, `var(--accent-soft)` /
    `var(--accent-strong)` con `var(--text-primary)`.
  - Caso "ya mandé, espero respuesta" (el tercer caso del bucket): texto
    "info mandada" + la fecha de `closedAt`, con el mismo estilo neutro.
  - Cadena vacía si el lead no tiene compromiso.

**D. Derivación y render en `loadHoyView`.**
Después de calcular `interesados` (línea 5974-5975) y ANTES de `virgenesCount`:
  - `const nowMsHoy = now;` (ya existe `now` en la función).
  - `misCompromisos` = leads con `notDnc(l)` y
    `_commitmentHoyBucket(l, nowMsHoy) === 'yo'`, ordenados por
    `commitment.dueAt` ascendente (lo más vencido primero).
  - `compromisosProspecto` = ídem con bucket `'prospecto'`.
  - **NO llamar `claimed.add`** para estos leads y **no** filtrar por
    `claimed.has`: un lead puede estar en Callbacks y además deberme algo.
    Comentario obligatorio con esa razón.
  - `virgenesCount` queda EXACTAMENTE como está (sigue usando `claimed`), así
    que el puntero de leads nuevos no se mueve.

En `secEl.innerHTML` (línea 6013-6016), poner las dos secciones nuevas PRIMERO,
antes de Callbacks:
  - `_hoyRenderSection('Mis compromisos', misCompromisos, <acento>, 'Le prometí algo — falta cumplirlo', null, { rowBadge: _hoyCommitBadge })`
  - `_hoyRenderSection('Esperando del prospecto', compromisosProspecto, <acento>, 'Se comprometió él — si vence, seguimiento', null, { rowBadge: _hoyCommitBadge })`
  Los títulos van LITERALES: `_dispoDestination` (31-03) promete esos nombres.
  `dialerMode` va en `null` a propósito: D-10 no pide cola de discado para
  compromisos (un "mandar info" no se disca), y el botón Llamar de cada fila
  sigue disponible.
  Acentos: para "Mis compromisos" usar `var(--warning)` (es deuda propia, tiene
  que saltar a la vista); para "Esperando del prospecto", `var(--accent)`.

Actualizar `totalPend` (línea 6010) para que cuente leads ÚNICOS de las cuatro
secciones (construir un `Set` con los ids de callbacks + interesados +
misCompromisos + compromisosProspecto y usar su `size`). Sin esto el texto
"N para seguir" contaría dos veces al mismo lead.

Extender `_hoyState` (línea 5983) con `commitYoIds` y `commitProspectoIds`
(mismo formato que `callbackIds`/`interesadoIds`) — no lo consume nadie todavía,
pero mantiene la invariante de que `_hoyState` describe lo que la vista muestra,
que es de donde `_pdBuildQueueHoy` arma sus colas.

**E. Cache-buster.** Bumpear `app.js?v=` en `public/index.html` al siguiente
valor después del que dejó 31-03 (`v=20260815e` → `v=20260815f`).
`style.css?v=` NO se toca.
  </action>

  <verify>
    <automated>node --check public/app.js &amp;&amp; npx vitest run tests/commitment-ui.test.js tests/gate-destination.test.js tests/panel-drag.test.js tests/dtpicker-wiring.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "_commitmentHoyBucket" public/app.js` devuelve al menos 3 (la
      definición dentro del bloque puro + los dos filtros de `loadHoyView`).
    - La definición de `_commitmentHoyBucket` cae DENTRO del rango entre
      `// ─── [31-03] COMMITMENT-PURE: INICIO ───` y su marcador de FIN
      (verificable comparando índices de substring).
    - `grep -c "_hoyRenderSection('Mis compromisos'" public/app.js` devuelve 1 y
      `grep -c "_hoyRenderSection('Esperando del prospecto'" public/app.js`
      devuelve 1.
    - Los dos títulos literales coinciden con los que usa `_dispoDestination`:
      `grep -c "Mis compromisos" public/app.js` devuelve al menos 2 y
      `grep -c "Esperando del prospecto" public/app.js` devuelve al menos 2.
    - Las dos llamadas nuevas a `_hoyRenderSection` aparecen ANTES de la de
      `'Callbacks'` en el archivo (comparación de índices de substring dentro de
      `loadHoyView`).
    - `grep -c "claimed.add" public/app.js` NO cambia respecto del baseline
      pre-task (los compromisos no reclaman leads).
    - `grep -n "rowBadge" public/app.js` muestra la firma con default
      (`opts = {}`) y al menos 2 call sites.
    - `grep -n "app.js?v=20260815f" public/index.html` devuelve exactamente 1
      línea, y `grep -c "app.js?v=20260815e" public/index.html` devuelve 0.
    - `grep -n "style.css?v=" public/index.html` sigue mostrando `v=20260815d`.
    - `git diff --name-only` devuelve exactamente `public/app.js` y
      `public/index.html`.
  </acceptance_criteria>

  <done>
Hoy muestra las dos secciones de compromisos arriba de todo, con el vencimiento
visible en cada fila, sin alterar el render de Callbacks/Interesados ni el
puntero de leads nuevos.
  </done>
</task>

<task type="auto">
  <name>Task 2: El compromiso cerrado en el historial del lead (D-11)</name>

  <read_first>
    - `public/app.js` líneas 9732-9768 — `_renderCallHistory` completa: la mezcla
      de eventos por `kind`, el `events.sort` por `ts`, el `outcomeMap`, el
      `fmtAgo` y el corte a 6 eventos con el contador "+N eventos más".
    - `public/app.js` líneas 7878-7937 — `historyHtml` y `notesHtml` dentro de
      `_callsRenderExpandedPanel` (las dos listas de la ficha).
    - El bloque "Compromiso" de la ficha creado en 31-03 Task 2 (el estado
      "cerrado" de ese bloque, que hoy muestra una sola línea compacta).
    - `.planning/phases/31-comm-compromisos/31-CONTEXT.md` — D-04 (el objeto NO
      se borra al cerrarse) y D-11.
  </read_first>

  <files>public/app.js</files>

  <action>
D-11: "el historial de compromisos cerrados vive en el timeline del lead, no en
una lista aparte. Un compromiso cumplido es contexto, no una tarea."

**A. Tercer `kind` en la timeline unificada (`_renderCallHistory`).**
Es el único lugar del código con el comentario "Phase 17 Ola 4: timeline
unificada del lead" que el CONTEXT cita como referencia, y es lo que el SDR mira
DURANTE la llamada — que es cuando importa saber "a este ya le mandé la info".

Después del `for` de notas (línea 9743), agregar: si `lead.commitment` existe y
su `estado !== 'pendiente'` y tiene `closedAt`, empujar un evento
`{ kind: 'commitment', ts: <ms de closedAt>, estado, tipo, parte }`. El `sort`
por `ts` existente lo ubica solo en la cronología.

En el `map` de items (línea 9757-9765), agregar la rama de render de ese `kind`,
con el mismo lenguaje visual que las otras dos filas (mismo padding, mismo
`border-bottom`, misma opacidad para el `· <cuando>`): un texto del estilo
"Compromiso cumplido: mandar info" / "Compromiso no cumplido: hablar con su
socio", con la etiqueta del tipo tomada de `_commitmentLabel` y todo
interpolado con `escHtml`. Para `estado === 'vencido'`, el texto dice
"Compromiso vencido". Sin emojis nuevos: usar el mismo criterio del resto del
bloque (las llamadas ya usan 📞, las notas ninguno).

Ojo con el guard de la línea 9744: hoy `if (events.length === 0)` esconde la
caja. Un lead cuyo ÚNICO evento sea un compromiso cerrado ahora debe mostrar la
caja — el guard sigue funcionando tal cual porque el evento ya está en el array,
pero verificar que no quedó ninguna condición extra que lo excluya.

**B. Detalle del cierre en el bloque "Compromiso" de la ficha.**
Enriquecer la línea compacta que 31-03 dejó para el estado cerrado, para que
responda COMM-04 sin abrir nada más: estado en palabras, etiqueta del tipo,
quién se había comprometido, la fecha del compromiso (`dueAt`) y la fecha de
cierre (`closedAt`) con `var(--font-mono)` + `tabular-nums`, y `closedBy` si
está. Para un `enviar_info` de `parte:'yo'` cerrado como `cumplido`, el texto
tiene que decir explícitamente que la información se mandó y cuándo — es la
frase que el usuario pidió textual ("saber que esa persona ya le mandé
información").

Todo lo que se interpola del lead va con `escHtml`.

**C. Limitación conocida, documentada en el código.**
Dejar un comentario en el bloque de la ficha: por D-01 hay UN `lead.commitment`,
así que cargar uno nuevo REEMPLAZA al anterior y el historial visible es el del
ÚLTIMO compromiso. Un array de compromisos quedó explícitamente fuera del
alcance de esta fase; si en el futuro hace falta, ese es el lugar a cambiar.
  </action>

  <verify>
    <automated>node --check public/app.js &amp;&amp; npx vitest run tests/commitment-ui.test.js tests/gate-destination.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "kind: 'commitment'" public/app.js` devuelve exactamente 1 (el
      push del evento).
    - `grep -c "e.kind === 'commitment'" public/app.js` devuelve al menos 1 (la
      rama de render).
    - El push del evento aparece DESPUÉS del `for (const n of notes)` y ANTES de
      `if (events.length === 0)` (comparación de índices de substring).
    - `grep -c "closedAt" public/app.js` devuelve al menos 3 (el push del evento,
      la línea de la ficha y el bloque de 31-03).
    - Dentro del rango del bloque "Compromiso" de la ficha, cada interpolación de
      un campo del compromiso está envuelta en `escHtml(` — verificable listando
      las ocurrencias de `commitment.` en ese rango.
    - `npx vitest run tests/gate-destination.test.js` sigue pasando (el bloque
      DISPO-DEST no se tocó en esta task).
  </acceptance_criteria>

  <done>
Un compromiso cerrado aparece como un evento más en la timeline del lead durante
la llamada y con su detalle en la ficha, con la fecha en que se cumplió.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests de la vista de consulta y verificación final de la fase</name>

  <read_first>
    - `tests/commitment-ui.test.js` (creado en 31-03) — el molde de extracción
      del bloque `[31-03] COMMITMENT-PURE` con `new Function` y las aserciones de
      fuente. Este archivo NO se modifica; el nuevo lo replica.
    - `tests/gate-next-step-ui.test.js` — helper `countOccurrences` y el patrón
      de aserción por índices de substring.
    - `public/app.js` con los cambios de las Tasks 1 y 2.
    - `.planning/phases/31-comm-compromisos/31-CONTEXT.md` — D-10, D-11, D-12.
  </read_first>

  <files>tests/commitment-hoy.test.js</files>

  <action>
Crear `tests/commitment-hoy.test.js` con los dos modos del proyecto (bloque puro
+ aserciones de fuente). Reloj FIJO en todos los casos de fecha.

**Bloque puro — `_commitmentHoyBucket`** (extraído del bloque
`[31-03] COMMITMENT-PURE`):
  - Compromiso `pendiente` con `parte:'yo'` y fecha futura → `'yo'`.
  - El mismo con fecha PASADA → sigue devolviendo `'yo'` (un vencido no
    desaparece de Hoy). Assertion explícita de esta regla, que es el punto de la
    decisión sobre `vencido`.
  - `pendiente` con `parte:'prospecto'` → `'prospecto'`.
  - `cumplido` + `parte:'yo'` + `nextAction {origen:'compromiso',
    tipo:'esperar_respuesta'}` → `'prospecto'` (COMM-04: "ya mandé, espero
    respuesta").
  - `cumplido` + `parte:'yo'` SIN ese `nextAction` → `null`.
  - `incumplido` → `null`. Lead sin `commitment` → `null`. `lead` nulo → `null`.
  - La función no muta el lead que recibe (comparar un `JSON.stringify` antes y
    después).

**Aserciones de fuente sobre `public/app.js`:**
  - `_hoyRenderSection` tiene el sexto parámetro con default (`opts = {}`) y su
    cuerpo referencia `opts.rowBadge`.
  - Las dos llamadas nuevas existen con los títulos LITERALES `'Mis compromisos'`
    y `'Esperando del prospecto'`, ambas con `null` como `dialerMode` y con
    `rowBadge`.
  - Las dos llamadas nuevas aparecen antes que la de `'Callbacks'` dentro de
    `loadHoyView` (índices de substring).
  - Los mismos dos títulos literales aparecen también dentro del rango de
    marcadores `[30-03] DISPO-DEST` — el aviso de destino y las secciones no
    pueden divergir. **Este test es la garantía anti-deriva de la fase.**
  - `loadHoyView` no llama `claimed.add` para los leads de compromiso: el conteo
    de `claimed.add(` en todo el archivo sigue siendo 2.
  - `_hoyState` incluye `commitYoIds` y `commitProspectoIds`.
  - `totalPend` se calcula con un `Set` de ids (buscar `new Set` dentro del
    rango de `loadHoyView` cerca de `totalPend`).
  - `_renderCallHistory` empuja `kind: 'commitment'` y tiene su rama de render.
  - El evento de compromiso se empuja después del loop de notas y antes del
    guard de `events.length === 0`.
  - `escHtml` envuelve toda interpolación de campos del compromiso en
    `_hoyCommitBadge`, en la rama de timeline y en el bloque de la ficha.
  - Cache-buster de `app.js` estrictamente mayor que `20260815e`;
    `style.css?v=` sigue en `20260815d`.

**Anti-alcance (D-12), como test explícito:**
  - `public/app.js` e `index.js` no contienen ninguna llamada que derive un
    compromiso de una transcripción o de un resumen IA: assertion de que no
    existen las substrings `commitmentFromTranscript`,
    `_extractCommitment` ni `commitment` dentro del cuerpo de
    `_trainingSummaryLLM` / `_autoDispositionLLM`. La extracción automática está
    DIFERIDA a propósito (el research la marca poco confiable justo en los
    condicionales, que son la casuística del usuario).

Verificación por mutación al terminar: cambiar temporalmente el título
`'Mis compromisos'` de `loadHoyView` por otro string, correr la suite, registrar
los tests en rojo (tiene que fallar el test de coincidencia con
`_dispoDestination`), restaurar y confirmar con `git diff` que no quedó marca.

**Verificación final de la fase** (esta task es la última de la Phase 31):
correr `npm test` completo y dejar registrado en el SUMMARY el total de tests y
que `tests/metrics-consistency.test.js` sigue verde sin haber sido editado en
ninguno de los 4 planes.
  </action>

  <verify>
    <automated>npx vitest run tests/commitment-hoy.test.js &amp;&amp; npm test</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/commitment-hoy.test.js` pasa con al menos 20 tests y
      0 fallos.
    - `npm test` pasa sin fallos y con un total estrictamente mayor a 1365
      (baseline de `30-03-SUMMARY.md`).
    - `npx vitest run tests/metrics-consistency.test.js` pasa, y
      `git log --oneline -- tests/metrics-consistency.test.js` no muestra ningún
      commit de la Phase 31.
    - `grep -c "delete process.env" tests/commitment-hoy.test.js` devuelve 0.
    - El test de coincidencia de títulos entre `loadHoyView` y `_dispoDestination`
      existe y falla cuando uno de los dos strings se cambia (verificado por la
      mutación, registrado en el SUMMARY).
    - `git diff --name-only` no incluye `public/style.css`.
  </acceptance_criteria>

  <done>
La vista de consulta está cubierta por tests que corren contra el archivo real,
la coincidencia de títulos entre Hoy y el aviso de destino está protegida por
assertion, y la suite completa del repo cierra verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos del lead → `innerHTML` | El badge de la fila de Hoy, la rama de timeline y la línea de cierre de la ficha pintan datos del compromiso (incluido `motivo`, texto libre) por `innerHTML`. |
| scoping por SDR | Las secciones nuevas se derivan del array que ya entrega `GET /leads/sin-wsp`, que aplica el scoping de la Phase 18. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Tampering (XSS almacenado) | `_hoyCommitBadge`, rama `commitment` de `_renderCallHistory`, línea de cierre de la ficha | mitigate | Toda interpolación de campos del compromiso pasa por `escHtml`, igual que `_hoyRenderSection` ya hace con `l.name`/`l.phone`. Assertion de fuente dedicada en la suite. |
| T-31-12 | Information Disclosure | secciones de Hoy | mitigate | No se agrega ningún fetch nuevo: los leads salen del mismo `GET /leads/sin-wsp?include=callable` que ya respeta `_visibleSetterIds` y el filtro por SDR (nota #147). Un supervisor scoped no puede ver compromisos de un SDR fuera de su set. |
| T-31-13 | Tampering | títulos de sección vs `_dispoDestination` | mitigate | Los dos strings viven duplicados por una restricción real (el bloque DISPO-DEST se evalúa aislado con `new Function`). Un test dedicado falla si divergen — verificado por mutación. |
| T-31-05 | Denial of Service | derivación de Hoy | accept | Dos `filter` + un `sort` sobre el array que Hoy ya tiene en memoria; costo lineal sobre un conjunto que la vista ya recorre varias veces. Sin llamadas de red nuevas. |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | Este plan no instala paquetes; `package.json` no se modifica. |
</threat_model>

<verification>
1. `node --check public/app.js` limpio.
2. `npx vitest run tests/commitment-hoy.test.js` verde.
3. `npm test` verde con total > 1365.
4. `npx vitest run tests/metrics-consistency.test.js` verde sin que el archivo
   haya sido editado en toda la fase.
5. `grep -n "app.js?v=" public/index.html` muestra `v=20260815f`;
   `style.css?v=` sigue en `v=20260815d`.
6. `git diff --name-only` devuelve exactamente `public/app.js`,
   `public/index.html` y `tests/commitment-hoy.test.js`.
7. El test anti-alcance de D-12 pasa: no hay extracción automática de
   compromisos desde transcripción ni desde resumen IA en ninguna parte.
</verification>

<success_criteria>
- Hoy muestra "Mis compromisos" y "Esperando del prospecto", en ese orden y
  arriba de Callbacks e Interesados.
- Cada fila muestra qué se prometió y para cuándo, con marca visible de vencido.
- Un compromiso vencido sigue en la lista; un compromiso propio ya cumplido con
  espera de respuesta sigue visible con la fecha de envío.
- El compromiso cerrado aparece en la timeline del lead durante la llamada y con
  su detalle en la ficha.
- Los títulos de sección coinciden con los que promete el aviso de destino, y hay
  un test que lo garantiza.
- Cache-buster bumpeado; `style.css` intacto; suite completa verde.
</success_criteria>

<output>
Crear `.planning/phases/31-comm-compromisos/31-04-SUMMARY.md` al terminar.

Anotar en el SUMMARY:
- El cierre de la Phase 31: qué requirement cubrió cada plan (COMM-01..04) y el
  total de tests de la suite al final.
- Las notas sugeridas para CLAUDE.md (el orquestador las consolida):
  (a) `lead.commitment` es el objeto del compromiso hablado, uno por lead, y su
  reloj se escribe SIEMPRE vía `_setCommitment`/`_closeCommitment` — nunca
  tocando `nextAction`/`callbackAt` a mano;
  (b) `origen:'compromiso'` en `nextAction` significa "este reloj lo puso un
  compromiso" y es lo único que `_closeCommitment` tiene permitido apagar;
  (c) los títulos `Mis compromisos` / `Esperando del prospecto` están duplicados
  a propósito entre `loadHoyView` y `_dispoDestination` (el bloque DISPO-DEST se
  evalúa aislado en tests) y hay un test que los ata;
  (d) la limitación aceptada de D-01: un compromiso por lead, el nuevo reemplaza
  al anterior.
- Checklist de verificación en vivo pendiente (no hay browser en el entorno):
  abrir Hoy con un compromiso propio pendiente y uno del prospecto y confirmar el
  orden y los badges; marcar "Cumplido" sobre un "le mando info" y confirmar que
  el lead pasa a "Esperando del prospecto" con la fecha de envío; abrir el panel
  de una llamada sobre un lead con compromiso cerrado y confirmar que aparece en
  la timeline; confirmar que el aviso de destino tras marcar Interesado con
  compromiso nombra la sección correcta.
</output>
