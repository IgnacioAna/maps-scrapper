---
phase: 21-reporte-diario-canal-whatsapp
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - public/index.html
  - public/app.js
autonomous: true
requirements: [REP-06]

must_haves:
  truths:
    - "El admin ve en Centro de Comando si el canal está sano: grupo destino, último envío, cuántos hay en cola y si la computadora está conectada"
    - "El admin puede pausar el envío automático sin reiniciar el servidor"
    - "El admin puede disparar el reporte al grupo sin esperar a las 23:00, con confirmación previa"
    - "El admin puede marcar a una vendedora de licencia con fecha de vencimiento desde Equipo, y vuelve sola al vencer"
    - "El cache-buster de app.js en index.html cambió respecto de v=20260725c"
  artifacts:
    - path: "public/index.html"
      provides: "#cmd-daily-report-panel en view-command + #team-leave-modal en view-team + cache-buster nuevo"
      contains: "cmd-report-send-now-btn"
    - path: "public/app.js"
      provides: "_cmdLoadReportPanel + máquina de estados del botón Mandar ahora + _teamOpenLeaveModal"
      contains: "_cmdLoadReportPanel"
  key_links:
    - from: "loadCommandCenter()"
      to: "_cmdLoadReportPanel()"
      via: "fire-and-forget, no bloquea el resto del panel si falla"
      pattern: "_cmdLoadReportPanel\\(\\)"
    - from: "#cmd-report-send-now-btn"
      to: "POST /api/admin/daily-report/send-now"
      via: "askConfirm → fetch con apiUrl()"
      pattern: "daily-report/send-now"
    - from: "_teamRenderTable"
      to: "PATCH /api/setters/team/:id"
      via: "modal de licencia con {leaveUntil}"
      pattern: "leaveUntil"
---

<objective>
Agregar la superficie mínima de panel: un bloque de configuración/diagnóstico del
canal en Centro de Comando (D-29) y el control de licencia por vendedora en
Equipo (D-18).

Purpose: el botón "Mandar ahora" es lo que se usa para la prueba en vivo del
grupo sin esperar a las 23:00 (plan 21-07), y el bloque de estado es el único
lugar donde se ve por qué un reporte no salió.
Output: `#cmd-daily-report-panel`, `#team-leave-modal`, sus handlers y el
cache-buster bumpeado.

**Sin vista nueva y sin entrada de navegación nueva.** Alcance deliberadamente
chico: el principio del milestone es que nadie entre al panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-reporte-diario-canal-whatsapp/21-UI-SPEC.md
@.planning/phases/21-reporte-diario-canal-whatsapp/21-CONTEXT.md
@.planning/phases/21-reporte-diario-canal-whatsapp/21-03-SUMMARY.md
@CLAUDE.md

**`21-UI-SPEC.md` es contrato, no sugerencia:** ids de elementos, copy exacto en
español, clases CSS reusadas, precedencia del chip de estado y la máquina de
estados del botón están LOCKED ahí. No parafrasear ningún string.

Reglas que aplican:
- **Cache-buster obligatorio** (CLAUDE.md, "bug invisible"): este plan toca
  `public/app.js` y `public/index.html` → hay que bumpear el `?v=` del `<script>`
  de app.js. Actual `v=20260725c` (index.html:3388) → nuevo `v=20260726a`.
  `style.css` NO se toca (cero clases nuevas) → su `?v=20260725a` queda igual.
- CERO clases CSS nuevas: todo sale del inventario del UI-SPEC
  (`.admin-variable-panel`, `.chip` + `.chip-success/.chip-warning/.chip-danger/.chip-neutral`,
  `.btn` + `.btn-primary`/`.btn-secondary`, `.btn-table-action`, `.btn-sm`,
  `.setter-input`, `.loader`).
- Tipografía: `.btn-primary`/`.btn-secondary` NO declaran `font-size` (lo declara
  el selector agrupado `.btn, .pill-btn, .btn-table-action`). Todo botón con
  variante lleva la clase base: `class="btn btn-primary"`.
- `data-roles` NO funciona en HTML generado por JS (`app.js:502` corre una sola
  vez al boot): el botón de licencia dentro de `_teamRenderTable` se condiciona
  con `${currentUser.role === 'admin' ? '...' : ''}`.
- Usar `apiUrl()` y no `fetch()` crudo (regla #146 — `view-team` la ve un
  supervisor scoped).

<interfaces>
Endpoints del plan 21-03:
```
GET  /api/admin/daily-report/status
  → { groupConfigured: bool, groupName: string|null, jidCaptured: bool,
      lastSent: { at: ISO, periodLabel: string, status: 'sent'|'failed' } | null,
      queueCount: number, desktopOnline: bool, paused: bool, backupEmails: string[] }

PUT  /api/admin/daily-report/config
  body { backupEmails?: string[], paused?: bool }   → mismo shape que el GET

POST /api/admin/daily-report/send-now
  → { ok: true,
      status: 'sent' | 'sent_via_dm' | 'queued' | 'failed',
      reason?: 'offline' | 'sin_grupo' | 'busy' | 'sending' | 'fallback_dm' | 'account-not-connected' | string,
      groupName?, sentAt?, queueCount? }

PATCH /api/setters/team/:id     body { leaveUntil: 'YYYY-MM-DD' | null }   → { setter }
GET   /api/setters/team-performance   → perSetter[] incluye `leaveUntil: 'YYYY-MM-DD' | null`
```

Helpers del panel ya existentes:
```js
window.showToast(message, { type })                                      // app.js:16034 (vigente; la de :3835 es legacy)
window.askConfirm({ title, message, confirmLabel, cancelLabel, danger })  // app.js:16001
apiUrl(path)                                                             // wrapper de fetch con viewAs
escHtml(str)                                                             // app.js:656
loadCommandCenter()                                                      // app.js:9709
_teamRenderTable(d)                                                      // app.js:14932 (assignedBadge en :14969)
_teamLoad()                                                              // app.js:15216
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML del bloque de Comando + modal de licencia + cache-buster</name>

  <read_first>
    - `.planning/phases/21-reporte-diario-canal-whatsapp/21-UI-SPEC.md`
      §"Bloque 1 — Centro de Comando" (el HTML está escrito literal) y
      §"Modal nuevo — #team-leave-modal".
    - `public/index.html:1844-1880` (arranque de `view-command`: `.content-header`
      y la sección `<!-- EQUIPO -->` — el bloque nuevo va EN MEDIO).
    - `public/index.html:2655-2690` (`#team-config-modal` — el chrome del modal
      nuevo se replica de ahí).
    - `public/index.html:1984` (`.admin-variable-panel` en uso real).
    - `public/index.html:14` y `public/index.html:3388` (los dos cache-busters).
  </read_first>

  <action>
**1a.** Insertar en `public/index.html` el `<section id="cmd-daily-report-panel"
class="admin-variable-panel" data-roles="admin" ...>` **exactamente como está
escrito en `21-UI-SPEC.md` §"HTML — estructura exacta"**, ubicado inmediatamente
después del `</div>` del `.content-header` de `view-command` (index.html:1847) y
ANTES del comentario `<!-- EQUIPO: invitaciones ... -->` (index.html:1849).

Ids que son contrato (no renombrar):
`cmd-daily-report-panel`, `cmd-report-status-chip`, `cmd-report-status-detail`,
`cmd-report-group-name`, `cmd-report-last-sent`, `cmd-report-queue-count`,
`cmd-report-desktop-status`, `cmd-report-setup-hint`, `cmd-report-backup-emails`,
`cmd-report-backup-emails-save`, `cmd-report-backup-emails-result`,
`cmd-report-pause-toggle`, `cmd-report-send-now-btn`.

Copy exacto (no parafrasear):
- `<h3>`: `Reporte diario · WhatsApp`
- subtítulo: `Todos los días a las 23:00 se manda solo, al grupo de WhatsApp de los socios. Esta sección es para configurarlo y para probarlo sin esperar a la hora.`
- filas de estado: `Grupo destino:`, `Último envío:`, `En cola:`, `Desktop ahora:`
- label del input: `Mails de respaldo` + el aclarador `(apagado — D-04, no se usa mientras el diario no tenga fallback a email)`
- botón de guardado: `Guardar mails` (clase `btn-table-action`)
- checkbox: `Pausar envío automático (diario y semanal)`
- CTA: `Mandar ahora` con `class="btn btn-primary"`
- hint de setup: `Necesitás elegir el grupo desde la app de escritorio (wa-multi) antes de poder mandar.`

**1b.** Insertar `<div id="team-leave-modal" ...>` **literal del UI-SPEC**
§"Modal nuevo", junto a `#team-config-modal` (después de su cierre, ~index.html:2690).
Ids: `team-leave-modal`, `team-leave-title`, `team-leave-until`,
`team-leave-clear`, `team-leave-cancel`, `team-leave-save`.

Copy exacto:
- subtítulo: `Mientras esté de licencia, sale de la línea de "sin actividad hoy" del reporte y aparece al pie como "de licencia". Vuelve sola al vencer la fecha — no hace falta acordarse de sacarla.`
- label del campo: `De licencia hasta`
- botones: `Quitar licencia` (`btn-table-action` + `color:var(--danger)`),
  `Cancelar` (`class="btn btn-secondary"`), `Guardar licencia`
  (`class="btn btn-secondary"` — deliberadamente NO `.btn-primary`: el acento
  violeta queda reservado a "Mandar ahora", que es la única acción con riesgo real).

**1c. Cache-buster (hard constraint).** En `public/index.html:3388`:
`src="/app.js?v=20260725c"` → `src="/app.js?v=20260726a"`.
**NO** tocar `public/index.html:14` (`style.css?v=20260725a`): esta fase no
agrega ni modifica ninguna regla CSS.
  </action>

  <verify>
    <automated>grep -c 'id="cmd-report-send-now-btn"\|id="cmd-report-status-chip"\|id="cmd-report-pause-toggle"\|id="team-leave-modal"\|id="team-leave-until"\|id="team-leave-save"' public/index.html</automated>
  </verify>

  <acceptance_criteria>
    - Los 17 ids existen: `grep -c 'id="cmd-daily-report-panel"\|id="cmd-report-status-chip"\|id="cmd-report-status-detail"\|id="cmd-report-group-name"\|id="cmd-report-last-sent"\|id="cmd-report-queue-count"\|id="cmd-report-desktop-status"\|id="cmd-report-setup-hint"\|id="cmd-report-backup-emails"\|id="cmd-report-backup-emails-save"\|id="cmd-report-backup-emails-result"\|id="cmd-report-pause-toggle"\|id="cmd-report-send-now-btn"\|id="team-leave-modal"\|id="team-leave-title"\|id="team-leave-until"\|id="team-leave-clear"\|id="team-leave-cancel"\|id="team-leave-save"' public/index.html` = 19.
    - `grep -c "app.js?v=20260725c" public/index.html` = 0.
    - `grep -c "app.js?v=20260726a" public/index.html` = 1.
    - `grep -c "style.css?v=20260725a" public/index.html` = 1 (sin tocar).
    - `grep -c 'class="btn btn-primary"' public/index.html` >= 1 (el CTA lleva la clase base).
    - Copy literal presente: `grep -c "Mandar ahora\|Guardar mails\|Guardar licencia\|Quitar licencia\|Pausar envío automático\|Reporte diario · WhatsApp\|De licencia hasta" public/index.html` >= 7.
    - `git diff --stat public/style.css` vacío (cero clases nuevas).
  </acceptance_criteria>

  <done>El bloque de Comando y el modal de licencia existen en el HTML con los ids y el copy exactos del UI-SPEC, y el cache-buster de app.js ya no es `v=20260725c`.</done>
</task>

<task type="auto">
  <name>Task 2: _cmdLoadReportPanel + máquina de estados de "Mandar ahora" + licencia en Equipo</name>

  <read_first>
    - `.planning/phases/21-reporte-diario-canal-whatsapp/21-UI-SPEC.md`
      §"Precedencia del chip de estado", §"Interacciones",
      §"State machine — botón Mandar ahora", §"Copy exacto por estado",
      §"Decisión de ubicación — D-18".
    - `public/app.js:9709-9760` (`loadCommandCenter` — de acá cuelga el loader nuevo).
    - `public/app.js:12774-12820` (patrón de botón con estado de carga y del bloque
      "Enviar reporte ahora" del semanal — analog directo).
    - `public/app.js:14932-15000` (`_teamRenderTable`: `assignedBadge` en :14969 y
      el `tr.innerHTML` en :14978).
    - `public/app.js:15216-15250` (`_teamLoad` y cómo cuelga `_teamLoadDispoAudit`).
    - `public/app.js:16001-16060` (`askConfirm` y `showToast` vigentes).
  </read_first>

  <action>
**2a. `_cmdLoadReportPanel()`** — colgado de `loadCommandCenter()` como
fire-and-forget (igual que `_teamLoadDispoAudit` cuelga de `_teamLoad`): si falla
NO puede romper el resto del Centro de Comando.

```js
async function _cmdLoadReportPanel() {
  const chip = document.getElementById('cmd-report-status-chip');
  if (!chip) return;
  try {
    const r = await fetch(apiUrl('/api/admin/daily-report/status'));
    if (!r.ok) throw new Error('status ' + r.status);
    const d = await r.json();
    window._cmdReportStatus = d;
    // ... pintar detalle, hint, inputs y chip
  } catch (e) {
    chip.className = 'chip chip-danger';
    chip.textContent = 'Sin datos';
    document.getElementById('cmd-report-status-detail').textContent =
      'No se pudo cargar el estado del canal. Recargá la página; si sigue, el server puede estar caído.';
  }
}
```
Pintado (usar `textContent`, nunca `innerHTML`, para `groupName` — viene de
WhatsApp, T-21-19):
- `#cmd-report-group-name` → `d.groupName || 'Sin configurar'`
- `#cmd-report-last-sent` → `d.lastSent ? new Date(d.lastSent.at).toLocaleString() + ' · ' + d.lastSent.periodLabel + ' · ' + (d.lastSent.status === 'sent' ? 'OK' : 'falló') : '—'`
- `#cmd-report-queue-count` → `String(d.queueCount)`
- `#cmd-report-desktop-status` → `d.desktopOnline ? 'conectada' : 'desconectada'`
- `#cmd-report-setup-hint` → toggle de la clase `hidden` según `!d.groupConfigured`
- `#cmd-report-backup-emails` → `.value = (d.backupEmails || []).join(', ')`
- `#cmd-report-pause-toggle` → `.checked = !!d.paused`
- `#cmd-report-send-now-btn` → `.disabled = !d.groupConfigured`

**Precedencia del chip — determinística, el PRIMERO que matchea gana**
(orden literal del UI-SPEC, no reordenar):
1. `!groupConfigured` → `chip chip-neutral` · `Sin configurar`
2. `!desktopOnline` → `chip chip-danger` · `Desktop desconectado`
3. `paused === true` → `chip chip-warning` · `Pausado`
4. `queueCount > 0` → `chip chip-warning` · `{N} en cola`
5. `lastSent && lastSent.status === 'failed'` → `chip chip-danger` · `Último envío falló`
6. else → `chip chip-success` · `Al día`

**2b. Interacciones del bloque:**
- `#cmd-report-pause-toggle` `onchange` → auto-guarda (patrón del checkbox
  "Activo" de números Telnyx, sin botón aparte): `PUT config { paused }` →
  `showToast('Envío automático pausado.', {type:'warn'})` /
  `showToast('Envío automático reanudado.', {type:'success'})`.
- `#cmd-report-backup-emails-save` `onclick` → `PUT config { backupEmails }`
  (split por coma, trim, filtrar vacíos) → feedback inline en
  `#cmd-report-backup-emails-result` (`Guardado.` o el error del backend).

**2c. Máquina de estados de `#cmd-report-send-now-btn`** — es un envío REAL a un
grupo con personas reales. Estados nombrados, transiciones exactas:

```
IDLE ──click──> CONFIRMING ──cancela──> IDLE
                     │ confirma
                     ▼
                  SENDING ──status:'sent'──────────> SUCCESS ──2.5s──> IDLE
                     ├──status:'sent_via_dm'──────> SUCCESS (con matiz)
                     ├──status:'queued'───────────> QUEUED ──toast──> IDLE
                     ├──status:'failed'───────────> FAILED ──toast──> IDLE
                     └──timeout cliente 60s───────> UNKNOWN ──toast──> IDLE
```
- **IDLE**: `disabled = !groupConfigured`, texto `Mandar ahora`.
- **CONFIRMING**: `window.askConfirm({ title:'Mandar reporte ahora', message:'Esto arma el reporte con los datos de HOY hasta este momento y lo manda YA al grupo de WhatsApp de los socios (o a los 3 por separado si el grupo no aparece). No se puede deshacer. ¿Confirmás?', confirmLabel:'Sí, mandar ahora', cancelLabel:'Cancelar', danger:true })`.
  Cancelar/Esc → vuelve a IDLE sin disparar ningún request.
- **SENDING**: `btn.disabled = true`, `btn.innerHTML = '<span class="loader"></span> Enviando…'`,
  `POST` con timeout de cliente de **60s** (`Promise.race` contra un `setTimeout`).
  60s alcanza porque el backend responde a lo sumo a los 25s
  (`REPORT_SEND_NOW_WAIT_MS`, plan 21-03): el cliente nunca se rinde antes que el
  server. El tipeo largo NO deja la conexión colgada — el server devuelve
  `queued/sending` y el panel refresca el estado.
- **SUCCESS** (`status:'sent'`): `showToast('Reporte enviado al grupo.', {type:'success'})`,
  botón con texto temporal `Enviado` 2.5s, después IDLE.
- **SUCCESS con matiz** (`status:'sent_via_dm'`):
  `showToast('Grupo no encontrado — se mandó por WhatsApp a los 3 socios por separado.', {type:'warn'})`.
- **QUEUED** (`status:'queued'`) — el toast depende de `reason`:
  - `offline` (o sin reason): `'La computadora con WhatsApp está apagada ahora. El reporte quedó en cola — sale solo apenas reconecte.'`
  - `sin_grupo`: `'Todavía no se eligió un grupo desde wa-multi. Abrí la app de escritorio, iniciá sesión con el número dedicado y elegí el grupo de socios de la lista.'`
  - `busy`: `'Ya hay un envío en curso. Esperá a que termine.'`
  - `sending`: `'Se está enviando: el mensaje se tipea como una persona y puede tardar un minuto. Mirá el grupo.'`
  - `fallback_dm`: `'Grupo no encontrado — se está mandando a los 3 socios por separado.'`
  Botón vuelve a IDLE en el acto (no fue un éxito: no lleva el label temporal).
- **FAILED** (`status:'failed'`): `reason === 'account-not-connected'` →
  `'El número dedicado no está conectado a WhatsApp — hay que escanear el QR de nuevo.'`;
  cualquier otro → `'No se pudo enviar. Probá de nuevo en un momento.'` (`type:'error'`).
- **UNKNOWN** (timeout del cliente): `'No llegó confirmación a tiempo. Fijate en el grupo antes de mandar de nuevo — puede que se haya enviado igual.'` (`type:'warn'`).
- **Después de CUALQUIER estado terminal**: volver a llamar `_cmdLoadReportPanel()`
  para que el chip, `#cmd-report-last-sent` y `#cmd-report-queue-count` reflejen el
  estado real del server, nunca solo la actualización optimista del cliente.

Los 3 `reason` nuevos (`sin_grupo`, `busy`, `sending`, `fallback_dm`) son un
agregado ADITIVO al UI-SPEC: no eliminan ningún estado, solo hacen que el toast
de `queued` diga la verdad en vez del texto genérico de "computadora apagada".
Anotarlo en el SUMMARY.

**2d. Licencia en Equipo (D-18).** En `_teamRenderTable` (app.js:14969-14984),
después de `assignedBadge`, agregar el badge + botón **literal del UI-SPEC**
§"HTML — cambios en `_teamRenderTable`" e insertarlos en el `tr.innerHTML` como
`${alertBadge}${assignedBadge}${leaveBadge}${leaveEditBtn}`.
- badge: `Licencia hasta DD/MM` (`padding:2px 6px`, réplica exacta de `assignedBadge`)
- botón: `+ Licencia` / `Editar licencia` con `class="btn-table-action btn-sm"`,
  **solo si `currentUser.role === 'admin'`** (condicionado en el template string:
  `data-roles` no aplica a HTML inyectado).

`window._teamOpenLeaveModal(setterId, name, currentLeaveUntil)`: título
`Licencia — {name}`, popula `#team-leave-until`, muestra/oculta
`#team-leave-clear` según haya o no fecha, abre el modal.
- `#team-leave-save` → `PATCH apiUrl('/api/setters/team/' + id)` con
  `{ leaveUntil: <valor del input> }` → cierra, `showToast('Licencia guardada.', {type:'success'})`, `_teamLoad()`.
- `#team-leave-clear` → mismo PATCH con `{ leaveUntil: null }` (sin `askConfirm`:
  acción reversible y de bajo impacto) → `showToast('Licencia quitada.', {type:'success'})`, `_teamLoad()`.
- `#team-leave-cancel` / click en el overlay / Esc → cierra sin guardar (mismo
  patrón que `#team-config-modal`).

**2e.** Colgar `_cmdLoadReportPanel()` al final de `loadCommandCenter()` sin
`await` bloqueante (`_cmdLoadReportPanel();` con `.catch(() => {})`), y exponer
`window._teamOpenLeaveModal`.
  </action>

  <verify>
    <automated>node --check public/app.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale 0.
    - `grep -c "_cmdLoadReportPanel" public/app.js` >= 3 (definición + llamada desde loadCommandCenter + refresco post-envío).
    - `grep -c "daily-report/status\|daily-report/config\|daily-report/send-now" public/app.js` >= 3.
    - `grep -c "window._teamOpenLeaveModal" public/app.js` >= 1 y `grep -c "leaveUntil" public/app.js` >= 3.
    - Chip determinístico: `grep -c "Sin configurar\|Desktop desconectado\|Pausado\|en cola\|Último envío falló\|Al día" public/app.js` >= 6.
    - Copy del confirm literal: `grep -c "Sí, mandar ahora" public/app.js` = 1 y `grep -c "Mandar reporte ahora" public/app.js` = 1.
    - Toasts literales: `grep -c "Reporte enviado al grupo.\|Envío automático pausado.\|Envío automático reanudado.\|Licencia guardada.\|Licencia quitada." public/app.js` >= 5.
    - Guard de confirmación presente: `grep -c "askConfirm" public/app.js` >= 1 dentro del handler de `cmd-report-send-now-btn`.
    - Sin `fetch(` crudo en el código nuevo: todas las llamadas nuevas usan `apiUrl(`.
    - `groupName` se pinta con `textContent` (no `innerHTML`): `grep -c "cmd-report-group-name').innerHTML" public/app.js` = 0.
  </acceptance_criteria>

  <done>El panel carga el estado del canal, pinta el chip por precedencia, guarda pausa y mails, dispara "Mandar ahora" con confirmación y feedback por estado, y Equipo permite poner/quitar licencia con fecha.</done>
</task>

<task type="auto">
  <name>Task 3: verificación en preview (server local, sin tocar data/)</name>

  <read_first>
    - `CLAUDE.md` nota #15 (preview server local: `.claude/launch.json` con
      `env.DATA_DIR=tmp/preview-data`; **NUNCA escribir a `./data/auth.json`**).
    - `CLAUDE.md` nota #114 (gotcha del preview: si el tab deja de pintar frames,
      `getComputedStyle` devuelve valores imposibles — verificar con un tab fresco).
  </read_first>

  <action>
Levantar el preview con `DATA_DIR=tmp/preview-data`, loguear como admin y
verificar en el DOM (inspección por JS, que es lo que funciona de forma
confiable en este entorno — ver nota #124):

Checklist (a-f), documentar PASS/FAIL de cada uno en el SUMMARY:
- **a)** `view-command` muestra `#cmd-daily-report-panel` arriba de todo, y el
  chip dice `Sin configurar` con `class="chip chip-neutral"` (estado inicial: no
  hay transporte configurado todavía porque el desktop no eligió grupo).
- **b)** `#cmd-report-setup-hint` está visible (sin la clase `hidden`) y
  `#cmd-report-send-now-btn.disabled === true` con `groupConfigured=false`.
- **c)** Tildar `#cmd-report-pause-toggle` dispara el `PUT` y el toast
  `Envío automático pausado.`; recargar Comando y el checkbox sigue tildado
  (persistió en `reports.json` de `tmp/preview-data`).
- **d)** Guardar `a@b.com, c@d.com` en `#cmd-report-backup-emails` deja
  `Guardado.` en `#cmd-report-backup-emails-result` y el `GET status` devuelve
  los 2 mails.
- **e)** En `view-team`, una fila de SDR muestra el botón `+ Licencia`; el modal
  abre con el título `Licencia — {nombre}`, guardar una fecha futura repinta la
  fila con el badge `Licencia hasta DD/MM`, y `Quitar licencia` lo saca.
- **f)** Logueado como **supervisor**, `#cmd-daily-report-panel` NO aparece
  (`data-roles="admin"`) y en `view-team` NO aparece el botón de licencia
  (condicional por `currentUser.role`), pero SÍ el badge si la SDR está de licencia.

Si algún punto falla, arreglarlo en esta misma task antes de cerrar el plan.
**No** escribir en `./data/` en ningún momento.
  </action>

  <verify>
    <automated>node --check public/app.js && grep -c "app.js?v=20260726a" public/index.html</automated>
  </verify>

  <acceptance_criteria>
    - Los 6 puntos a-f documentados con PASS en el SUMMARY (o arreglados y re-verificados).
    - `git status --porcelain data/` vacío (el preview no tocó la data del repo).
    - `git diff --stat` muestra SOLO `public/index.html` y `public/app.js`.
  </acceptance_criteria>

  <done>Los dos bloques funcionan en el preview con un admin real y quedan correctamente ocultos para un supervisor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos que vienen de WhatsApp (nombre del grupo) → DOM del panel | string controlado fuera del sistema, elegido en el desktop |
| click del admin → envío saliente real | un click manda un mensaje a 3 personas reales, sin deshacer |
| rol supervisor → bloque de config | `view-team` la ve un supervisor scoped; `view-command` es admin-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-19 | Tampering (XSS) | `#cmd-report-group-name` | mitigate | el nombre del grupo se pinta con `textContent`, nunca con `innerHTML`; en `_teamRenderTable` el nombre de la SDR pasa por `escHtml` como ya hace `assignedBadge` |
| T-21-20 | Denial of service | `#cmd-report-send-now-btn` | mitigate | `askConfirm` obligatorio + `disabled` durante `SENDING` + refetch de estado al terminar. La defensa REAL es el lock server-side (`status:'queued', reason:'busy'`, plan 21-03): el backend no confía en la UI |
| T-21-21 | Elevation of privilege | bloque de config | mitigate | `data-roles="admin"` en el HTML estático + los endpoints son admin-only en el backend; el botón de licencia se condiciona por `currentUser.role === 'admin'` porque `data-roles` no aplica a HTML inyectado por JS |
| T-21-22 | Information disclosure | estado del canal | accept | el panel muestra nombre del grupo y último envío, no el contenido del reporte ni el JID entero (`jidCaptured` es booleano). Es admin-only |
</threat_model>

<verification>
- `node --check public/app.js` sale 0.
- `grep -c "app.js?v=20260725c" public/index.html` = 0 y `grep -c "app.js?v=20260726a" public/index.html` = 1.
- `git diff --stat public/style.css` vacío.
- Checklist a-f del preview: 6/6 PASS documentado en el SUMMARY.
- `npx vitest run` sigue verde (este plan no toca backend, pero se corre igual antes de cerrar).
</verification>

<success_criteria>
- El admin puede probar el canal sin esperar a las 23:00 (D-29 — es lo que habilita el plan 21-07).
- El bloque respeta el UI-SPEC al pie de la letra: ids, copy, clases reusadas, precedencia del chip.
- Cache-buster bumpeado (la regla que más se olvida en este repo).
</success_criteria>

<output>
Al terminar, crear `.planning/phases/21-reporte-diario-canal-whatsapp/21-04-SUMMARY.md`.
</output>
