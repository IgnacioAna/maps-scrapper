---
phase: 21-reporte-diario-canal-whatsapp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.js
  - tests/daily-report.test.js
autonomous: true
requirements: [REP-04, REP-05, REP-09, REP-10]

must_haves:
  truths:
    - "Existe un builder que devuelve los datos del reporte de UN día en TZ de negocio, derivados del CALL METRICS CORE"
    - "El texto plano generado arranca con la excepción (quién no trabajó hoy), antes del título"
    - "El texto no contiene ninguna línea/segmento en cero (ni '0 min', ni 'Ayer 0 llam', ni nombres con 0 llamadas)"
    - "Ignacio y Paula (ADMIN_ONLY_SETTER_IDS) no aparecen en ninguna parte del reporte"
    - "El sesgo del canal manual y las llamadas sin atribuir quedan anotados en el propio texto cuando corresponde"
    - "Las llamadas discadas sin marcar salen por nombre y NO suman al conteo de llamadas"
    - "N días de reportes se pueden renderizar como UN solo mensaje con una línea por día"
  artifacts:
    - path: "index.js"
      provides: "buildDailyReportData / buildDailyReportText / buildDailyReportLine / buildConsolidatedReportText + globalThis.__dailyReport"
      contains: "globalThis.__dailyReport"
    - path: "tests/daily-report.test.js"
      provides: "regresión del builder diario y del molde D-19"
      min_lines: 200
  key_links:
    - from: "buildDailyReportData"
      to: "globalThis.__callCore"
      via: "_ccResolveRange / _ccCollectCalls / _ccFunnelAggregate"
      pattern: "_ccFunnelAggregate\\("
    - from: "buildDailyReportData"
      to: "_SUPERVISOR_EXCLUSION_SET"
      via: "visibleSet + _filterSettersVisible"
      pattern: "_filterSettersVisible"
    - from: "buildDailyReportData"
      to: "pending_calls.json"
      via: "loadPendingCalls() para D-23"
      pattern: "loadPendingCalls\\("
---

<objective>
Construir el builder de datos del reporte DIARIO y los builders de texto plano
para WhatsApp, exactamente con el molde que el user validó (D-19), más el
renderizado consolidado que la cola va a usar cuando haya días pendientes.

Purpose: es la pieza de contenido de toda la fase. Todo lo demás (cola,
transporte, panel, desktop) mueve el texto que sale de acá.
Output: `buildDailyReportData`, `buildDailyReportText`, `buildDailyReportLine`,
`buildConsolidatedReportText`, helpers de licencia/escalada, y
`globalThis.__dailyReport` como superficie de test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-reporte-diario-canal-whatsapp/21-CONTEXT.md
@.planning/phases/21-reporte-diario-canal-whatsapp/21-PATTERNS.md
@CLAUDE.md

Reglas del proyecto que aplican y NO son negociables:
- #157 CALL METRICS CORE: toda métrica de llamadas deriva de
  `_ccCollectCalls` / `_ccFunnelAggregate` / `_ccResolveRange`. Jamás
  re-implementar el funnel inline. `tests/metrics-consistency.test.js` es la
  garantía y tiene que seguir verde.
- #113 TZ de negocio: todo corte de día pasa por `_biz*`. `setHours(0,0,0,0)` y
  `toISOString().slice(0,10)` son BUGS en este repo.
- #144 `ADMIN_ONLY_SETTER_IDS` + `_filterSettersVisible` (REP-09) — no escribir
  un filtro nuevo.
- #121 tests: `process.env.X_API_KEY = ""`, NUNCA `delete`.
- Backend puro: este plan NO toca `public/` → NO se bumpea cache-buster.

<interfaces>
<!-- Contratos ya existentes en index.js que el executor usa tal cual. -->

index.js:5701 — entries pre-atribuidas (NO exponen `channel`; para el
complemento manual hay que hacer una segunda pasada con `{channel:'telnyx_webrtc'}`):
```js
function _ccCollectCalls(data, { setterId = '', visibleSet = null, channel = '' } = {})
// → [{ ts, outcome, duration, setterId, leadId, disqualifyReason }]
```

index.js:5730 — funnel canónico:
```js
function _ccFunnelAggregate(calls, calendar, fromTs, toTs, { setterId = '', visibleSet = null } = {})
// → { dials, connects, conversations, appointments, deals, revenue,
//     totalDurationS, byReason, avgConvDurationS,
//     rates: { connectRate, conversationRate, bookingRate, closeRate, dialToAppointment } }
```

index.js:5790 — rango canónico (`from`/`to` YYYY-MM-DD inclusivo gana sobre period):
```js
function _ccResolveRange(period, { from, to } = {})
// → { period, fromTs, toTs }
```

index.js:5596-5604 — TZ de negocio:
```js
_bizStartOfDay(ts) → ms; _bizDayStr(ts) → 'YYYY-MM-DD';
_bizHour(ts) → 0-23; _bizDayOfWeek(ts) → 0=domingo..6=sábado
```

index.js:5649-5673 — alcance de vendedoras:
```js
const ADMIN_ONLY_SETTER_IDS = new Set(['setter_ignacio', 'setter_paula_kroff']);
const _SUPERVISOR_EXCLUSION_SET = { has: (id) => !ADMIN_ONLY_SETTER_IDS.has(id) };
function _filterSettersVisible(setters, visibleSet)
```

index.js:5566-5570 — outcomes canónicos:
```js
const COLD_CALL_CONV_MIN_S = 30;
const COLD_CALL_CONNECT_OUTCOMES = new Set(['answered_interested','answered_not_interested','scheduled_with_admin','callback_later','hung_up']);
const COLD_CALL_APPOINTMENT_OUTCOMES = new Set(['scheduled_with_admin']);
```

index.js:1771 — llamadas discadas sin marcar (fuente de D-23):
```js
function loadPendingCalls() // → { pending: [ { id, leadId, leadName, setterId, userId, startedAt, endedAt, durationSecs, ... } ] }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: buildDailyReportData + helpers de licencia/escalada + `now` inyectable en _ccResolveRange</name>

  <read_first>
    - `index.js:1740-1945` (bloque completo del reporte semanal: `getReportsFile`,
      `loadReportsState`, `loadPendingCalls`, `buildWeeklyReportData`,
      `buildWeeklyReportHtml`, `_reportRecipients`, `maybeRunWeeklyReportCron`,
      `globalThis.__weeklyReport`) — **`buildWeeklyReportData` (1793-1843) es el
      analog exacto a replicar**.
    - `index.js:5560-5680` (constantes de outcomes, helpers `_biz*`,
      `_buildUserSetterMap`, `_callSetterId`, `ADMIN_ONLY_SETTER_IDS`,
      `_SUPERVISOR_EXCLUSION_SET`, `_filterSettersVisible`).
    - `index.js:5698-5835` (CALL METRICS CORE completo, incluido `_ccResolveRange`
      y `globalThis.__callCore`).
    - `index.js:9112-9126` (cómo `disposition-audit` cuenta pendientes por setter
      dentro de un rango — patrón exacto a copiar para D-23).
    - `index.js:5461-5470` (`PATCH /api/setters/team/:id` — dónde se agrega `leaveUntil`).
    - `index.js:10097-10165` (`perSetter` de `team-performance` — dónde se agrega
      el campo `leaveUntil` que el panel necesita para pintar el badge).
  </read_first>

  <action>
**1a. `_ccResolveRange` acepta `now` inyectable (2 líneas, aditivo, retro-compatible).**

En `index.js:5790` cambiar la firma y la primera línea:
```js
// `now` inyectable (default Date.now()) para que los builders de reportes se
// puedan testear sin reloj real, SIN duplicar la semántica de rango fuera del
// CORE (regla #157).
function _ccResolveRange(period, { from, to, now: nowIn } = {}) {
  const now = nowIn || Date.now();
```
El resto del cuerpo queda idéntico. Ningún call site existente pasa `now` → cero
cambio de comportamiento.

**1b. Helpers nuevos** — insertar INMEDIATAMENTE DESPUÉS de
`globalThis.__weeklyReport = {...}` (index.js:1940), en un bloque nuevo
`// ── Phase 21: reporte diario ──`:

```js
// Días HÁBILES (lun-vie, TZ negocio) transcurridos desde `lastTs` hasta el día
// de `nowTs`, sin contar el día de la última llamada ni el día de hoy.
// D-16: a los 5 hábiles seguidos sin llamar, la vendedora ESCALA.
function _reportWeekdaysSince(lastTs, nowTs) {
  if (!lastTs) return 0;
  const oneDay = 86400000;
  let cur = _bizStartOfDay(lastTs) + oneDay;
  const today = _bizStartOfDay(nowTs);
  let n = 0;
  while (cur < today && n < 400) {
    const dow = _bizDayOfWeek(cur);
    if (dow >= 1 && dow <= 5) n++;
    cur += oneDay;
  }
  return n;
}
// D-18: ¿está de licencia HOY? `leaveUntil` es 'YYYY-MM-DD' inclusive; al vencer
// vuelve sola (comparación de strings de día en TZ de negocio, sin timers).
function _reportOnLeave(setter, nowTs) {
  const until = String(setter?.leaveUntil || '').slice(0, 10);
  if (!/^\d{4}-\d{2}-\d{2}$/.test(until)) return false;
  return _bizDayStr(nowTs) <= until;
}
// Nombre seguro para un mensaje de texto plano: sin saltos de línea (romperían
// la estructura del mensaje) y acotado. T-21-01.
function _reportSafeName(name) {
  return String(name || '').replace(/[\r\n]+/g, ' ').trim().slice(0, 40);
}
// 'mié 22/07' en TZ de negocio, sin punto final (Intl mete '.' en es-AR).
function _reportDayLabel(ts) {
  const d = new Date(ts + _bizOffsetMs(ts));
  const dias = ['dom', 'lun', 'mar', 'mié', 'jue', 'vie', 'sáb'];
  const dd = String(d.getUTCDate()).padStart(2, '0');
  const mm = String(d.getUTCMonth() + 1).padStart(2, '0');
  return `${dias[d.getUTCDay()]} ${dd}/${mm}`;
}
```

**1c. `buildDailyReportData(nowTs = Date.now(), dayTs = nowTs)`** — a continuación
de los helpers. Replica la forma de `buildWeeklyReportData` (index.js:1793) con
las diferencias de D-10..D-25:

```js
function buildDailyReportData(nowTs = Date.now(), dayTs = nowTs) {
  const settersData = loadSettersData();
  const visibleSet = _SUPERVISOR_EXCLUSION_SET;              // REP-09
  const calendar = (settersData.calendar || []).filter(e => !ADMIN_ONLY_SETTER_IDS.has(e.setterId));
  const dayStr = _bizDayStr(dayTs);
  // Rango del CORE: from/to iguales = ese día completo, capado a `now`
  // (hoy → [medianoche, ahora]; día pasado → el día entero).
  const { fromTs, toTs } = _ccResolveRange('custom', { from: dayStr, to: dayStr, now: nowTs });
  const prevStr = _bizDayStr(fromTs - 86400000);
  const prev = _ccResolveRange('custom', { from: prevStr, to: prevStr, now: nowTs });

  const allCalls = _ccCollectCalls(settersData, { visibleSet });
  const telnyxCalls = _ccCollectCalls(settersData, { visibleSet, channel: 'telnyx_webrtc' });
  const dayCalls = allCalls.filter(c => c.ts >= fromTs && c.ts < toTs);
  const agg = _ccFunnelAggregate(allCalls, calendar, fromTs, toTs, { visibleSet });
  const aggPrev = _ccFunnelAggregate(allCalls, calendar, prev.fromTs, prev.toTs, { visibleSet });

  // Vendedoras en alcance: visibles (REP-09) y NO ocultas (setter.hidden = fuera
  // del sistema operativo; si no, un setter oculto sin llamadas quedaría para
  // siempre en la línea "Sin arrancar").
  const setters = _filterSettersVisible(settersData.setters || [], visibleSet)
    .filter(s => s.hidden !== true);

  const perSetter = [];
  const neverStarted = [];
  const idleToday = [];
  const escalated = [];
  const onLeave = [];
  const interested = [];
  for (const s of setters) {
    const name = _reportSafeName(s.name);
    const mine = dayCalls.filter(c => c.setterId === s.id);
    const a = _ccFunnelAggregate(mine, [], fromTs, toTs);
    const int = mine.filter(c => c.outcome === 'answered_interested').length;
    if (a.dials > 0) {
      perSetter.push({ id: s.id, name, dials: a.dials, connects: a.connects, minutes: Math.round(a.totalDurationS / 60) });
      if (int > 0) interested.push({ name, count: int });      // D-21
      continue;
    }
    const everTs = allCalls.reduce((mx, c) => (c.setterId === s.id && c.ts > mx ? c.ts : mx), 0);
    if (!everTs) { neverStarted.push(name); continue; }         // D-15
    if (_reportOnLeave(s, nowTs)) { onLeave.push(name); continue; }  // D-18
    const wd = _reportWeekdaysSince(everTs, nowTs);
    if (wd >= 5) escalated.push({ name, days: Math.floor((_bizStartOfDay(nowTs) - _bizStartOfDay(everTs)) / 86400000) }); // D-16 + D-25
    else idleToday.push(name);                                  // D-14
  }
  perSetter.sort((a, b) => b.dials - a.dials);

  // D-22 — sesgo del canal manual: llamada sin minutos. Complemento del CORE.
  const manualCalls = dayCalls.length - telnyxCalls.filter(c => c.ts >= fromTs && c.ts < toTs).length;
  const manualFlag = manualCalls >= 5 || (agg.dials > 0 && manualCalls / agg.dials > 0.10);
  // REP-10 — llamadas de users borrados: suman al total del equipo pero no caen
  // en ninguna fila (_callSetterId → '', el pseudo-set las deja pasar).
  const unattributed = dayCalls.filter(c => !c.setterId).length;

  // D-23 — discadas sin marcar HOY, por nombre. NO se suman a dials (regla
  // transversal del milestone: una sola forma de contar llamadas).
  const nameById = new Map(setters.map(s => [s.id, _reportSafeName(s.name)]));
  const unmarkedBy = new Map();
  try {
    for (const p of (loadPendingCalls().pending || [])) {
      const ts = Date.parse(p.startedAt || '') || 0;
      if (!ts || ts < fromTs || ts >= toTs) continue;
      if (!nameById.has(p.setterId)) continue;
      unmarkedBy.set(p.setterId, (unmarkedBy.get(p.setterId) || 0) + 1);
    }
  } catch {}
  const unmarked = [...unmarkedBy.entries()]
    .map(([sid, count]) => ({ name: nameById.get(sid), count }))
    .sort((a, b) => b.count - a.count);

  return {
    dayStr, dayLabel: _reportDayLabel(fromTs), period: { fromTs, toTs },
    team: { dials: agg.dials, connects: agg.connects, connectRate: agg.rates.connectRate, minutes: Math.round(agg.totalDurationS / 60) },
    yesterday: { dials: aggPrev.dials, connects: aggPrev.connects, connectRate: aggPrev.rates.connectRate },
    perSetter, neverStarted, idleToday, escalated, onLeave, interested,
    manualCalls, manualFlag, unattributed, unmarked,
  };
}
```

**1d. Campo `leaveUntil` en el backend de setters** (lo escribe el panel en el
plan 21-04; el builder ya lo lee):

- `index.js:5461` `PATCH /api/setters/team/:id`, agregar después de la línea de
  `hidden`:
```js
  // D-18: licencia con fecha de vencimiento ('YYYY-MM-DD' o null para quitarla).
  // NO se reusa `hidden` a propósito: hidden no vence y olvidarse de revertirlo
  // borra a una persona del reporte para siempre.
  if (req.body.leaveUntil !== undefined) {
    const v = String(req.body.leaveUntil || '').slice(0, 10);
    if (req.body.leaveUntil === null || v === '') setter.leaveUntil = null;
    else if (/^\d{4}-\d{2}-\d{2}$/.test(v)) setter.leaveUntil = v;
    else return res.status(400).json({ error: 'leaveUntil debe ser YYYY-MM-DD o null.' });
  }
```
- `index.js:10152` (`return {` del `perSetter` de `team-performance`), agregar la
  clave `leaveUntil: s.leaveUntil || null,` después de `name: s.name,`.

**1e. Superficie de test**, al final del bloque nuevo:
```js
globalThis.__dailyReport = { buildDailyReportData, _reportWeekdaysSince, _reportOnLeave, _reportDayLabel, _reportSafeName };
```
(en la Task 2 se le agregan los builders de texto).

⚠️ **TDZ:** el bloque vive en la línea ~1940 pero usa consts definidas en la
~5560 (`ADMIN_ONLY_SETTER_IDS`, `COLD_CALL_*`). Es seguro porque solo se evalúan
al LLAMAR la función (igual que `buildWeeklyReportData` hoy). **Nunca invocar
`buildDailyReportData` en el top level del módulo.**
  </action>

  <verify>
    <automated>node --check index.js && npx vitest run tests/metrics-consistency.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` sale 0.
    - `grep -c "now: nowIn" index.js` = 1.
    - `grep -c "function buildDailyReportData(" index.js` = 1.
    - `grep -c "globalThis.__dailyReport" index.js` >= 1.
    - `grep -c "_reportWeekdaysSince\|_reportOnLeave\|_reportDayLabel\|_reportSafeName" index.js` >= 8.
    - En el cuerpo de `buildDailyReportData`: aparece `_ccResolveRange(`, `_ccCollectCalls(`,
      `_ccFunnelAggregate(`, `_filterSettersVisible(`, `_SUPERVISOR_EXCLUSION_SET`, `loadPendingCalls(`.
    - `grep -n "setHours(0, *0, *0, *0)" index.js` NO devuelve ninguna línea dentro del bloque nuevo.
    - `grep -c "leaveUntil" index.js` >= 4 (PATCH + team-performance + helper + builder).
    - `npx vitest run tests/metrics-consistency.test.js` sale 0.
    - `npx vitest run tests/team-performance.test.js tests/performance.test.js` sale 0.
  </acceptance_criteria>

  <done>El builder devuelve el objeto completo del día pedido, derivado del CALL METRICS CORE, con Ignacio/Paula y setters ocultos fuera, y la suite de consistencia de métricas sigue verde.</done>
</task>

<task type="auto">
  <name>Task 2: buildDailyReportText + buildDailyReportLine + buildConsolidatedReportText (molde D-19)</name>

  <read_first>
    - `index.js:1845-1851` (`buildWeeklyReportHtml` — el analog de ROL: misma
      firma `(data) => string`, mismo punto en el flujo; NO copiar el HTML).
    - El bloque `// ── Phase 21: reporte diario ──` recién creado en la Task 1.
    - `.planning/phases/21-reporte-diario-canal-whatsapp/21-CONTEXT.md` §D-19,
      D-21, D-22, D-23, D-05, D-26 (el molde es LITERAL, no se re-inventa).
  </read_first>

  <action>
Agregar al bloque `// ── Phase 21: reporte diario ──`, después de
`buildDailyReportData`. **El molde es el que el user validó con datos reales — se
escribe literal, no se reinterpreta.** Formato de WhatsApp: `*negrita*`,
`_cursiva_`, cero emojis (preferencia estable del user).

Molde objetivo (D-19), con datos del mié 22/07 en que solo llamó Brenda:
```
*Sin actividad hoy: Judith, Teresa*
Reporte diario · mié 22/07

*Brenda* 13 llam · 8 at · 23 min

_Equipo 13 llam · 8 at (62%) · 23 min_
_Ayer 66 llam · 23 at (35%)_
_Sin arrancar: Dalia, Adela, Melissa_
```

```js
// D-19: el molde exacto validado por el user con datos reales de producción.
// El orden NO es estético: el preview de la notificación de WhatsApp muestra las
// primeras líneas, así que lo primero que se lee SIN abrir el mensaje es quién
// no trabajó. Cada línea del pie se arma como `string | ''` y se filtra —
// "nada de métricas en cero" (regla transversal del milestone v2.0).
function buildDailyReportText(data, { gapNote = '' } = {}) {
  const d = data;
  const head = d.team.dials === 0
    ? '*Hoy no llamó nadie*'                                          // D-11
    : (d.idleToday.length
        ? `*Sin actividad hoy: ${d.idleToday.join(', ')}*`            // D-14
        : '*Todas trabajaron hoy*');
  const rows = d.perSetter.map(s => {
    const segs = [`${s.dials} llam`, `${s.connects} at`];
    if (s.minutes > 0) segs.push(`${s.minutes} min`);                 // sin "0 min"
    return `*${s.name}* ${segs.join(' · ')}`;
  });
  const teamSegs = [`${d.team.dials} llam`, `${d.team.connects} at (${d.team.connectRate}%)`];
  if (d.team.minutes > 0) teamSegs.push(`${d.team.minutes} min`);
  const foot = [
    d.team.dials > 0 ? `_Equipo ${teamSegs.join(' · ')}_` : '',
    // D-19 + discreción: la comparación solo aparece si AYER hubo llamadas
    // (el primer día no hay ayer; el lunes no compara contra el sábado en cero).
    d.yesterday.dials > 0 ? `_Ayer ${d.yesterday.dials} llam · ${d.yesterday.connects} at (${d.yesterday.connectRate}%)_` : '',
    d.interested.length ? `_Interesados: ${d.interested.map(i => `${i.name} ${i.count}`).join(', ')}_` : '',     // D-21
    d.unmarked.length ? `_Sin marcar hoy: ${d.unmarked.map(u => `${u.name} ${u.count}`).join(', ')}_` : '',      // D-23
    d.manualFlag ? `_${d.manualCalls} llamadas cargadas a mano — sin minutos_` : '',                             // D-22
    d.unattributed > 0 ? `_${d.unattributed} llamadas sin atribuir_` : '',                                       // REP-10
    ...d.escalated.map(e => `_${e.name}: ${e.days} días sin llamar_`),                                           // D-16
    ...d.onLeave.map(n => `_${n}: de licencia_`),                                                                // D-18
    d.neverStarted.length ? `_Sin arrancar: ${d.neverStarted.join(', ')}_` : '',                                 // D-15
  ].filter(Boolean);
  const body = [head, `Reporte diario · ${d.dayLabel}`];
  if (rows.length) body.push('', ...rows);
  if (foot.length) body.push('', ...foot);
  const text = body.join('\n');
  // D-05: el próximo mensaje que SÍ sale confiesa los baches, arriba de todo.
  return gapNote ? `${gapNote}\n\n${text}` : text;
}

// Una línea por día para el mensaje consolidado (D-26). La cola la guarda junto
// al texto al encolar, así consolidar no depende de recomputar días viejos.
function buildDailyReportLine(data) {
  const d = data;
  if (d.team.dials === 0) return `*${d.dayLabel}* sin llamadas`;
  const segs = [`${d.team.dials} llam`, `${d.team.connects} at`];
  if (d.team.minutes > 0) segs.push(`${d.team.minutes} min`);
  const tail = d.idleToday.length ? ` · sin actividad: ${d.idleToday.join(', ')}` : '';
  return `*${d.dayLabel}* ${segs.join(' · ')}${tail}`;
}

// D-26: con la desktop apagada N días, al reconectar sale UN mensaje con una
// línea por día — no N mensajes.
function buildConsolidatedReportText(lines, { gapNote = '', neverStarted = [] } = {}) {
  const body = [`*Reporte acumulado · ${lines.length} días*`, '', ...lines];
  if (neverStarted.length) body.push('', `_Sin arrancar: ${neverStarted.join(', ')}_`);
  const text = body.join('\n');
  return gapNote ? `${gapNote}\n\n${text}` : text;
}
```

Actualizar la superficie de test:
```js
globalThis.__dailyReport = { buildDailyReportData, buildDailyReportText, buildDailyReportLine, buildConsolidatedReportText, _reportWeekdaysSince, _reportOnLeave, _reportDayLabel, _reportSafeName };
```

**Regla de mantenimiento a dejar como comentario arriba de `buildDailyReportText`:**
cambiar la redacción o el orden del mensaje = editar ESTA función, nada más. Que
el molde siga siendo PRELIMINAR hasta que el user lo lea en su celular (REP-05)
es exactamente la razón por la que el texto no se concatena en ningún otro lado.
  </action>

  <verify>
    <automated>node --check index.js && node -e "process.env.NODE_ENV='test';" && npx vitest run tests/metrics-consistency.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` sale 0.
    - `grep -c "function buildDailyReportText(\|function buildDailyReportLine(\|function buildConsolidatedReportText(" index.js` = 3.
    - `grep -c "Reporte diario · " index.js` >= 1 y `grep -c "Sin actividad hoy: " index.js` >= 1.
    - `grep -c "Todas trabajaron hoy\|Hoy no llamó nadie\|Sin arrancar: \|de licencia\|días sin llamar\|Sin marcar hoy: \|cargadas a mano\|sin atribuir\|Reporte acumulado" index.js` >= 9.
    - `grep -c "0 min" index.js` = 0 (los minutos en cero se omiten, no se imprimen).
    - `grep -n "globalThis.__dailyReport" index.js` muestra las 4 funciones de texto exportadas.
    - Ninguna de las 3 funciones nuevas llama a `loadSettersData`/`loadPendingCalls`
      (reciben `data`, no leen disco): `grep -A40 "function buildDailyReportText(" index.js | grep -c "load"` = 0.
  </acceptance_criteria>

  <done>Con un objeto del builder, `buildDailyReportText` produce exactamente el molde de D-19 (excepción primero, título después, pie en cursiva sin ninguna métrica en cero), y `buildConsolidatedReportText` produce un solo mensaje con una línea por día.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/daily-report.test.js</name>

  <read_first>
    - `tests/weekly-report.test.js` (completo — el patrón de setup, fixtures y
      fechas determinísticas se copia literal).
    - `tests/metrics-consistency.test.js:1-90` (setup del CORE en tests).
    - El bloque `// ── Phase 21: reporte diario ──` de index.js (Tasks 1 y 2).
  </read_first>

  <behavior>
    - Rango: `buildDailyReportData(nowTs)` cubre [medianoche TZ negocio, nowTs]; a las 23:30 AR del 22/07 NO incluye llamadas del 23/07 UTC.
    - REP-09: llamadas de `setter_ignacio` / `setter_paula_kroff` no aparecen ni en `team` ni en `perSetter`.
    - Setter con `hidden: true` no aparece en ninguna lista (ni en `neverStarted`).
    - D-15: setter sin ninguna llamada histórica → `neverStarted`, nunca `idleToday`.
    - D-14: setter con llamadas ayer y 0 hoy → `idleToday`.
    - D-16: setter con última llamada hace ≥5 días hábiles → `escalated` con `days`, y NO en `idleToday`.
    - D-18: `leaveUntil` vigente saca del `idleToday` y mete en `onLeave`; `leaveUntil` vencido lo devuelve a `idleToday` sin intervención.
    - D-21: `interested` solo lista setters con ≥1 `answered_interested` hoy.
    - D-22: `manualCalls` cuenta las entries con `channel` distinto de `telnyx_webrtc`; `manualFlag` true con 5+ o con >10%.
    - D-23: pendientes de `pending_calls.json` de hoy salen en `unmarked` por nombre y `team.dials` NO cambia.
    - REP-10: entry con `by` de un user inexistente → `unattributed` >= 1 y ninguna fila de `perSetter` la contiene.
    - Texto: primera línea es la excepción; con todas trabajando dice "Todas trabajaron hoy"; con 0 llamadas del equipo dice "Hoy no llamó nadie".
    - Texto: no contiene "0 min" ni una línea "Ayer" cuando ayer hubo 0 llamadas.
    - Consolidado: 3 datas → 1 string con 3 líneas de día y el encabezado "*Reporte acumulado · 3 días*".
    - Consistencia con el CORE: `data.team.dials` === `_ccFunnelAggregate(_ccCollectCalls(...), [], fromTs, toTs, {visibleSet}).dials`.
  </behavior>

  <action>
Crear `tests/daily-report.test.js` copiando el setup de `tests/weekly-report.test.js`
(líneas 14-72) y adaptando:

```js
const tmpData = path.join(os.tmpdir(), `daily-report-${Date.now()}`);
fs.mkdirSync(tmpData, { recursive: true });
process.env.NODE_ENV = "test";
process.env.DATA_DIR = tmpData;
process.env.ADMIN_EMAIL = "admin-dr@local.test";
process.env.ADMIN_PASSWORD = "drpass1234";
process.env.JWT_SECRET = "test-secret-dr";
process.env.BUSINESS_TZ = "America/Argentina/Buenos_Aires"; // -03, sin DST
process.env.RESEND_API_KEY = "";   // regla #121: definida-VACÍA, jamás delete
process.env.REPORT_EMAILS = "";
```
Escribir `auth.json` y `setters.json` ANTES de `const { app } = await import("../index.js")`.
Tomar `const D = globalThis.__dailyReport;` `const C = globalThis.__callCore;`
`const M = globalThis.__metricsAudit;`.

Fixture de setters (todos con user vinculado en `auth.json` salvo el borrado):
`s_bren` (Brenda), `s_jud` (Judith), `s_ter` (Teresa), `s_dal` (Dalia, nunca
llamó), `s_hid` (Oculta, `hidden: true`), `setter_ignacio` (Ignacio, admin-only).

Fechas determinísticas al estilo del analog: usar `Date.UTC(...)` para fijar
`NOW23` = 2026-07-22 23:30 AR (= 2026-07-23 02:30 UTC) y derivar el resto con
`M._bizStartOfDay` / offsets de 86400000. **Los loaders leen de disco en cada
llamada** → reescribir `setters.json` / `pending_calls.json` dentro de cada
`describe` que necesite otro fixture (patrón de weekly-report.test.js:88).

Cubrir cada punto de `<behavior>` con un `it(...)`. Para D-23 escribir
`pending_calls.json` a mano con `{ pending: [{ id:'pc_x_1', leadId:'l1',
setterId:'s_ter', startedAt: iso(<hoy 10:00>), endedAt: null, createdAt: iso(...) }] }`.
Para REP-10 usar una entry con `by: 'u_borrado'` (user que no existe en auth.json).

⚠️ NO usar fake timers: los builders reciben `nowTs` por parámetro — es
precisamente para lo que se hizo inyectable `_ccResolveRange`.
  </action>

  <verify>
    <automated>npx vitest run tests/daily-report.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/daily-report.test.js` sale 0 con >= 14 tests.
    - `grep -c 'process.env.RESEND_API_KEY = ""' tests/daily-report.test.js` = 1
      y `grep -c "delete process.env" tests/daily-report.test.js` = 0 (regla #121).
    - `grep -c "vi.useFakeTimers" tests/daily-report.test.js` = 0.
    - El archivo contiene un test que compara `data.team.dials` contra
      `_ccFunnelAggregate` (`grep -c "_ccFunnelAggregate" tests/daily-report.test.js` >= 1).
    - `npx vitest run tests/weekly-report.test.js tests/metrics-consistency.test.js tests/team-performance.test.js` sale 0.
  </acceptance_criteria>

  <done>La suite del reporte diario cubre las 13 conductas de `<behavior>` y pasa, y las suites vecinas de métricas siguen verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos de setters/leads → texto del reporte | nombres creados por el admin se interpolan en un mensaje que va a un grupo de WhatsApp |
| `pending_calls.json` → métricas del reporte | segunda fuente de "llamadas" que NO debe entrar al conteo canónico |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Spoofing | `buildDailyReportText` | mitigate | `_reportSafeName` elimina `\r\n` y trunca a 40 chars: un nombre con salto de línea podría fabricar líneas falsas ("_Equipo 0 llam_") dentro del mensaje |
| T-21-02 | Information disclosure | `buildDailyReportData` | mitigate | el alcance sale de `_SUPERVISOR_EXCLUSION_SET` + `_filterSettersVisible` + `hidden !== true`; ningún dato de Ignacio/Paula puede filtrarse al mensaje (REP-09) |
| T-21-03 | Tampering | conteo de llamadas | mitigate | `unmarked` (D-23) se computa aparte y NUNCA se suma a `dials`; el conteo canónico sigue saliendo de `_ccFunnelAggregate` (una sola forma de contar llamadas) |
| T-21-04 | Denial of service | `buildDailyReportData` | accept | recorre `leads × callLog` en memoria; corre a lo sumo 1 vez por día por el cron. El disparo manual queda admin-only + one-in-flight en el plan 21-03 |
| T-21-05 | Repudiation | contenido del reporte | accept | el mensaje nombra personas reales pero no se persiste fuera de `reports.json` (historial 30, plan 21-02); el destino cerrado de 3 socios es responsabilidad de D-24, verificado en el plan 21-07 |
</threat_model>

<verification>
- `node --check index.js` sale 0.
- `npx vitest run tests/daily-report.test.js tests/weekly-report.test.js tests/metrics-consistency.test.js tests/team-performance.test.js tests/performance.test.js` sale 0.
- El bloque nuevo de index.js no contiene `setHours(0`, ni `toISOString().slice(0, 10)` fuera de `_bizDayStr`.
- `git diff --stat` muestra SOLO `index.js` y `tests/daily-report.test.js` (backend puro: sin cache-buster, sin `public/`).
</verification>

<success_criteria>
- Con datos reales de producción, `buildDailyReportText(buildDailyReportData())` produce un mensaje idéntico en estructura al molde de D-19.
- Cero métricas en cero en el mensaje (criterio 3 del ROADMAP).
- Ignacio y Paula no aparecen (criterio 5 del ROADMAP).
- Canal manual y llamadas sin atribuir anotados (criterio 6 del ROADMAP).
- Tests del builder diario y de la consolidación (criterio 7 del ROADMAP, parte 1 de 2).
</success_criteria>

<output>
Al terminar, crear `.planning/phases/21-reporte-diario-canal-whatsapp/21-01-SUMMARY.md`.
</output>
