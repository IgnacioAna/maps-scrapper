---
phase: 21-reporte-diario-canal-whatsapp
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - index.js
  - tests/daily-cron.test.js
  - tests/weekly-report.test.js
autonomous: true
requirements: [REP-05, REP-08]

must_haves:
  truths:
    - "El diario se encola solo a las 23:00 hora de negocio, de lunes a viernes, aunque el equipo entero esté en cero"
    - "Sábado solo se manda si hubo actividad; el domingo el diario le cede el lugar al semanal"
    - "El domingo 23:00 sale el semanal corto al grupo y el mail detallado, cubriendo la semana que termina ese día"
    - "Un período ya cubierto no se vuelve a encolar ni aunque el cron corra varias veces"
    - "Una prueba manual NO suprime el envío automático de ese período"
    - "El admin puede pausar el envío automático y disparar uno manual desde el panel"
  artifacts:
    - path: "index.js"
      provides: "maybeRunDailyReportCron + cron semanal domingo 23:00 + buildWeeklyReportTextShort + /api/admin/daily-report/{status,config,send-now}"
      contains: "maybeRunDailyReportCron"
    - path: "tests/daily-cron.test.js"
      provides: "regresión de ventanas, guard por período y RBAC de los endpoints"
      min_lines: 220
  key_links:
    - from: "maybeRunDailyReportCron"
      to: "enqueueReportMessage"
      via: "encola el texto del diario en la cola de reports.json"
      pattern: "enqueueReportMessage\\("
    - from: "maybeRunWeeklyReportCron"
      to: "buildWeeklyReportTextShort"
      via: "semanal corto al grupo (D-13/D-20)"
      pattern: "buildWeeklyReportTextShort"
    - from: "POST /api/admin/daily-report/send-now"
      to: "reportQueueTick"
      via: "dispara el tick en el acto en vez de esperar 60s"
      pattern: "reportQueueTick\\("
---

<objective>
Encender el automatismo: cron diario 23:00 con las reglas de días (D-10..D-12),
mudanza del semanal al domingo 23:00 con su versión corta para el grupo
(D-13/D-20), y los tres endpoints admin que el panel necesita (estado, config,
mandar ahora).

Purpose: sin esto el reporte no sale solo, que es el punto entero del milestone.
Output: `maybeRunDailyReportCron`, `buildWeeklyReportTextShort`, extensión
aditiva de `buildWeeklyReportData`, `/api/admin/daily-report/{status,config,send-now}`,
más los tres hardening del `19-REVIEW.md` (WR-01/WR-02/WR-03) que caen
naturalmente sobre el código que este plan reescribe.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-reporte-diario-canal-whatsapp/21-CONTEXT.md
@.planning/phases/21-reporte-diario-canal-whatsapp/21-UI-SPEC.md
@.planning/phases/19-encender-reporte-semanal/19-REVIEW.md
@.planning/phases/21-reporte-diario-canal-whatsapp/21-01-SUMMARY.md
@.planning/phases/21-reporte-diario-canal-whatsapp/21-02-SUMMARY.md
@CLAUDE.md

Nota de alcance sobre `19-REVIEW.md`: los 3 warnings entran en ESTE plan porque
caen exactamente sobre las líneas que hay que reescribir igual (la ventana del
cron semanal se muda, el anti-duplicado se cambia a guard por período y
`_reportRecipients` se toca por el nuevo endpoint de config). WR-01 y WR-03 son
además correcciones de correctitud del propio objetivo de esta fase (un envío de
prueba NO puede suprimir el automático; un fallo de escritura NO puede duplicar
un mensaje al grupo de los socios). IN-01..IN-04 quedan fuera: son cosméticos o
de tests y no tocan el código de esta fase.

Reglas que aplican: #113 (TZ de negocio), #157 (CALL METRICS CORE), #19 (mutex,
vía `mutateReportsState`), #121 (tests con env vars definidas-vacías).
Backend puro: NO se toca `public/` → sin cache-buster.

<interfaces>
<!-- Del plan 21-01 -->
```js
globalThis.__dailyReport = { buildDailyReportData, buildDailyReportText, buildDailyReportLine, buildConsolidatedReportText, ... }
buildDailyReportData(nowTs = Date.now(), dayTs = nowTs)
// → { dayStr, dayLabel, period:{fromTs,toTs}, team:{dials,connects,connectRate,minutes},
//     yesterday:{dials,connects,connectRate}, perSetter:[{id,name,dials,connects,minutes}],
//     neverStarted:[], idleToday:[], escalated:[{name,days}], onLeave:[], interested:[{name,count}],
//     manualCalls, manualFlag, unattributed, unmarked:[{name,count}] }
```

<!-- Del plan 21-02 -->
```js
globalThis.__reportQueue = { enqueueReportMessage, mutateReportsState, reportQueueTick,
  handleReportSendResult, handleReportGroupConfigured, _reportStateDefaults,
  _reportPrune, _reportExpireStale, _reportGapNote, _reportTransportReady, _reportDmFallback }
enqueueReportMessage(state, { kind, periodKey, dayStr, text, line, phone, parentId })
// → { queued: true, id } | { queued: false, reason: 'periodo_ya_cubierto' }
async function reportQueueTick(nowTs = Date.now())
async function mutateReportsState(mutator)   // regla #19
// reports.json: { config:{paused, backupEmails, transport:{userId,accountId,groupName,groupJid,...}},
//                 dailyState:{lastDailyPeriodKey}, weeklyState:{lastWeeklyPeriodKey}, queue:[], history:[] }
```

<!-- Existente (Phase 19) -->
```js
index.js:1793 buildWeeklyReportData()            // ventana lunes-a-lunes de la semana PASADA
index.js:1845 buildWeeklyReportHtml(data)        // mail HTML — NO se toca
index.js:1855 _reportRecipients()                // REPORT_EMAILS CSV > ADMIN_EMAIL > admin de auth.json
index.js:1870 async sendWeeklyReport(toEmails, dataOverride = null) // → { sent, id } | { sent:false, reason }
index.js:1894 async maybeRunWeeklyReportCron(nowTs = Date.now(), sendFn = sendWeeklyReport)
index.js:1913 setInterval horario + setTimeout de arranque
```

<!-- Contrato que consume el panel (21-UI-SPEC.md §"Contrato de endpoints propuesto") -->
```
GET  /api/admin/daily-report/status
  → { groupConfigured, groupName, jidCaptured, lastSent:{at,periodLabel,status}|null,
      queueCount, desktopOnline, paused, backupEmails[] }
PUT  /api/admin/daily-report/config     body { backupEmails?: string[], paused?: bool }
POST /api/admin/daily-report/send-now
  → { ok:true, status:'sent'|'sent_via_dm'|'queued'|'failed', reason?, groupName?, sentAt?, queueCount? }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: semanal al domingo 23:00 + versión corta para el grupo (D-13/D-20) + WR-01/WR-02/WR-03</name>

  <read_first>
    - `index.js:1793-1943` (bloque semanal completo: data, HTML, `_reportRecipients`,
      `sendWeeklyReport`, `maybeRunWeeklyReportCron`, endpoints manuales,
      `globalThis.__weeklyReport`).
    - `.planning/phases/19-encender-reporte-semanal/19-REVIEW.md` §WR-01, WR-02, WR-03
      (el fix propuesto está escrito ahí, con el código).
    - `tests/weekly-report.test.js:75-115` (fixture de fechas: hay que MOVERLO a
      la ventana nueva).
    - El bloque `// ── Phase 21 ──` de index.js (planes 21-01 y 21-02).
  </read_first>

  <action>
**1a. Ventana del semanal: la semana que TERMINA hoy (D-13).** En
`buildWeeklyReportData` (index.js:1799-1805) reemplazar el cálculo:
```js
  // D-13 (2026-07-26): el semanal pasó de "la semana pasada completa" a "la
  // semana que termina hoy" (lunes → ahora), porque ahora sale el DOMINGO 23:00
  // junto al último diario, no el lunes a la mañana.
  const nowTs = Date.now();
  const todayStart = _bizStartOfDay(nowTs);
  const dayOfWeek = _bizDayOfWeek(todayStart) || 7;      // domingo = 7
  const thisMonday = todayStart - (dayOfWeek - 1) * 86400000;
  const fromTs = thisMonday;
  const toTs = Math.min(nowTs, thisMonday + 7 * 86400000);
  const prevFromTs = thisMonday - 7 * 86400000;
  const prevToTs = thisMonday;
```
⚠️ Esto CAMBIA el comportamiento a propósito. `tests/weekly-report.test.js` tiene
fixtures en la semana anterior: hay que moverlos a la semana actual (Task 3), no
revertir el cambio.

**1b. Extensión ADITIVA de `buildWeeklyReportData`.** No se quita ni se renombra
ninguna clave existente (`buildWeeklyReportHtml` y los tests de Phase 19 las
usan). Se agregan:
- `calls.minutes` = `Math.round(agg.totalDurationS / 60)`
- `calls.interested` = `weekCalls.filter(c => c.outcome === 'answered_interested').length`
- `perSetter[].minutos` y `perSetter[].interesados` (misma técnica que el diario:
  `_ccFunnelAggregate(callsDelSetter, [], fromTs, toTs)` para los minutos, y
  conteo de `answered_interested` para interesados)
- `previous` = `{ dials, connects, connectRate, interested }` sobre
  `[prevFromTs, prevToTs)` usando `_ccFunnelAggregate`
- `neverStarted` = mismos criterios que el diario (visible, `hidden !== true`,
  cero llamadas históricas)

**1c. `buildWeeklyReportTextShort(data, { emailSent = false } = {})`** — molde
LITERAL de D-20, en el bloque Phase 21:
```
*Semana 20–26/07*
Equipo 312 llam · 91 at (29%) · 182 min
32 interesados · 0 reuniones agendadas

*Teresa* 165 llam · 38 at · 70 min · 11 int
*Brenda* 74 llam · 24 at · 70 min · 14 int
*Judith* 73 llam · 29 at · 42 min · 7 int

_Semana anterior: 70 llam · 25 at (36%) · 8 int_
_Sin arrancar: Dalia, Adela, Melissa_
_Detalle completo en el mail._
```
Reglas exactas:
- Encabezado: `*Semana DD/MM–DD/MM*` con `_reportDayLabel` recortado a `DD/MM`
  (from y to-1 día).
- **`0 reuniones agendadas` SE MUESTRA aunque sea cero** — excepción consciente y
  explícita de D-20 a la regla "nada de métricas en cero" (32 interesados y 0
  reuniones es LA noticia). Dejarlo comentado en el código para que nadie lo
  "optimice" después.
- Filas: solo setters con `llamadas > 0`, orden desc; segmentos `min` e `int` se
  omiten si son 0.
- `_Semana anterior: ..._` solo si `previous.dials > 0`.
- `_Sin arrancar: ..._` solo si hay.
- `_Detalle completo en el mail._` **solo si `emailSent === true`** — si no hay
  `RESEND_API_KEY` el mail no salió y la línea sería mentira.

**1d. `maybeRunWeeklyReportCron` reescrito** (misma firma inyectable):
```js
async function maybeRunWeeklyReportCron(nowTs = Date.now(), sendFn = sendWeeklyReport) {
  // D-13: domingo 23:00 en TZ de negocio (antes: lunes 8am). Un solo momento y
  // un solo reporte por las dos vías (mail detallado + corto al grupo).
  if (_bizDayOfWeek(nowTs) !== 0 || _bizHour(nowTs) < 23) return { ran: false, reason: 'fuera_de_ventana' };
  ...
}
```
- **Guard por período (D-28 + WR-01)**: `periodKey = _bizDayStr(nowTs)` (el
  domingo que cierra la semana). Si `state.weeklyState.lastWeeklyPeriodKey ===
  periodKey` → `{ ran:false, reason:'ya_enviado' }`. **Se elimina la ventana de
  "6 días"**: era lo que hacía que un envío manual de prueba suprimiera el
  automático del lunes siguiente (WR-01). El endpoint manual
  `POST /api/admin/weekly-report/send` (index.js:1923) deja de tocar
  `lastWeeklyReportAt` como guard y escribe `lastManualWeeklySendAt` para
  trazabilidad.
- **Guard en memoria (WR-03)**: `let _weeklyPeriodSentMem = ''` a nivel de módulo;
  se compara `periodKey` contra `Math.max`-equivalente (`=== periodKey` en
  cualquiera de los dos) y se setea tras el envío. Si `saveReportsState` falla
  (volumen lleno/read-only), el proceso igual no re-manda. Ídem para el diario.
- Si `state.config.paused` → `{ ran:false, reason:'pausado' }`.
- Manda el mail (`sendFn(recipients)`) y **encola el corto al grupo**:
  ```js
  await mutateReportsState((s) => enqueueReportMessage(s, {
    kind: 'weekly', periodKey, dayStr: periodKey,
    text: buildWeeklyReportTextShort(data, { emailSent: !!result.sent }),
  }));
  ```
  El semanal se encola **aunque el mail falle** (D-04: el canal del grupo no
  depende del email) y **nunca se consolida ni expira** (ya cubierto en 21-02).

**1e. WR-02 en `_reportRecipients`** (2 líneas):
```js
  if (csv && !list.length) console.warn(`REPORT_EMAILS seteada pero sin emails válidos ("${csv}") — fallback a ADMIN_EMAIL`);
  ...
  return [...new Set(list)];   // dedup antes de mandárselo a Resend
```
  </action>

  <verify>
    <automated>node --check index.js && npx vitest run tests/onboarding.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` sale 0.
    - `grep -c "function buildWeeklyReportTextShort(" index.js` = 1.
    - `grep -c "reuniones agendadas" index.js` >= 1.
    - `grep -c "Detalle completo en el mail" index.js` = 1.
    - Ventana nueva: `grep -A6 "async function maybeRunWeeklyReportCron" index.js | grep -c "_bizDayOfWeek(nowTs) !== 0"` = 1.
    - Guard por período: `grep -c "lastWeeklyPeriodKey" index.js` >= 3 y
      `grep -c "6 \* 24 \* 60 \* 60 \* 1000" index.js` = 0 (la ventana de 6 días de WR-01 ya no existe).
    - WR-03: `grep -c "_weeklyPeriodSentMem\|_dailyPeriodSentMem" index.js` >= 2.
    - WR-02: `grep -c "REPORT_EMAILS seteada pero sin emails válidos" index.js` = 1 y
      `grep -c "new Set(list)" index.js` = 1.
    - Claves de Phase 19 intactas: `grep -c "totalWeek\|answeredWeek\|scheduledWeek\|deadWeek\|pctAtendidas\|leadsAsignados\|agendadosLlamada" index.js` >= 7.
    - `npx vitest run tests/onboarding.test.js` sale 0 (contiene assertions del semanal).
  </acceptance_criteria>

  <done>El semanal sale el domingo 23:00 cubriendo la semana que termina ese día, con mail detallado + versión corta encolada al grupo, guard por período que ya no se deja suprimir por una prueba manual, y guard en memoria contra fallos de disco.</done>
</task>

<task type="auto">
  <name>Task 2: maybeRunDailyReportCron (D-10..D-12) + endpoints admin del panel</name>

  <read_first>
    - `index.js:1894-1916` (cron semanal ya reescrito en la Task 1 — el diario
      copia su forma inyectable `(nowTs, ...)` y su registro con setInterval).
    - `index.js:1918-1937` (endpoints `/api/admin/weekly-report/*` — patrón de
      `requireAuth, requireRole('admin')` a replicar).
    - `.planning/phases/21-reporte-diario-canal-whatsapp/21-UI-SPEC.md`
      §"Contrato de endpoints propuesto", §"State machine — botón Mandar ahora",
      §"Decisiones de esta fase" (1: "Mandar ahora" ignora la pausa;
      2: un fallo de desktop en el manual ENCOLA, no descarta).
    - Los helpers de 21-01 y 21-02.
  </read_first>

  <action>
**2a. `maybeRunDailyReportCron(nowTs = Date.now())`** en el bloque Phase 21:

```js
// D-10: 23:00 hora de negocio (BUSINESS_TZ = AR/UY) = 20:00 en México. Corte
// ÚNICO para todo el equipo: el sistema no guarda el país de cada vendedora y
// esperar a la más occidental empujaría el mensaje a la madrugada.
// D-11: lunes a viernes SIEMPRE, aun con el equipo entero en cero (un día hábil
// sin una sola llamada es LA noticia — sale como una línea, no como seis ceros).
// D-12: sábado solo si hubo actividad (el histórico tiene cero llamadas todos
// los fines de semana; un mensaje vacío entrena al grupo a no leer).
// D-13: el domingo el diario le cede el lugar al semanal.
async function maybeRunDailyReportCron(nowTs = Date.now()) {
  const dow = _bizDayOfWeek(nowTs);
  if (dow === 0) return { ran: false, reason: 'domingo_semanal' };
  if (_bizHour(nowTs) < 23) return { ran: false, reason: 'fuera_de_ventana' };
  const periodKey = _bizDayStr(nowTs);
  if (_dailyPeriodSentMem === periodKey) return { ran: false, reason: 'ya_enviado' };
  return mutateReportsState((state) => {
    if (state.config.paused) return { ran: false, reason: 'pausado' };
    if (state.dailyState.lastDailyPeriodKey === periodKey) return { ran: false, reason: 'ya_enviado' };
    const data = buildDailyReportData(nowTs);
    if (dow === 6 && data.team.dials === 0) {          // D-12
      state.dailyState.lastDailyPeriodKey = periodKey; // no reintentar toda la noche
      _dailyPeriodSentMem = periodKey;
      return { ran: false, reason: 'finde_sin_actividad' };
    }
    const r = enqueueReportMessage(state, {
      kind: 'daily', periodKey, dayStr: data.dayStr,
      text: buildDailyReportText(data),
      line: buildDailyReportLine(data),      // D-26: la usa la consolidación
    });
    if (!r.queued) return { ran: false, reason: r.reason };
    state.dailyState.lastDailyPeriodKey = periodKey;   // D-28
    _dailyPeriodSentMem = periodKey;                    // WR-03
    return { ran: true, queued: true, periodKey, id: r.id };
  });
}
```
Registro (D-13: el semanal corre PRIMERO en el mismo tick; el diario ya se
autoexcluye los domingos):
```js
if (process.env.NODE_ENV !== 'test') {
  const _reportCrons = async () => {
    await maybeRunWeeklyReportCron().catch(e => console.warn('weekly cron:', e.message));
    await maybeRunDailyReportCron().catch(e => console.warn('daily cron:', e.message));
  };
  setInterval(() => { _reportCrons(); }, 60 * 60 * 1000);
  setTimeout(() => { _reportCrons(); }, 60 * 1000);
}
```
⚠️ Reemplaza el `setInterval`/`setTimeout` de index.js:1913 — **no dejar los dos**
(se duplicaría el chequeo semanal).

**2b. Los tres endpoints**, junto a los de `weekly-report` y con la misma
protección `requireAuth, requireRole('admin')`:

```js
// GET /api/admin/daily-report/status — lo consume _cmdLoadReportPanel() del panel.
app.get('/api/admin/daily-report/status', requireAuth, requireRole('admin'), (_req, res) => {
  const state = _reportStateDefaults(loadReportsState());
  const t = state.config.transport || {};
  const gw = globalThis.__waGateway;
  const last = [...(state.history || []), ...(state.queue || [])]
    .filter(i => i.status === 'sent' || i.status === 'failed')
    .sort((a, b) => (Date.parse(b.sentAt || b.failedAt || '') || 0) - (Date.parse(a.sentAt || a.failedAt || '') || 0))[0] || null;
  res.json({
    groupConfigured: !!(t.groupName && t.accountId && t.userId),
    groupName: t.groupName || null,
    jidCaptured: !!t.groupJid,          // el JID no se expone entero: es un identificador del destino
    lastSent: last ? { at: last.sentAt || last.failedAt, periodLabel: last.dayStr || last.periodKey, status: last.status } : null,
    queueCount: (state.queue || []).filter(i => i.status === 'pending' || i.status === 'sending').length,
    desktopOnline: !!(t.userId && gw && typeof gw.isUserConnected === 'function' && gw.isUserConnected(t.userId)),
    paused: !!state.config.paused,
    backupEmails: state.config.backupEmails || [],
  });
});
```

```js
// PUT /api/admin/daily-report/config — body { backupEmails?: string[], paused?: bool }
// backupEmails es el fallback del DIARIO, hoy APAGADO por D-04: se persiste para
// que encenderlo después sea configuración y no construcción. NO es REPORT_EMAILS
// (esa env var gobierna el mail del SEMANAL, que sí sale hoy).
```
Validaciones: `paused` → boolean estricto; `backupEmails` → array, cap 10,
regex `/^[^\s@]+@[^\s@]+\.[^\s@]+$/`, trim, dedup, rechaza el resto con 400.
Responde el mismo shape que el GET (la UI refresca con la respuesta).

```js
// POST /api/admin/daily-report/send-now — D-29. Arma el reporte con los datos de
// HOY hasta este momento y lo manda YA.
const REPORT_SEND_NOW_WAIT_MS = 25000;
```
Flujo exacto:
1. **Ignora la pausa** (decisión 1 del UI-SPEC: si el admin pausó y quiere probar
   el canal, el botón tiene que seguir funcionando).
2. `data = buildDailyReportData()`, `text = buildDailyReportText(data)`.
3. Encolar con `periodKey = 'manual_' + new Date().toISOString()` y
   `kind:'daily'`... **NO**: usar `kind:'custom'` para que no participe de la
   consolidación ni consuma el período del cron (WR-01 generalizado: una prueba
   manual jamás suprime el envío automático). Guardar `line` igual, por si acaso
   no se usa.
4. `await reportQueueTick()` en el acto — no esperar hasta 60s al próximo tick.
5. Esperar hasta `REPORT_SEND_NOW_WAIT_MS` **haciendo polling cada 1s** del estado
   del item (`loadReportsState`), hasta que salga de `pending`/`sending`.
   (Polling y no una promesa cruzada con el gateway: mantiene el request por
   debajo de los 25s — el tipeo OS-level de un mensaje largo puede tardar más de
   un minuto y no tiene sentido dejar una conexión HTTP colgada esperándolo.)
6. Responder:
   - item `sent` con `method` de grupo → `{ status:'sent', sentAt, groupName }`
   - item `sent` con `method:'dm'` → `{ status:'sent_via_dm', sentAt }`
   - item `failed` con `reason:'account-not-connected'` →
     `{ status:'failed', reason:'account-not-connected' }`
   - item `failed` con `reason:'group-not-found'` (ya se encolaron los DM) →
     `{ status:'queued', reason:'fallback_dm', queueCount }`
   - desktop offline / sin grupo configurado → `{ status:'queued', reason:'offline'|'sin_grupo', queueCount }`
   - sigue en `sending` al vencer la espera → `{ status:'queued', reason:'sending', queueCount }`
   Siempre `ok: true` salvo error de servidor (el estado va en `status`).
7. **Lock de un solo envío en vuelo (T-21-14)**: si ya hay un item en `sending`
   al entrar, devolver `{ status:'queued', reason:'busy', queueCount }` SIN
   encolar otro. El backend no confía en el `disabled` del botón.

**2c. Exponer** para los tests:
`globalThis.__dailyReport.maybeRunDailyReportCron = maybeRunDailyReportCron;`
y `globalThis.__weeklyReport.buildWeeklyReportTextShort = buildWeeklyReportTextShort;`
(o incluirlos en los objetos al definirlos).
  </action>

  <verify>
    <automated>node --check index.js && npx vitest run tests/security-rbac.test.js tests/hardening.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` sale 0.
    - `grep -c "async function maybeRunDailyReportCron(" index.js` = 1.
    - `grep -c "'/api/admin/daily-report/status'\|'/api/admin/daily-report/config'\|'/api/admin/daily-report/send-now'" index.js` = 3.
    - Los 3 endpoints son admin-only: `grep -c "daily-report/.*requireAuth, requireRole('admin')" index.js` = 3.
    - Un solo registro de crons de reporte: `grep -c "maybeRunWeeklyReportCron()" index.js` = 2 (las dos llamadas dentro de `_reportCrons`), y `grep -c "setInterval(() => maybeRunWeeklyReportCron" index.js` = 0.
    - `grep -c "domingo_semanal\|finde_sin_actividad\|fuera_de_ventana\|pausado" index.js` >= 5.
    - `grep -c "REPORT_SEND_NOW_WAIT_MS" index.js` >= 2.
    - Prueba manual no consume el período: `grep -A25 "daily-report/send-now" index.js | grep -c "lastDailyPeriodKey"` = 0.
    - `npx vitest run tests/security-rbac.test.js tests/hardening.test.js tests/report-queue.test.js tests/daily-report.test.js` sale 0.
  </acceptance_criteria>

  <done>El diario se encola solo a las 23:00 con las reglas de día correctas, el panel tiene sus tres endpoints admin-only, y "Mandar ahora" dispara el envío en el acto sin consumir el período del automático ni dejar colgada la conexión HTTP.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/daily-cron.test.js + adaptación de tests/weekly-report.test.js</name>

  <read_first>
    - `tests/weekly-report.test.js` completo (setup, fechas fijas `MON9/MON7/TUE/MONTZ/NEXTMON`,
      `mkSend`, tests de ventana/anti-duplicado/shape/persistencia).
    - `tests/report-queue.test.js` (gateway falso del plan 21-02).
    - Los crons y endpoints de las Tasks 1 y 2.
  </read_first>

  <behavior>
    - Ventana: 22:00 hora de negocio → `fuera_de_ventana`; 23:10 → corre.
    - TZ: un instante que en UTC es otro día pero en TZ de negocio son las 23:xx cae dentro de la ventana (caso `MONTZ` del analog).
    - Lunes-viernes con 0 llamadas del equipo → SÍ encola, y el texto encolado contiene "Hoy no llamó nadie" (D-11).
    - Sábado con 0 llamadas → `finde_sin_actividad`, no encola, y marca el período para no reintentar toda la noche (D-12).
    - Sábado con llamadas → encola.
    - Domingo → `domingo_semanal` (el diario cede) y `maybeRunWeeklyReportCron` a las 23:00 de ese domingo SÍ corre.
    - Segundo tick el mismo día → `ya_enviado`, la cola sigue con 1 solo item (D-28).
    - Con `config.paused = true` el cron devuelve `pausado` y no encola.
    - El cron semanal del domingo encola un item `kind:'weekly'` cuyo texto contiene "Semana " y "reuniones agendadas", y llama al `sendFn` de mail una sola vez.
    - Con `sendFn` que devuelve `{sent:false}`, el semanal IGUAL encola el corto al grupo y el texto NO contiene "Detalle completo en el mail." (D-04: el grupo no depende del email).
    - WR-01: `POST /api/admin/weekly-report/send` (manual) NO impide que el cron del domingo siguiente mande.
    - Ventana nueva del semanal: `buildWeeklyReportData()` incluye llamadas de ESTA semana (lunes → ahora) y excluye las de la anterior, que aparecen en `previous`.
    - RBAC: los 3 endpoints `/api/admin/daily-report/*` devuelven 403 para un setter y 401 sin sesión.
    - `PUT config` con `backupEmails: ["mal", "a@b.com", "a@b.com"]` → 400 o lista deduplicada según la validación; `paused:true` persiste y se ve en el `GET status`.
    - `POST send-now` sin grupo configurado → `{status:'queued', reason:'sin_grupo'}` y NO toca `dailyState.lastDailyPeriodKey`.
  </behavior>

  <action>
Crear `tests/daily-cron.test.js` con el setup de `tests/weekly-report.test.js`
(`DATA_DIR` propio en tmp, `RESEND_API_KEY=""`, `BUSINESS_TZ` fija, auth.json con
admin + setter, `setters.json` con vendedoras) y el gateway falso del plan 21-02.

Fechas determinísticas (elegir domingos/sábados reales de 2026 y documentarlos
en comentario, igual que el analog):
```js
const SUN23 = Date.UTC(2026, 6, 27, 2, 0, 0);   // dom 26/07 23:00 AR
const MON23 = Date.UTC(2026, 6, 28, 2, 0, 0);   // lun 27/07 23:00 AR
const MON22 = Date.UTC(2026, 6, 28, 1, 0, 0);   // lun 27/07 22:00 AR (fuera)
const SAT23 = Date.UTC(2026, 6, 26, 2, 0, 0);   // sáb 25/07 23:00 AR
```
Resetear `reports.json` entre bloques (`fs.rmSync(..., {force:true})`) para que el
guard por período no contamine tests vecinos, y resetear el guard en memoria
recargando el estado por el camino público (encolar con otro `periodKey`).

**Adaptar `tests/weekly-report.test.js`** (cambio intencional, NO revertir la
feature):
- las fechas `MON9/MON7/TUE/MONTZ/NEXTMON` pasan a domingos 23:00 equivalentes;
- el fixture de llamadas se mueve de `thisMonday - 3 días` a **dentro** de la
  semana actual (`thisMonday + 1 día`), con al menos una llamada en la semana
  anterior para que `previous` no quede vacío;
- el test del anti-duplicado pasa a afirmar el guard por `lastWeeklyPeriodKey`;
- agregar en el encabezado del archivo una nota de 3 líneas explicando que D-13
  movió la ventana y por qué.
  </action>

  <verify>
    <automated>npx vitest run tests/daily-cron.test.js tests/weekly-report.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/daily-cron.test.js` sale 0 con >= 14 tests.
    - `npx vitest run tests/weekly-report.test.js` sale 0 (fixtures adaptados a la ventana nueva).
    - `grep -c "delete process.env" tests/daily-cron.test.js` = 0.
    - `grep -c "D-13" tests/weekly-report.test.js` >= 1 (la nota que explica el cambio de ventana).
    - `npx vitest run` (suite completa) sale 0.
  </acceptance_criteria>

  <done>Las ventanas de días/horas, el guard por período, la pausa, el RBAC de los endpoints y la mudanza del semanal al domingo quedan cubiertos por tests, y la suite completa del repo sigue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin del panel → envío saliente real | `send-now` dispara un mensaje a un grupo de WhatsApp con 3 personas reales |
| admin del panel → config del canal | `PUT config` cambia pausa y mails de respaldo |
| cron → cola | el automatismo puede duplicar mensajes si el guard falla |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-13 | Elevation of privilege | `/api/admin/daily-report/*` | mitigate | los 3 endpoints con `requireAuth, requireRole('admin')`, igual que `/api/admin/weekly-report/*`. Un supervisor scoped NO los alcanza (todo supervisor es scoped desde la nota #144) |
| T-21-14 | Denial of service | `send-now` spameando al grupo | mitigate | lock server-side de un solo envío en vuelo (devuelve `queued/busy` sin encolar otro); el `askConfirm` + `disabled` del panel es defensa en profundidad, NO la defensa (el backend no confía en la UI) |
| T-21-15 | Tampering | duplicado del reporte automático | mitigate | guard por PERÍODO cubierto (`lastDailyPeriodKey`/`lastWeeklyPeriodKey`, D-28) + guard gemelo en memoria (WR-03) para que un fallo de escritura en el Railway Volume no reviva el bug de los 16 mails, esta vez contra un grupo de WhatsApp |
| T-21-16 | Tampering | `PUT config` con `backupEmails` | mitigate | array, cap 10, regex de email, trim + dedup; cualquier otra cosa → 400. Nunca se interpola en HTML ni en comandos |
| T-21-17 | Information disclosure | `GET status` | mitigate | admin-only y devuelve `jidCaptured: boolean` en vez del JID entero (el identificador del destino no necesita viajar al browser) |
| T-21-18 | Repudiation | prueba manual vs envío automático | mitigate | el manual usa `kind:'custom'` con `periodKey` propio y NO escribe `lastDailyPeriodKey` — un envío de prueba jamás suprime el automático (WR-01 generalizado) |
</threat_model>

<verification>
- `node --check index.js` sale 0.
- `npx vitest run` (suite completa) sale 0 — baseline ~864 tests + los nuevos.
- `grep -c "setInterval(() => maybeRunWeeklyReportCron" index.js` = 0 (el registro viejo fue reemplazado, no duplicado).
- `git diff --stat` muestra SOLO `index.js`, `tests/daily-cron.test.js` y `tests/weekly-report.test.js`.
</verification>

<success_criteria>
- El diario llega solo, todos los días hábiles, sin intervención de nadie (criterio 1 del ROADMAP, parte server).
- Sin duplicados por período cubierto (criterio 4 del ROADMAP).
- El semanal corto usa el mismo lenguaje que el diario y sale por el mismo canal el domingo 23:00 (D-13/D-20, REP-05).
</success_criteria>

<output>
Al terminar, crear `.planning/phases/21-reporte-diario-canal-whatsapp/21-03-SUMMARY.md`.
</output>
