---
phase: 21-reporte-diario-canal-whatsapp
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - index.js
  - src/wa/gateway.js
  - tests/report-queue.test.js
autonomous: true
requirements: [REP-06, REP-07, REP-08]

must_haves:
  truths:
    - "Un mensaje de texto cualquiera se puede encolar y sale solo al grupo cuando el desktop está conectado"
    - "Con el desktop apagado el mensaje NO se marca como enviado: queda pendiente y reintenta"
    - "Con N diarios pendientes sale UN solo mensaje consolidado, no N mensajes"
    - "Los diarios de más de 3 días expiran; el semanal nunca se consolida ni expira"
    - "Un período ya entregado no se re-manda por ningún canal"
    - "Si el grupo no aparece, el reporte sale por mensajes individuales a los socios en vez de perderse"
    - "El próximo mensaje que sí sale confiesa los reportes que no se pudieron enviar"
  artifacts:
    - path: "index.js"
      provides: "cola persistida en reports.json + reportQueueTick + enqueueReportMessage + globalThis.__reportQueue"
      contains: "function reportQueueTick"
    - path: "src/wa/gateway.js"
      provides: "listeners report:send-result y report:group-configured con authz"
      contains: "report:send-result"
    - path: "tests/report-queue.test.js"
      provides: "regresión de cola, consolidación, expiración y correlación"
      min_lines: 220
  key_links:
    - from: "reportQueueTick"
      to: "globalThis.__waGateway.isUserConnected"
      via: "guard de alcanzabilidad ANTES de emitir (REP-07)"
      pattern: "isUserConnected"
    - from: "reportQueueTick"
      to: "desktop wa-multi"
      via: "sendToUser(userId, 'report:send-message', payload)"
      pattern: "'report:send-message'"
    - from: "src/wa/gateway.js"
      to: "index.js"
      via: "deps.onReportEvent('send-result'|'group-configured', payload, user)"
      pattern: "onReportEvent"
---

<objective>
Construir la cola persistida de mensajes al grupo de WhatsApp: encolado
genérico, guard de alcanzabilidad, emisión por Socket.io al desktop,
correlación real del resultado (nada de fire-and-forget), reintentos,
consolidación, expiración, historial y confesión de baches.

Purpose: es el transporte de toda la fase y de la Phase 23 (D-06: el servicio
es genérico desde acá, acepta cualquier texto, no solo reportes).
Output: `enqueueReportMessage`, `reportQueueTick`, handlers de socket con
authz, y el esquema extendido de `reports.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-reporte-diario-canal-whatsapp/21-CONTEXT.md
@.planning/phases/21-reporte-diario-canal-whatsapp/21-RESEARCH.md
@.planning/phases/21-reporte-diario-canal-whatsapp/21-PATTERNS.md
@.planning/phases/21-reporte-diario-canal-whatsapp/21-01-SUMMARY.md
@CLAUDE.md

Reglas que aplican:
- #21/#128 persistencia: **NO se crea ningún archivo JSON nuevo**. Todo el estado
  vive en `reports.json`, que YA está registrado en los 5 lugares
  (export-data index.js:2159, import-data index.js:2258, seedVolumeFromRepo
  index.js:4080, BACKUP_FILES index.js:4133, scripts/pre-deploy.js:186). Crear
  un archivo nuevo obligaría a repetir ese registro y un olvido lo borra en el
  próximo redeploy.
- #19 mutex: el tick tiene `await`/emisión entre load y save → usar el mutex
  `mutateReportsState` que se crea en la Task 1. NUNCA `loadReportsState() →
  await algo → saveReportsState()` naive.
- #113 TZ de negocio para claves de día (`_bizDayStr`).
- #121 tests: `process.env.X_API_KEY = ""`, NUNCA `delete`.
- Backend puro: NO se toca `public/` → sin cache-buster.

<interfaces>
<!-- Contratos que el executor consume tal cual. -->

src/wa/gateway.js:328-348 — transporte:
```js
export function isUserOnline(userId)            // sockets del user > 0
export function sendToUser(userId, event, payload)
// ⚠️ sendToUser hace io.to(room).emit(...) y devuelve `true` con la room VACÍA.
// NO es señal de entrega (REP-07). El guard real es isUserOnline ANTES de emitir,
// y el resultado real llega por el evento de vuelta report:send-result.
export function exposeGlobals() // globalThis.__waGateway = { sendToUser, isUserConnected: isUserOnline, getPresenceList }
```

index.js:1744-1754 — persistencia existente que se extiende:
```js
function getReportsFile()      // DATA_DIR/reports.json
function loadReportsState()    // → {} si no existe
function saveReportsState(state)
```

index.js:15674 — objeto `deps` de `mountWa(app, server, {...})`: es donde se
agrega `onReportEvent` (mismo patrón que `markLeadContacted`).

**Contrato de eventos (definido en 21-RESEARCH.md Q5 — NO rediseñar):**

server → desktop, evento `report:send-message`:
```js
{
  queueId: "rpt_2026-07-26_1753574400000",  // clave de correlación
  accountId: "<cuenta wa-multi dedicada>",
  text: "<mensaje ya armado, texto plano>",
  target: {
    kind: "group",                 // "group" | "dm"
    groupName: "<nombre persistido>",
    groupJid: "<...@g.us o null>", // checksum, null hasta el primer envío
    phone: "<E.164, solo si kind:'dm'>"
  }
}
```

desktop → server, evento `report:send-result`:
```js
{
  queueId: "rpt_2026-07-26_1753574400000",
  accountId: "...",
  ok: true | false,
  method: "pinned-row0" | "search-by-name" | "dm",
  reason: null | "account-not-connected" | "group-not-found" | "jid-mismatch"
        | "composer-not-found" | "not-on-whatsapp" | "exception",
  matchedName: "<header leído>" | null,
  matchedJid: "<...@g.us>" | null,   // permite BACKFILLEAR groupJid si era null
  sentAt: "<ISO>"
}
```

desktop → server, evento `report:group-configured` (setup, D-03):
```js
{ accountId, groupName, groupJid: null|"<...@g.us>" }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: esquema de reports.json + helpers de cola con mutex + enqueueReportMessage</name>

  <read_first>
    - `index.js:1744-1760` (`getReportsFile`/`loadReportsState`/`saveReportsState`).
    - `index.js:5123-5185` (`SCHEDULED_FILE`/`SCHEDULED_CAP`/`loadScheduledMessages`/
      `saveScheduledMessages`/`pickStaggerOffset`/`_isSetterReachable`) — **el
      analog exacto del cap FIFO "separar por status antes de cortar"**.
    - `index.js:5104-5114` (`mutateSettersData` — el patrón de mutex a calcar).
    - `index.js:1940` (`globalThis.__weeklyReport`) y el bloque
      `// ── Phase 21: reporte diario ──` del plan 21-01.
    - `.planning/phases/21-reporte-diario-canal-whatsapp/21-RESEARCH.md` §Q5
      (por qué NO se usan acks de Socket.IO y por qué el estado intermedio es
      `sending`, no `sent`).
  </read_first>

  <action>
Agregar al final del bloque `// ── Phase 21: reporte diario ──` de index.js un
sub-bloque `// ── Phase 21: cola de envío al grupo de WhatsApp ──`.

**1a. Constantes y esquema.** El estado vive TODO en `reports.json` (regla
#21/#128: cero archivos nuevos). Forma final:

```js
// reports.json (extiende lo de Phase 19, aditivo):
// {
//   lastWeeklyReportAt, lastWeeklyReportTo,          // Phase 19
//   config: {
//     paused: false,
//     backupEmails: [],                              // D-04: se persiste, hoy apagado
//     transport: { userId, accountId, groupName, groupJid, jidCapturedAt, configuredAt, configuredBy }
//   },
//   dailyState:  { lastDailyPeriodKey: '' },         // 'YYYY-MM-DD' TZ negocio (D-28)
//   weeklyState: { lastWeeklyPeriodKey: '' },        // 'YYYY-MM-DD' del domingo cubierto (D-28)
//   queue:   [ item ],                               // pending|sending|sent|failed|expired
//   history: [ item ],                               // últimos 30 terminales (D-27)
// }
// item = { id, kind: 'daily'|'weekly'|'custom'|'dm', periodKey, dayStr, text,
//          line, phone, parentId, status, attempts, confessedAt,
//          createdAt, sendingAt, sentAt, failedAt, expiredAt,
//          lastFailureReason, method, matchedName, matchedJid }
const REPORT_QUEUE_CAP = 200;          // pending nunca se recorta; terminales sí
const REPORT_HISTORY_CAP = 30;         // D-27
const REPORT_DAILY_EXPIRY_DAYS = 3;    // D-26 — el semanal NUNCA expira
const REPORT_SEND_TIMEOUT_MS = 150000; // 2,5 min: cold start de WhatsApp Web (~21s
                                       // de polling del composer) + tipeo OS-level
                                       // letra por letra de un mensaje de ~400 chars
const REPORT_MAX_ATTEMPTS = 20;
```

**1b. Normalizador + mutex.** `_reportStateDefaults(state)` completa las claves
faltantes sin pisar las existentes (Phase 19 escribió `reports.json` sin ellas).
Mutex calcado de `mutateSettersData` (regla #19 — el tick emite y espera entre
load y save):

```js
let _reportsMutex = Promise.resolve();
async function mutateReportsState(mutator) {
  const next = _reportsMutex.then(async () => {
    const state = _reportStateDefaults(loadReportsState());
    const result = await Promise.resolve(mutator(state));
    saveReportsState(_reportPrune(state));
    return result;
  });
  _reportsMutex = next.catch(() => {});
  return next;
}
```

**1c. `_reportPrune(state)`** — mismo mecanismo que `saveScheduledMessages`
(index.js:5143): separar por status ANTES de cortar, para no perder pendientes.
- mueve los terminales (`sent`/`failed`/`expired`) de `queue` a `history`,
- `history` = últimos `REPORT_HISTORY_CAP` (D-27),
- `queue` = pendientes + los `sending` (nunca se recortan) + los últimos
  terminales hasta `REPORT_QUEUE_CAP`.

**1d. `_reportExpireStale(state, nowTs)`** (D-26): items `kind:'daily'` en
`pending` cuyo `dayStr` es más viejo que `REPORT_DAILY_EXPIRY_DAYS` días
respecto de `_bizDayStr(nowTs)` → `status:'expired'`, `expiredAt`. **`kind:'weekly'`
queda excluido explícitamente: nunca expira ni se consolida.**

**1e. `_reportGapNote(state, nowTs)`** (D-05): junta los items `expired`/`failed`
con `confessedAt == null`, toma sus `dayStr` → devuelve
`_No pude enviar el reporte de jue 24/07 y vie 25/07._` usando `_reportDayLabel`
(unir con ", " y la última con " y "). Devuelve `''` si no hay ninguno.
El sello `confessedAt` lo pone el tick recién cuando el mensaje que la lleva sale
OK — si ese envío falla, la confesión se vuelve a intentar en el siguiente.

**1f. `enqueueReportMessage(state, { kind, periodKey, dayStr, text, line, phone, parentId })`**
(D-06 — genérico, acepta cualquier texto):
- guard D-28: si ya existe en `queue` o en `history` un item con el MISMO
  `kind` + `periodKey` y status `pending|sending|sent`, NO encola y devuelve
  `{ queued:false, reason:'periodo_ya_cubierto' }`.
- id: `` `rpt_${kind}_${periodKey}_${Date.now()}` ``.
- status `pending`, `attempts: 0`, `createdAt` ISO.
- devuelve `{ queued:true, id }`.

**1g. `_reportTransportReady(state)`** → `{ ok, userId, accountId, groupName,
groupJid, reason }`. `ok:false` con `reason:'sin_grupo'` si falta
`config.transport.groupName` (o accountId/userId).

**1h. `_reportDmFallback()`** (D-02): lee `process.env.REPORT_DM_FALLBACK` (CSV
de teléfonos E.164), filtra con `/^\+?\d{8,15}$/`, dedup, cap 5. Se resuelve por
env var y NO por UI a propósito: el panel de D-29 no tiene campo para esto y el
precedente del proyecto para listas de destinatarios es `REPORT_EMAILS`
(Phase 19). Documentarlo con un comentario.

**1i. Exponer** `globalThis.__reportQueue = { enqueueReportMessage, mutateReportsState, _reportStateDefaults, _reportPrune, _reportExpireStale, _reportGapNote, _reportTransportReady, _reportDmFallback };`
(en la Task 2 se le agregan `reportQueueTick` y los handlers).
  </action>

  <verify>
    <automated>node --check index.js && npx vitest run tests/weekly-report.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` sale 0.
    - `grep -c "REPORT_QUEUE_CAP\|REPORT_HISTORY_CAP\|REPORT_DAILY_EXPIRY_DAYS\|REPORT_SEND_TIMEOUT_MS" index.js` >= 8.
    - `grep -c "async function mutateReportsState(" index.js` = 1.
    - `grep -c "function enqueueReportMessage(\|function _reportPrune(\|function _reportExpireStale(\|function _reportGapNote(\|function _reportTransportReady(\|function _reportDmFallback(\|function _reportStateDefaults(" index.js` = 7.
    - `grep -c "globalThis.__reportQueue" index.js` >= 1.
    - **Cero archivos JSON nuevos:** `git diff -- index.js | grep -c "path.join(DATA_DIR, \"report"` = 0
      y `grep -c "report_queue.json" index.js scripts/pre-deploy.js` = 0.
    - `grep -c "periodo_ya_cubierto" index.js` >= 1 (guard D-28).
    - `npx vitest run tests/weekly-report.test.js` sale 0 (el normalizador no rompe el estado de Phase 19).
  </acceptance_criteria>

  <done>`reports.json` soporta config/estado/cola/historial con mutex, cap FIFO que nunca descarta pendientes, expiración de diarios a 3 días y guard por período cubierto — sin crear ningún archivo nuevo.</done>
</task>

<task type="auto">
  <name>Task 2: reportQueueTick (guard, emisión, correlación, consolidación, DM fallback) + listeners de socket con authz</name>

  <read_first>
    - `index.js:5187-5306` (`scheduledMessagesTick` completo + su registro con
      `setInterval`) — **el esqueleto se copia; la línea 5279 `msg.status='sent'`
      optimista es EXACTAMENTE lo que NO hay que replicar**.
    - `src/wa/gateway.js:150-320` (bloque de `socket.on(...)`: `account:status`,
      `account:event`, `wamulti:file-result`, `lead:contacted` — el patrón de
      listener con ownership check).
    - `index.js:15674-15706` (objeto `deps` de `mountWa`).
    - Los helpers de la Task 1.
    - `.planning/phases/21-reporte-diario-canal-whatsapp/21-RESEARCH.md` §Q5 y §Q7
      (L1 doble máquina, L4 `isUserOnline` no mide la sesión de WhatsApp).
  </read_first>

  <action>
**2a. `reportQueueTick(nowTs = Date.now())`** — async, envuelto en
`mutateReportsState`. Reglas, en orden:

1. `_reportExpireStale(state, nowTs)` (D-26).
2. **Timeout de lo que quedó `sending`**: item con `sendingAt` más viejo que
   `REPORT_SEND_TIMEOUT_MS` → `failed`, `lastFailureReason: 'timeout'`,
   `attempts++`. Si `attempts < REPORT_MAX_ATTEMPTS` vuelve a `pending`
   (reintento), si no queda `failed` definitivo.
3. **Un solo envío en vuelo**: si queda algún item en `sending` (no vencido),
   RETORNAR sin emitir. Con el tick cada 60s esto da el espaciado de ≥60s entre
   mensajes distintos que pide REP-08 (**no se aplican caps de warming: es un
   grupo propio, no outreach frío**).
4. Elegir el próximo `pending` (FIFO por `createdAt`). Si no hay, retornar.
5. **Consolidación (D-26)**: si el elegido es `kind:'daily'` y hay 2+ dailies
   `pending`, se toman TODOS los dailies pendientes ordenados por `dayStr`,
   `text = buildConsolidatedReportText(items.map(i => i.line), { gapNote, neverStarted })`,
   y los items se marcan juntos (`consolidatedInto` = id del primero). Sale UN
   solo mensaje, no N. `kind:'weekly'`/`'custom'`/`'dm'` NUNCA se consolidan.
6. **Guard de transporte**: `_reportTransportReady(state)`; si `!ok` → dejar
   `pending`, `lastFailureReason: 'sin_grupo'`, retornar.
7. **Guard de alcanzabilidad (REP-07)**:
   ```js
   const gw = globalThis.__waGateway;
   const online = !!(gw && typeof gw.isUserConnected === 'function' && gw.isUserConnected(t.userId));
   ```
   Si `!online` → dejar `pending`, `attempts++`, `lastFailureReason: 'desktop offline'`,
   retornar. **Jamás marcar `sent` sin confirmación** (ni siquiera si `sendToUser`
   devuelve `true`: devuelve `true` con la room vacía).
8. Emitir:
   ```js
   gw.sendToUser(t.userId, 'report:send-message', {
     queueId: item.id, accountId: t.accountId, text,
     target: item.kind === 'dm'
       ? { kind: 'dm', phone: item.phone }
       : { kind: 'group', groupName: t.groupName, groupJid: t.groupJid || null },
   });
   ```
   y marcar `status:'sending'`, `sendingAt: ISO`, `attempts++`.
9. Registro (patrón index.js:5302):
   ```js
   if (process.env.NODE_ENV !== 'test') {
     setInterval(() => reportQueueTick().catch(e => console.warn('[report-queue] tick:', e.message)), 60 * 1000);
     setTimeout(() => reportQueueTick().catch(() => {}), 15000);
   }
   ```

**2b. `handleReportSendResult(payload, user)`** — async, dentro de
`mutateReportsState`:
- **Authz (T-21-06)**: aceptar solo si `user.role === 'admin'` o
  `user.id === state.config.transport.userId`. Si no, `console.warn` y salir.
- Buscar el item por `queueId` en `queue`. Si no existe o no está en `sending`
  → salir en silencio (idempotente ante replays/doble máquina).
- `ok === true` → `status:'sent'`, `sentAt`, `method`, `matchedName`, `matchedJid`;
  marcar `confessedAt = sentAt` en los items que la nota de baches confesó;
  marcar también los consolidados hermanos como `sent`.
- **Backfill del JID (D-03)**: si `matchedJid` vino y
  `state.config.transport.groupJid` es null → guardarlo + `jidCapturedAt`.
- `ok === false`:
  - `reason === 'group-not-found'` y hay `_reportDmFallback()` → item padre
    `status:'failed'`, `lastFailureReason:'group-not-found'`, y **el SERVER
    orquesta el fallback (D-02)**: encolar un item `kind:'dm'` por teléfono
    (`periodKey: item.periodKey + '_dm' + i`, `parentId: item.id`, mismo `text`).
    Cada uno sale por el tick con su propio timeout y espaciado — más fácil de
    diagnosticar que meter la cadena dentro del desktop (recomendación de
    21-RESEARCH.md Q5).
  - `reason === 'account-not-connected'` → `failed` definitivo
    (`attempts = REPORT_MAX_ATTEMPTS`): no se resuelve solo, necesita que alguien
    reescanee el QR. La confesión de D-05 lo dice con ese texto.
  - cualquier otro reason → `pending` de nuevo si `attempts < REPORT_MAX_ATTEMPTS`,
    si no `failed`.
- Guardar `reason` en `lastFailureReason` siempre.

**2c. `handleReportGroupConfigured(payload, user)`** — async, dentro de
`mutateReportsState`:
- **Authz (T-21-07)**: SOLO `user.role === 'admin'`. Cambiar el destino del
  reporte es cambiar a quién le llegan datos nominales de empleadas.
- Validar: `accountId` string 1-64 chars, `groupName` string 1-100 chars
  (sin `\r\n`), `groupJid` null o `/^[\w.-]+@g\.us$/`.
- Persistir en `state.config.transport`: `userId: user.id`, `accountId`,
  `groupName`, `groupJid`, `configuredAt`, `configuredBy: user.id`.

**2d. Cablear en `src/wa/gateway.js`**, junto a `socket.on("lead:contacted", ...)`
(línea ~287), replicando ese patrón:
```js
    // Phase 21 — canal de reportes al grupo. El resultado del envío NO es un ack
    // de Socket.IO (sendToUser hace broadcast a room y los acks solo existen en
    // emits directos): viene como evento separado correlacionado por queueId.
    socket.on("report:send-result", async (payload = {}) => {
      try {
        if (typeof deps.onReportEvent === "function") {
          await deps.onReportEvent("send-result", payload, user);
        }
      } catch (err) {
        console.error("[wa-gateway] report:send-result error:", err?.message || err);
      }
    });
    socket.on("report:group-configured", async (payload = {}) => {
      try {
        if (typeof deps.onReportEvent === "function") {
          await deps.onReportEvent("group-configured", payload, user);
        }
      } catch (err) {
        console.error("[wa-gateway] report:group-configured error:", err?.message || err);
      }
    });
```
La authz vive en index.js (donde está el estado), NO en el gateway — mismo
criterio que `lead:contacted`, que delega en `deps.markLeadContacted`.

**2e. Inyectar el dep** en `index.js:15674` (`mountWa(app, server, {...})`),
después de `markLeadContacted`:
```js
  // Phase 21 — eventos del canal de reportes que reporta el desktop wa-multi.
  onReportEvent: (type, payload, user) =>
    (type === 'send-result' ? handleReportSendResult(payload, user)
     : type === 'group-configured' ? handleReportGroupConfigured(payload, user)
     : Promise.resolve()),
```

**2f. Actualizar** `globalThis.__reportQueue` con
`reportQueueTick, handleReportSendResult, handleReportGroupConfigured`.
  </action>

  <verify>
    <automated>node --check index.js && node --check src/wa/gateway.js && npx vitest run tests/wa.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` y `node --check src/wa/gateway.js` salen 0.
    - `grep -c "async function reportQueueTick(" index.js` = 1.
    - `grep -c "'report:send-message'" index.js` = 1.
    - `grep -c "report:send-result" src/wa/gateway.js` >= 1 y `grep -c "report:group-configured" src/wa/gateway.js` >= 1.
    - `grep -c "onReportEvent" index.js src/wa/gateway.js` >= 3.
    - **Anti fire-and-forget:** en el cuerpo de `reportQueueTick` NO existe
      `status = 'sent'` — `grep -A80 "async function reportQueueTick(" index.js | grep -c "status = 'sent'"` = 0;
      `grep -A80 "async function reportQueueTick(" index.js | grep -c "status = 'sending'"` >= 1.
    - **Guard REP-07 antes de emitir:** `grep -B20 "'report:send-message'" index.js | grep -c "isUserConnected"` >= 1.
    - `grep -c "buildConsolidatedReportText" index.js` >= 2 (definición del plan 01 + uso en el tick).
    - `npx vitest run tests/wa.test.js tests/hardening.test.js tests/security-rbac.test.js` sale 0.
  </acceptance_criteria>

  <done>El tick emite como máximo un mensaje por vuelta, solo con el desktop conectado, deja el item en `sending` hasta que llega el resultado correlacionado, consolida diarios pendientes, cae a mensajes individuales si el grupo no aparece, y los dos listeners de socket validan quién puede reportar y quién puede cambiar el destino.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/report-queue.test.js</name>

  <read_first>
    - `tests/weekly-report.test.js:14-72` (setup + fake sender inyectado).
    - `tests/wa.test.js` (patrón de tests del módulo WA / gateway).
    - Los helpers y el tick de las Tasks 1 y 2.
  </read_first>

  <behavior>
    - Encolar dos veces el mismo `kind`+`periodKey` → el segundo devuelve `queued:false, reason:'periodo_ya_cubierto'` (D-28).
    - Sin gateway (`globalThis.__waGateway` undefined) el tick NO marca `sent`: el item queda `pending` con `lastFailureReason` de offline.
    - Con gateway falso `isUserConnected → false`, idem: `pending`, `attempts` sube.
    - Con gateway falso `isUserConnected → true`: el tick emite exactamente 1 evento `report:send-message` con `queueId`, `accountId`, `text` y `target.kind === 'group'`, y el item queda en `sending` (nunca `sent`).
    - Un segundo tick con un item en `sending` no emite nada (un solo envío en vuelo = espaciado ≥60s).
    - `handleReportSendResult({queueId, ok:true, method:'pinned-row0', matchedJid:'123@g.us'})` de un user autorizado → item `sent` + `groupJid` backfilleado en la config.
    - `handleReportSendResult` de un user NO autorizado (setter cualquiera) → el item sigue en `sending` (evento ignorado).
    - `handleReportSendResult` con `queueId` inexistente → no lanza y no cambia nada.
    - Resultado `ok:false, reason:'group-not-found'` con `REPORT_DM_FALLBACK` seteada → se encolan 3 items `kind:'dm'` con `parentId` del padre (D-02).
    - Resultado `ok:false, reason:'account-not-connected'` → `failed` definitivo, sin reintento.
    - Timeout: item `sending` con `sendingAt` viejo → el tick lo pasa a `pending` (reintento) y luego de `REPORT_MAX_ATTEMPTS` a `failed`.
    - 3 dailies `pending` → un solo tick emite UN evento cuyo `text` contiene las 3 líneas y el encabezado "*Reporte acumulado · 3 días*" (D-26).
    - Un daily `pending` con `dayStr` de hace 4 días → `expired`; un `weekly` de hace 30 días sigue `pending` (nunca expira).
    - Tras una expiración, el siguiente envío exitoso lleva la nota de baches arriba (`No pude enviar el reporte de ...`) y esos items quedan con `confessedAt` (D-05).
    - `history` conserva a lo sumo 30 terminales (D-27) y los `pending` nunca se recortan.
    - Round-trip: `GET /api/admin/export-data` incluye `reports` con `config`/`queue`/`history`, y `POST /api/admin/import-data` los restaura.
  </behavior>

  <action>
Crear `tests/report-queue.test.js` con el setup de `tests/weekly-report.test.js`
(`DATA_DIR` en tmp, `auth.json` pre-escrito con un admin + un setter,
`RESEND_API_KEY=""`, `BUSINESS_TZ` fija) y `const Q = globalThis.__reportQueue;`.

Gateway falso (nunca tocar un socket real):
```js
const mkGateway = (online = true) => {
  const emitted = [];
  const gw = {
    isUserConnected: () => online,
    sendToUser: (userId, event, payload) => { emitted.push({ userId, event, payload }); return true; },
    getPresenceList: () => [],
  };
  gw.emitted = emitted;
  return gw;
};
// en cada test: globalThis.__waGateway = mkGateway(true)  /  delete globalThis.__waGateway
```
(⚠️ el `delete` acá es sobre `globalThis`, NO sobre `process.env` — la regla #121
es específica de las env vars.)

Sembrar el estado escribiendo `reports.json` en `DATA_DIR` antes de cada bloque
(los loaders leen de disco en cada llamada) y usar `nowTs` inyectado en
`reportQueueTick(nowTs)` para los casos de timeout/expiración — sin fake timers.

Para el round-trip de export/import, loguear como admin con `supertest` igual que
`tests/weekly-report.test.js:227-245`.
  </action>

  <verify>
    <automated>npx vitest run tests/report-queue.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/report-queue.test.js` sale 0 con >= 15 tests.
    - `grep -c "delete process.env" tests/report-queue.test.js` = 0.
    - `grep -c "vi.useFakeTimers" tests/report-queue.test.js` = 0.
    - El archivo tiene un test que afirma que el item queda en `'sending'` y NO en `'sent'` tras emitir.
    - `npx vitest run tests/daily-report.test.js tests/weekly-report.test.js tests/wa.test.js tests/metrics-consistency.test.js` sale 0.
  </acceptance_criteria>

  <done>La suite de la cola cubre las 15 conductas de `<behavior>` y pasa, incluido el caso que documenta por qué NO se reusó `scheduledMessagesTick` (el `sent` optimista).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket autenticado (cualquier rol) → estado de la cola | un evento `report:send-result` falso puede marcar como entregado un reporte que nunca salió |
| socket autenticado → config del canal | `report:group-configured` decide a QUÉ grupo van los datos nominales de las empleadas |
| server → desktop | `report:send-message` hace que el desktop TIPEE texto arbitrario en WhatsApp |
| `reports.json` en el Railway Volume | contiene el historial de mensajes con nombres y métricas de personas reales |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Spoofing | `handleReportSendResult` | mitigate | aceptar solo si `user.role === 'admin'` o `user.id === config.transport.userId`; ignorar `queueId` desconocido o en estado distinto de `sending` (idempotente ante replay) |
| T-21-07 | Tampering | `handleReportGroupConfigured` | mitigate | admin-only; `groupName` validado (1-100 chars, sin `\r\n`), `groupJid` contra `/^[\w.-]+@g\.us$/`; se persiste `configuredBy` para auditoría |
| T-21-08 | Elevation of privilege | `report:send-message` (desktop tipea texto) | mitigate | el server solo lo emite desde la cola, y a la cola solo se encola desde el cron o desde endpoints admin-only (plan 21-03). El desktop además valida el payload (plan 21-05: longitud, `kind` en whitelist, dedupe por `queueId`) |
| T-21-09 | Repudiation | correlación del envío | mitigate | estado intermedio `sending` + resultado correlacionado por `queueId` + `history` con `method`/`reason`/`matchedJid`. Se elimina el `status='sent'` optimista de `scheduledMessagesTick`, que hacía imposible saber si un mensaje salió |
| T-21-10 | Denial of service | crecimiento de `reports.json` | mitigate | `REPORT_QUEUE_CAP` 200 + `REPORT_HISTORY_CAP` 30 + expiración de diarios a 3 días; los `pending` nunca se recortan (patrón `saveScheduledMessages`) |
| T-21-11 | Information disclosure | envío al chat equivocado | mitigate | el payload lleva `groupJid` como checksum y el desktop aborta con `jid-mismatch` antes de tipear (plan 21-05); el fallback DM usa solo teléfonos de `REPORT_DM_FALLBACK` (env, no editable desde la UI) |
| T-21-12 | Denial of service | doble máquina con wa-multi abierto | mitigate | `sendToUser` es broadcast a room: el desktop deduplica por `queueId` (plan 21-05, L1 del research) y el server ignora el segundo `report:send-result` porque el item ya no está en `sending` |
</threat_model>

<verification>
- `node --check index.js && node --check src/wa/gateway.js` salen 0.
- `npx vitest run tests/report-queue.test.js tests/daily-report.test.js tests/weekly-report.test.js tests/wa.test.js tests/security-rbac.test.js tests/hardening.test.js` sale 0.
- `grep -c "report_queue.json\|reports_queue.json" index.js scripts/pre-deploy.js` = 0 (se reusó `reports.json`; cero registro de persistencia nuevo).
- `git diff --stat` muestra SOLO `index.js`, `src/wa/gateway.js` y `tests/report-queue.test.js`.
</verification>

<success_criteria>
- Con la desktop apagada N días, al reconectar sale UN mensaje consolidado; los diarios de más de 3 días expiran; el semanal nunca expira; sin duplicados por período cubierto (criterio 4 del ROADMAP).
- El guard de alcanzabilidad corre ANTES de emitir y `sendToUser` no se usa como señal de éxito (REP-07).
- Tests de la cola y de la consolidación (criterio 7 del ROADMAP, parte 2 de 2).
</success_criteria>

<output>
Al terminar, crear `.planning/phases/21-reporte-diario-canal-whatsapp/21-02-SUMMARY.md`.
</output>
