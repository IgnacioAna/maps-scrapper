---
phase: 34-hoy-vista-diaria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.js
  - tests/hoy-hygiene-snapshot.test.js
autonomous: true
requirements: [HOY-04, HOY-05]
must_haves:
  truths:
    - "GET /api/setters/leads/sin-wsp devuelve el próximo paso RESUELTO de cada lead (lead.nextAction), no el crudo — un lead legacy sin migrar responde igual que uno migrado"
    - "El front puede persistir el conteo de hoy (vencidos + red de seguridad) y recibir a cambio el de ayer y la tendencia, sin tener que calcular nada server-side"
    - "Un setter solo puede grabar/leer su propio scope; un supervisor scoped no puede pisar el scope de otro ni ver el de un setter fuera de su visibilidad"
  artifacts:
    - path: "index.js"
      provides: "l.nextAction resuelto en /leads/sin-wsp + POST /api/setters/hoy-hygiene-snapshot"
      contains: "app.post('/api/setters/hoy-hygiene-snapshot'"
    - path: "tests/hoy-hygiene-snapshot.test.js"
      provides: "Suite HTTP: RBAC, coerción de input, cálculo de tendencia, poda a 14 días"
  key_links:
    - from: "GET /api/setters/leads/sin-wsp"
      to: "_leadNextAction(l)"
      via: "resolución server-side del reloj único para cada lead de la respuesta"
      pattern: "l\\.nextAction = _leadNextAction\\(l\\)"
    - from: "POST /api/setters/hoy-hygiene-snapshot"
      to: "data.hoyHygieneSnapshots"
      via: "persistencia dentro de setters.json (sin archivo nuevo, sin wiring de export/pre-deploy)"
      pattern: "hoyHygieneSnapshots"
---

<objective>
Dos cambios de backend, ambos condición necesaria para que la Phase 34
pueda reordenar Hoy en el frontend (34-02/34-03):

1. `GET /api/setters/leads/sin-wsp` deja de devolver el campo `nextAction`
   crudo (que puede quedar `null` para leads legacy sin migrar, aunque el
   próximo paso SÍ sea derivable de `callbackAt`/`followUps`) y en su lugar
   devuelve el valor RESUELTO vía `_leadNextAction(l)` — la misma función que
   ya usa el endpoint para calcular `manualCallbackByOwner`. Sin esto, la
   clasificación en 5 secciones de 34-02 clasificaría mal cualquier lead
   todavía no migrado por el backfill de la Phase 29.

2. `POST /api/setters/hoy-hygiene-snapshot` — el backend de HOY-05 (D-12/D-13):
   recibe del frontend los 2 números que YA computó al clasificar Hoy
   (cuántos "vencen hoy", cuántos "tocados sin próximo paso"), los persiste
   con fecha (TZ de negocio) dentro de `setters.json` — sin archivo nuevo, sin
   wiring de export/pre-deploy/backup (regla #21: es una key más del objeto
   que `loadSettersData()`/`saveSettersData()` ya redondean completo) — y
   devuelve el número de AYER + la tendencia (creciendo/bajando/estable), que
   es el KPI de saturación que D-13 pide.

Purpose: separar "qué cuenta como cada tier" (lógica de negocio, sigue
viviendo en el frontend igual que hoy) de "resolver el reloj de un lead
legacy" (ya existe server-side, solo falta exponerlo) y de "recordar cuánto
había ayer" (necesita persistencia, que solo el backend puede dar).

Output: `l.nextAction` resuelto en la respuesta de `/leads/sin-wsp`; el
endpoint nuevo con RBAC/scope consistente con el resto de la app; 
`tests/hoy-hygiene-snapshot.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-hoy-vista-diaria/34-CONTEXT.md
@.planning/research/2026-08-13-requisitos-seguimiento-ignacio.md
@CLAUDE.md

Referencia canónica — el modelo del reloj único y por qué `_leadNextAction`
es el único lector autorizado:
@.planning/phases/29-next-reloj-unico/29-04-SUMMARY.md
@.planning/phases/33-dial-motor-unico/33-04-SUMMARY.md
</context>

<interfaces>
<!-- Contratos vigentes en index.js. Leer los rangos de <read_first> antes de editar. -->

Único lector autorizado del reloj (index.js ~11688, PURA):
```js
function _leadNextAction(lead) {
  if (lead && typeof lead.nextAction === 'object' && lead.nextAction !== null) return lead.nextAction;
  return _deriveNextActionFromLegacy(lead); // callbackAt gana; si no, followUps activo; si no, null
}
```

`GET /api/setters/leads/sin-wsp` (index.js ~8880-8977) — el loop donde YA se
computan campos derivados por lead, antes de `res.json({ leads, userNames })`:
```js
for (const l of leads) {
  l.calledByOwner = _setterCalledLead(l, l.assignedTo, _swUserMap);
  const _log = Array.isArray(l.callLog) ? l.callLog : [];
  const _last = _log.length ? _log[_log.length - 1] : null;
  const _na = _leadNextAction(l);
  l.manualCallbackByOwner = !!(_na && _na.dueAt && _na.origen === 'manual'
    && _callSetterId(_last, l, _swUserMap) === l.assignedTo);
}
```
Nota: `l` acá es un objeto ya SPREADEADO (`{ id, ...l }`, línea ~8921) — no es
el lead persistido, así que escribirle un campo NO muta `data.leads`.

Patrón de scope/RBAC ya establecido (index.js ~8171-8193, `cold-call-metrics`):
```js
const eff = getEffectiveAuth(req);
let setterId = '';
if (eff.role === 'setter') { if (!eff.setterId) return res.status(403)...; setterId = eff.setterId; }
else if (eff.role === 'admin' || eff.role === 'supervisor') { setterId = req.query.setter || ''; }
else return res.status(403)...;
const visibleSet = _visibleSetterIds(req.auth.user);
if (visibleSet && req.query.setter && !visibleSet.has(req.query.setter)) return res.status(403)...;
```

Helpers de fecha en TZ de negocio (index.js ~7872-7886, expuestos en
`globalThis.__metricsAudit`):
```js
function _bizOffsetMs(ts = Date.now()) { ... }
function _bizStartOfDay(ts = Date.now()) { ... }
function _bizDayStr(ts) { return new Date(ts + _bizOffsetMs(ts)).toISOString().slice(0, 10); }
```

Escritura sync de `setters.json` sin mutex (index.js ~10580, `PATCH .../followup`
— mismo shape: `loadSettersData()` → mutar → `saveSettersData(data)`, CERO
`await` entre medio, por eso no necesita `mutateSettersData`):
```js
app.patch('/api/setters/leads/:id/followup', requireAuth, (req, res) => {
  const data = loadSettersData();
  ...
  saveSettersData(data);
  res.json({ ok: true, ... });
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: nextAction resuelto en /leads/sin-wsp + endpoint de snapshot de higiene</name>

  <read_first>
    - `index.js` líneas 8880-8977 (`GET /api/setters/leads/sin-wsp` completo, incluido el loop de campos derivados)
    - `index.js` líneas 8171-8299 (`cold-call-metrics`, el patrón de scope/RBAC a reusar, y dónde termina — el nuevo endpoint va justo después, antes de `team/:id/calls-today`)
    - `index.js` líneas 7872-7959 (`_bizOffsetMs`/`_bizStartOfDay`/`_bizDayStr`, `_buildUserSetterMap`, `_setterCalledLead`, `ADMIN_ONLY_SETTER_IDS`/`_visibleSetterIds`)
    - `index.js` líneas 11605-11691 (`_setNextAction`/`_clearNextAction`/`_deriveNextActionFromLegacy`/`_leadNextAction` — no se tocan, solo se consumen)
    - `index.js` líneas 540-575 (`requireRole`/`getEffectiveAuth`)
  </read_first>

  <action>
    Trabajar SOLO sobre `index.js`.

    1. **`l.nextAction` resuelto en `/leads/sin-wsp`**: dentro del loop
       existente `for (const l of leads) { l.calledByOwner = ...; ... }`
       (~línea 8957-8975), agregar una línea que sobreescriba el campo con el
       valor RESUELTO:
       ```js
       // Fase 34 (HOY-04/HOY-05): resolver el reloj ACÁ, no en el frontend —
       // un lead legacy sin migrar (nextAction:null en disco pero derivable de
       // callbackAt/followUps) tiene que responder IGUAL que uno ya migrado.
       // Es un no-op para leads migrados: _leadNextAction devuelve la MISMA
       // referencia cuando lead.nextAction ya es un objeto.
       l.nextAction = _leadNextAction(l);
       ```
       Colocarla junto a la línea `const _na = _leadNextAction(l);` que ya
       existe (reusar esa misma variable si es más prolijo: `l.nextAction = _na;`
       ANTES de usar `_na` para `manualCallbackByOwner`, o después — el orden
       no importa, `_na` es puro). NO tocar el resto del loop.

    2. **`_hoyHygieneScope(req, requestedSetter)`** — helper nuevo, declarado
       junto a `_visibleSetterIds` (o inmediatamente antes del endpoint
       nuevo). Devuelve la clave de persistencia:
       ```js
       // Fase 34 (HOY-05, D-12/D-13): a qué "cajón" de hoyHygieneSnapshots
       // escribe cada request. Un supervisor SCOPED sin setter puntual usa su
       // propio userId como clave — su agregado ("mi equipo") es un scope
       // distinto del agregado global del admin, y dos supervisores distintos
       // no deben pisarse entre sí.
       function _hoyHygieneScope(req, requestedSetter) {
         const eff = getEffectiveAuth(req);
         if (eff.role === 'setter') return eff.setterId || '';
         if (requestedSetter) return requestedSetter;
         const visibleSet = _visibleSetterIds(req.auth.user);
         return visibleSet ? ('supervisor_' + (req.auth.user.id || '')) : '__all__';
       }
       ```

    3. **`POST /api/setters/hoy-hygiene-snapshot`** — insertar inmediatamente
       después del `});` que cierra `cold-call-metrics` (~línea 8299), antes
       de `app.get('/api/setters/team/:id/calls-today', ...)`:
       ```js
       app.post('/api/setters/hoy-hygiene-snapshot', requireAuth, (req, res) => {
         const eff = getEffectiveAuth(req);
         if (!['admin', 'supervisor', 'setter'].includes(eff.role)) {
           return res.status(403).json({ error: 'No autorizado.' });
         }
         const requestedSetter = eff.role === 'setter' ? '' : String(req.body?.setter || '');
         if (eff.role !== 'setter') {
           const visibleSet = _visibleSetterIds(req.auth.user);
           if (visibleSet && requestedSetter && !visibleSet.has(requestedSetter)) {
             return res.status(403).json({ error: 'Setter fuera de tu visibilidad.' });
           }
         }
         const scope = _hoyHygieneScope(req, requestedSetter);
         // Whitelist-and-coerce (mismo idioma que el resto del proyecto): un
         // body raro nunca tira 4xx, se clampea a 0.
         const toCount = (v) => { const n = Math.trunc(Number(v)); return Number.isFinite(n) && n >= 0 ? n : 0; };
         const vencidos = toCount(req.body?.vencidos);
         const redSeguridad = toCount(req.body?.redSeguridad);
         const data = loadSettersData();
         if (!data.hoyHygieneSnapshots || typeof data.hoyHygieneSnapshots !== 'object') data.hoyHygieneSnapshots = {};
         if (!data.hoyHygieneSnapshots[scope] || typeof data.hoyHygieneSnapshots[scope] !== 'object') data.hoyHygieneSnapshots[scope] = {};
         const bucket = data.hoyHygieneSnapshots[scope];
         const now = Date.now();
         const todayStr = _bizDayStr(now);
         const yesterdayStr = _bizDayStr(_bizStartOfDay(now) - 1);
         bucket[todayStr] = { vencidos, redSeguridad, updatedAt: new Date(now).toISOString() };
         // Poda: últimas 14 fechas por scope (mismo criterio que el cap de
         // callLog/interactions, CLAUDE.md nota #28 — evita crecimiento sin fin).
         const keys = Object.keys(bucket).sort();
         if (keys.length > 14) for (const k of keys.slice(0, keys.length - 14)) delete bucket[k];
         saveSettersData(data);
         const yesterday = bucket[yesterdayStr] || null;
         let trend = 'sin_dato_previo';
         if (yesterday) {
           trend = vencidos > yesterday.vencidos ? 'creciendo' : vencidos < yesterday.vencidos ? 'bajando' : 'estable';
         }
         res.json({
           scope, date: todayStr,
           today: { vencidos, redSeguridad, date: todayStr },
           yesterday: yesterday ? { vencidos: yesterday.vencidos, redSeguridad: yesterday.redSeguridad, date: yesterdayStr } : null,
           trend,
         });
       });
       ```
       Sin `await` en ningún punto del handler → no necesita `mutateSettersData`
       (mismo criterio documentado en `PATCH .../followup`).

    4. Exponer `_hoyHygieneScope` en `globalThis.__metricsAudit` (agregarlo al
       objeto existente, ~línea 7959) por si algún test futuro quiere
       evaluarlo aislado — no es estrictamente necesario para este plan (los
       tests de la Task 2 pueden probarlo vía HTTP), pero es gratis y sigue el
       patrón `globalThis.__X` ya establecido en el archivo.
  </action>

  <verify>
    <automated>node --check index.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` sale con código 0.
    - `grep -c "app.post('/api/setters/hoy-hygiene-snapshot'" index.js` devuelve `1`.
    - `grep -c "l.nextAction = _leadNextAction(l)" index.js` devuelve `1`, dentro del cuerpo de `GET /api/setters/leads/sin-wsp` (verificable con `extractFunctionBody`/búsqueda de rango en la Task 2).
    - `grep -c "hoyHygieneSnapshots" index.js` es `>= 3` (creación del objeto, del bucket por scope, y su uso en `res.json`).
    - El endpoint nuevo NO usa `mutateSettersData` (no hay `await` en su cuerpo).
    - `git diff --stat package.json package-lock.json` vacío (sin instalar nada).
  </acceptance_criteria>

  <done>
    `/leads/sin-wsp` devuelve el próximo paso resuelto para cualquier lead,
    migrado o no. El endpoint de snapshot persiste y compara sin tocar ningún
    archivo nuevo ni requerir wiring de export/pre-deploy.
  </done>
</task>

<task type="auto">
  <name>Task 2: Suite HTTP de HOY-04/HOY-05 (backend)</name>

  <read_first>
    - `tests/hangup-cap.test.js` líneas 1-48 (boilerplate mínimo: `tmpData`, `auth.json`/`setters.json` de fixture, teléfonos de **7+ dígitos** — nota #163, cookie de login)
    - `tests/next-action-model.test.js` líneas 225-251 (cómo se testea hoy `nextAction` contra `/leads/sin-wsp` — no romper esos 2 casos, solo sumar cobertura nueva)
    - `tests/next-action-disposition.test.js` líneas 196-202 (el caso legacy `l_legacy` — mismo criterio a extender)
  </read_first>

  <action>
    Crear `tests/hoy-hygiene-snapshot.test.js` (mínimo 14 tests), mismo molde
    que `tests/hangup-cap.test.js` (fixture inline, sin `DATA_DIR` compartido,
    `OPENAI_API_KEY`/`MERCURY_API_KEY`/`QWEN_API_KEY` seteadas a `""`, nunca
    `delete` — nota #121 de CLAUDE.md). Cobertura obligatoria:

    **`l.nextAction` resuelto:**
    - Un lead con `callbackAt` seteado pero `nextAction: null` en el fixture
      (legacy, sin migrar) responde en `/leads/sin-wsp` con `l.nextAction`
      NO nulo, `dueAt` igual a `callbackAt`, `origen: 'manual'` (si el último
      callLog es `callback_later`) — mismo criterio que
      `_deriveNextActionFromLegacy`.
    - Un lead YA migrado (`nextAction` explícito en el fixture) responde con
      ese mismo objeto (test de contenido, no de referencia — la respuesta
      pasa por JSON así que la identidad de objeto no aplica en la aserción
      HTTP, pero el valor debe ser idéntico byte a byte).
    - Un lead sin `callbackAt` ni `followUps` activo sigue respondiendo
      `nextAction: null` (caso ya cubierto por `next-action-model.test.js`,
      replicarlo acá para que la suite de esta fase sea autocontenida).

    **`POST /api/setters/hoy-hygiene-snapshot`:**
    - Un setter puede grabar su propio snapshot (200, `today.vencidos`/`today.redSeguridad`
      igual a lo mandado).
    - Primera vez del día → `yesterday: null`, `trend: 'sin_dato_previo'`.
    - Segunda llamada el MISMO día (día de negocio, no reloj real) con un
      número mayor → sigue `trend: 'sin_dato_previo'` (todavía no hay "ayer"),
      pero `today` se actualiza al último valor mandado (upsert, no acumula).
    - Con un bucket de ayer sembrado directo en el fixture (`data.hoyHygieneSnapshots`)
      con un `vencidos` menor que el de hoy → `trend: 'creciendo'`; mayor →
      `'bajando'`; igual → `'estable'`.
    - Un `setter` no puede mandar `{setter: 'otro_setter_id'}` para escribir
      en el scope de otro (se ignora, escribe en el suyo propio).
    - Body con `vencidos: -5` o `vencidos: 'texto'` → coerciona a `0`, nunca
      400.
    - Sin auth → 401/403 (según el patrón estándar del proyecto).
    - La poda a 14 días: sembrar 20 fechas en el fixture para un scope, hacer
      un POST, y verificar que el bucket resultante tiene como máximo 14
      claves (usar `require`/`fs.readFileSync` sobre `DATA_DIR/setters.json`
      tras el POST, o un segundo request que lea el estado si existe una vía
      — si no hay endpoint de lectura directa, leer el archivo del `tmpData`
      igual que hacen otros tests de este proyecto contra `setters.json`).

    **RBAC de scope**: un supervisor SCOPED (con `visibleSetterIds`) pidiendo
    `?setter=` de un setter fuera de su visibilidad → 403 (mismo patrón que
    `cold-call-metrics`).
  </action>

  <verify>
    <automated>npx vitest run tests/hoy-hygiene-snapshot.test.js tests/next-action-model.test.js tests/next-action-disposition.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/hoy-hygiene-snapshot.test.js` pasa con al menos 14 tests y 0 fallos.
    - `tests/next-action-model.test.js` y `tests/next-action-disposition.test.js` pasan SIN editarlos (`git diff --stat` de ambos vacío) — confirma que resolver `l.nextAction` no rompió los 2 casos existentes que ya lo chequean.
    - `npm test` completo: 0 fallos nuevos respecto del baseline actual (1801 en 106 archivos, según el prompt de esta sesión — reconfirmar el número real en disco antes de comparar).
  </acceptance_criteria>

  <done>
    HOY-04 (soporte de backend para clasificar por reloj resuelto) y HOY-05
    (persistencia + tendencia) quedan protegidos por tests permanentes. La
    suite completa del repo sigue verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → `POST /hoy-hygiene-snapshot` | body `{vencidos, redSeguridad, setter}` es input NO confiable — un número o un scope arbitrario |
| respuesta de `/leads/sin-wsp` → clasificación de Hoy (34-02/34-03) | `l.nextAction` resuelto pasa a ser la fuente que decide en qué sección cae cada lead |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | `vencidos`/`redSeguridad` del body | mitigate | `toCount()` coerciona a entero `>= 0`, nunca confía en el valor crudo del cliente — el propio scope que lo mandó es el único afectado si miente (no hay impacto cross-user) |
| T-34-02 | Elevation of Privilege | `setter` del body para escribir/leer el scope de otro | mitigate | Un `setter` real siempre escribe en `eff.setterId` (ignora el body); admin/supervisor pasan por el mismo chequeo `visibleSet.has()` que `cold-call-metrics` |
| T-34-03 | Information Disclosure | un supervisor scoped podría intentar leer/pisar el snapshot `'__all__'` (agregado global) | mitigate | El agregado sin setter puntual de un supervisor scoped usa `'supervisor_' + userId`, nunca `'__all__'` — no puede tocar la clave del admin |
| T-34-04 | Denial of Service | crecimiento sin límite de `hoyHygieneSnapshots` (un scope nuevo por cada supervisor/setter, cada uno con fechas indefinidas) | mitigate | Poda a 14 fechas por scope en cada write; el número de scopes está acotado por la cantidad de users reales del sistema (no crece con requests) |
| T-34-SC | Tampering | instalación de paquetes npm | mitigate | Este plan no instala nada. `git diff --stat package.json package-lock.json` vacío al cerrar |
</threat_model>

<verification>
1. `node --check index.js`.
2. `npx vitest run tests/hoy-hygiene-snapshot.test.js` — suite nueva verde.
3. `npx vitest run tests/next-action-model.test.js tests/next-action-disposition.test.js` — sin editarlos.
4. `npm test` completo contra el baseline actual del repo.
5. `git diff --stat package.json package-lock.json` vacío.
</verification>

<success_criteria>
- `GET /api/setters/leads/sin-wsp` responde `l.nextAction` siempre resuelto (nunca el crudo `null` de un lead legacy con próximo paso derivable).
- `POST /api/setters/hoy-hygiene-snapshot` persiste el snapshot del día, compara contra ayer, y nunca 400 por un input raro.
- El scope respeta la visibilidad de Phase 18 (setter propio, supervisor scoped, admin sin restricción).
- Cero archivos nuevos de datos — todo vive dentro de `setters.json`, sin wiring adicional de export/pre-deploy.
</success_criteria>

<output>
Crear `.planning/phases/34-hoy-vista-diaria/34-01-SUMMARY.md` al terminar.

Documentar explícitamente:
- El baseline REAL de `npm test` leído en disco antes de empezar (no asumir 1801).
- Confirmación de que `next-action-model.test.js`/`next-action-disposition.test.js` no necesitaron edición.
- Qué queda para 34-02/34-03: el frontend todavía NO consume ninguno de estos 2 cambios — esta es la única pieza de la fase que corre sola, sin dependencia de frontend.
</output>
