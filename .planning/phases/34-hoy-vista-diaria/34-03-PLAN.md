---
phase: 34-hoy-vista-diaria
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - public/app.js
  - public/index.html
  - tests/hoy-dialer-hygiene.test.js
autonomous: true
requirements: [HOY-03, HOY-05]
must_haves:
  truths:
    - "Cada una de las 5 secciones reclamables de Hoy (Mis compromisos, Esperando del prospecto, Callbacks, Interesados, Reintentos) tiene su propio botón de Power Dialer, visible junto al contador de cuántas quedan"
    - "El botón general del header de Hoy encadena las 5 secciones en el orden de D-01, no solo callbacks+interesados como hasta ahora"
    - "El panel de higiene muestra 3 números — nunca un 0 desnudo: en cero dice explícitamente que está al día"
    - "El tercer número (tendencia) compara contra el snapshot de ayer y avisa si la cola de vencidos está creciendo"
    - "Los números del panel de higiene NO cambian cuando el SDR cambia el filtro de país — miden todo el pipeline, siempre, aunque en pantalla se esté viendo un solo país"
  artifacts:
    - path: "public/app.js"
      provides: "_pdBuildQueueHoy reescrito para 5 sub-colas + _hoyRenderHygienePanel (clasifica sobre allLeadsForHygiene)"
      contains: "function _hoyRenderHygienePanel"
    - path: "public/index.html"
      provides: "div#hoy-hygiene en el header de Hoy"
      contains: "hoy-hygiene"
    - path: "tests/hoy-dialer-hygiene.test.js"
      provides: "Suite de HOY-03/HOY-05: cola por sub-filtro, chain completo, panel D-14 (nunca cero desnudo), invarianza al filtro de país"
  key_links:
    - from: "_hoyRenderSection(..., 'hoy-compromisos', ...)"
      to: "window._pdStart('hoy-compromisos')"
      via: "el botón de la sección arranca el dialer filtrado a ese tier"
      pattern: "hoy-compromisos"
    - from: "_hoyRenderHygienePanel"
      to: "POST /api/setters/hoy-hygiene-snapshot"
      via: "el frontend manda los 2 conteos, calculados sobre allLeadsForHygiene (34-02), el backend devuelve la tendencia"
      pattern: "hoy-hygiene-snapshot"
    - from: "allLeadsForHygiene (34-02)"
      to: "_hoyState.hygiene"
      via: "clasificación PARALELA sin filtro de país, con su propio claimed set — nunca reusa callbacks.length/misCompromisos.length/etc. (esos arrays sí están filtrados por país)"
      pattern: "allLeadsForHygiene"
---

<objective>
Cierra la fase: extiende el Power Dialer por sección (D-07) a las 3
secciones nuevas de 34-02 que quedaron con `dialerMode: null` a propósito
(Mis compromisos, Esperando del prospecto, Reintentos de no-contacto), y
cablea el panel de higiene (HOY-05) contra el endpoint que dejó 34-01.

Purpose: D-07 pide explícito "extenderlo a las secciones que no lo tienen y
unificar el botón" — con `_hoyBuildQueueHoy` reescrito, el botón GENERAL del
header (que hoy solo encadena callbacks→interesados) automáticamente
empieza a cubrir las 5 secciones, sin tocar su HTML: es la misma función que
decide qué entra a la cola.

**Blocker del checker (2026-08-16), resuelto en este plan**: el panel de
higiene (D-12/D-13) mide "cuántos vencen hoy" y "cuántos quedaron en la red
de seguridad" sobre TODO el pipeline del SDR, nunca sobre el subconjunto que
el filtro de país (34-02) esté mostrando en ese instante — el filtro de país
es una preferencia visual que D-05 espera que se cambie seguido durante el
día, y el snapshot que este plan persiste vía `POST /hoy-hygiene-snapshot`
se pisa en cada `loadHoyView()`. Por eso `_hoyRenderHygienePanel` clasifica
sobre `allLeadsForHygiene` (la copia sin filtrar que dejó 34-02), con una
pasada de clasificación PROPIA — nunca reusando `callbacks.length`/
`misCompromisos.length`/etc., que sí están recortados por país.

Output: `_pdBuildQueueHoy` cubriendo 5 sub-filtros + el chain completo;
botones de dialer en las 3 secciones que faltaban; `_hoyRenderHygienePanel`
(invariante al filtro de país); `tests/hoy-dialer-hygiene.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-hoy-vista-diaria/34-CONTEXT.md
@.planning/phases/34-hoy-vista-diaria/34-01-SUMMARY.md
@.planning/phases/34-hoy-vista-diaria/34-02-SUMMARY.md
@.planning/research/2026-08-13-requisitos-seguimiento-ignacio.md
@CLAUDE.md

Referencia canónica — el Power Dialer por sección tal como lo dejaron las
notas #179/#180/#181 de CLAUDE.md y la Fase 33 (motor único, hold universal):
@.planning/phases/33-dial-motor-unico/33-01-SUMMARY.md
@.planning/phases/33-dial-motor-unico/33-02-SUMMARY.md
</context>

<interfaces>
<!-- Contratos vigentes en public/app.js. Leer los rangos de <read_first> antes de editar. -->

`_pd` (app.js ~6647), `mode`/`hoyFilter` son los 2 campos relevantes acá:
```js
const _pd = {
  active: false,
  mode: 'calls',      // 'calls' | 'hoy'
  hoyFilter: null,    // en modo hoy: null (todo) | 'callbacks' | 'interesados' — ESTE plan suma 'compromisos' | 'esperando' | 'reintentos'
  queue: [], currentIdx: 0, ...
};
```

`_pdBuildQueueHoy(filter)` (app.js ~6812-6819) — la función que ESTE plan
reescribe por completo:
```js
function _pdBuildQueueHoy(filter) {
  const now = Date.now();
  const due = filter === 'interesados' ? [] : _hoyState.callbackIds
    .map(id => _callsLeadsById.get(id))
    .filter(l => l && l.callbackAt && new Date(l.callbackAt).getTime() <= now);
  const interesados = filter === 'callbacks' ? [] : _hoyState.interesadoIds.map(id => _callsLeadsById.get(id)).filter(Boolean);
  return due.concat(interesados).map(l => l.id);
}
```

`window._pdStart` (app.js ~6820-6915) — el parseo de `mode` y los mensajes
de toast que ESTE plan extiende (NO reescribir la función entera, solo estos
2 fragmentos):
```js
const isHoy = typeof mode === 'string' && mode.startsWith('hoy');
_pd.mode = isHoy ? 'hoy' : 'calls';
_pd.hoyFilter = mode === 'hoy-callbacks' ? 'callbacks' : mode === 'hoy-interesados' ? 'interesados' : null;
...
if (_pd.queue.length === 0 && !hasResolvableSeed) {
  if (_pd.mode === 'hoy') {
    const later = (_pd.hoyFilter === 'interesados' || !_hoyState.callbackIds.length) ? 0
      : _hoyState.callbackIds.map(id => _callsLeadsById.get(id)).filter(l => l && l.callbackAt && new Date(l.callbackAt).getTime() > Date.now()).length;
    window.showToast?.(later
      ? `Nada para discar ahora — ${later} callback${later > 1 ? 's' : ''} de hoy entra${later > 1 ? 'n' : ''} a su hora.`
      : (_pd.hoyFilter === 'interesados' ? 'Sin interesados por agendar.'
        : _pd.hoyFilter === 'callbacks' ? 'Sin callbacks pendientes hoy.'
        : 'Sin seguimientos pendientes en Hoy.'), { type: 'info', duration: 3500 });
  } else { ... }
  return;
}
...
window.showToast?.(_pd.mode === 'hoy'
  ? (_pd.hoyFilter === 'callbacks' ? `Power dialer · ${_n} callback${_n > 1 ? 's' : ''} en cola`
    : _pd.hoyFilter === 'interesados' ? `Power dialer · ${_n} interesado${_n > 1 ? 's' : ''} por agendar en cola`
    : `Power dialer · ${_n} seguimiento${_n > 1 ? 's' : ''} de Hoy en cola`)
  : `Power dialer activado · ${_n} leads en cola`, { type: 'success', duration: 2500 });
```

`_hoyState` (Fase 34-02 la deja con `retryIds` + `hygiene` sin poblar
todavía) — este plan suma `_hoyState.hygiene`:
```js
_hoyState = {
  callbackIds, interesadoIds, commitYoIds, commitProspectoIds, retryIds, at,
};
```

`allLeadsForHygiene` (Fase 34-02, dentro del cuerpo de `_hoyRenderFromStore`,
declarada ANTES de que el filtro de país recorte `leads`) — la única fuente
válida para `_hoyState.hygiene`:
```js
const allLeadsForHygiene = leads;
const countryFilter = (typeof _hoySelectedCountry === 'function') ? _hoySelectedCountry() : '';
if (countryFilter) leads = leads.filter(l => (l.country || '').trim() === countryFilter);
```
**NO usar `callbacks`, `misCompromisos`, `compromisosProspecto`, `reintentos`
ni `redSeguridad` para calcular `_hoyState.hygiene`** — esos 5 arrays se
computan más abajo, sobre `leads` YA filtrado por país (34-02, Task 1). Son
correctos para lo que se RENDERIZA en pantalla, pero el panel de higiene
necesita medir el pipeline completo (ver Objective, blocker del checker).

`secEl.innerHTML` de `_hoyRenderFromStore` tal como lo dejó 34-02 (3
secciones con `dialerMode: null` que ESTE plan activa):
```js
secEl.innerHTML =
  _hoyRenderSection('Mis compromisos', misCompromisos, 'var(--warning)', 'Le prometí algo — vence hoy', null, { rowBadge: _hoyCommitBadge }) +
  _hoyRenderSection('Esperando del prospecto', compromisosProspecto, 'var(--accent)', 'Se comprometió él — el plazo venció', null, { rowBadge: _hoyCommitBadge }) +
  _hoyRenderSection('Callbacks', callbacks, '#5BA3F2', 'Quedaron en volver a contactar', 'hoy-callbacks') +
  _hoyRenderSection('Interesados sin agendar', interesados, 'var(--accent-hover)', 'Marcaron interés — agendar', 'hoy-interesados') +
  _hoyRenderSection('Reintentos de no-contacto', reintentos, '#8892A6', 'No atendieron — bajo esfuerzo, alto volumen', null) +
  (tier123Empty ? _hoyNewLeadsPointer(virgenesCount) : '') +
  _hoyRenderSection('Red de seguridad', redSeguridad, 'var(--text-tertiary)', 'Tocados sin próxima acción — resolvé desde acá', null, { collapsible: true });
```

Endpoint de higiene (Fase 34-01, `index.js`):
```
POST /api/setters/hoy-hygiene-snapshot
body:  { vencidos, redSeguridad, setter? }
resp:  { scope, date, today: {vencidos, redSeguridad, date}, yesterday: {...}|null, trend: 'creciendo'|'bajando'|'estable'|'sin_dato_previo' }
```

Bloque de tiles del funnel diario, patrón `.myp-tile` a reusar (app.js
~6103-6116, dentro de `loadHoyView`, NO tocar — solo copiar las clases):
```js
const tile = (label, val, accent, meterPct, conv, accentNum) => `
  <div class="myp-tile" style="--tile-accent:${accent};">
    <div class="myp-tile-label">${label}</div>
    <div class="myp-tile-num${accentNum ? ' is-accent' : ''}">${val}</div>
    ...
  </div>`;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Power Dialer por sección — 5 sub-colas + botón unificado (HOY-03, D-07/D-08)</name>

  <read_first>
    - `public/app.js` líneas 6644-6680 (declaración de `_pd`)
    - `public/app.js` líneas 6803-6915 (`_pdBuildQueueHoy` + `window._pdStart` completo — el parseo de `mode`, la construcción de `_pd.queue`, los 2 bloques de toast)
    - `public/app.js` (la sección `secEl.innerHTML` de `_hoyRenderFromStore`, tal como quedó en 34-02 — buscar por el literal `_hoyRenderSection('Mis compromisos'`)
    - `public/index.html` líneas 798-814 (`hoy-power-dialer-btn`, ya cableado a `_pdStart('hoy')` — no se toca el HTML del botón general, solo el comportamiento de `_pdBuildQueueHoy`/`_pdStart` que consume)
  </read_first>

  <action>
    Trabajar SOLO sobre `public/app.js`.

    1. **Activar los 3 botones que faltaban**, cambiando el 5to argumento
       (`dialerMode`) de `null` a la clave de modo correspondiente, en las 3
       llamadas de `_hoyRenderSection` dentro de `_hoyRenderFromStore`:
       ```js
       _hoyRenderSection('Mis compromisos', misCompromisos, 'var(--warning)', 'Le prometí algo — vence hoy', 'hoy-compromisos', { rowBadge: _hoyCommitBadge }) +
       _hoyRenderSection('Esperando del prospecto', compromisosProspecto, 'var(--accent)', 'Se comprometió él — el plazo venció', 'hoy-esperando', { rowBadge: _hoyCommitBadge }) +
       _hoyRenderSection('Callbacks', callbacks, '#5BA3F2', 'Quedaron en volver a contactar', 'hoy-callbacks') +
       _hoyRenderSection('Interesados sin agendar', interesados, 'var(--accent-hover)', 'Marcaron interés — agendar', 'hoy-interesados') +
       _hoyRenderSection('Reintentos de no-contacto', reintentos, '#8892A6', 'No atendieron — bajo esfuerzo, alto volumen', 'hoy-reintentos') +
       (tier123Empty ? _hoyNewLeadsPointer(virgenesCount) : '') +
       _hoyRenderSection('Red de seguridad', redSeguridad, 'var(--text-tertiary)', 'Tocados sin próxima acción — resolvé desde acá', null, { collapsible: true });
       ```
       "Red de seguridad" queda `null` (D-11: se resuelve individualmente, no
       en modo cola — cada lead ahí necesita una decisión, no un discado
       rápido en cadena).

    2. **`_pd.hoyFilter` gana 3 valores nuevos**, en `window._pdStart`:
       ```js
       _pd.hoyFilter = mode === 'hoy-callbacks' ? 'callbacks'
         : mode === 'hoy-interesados' ? 'interesados'
         : mode === 'hoy-compromisos' ? 'compromisos'
         : mode === 'hoy-esperando' ? 'esperando'
         : mode === 'hoy-reintentos' ? 'reintentos'
         : null;
       ```

    3. **`_pdBuildQueueHoy(filter)` reescrito** — reemplazar la función
       completa. El orden del chain (`filter` vacío) sigue D-01: compromisos
       → esperando → callbacks → interesados → reintentos:
       ```js
       function _pdBuildQueueHoy(filter) {
         const now = Date.now();
         // Los 3 sub-relojes basados en nextAction ya vienen filtrados a
         // "vence hoy/vencido" desde la clasificación (34-02) — acá solo se
         // restringe a lo YA VENCIDO EN ESTE INSTANTE (mismo criterio
         // histórico de Callbacks: "no adelantarse" a algo pactado para más
         // tarde hoy). `l.callbackAt` es el espejo del reloj único, sirve
         // para los 4 (compromisos/esperando/callbacks/reintentos).
         const dueNow = (ids) => ids.map(id => _callsLeadsById.get(id))
           .filter(l => l && (!l.callbackAt || new Date(l.callbackAt).getTime() <= now));
         const parts = [];
         if (!filter || filter === 'compromisos') parts.push(...dueNow(_hoyState.commitYoIds));
         if (!filter || filter === 'esperando') parts.push(...dueNow(_hoyState.commitProspectoIds));
         if (!filter || filter === 'callbacks') parts.push(...dueNow(_hoyState.callbackIds));
         if (!filter || filter === 'interesados') parts.push(..._hoyState.interesadoIds.map(id => _callsLeadsById.get(id)).filter(Boolean));
         if (!filter || filter === 'reintentos') parts.push(...dueNow(_hoyState.retryIds));
         const seen = new Set();
         return parts.filter(l => l && !seen.has(l.id) && (seen.has(l.id) ? false : (seen.add(l.id), true))).map(l => l.id);
       }
       ```
       (El filtrado de duplicados es defensivo — por construcción de 34-02
       ningún lead debería estar en 2 de estos arrays a la vez, pero si algo
       cambió el `claimed` entre el render y el click, esto evita discarlo
       2 veces en la misma sesión de dialer.)

    4. **Mensajes de cola vacía**: extender el ternario existente para
       nombrar las 3 secciones nuevas (mismo tono que las 2 que ya existen):
       ```js
       : (_pd.hoyFilter === 'interesados' ? 'Sin interesados por agendar.'
         : _pd.hoyFilter === 'callbacks' ? 'Sin callbacks pendientes hoy.'
         : _pd.hoyFilter === 'compromisos' ? 'Sin compromisos propios que venzan hoy.'
         : _pd.hoyFilter === 'esperando' ? 'Nada esperando respuesta vencida.'
         : _pd.hoyFilter === 'reintentos' ? 'Sin reintentos pendientes hoy.'
         : 'Sin seguimientos pendientes en Hoy.'), { type: 'info', duration: 3500 });
       ```

    5. **Toast de éxito al abrir**: mismo criterio, sumar las 3 ramas nuevas
       al ternario de `_pd.hoyFilter`:
       ```js
       window.showToast?.(_pd.mode === 'hoy'
         ? (_pd.hoyFilter === 'callbacks' ? `Power dialer · ${_n} callback${_n > 1 ? 's' : ''} en cola`
           : _pd.hoyFilter === 'interesados' ? `Power dialer · ${_n} interesado${_n > 1 ? 's' : ''} por agendar en cola`
           : _pd.hoyFilter === 'compromisos' ? `Power dialer · ${_n} compromiso${_n > 1 ? 's' : ''} propio${_n > 1 ? 's' : ''} en cola`
           : _pd.hoyFilter === 'esperando' ? `Power dialer · ${_n} en espera vencida en cola`
           : _pd.hoyFilter === 'reintentos' ? `Power dialer · ${_n} reintento${_n > 1 ? 's' : ''} en cola`
           : `Power dialer · ${_n} seguimiento${_n > 1 ? 's' : ''} de Hoy en cola`)
         : `Power dialer activado · ${_n} leads en cola`, { type: 'success', duration: 2500 });
       ```

    6. **D-08 (contador visible antes de entrar)**: verificar (no debería
       hacer falta código nuevo) que las 5 secciones dial-ables muestran
       `<span class="hoy-section-count">${leads.length}</span>` junto al
       botón — ya lo hace `_hoyRenderSection` para TODAS las secciones desde
       la Fase 13. Si por algún motivo el count no se ve junto al botón en
       alguna de las 3 nuevas, es un bug de esta task, no algo a ignorar.

    7. **NO tocar**: `_pdStart` fuera de los 3 fragmentos de arriba,
       `_pdRender`, `_pdHold`, `_pdAdvance`, `_pdDialHere`, `_pdExit`,
       `_hoyPopulateSetterSelect`, `_hoyPopulateCountryFilter` (34-02),
       `allLeadsForHygiene` (34-02, no lo consume esta Task — lo consume la
       Task 2 de este mismo plan).
  </action>

  <verify>
    <automated>node --check public/app.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "'hoy-compromisos'" public/app.js` es `>= 2` (el call site de `_hoyRenderSection` + el parseo de `_pdStart`).
    - `grep -c "'hoy-esperando'" public/app.js` es `>= 2`.
    - `grep -c "'hoy-reintentos'" public/app.js` es `>= 2`.
    - El cuerpo de `_pdBuildQueueHoy` contiene `_hoyState.commitYoIds`, `_hoyState.commitProspectoIds`, `_hoyState.callbackIds`, `_hoyState.interesadoIds`, y `_hoyState.retryIds`.
    - Llamar `_pdBuildQueueHoy(null)` (chain completo) debe devolver, en el fixture de test de la Task 3, exactamente la unión ordenada de los 5 sub-arrays sin duplicados.
    - "Red de seguridad" sigue con `dialerMode: null` (5to argumento) — `grep` de esa línea específica no debe contener `'hoy-'`.
  </acceptance_criteria>

  <done>
    Las 5 secciones reclamables de Hoy tienen su Power Dialer propio, y el
    botón general del header las encadena en orden D-01. Red de seguridad
    queda sin modo cola, a propósito.
  </done>
</task>

<task type="auto">
  <name>Task 2: Panel de higiene (HOY-05, D-12/D-13/D-14)</name>

  <read_first>
    - `public/app.js` líneas 6051-6123 (`loadHoyView` completo — dónde llamar al panel nuevo, DESPUÉS de `_hoyRenderFromStore(leads)`)
    - `public/app.js` líneas 6094-6117 (bloque de tiles `.myp-tile` de `#hoy-kpis` — patrón visual a reusar, NO duplicar CSS)
    - `public/index.html` líneas 798-814 (`view-hoy` — dónde insertar `div#hoy-hygiene`, entre `#hoy-kpis` y `#hoy-sections`)
    - El bloque de `_hoyRenderFromStore` que dejó 34-02 (Task 2 de ese plan) —
      localizar `const allLeadsForHygiene = leads;` y los predicados
      `notDnc`/`terminal`/`lastOutcome`/`_commitDueAt`/`_commitmentHoyBucket`
      que ya existen en el mismo closure. NINGUNO de esos predicados depende
      del país filtrado — son seguros de reusar tal cual para la
      clasificación paralela de esta Task.
  </read_first>

  <action>
    1. **`public/index.html`**: insertar el contenedor entre `#hoy-kpis` y
       `#hoy-sections` (línea ~812-813):
       ```html
       <div id="hoy-hygiene" style="padding:0 32px; margin-top:8px; display:flex; gap:10px; flex-wrap:wrap;"></div>
       ```

    2. **`public/app.js`**: extender `_hoyState = {...}` (dentro de
       `_hoyRenderFromStore`, justo después de que se asigna) para exponer
       los 2 números que el panel necesita.

       **Blocker del checker (2026-08-16) — NO reusar `callbacks.length`/
       `misCompromisos.length`/`compromisosProspecto.length`/`reintentos.length`/
       `redSeguridad.length`.** Esos 5 arrays ya están filtrados por país
       (34-02, Task 1) y el snapshot de higiene tiene que medir TODO el
       pipeline, sin importar qué país esté filtrado en pantalla en ese
       momento — si se midiera sobre el subconjunto filtrado, cada cambio de
       filtro de D-05 pisaría el snapshot de "hoy" con un recorte distinto y
       la tendencia de D-13 compararía peras con manzanas.

       En su lugar, clasificar de nuevo contra `allLeadsForHygiene` (la copia
       completa que dejó 34-02) — mismos predicados que ya existen en el
       closure (`notDnc`, `terminal`, `lastOutcome`, `_commitDueAt`,
       `_commitmentHoyBucket`, `endTodayTs`, `now`/`nowMsHoy`; NINGUNO
       depende del país, son seguros de reusar tal cual), pero con un `Set`
       de exclusividad PROPIO — nunca el `claimed` de la clasificación
       mostrada en pantalla, que sí quedó acotado al país filtrado:
       ```js
       // HOY-05 (D-12/D-13) — blocker fix (checker 2026-08-16): clasificación
       // PARALELA, sobre TODO el pipeline (allLeadsForHygiene, sin filtro de
       // país), con un claimed set propio. Reusa los mismos predicados que
       // ya calculó esta misma función más arriba — no son sensibles al país.
       // Es intencionalmente una segunda pasada, no una optimización
       // prematura: es lo que garantiza que el panel de higiene sea
       // invariante al filtro de país, que es justamente el punto del fix.
       const claimedHygiene = new Set();
       let vencidosHygiene = 0;
       for (const l of allLeadsForHygiene) {
         if (!notDnc(l) || terminal(l)) continue;
         if (l.callbackAt && new Date(l.callbackAt).getTime() <= endTodayTs && lastOutcome(l) === 'callback_later') {
           claimedHygiene.add(l.id); vencidosHygiene++; continue;
         }
         const bucket = _commitmentHoyBucket(l, nowMsHoy);
         if (bucket === 'yo') {
           const d = _commitDueAt(l);
           if (d && new Date(d).getTime() <= endTodayTs) { claimedHygiene.add(l.id); vencidosHygiene++; continue; }
         } else if (bucket === 'prospecto') {
           const d = _commitDueAt(l);
           if (d && new Date(d).getTime() <= now) { claimedHygiene.add(l.id); vencidosHygiene++; continue; }
         }
       }
       for (const l of allLeadsForHygiene) {
         if (claimedHygiene.has(l.id) || !notDnc(l) || terminal(l)) continue;
         const na = l.nextAction;
         if (na && na.origen === 'cadencia' && na.dueAt && new Date(na.dueAt).getTime() <= endTodayTs) {
           claimedHygiene.add(l.id); vencidosHygiene++;
         }
       }
       let redSeguridadHygiene = 0;
       for (const l of allLeadsForHygiene) {
         if (claimedHygiene.has(l.id) || !notDnc(l) || terminal(l)) continue;
         if (Array.isArray(l.callLog) && l.callLog.length > 0 && !(l.nextAction && l.nextAction.dueAt)) redSeguridadHygiene++;
       }
       _hoyState.hygiene = { vencidos: vencidosHygiene, redSeguridad: redSeguridadHygiene };
       ```
       Colocar este bloque inmediatamente después del literal
       `_hoyState = {...}` de 34-02 (Task 1, paso 8) — es una asignación
       ADICIONAL sobre la propiedad `hygiene`, no reemplaza el resto de
       `_hoyState`.

    3. **`_hoyRenderHygienePanel()`** — declarar junto a `loadHoyView`
       (después de `_hoyNewLeadsPointer`, antes de `window.loadHoyView = loadHoyView;`):
       ```js
       // HOY-05: panel de higiene, 3 números — nunca un dashboard (D-12).
       // Manda los 2 conteos que _hoyRenderFromStore YA calculó (sobre TODO
       // el pipeline, invariante al filtro de país — ver paso 2); el backend
       // (34-01) solo persiste y compara contra ayer.
       async function _hoyRenderHygienePanel() {
         const el = document.getElementById('hoy-hygiene');
         if (!el) return;
         const h = _hoyState.hygiene || { vencidos: 0, redSeguridad: 0 };
         let resp = null;
         try {
           const r = await fetch(apiUrl('/api/setters/hoy-hygiene-snapshot'), {
             method: 'POST', headers: { 'Content-Type': 'application/json' }, credentials: 'include',
             body: JSON.stringify({ vencidos: h.vencidos, redSeguridad: h.redSeguridad, setter: _hoySelectedSetter() }),
           });
           if (r.ok) resp = await r.json();
         } catch {}
         // D-14: nada en cero — un 0 desnudo entrena a ignorar el panel.
         const tile = (label, n, okText) => {
           const shown = n > 0 ? String(n) : okText;
           const color = n > 0 ? 'var(--warning)' : 'var(--text-secondary)';
           return `<div class="myp-tile" style="--tile-accent:${color};">
             <div class="myp-tile-label">${escHtml(label)}</div>
             <div class="myp-tile-num" style="color:${color};">${escHtml(shown)}</div>
           </div>`;
         };
         let trendHtml = '';
         if (resp) {
           const map = {
             creciendo: ['↑ creciendo', 'var(--warning)'],
             bajando: ['↓ bajando', 'var(--text-secondary)'],
             estable: ['→ estable', 'var(--text-secondary)'],
             sin_dato_previo: ['Sin dato de ayer todavía', 'var(--text-tertiary)'],
           };
           const [txt, col] = map[resp.trend] || map.sin_dato_previo;
           trendHtml = `<div class="myp-tile" style="--tile-accent:${col};">
             <div class="myp-tile-label">Cola de vencidos vs. ayer</div>
             <div class="myp-tile-num" style="color:${col}; font-size:15px;">${escHtml(txt)}</div>
           </div>`;
         }
         el.innerHTML = tile('Tocados sin próximo paso', h.redSeguridad, 'Al día — sin backlog')
           + tile('Compromisos vencidos hoy', h.vencidos, 'Sin vencidos')
           + trendHtml;
       }
       window._hoyRenderHygienePanel = _hoyRenderHygienePanel;
       ```
       Nota de color: NUNCA `var(--accent)`/verde para el estado "todo bien"
       (regla del proyecto, "no verde nuevo" — el acento se reserva para
       acción primaria/chip habilitado/ítem de menú activo/foco). El estado
       positivo usa `var(--text-secondary)`, neutro. El estado "hay algo
       pendiente" usa `var(--warning)`.

    4. **Llamarlo desde `loadHoyView`**: fire-and-forget (no bloquea el resto
       del render), inmediatamente después de `_hoyRenderFromStore(leads);`:
       ```js
       _hoyRenderFromStore(leads);
       _hoyRenderHygienePanel();
       ```
  </action>

  <verify>
    <automated>node --check public/app.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "hoy-hygiene" public/index.html` es `>= 1`.
    - `grep -c "function _hoyRenderHygienePanel" public/app.js` devuelve `1`.
    - El cuerpo de `_hoyRenderHygienePanel` contiene `hoy-hygiene-snapshot` y `_hoyState.hygiene`.
    - El cálculo de `_hoyState.hygiene` (dentro de `_hoyRenderFromStore`) contiene `allLeadsForHygiene`, `vencidosHygiene`, y `redSeguridadHygiene` — y NO contiene `vencidos: callbacks.length` ni ninguna variante que sume `callbacks.length + misCompromisos.length + compromisosProspecto.length + reintentos.length` (el patrón viejo, post-filtro-de-país, que el checker marcó como blocker).
    - La línea `const claimedHygiene = new Set();` aparece en el cuerpo de `_hoyRenderFromStore`, DISTINTA del `const claimed = new Set();` de la clasificación mostrada en pantalla (34-02) — nunca el mismo Set reusado para las 2 pasadas.
    - El cuerpo de la función NUNCA renderiza el carácter `'0'` como único contenido de `.myp-tile-num` — siempre pasa por el operador ternario `n > 0 ? String(n) : okText`.
    - `grep -c "var(--accent)" public/app.js` no aumenta atribuible a este archivo nuevo de código (el panel de higiene no usa `--accent` ni ningún tono verde para el estado "todo bien") — verificar leyendo el cuerpo de `_hoyRenderHygienePanel` específicamente.
    - `loadHoyView` contiene la llamada `_hoyRenderHygienePanel();` DESPUÉS de `_hoyRenderFromStore(leads);`.
  </acceptance_criteria>

  <done>
    El panel de higiene existe, muestra 3 números sin ceros desnudos, la
    tendencia compara contra el snapshot de ayer persistido por 34-01, y los
    2 conteos que manda son invariantes al filtro de país (clasificados
    sobre `allLeadsForHygiene`, no sobre los arrays ya filtrados).
  </done>
</task>

<task type="auto">
  <name>Task 3: Suite de HOY-03/HOY-05 + cache-buster + cierre de fase</name>

  <read_first>
    - `tests/dial-start-at.test.js` (molde de tests de lógica pura de cola del dialer — `_pdBuildQueueHoy`/`_pdStart` extraídos por aserción de fuente, no hay DOM real en el entorno)
    - `public/index.html` (única línea con `app.js?v=` — leer el valor REAL que dejó 34-02 antes de bumpear)
    - `.planning/ROADMAP.md` líneas 353-401 (bloque de la Phase 34 — actualizar Plans/checklist al cerrar, mismo criterio que 31-04/32-04: edición a mano con Edit, NO `gsd-sdk query` sobre estos 2 archivos — patrón de corrupción ya documentado 5+ veces)
  </read_first>

  <action>
    1. Crear `tests/hoy-dialer-hygiene.test.js` (mínimo 20 tests). Cobertura
       obligatoria:

       - **`_pdBuildQueueHoy` por sub-filtro**: aserción de fuente de que el
         cuerpo referencia los 5 arrays de `_hoyState` (`commitYoIds`,
         `commitProspectoIds`, `callbackIds`, `interesadoIds`, `retryIds`) y
         que cada uno está condicionado por su propio valor de `filter`.
       - **Orden del chain**: en el cuerpo de `_pdBuildQueueHoy`, el orden de
         aparición de las 5 condiciones `if (!filter || filter === '...')`
         es: `compromisos` < `esperando` < `callbacks` < `interesados` <
         `reintentos` (mismo orden D-01).
       - **Los 5 modos parsean bien**: `_pd.hoyFilter` para `'hoy-compromisos'`
         → `'compromisos'`, `'hoy-esperando'` → `'esperando'`,
         `'hoy-reintentos'` → `'reintentos'`, y los 2 preexistentes
         (`'hoy-callbacks'`/`'hoy-interesados'`) sin cambio de valor.
       - **Los 3 botones nuevos existen**: `_hoyRenderSection('Mis compromisos'`
         pasa `'hoy-compromisos'` como 5to argumento (no `null`), ídem
         `'Esperando del prospecto'` → `'hoy-esperando'`, `'Reintentos de
         no-contacto'` → `'hoy-reintentos'`. `'Red de seguridad'` SIGUE con
         `null`.
       - **D-14 (nunca cero desnudo)**: el cuerpo de `_hoyRenderHygienePanel`
         contiene el patrón `n > 0 ? String(n) : okText` (o equivalente) y
         NUNCA interpola `${n}` directo sin ese branching.
       - **Panel manda lo que `_hoyRenderFromStore` calculó**: el cuerpo de
         `_hoyRenderHygienePanel` lee `_hoyState.hygiene`, no re-deriva
         `callbacks.length`/`misCompromisos.length`/etc. por su cuenta.
       - **Blocker fix — invarianza al filtro de país (checker, 2026-08-16)**:
         2 tests dedicados:
         (a) Aserción de fuente: el bloque que calcula `_hoyState.hygiene`
         contiene `allLeadsForHygiene` y `claimedHygiene`, y NO contiene el
         patrón viejo `callbacks.length + misCompromisos.length` (buscar el
         substring literal para confirmar que no quedó ningún resabio).
         (b) Si el entorno de este archivo de test permite extraer y
         ejecutar el bloque de clasificación de higiene de forma aislada
         (mismo patrón que otros archivos de este proyecto que corren
         fragmentos de `_hoyRenderFromStore` con `new Function` sobre un
         fixture inyectado — ver `tests/commitment-hoy.test.js` para el
         patrón de extracción): armar un fixture de 2 países con leads
         vencidos en AMBOS, calcular `_hoyState.hygiene` una vez con
         `leads` sin filtrar y otra con `leads` recortado a 1 solo país
         (simulando lo que 34-02 hace), y confirmar que el resultado de
         `_hoyState.hygiene` es IDÉNTICO en los 2 casos (siempre se computa
         sobre `allLeadsForHygiene`, nunca sobre el `leads` ya filtrado). Si
         extraer y ejecutar el bloque real no es viable en este entorno de
         test (sin DOM/`_leadLocalTime`/dependencias), el test (a) de
         aserción de fuente es OBLIGATORIO igual — no se puede omitir la
         cobertura del blocker completa.
       - **Color discipline**: el cuerpo de `_hoyRenderHygienePanel` no
         contiene `var(--accent)` ni `#4ADE80`/verde para ningún estado.
       - **Cache-buster**: por forma + monotonía, nunca pineando el literal
         nuevo.

    2. Bumpear el cache-buster de `app.js` en `public/index.html`, leyendo
       primero el valor REAL en disco (lo dejó 34-02). NO tocar `style.css`.

    3. **Cierre de fase** — actualizar a mano (Edit, sin `gsd-sdk query`):
       - `.planning/ROADMAP.md`: Phase 34 → `Complete (3/3 planes)` en las 3
         tablas que la mencionan (Resumen, checklist, Progress); las 3 filas
         de `Plans:` con sus objetivos.
       - `.planning/REQUIREMENTS.md`: `HOY-01`..`HOY-05` marcados `[x]`.
       - `.planning/STATE.md`: Current Position actualizado; frontmatter
         `completed_phases`/`completed_plans`/`percent` según corresponda al
         estado real del milestone tras esta fase (confirmar los números
         actuales del archivo antes de incrementar — no asumir el valor
         previo documentado en ningún doc de esta sesión).
  </action>

  <verify>
    <automated>npx vitest run tests/hoy-dialer-hygiene.test.js tests/dial-start-at.test.js tests/dial-hold.test.js tests/hoy-sections.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/hoy-dialer-hygiene.test.js` pasa con al menos 20 tests y 0 fallos.
    - `tests/dial-start-at.test.js` y `tests/dial-hold.test.js` pasan SIN editarlos (`git diff --stat` de ambos vacío).
    - `tests/hoy-sections.test.js` (34-02) sigue pasando 100% (el `dialerMode` de las 3 secciones cambió de `null` a un string, pero ningún test de 34-02 debería depender de que sea `null` específicamente — si alguno lo hace, es un gap de 34-02 a corregir acá, documentado).
    - `grep -c "app.js?v=" public/index.html` devuelve `1` y el valor es estrictamente mayor que el baseline de 34-02.
    - `git diff --stat public/style.css` no imprime nada.
    - `npm test` completo: 0 fallos nuevos respecto del baseline de 34-02.
    - `git diff --stat package.json package-lock.json` vacío.
    - `.planning/ROADMAP.md`/`.planning/REQUIREMENTS.md`/`.planning/STATE.md` reflejan la Phase 34 completa.
  </acceptance_criteria>

  <done>
    HOY-03/HOY-05 quedan protegidos por tests permanentes, incluida la
    invarianza al filtro de país del panel de higiene. El cache-buster
    está bumpeado. La Phase 34 y el milestone quedan cerrados en los 3
    documentos de gestión. Suite completa del repo verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → `POST /hoy-hygiene-snapshot` | mismo boundary que documentó 34-01; este plan es el ÚNICO consumidor real del endpoint |
| `resp.trend`/`resp.today`/`resp.yesterday` → panel de higiene | valores del server, interpolados con `escHtml` igual que cualquier otro texto dinámico de Hoy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Tampering (XSS) | `_hoyRenderHygienePanel` interpola `label`/`shown`/`txt` en `innerHTML` | mitigate | Todo pasa por `escHtml` — ninguno de los valores viene de input de usuario libre (son labels hardcodeados o números coercionados por el backend) |
| T-34-08 | Denial of Service | el panel de higiene hace un `POST` en CADA `loadHoyView` (cada vez que se abre/refresca Hoy) | accept | Volumen bajo (un puñado de usuarios, refrescos manuales, no un polling agresivo) — mismo patrón ya aceptado por `_dispoLoadPendingStrip` (fire-and-forget en cada `loadCallsView`) |
| T-34-10 | Tampering (integridad de métrica) | el panel de higiene podría medir el subconjunto filtrado por país en vez del pipeline completo, invalidando la tendencia D-13 | mitigate | Blocker resuelto (checker 2026-08-16): clasificación paralela sobre `allLeadsForHygiene`, con `claimedHygiene` propio — nunca reusa los arrays ya filtrados por país |
| T-34-SC | Tampering | instalación de paquetes npm | mitigate | Este plan no instala nada. `git diff --stat package.json package-lock.json` vacío al cerrar |
</threat_model>

<verification>
1. `node --check public/app.js`.
2. `npx vitest run tests/hoy-dialer-hygiene.test.js` — suite nueva verde.
3. `npx vitest run tests/dial-start-at.test.js tests/dial-hold.test.js tests/hoy-sections.test.js`.
4. `npm test` completo contra el baseline de 34-02.
5. `git diff --stat package.json package-lock.json` y `git diff --stat public/style.css` vacíos.
6. **Verificación por mutación**: romper temporalmente el orden de una de las
   condiciones de `_pdBuildQueueHoy` (por ejemplo, invertir `compromisos` y
   `callbacks`) y confirmar que `tests/hoy-dialer-hygiene.test.js` se pone en
   rojo en el test de orden del chain. Restaurar con
   `git checkout -- public/app.js`.
7. **Verificación por mutación (blocker)**: reemplazar temporalmente
   `allLeadsForHygiene` por `leads` dentro del bloque que calcula
   `_hoyState.hygiene` y confirmar que el test de invarianza al filtro de
   país (Task 3) se pone en rojo. Restaurar con `git checkout -- public/app.js`.
</verification>

<success_criteria>
- Las 5 secciones reclamables de Hoy son discables en modo cola,
  individualmente y encadenadas desde el botón general.
- El panel de higiene muestra 3 números, nunca un cero desnudo, compara la
  cola de vencidos contra ayer, y es invariante al filtro de país activo.
- Milestone v4.0 queda cerrado: ROADMAP/REQUIREMENTS/STATE reflejan la
  Phase 34 completa.
- Cache-buster bumpeado; `style.css` intacto; suite completa verde.
</success_criteria>

<output>
Crear `.planning/phases/34-hoy-vista-diaria/34-03-SUMMARY.md` al terminar.

Documentar explícitamente:
- El valor del cache-buster ANTES y DESPUÉS (leído de disco).
- El estado final de ROADMAP.md/REQUIREMENTS.md/STATE.md (números reales
  leídos antes de editar, no asumidos de ningún doc anterior de esta sesión).
- Confirmación explícita de que el panel de higiene se probó (al menos por
  aserción de fuente, idealmente también funcional) invariante al filtro de
  país — el blocker que el checker encontró el 2026-08-16.
- Qué queda SIN verificar en vivo (no hay browser en el entorno de
  ejecución): los 5 botones de Power Dialer de Hoy funcionando en una sesión
  real con data de producción; el panel de higiene con datos reales
  acumulando 2+ días de snapshot para ver la tendencia funcionar de punta a
  punta; el `<details>` de Red de seguridad abriéndose/cerrándose sin saltos
  visuales; el comportamiento real del panel de higiene al cambiar el
  filtro de país en una sesión de browser real (la cobertura de este plan es
  por aserción de fuente/fixture, no un test E2E con DOM).
- Confirmación de que este es el ÚLTIMO plan del milestone v4.0 (Phase 34 es
  la última fase, según el prompt de esta sesión) — el orquestador debería
  ofrecer la verificación de fase / cierre de milestone como siguiente paso,
  no otro `/gsd:plan-phase`.
</output>
