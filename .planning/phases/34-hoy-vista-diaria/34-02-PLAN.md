---
phase: 34-hoy-vista-diaria
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - public/app.js
  - public/index.html
  - tests/hoy-sections.test.js
autonomous: true
requirements: [HOY-01, HOY-02, HOY-04]
must_haves:
  truths:
    - "Al abrir Hoy, las secciones aparecen en este orden: compromisos que vencen hoy (Mis compromisos / Esperando del prospecto / Callbacks) → Interesados → Reintentos de no-contacto que vencen hoy → Nuevos por score (solo si las anteriores están vacías) → Red de seguridad (colapsable, al fondo)"
    - "Un lead aparece en UNA sola sección de Hoy, nunca en dos a la vez"
    - "Un lead descartado, agendado o cerrado (deal ganado) nunca aparece en ninguna sección"
    - "La sección Red de seguridad solo cuenta leads con al menos una llamada — el stock virgen nunca entra ahí"
    - "Se puede filtrar Hoy por país; el desplegable lista primero los países en horario hábil ahora mismo; la elección se recuerda entre sesiones"
  artifacts:
    - path: "public/app.js"
      provides: "_hoyRenderFromStore reclasificado en 5 tiers + filtro de país (_hoySelectedCountry/_hoyPopulateCountryFilter)"
      contains: "function _hoyPopulateCountryFilter"
    - path: "public/index.html"
      provides: "select#hoy-country-filter en el header de Hoy"
      contains: "hoy-country-filter"
    - path: "tests/hoy-sections.test.js"
      provides: "Suite de HOY-01/HOY-02/HOY-04: cascada de 5 tiers, exclusividad, terminal() con 'cerrado', filtro de país por horario hábil"
  key_links:
    - from: "_hoyRenderFromStore"
      to: "lead.nextAction"
      via: "clasificación por origen ('manual'/'compromiso' → tier 1, 'cadencia' → tier 3) y dueAt (vence hoy / vencido)"
      pattern: "na\\.origen === 'cadencia'"
    - from: "_hoyPopulateCountryFilter"
      to: "_leadLocalTime"
      via: "orden del desplegable por horario hábil (D-05), mismo criterio que el panel de Distribución"
      pattern: "_leadLocalTime\\(\\{ country: c \\}\\)"
    - from: "_hoyRenderSection"
      to: "<details>"
      via: "opts.collapsible envuelve la sección de red de seguridad sin duplicar el header"
      pattern: "opts\\.collapsible"
---

<objective>
Reescribir la clasificación de `_hoyRenderFromStore` (Fase 33) de 4 buckets
sueltos (sin orden de prioridad explícito entre sí, sin exclusividad
garantizada, "Mis compromisos" mostrando TODO sin importar la fecha) a la
cascada de 5 tiers que pide D-01, con exclusividad real (D-02) y la Red de
seguridad nueva (D-09/D-10/D-11). Sumar el filtro por país (HOY-02).

Purpose: es la queja textual del user — *"la vista de hoy… es poco
intuitiva, está muy mezclado todo y es muy caótico"*. El criterio de
prioridad hoy vive implícito en el orden en que el código feliz calcula cada
bucket; esta fase lo hace EXPLÍCITO: un solo `claimed` Set compartido entre
las 5 tiers (Fase 31 dejó a "Mis compromisos"/"Esperando del prospecto"
deliberadamente FUERA de `claimed` — Fase 34 los suma, es un cambio de
diseño intencional, documentado abajo).

Output: `_hoyRenderFromStore` con 5 tiers + gate del tier "Nuevos" + Red de
seguridad colapsable; `select#hoy-country-filter`; `tests/hoy-sections.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/34-hoy-vista-diaria/34-CONTEXT.md
@.planning/phases/34-hoy-vista-diaria/34-01-SUMMARY.md
@.planning/research/2026-08-13-requisitos-seguimiento-ignacio.md
@CLAUDE.md

Referencia canónica obligatoria — Hoy tal como quedó tras la Fase 33 (el
punto de partida exacto de este plan, NO reimplementar desde cero):
@.planning/phases/33-dial-motor-unico/33-03-SUMMARY.md
@.planning/phases/31-comm-compromisos/31-04-SUMMARY.md
</context>

<interfaces>
<!-- Contratos vigentes en public/app.js. Leer los rangos de <read_first> antes de editar. -->

`_hoyRenderFromStore` completo, tal como quedó la Fase 33 (~app.js:5959-6049)
— ESTE plan reescribe el CUERPO, no la firma ni los guards de arriba
(leadsArg / anti-repintado / población desde `_leadStoreDirty`):
```js
function _hoyRenderFromStore(leadsArg) {
  const secEl = document.getElementById('hoy-sections');
  const greetEl = document.getElementById('hoy-greeting');
  if (!secEl) return;
  if (!leadsArg && _hoyRenderedVersion === _leadStoreVersion && secEl.children.length) return;
  let leads;
  if (leadsArg) { leads = leadsArg; }
  else { /* población desde _hoyLeadIds + _leadStoreDirty, sin tocar */ }
  const now = Date.now();
  const _endToday = new Date(); _endToday.setHours(23, 59, 59, 999); const endTodayTs = _endToday.getTime();
  const claimed = new Set();
  const notDnc = (l) => !l.doNotCall;
  const terminal = (l) => l.estado === 'descartado' || l.estado === 'agendado';
  const lastOutcome = (l) => (Array.isArray(l.callLog) && l.callLog.length) ? l.callLog[l.callLog.length - 1].outcome : null;
  const callbacks = leads.filter(l => notDnc(l) && l.callbackAt && new Date(l.callbackAt).getTime() <= endTodayTs && lastOutcome(l) === 'callback_later')
    .sort((a, b) => new Date(a.callbackAt) - new Date(b.callbackAt));
  callbacks.forEach(l => claimed.add(l.id));
  const interesados = leads.filter(l => !claimed.has(l.id) && notDnc(l) && l.estado === 'interesado');
  interesados.forEach(l => claimed.add(l.id));
  const nowMsHoy = now;
  const misCompromisos = leads.filter(l => notDnc(l) && !terminal(l) && _commitmentHoyBucket(l, nowMsHoy) === 'yo')
    .sort((a, b) => new Date(a.commitment.dueAt) - new Date(b.commitment.dueAt));
  const compromisosProspecto = leads.filter(l => notDnc(l) && !terminal(l) && _commitmentHoyBucket(l, nowMsHoy) === 'prospecto')
    .sort((a, b) => new Date(a.commitment.dueAt) - new Date(b.commitment.dueAt));
  const virgenesCount = leads.filter(l => !claimed.has(l.id) && notDnc(l) && !terminal(l) && (Number(l.callAttempts || 0) === 0)).length;
  _hoyState = {
    callbackIds: callbacks.map(l => l.id), interesadoIds: interesados.map(l => l.id),
    commitYoIds: misCompromisos.map(l => l.id), commitProspectoIds: compromisosProspecto.map(l => l.id),
    at: Date.now(),
  };
  const totalPend = new Set([...callbacks, ...interesados, ...misCompromisos, ...compromisosProspecto].map(l => l.id)).size;
  if (greetEl) greetEl.textContent = `${totalPend} para seguir`;
  secEl.innerHTML =
    _hoyRenderSection('Mis compromisos', misCompromisos, 'var(--warning)', 'Le prometí algo — falta cumplirlo', null, { rowBadge: _hoyCommitBadge }) +
    _hoyRenderSection('Esperando del prospecto', compromisosProspecto, 'var(--accent)', 'Se comprometió él — si vence, seguimiento', null, { rowBadge: _hoyCommitBadge }) +
    _hoyRenderSection('Callbacks', callbacks, '#5BA3F2', 'Quedaron en volver a contactar', 'hoy-callbacks') +
    _hoyRenderSection('Interesados sin agendar', interesados, 'var(--accent-hover)', 'Marcaron interés — agendar', 'hoy-interesados') +
    _hoyNewLeadsPointer(virgenesCount);
  _hoyRenderedVersion = _leadStoreVersion;
}
```

`_hoyRenderSection` (app.js ~6271-6321) — firma y comportamiento por defecto
que NO cambia; solo se le agrega soporte para `opts.collapsible`:
```js
function _hoyRenderSection(title, leads, accent, hint, dialerMode, opts = {}) {
  // ... rows con opts.rowBadge (Fase 31, sin tocar) ...
  const dialBtn = (dialerMode && leads.length) ? `<button onclick="window._pdStart('${dialerMode}')" ...>Power dialer</button>` : '';
  return `<div class="hoy-section" style="--sec-accent:${accent};">
    <div class="hoy-section-head">
      <span class="hoy-section-dot"></span>
      <span class="hoy-section-title">${title}</span>
      <span class="hoy-section-count">${leads.length}</span>
      <span class="hoy-section-hint">${hint}</span>
      ${dialBtn}
    </div>
    ${leads.length ? rows : '<div class="hoy-empty">Sin pendientes.</div>'}
  </div>`;
}
```

`_commitmentHoyBucket(lead, nowMs)` (bloque `[31-03] COMMITMENT-PURE`, NO se
toca en este plan — se sigue llamando tal cual): devuelve `'yo'` | `'prospecto'`
| `null`. Para el caso `'prospecto'` hay DOS orígenes distintos (ver
`31-04-SUMMARY.md`): un compromiso `parte:'prospecto'` pendiente (su fecha
vigente es `lead.commitment.dueAt`), o un `enviar_info`/`pedir_presupuesto`
propio ya `cumplido` con `lead.nextAction = {origen:'compromiso', tipo:'esperar_respuesta'}`
(su fecha vigente real es `lead.nextAction.dueAt`, NO `lead.commitment.dueAt`
— ese quedó en el pasado apenas se cumple el compromiso). Ver Task 1, paso 3.

`_leadLocalTime(lead)` (app.js ~7939-7949) — usado por Distribución para
"¿A qué país llamar ahora?" (app.js ~14536-14537, patrón de orden a copiar
literal para D-05):
```js
const countryTiming = countries.map(c => { const lt = _leadLocalTime({ country: c }); return { country: c, count, ok: lt ? lt.ok : false, hasTz: !!lt }; });
countryTiming.sort((a, b) => (Number(b.ok) - Number(a.ok)) || (b.count - a.count));
```

`lead.nextAction` (Fase 34-01 lo garantiza SIEMPRE resuelto en la respuesta
de `/leads/sin-wsp`, migrado o no): `{ tipo, dueAt, canal, motivo, origen, createdAt, createdBy } | null`.
`origen` ∈ `'manual' | 'compromiso' | 'cadencia'`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Cascada de 5 tiers en _hoyRenderFromStore (HOY-01, HOY-04)</name>

  <read_first>
    - `public/app.js` líneas 5910-6049 (`_hoySelectedSetter`/`_hoyPopulateSetterSelect`/`_hoyState`/`_hoyRenderFromStore` completo — la función a reescribir)
    - `public/app.js` líneas 6096-6136 (`_dispoSelectHTML`, `_actButtonsHTML`, `_leadHistoryHTML` — no se tocan, solo se siguen llamando desde dentro de `_hoyRenderSection`)
    - `public/app.js` líneas 6271-6321 (`_hoyRenderSection` — se le agrega `opts.collapsible`)
    - `public/app.js` líneas 6389-6401 (`_hoyNewLeadsPointer`, `window.loadHoyView = loadHoyView`)
    - `public/app.js` líneas 6803-6819 (`_pdBuildQueueHoy` — lee `_hoyState`; este plan cambia la FORMA de `_hoyState` con un campo nuevo (`retryIds`), pero NO reescribe `_pdBuildQueueHoy` — eso es 34-03. Verificar que `_pdBuildQueueHoy` sigue funcionando con `commitYoIds`/`commitProspectoIds`/`callbackIds`/`interesadoIds` intactos)
    - `tests/commitment-hoy.test.js` completo (los 4 `describe` de "Aserciones de fuente" que van a verse afectados por este plan — leerlo ANTES de tocar `app.js`, no después)
  </read_first>

  <action>
    Trabajar SOLO sobre `public/app.js`. Reescribir el CUERPO de
    `_hoyRenderFromStore` (mantener la firma, los guards de arriba, y el
    bloque de población de `leads` intactos — solo cambia desde
    `const now = Date.now();` en adelante):

    1. **`terminal()` gana `'cerrado'`** (Fase 9 introdujo `estado:'cerrado'`
       para deals ganados — hoy `terminal()` no lo excluye, así que un lead
       CERRADO con callLog podía colarse en "Red de seguridad" como si le
       faltara un próximo paso, cuando en realidad no necesita ninguno):
       ```js
       const terminal = (l) => l.estado === 'descartado' || l.estado === 'agendado' || l.estado === 'cerrado';
       ```

    2. **Helper local de fecha efectiva del compromiso** (closure dentro de
       `_hoyRenderFromStore`, junto a `notDnc`/`terminal`/`lastOutcome` — NO
       es un helper puro nuevo del bloque `[31-03]`, vive acá adentro porque
       solo lo usa esta función):
       ```js
       // D-06 mapea el tipo de compromiso a un dueAt, pero la fecha VIGENTE
       // para decidir "vence hoy" es la del reloj único (lead.nextAction),
       // cuando ese reloj sigue siendo el que puso el compromiso — cubre el
       // caso 'esperando del prospecto' (cumplido, dueAt del compromiso ya
       // quedó en el pasado; lo que importa es cuándo vence la ESPERA).
       // Fallback a commitment.dueAt si el reloj fue pisado por otra cosa.
       const _commitDueAt = (l) => (l.nextAction && l.nextAction.origen === 'compromiso' && l.nextAction.dueAt)
         ? l.nextAction.dueAt
         : (l.commitment ? l.commitment.dueAt : null);
       ```

    3. **Tier 1 — "Compromisos que vencen hoy" (D-01 #1), 3 tarjetas con UN
       `claimed` compartido** (antes "Mis compromisos"/"Esperando del
       prospecto" NO participaban de `claimed` — Fase 31 lo dejó así a
       propósito; Fase 34 lo cambia: D-02 pide exclusividad real):
       ```js
       // 1a. Callbacks manuales que vencen hoy — filtro sin cambios, pero
       // ahora TAMBIÉN excluye terminal() (antes no lo chequeaba explícito,
       // aunque en la práctica un descartado/agendado casi nunca tiene
       // callback_later como último outcome).
       const callbacks = leads.filter(l => notDnc(l) && !terminal(l) && l.callbackAt && new Date(l.callbackAt).getTime() <= endTodayTs && lastOutcome(l) === 'callback_later')
         .sort((a, b) => new Date(a.callbackAt) - new Date(b.callbackAt));
       callbacks.forEach(l => claimed.add(l.id));

       // 1b. Mis compromisos (parte 'yo', pendiente) QUE VENCEN HOY. Antes
       // mostraba TODOS los pendientes sin importar la fecha (un compromiso
       // a 5 días competía por atención arriba de todo, hoy) — D-01 lo acota
       // a "vencen hoy", igual que Callbacks.
       const misCompromisos = leads.filter(l => !claimed.has(l.id) && notDnc(l) && !terminal(l)
           && _commitmentHoyBucket(l, nowMsHoy) === 'yo'
           && new Date(_commitDueAt(l)).getTime() <= endTodayTs)
         .sort((a, b) => new Date(_commitDueAt(a)) - new Date(_commitDueAt(b)));
       misCompromisos.forEach(l => claimed.add(l.id));

       // 1c. Esperando del prospecto — VENCIDO (el plazo de espera ya pasó;
       // antes mostraba TODOS los pendientes/cumplidos sin filtrar fecha).
       const compromisosProspecto = leads.filter(l => !claimed.has(l.id) && notDnc(l) && !terminal(l)
           && _commitmentHoyBucket(l, nowMsHoy) === 'prospecto'
           && new Date(_commitDueAt(l)).getTime() <= now)
         .sort((a, b) => new Date(_commitDueAt(a)) - new Date(_commitDueAt(b)));
       compromisosProspecto.forEach(l => claimed.add(l.id));
       ```

    4. **Tier 2 — "Interesados con próximo paso vencido o sin próximo paso"
       (D-01 #2)**: SIN cambio de filtro (D-03 protege "el interesado
       aparece todos los días hasta agendar/descartar" — no se acota por
       fecha), solo excluye lo ya reclamado por el tier 1:
       ```js
       const interesados = leads.filter(l => !claimed.has(l.id) && notDnc(l) && !terminal(l) && l.estado === 'interesado');
       interesados.forEach(l => claimed.add(l.id));
       ```

    5. **Tier 3 — "Reintentos de no-contacto que vencen hoy" (D-01 #3,
       NUEVO)**: leads cuyo reloj es de ORIGEN cadencia (no_answer/voicemail
       reintentados automáticamente) y vence hoy. Sigue viviendo TAMBIÉN en
       Llamadas/Power Dialer — esto no los saca de ahí, los suma a Hoy:
       ```js
       const reintentos = leads.filter(l => {
           if (claimed.has(l.id) || !notDnc(l) || terminal(l)) return false;
           const na = l.nextAction;
           return !!(na && na.origen === 'cadencia' && na.dueAt && new Date(na.dueAt).getTime() <= endTodayTs);
         })
         .sort((a, b) => new Date(a.nextAction.dueAt) - new Date(b.nextAction.dueAt));
       reintentos.forEach(l => claimed.add(l.id));
       ```

    6. **Tier 5 — "Red de seguridad" (D-09/D-10/D-11)**: SOLO leads TOCADOS
       (`callLog.length > 0` — nunca el stock virgen, D-10 es la restricción
       transversal de la fase) sin próxima acción, no reclamados arriba.
       Calcularlo ANTES del tier 4 (el orden de cálculo no importa para el
       resultado, pero sí importa que corra DESPUÉS de los tiers 1-3 para que
       `claimed` ya los excluya):
       ```js
       const redSeguridad = leads.filter(l => !claimed.has(l.id) && notDnc(l) && !terminal(l)
           && Array.isArray(l.callLog) && l.callLog.length > 0
           && !(l.nextAction && l.nextAction.dueAt));
       redSeguridad.forEach(l => claimed.add(l.id));
       ```

    7. **Tier 4 — "Nuevos por score" (D-01 #4), gateado**: SOLO se cuenta/se
       muestra si los tiers 1-3 están vacíos (D-01: "protege el pipeline
       futuro sin canibalizar el seguimiento"). El cálculo de `virgenesCount`
       NO cambia de línea (sigue siendo el literal exacto de siempre, para no
       romper el test anti-deriva existente); lo que cambia es que su render
       queda condicionado:
       ```js
       const virgenesCount = leads.filter(l => !claimed.has(l.id) && notDnc(l) && !terminal(l) && (Number(l.callAttempts || 0) === 0)).length;
       const tier123Empty = (callbacks.length + misCompromisos.length + compromisosProspecto.length + interesados.length + reintentos.length) === 0;
       ```

    8. **`_hoyState`**: preservar el literal EXACTO
       `commitYoIds: misCompromisos.map(l => l.id), commitProspectoIds: compromisosProspecto.map(l => l.id),`
       (protege un test anti-deriva existente, ver acceptance criteria) y
       sumar `retryIds`:
       ```js
       _hoyState = {
         callbackIds: callbacks.map(l => l.id), interesadoIds: interesados.map(l => l.id),
         commitYoIds: misCompromisos.map(l => l.id), commitProspectoIds: compromisosProspecto.map(l => l.id),
         retryIds: reintentos.map(l => l.id),
         at: Date.now(),
       };
       ```

    9. **`totalPend`**: sumar `reintentos` al Set de ids únicos (mantener
       `callbacks`/`interesados`/`misCompromisos`/`compromisosProspecto` en
       la MISMA línea — protege el test que verifica que esos 4 substrings
       siguen presentes):
       ```js
       const totalPend = new Set([...callbacks, ...interesados, ...misCompromisos, ...compromisosProspecto, ...reintentos].map(l => l.id)).size;
       ```
       (Red de seguridad NO participa de `totalPend` — es un backlog aparte,
       que el panel de higiene de 34-03 muestra con su propio número.)

    10. **`_hoyRenderSection` gana `opts.collapsible`** — extensión NO
        invasiva (mismo criterio que `opts.rowBadge`, Fase 31): sin la opción,
        el markup queda byte-idéntico. Cambiar el `return` final:
        ```js
        const outerTag = opts.collapsible ? 'details' : 'div';
        const headTag = opts.collapsible ? 'summary' : 'div';
        const openAttr = opts.collapsible && leads.length ? ' open' : '';
        return `<${outerTag} class="hoy-section" style="--sec-accent:${accent};"${openAttr}>
          <${headTag} class="hoy-section-head"${opts.collapsible ? ' style="cursor:pointer;"' : ''}>
            <span class="hoy-section-dot"></span>
            <span class="hoy-section-title">${title}</span>
            <span class="hoy-section-count">${leads.length}</span>
            <span class="hoy-section-hint">${hint}</span>
            ${dialBtn}
          </${headTag}>
          ${leads.length ? rows : '<div class="hoy-empty">Sin pendientes.</div>'}
        </${outerTag}>`;
        ```
        (D-09: colapsable, empieza ABIERTA si hay algo que resolver —
        "idealmente vacía" no significa "escondida", significa que el
        objetivo es que no haya nada que mostrar).

    11. **`secEl.innerHTML`**: nuevo orden completo, D-01. `dialerMode: null`
        para "Mis compromisos"/"Esperando del prospecto"/"Reintentos" en ESTE
        plan (34-03 les agrega el botón — no dejar un botón que apunte a un
        modo que `_pdStart` todavía no entiende):
        ```js
        secEl.innerHTML =
          _hoyRenderSection('Mis compromisos', misCompromisos, 'var(--warning)', 'Le prometí algo — vence hoy', null, { rowBadge: _hoyCommitBadge }) +
          _hoyRenderSection('Esperando del prospecto', compromisosProspecto, 'var(--accent)', 'Se comprometió él — el plazo venció', null, { rowBadge: _hoyCommitBadge }) +
          _hoyRenderSection('Callbacks', callbacks, '#5BA3F2', 'Quedaron en volver a contactar', 'hoy-callbacks') +
          _hoyRenderSection('Interesados sin agendar', interesados, 'var(--accent-hover)', 'Marcaron interés — agendar', 'hoy-interesados') +
          _hoyRenderSection('Reintentos de no-contacto', reintentos, '#8892A6', 'No atendieron — bajo esfuerzo, alto volumen', null) +
          (tier123Empty ? _hoyNewLeadsPointer(virgenesCount) : '') +
          _hoyRenderSection('Red de seguridad', redSeguridad, 'var(--text-tertiary)', 'Tocados sin próxima acción — resolvé desde acá', null, { collapsible: true });
        _hoyRenderedVersion = _leadStoreVersion;
        ```
        Nota: "Interesados sin agendar", "Callbacks" y "Mis compromisos"/
        "Esperando del prospecto" quedan en el MISMO orden relativo que ya
        tenían entre sí (Mis compromisos → Esperando → Callbacks → Interesados)
        — el cambio real es que Reintentos + Red de seguridad se suman
        DESPUÉS, y Nuevos queda gateado.

    12. **NO tocar**: `_commitmentHoyBucket`, `_hoyCommitBadge`,
        `_hoyOwnerName`, `_dispoSelectHTML`, `_actButtonsHTML`,
        `_leadHistoryHTML`, `_hoyOpenFicha`, `_hoyRefreshFicha`,
        `_refreshLeadPanels`, `loadHoyView` (más allá de lo que pida la
        Task 2 de este mismo plan para el filtro de país).
  </action>

  <verify>
    <automated>node --check public/app.js && npx vitest run tests/commitment-hoy.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "l.estado === 'cerrado'" public/app.js` es `>= 1`, dentro del cuerpo de `terminal` de `_hoyRenderFromStore`.
    - `grep -c "commitYoIds: misCompromisos.map(l => l.id), commitProspectoIds: compromisosProspecto.map(l => l.id)," public/app.js` devuelve `1` (literal preservado).
    - `grep -c "retryIds: reintentos.map(l => l.id)," public/app.js` devuelve `1`.
    - `grep -c "opts.collapsible" public/app.js` es `>= 3` (las 3 ramas del `return` de `_hoyRenderSection`).
    - El cuerpo de `_hoyRenderFromStore` contiene, en este orden de aparición: `'Mis compromisos'`, `'Esperando del prospecto'`, `'Callbacks'`, `'Interesados sin agendar'`, `'Reintentos de no-contacto'`, `'Red de seguridad'`.
    - `tests/commitment-hoy.test.js` corre y falla EXACTAMENTE en el describe "`_hoyRenderFromStore` NO llama claimed.add para los leads de compromiso" (el conteo global de `claimed.add` ya no es 2 — subió porque `misCompromisos`/`compromisosProspecto`/`reintentos`/`redSeguridad` ahora SÍ participan, a propósito). Es el ÚNICO test que se espera romper en este archivo — actualizarlo en la Task 3 de este plan, NO revertir el diseño para hacerlo pasar tal cual está.
  </acceptance_criteria>

  <done>
    `_hoyRenderFromStore` clasifica en 5 tiers exclusivos, con el orden de
    D-01, la Red de seguridad al fondo (colapsable, solo leads tocados), y el
    tier de Nuevos gateado. `_hoyState` trae `retryIds`. `_hoyRenderSection`
    soporta `opts.collapsible` sin romper los call sites existentes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Filtro por país en Hoy (HOY-02, D-04/D-05/D-06)</name>

  <read_first>
    - `public/app.js` líneas 6051-6123 (`loadHoyView` completo — dónde insertar el poblado del filtro)
    - `public/app.js` líneas 14533-14542 (patrón EXACTO de orden por horario hábil de Distribución, D-05 — copiar la lógica de sort, no reinventar)
    - `public/app.js` líneas 6403-6480 (`loadCallsView`, el patrón de `calls-country-filter`: `localStorage.getItem('calls_country_filter_' + (currentUser?.id || 'anon'))`, poblado del `<select>`, preservación de la selección actual — D-04 pide reusar ESTE patrón, no inventar uno nuevo)
    - `public/index.html` líneas 798-814 (`view-hoy` completo — dónde va el nuevo `<select>`)
  </read_first>

  <action>
    1. **`public/index.html`**: agregar un `<select id="hoy-country-filter">`
       en el header de `view-hoy` (línea ~802-804, junto al select de SDR),
       mismo estilo que `calls-country-filter`:
       ```html
       <select id="hoy-country-filter" class="calls-filter-select" style="min-width:160px;" title="Filtrar por país — ordenado por a quién podés llamar AHORA">
         <option value="">Todos los países</option>
       </select>
       ```

    2. **`public/app.js`**: declarar `_hoySelectedCountry()` y
       `_hoyPopulateCountryFilter(leads)` junto a `_hoySelectedSetter`/
       `_hoyPopulateSetterSelect` (~línea 5922):
       ```js
       // HOY-02 (Fase 34, D-04/D-05/D-06): mismo patrón que
       // calls_country_filter_ de Llamadas, pero el desplegable se ordena
       // por horario hábil (D-05: "a quién puedo llamar AHORA"), no
       // alfabético — misma lógica que "¿A qué país llamar ahora?" de
       // Distribución (app.js ~14536-14537).
       function _hoySelectedCountry() {
         return document.getElementById('hoy-country-filter')?.value || '';
       }
       function _hoyPopulateCountryFilter(leads) {
         const sel = document.getElementById('hoy-country-filter');
         if (!sel) return;
         const counts = {};
         for (const l of leads) {
           const c = (l.country || '').trim();
           if (c) counts[c] = (counts[c] || 0) + 1;
         }
         const rows = Object.keys(counts).map(c => {
           const lt = (typeof _leadLocalTime === 'function') ? _leadLocalTime({ country: c }) : null;
           return { country: c, count: counts[c], ok: lt ? lt.ok : false, hasTz: !!lt };
         });
         rows.sort((a, b) => (Number(b.ok) - Number(a.ok)) || (b.count - a.count));
         const key = 'hoy_country_filter_' + (currentUser?.id || 'anon');
         const cur = sel.value || localStorage.getItem(key) || '';
         sel.innerHTML = '<option value="">Todos los países</option>' + rows.map(r =>
           `<option value="${escHtml(r.country)}">${r.ok ? '🟢 ' : r.hasTz ? '🟡 ' : ''}${escHtml(r.country)} (${r.count})</option>`
         ).join('');
         if (cur && counts[cur]) sel.value = cur;
         if (!sel.dataset.wired) {
           sel.dataset.wired = '1';
           sel.addEventListener('change', () => {
             try { localStorage.setItem(key, sel.value); } catch {}
             _hoyRenderedVersion = -1; // fuerza el repintado (no hubo escritura de estado que suba _leadStoreVersion)
             _hoyRenderFromStore();
           });
         }
       }
       ```

    3. **Aplicar el filtro dentro de `_hoyRenderFromStore`**: justo después de
       resolver `leads` (tanto el camino `leadsArg` como el derivado desde el
       store — el filtro se aplica a los DOS caminos por igual) y ANTES de
       calcular `now`/`_endToday`/`claimed`:
       ```js
       const countryFilter = (typeof _hoySelectedCountry === 'function') ? _hoySelectedCountry() : '';
       if (countryFilter) leads = leads.filter(l => (l.country || '').trim() === countryFilter);
       ```

    4. **Poblar el filtro desde `loadHoyView`**: llamar
       `_hoyPopulateCountryFilter(leads);` inmediatamente antes de
       `_hoyRenderFromStore(leads);` (~línea 6092, mismo punto donde ya se
       llama `_hoyPopulateSetterSelect()` — pero esa se llama ANTES del
       fetch, con la lista de setters; ésta va DESPUÉS del fetch, con los
       leads recién traídos, porque necesita los países presentes en esos
       leads).
  </action>

  <verify>
    <automated>node --check public/app.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "hoy-country-filter" public/index.html` es `>= 1`.
    - `grep -c "function _hoyPopulateCountryFilter" public/app.js` devuelve `1`.
    - El cuerpo de `_hoyPopulateCountryFilter` contiene `_leadLocalTime(` y `.sort((a, b) => (Number(b.ok) - Number(a.ok))`.
    - El cuerpo de `_hoyRenderFromStore` contiene `_hoySelectedCountry()` y aplica el filtro ANTES de calcular `claimed`.
    - `grep -c "hoy_country_filter_" public/app.js` es `>= 2` (lectura y escritura en localStorage).
    - `loadHoyView` contiene la llamada `_hoyPopulateCountryFilter(leads)` ANTES de `_hoyRenderFromStore(leads)`.
  </acceptance_criteria>

  <done>
    El filtro por país existe, ordena por horario hábil primero, persiste por
    usuario, y se aplica ANTES de la clasificación en tiers (afecta a las 5
    secciones por igual).
  </done>
</task>

<task type="auto">
  <name>Task 3: Suite de HOY-01/HOY-02/HOY-04 + cache-buster</name>

  <read_first>
    - `tests/commitment-hoy.test.js` completo (el archivo que este plan rompe en 1 punto — Task 1, acceptance criteria)
    - `tests/dial-sync.test.js` (mismo molde: aserciones de fuente + `extractFunctionBody`, patrón a reusar)
    - `public/index.html` (única línea con `app.js?v=` — leer el valor REAL antes de bumpear, puede haberse movido desde `20260816f`)
  </read_first>

  <action>
    1. **Actualizar `tests/commitment-hoy.test.js`** (el único archivo
       preexistente que este plan invalida a propósito, mismo patrón
       documentado en `33-03-SUMMARY.md`/`33-04-SUMMARY.md` — Rule 1, no
       reducir cobertura, solo apuntarla a la intención nueva):
       - El `it("_hoyRenderFromStore NO llama claimed.add para los leads de
         compromiso — el conteo global sigue en 2")` se reescribe: ahora la
         intención es la OPUESTA (Fase 34, D-02) — verificar que
         `misCompromisos.forEach(l => claimed.add`, `compromisosProspecto.forEach(l => claimed.add`,
         `reintentos.forEach(l => claimed.add`, y `redSeguridad.forEach(l => claimed.add`
         SÍ aparecen dentro del cuerpo de `_hoyRenderFromStore` (`.toContain`
         en vez de `.not.toContain`), y documentar en un comentario por qué
         cambió (referenciar este plan).
       - Los demás `describe`/`it` de ese archivo (bloque puro de
         `_commitmentHoyBucket`, `_hoyRenderSection` con `opts.rowBadge`, los
         títulos "Mis compromisos"/"Esperando del prospecto" match con
         `_dispoDestination`, `_hoyCommitBadge`, `_renderCallHistory`,
         cache-buster, anti-alcance D-12) NO deberían necesitar cambios —
         confirmarlo corriendo la suite ANTES de tocar nada y viendo que solo
         ese 1 `it` está en rojo.

    2. Crear `tests/hoy-sections.test.js` (mínimo 20 tests), mismo molde de
       aserciones de fuente que `tests/dial-sync.test.js`/
       `tests/commitment-hoy.test.js`. Cobertura obligatoria:

       - **Orden D-01**: el cuerpo de `_hoyRenderFromStore` tiene, en este
         orden de `indexOf`, las 6 llamadas a `_hoyRenderSection`/
         `_hoyNewLeadsPointer` (Mis compromisos < Esperando < Callbacks <
         Interesados < Reintentos < gate-de-Nuevos-o-nada < Red de
         seguridad).
       - **Exclusividad (D-02)**: los 5 tiers "reclamables" (callbacks,
         misCompromisos, compromisosProspecto, interesados, reintentos)
         participan de `claimed` (grep de `.forEach(l => claimed.add` — debe
         haber al menos 5 ocurrencias en el cuerpo, más `redSeguridad` que
         también lo hace = 6).
       - **`terminal()` incluye 'cerrado'**: aserción de fuente directa.
       - **D-10 (Red de seguridad solo tocados)**: el filtro de
         `redSeguridad` contiene `l.callLog.length > 0` — un lead con
         `callAttempts:0`/`callLog:[]` nunca puede entrar ahí aunque cumpla
         el resto de condiciones (verificar por lectura del filtro, no hace
         falta ejecutar DOM real).
       - **Gate del tier 4**: `tier123Empty` se calcula sumando
         `callbacks.length + misCompromisos.length + compromisosProspecto.length + interesados.length + reintentos.length`
         y el render de `_hoyNewLeadsPointer` está condicionado a ese booleano.
       - **`opts.collapsible`**: sin la opción (llamada sin 6to argumento o
         con `{}`), el markup sigue siendo `<div class="hoy-section"`, NUNCA
         `<details>` — test de regresión explícito llamando
         `_hoyRenderSection` extraída con `new Function` contra un fixture
         chico (0 y 2 leads) y verificando el tag de apertura/cierre.
       - **Filtro de país**: `_hoyPopulateCountryFilter` ordena por `ok`
         descendente y por `count` descendente como segundo criterio — test
         de la función pura-ish extraída, con un fixture de países con/sin
         zona horaria conocida (mockear `_leadLocalTime` o usar países reales
         del mapa `_LEAD_TZ` con una hora fija inyectada si el helper lo
         permite; si no, verificar por aserción de fuente el criterio de
         sort literal).
       - **Persistencia**: `hoy_country_filter_` aparece tanto en un
         `localStorage.getItem` como en un `localStorage.setItem`.
       - **`_commitDueAt`**: aserción de fuente de que existe y que
         `misCompromisos`/`compromisosProspecto` lo usan tanto para filtrar
         como para ordenar (no dos criterios de fecha distintos por
         accidente).
       - **Cache-buster**: por forma + monotonía, nunca pineando el literal
         nuevo.

    3. Bumpear el cache-buster de `app.js` en `public/index.html`, leyendo
       primero el valor REAL en disco. NO tocar `style.css` (esta fase no
       agrega CSS nuevo — todo el estilo nuevo es inline, siguiendo la
       convención ya establecida de `_hoyRenderSection`/`_hoyCommitBadge`).
  </action>

  <verify>
    <automated>npx vitest run tests/hoy-sections.test.js tests/commitment-hoy.test.js tests/dial-sync.test.js tests/dial-history.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/hoy-sections.test.js` pasa con al menos 20 tests y 0 fallos.
    - `npx vitest run tests/commitment-hoy.test.js` pasa 100% (incluido el `it` reescrito).
    - `tests/dial-sync.test.js` y `tests/dial-history.test.js` pasan SIN editarlos (`git diff --stat` de ambos vacío) — ninguno de los 2 debería verse afectado por este plan.
    - `grep -c "app.js?v=" public/index.html` devuelve `1` y el valor es estrictamente mayor (string compare) que el baseline real leído en disco.
    - `git diff --stat public/style.css` no imprime nada.
    - `npm test` completo: 0 fallos nuevos respecto del baseline de 34-01.
    - `git diff --stat package.json package-lock.json` vacío.
  </acceptance_criteria>

  <done>
    HOY-01/HOY-02/HOY-04 quedan protegidos por tests permanentes, incluida la
    actualización explícita y documentada del único test preexistente
    invalidado a propósito. Cache-buster bumpeado, `style.css` intacto.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `lead.country` → `<option>` del filtro | interpolado en `innerHTML`, debe pasar por `escHtml` |
| `lead.nextAction`/`lead.commitment` → clasificación de tiers | datos del server, ya confiables (no hay input de usuario directo en esta capa) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-05 | Tampering (XSS) | `_hoyPopulateCountryFilter` interpola `r.country`/`r.count` en `innerHTML` | mitigate | `escHtml(r.country)` en el nombre del país; `r.count` es un número derivado, nunca texto de usuario |
| T-34-06 | Information Disclosure | la Red de seguridad podría filtrar leads que un setter no debería ver | accept | Los leads que llegan a `_hoyRenderFromStore` YA vienen filtrados por RBAC desde `/leads/sin-wsp` (Fase 18) — esta fase solo reclasifica lo que el backend ya autorizó a mostrar, no agrega ninguna vía nueva de lectura |
| T-34-SC | Tampering | instalación de paquetes npm | mitigate | Este plan no instala nada. `git diff --stat package.json package-lock.json` vacío al cerrar |
</threat_model>

<verification>
1. `node --check public/app.js`.
2. `npx vitest run tests/hoy-sections.test.js` — suite nueva verde.
3. `npx vitest run tests/commitment-hoy.test.js tests/dial-sync.test.js tests/dial-history.test.js`.
4. `npm test` completo contra el baseline de 34-01.
5. `git diff --stat package.json package-lock.json` y `git diff --stat public/style.css` vacíos.
6. **Verificación por mutación**: romper temporalmente la condición
   `new Date(_commitDueAt(l)).getTime() <= endTodayTs` de `misCompromisos`
   (por ejemplo, sacando la comparación de fecha) y confirmar que
   `tests/hoy-sections.test.js` se pone en rojo en un test específico y
   contable. Restaurar con `git checkout -- public/app.js` y confirmar
   `git diff public/app.js` vacío.
</verification>

<success_criteria>
- Hoy muestra 6-7 bloques en el orden de D-01, con exclusividad real entre
  los 5 primeros tiers.
- Un lead cerrado (deal ganado) nunca aparece en ninguna sección.
- La Red de seguridad solo cuenta leads con al menos una llamada.
- El tier "Nuevos" solo aparece cuando no hay nada pendiente arriba.
- El filtro por país existe, ordena por horario hábil, y persiste por usuario.
- Cache-buster bumpeado; `style.css` intacto.
</success_criteria>

<output>
Crear `.planning/phases/34-hoy-vista-diaria/34-02-SUMMARY.md` al terminar.

Documentar explícitamente:
- El valor del cache-buster ANTES y DESPUÉS (leído de disco).
- Confirmación de que el ÚNICO test roto a propósito en `commitment-hoy.test.js`
  fue exactamente el esperado (nombrarlo) y que el resto pasó sin tocar.
- Qué queda SIN verificar en vivo: cómo se ve la Red de seguridad colapsada
  vs. abierta en un browser real; si el `<details>`/`<summary>` se ve bien
  con el resto del lenguaje visual de Hoy; el desplegable de país con datos
  reales de producción.
- Qué queda para 34-03: los botones de Power Dialer de "Mis compromisos"/
  "Esperando del prospecto"/"Reintentos de no-contacto" están con
  `dialerMode: null` a propósito — 34-03 los activa junto con el rewrite de
  `_pdBuildQueueHoy`. El panel de higiene (HOY-05) todavía no existe en la UI.
</output>
