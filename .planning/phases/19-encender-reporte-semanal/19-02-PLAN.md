---
phase: 19-encender-reporte-semanal
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified: [tests/weekly-report.test.js]
autonomous: true
requirements: [REP-01, REP-02, REP-03]
must_haves:
  truths:
    - "Existe un test de regresión del cron semanal que cubre día/hora/TZ de negocio/anti-duplicado — hoy no existe ninguno"
    - "Si alguien reintroduce el bug del duplicado o rompe la exclusión de Ignacio/Paula, la suite falla"
    - "La suite completa del repo queda verde (~836 tests) antes de cerrar la phase"
  artifacts:
    - path: "tests/weekly-report.test.js"
      provides: "Regresión completa: ventana del cron, anti-dup, TZ, fallo-no-persiste, destinatarios, shape sin wsp, exclusión admin-only, round-trip export/import de reports"
      min_lines: 120
  key_links:
    - from: "tests/weekly-report.test.js"
      to: "globalThis.__weeklyReport"
      via: "helpers expuestos por el plan 19-01"
      pattern: "globalThis\\.__weeklyReport"
    - from: "tests/weekly-report.test.js"
      to: "reports.json"
      via: "lectura/borrado del archivo en DATA_DIR entre tests"
      pattern: "reports\\.json"
---

<objective>
Test de regresión del reporte semanal (REP-01 exige "test de regresión del cron
(día/hora/TZ de negocio/anti-duplicado) — hoy no existe ninguno") + verificación de
la suite completa del repo.

Purpose: garantizar que el bug de los 16 mails del lunes no pueda volver sin que la
suite lo grite, y que el shape nuevo del reporte (sin WSP, sin Ignacio/Paula) quede
blindado.

Output: `tests/weekly-report.test.js` (~12 tests) + suite completa verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-encender-reporte-semanal/19-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contratos creados por el plan 19-01 — usar tal cual, sin explorar. -->
```js
globalThis.__weeklyReport = {
  // async; retorna { ran, sent?, to?, reason? }. nowTs y sendFn inyectables.
  maybeRunWeeklyReportCron(nowTs = Date.now(), sendFn = sendWeeklyReport),
  buildWeeklyReportData(),      // → { period, calls, calendar, perSetter, leadsTotal } — SIN clave wsp
  buildWeeklyReportHtml(data),  // → string html sin sección WhatsApp
  sendWeeklyReport(toEmails, dataOverride), // toEmails: string | string[]
  loadReportsState(), saveReportsState(state), getReportsFile(),
  _reportRecipients(),          // lee process.env.REPORT_EMAILS en cada llamada
};
globalThis.__metricsAudit // ya existía: { _bizStartOfDay, _bizDayOfWeek, _bizHour, _bizDayStr, ... }
```
Reasons del cron: 'fuera_de_ventana' | 'ya_enviado' | 'sin_destinatarios'.
perSetter rows: { name, leadsAsignados, llamadas, atendidas, agendadosLlamada }.
Endpoints: GET /api/admin/export-data (incluye bloque `reports`),
POST /api/admin/import-data (acepta `{ reports: {...} }`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/weekly-report.test.js — regresión del cron + shape del reporte + persistencia</name>
  <files>tests/weekly-report.test.js</files>
  <read_first>
    - tests/metrics-timezone-attribution.test.js (patrón de setup COMPLETO a replicar: tmpData + env vars ANTES del import, pwd() con scrypt, fixture de setters.json reescrito en beforeAll, login supertest)
    - index.js: bloque del reporte semanal post-19-01 (buscar `globalThis.__weeklyReport`) para confirmar firmas reales
  </read_first>
  <behavior>
    Fechas fijas determinísticas (BUSINESS_TZ=America/Argentina/Buenos_Aires, UTC-3 sin DST; 2026-07-27 y 2026-08-03 son lunes):
    - MON9  = Date.UTC(2026, 6, 27, 12, 0, 0)  → lunes 09:00 AR (dentro de ventana)
    - MON7  = Date.UTC(2026, 6, 27, 10, 30, 0) → lunes 07:30 AR (antes de las 8)
    - TUE   = Date.UTC(2026, 6, 28, 15, 0, 0)  → martes (fuera)
    - MONTZ = Date.UTC(2026, 6, 28, 2, 0, 0)   → martes 02:00 UTC = lunes 23:00 AR (prueba TZ: dentro)
    - NEXTMON = Date.UTC(2026, 7, 3, 12, 0, 0) → lunes siguiente 09:00 AR

    Tests del cron (fake sendFn que cuenta invocaciones; borrar reports.json del DATA_DIR antes de cada caso que arranca "sin estado"):
    - Test 1: TUE → { ran:false, reason:'fuera_de_ventana' }, sendFn no llamado
    - Test 2: MON7 → { ran:false, reason:'fuera_de_ventana' }
    - Test 3 (el bug histórico de los 16 mails): sin reports.json, MON9 → sent:true y el archivo queda con lastWeeklyReportAt === new Date(MON9).toISOString(); segunda llamada MON9+1h → { ran:false, reason:'ya_enviado' } y sendFn total 1 sola vez
    - Test 4 (TZ): sin reports.json, MONTZ → ran:true (lunes 23:00 AR aunque en UTC ya es martes)
    - Test 5: con lastWeeklyReportAt = ISO(MON9), NEXTMON (+7 días) → ran:true, sent:true (vuelve a mandar)
    - Test 6 (fallo no persiste): sin reports.json, MON9 con sendFn que retorna {sent:false, reason:'x'} → { ran:true, sent:false } y loadReportsState().lastWeeklyReportAt undefined; reintento MON9+1h con sendFn ok → sent:true
    - Test 7 (destinatarios): REPORT_EMAILS=" a@x.com , b@x.com " → _reportRecipients() === ['a@x.com','b@x.com']; REPORT_EMAILS="" → [ADMIN_EMAIL del env]
    - Test 8: sin reports.json y REPORT_EMAILS='a@x.com,b@x.com', MON9 → el fake sendFn recibe ['a@x.com','b@x.com'] (resetear REPORT_EMAILS='' al final)

    Tests del shape (fixture setters.json reescrito en beforeAll DESPUÉS del import, con llamadas en la semana pasada calculada con __metricsAudit: thisMonday = _bizStartOfDay(now) - ((_bizDayOfWeek(_bizStartOfDay(now))||7)-1)*86400000; lastWeekTs = thisMonday - 3*86400000):
    - Fixture: setters [{id:'s_v',name:'Vendedora'},{id:'setter_ignacio',name:'Ignacio'}]; users u_v (setterId s_v) y u_ign (setterId setter_ignacio); leads con callLog en lastWeekTs: u_v → answered_interested (duration 60), hung_up (duration 5), no_answer; u_ign → answered_interested (duration 120)
    - Test 9: buildWeeklyReportData() → sin clave wsp ('wsp' in d === false); calls.totalWeek === 3 (la de Ignacio NO cuenta); calls.answeredWeek === 2 (answered_interested + hung_up; no_answer no es connect); perSetter con 1 sola row (name 'Vendedora', llamadas 3, atendidas 2) y ningún name 'Ignacio'
    - Test 10: buildWeeklyReportHtml(d) no matchea /WhatsApp/ ni /Conexiones/, y no contiene 'Ignacio'

    Tests de persistencia (supertest con login admin, patrón cookie gs_session):
    - Test 11: saveReportsState({lastWeeklyReportAt:'2026-07-27T12:00:00.000Z'}) → GET /api/admin/export-data → body.reports.lastWeeklyReportAt === ese valor
    - Test 12: borrar reports.json → POST /api/admin/import-data {reports:{lastWeeklyReportAt:'2026-07-27T12:00:00.000Z'}} → 200 y loadReportsState().lastWeeklyReportAt restaurado
  </behavior>
  <action>
Crear `tests/weekly-report.test.js` con el patrón de setup EXACTO de
tests/metrics-timezone-attribution.test.js:

```js
const tmpData = path.join(os.tmpdir(), `weekly-report-${Date.now()}`);
fs.mkdirSync(tmpData, { recursive: true });
process.env.NODE_ENV = "test";
process.env.DATA_DIR = tmpData;
process.env.ADMIN_EMAIL = "admin-wr@local.test";
process.env.ADMIN_PASSWORD = "wrpass1234";
process.env.JWT_SECRET = "test-secret-wr";
process.env.BUSINESS_TZ = "America/Argentina/Buenos_Aires";
process.env.RESEND_API_KEY = "";   // regla #121: definida-VACÍA, NUNCA delete
process.env.REPORT_EMAILS = "";
```

Pre-popular `auth.json` (admin + u_v + u_ign con pwd scrypt, mismo helper pwd() del
patrón) y un `setters.json` mínimo ANTES de `const { app } = await import("../index.js");`.
Después del import: `const W = globalThis.__weeklyReport; const M = globalThis.__metricsAudit;`.
En beforeAll: login admin vía supertest (cookie gs_session) y reescribir setters.json
con el fixture de llamadas de la semana pasada (los loaders leen de disco en cada
llamada — el rewrite post-import es el patrón establecido).

Helper local `resetReportsState()` que hace `fs.rmSync(path.join(tmpData,'reports.json'), {force:true})`.
Fake senders: `const mkSend = (result = {sent:true}) => { const calls = []; const fn = async (to) => { calls.push(to); return result; }; fn.calls = calls; return fn; };`

Implementar los 12 tests del behavior. Los tests del cron NUNCA llaman al
sendWeeklyReport real (el fake evita red — no depende de RESEND_API_KEY).
Los tests que mutan process.env.REPORT_EMAILS lo restauran a "" al final del test
(afterEach o inline) para no contaminar los siguientes.
Comentario cabecera del archivo en español explicando qué regresión cubre
(bug de los 16 mails del lunes, REP-01/02/03), estilo del repo.
  </action>
  <verify>
    <automated>npx vitest run tests/weekly-report.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/weekly-report.test.js` → 12 tests (mínimo), todos verdes
    - `grep -c "process.env.RESEND_API_KEY = \"\"" tests/weekly-report.test.js` → 1 (definida-vacía, sin ningún `delete process.env`)
    - `grep -c "delete process.env" tests/weekly-report.test.js` → 0
    - `grep -n "ya_enviado" tests/weekly-report.test.js` → al menos 1 match (anti-dup asserted)
    - `grep -n "Date.UTC(2026, 6, 28, 2" tests/weekly-report.test.js` → 1 match (caso TZ lunes 23:00 AR)
    - `grep -n "setter_ignacio" tests/weekly-report.test.js` → matches (fixture de exclusión presente)
  </acceptance_criteria>
  <done>El cron, los destinatarios, el shape sin WSP, la exclusión de admin-only y el round-trip export/import de reports.json tienen regresión automatizada.</done>
</task>

<task type="auto">
  <name>Task 2: Suite completa verde (gate de cierre de la phase)</name>
  <files>tests/weekly-report.test.js</files>
  <read_first>
    - CLAUDE.md: sección Tests (notas #93/#121 — flakies ambientales conocidos y política de retry)
  </read_first>
  <action>
Correr la suite completa del repo: `npm test`. Criterio de cierre del milestone:
"Suite completa verde antes de cerrar la phase (~836 tests)".

Si falla algo:
- `wa-campaign-engine` (depende de hora/día) es el único flaky ambiental conocido
  (nota #93/#110): re-correr en aislado (`npx vitest run tests/wa-campaign-engine.test.js`);
  si pasa en retry, NO es regresión — documentarlo en el SUMMARY.
- Cualquier otro fallo relacionado con weekly report / onboarding / metrics-consistency
  ES regresión de esta phase: arreglar en tests/weekly-report.test.js si el test nuevo
  asserta mal, o reportar el gap si el problema está en index.js (no parchear index.js
  desde este plan sin entender la causa — ese archivo lo modificó el plan 19-01).

No tocar ningún otro archivo del repo en esta task.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` termina con exit code 0 (o con el único fallo siendo el flaky ambiental conocido `wa-campaign-engine`, que pasa en re-corrida aislada — documentado en el SUMMARY si ocurre)
    - Los suites tocados por la phase pasan todos: weekly-report, onboarding, metrics-consistency
  </acceptance_criteria>
  <done>Suite completa verde — la phase puede cerrarse sin deuda de tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests→filesystem | los tests escriben solo en tmpdir (DATA_DIR aislado) — nunca en data/ del repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | T | data/ del repo contaminado por tests | mitigate | DATA_DIR = os.tmpdir() + guard existente de index.js (VITEST fallback a tmp, nota #20/regla #15) — el fixture jamás toca ./data |
| T-19-07 | I | Llamadas de red reales desde tests | mitigate | RESEND_API_KEY="" (definida-vacía) + sendFn fake inyectado — ningún test pega a Resend |
</threat_model>

<verification>
- `npx vitest run tests/weekly-report.test.js` → 12+ tests verdes.
- `npm test` → suite completa verde (módulo flaky ambiental conocido).
- Grep gates de la Task 1 cumplidos.
</verification>

<success_criteria>
- REP-01 verificado por regresión: día/hora/TZ de negocio/anti-duplicado/fallo-no-persiste.
- REP-02 verificado: parsing CSV de REPORT_EMAILS + fallback + array entregado al sender.
- REP-03 verificado: data sin `wsp`, html sin sección WhatsApp, Ignacio excluido de totales y perSetter.
- Persistencia verificada: round-trip export-data → import-data de `reports`.
- Suite completa verde (~836 + 12 nuevos).
</success_criteria>

<output>
Al completar, crear `.planning/phases/19-encender-reporte-semanal/19-02-SUMMARY.md`
(template summary.md).
</output>
