---
phase: 19-encender-reporte-semanal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.js, scripts/pre-deploy.js, tests/onboarding.test.js]
autonomous: true
requirements: [REP-01, REP-02, REP-03]
user_setup:
  - service: resend
    why: "Envío del reporte semanal por email (sin la key, el cron loguea 'RESEND_API_KEY no configurada' y no manda nada)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key → cargar en Railway -> Variables"
      - name: REPORT_EMAILS
        source: "Opcional: CSV de destinatarios (ej. 'socio1@x.com,socio2@x.com,socio3@x.com') en Railway -> Variables. Sin ella, fallback a ADMIN_EMAIL."
must_haves:
  truths:
    - "El cron del lunes >=8am (TZ negocio) manda exactamente UN mail por semana: no crashea por ReferenceError y el anti-duplicado persiste en reports.json"
    - "El reporte sale a una lista configurable de destinatarios (REPORT_EMAILS CSV, fallback ADMIN_EMAIL, fallback admin activo de auth.json)"
    - "El mail semanal NO contiene la sección WhatsApp ni ningún acumulado histórico bajo el encabezado 'Semana del X al Y'"
    - "Ignacio y Paula (ADMIN_ONLY_SETTER_IDS) no aparecen en ninguna métrica ni fila del reporte"
    - "reports.json sobrevive redeploys de Railway (export-data, import-data, pre-deploy, seedVolumeFromRepo, BACKUP_FILES)"
  artifacts:
    - path: "index.js"
      provides: "maybeRunWeeklyReportCron(nowTs, sendFn) sin ReferenceError + _reportRecipients() + buildWeeklyReportData derivado del CALL METRICS CORE + globalThis.__weeklyReport"
      contains: "globalThis.__weeklyReport"
    - path: "scripts/pre-deploy.js"
      provides: "descarga de reports.json en el pre-deploy"
      contains: "reports.json"
    - path: "tests/onboarding.test.js"
      provides: "assertion de wsp actualizada al nuevo shape (wsp ya no existe)"
  key_links:
    - from: "maybeRunWeeklyReportCron"
      to: "saveReportsState"
      via: "new Date(nowTs).toISOString() tras envío exitoso"
      pattern: "new Date\\(nowTs\\)\\.toISOString\\(\\)"
    - from: "buildWeeklyReportData"
      to: "_ccCollectCalls / _ccFunnelAggregate"
      via: "CALL METRICS CORE con visibleSet = _SUPERVISOR_EXCLUSION_SET"
      pattern: "_ccCollectCalls\\(settersData"
    - from: "/api/admin/export-data"
      to: "loadReportsState"
      via: "bloque reports en el JSON de export"
      pattern: "reports = loadReportsState"
---

<objective>
Arreglar de raíz el reporte semanal existente: (1) el cron muere o duplica por un
`ReferenceError` (`now` no declarado en `maybeRunWeeklyReportCron`), (2) solo sale a
UN destinatario, (3) la sección "💬 WhatsApp" del mail presenta acumulados históricos
all-time bajo un encabezado que dice "Semana del X al Y" — con el embudo WSP muerto
desde Phase 15 — y (4) `data/reports.json` no sobrevive un redeploy de Railway porque
no está en el patrón de persistencia de 4 lugares.

Purpose: Phase 19 del milestone v2.0 — el reporte tiene que llegar solo, una vez,
a los 3 socios, sin datos engañosos. Es la base sobre la que Phase 21 monta el
reporte diario.

Output: `index.js` con el bloque del reporte semanal reescrito (cron testeable con
`nowTs`/`sendFn` inyectables, destinatarios configurables, data derivada del CALL
METRICS CORE, sin sección WSP), `scripts/pre-deploy.js` con `reports.json`, y
`tests/onboarding.test.js` con la assertion de `wsp` actualizada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

Bloque a modificar: index.js líneas ~1742-1897 (comentario `// ── Reporte semanal
automático (Resend) ──` hasta el endpoint `POST /api/admin/weekly-report/send`).
⚠️ El user commitea en paralelo — ubicar SIEMPRE por nombre de función/string, nunca
por número de línea.

<interfaces>
<!-- Contratos existentes que el executor usa tal cual — NO explorar el codebase, están acá. -->

CALL METRICS CORE (index.js ~5620, function declarations hoisted — llamables desde
buildWeeklyReportData aunque se definan después en el archivo):
```js
// Aplana todos los callLog en entries pre-atribuidas por quién llamó (_callSetterId).
// visibleSet: pseudo-set con .has(setterId) — filtra entries.
_ccCollectCalls(data, { setterId = '', visibleSet = null, channel = '' } = {})
  // → [{ ts:number, outcome:string, duration:number, setterId:string, leadId, disqualifyReason }]

_ccFunnelAggregate(calls, calendar, fromTs, toTs, { setterId = '', visibleSet = null } = {})
  // → { dials, connects, conversations, appointments, deals, revenue, totalDurationS,
  //     byReason, avgConvDurationS, rates: { connectRate:Number, ... } }
  // connectRate viene como Number con 1 decimal (ej. 33.3)
```

Exclusión de setters admin-only (index.js ~5588 — const a nivel módulo, accesible en
runtime desde funciones definidas antes):
```js
const ADMIN_ONLY_SETTER_IDS = new Set(['setter_ignacio', 'setter_paula_kroff']);
const _SUPERVISOR_EXCLUSION_SET = { has: (id) => !ADMIN_ONLY_SETTER_IDS.has(id) };
function _filterSettersVisible(setters, visibleSet) // visibleSet null → sin filtrar
```

Helpers TZ de negocio (nivel módulo, ya usados por el cron actual):
```js
_bizStartOfDay(ts) // medianoche del día de negocio (number)
_bizDayOfWeek(ts)  // 1=lunes ... (usar `|| 7` para domingo, como el código actual)
_bizHour(ts)       // hora 0-23 en TZ negocio
_bizDayStr(ts)     // 'YYYY-MM-DD' en TZ negocio
```

Outcomes canónicos: `COLD_CALL_CONNECT_OUTCOMES` (Set: answered_interested,
answered_not_interested, scheduled_with_admin, callback_later, hung_up).

Persistencia reports (ya existen, NO tocar): `getReportsFile()`, `loadReportsState()`
→ `{}` si no existe el archivo, `saveReportsState(state)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix del cron (ReferenceError `now`) + destinatarios múltiples + testabilidad</name>
  <files>index.js</files>
  <read_first>
    - index.js: bloque completo desde `// ── Reporte semanal automático (Resend) ──` hasta el cierre del endpoint `POST /api/admin/weekly-report/send` (buscar por esos strings, no por línea)
  </read_first>
  <action>
Seis cambios en el bloque del reporte semanal de index.js (comentarios en español,
estilo del archivo):

**(a) Nuevo helper `_reportRecipients()`** — insertarlo justo antes de
`sendWeeklyReport`. Decisión de diseño (discreción documentada): la lista vive en la
env var `REPORT_EMAILS` (CSV), consistente con el resto de la config del proyecto
(patrón WA_CORS_ORIGINS / env vars en Railway), con doble fallback:

```js
// Destinatarios del reporte (REP-02): env REPORT_EMAILS (CSV) > ADMIN_EMAIL >
// primer admin activo de auth.json. Editable sin deploy desde Railway Variables.
function _reportRecipients() {
  const csv = String(process.env.REPORT_EMAILS || '').trim();
  let list = csv
    ? csv.split(',').map(s => s.trim()).filter(s => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(s))
    : [];
  if (!list.length && process.env.ADMIN_EMAIL) list = [process.env.ADMIN_EMAIL];
  if (!list.length) {
    try {
      const admin = (loadAuthData().users || []).find(u => u.role === 'admin' && u.status === 'active');
      if (admin?.email) list = [admin.email];
    } catch {}
  }
  return list;
}
```

**(b) `sendWeeklyReport` acepta string O array** (retro-compatible con el endpoint
manual). Cambiar la firma a `sendWeeklyReport(toEmails, dataOverride = null)` y,
después del guard de `resendKey`, agregar:
```js
const to = (Array.isArray(toEmails) ? toEmails : [toEmails]).filter(Boolean);
if (!to.length) return { sent: false, reason: 'Sin destinatarios' };
```
En el body del fetch a Resend cambiar `to: [toEmail]` → `to` (Resend ya acepta array).

**(c) Reescribir `maybeRunWeeklyReportCron` completa** — el fix de REP-01. La versión
actual usa `now.getTime()` y `now.toISOString()` con `now` NUNCA declarado (solo
existe `nowTs`): con reports.json vacío manda el mail y explota en el callback sin
persistir (→ re-manda cada tick horario del lunes, hasta 16 mails); con estado
existente el ReferenceError es síncrono y el cron muere. Reemplazo exacto:

```js
// REP-01 (2026-07-25): fix del ReferenceError (`now` no existía — solo nowTs) que
// duplicaba el mail cada tick horario del lunes o mataba el cron. nowTs y sendFn
// son inyectables para los tests (patrón campaignEngineTick, regla #72).
async function maybeRunWeeklyReportCron(nowTs = Date.now(), sendFn = sendWeeklyReport) {
  // Lunes 8am en TZ de negocio (antes era TZ del server = UTC → 5am AR).
  if (_bizDayOfWeek(nowTs) !== 1 || _bizHour(nowTs) < 8) return { ran: false, reason: 'fuera_de_ventana' };
  const state = loadReportsState();
  const last = state.lastWeeklyReportAt ? new Date(state.lastWeeklyReportAt).getTime() : 0;
  if (last && (nowTs - last) < 6 * 24 * 60 * 60 * 1000) return { ran: false, reason: 'ya_enviado' };
  const recipients = _reportRecipients();
  if (!recipients.length) { console.warn('Weekly report skipped: sin destinatarios'); return { ran: false, reason: 'sin_destinatarios' }; }
  const result = await sendFn(recipients);
  if (result.sent) {
    state.lastWeeklyReportAt = new Date(nowTs).toISOString();
    state.lastWeeklyReportTo = recipients;
    saveReportsState(state);
    console.log(`📨 Reporte semanal enviado a ${recipients.join(', ')}`);
    return { ran: true, sent: true, to: recipients };
  }
  console.warn('Weekly report failed:', result.reason);
  return { ran: true, sent: false, reason: result.reason };
}
```
Nota: la resolución de `adminEmail` de la versión vieja queda absorbida por
`_reportRecipients()` (mismo fallback a auth.json). Si el envío falla, NO se persiste
`lastWeeklyReportAt` → el próximo tick horario del lunes reintenta (comportamiento
intencional, igual que antes).

**(d) Timers**: la función ahora es async — reemplazar las dos líneas del bloque
`if (process.env.NODE_ENV !== 'test')` por:
```js
if (process.env.NODE_ENV !== 'test') {
  setInterval(() => maybeRunWeeklyReportCron().catch(e => console.warn('weekly cron:', e.message)), 60 * 60 * 1000);
  setTimeout(() => maybeRunWeeklyReportCron().catch(e => console.warn('weekly cron:', e.message)), 60 * 1000);
}
```

**(e) Endpoint manual `POST /api/admin/weekly-report/send`** — multi-destinatario con
validación de formato (solo admin llega acá, pero se valida igual). Reemplazar el
cuerpo del handler por:
```js
const raw = req.body?.to;
const to = raw
  ? (Array.isArray(raw) ? raw : [raw]).map(s => String(s).trim()).filter(s => /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(s))
  : _reportRecipients();
if (!to.length) return res.status(400).json({ error: 'No hay email destinatario.' });
const result = await sendWeeklyReport(to);
if (!result.sent) return res.status(500).json(result);
const state = loadReportsState();
state.lastWeeklyReportAt = new Date().toISOString();
state.lastWeeklyReportTo = to;
saveReportsState(state);
res.json({ ok: true, ...result, to });
```
(El test existente de onboarding.test.js "send sin RESEND_API_KEY → 500 con reason"
sigue verde: `to: "test@test.com"` pasa la regex y `sendWeeklyReport` corta en el
guard de la key con reason que matchea /RESEND/i.)

**(f) Exponer para tests** (patrón `globalThis.__callCore` / `__metricsAudit`) —
insertar DESPUÉS del cierre del endpoint `POST /api/admin/weekly-report/send`:
```js
// Expuestos para tests (patrón __callCore / __metricsAudit).
globalThis.__weeklyReport = { maybeRunWeeklyReportCron, buildWeeklyReportData, buildWeeklyReportHtml, sendWeeklyReport, loadReportsState, saveReportsState, getReportsFile, _reportRecipients };
```

NO loguear nunca `process.env.RESEND_API_KEY` ni los headers del fetch a Resend.
  </action>
  <verify>
    <automated>node --check index.js && NODE_ENV=test DATA_DIR="$(mktemp -d)" ADMIN_EMAIL=a@b.co node --input-type=module -e "await import('./index.js'); const W = globalThis.__weeklyReport; const tue = await W.maybeRunWeeklyReportCron(Date.UTC(2026,6,28,15), async()=>({sent:true})); if (tue.ran !== false) { console.error('FAIL martes'); process.exit(1); } const mon = Date.UTC(2026,6,27,12); let sends = 0; const r1 = await W.maybeRunWeeklyReportCron(mon, async(to)=>{ sends++; return {sent:true, to}; }); const r2 = await W.maybeRunWeeklyReportCron(mon + 3600000, async()=>{ sends++; return {sent:true}; }); if (!(r1.sent === true && r2.ran === false && sends === 1)) { console.error('FAIL anti-dup', r1, r2, sends); process.exit(1); } console.log('OK cron'); process.exit(0);"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async function maybeRunWeeklyReportCron(nowTs = Date.now(), sendFn = sendWeeklyReport)" index.js` → 1 match
    - `grep -n "new Date(nowTs).toISOString()" index.js` → 1 match (dentro del cron)
    - `grep -n "function _reportRecipients" index.js` → 1 match
    - `grep -n "globalThis.__weeklyReport" index.js` → 1 match
    - `sed -n '/Reporte semanal automático/,/weekly-report\/send/p' index.js | grep -cE "now\.getTime\(\)|now\.toISOString\(\)"` devuelve 0 (no queda referencia al `now` fantasma en el bloque del reporte semanal; OJO: NO greppear el archivo entero — index.js:6811 y :6980 usan `now.toISOString()` con un `now` local válido en handlers de import de leads, fuera de scope; NO tocarlos)
    - El smoke del verify imprime "OK cron": martes no corre, lunes 09:00 AR (Date.UTC(2026,6,27,12) — 2026-07-27 es lunes) manda exactamente 1 vez, y el segundo tick del mismo lunes queda bloqueado por el anti-duplicado
  </acceptance_criteria>
  <done>El cron es determinístico y testeable: ventana lunes >=8am TZ negocio, anti-duplicado persistido sin ReferenceError, destinatarios desde REPORT_EMAILS/ADMIN_EMAIL/auth.json, endpoint manual multi-destinatario, todo expuesto en globalThis.__weeklyReport.</done>
</task>

<task type="auto">
  <name>Task 2: buildWeeklyReportData al CALL METRICS CORE + exclusión Ignacio/Paula + quitar sección WhatsApp</name>
  <files>index.js, tests/onboarding.test.js</files>
  <read_first>
    - index.js: `buildWeeklyReportData` y `buildWeeklyReportHtml` completas (buscar por nombre de función)
    - tests/onboarding.test.js: describe "admin · weekly report" (contiene `expect(r.body.data.wsp).toBeDefined()` — ~línea 670)
    - tests/metrics-consistency.test.js: test "A4: reporte semanal" (asserta totalWeek/answeredWeek contra fixture de s_a/s_b — debe seguir verde sin tocarlo)
  </read_first>
  <action>
**(a) Reemplazar `buildWeeklyReportData` completa** por esta versión (REP-03 + regla
milestone "toda métrica deriva del core" + regla "solo vendedoras nuevas"):

```js
function buildWeeklyReportData() {
  const settersData = loadSettersData();
  // Regla milestone v2.0: solo vendedoras nuevas — Ignacio y Paula (admin-only)
  // fuera de todo reporte. Mismo pseudo-set que usa el supervisor sin lista.
  const visibleSet = _SUPERVISOR_EXCLUSION_SET;
  const calendar = (settersData.calendar || []).filter(e => !ADMIN_ONLY_SETTER_IDS.has(e.setterId));
  // Semana pasada completa (lunes a lunes) en TZ de negocio (audit 2026-07-08).
  const nowTs = Date.now();
  const todayStart = _bizStartOfDay(nowTs);
  const dayOfWeek = _bizDayOfWeek(todayStart) || 7;
  const thisMonday = todayStart - (dayOfWeek - 1) * 86400000;
  const fromTs = thisMonday - 7 * 86400000;
  const toTs = thisMonday;
  // CALL METRICS CORE (regla v2.0): jamás re-implementar el funnel inline.
  const calls = _ccCollectCalls(settersData, { visibleSet });
  const agg = _ccFunnelAggregate(calls, calendar, fromTs, toTs, { visibleSet });
  const weekCalls = calls.filter(c => c.ts >= fromTs && c.ts < toTs);
  // deadWeek = EVENTOS de la semana (llamadas que terminaron en número muerto).
  // Distinto del KPI "Números muertos" del Comando (estado phoneStatus actual).
  const callsDeadWeek = weekCalls.filter(c => c.outcome === 'wrong_number' || c.outcome === 'invalid_number').length;
  const calRealized = calendar.filter(e => { const t = e.fecha ? new Date(e.fecha).getTime() : 0; return e.calendarioEstado === 'realizada' && t >= fromTs && t < toTs; }).length;
  const calNoShow = calendar.filter(e => { const t = e.fecha ? new Date(e.fecha).getTime() : 0; return e.calendarioEstado === 'no_show' && t >= fromTs && t < toTs; }).length;
  const calPendingNow = calendar.filter(e => e.calendarioEstado === 'pendiente').length;
  const calOverdueNow = calendar.filter(e => e.calendarioEstado === 'pendiente' && e.fecha && new Date(e.fecha).getTime() < nowTs).length;
  const allLeads = Object.values(settersData.leads || {});
  // Por SDR: llamadas de la semana atribuidas por quién llamó (entries ya vienen
  // pre-atribuidas de _ccCollectCalls). Sin columna WSP (embudo muerto, REP-03).
  const perSetter = _filterSettersVisible(settersData.setters || [], visibleSet).map(s => {
    const w = weekCalls.filter(c => c.setterId === s.id);
    return {
      name: s.name,
      leadsAsignados: allLeads.filter(l => l.assignedTo === s.id).length,
      llamadas: w.length,
      atendidas: w.filter(c => COLD_CALL_CONNECT_OUTCOMES.has(c.outcome)).length,
      agendadosLlamada: w.filter(c => c.outcome === 'scheduled_with_admin').length,
    };
  }).filter(s => s.llamadas > 0);
  return {
    period: { from: _bizDayStr(fromTs), to: _bizDayStr(toTs - 1) },
    calls: {
      totalWeek: agg.dials,
      answeredWeek: agg.connects,
      scheduledWeek: agg.appointments,
      deadWeek: callsDeadWeek,
      pctAtendidas: agg.dials > 0 ? agg.rates.connectRate.toFixed(1) : '0.0',
    },
    calendar: { realized: calRealized, noShow: calNoShow, pendingNow: calPendingNow, overdueNow: calOverdueNow },
    perSetter,
    leadsTotal: allLeads.length
  };
}
```
La clave `wsp` DESAPARECE del objeto (era: `conexionesNew` semanal mezclado con
`respondieronTotal`/`interesadosTotal`/`agendadosTotal` ALL-TIME bajo encabezado
"semana" — el dato engañoso de REP-03). El embudo WSP está en cero desde Phase 15 y
la regla del milestone es "nada de métricas en cero" → la sección se quita entera.
Notas de compatibilidad: `answeredWeek = agg.connects` conserva la definición canónica
(el test A4 de metrics-consistency sigue verde — su fixture usa solo s_a/s_b, no
setters admin-only); `pctAtendidas` sigue siendo string con 1 decimal.

**(b) `buildWeeklyReportHtml`**: en la primera línea cambiar el destructuring
`const { period, wsp, calls, calendar: cal, perSetter, leadsTotal } = data;` →
`const { period, calls, calendar: cal, perSetter, leadsTotal } = data;`.
Del template string eliminar el bloque completo de la sección WhatsApp: desde
`<h3 ...>💬 WhatsApp</h3>` hasta el `</div>` que cierra su fila de cards inclusive
(las 4 cards `Conexiones nuevas`/`Respondieron (total)`/`Interesados (total)`/
`Agendados (total)`). La sección "📞 Llamadas (semana)" pasa a ser la primera.
En la tabla por setter: título `👤 Por setter` → `👤 Por SDR`, header `Setter` → `SDR`
(wording de la barrida #153 de CLAUDE.md), y reemplazar la columna `Conexiones` por
`Atendidas`: las celdas de cada row quedan `${s.name} | ${s.leadsAsignados} |
${s.llamadas} | ${s.atendidas} | ${s.agendadosLlamada}` (5 columnas: SDR, Leads,
Llamadas, Atendidas, Agendados — el colspan del empty-row queda en 5).

**(c) `tests/onboarding.test.js`**: en el test "GET /api/admin/weekly-report/preview
devuelve data + html", cambiar la línea `expect(r.body.data.wsp).toBeDefined();` por
`expect(r.body.data.wsp).toBeUndefined(); // REP-03: sección WSP eliminada del semanal`.
  </action>
  <verify>
    <automated>node --check index.js && NODE_ENV=test DATA_DIR="$(mktemp -d)" node --input-type=module -e "await import('./index.js'); const W = globalThis.__weeklyReport; const d = W.buildWeeklyReportData(); if ('wsp' in d) { console.error('FAIL wsp presente'); process.exit(1); } const html = W.buildWeeklyReportHtml(d); if (/WhatsApp|Conexiones/.test(html)) { console.error('FAIL html con seccion WSP'); process.exit(1); } console.log('OK data+html'); process.exit(0);" && npx vitest run tests/onboarding.test.js tests/metrics-consistency.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "conexionesNew" index.js` → 0 y `grep -c "respondieronTotal" index.js` → 0 (la data WSP del reporte ya no se computa)
    - `grep -n "_ccCollectCalls(settersData" index.js` → 1 match dentro de buildWeeklyReportData
    - `grep -n "_SUPERVISOR_EXCLUSION_SET" index.js` muestra un uso dentro de buildWeeklyReportData
    - `grep -c "💬 WhatsApp" index.js` → 0
    - `grep -c "Por SDR" index.js` → 2 (el título nuevo de la tabla del mail en `buildWeeklyReportHtml` + un comentario pre-existente en index.js:7508 de pool-setter-breakdown que NO se toca)
    - El smoke del verify imprime "OK data+html" (sin clave wsp, html sin 'WhatsApp' ni 'Conexiones')
    - `npx vitest run tests/onboarding.test.js tests/metrics-consistency.test.js` → todo verde (incluye A4 del semanal y el 500 sin RESEND_API_KEY)
  </acceptance_criteria>
  <done>El mail semanal muestra SOLO datos de la semana (llamadas + calendario + tabla por SDR), deriva del CALL METRICS CORE, y Ignacio/Paula no aparecen en ningún número ni fila.</done>
</task>

<task type="auto">
  <name>Task 3: reports.json sobrevive redeploys (patrón de persistencia de 4 lugares + BACKUP_FILES)</name>
  <files>index.js, scripts/pre-deploy.js</files>
  <read_first>
    - index.js: handler `GET /api/admin/export-data` completo (buscar `'/api/admin/export-data'`)
    - index.js: handler `POST /api/admin/import-data` completo (destructuring, bloque de validación `errors`, `hasAny`, bloque `restored`)
    - index.js: `seedVolumeFromRepo` (lista de archivos) y `const BACKUP_FILES`
    - scripts/pre-deploy.js: array `extras` (buscar `scrapeBatches`)
  </read_first>
  <action>
Aplicar la regla #21/#128 de CLAUDE.md (mismo bug histórico que faqs/scrape_batches):
sin esto, un container nuevo de Railway arranca sin `reports.json` → el cron re-manda
el mail de una semana ya reportada.

**(a) index.js — `GET /api/admin/export-data`**: junto a los otros loaders con
try/catch agregar:
```js
// Phase 19: estado del reporte semanal (lastWeeklyReportAt) — sin esto, un
// container nuevo de Railway re-manda el mail de una semana ya reportada.
let reports = null;
try { reports = loadReportsState(); } catch {}
```
y agregar `reports` al objeto de `res.json({...})` (después de `scrapeBatches`).

**(b) index.js — `POST /api/admin/import-data`**:
1. Agregar `reports,` al destructuring del `req.body`.
2. En el bloque de validación agregar:
```js
if (reports !== undefined && (!reports || typeof reports !== 'object' || Array.isArray(reports))) {
  errors.push('reports debe ser objeto');
}
```
3. Agregar `|| reports !== undefined` a la condición `hasAny`.
4. En el bloque de restore agregar: `if (reports) { saveReportsState(reports); restored.push('reports'); }`

**(c) index.js — `seedVolumeFromRepo`**: agregar `'reports.json'` a la lista
`['history.json', 'auth.json', ...]` (al final, después de `'scrape_batches.json'`).

**(d) index.js — `BACKUP_FILES`**: agregar `'reports.json'` al final del array.

**(e) scripts/pre-deploy.js**: en el array `extras` agregar la entrada
`['reports', 'reports.json'],` después de la de `scrapeBatches` con el comentario:
```js
// Phase 19: estado del reporte semanal — sin esto un container nuevo re-manda
// el mail de una semana ya reportada.
['reports', 'reports.json'],
```

NO crear `data/reports.json` en el repo (el seed y el backup skipean archivos
inexistentes; el archivo nace en prod con el primer envío y el pre-deploy lo baja).
  </action>
  <verify>
    <automated>node --check index.js && node --check scripts/pre-deploy.js && grep -q "reports = loadReportsState" index.js && grep -q "restored.push('reports')" index.js && grep -c "'reports.json'" index.js | grep -qx "3" && grep -q "'reports', 'reports.json'" scripts/pre-deploy.js && echo OK-persistencia</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "reports = loadReportsState" index.js` → 1 match (en export-data)
    - `grep -n "reports debe ser objeto" index.js` → 1 match (validación de import-data)
    - `grep -n "restored.push('reports')" index.js` → 1 match
    - `grep -c "'reports.json'" index.js` → 3 (getReportsFile en :1746 que YA existe + seedVolumeFromRepo + BACKUP_FILES nuevos)
    - `grep -n "'reports', 'reports.json'" scripts/pre-deploy.js` → 1 match
    - `node --check` pasa en ambos archivos
  </acceptance_criteria>
  <done>reports.json está en los 4 lugares del patrón de persistencia (export-data, import-data, pre-deploy, seedVolumeFromRepo) + BACKUP_FILES — un redeploy de Railway ya no borra el estado anti-duplicado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API admin | body `to` del endpoint manual de envío; payload de import-data |
| server→Resend | request saliente con la API key en el header Authorization |
| env vars Railway | REPORT_EMAILS / RESEND_API_KEY / ADMIN_EMAIL controlan destino y credencial |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | E (elevation) | POST /api/admin/weekly-report/send | mitigate | Ya tiene requireAuth + requireRole('admin') — no tocar los middlewares (tests de RBAC existentes en onboarding.test.js lo garantizan) |
| T-19-02 | T (tampering) | Lista de destinatarios | mitigate | Solo proviene de env vars (REPORT_EMAILS/ADMIN_EMAIL) o de body `to` en endpoint admin-only, validado con regex de email antes de usar; roles no-admin no tienen ninguna vía para alterarla |
| T-19-03 | I (info disclosure) | RESEND_API_KEY en logs | mitigate | Ningún console.log/logError incluye la key ni los headers del fetch; solo se loguean destinatarios y reason |
| T-19-04 | T | import-data restaura reports.json | accept | Endpoint admin-only con validación de shape + backup pre-import — mismo nivel de confianza que el resto de los 13 bloques |
| T-19-05 | D (DoS/spam) | Cron re-mandando mails | mitigate | Guard anti-duplicado persistido (ventana 6 días) + fix del ReferenceError + reports.json en persistencia de redeploys; test de regresión en plan 19-02 |
</threat_model>

<verification>
- `node --check index.js` y `node --check scripts/pre-deploy.js` pasan.
- Smoke del cron (verify Task 1) imprime "OK cron": ventana lunes/hora/TZ + anti-duplicado con exactamente 1 send.
- Smoke de data (verify Task 2) imprime "OK data+html": sin clave `wsp`, html sin sección WhatsApp.
- `npx vitest run tests/onboarding.test.js tests/metrics-consistency.test.js` → verde (los dos suites que tocan el reporte semanal).
- Grep gates de las 3 tasks cumplidos.
</verification>

<success_criteria>
- REP-01: `maybeRunWeeklyReportCron` no referencia `now` sin declarar; con estado vacío manda 1 vez y persiste; con estado del mismo lunes no re-manda.
- REP-02: `_reportRecipients()` devuelve lista desde REPORT_EMAILS CSV con fallback ADMIN_EMAIL → auth.json; `sendWeeklyReport` manda `to` como array a Resend.
- REP-03: ni `buildWeeklyReportData` ni el HTML contienen la sección WhatsApp/acumulados all-time; Ignacio y Paula excluidos de agregados, calendario y tabla por SDR.
- Persistencia: `reports.json` en export-data, import-data, pre-deploy, seedVolumeFromRepo y BACKUP_FILES.
- Sin cambios en frontend → sin cache-buster (regla de la phase).
</success_criteria>

<output>
Al completar, crear `.planning/phases/19-encender-reporte-semanal/19-01-SUMMARY.md`
(template summary.md). Documentar en el SUMMARY la acción pendiente del user:
cargar `RESEND_API_KEY` (y opcionalmente `REPORT_EMAILS`) en Railway → Variables.
</output>
