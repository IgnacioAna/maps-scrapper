---
phase: 30-gate-proximo-paso
plan: 03
type: execute
wave: 3
depends_on: ["30-01", "30-02"]
files_modified:
  - public/app.js
  - public/index.html
  - tests/gate-destination.test.js
autonomous: true
requirements: [GATE-04]

must_haves:
  truths:
    - "Después de marcar CUALQUIER resultado, el sistema dice a dónde se fue el lead y cuándo vuelve"
    - "El aviso nombra la vista real: Hoy → Callbacks, Hoy → Interesados, la cola de Llamadas, Reuniones agendadas o Descartados"
    - "En el Power Dialer el destino aparece dentro del banner '✓ Resultado guardado', sin toast encima"
    - "Mandar un hold de calendario también avisa que el lead queda esperando respuesta y cuándo vuelve"
    - "Un descartado dice cómo volver a encontrarlo (buscándolo por nombre en Llamadas)"
  artifacts:
    - path: "public/app.js"
      provides: "_dispoDestination puro + _dispoAnnounce universal desde _dispoAfterSaved + destino en el banner del Power Dialer"
      contains: "DISPO-DEST"
    - path: "tests/gate-destination.test.js"
      provides: "cobertura de las 6 ramas de destino + aserciones de cableado universal"
  key_links:
    - from: "public/app.js _dispoAfterSaved"
      to: "_dispoAnnounce"
      via: "punto único post-guardado (D-05)"
      pattern: "_dispoAnnounce"
    - from: "_dispoAnnounce"
      to: "_pd.holdMeta"
      via: "rama del Power Dialer (D-07, sin toast)"
      pattern: "holdMeta"
    - from: "_pdRender"
      to: "_pd.holdMeta"
      via: "línea de destino dentro del banner ✓ Resultado guardado"
      pattern: "_holdBanner"
---

<objective>
Cerrar el reclamo #1 del usuario ("lo marco y desaparece"): toda disposición
—incluidas las que hoy no avisan nada— termina diciendo qué lead se movió, a qué
vista real fue y cuándo vuelve. En el Power Dialer el destino se integra al
banner que ya existe en vez de tirar un toast encima del flujo.

Purpose: GATE-04 / R2. El lead SALE de la cola por diseño; lo que faltaba era
decirlo.
Output: `_dispoDestination` (puro, testeable) + `_dispoAnnounce` universal desde
`_dispoAfterSaved`, banner del Power Dialer con destino, cache-buster bumpeado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-gate-proximo-paso/30-CONTEXT.md
@.planning/phases/30-gate-proximo-paso/30-PATTERNS.md
@.planning/phases/30-gate-proximo-paso/30-01-SUMMARY.md
@.planning/phases/30-gate-proximo-paso/30-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- El aviso que se universaliza (a reemplazar) y el punto único donde vive. -->
```javascript
function _dispoWhereToast(leadId, opts = {})  // app.js:8559 — HOY: 3 call sites, se rinde
                                              // ante terminales y ante leads sin callbackAt
function _dispoAfterSaved(leadId)             // app.js:8532 — punto único post-guardado
```

<!-- Dónde vive REALMENTE cada lead después de una disposición (filtros verificados). -->
| Estado resultante | Vista donde aparece | Filtro que lo decide |
|---|---|---|
| `descartado` | ninguna lista normal; se encuentra con el **buscador global de Llamadas** | `hiddenStates` de `renderCallsList` (app.js:8027) + bypass por búsqueda (#183) |
| `agendado` | **Reuniones agendadas** (`view-scheduled-calls`) | `hiddenStates` lo saca de Llamadas |
| `interesado` | **Hoy → Interesados** | `loadHoyView` filtra `estado === 'interesado'` (app.js:5974) |
| último outcome `callback_later` con fecha | **Hoy → Callbacks** | `loadHoyView` exige `callbackAt <= fin de hoy` **y** `lastOutcome === 'callback_later'` (app.js:5970) |
| cualquier otro con `callbackAt` futuro | **cola de Llamadas**, cuando venza | `renderCallsList` oculta `callbackAt` futuro (app.js:8014) |
| `doNotCall` | vista **No-llamar** de Llamadas (`calls-show-dnc`) | app.js:8021 |

<!-- Estado del Power Dialer para D-07. -->
```javascript
_pd.holdCurrent  // bool — la tarjeta se queda tras guardar (#151)
_pd.holdOutcome  // string — outcome mostrado en el banner
// se setean en _pdHandleDisposition (app.js:7226-7227) y _autoMarkNoAnswer (app.js:8633)
// se limpian en app.js:6618-6619, 6716-6717 y _pdBack (7335-7336)
// el banner se arma en _pdRender (app.js:6863-6873) y se inyecta con main.innerHTML
```

<!-- showToast: el mensaje se pinta con textContent (app.js:18276) → no hace falta escapar
     para el toast. El banner del Power Dialer va por innerHTML → SÍ hace falta escHtml. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _dispoDestination puro + aviso universal desde _dispoAfterSaved</name>
  <files>public/app.js</files>
  <read_first>
    - `public/app.js` líneas 8517-8600 (`_dispoGateSet`/`_dispoGateClear`/`_dispoAfterSaved`/`_dispoWhereToast`/`_dispoEnforcementBody`) — es la región que se modifica.
    - `public/app.js` líneas 8607-8642 (`_autoMarkNoAnswer`) — uno de los call sites.
    - `public/app.js` líneas 10617-10695 (`_handleCallDisposition`), 10699-10766 (`openPlaceholderModal`), 10768-10846 (`openCallbackModal`), 10959-10992 (`submit` de `openObjectionModal`), 11039-11088 (`openScheduleModal` + su observer de fallback) — los otros call sites.
    - `public/app.js` la función `openNextStepModal` creada por el plan 30-02.
    - `public/app.js` líneas 5960-5985 (`loadHoyView`: los filtros que deciden en qué sección de Hoy cae cada lead) y 8010-8048 (`renderCallsList`: los filtros de la cola de Llamadas) — la fuente de verdad de las vistas que el aviso tiene que nombrar.
    - `public/app.js` líneas 4608-4700 (bloque `[28-01] DTPICKER-PURE`) — formato de los marcadores del bloque puro. Si ya quedó en contexto por el plan 30-02, NO releer.
  </read_first>
  <behavior>
    - `_dispoDestination(lead, now)` con `estado:'descartado'` → `vista:'Descartados'`, sin fecha, y un texto que dice cómo volver a encontrarlo (buscándolo por nombre en Llamadas).
    - Con `autoDiscarded:true` y `autoDiscardReason:'sin_contacto_2x'` → el texto además dice que fue automático por 2 intentos sin contacto.
    - Con `doNotCall:true` → `vista` de No-llamar, aunque el estado también sea descartado (gana DNC, es más específico).
    - Con `estado:'agendado'` → `vista:'Reuniones agendadas'`.
    - Con `estado:'interesado'` y `nextAction.dueAt` a +3 días → `vista:'Hoy → Interesados'` y `cuando` con el día y la hora.
    - Con último `callLog` outcome `'callback_later'` y `callbackAt` futuro → `vista:'Hoy → Callbacks'` + `cuando`.
    - Con `nextAction.origen:'cadencia'` a +24h (o cualquier otro no-callback_later con fecha futura) → `vista:'la cola de Llamadas'` + `cuando`.
    - Sin `nextAction` y sin `callbackAt`, no terminal → `vista:'la cola de Llamadas'` y `cuando` vacío (sigue disponible ahora).
    - `cuando` se formatea "hoy HH:MM" / "mañana HH:MM" / "<día abreviado> DD/MM HH:MM" según la distancia en días de calendario, igual que hoy hace `_dispoWhereToast`.
    - La función es pura: mismo lead + mismo `now` → mismo resultado; no toca el DOM, la red ni `_pd`.
  </behavior>
  <action>
**A. Bloque puro** en `public/app.js`, delimitado por `// ─── [30-03] DISPO-DEST: INICIO ───` y `// ─── [30-03] DISPO-DEST: FIN ───` (mismo formato que `[28-01] DTPICKER-PURE`), ubicado inmediatamente antes de `_dispoWhereToast` (~8552). Contenido:
   - `_dispoNextAt(lead)` — lector defensivo del reloj: devuelve el timestamp del próximo paso leyendo `lead.nextAction?.dueAt` y cayendo a `lead.callbackAt` cuando `nextAction` es `null` (leads legacy sin migrar; el backend deriva lo mismo con `_deriveNextActionFromLegacy`). Devuelve `0` si no hay nada parseable.
   - `_dispoWhenLabel(ts, now)` — el formato relativo ("hoy 15:30" / "mañana 10:00" / "mar 19/08 10:00"). Mover acá la aritmética que hoy vive inline en `_dispoWhereToast` (app.js:8565-8571), sin cambiarla.
   - `_dispoDestination(lead, now)` → `{ vista, cuando, texto, tono }` con las ramas del `<behavior>`, en ESE orden de precedencia: DNC → descartado → agendado → interesado → último outcome `callback_later` → cualquier otro con fecha futura → sin fecha.
     - `texto` es el mensaje completo listo para mostrar, en el idioma del aviso actual: `«<nombre>» sale de la cola — vuelve <cuando> <vista>`, adaptado por rama (un descartado no "vuelve").
     - `tono` es `'info' | 'success' | 'warn'` para elegir el tipo de toast (`warn` para el descarte automático, `success` para agendado, `info` para el resto).
     - Nombres de vista LITERALES, tomados de la tabla de `<interfaces>`: `'en Hoy → Callbacks'`, `'en Hoy → Interesados'`, `'a la cola de Llamadas'`, `'en Reuniones agendadas'`, `'en Descartados'`, `'en la vista No-llamar'`.
   - El bloque no puede referenciar nada de afuera (tiene que evaluarse aislado con `new Function`).

**B. `_dispoAnnounce(leadId, opts = {})`** — reemplaza a `_dispoWhereToast`. Ubicarla justo después del bloque puro:
   - Resuelve el lead como `opts.lead || _callsLeadsById.get(leadId)` — el `lead` del body de la respuesta es la fuente autoritativa (el cache optimista puede no tener el `callLog` fresco, que es justo lo que discrimina Hoy → Callbacks).
   - Llama `_dispoDestination(lead, new Date())`.
   - Si `_pd.active` → **no** toast (D-07): guarda `_pd.holdMeta = { texto, vista, cuando, tono }` y sale. La Task 2 lo pinta en el banner.
   - Si no → `window.showToast?.(dest.texto, { type: dest.tono, duration: 5000 })`.
   - `opts.manual === true` fuerza la rama "Hoy → Callbacks" (mismo escape hatch que hoy tiene `_dispoWhereToast`, para cuando el caller SABE que fue un callback manual y el cache todavía no tiene el callLog).
   - **Borrar `_dispoWhereToast`** por completo: no dejar la función vieja conviviendo (dos avisos con criterios distintos es exactamente lo que esta fase viene a arreglar).

**C. Cableado universal (D-05)**: cambiar la firma a `_dispoAfterSaved(leadId, opts = {})` y llamar `_dispoAnnounce(leadId, opts)` como ÚLTIMA línea de su cuerpo, después de todo lo que ya hace (limpiar gate, despinnear, refrescar la franja de pendientes y Hoy). Todo lo existente en esa función queda igual.

**D. Actualizar los 6 call sites** para que pasen el lead fresco y queden llamados DESPUÉS de la escritura optimista:
   1. `_handleCallDisposition` (app.js:10669): parsear el cuerpo antes con `const data = await resp.json().catch(() => ({}));` (así un body ilegible no impide limpiar el gate), mover la llamada a después de `_leadStoreApply(leadId, data.lead)` y pasarle `{ lead: data.lead, outcome }`. **Borrar** el `_dispoWhereToast(leadId)` de la línea 10685.
   2. `openCallbackModal` (app.js:10828): parsear el cuerpo (hoy solo mira `resp.ok`), mantener el `_leadStoreApply` optimista con `callbackAt`, y llamar `_dispoAfterSaved(leadId, { lead: data.lead, outcome: 'callback_later', manual: true })` después. **Borrar** el `_dispoWhereToast(leadId, { manual: true })` de la línea 10838.
   3. `openObjectionModal` (app.js:10979): parsear el cuerpo y llamar `_dispoAfterSaved(leadId, { lead: data.lead, outcome: 'answered_not_interested' })` después del `_leadStoreApply` de la 10983.
   4. `openScheduleModal` (app.js:11056): igual, con `outcome: 'scheduled_with_admin'`, después del `_leadStoreApply` de la 11061. En el observer de fallback (app.js:11080) pasar `{ lead: <lead del body parseado>, outcome: fallbackOnCancel }`.
   5. `_autoMarkNoAnswer` (app.js:8621): ya está después del `_leadStoreApply` — solo agregarle `{ lead: data.lead, outcome: 'no_answer' }`. **Borrar** el `_dispoWhereToast(leadId)` de la línea 8628.
   6. `openNextStepModal` (creado en 30-02): pasar `{ lead: data.lead, outcome: 'answered_interested' }`.

**E. Hold de calendario** (`openPlaceholderModal`, app.js:10740-10764): este camino NO pasa por `_dispoAfterSaved` y NO debe hacerlo — mandar un hold no es marcar el resultado de la llamada, y limpiar el gate de la Phase 20 ahí sería falsear que la llamada quedó marcada (D-08). Llamar SOLO `_dispoAnnounce(leadId, { lead: <lead de la respuesta de send-placeholder> })` después del toast de éxito existente, para que el usuario vea que el lead queda esperando respuesta 48h (el `nextAction` lo escribe el plan 30-01). Dejar un comentario con este razonamiento.
  </action>
  <verify>
    <automated>node --check public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check public/app.js` sale 0.
    - `grep -c "_dispoWhereToast" public/app.js` devuelve **0** (la función vieja y sus 3 call sites desaparecieron).
    - `grep -c "_dispoAnnounce" public/app.js` devuelve ≥ 3 (declaración + llamada desde `_dispoAfterSaved` + llamada desde `openPlaceholderModal`).
    - `grep -c "_dispoAfterSaved(leadId" public/app.js` devuelve ≥ 7 (declaración + 6 call sites; el observer de fallback puede sumar uno más).
    - Ningún call site llama `_dispoAfterSaved` sin segundo argumento: `grep -n "_dispoAfterSaved(leadId);" public/app.js` devuelve 0 líneas.
    - `grep -n "DISPO-DEST: INICIO\|DISPO-DEST: FIN" public/app.js` devuelve exactamente 2 líneas en ese orden, y el rango entre ellas no contiene `document.`, `window.`, `fetch(` ni `_pd.` (0 hits en los 4).
    - `grep -c "_dispoGateSet\|_dispoGateClear\|_dispoGateRestore" public/app.js` devuelve el mismo número que dejó el plan 30-02 (D-08 intacto).
  </acceptance_criteria>
  <done>Existe un único cálculo de destino, puro y testeable, y TODA disposición (más el hold de calendario) lo anuncia desde un único punto.</done>
</task>

<task type="auto">
  <name>Task 2: Destino dentro del banner del Power Dialer (D-07)</name>
  <files>public/app.js</files>
  <read_first>
    - `public/app.js` líneas 6860-6876 (armado de `_holdBanner` y el `main.innerHTML` que lo inyecta).
    - `public/app.js` líneas 6610-6625 y 6710-6720 (los dos lugares donde se resetean `holdCurrent`/`holdOutcome` al arrancar/avanzar).
    - `public/app.js` líneas 7211-7260 (`_pdHandleDisposition`, donde se setean) y 7330-7340 (`_pdBack`).
    - Si ya están en contexto por la Task 1, NO releer.
  </read_first>
  <action>
1. Sumar `_pd.holdMeta` como tercer campo de estado junto a `holdCurrent`/`holdOutcome`: inicializarlo en `null` donde hoy se resetean los otros dos (los dos bloques de reseteo y `_pdBack`), para que el destino de un lead nunca se muestre sobre el siguiente.
2. En `_pdRender`, dentro del bloque `_holdBanner` (app.js:6863-6873), agregar una tercera línea debajo del texto "Podés seguir agregando notas…": cuando `_pd.holdMeta` tiene contenido, mostrar el destino con el mismo tamaño/color de esa línea secundaria pero destacando la vista (ej. `<strong>` sobre `vista` y `cuando`). Si `_pd.holdMeta` es `null`, no renderizar nada extra (el banner queda igual que hoy).
3. **Escapar SIEMPRE** el texto con `escHtml` antes de interpolarlo: el banner se inyecta con `main.innerHTML` y el texto incluye el nombre del lead (dato importado de Google Maps) y puede incluir el `motivo` que tipea el usuario (T-30-05 del threat model del plan 30-02). El toast no lo necesita porque `showToast` pinta con `textContent`, pero acá sí.
4. NO agregar clases CSS nuevas: estilos inline, igual que el resto del banner. `public/style.css` no se toca.
  </action>
  <verify>
    <automated>node --check public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check public/app.js` sale 0.
    - `grep -c "holdMeta" public/app.js` devuelve ≥ 6 (asignación en `_dispoAnnounce`, uso en el banner, y los reseteos junto a `holdCurrent`).
    - Cada línea que asigna `_pd.holdOutcome = null` tiene, adyacente, una que asigna `_pd.holdMeta = null` (verificable con `grep -n -A1 "holdOutcome = null" public/app.js`).
    - La interpolación del destino en `_holdBanner` pasa por `escHtml(` (verificable leyendo el bloque: la expresión que inserta `_pd.holdMeta.texto` o sus partes está envuelta).
    - `git diff --name-only` NO incluye `public/style.css`.
  </acceptance_criteria>
  <done>En el Power Dialer el destino se lee dentro del banner verde de "Resultado guardado", sin toast que interrumpa el flujo, y se limpia al cambiar de lead.</done>
</task>

<task type="auto">
  <name>Task 3: Tests del destino + verificación de universalidad + cache-buster</name>
  <files>tests/gate-destination.test.js, public/index.html</files>
  <read_first>
    - `tests/dtpicker-core.test.js` líneas 1-35 (harness de extracción por marcadores + `new Function`).
    - `tests/gate-next-step-ui.test.js` (creado en el plan 30-02) — mismo idioma de aserciones de fuente, para no duplicar criterios.
    - `public/index.html`, la línea del `<script type="module" src="/app.js?v=...">` (~3623).
  </read_first>
  <action>
**A. Crear `tests/gate-destination.test.js`** con los dos modos del proyecto:

   1. **Bloque puro** `[30-03] DISPO-DEST` extraído con `new Function`, devolviendo `{ _dispoDestination, _dispoWhenLabel, _dispoNextAt }`. Un test por rama, con `now` FIJO (nunca `Date.now()` real, para que no dependa de la hora de la corrida — es la causa raíz de los flaky documentada en la nota #163 de CLAUDE.md):
      - descartado normal / descartado con `autoDiscarded` + `autoDiscardReason` / DNC (gana sobre descartado) / agendado / interesado con `nextAction` a +3 días / último outcome `callback_later` con fecha futura / `nextAction.origen:'cadencia'` a +24h / lead sin fecha ni estado terminal.
      - En cada una, assertear `vista` con el literal exacto y que `texto` incluye el nombre del lead.
      - `_dispoNextAt` con `nextAction:null` y `callbackAt` seteado (lead legacy sin migrar) devuelve el timestamp de `callbackAt`.
      - `_dispoWhenLabel`: "hoy", "mañana" y un día lejano, con `now` fijo a media tarde y otro con `now` a las 23:30 (borde de día).
      - Determinismo: llamar `_dispoDestination` dos veces con el mismo input devuelve objetos con el mismo `texto`.

   2. **Aserciones de fuente** sobre `public/app.js`:
      - `_dispoWhereToast` ya no existe (0 ocurrencias).
      - `_dispoAnnounce` se llama desde el cuerpo de `_dispoAfterSaved` (cortar el texto de la función y buscar la llamada adentro).
      - No queda ningún `_dispoAfterSaved(leadId);` sin opts.
      - `openPlaceholderModal` llama `_dispoAnnounce` y **no** llama `_dispoAfterSaved` (D-08: mandar un hold no marca la llamada).
      - `_pd.holdMeta` se resetea en todos los lugares donde se resetea `_pd.holdOutcome`.
      - El destino del banner del Power Dialer pasa por `escHtml`.

**B. Cache-buster** (`public/index.html`): bumpear `app.js?v=` al siguiente valor libre respecto del que dejó el plan 30-02 (si 30-02 dejó `v=20260814d`, este pasa a `v=20260814e`; si la fecha de ejecución es otra, usar `v=<YYYYMMDD de hoy><letra siguiente>`). **No** tocar el de `style.css`.

**C. Suite completa** y comparación contra el baseline documentado en los SUMMARY de 30-01 y 30-02.
  </action>
  <verify>
    <automated>npx vitest run tests/gate-destination.test.js tests/gate-next-step-ui.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/gate-destination.test.js` existe con ≥ 14 tests, todos verdes.
    - `grep -c "Date.now()" tests/gate-destination.test.js` devuelve 0 en las aserciones del bloque puro (todos los casos usan un `now` fijo).
    - `grep -n "app.js?v=" public/index.html` muestra un valor distinto del que dejó el plan 30-02.
    - `npm test` termina con 0 fallos y el total documentado en el SUMMARY cuadra con: baseline 1293 + tests de 30-01 + tests de 30-02 + tests de este plan.
    - `npx vitest run tests/metrics-consistency.test.js` verde sin editar el archivo (D-09).
    - `git diff --name-only` NO incluye `public/style.css`.
  </acceptance_criteria>
  <done>Las 8 ramas de destino están cubiertas con reloj fijo, la universalidad del aviso está verificada por fuente, y el tab del usuario baja el `app.js` nuevo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos del lead (nombre importado de Google Maps, `motivo` tipeado por el usuario) → banner del Power Dialer | el banner se inyecta con `innerHTML` |
| respuesta del servidor → `_dispoAnnounce` | el `lead` del body decide qué vista se nombra |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-08 | Tampering | nombre del lead / `motivo` interpolados en `_holdBanner` (innerHTML) | mitigate | `escHtml()` obligatorio sobre todo lo que venga del destino antes de interpolar; verificado por aserción de fuente en la Task 3 |
| T-30-09 | Information Disclosure | el aviso nombra vistas que el rol podría no tener (ej. "Reuniones agendadas" es admin/supervisor) | accept | el destino es del lead que el propio usuario acaba de trabajar y no revela datos de terceros; el milestone v4.0 opera con un solo usuario (admin) |
| T-30-10 | Repudiation | el aviso podría mentir sobre dónde quedó el lead | mitigate | `_dispoDestination` replica los filtros REALES de `loadHoyView`/`renderCallsList` (tabla de `<interfaces>`), y cada rama tiene un test con `now` fijo |
| T-30-SC | Tampering | npm/pip/cargo installs | n/a | esta fase no instala paquetes |
</threat_model>

<verification>
1. `node --check public/app.js` → OK.
2. `npm test` → 0 fallos.
3. Verificación en preview (login por fetch + inspección del DOM con el tool de JavaScript; el screenshot puede colgarse, notas #124/#125):
   - Marcar "No atendió" en un lead de Llamadas → sale un toast que dice que vuelve mañana **a la cola de Llamadas**.
   - Marcar "No interesado" → toast que dice que quedó en **Descartados** y cómo encontrarlo.
   - Marcar "Volver a llamar" con fecha de pasado mañana → toast que nombra **Hoy → Callbacks** con esa fecha.
   - Marcar "Interesado" → toast que nombra **Hoy → Interesados** con la fecha del próximo paso.
   - Abrir el Power Dialer, marcar un resultado → NO sale toast; el banner verde "✓ Resultado guardado" muestra la línea de destino. Avanzar al siguiente lead → el banner (y el destino) desaparecen.
</verification>

<success_criteria>
- Ninguna disposición termina en silencio: las 8 ramas de destino tienen mensaje.
- El mensaje nombra la vista real, no una genérica, y coincide con dónde el lead aparece de verdad.
- El Power Dialer integra el destino en su banner y no interrumpe con toasts.
- El hold de calendario también avisa, sin tocar el gate de la Phase 20.
- Suite completa verde; `metrics-consistency` sin mover un número; `style.css` intacto.
</success_criteria>

<output>
Create `.planning/phases/30-gate-proximo-paso/30-03-SUMMARY.md` when done.

Anotar en el SUMMARY: el texto exacto de las 8 ramas de destino (para que el
próximo que las toque no las reinvente), el total final de la suite, y una nota
para `CLAUDE.md` explicando que `_dispoWhereToast` ya no existe y que el único
cálculo de destino es `_dispoDestination` dentro del bloque `[30-03] DISPO-DEST`.
</output>
