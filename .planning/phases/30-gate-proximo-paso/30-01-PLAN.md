---
phase: 30-gate-proximo-paso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.js
  - tests/gate-next-action.test.js
  - tests/next-action-disposition.test.js
autonomous: true
requirements: [GATE-01, GATE-02]

must_haves:
  truths:
    - "Ninguna disposición no-terminal deja el lead sin próximo paso: si el outcome no descarta ni agenda, el lead siempre sale con nextAction"
    - "Marcar 'Interesado' programa solo un seguimiento a +3 días (origen manual), sin que el usuario tenga que hacer nada más"
    - "Marcar 'Me cortó' (1er corte) programa el reintento a +24h en vez de dejar el lead flotando"
    - "Mandar un hold de calendario deja el lead esperando respuesta +48h"
    - "El servidor nunca devuelve 400 por falta de próximo paso — asigna el default"
    - "Un próximo paso mandado desde el cliente se respeta cuando el lead queda vivo, y se ignora cuando el outcome es terminal"
  artifacts:
    - path: "index.js"
      provides: "defaults D-02 en _applyCallOutcome + red de seguridad GATE-01 + override sanitizado en call-disposition + esperar_respuesta en send-placeholder"
      contains: "GATE_INTERESADO_DELTA_MS"
    - path: "tests/gate-next-action.test.js"
      provides: "cobertura del mapa D-02, la red de seguridad y el override"
  key_links:
    - from: "index.js _applyCallOutcome"
      to: "_setNextAction / _clearNextAction"
      via: "defaults por outcome + red de seguridad final"
      pattern: "_setNextAction\\(lead"
    - from: "POST /api/setters/leads/:id/call-disposition"
      to: "_applyCallOutcome"
      via: "opts.nextActionOverride"
      pattern: "nextActionOverride"
    - from: "POST /api/setters/leads/:id/send-placeholder"
      to: "_setNextAction"
      via: "dentro del callback de mutateSettersData"
      pattern: "esperar_respuesta"
---

<objective>
Que el backend garantice, por construcción, que ninguna llamada deje un lead sin
próximo paso: cada outcome no-terminal escribe su `nextAction` por defecto según
el mapa D-02, el cliente puede mandar una fecha propia (que se sanitiza y solo
gana si el lead quedó vivo), y una red de seguridad final cubre cualquier camino
—incluido el webhook del agente de voz— que llegue sin nada definido.

Purpose: es la mitad no-negociable de GATE-01. El frontend (plan 30-02) puede
fallar, quedar viejo en un tab sin recargar, o no existir para el agente de voz;
el backend es el único lugar donde "ningún lead flota" se puede garantizar.
Output: `index.js` con el mapa D-02 implementado + `tests/gate-next-action.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-gate-proximo-paso/30-CONTEXT.md
@.planning/phases/30-gate-proximo-paso/30-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- API REAL del reloj único (Phase 29), verificada en index.js. Usar tal cual. -->

index.js:10756-10790 — whitelists y plantillas (ÚNICA fuente de duraciones):
```javascript
const NEXT_ACTION_TIPOS   = new Set(['callback','cadencia','enviar_info','esperar_respuesta','otro']);
const NEXT_ACTION_CANALES = new Set(['llamada','whatsapp','email','']);   // '' es válido
const NEXT_ACTION_ORIGENES = new Set(['manual','cadencia','compromiso']); // 'compromiso' RESERVADO Phase 31
const NEXT_ACTION_TEMPLATES = [
  { key:'24hs', label:'24h', deltaMs: 24*60*60*1000 },
  { key:'48hs', label:'48h', deltaMs: 48*60*60*1000 },
  { key:'72hs', label:'72h', deltaMs: 72*60*60*1000 },
  { key:'7d',   label:'7d',  deltaMs: 7*24*60*60*1000 },
  { key:'15d',  label:'15d', deltaMs: 15*24*60*60*1000 },
];
function _nextActionTemplateForDelta(deltaMs) // → template | null (match EXACTO)
```

index.js:10797-10822 — escritura del reloj. NUNCA lanza (coerciona):
```javascript
function _setNextAction(lead, spec, nowIso)  // spec = {tipo,dueAt,canal,motivo,origen,createdBy}
                                             // escribe lead.nextAction Y lead.callbackAt = spec.dueAt
function _clearNextAction(lead)              // lead.nextAction = null; lead.callbackAt = ''
```

index.js:10888 — el helper compartido (handler humano + webhook Retell):
```javascript
function _applyCallOutcome(data, lead, logEntry, opts) // → { calendarEntry }
// opts = { leadId, nowIso, outcome, callbackAt, callbackShared, scheduled,
//          cleanReason, doNotCall, actorSetterId, actorName, skipCalendarCreation }
// Línea 10909: _clearNextAction(lead) al entrar (consume el compromiso pendiente, NEXT-04).
// Línea 10913-10991: switch por outcome.
// Línea 10995-11001: bloque DNC.
// Línea 11028-11037: tope de cortes (MAX_HUNG_UP = 2).
// Línea 11039-11074: cadencia de no-contacto (MAX_NO_CONTACT = 2).
// Línea 11076: return { calendarEntry }.
```

index.js:11081 — superficie expuesta para tests puros:
```javascript
globalThis.__voiceAgent = { _applyCallOutcome, _setNextAction, _clearNextAction, ... };
```
</interfaces>

<mapa_d02>
<!-- D-02 del CONTEXT — la tabla que este plan implementa. -->

| Outcome | nextAction resultante | Terminal |
|---|---|---|
| `answered_interested` | `callback` +3 días, `origen:'manual'`, canal `llamada` | no |
| `callback_later` | la fecha del body (ya implementado, NO tocar) | no |
| `no_answer` / `voicemail` | cadencia +24h (ya implementado, NO tocar) | no |
| `hung_up` | cadencia +24h **salvo** que el tope de 2 cortes lo descarte | no |
| `placeholder_sent` | `esperar_respuesta` +48h, canal `email`, `origen:'manual'` | no |
| `answered_not_interested` | ninguno (`_clearNextAction` de entrada) | **sí** |
| `scheduled_with_admin` | ninguno | **sí** |
| `wrong_number` / `invalid_number` | ninguno | **sí** |
</mapa_d02>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Defaults D-02 en _applyCallOutcome + red de seguridad GATE-01</name>
  <files>index.js</files>
  <read_first>
    - `index.js` líneas 10740-11120 (el bloque completo del modelo `nextAction`, `_applyCallOutcome` entero, `CALL_OUTCOMES`, `DISQUALIFY_REASONS`) — leer de una sola pasada, es el archivo que se modifica y todo el contexto necesario está en ese rango.
    - `.planning/phases/30-gate-proximo-paso/30-CONTEXT.md` (D-01, D-02, D-03, D-04).
    - `tests/apply-call-outcome.test.js` (COMPLETO, ~330 líneas) — es el test de paridad handler-humano vs helper-directo y define una restricción dura de esta task (ver `<behavior>`).
  </read_first>
  <behavior>
    - `answered_interested` → `lead.nextAction = {tipo:'callback', origen:'manual', canal:'llamada', dueAt: nowIso + 3 días}` y `lead.callbackAt` espejado. `estado` sigue siendo `'interesado'` (la cascada actual no cambia).
    - `hung_up` con 1 solo corte acumulado → `nextAction` de `tipo:'cadencia'`, `origen:'cadencia'`, `dueAt` a +24h. Con 2 cortes → sigue descartando y `nextAction` queda `null` (comportamiento actual intacto).
    - `answered_not_interested`, `wrong_number`, `invalid_number`, `scheduled_with_admin` → `nextAction` sigue siendo `null` (terminal, sin próximo paso).
    - Un lead que sale `descartado` por DNC (`doNotCall:true` o razón `no_contactar`) → `nextAction` null aunque el outcome de por sí no fuera terminal.
    - Un `no_answer` que descarta por 2do no-contacto → `nextAction` null (intacto).
    - Un `no_answer` de un lead `interesado` → sigue conservando la cadencia +24h y NO se descarta (#183, intacto).
    - **Determinismo (restricción dura)**: las fechas nuevas se calculan desde `opts.nowIso`, NO desde `Date.now()`. `tests/apply-call-outcome.test.js` compara `callbackAt` y `nextAction.dueAt` con igualdad ESTRICTA entre la vía HTTP y la vía helper para `answered_interested` (`assertParity` sin `looseCallbackAt`); con `Date.now()` las dos vías difieren en milisegundos y ese test se pone rojo.
    - Red de seguridad: cualquier lead que termine `_applyCallOutcome` en estado NO terminal y sin `nextAction` recibe una cadencia +24h con `motivo` que identifique que fue la red (ej. `'sin próximo paso definido'`). Nunca se lanza ni se devuelve error.
  </behavior>
  <action>
Modificar `_applyCallOutcome` en `index.js` (empieza en 10888) y agregar las constantes de la fase junto a `NEXT_ACTION_TEMPLATES` (~10784).

**1. Constantes nuevas** (declararlas después de `NEXT_ACTION_TEMPLATES` y antes de `_nextActionTemplateForDelta`, para que estén disponibles en `_applyCallOutcome`; recordar que `const` no hace hoisting):
   - `GATE_INTERESADO_DELTA_MS = 3 * 24 * 60 * 60 * 1000` — el +3 días de D-03 (la cadencia de research para un interesado que no avanza es día 1/3/7/14). Comentar en el código que NO coincide con ninguna plantilla de `NEXT_ACTION_TEMPLATES` a propósito, así que `_nextActionTemplateForDelta` devuelve `null` y `_computeFollowupsDue` lo reporta con `label:'próximo paso'`.
   - `GATE_CADENCIA_DELTA_MS` — **derivado de la plantilla `'24hs'`**: buscar la entry con `key === '24hs'` en `NEXT_ACTION_TEMPLATES` y usar su `deltaMs`. NO escribir `24 * 3600000` a mano (regla del PATTERNS: los defaults que coinciden con una duración de la tabla usan la plantilla).
   - `GATE_TERMINAL_ESTADOS = new Set(['descartado', 'agendado', 'cerrado'])` — un lead en cualquiera de estos estados NO lleva próximo paso.

**2. `case 'answered_interested'`** (línea 10914-10920): después de las 4 asignaciones de cascada existentes (`respondio`, `calificado`, `interes`, `estado`) agregar la llamada a `_setNextAction` con `tipo:'callback'`, `dueAt` = ISO de `Date.parse(opts.nowIso) + GATE_INTERESADO_DELTA_MS` (con fallback a `Date.now()` si `opts.nowIso` no parsea), `canal:'llamada'`, `motivo` corto y estable en español que explique el default (ej. `'seguimiento de interesado'`), `origen:'manual'`, `createdBy: opts.actorName || ''`. Mantener el comentario existente sobre que sigue en Llamadas con chip verde y agregar la referencia a GATE-02/D-03.

**3. Rama `hung_up`** (bloque de `MAX_HUNG_UP`, líneas 11028-11037): hoy solo actúa cuando descarta. Agregar el `else` — cuando `cortes < MAX_HUNG_UP` y el lead no es DNC, programar `_setNextAction` con `tipo:'cadencia'`, `origen:'cadencia'`, `canal:'llamada'`, `motivo:'reintento tras corte'`, `dueAt` = ISO de `nowIso + GATE_CADENCIA_DELTA_MS`. Respetar el guard existente `!callbackAt` (si el caller mandó una fecha manual explícita, no pisarla). Comentar que antes del GATE un 1er corte dejaba el lead sin próximo paso (flotando) y volvía a la cola al instante.

**4. Red de seguridad GATE-01** (D-01, backend permisivo): al final de `_applyCallOutcome`, justo ANTES del `return { calendarEntry };` de la línea 11076, agregar el bloque: si `!GATE_TERMINAL_ESTADOS.has(lead.estado)` y `!lead.nextAction` → `_setNextAction` con `tipo:'cadencia'`, `origen:'cadencia'`, `canal:'llamada'`, `motivo:'sin próximo paso definido'`, `dueAt` = `nowIso + GATE_CADENCIA_DELTA_MS`. Comentar POR QUÉ es una red y no un 400: `_applyCallOutcome` lo comparte el webhook del agente de voz (v3.0 parkeado) y un rechazo lo rompería al reactivarse; además garantizar un default es más fuerte para el objetivo real (que ningún lead flote) que rechazar.

**5. Exponer para tests puros**: agregar `GATE_INTERESADO_DELTA_MS`, `GATE_CADENCIA_DELTA_MS` y `GATE_TERMINAL_ESTADOS` al objeto `globalThis.__voiceAgent` (index.js:11081), junto a las claves de Phase 29 que ya están.

**NO TOCAR**: el bloque de cadencia de no-contacto (11039-11074) ni su uso de `Date.now()` (código verbatim de la Phase 17, con tolerancia de reloj ya asumida en los tests); el `_clearNextAction(lead)` de entrada (línea 10909, es NEXT-04); el `case 'callback_later'`; la creación del `calendarEntry`.
  </action>
  <verify>
    <automated>node --check index.js && npx vitest run tests/apply-call-outcome.test.js tests/call-cadence.test.js tests/hangup-cap.test.js tests/next-action-model.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check index.js` sale 0.
    - `grep -c "GATE_INTERESADO_DELTA_MS" index.js` devuelve ≥ 3 (declaración + uso en el case + export en `__voiceAgent`).
    - `grep -n "GATE_CADENCIA_DELTA_MS" index.js` muestra que se deriva de `NEXT_ACTION_TEMPLATES` (la línea de declaración contiene `NEXT_ACTION_TEMPLATES` y `'24hs'`) y NO contiene el literal `24 * 3600000` ni `86400000` en esa línea.
    - `tests/apply-call-outcome.test.js` pasa completo, incluido `"answered_interested — misma cascada por las dos vías"` que compara `nextAction.dueAt` con igualdad estricta entre las 2 vías (prueba directa del determinismo por `opts.nowIso`).
    - `tests/call-cadence.test.js` y `tests/hangup-cap.test.js` siguen verdes sin editarlos (ninguno assertea `callbackAt` tras un 1er `hung_up`; verificado al planificar).
    - En `index.js`, el bloque de la red de seguridad está ANTES del `return { calendarEntry };` de `_applyCallOutcome` y después del bloque de cadencia de no-contacto (verificable por número de línea creciente con `grep -n`).
  </acceptance_criteria>
  <done>Todo outcome no terminal deja `lead.nextAction` escrito; los terminales lo dejan en `null`; las fechas nuevas son deterministas respecto de `opts.nowIso`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Override de próximo paso en call-disposition + esperar_respuesta en send-placeholder</name>
  <files>index.js</files>
  <read_first>
    - `index.js` líneas 11118-11311 (endpoint `call-disposition` completo: destructuring del body, whitelist-and-coerce de `disqualifyReason`, corrección de auto-marca, delegación a `_applyCallOutcome`, `saveSettersData`).
    - `index.js` líneas 11492-11574 (endpoint `send-placeholder` completo, incluido el `mutateSettersData` con el `await` de red por delante).
    - `index.js` líneas 10756-10822 (whitelists + `_setNextAction`) — si ya quedaron en contexto de la Task 1, NO releer.
  </read_first>
  <behavior>
    - `POST /call-disposition` con `{outcome:'answered_interested', nextAction:{dueAt:'<ISO +10 días>'}}` → el lead queda con `nextAction.dueAt` en esa fecha (no el default de +3 días) y `origen:'manual'`.
    - El mismo body con `nextAction:{dueAt:'no-es-fecha'}` → se ignora el override y queda el default de +3 días. Status 200, nunca 400.
    - `{outcome:'answered_not_interested', nextAction:{dueAt:'<ISO futuro>'}}` → lead `descartado` y `nextAction` **null**: el estado terminal gana sobre el override.
    - `{outcome:'answered_interested', nextAction:{dueAt:'<ISO>', origen:'compromiso'}}` → se persiste `origen:'manual'`: el cliente no puede escribir `'compromiso'` (reservado Phase 31).
    - `{outcome:'answered_interested', nextAction:{dueAt:'<ISO>', tipo:'inventado', canal:'paloma'}}` → `tipo` y `canal` caen a los defaults válidos (`'callback'` / `'llamada'`), sin error.
    - `POST /send-placeholder` exitoso → el lead queda con `nextAction` `{tipo:'esperar_respuesta', canal:'email', origen:'manual'}` a +48h y `callbackAt` espejado, además del entry `placeholder_sent` en el callLog que ya existía.
  </behavior>
  <action>
**A. `POST /api/setters/leads/:id/call-disposition`** (index.js:11118).

1. Agregar `nextAction` al destructuring del body de la línea 11136 (mismo nivel que `outcome`, `notes`, `callbackAt`, …). Nombre del campo en el body: **`nextAction`**.
2. Escribir un helper de módulo `_gateSanitizeNextActionOverride(raw)` — declararlo junto a las constantes GATE de la Task 1 — que devuelve `null` o un spec válido:
   - `raw` debe ser objeto; si no, `null`.
   - `dueAt`: aceptar string; normalizar con `new Date(raw.dueAt)`; si `isNaN(getTime())` → devolver `null` (override descartado). Persistir el ISO normalizado (`toISOString()`).
   - `tipo`: si está en `NEXT_ACTION_TIPOS` y NO es `'cadencia'` → se usa; si no → `'callback'`. (Motivo: `'cadencia'` es del sistema, un override del cliente siempre es intención humana.)
   - `canal`: si está en `NEXT_ACTION_CANALES` → se usa; si no → `'llamada'`.
   - `motivo`: string recortado a 200 chars, o `''`.
   - `origen`: **siempre** `'manual'`, ignorando lo que venga del cliente. Comentar que `'compromiso'` está reservado para la Phase 31 y que aceptarlo desde el body permitiría fabricar compromisos que esa fase todavía no valida.
   Es el mismo idioma whitelist-and-coerce de `cleanReason` (línea 11141).
3. Pasar el resultado a `_applyCallOutcome` como `nextActionOverride` dentro del objeto `opts` de la línea 11268-11279.
4. En `_applyCallOutcome`, aplicar el override en un único punto: **después** del bloque de cadencia de no-contacto y **antes** de la red de seguridad de la Task 1. Condición: hay override válido **y** `!GATE_TERMINAL_ESTADOS.has(lead.estado)`. Efecto: `_setNextAction(lead, {...override, origen:'manual', createdBy: opts.actorName || ''}, opts.nowIso)`. Así el terminal siempre gana y la red de seguridad ya no dispara (el lead tiene `nextAction`).
5. NO agregar ninguna rama que devuelva 400/409 por el override (D-01).

**B. `POST /api/setters/leads/:id/send-placeholder`** (index.js:11496).

6. Dentro del callback de `mutateSettersData` (líneas 11556-11572), después de `l.lastContactAt = nowIso;` y antes del `return l;`, llamar a `_setNextAction(l, {...}, nowIso)` con `tipo:'esperar_respuesta'`, `canal:'email'`, `origen:'manual'`, `motivo` que mencione el hold (ej. `'esperando respuesta del hold'`), y `dueAt` = ISO de `Date.parse(nowIso) + <deltaMs de la plantilla '48hs'>` — igual que en la Task 1, **derivar el delta de `NEXT_ACTION_TEMPLATES`** buscando `key === '48hs'`, no escribir 48h a mano. Declarar esa constante junto a las otras GATE (`GATE_PLACEHOLDER_DELTA_MS`).
   La escritura va DENTRO del mutex porque hay un `await` de red (`_sendPlaceholderEmail`) entre el load y el save (regla #19 de CLAUDE.md).
7. Comentar el efecto visible: el lead sale de la cola de Llamadas por 48h (el filtro de `callbackAt` futuro del frontend) y vuelve solo cuando vence; antes del GATE quedaba en la cola como si no hubiera pasado nada.
  </action>
  <verify>
    <automated>node --check index.js && npx vitest run tests/next-action-disposition.test.js tests/disposition-enforcement.test.js tests/disposition-dnc.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check index.js` sale 0.
    - `grep -n "nextActionOverride" index.js` devuelve al menos 2 hits: el `opts` del endpoint y su uso dentro de `_applyCallOutcome`.
    - `grep -n "_gateSanitizeNextActionOverride" index.js` devuelve exactamente 2 hits (declaración + uso en el endpoint).
    - La función `_gateSanitizeNextActionOverride` NO contiene la string `'compromiso'` como valor asignable a `origen` (el `origen` se asigna literal `'manual'`).
    - `grep -n "esperar_respuesta" index.js` incluye una línea dentro del rango del callback de `mutateSettersData` de `send-placeholder` (comparar números de línea con `grep -n "mutateSettersData" index.js`).
    - `grep -n "GATE_PLACEHOLDER_DELTA_MS" index.js` muestra la declaración derivando de `NEXT_ACTION_TEMPLATES` con `'48hs'`.
    - Las 3 suites del `<verify>` pasan (`disposition-enforcement` prueba que la corrección de auto-marca y el gate de Phase 20 siguen intactos — D-08).
  </acceptance_criteria>
  <done>El cliente puede proponer la fecha del próximo paso, el servidor la sanitiza, el estado terminal siempre gana, y el hold de calendario deja el lead esperando respuesta 48h.</done>
</task>

<task type="auto">
  <name>Task 3: Tests del gate backend + actualizar las 2 aserciones que la Phase 29 dejó marcadas</name>
  <files>tests/gate-next-action.test.js, tests/next-action-disposition.test.js</files>
  <read_first>
    - `tests/next-action-disposition.test.js` (COMPLETO, 189 líneas) — es el modelo de fixture/setup a copiar Y el archivo que hay que editar (dos aserciones quedaron escritas con el comentario "hoy no exige uno; la Phase 30 sí").
    - `tests/hangup-cap.test.js` (COMPLETO) — idioma de aserción del tope de cortes.
    - `index.js` líneas 10888-11120 y 11118-11311 (lo que se acaba de escribir en las Tasks 1 y 2). Si ya está en contexto, NO releer.
  </read_first>
  <action>
**A. Crear `tests/gate-next-action.test.js`** copiando el setup exacto de `tests/next-action-disposition.test.js` (líneas 10-83): `tmpData` en `os.tmpdir()` con timestamp, `process.env.NODE_ENV='test'`, `DATA_DIR`, `ADMIN_EMAIL`/`ADMIN_PASSWORD`/`JWT_SECRET`, las 3 env de IA en `""` (**nunca `delete`** — regla #121 de CLAUDE.md, es la causa raíz documentada de los flaky históricos), `auth.json` sembrado con `pwd()` (scrypt) ANTES de `await import('../index.js')`, y teléfonos `+521555…` de ≥7 dígitos (regla #163: con menos dígitos el lead no entra en ninguna cola).

Cubrir, un lead distinto por caso (el fixture es un map, no un array):
   1. `answered_interested` → `nextAction.tipo==='callback'`, `origen==='manual'`, `dueAt` a ~72h (rango 71-73h con el helper `hoursFromNow`), `callbackAt === nextAction.dueAt`, `estado==='interesado'`.
   2. `hung_up` 1er corte → `nextAction.tipo==='cadencia'`, `origen==='cadencia'`, `dueAt` a ~24h, `estado !== 'descartado'`.
   3. `hung_up` 2do corte → `estado==='descartado'`, `nextAction === null`, `callbackAt` falsy (el tope gana sobre el default de D-02).
   4. Terminales: `answered_not_interested`, `wrong_number`, `invalid_number` y `scheduled_with_admin` → `nextAction === null` en los 4.
   5. DNC: `answered_not_interested` con `disqualifyReason:'no_contactar'` → `doNotCall===true`, `estado==='descartado'`, `nextAction===null`.
   6. Override respetado: `answered_interested` + `nextAction:{dueAt: +10 días}` → `dueAt` a ~240h y `origen==='manual'`.
   7. Override inválido: `nextAction:{dueAt:'no-es-fecha'}` → status **200** y default de +3 días (prueba de D-01: nunca 400).
   8. Override ignorado en terminal: `answered_not_interested` + `nextAction:{dueAt:+5 días}` → `nextAction===null`.
   9. Override no puede escribir `origen:'compromiso'` → el persistido es `'manual'`.
   10. Override con `tipo`/`canal` basura → `tipo==='callback'`, `canal==='llamada'`.
   11. Red de seguridad: invocar `_applyCallOutcome` DIRECTO desde `globalThis.__voiceAgent` (patrón de `tests/apply-call-outcome.test.js`, que ya lo hace) sobre un lead en memoria con un outcome que no toca nada — usar `no_answer` con `callbackAt` presente en `opts` (combinación que hoy saltea la cadencia por el guard `!callbackAt` y dejaría el lead sin próximo paso) — y assertear que igual sale con `nextAction` de cadencia. Comentar en el test que este es el camino que protege al webhook del agente de voz.
   12. `send-placeholder`: no se puede ejercitar el envío real de mail (depende de Resend). En vez de eso, assertear el contrato a nivel fuente: leer `index.js` y verificar que dentro del bloque de `mutateSettersData` de ese endpoint aparece `esperar_respuesta`. Documentar en un comentario POR QUÉ este caso es source-assertion y no request (mismo criterio que usan `tests/app-version.test.js` / `tests/dtpicker-wiring.test.js` para lo que no se puede ejecutar en el entorno).

**B. Editar `tests/next-action-disposition.test.js`** — 2 aserciones que la Phase 29 dejó explícitamente marcadas para esta fase:
   - Línea ~152, test `'consumo con vencido: callback manual VENCIDO + hung_up (1er corte) → nextAction null'`: con D-02 el 1er corte ahora programa cadencia +24h. Reescribir el título y las aserciones para que sigan probando lo que importaba (que el callback VIEJO se consumió) con el comportamiento nuevo: `estado !== 'descartado'`, `nextAction.origen === 'cadencia'`, `hoursFromNow(nextAction.dueAt)` entre 23 y 25 (o sea: NO es la fecha vencida arrastrada), y `new Date(nextAction.dueAt).getTime() > Date.now()`. Dejar el comentario del caso real del 2026-08-12 y agregar que la Phase 30 cambió el default de esta rama.
   - Línea ~168, test `'answered_interested → nextAction null (hoy no exige uno; la Phase 30 sí)'`: reescribir a que ahora SÍ exige — `nextAction.tipo==='callback'`, `origen==='manual'`, `dueAt` a ~72h, `estado==='interesado'`.
   - Línea ~174 (`scheduled_with_admin → nextAction null`): **NO se toca** — sigue siendo terminal. Solo actualizar el título sacando el paréntesis "(hoy no exige uno; la Phase 30 sí)" para que no mienta.

**C. Correr la suite completa** y comparar contra el baseline de 1293 tests que dejó la Phase 29.
  </action>
  <verify>
    <automated>npx vitest run tests/gate-next-action.test.js tests/next-action-disposition.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/gate-next-action.test.js` existe y aporta ≥ 12 tests, todos verdes.
    - `grep -c "process.env.OPENAI_API_KEY = \"\"\\|process.env.OPENAI_API_KEY = ''" tests/gate-next-action.test.js` devuelve 1 y `grep -c "delete process.env" tests/gate-next-action.test.js` devuelve 0.
    - `grep -n "la Phase 30 sí" tests/next-action-disposition.test.js` no devuelve ninguna línea (los 3 títulos que lo decían quedaron actualizados).
    - `npm test` termina con 0 fallos. El total es ≥ 1293 + los tests nuevos de este plan (baseline Phase 29 = 1293; si algún archivo suma o resta tests, dejar el número exacto documentado en el SUMMARY).
    - `npx vitest run tests/metrics-consistency.test.js` verde sin editar el archivo (D-09: cero cambios de métricas).
  </acceptance_criteria>
  <done>El mapa D-02, la red de seguridad y el override tienen cobertura propia; ninguna aserción vieja quedó mintiendo sobre el comportamiento nuevo; la suite completa está verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador del SDR → `POST /call-disposition` | el body del override (`nextAction`) es input no confiable de un usuario autenticado |
| webhook Retell → `_applyCallOutcome` | camino compartido, sin sesión de usuario (agente de voz, v3.0 parkeado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | `nextAction` del body de `call-disposition` | mitigate | `_gateSanitizeNextActionOverride`: `dueAt` re-parseado y normalizado a ISO, `tipo`/`canal` contra `NEXT_ACTION_TIPOS`/`NEXT_ACTION_CANALES`, `motivo` recortado a 200 chars, `origen` forzado a `'manual'` (nunca se acepta `'compromiso'`, reservado Phase 31) |
| T-30-02 | Tampering | override sobre un lead que quedó terminal | mitigate | el override solo se aplica si `!GATE_TERMINAL_ESTADOS.has(lead.estado)` — un cliente no puede resucitar un descartado/agendado mandando una fecha |
| T-30-03 | Elevation of Privilege | disposición sobre lead ajeno | accept | el control de ownership del endpoint (index.js:11125-11134, setter dueño o callback compartido vencido) NO se toca en esta fase; el override viaja en el mismo request ya autorizado |
| T-30-04 | Denial of Service | `_applyCallOutcome` lanzando en el camino del webhook de voz | mitigate | `_setNextAction` no lanza (coerciona) y la red de seguridad no valida nada del caller; ninguna rama nueva devuelve 4xx (D-01) |
| T-30-SC | Tampering | npm/pip/cargo installs | n/a | esta fase no instala ningún paquete (0 dependencias nuevas); no aplica el gate de legitimidad |
</threat_model>

<verification>
1. `node --check index.js` → OK.
2. `npm test` → 0 fallos, total ≥ 1293 + tests nuevos.
3. `npx vitest run tests/metrics-consistency.test.js` → verde sin editar (D-09).
4. Verificación manual del contrato de precedencia leyendo `_applyCallOutcome` de arriba a abajo: `_clearNextAction` de entrada → switch (defaults por outcome) → DNC → tope de cortes → cadencia de no-contacto → override del body → red de seguridad → `return`.
</verification>

<success_criteria>
- Todo outcome no terminal deja el lead con `nextAction` escrito y `callbackAt` espejado.
- `answered_interested` = callback +3 días manual; `hung_up` 1er corte = cadencia +24h; `placeholder_sent` = esperar_respuesta +48h.
- Los 4 outcomes terminales siguen dejando `nextAction` en `null`, incluido cuando llega un override.
- Ninguna rama nueva devuelve 400/409.
- Suite completa verde, `metrics-consistency` sin mover un número.
</success_criteria>

<output>
Create `.planning/phases/30-gate-proximo-paso/30-01-SUMMARY.md` when done.

Documentar en el SUMMARY (además del formato estándar) estos **efectos visibles esperados**, medidos o al menos razonados contra los datos:
- El contador de seguimientos del menú y la lista de follow-ups van a **subir**: `_computeFollowupsDue` (index.js:11763) incluye cualquier `nextAction` con `origen !== 'cadencia'`, y ahora cada interesado nuevo tiene uno. Es el efecto buscado (R1: 36 interesados con mediana de 21 días sin avanzar), no una regresión.
- Los leads con un `hung_up` reciente y los que recibieron un hold de calendario salen de la cola de Llamadas por 24h/48h (antes volvían al instante). El stock "para llamar" de Comando/Equipo/Distribución puede bajar levemente por eso — es consecuencia directa de D-02. Los interesados NO afectan ese stock: `_leadIsCallableNow` (index.js:9414) ya los excluía por estado.
- `metrics-consistency` no se mueve porque el funnel se deriva del `callLog`, no de `nextAction`.
</output>

