---
phase: 30-gate-proximo-paso
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - public/index.html
  - public/app.js
  - tests/gate-next-step-ui.test.js
  - tests/dtpicker-wiring.test.js
autonomous: true
requirements: [GATE-01, GATE-02]

must_haves:
  truths:
    - "Marcar 'Interesado' abre un paso de próximo-paso con la fecha ya propuesta (+3 días): un click en Guardar alcanza"
    - "Ese paso no se puede confirmar con la fecha vacía — avisa que un interesado no puede quedar sin fecha"
    - "Los atajos rápidos (mañana / 3 días / 7 días / 15 días) y el calendario de la Phase 28 conviven en ese paso"
    - "El botón del paso nunca queda muerto en 'Guardando…' entre aperturas"
    - "El Power Dialer sigue mostrando el interesado tras marcarlo (no lo expulsa por tener fecha futura)"
    - "El resto de los resultados (no atendió, buzón, me cortó, equivocado, no existe) se marcan con un solo click, igual que hoy"
  artifacts:
    - path: "public/index.html"
      provides: "markup del modal de próximo paso"
      contains: "call-next-modal"
    - path: "public/app.js"
      provides: "openNextStepModal + bloque puro GATE-PURE + ruteo de answered_interested + fix de expulsión del Power Dialer"
      contains: "openNextStepModal"
    - path: "tests/gate-next-step-ui.test.js"
      provides: "cobertura del bloque puro + aserciones de fuente del cableado"
  key_links:
    - from: "public/app.js _handleCallDisposition"
      to: "openNextStepModal"
      via: "rama del outcome answered_interested"
      pattern: "answered_interested"
    - from: "openNextStepModal"
      to: "POST /api/setters/leads/:id/call-disposition"
      via: "body con outcome answered_interested + nextAction {dueAt, motivo}"
      pattern: "nextAction:"
    - from: "public/app.js _pdHandleDisposition"
      to: "call-next-modal"
      via: "modalOpening + modalIds"
      pattern: "call-next-modal"
---

<objective>
Que marcar "Interesado" deje de ser un callejón sin salida: el resultado abre un
paso con la fecha del próximo seguimiento ya propuesta (+3 días, D-03), atajos y
calendario para editarla, y no deja confirmar sin fecha. Y que el Power Dialer no
expulse al interesado apenas queda con esa fecha futura.

Purpose: GATE-01 capa frontend + GATE-02 ("propuesta ya cargada que el usuario
acepta con un click o edita antes de guardar"). Es el único outcome que hoy deja
al lead vivo sin ningún paso siguiente — los demás o son terminales o los cubre
la cadencia.
Output: `#call-next-modal` en `index.html`, `openNextStepModal` + bloque puro en
`app.js`, cableado en los dos caminos de disposición, cache-buster bumpeado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-gate-proximo-paso/30-CONTEXT.md
@.planning/phases/30-gate-proximo-paso/30-PATTERNS.md
@.planning/phases/30-gate-proximo-paso/30-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contrato del backend que este plan consume (plan 30-01, wave 1). -->

POST /api/setters/leads/:id/call-disposition — body relevante:
```javascript
{
  outcome: 'answered_interested',
  notes: '<nota de la llamada, máx 500>',
  nextAction: { dueAt: '<ISO>', tipo?: 'callback', canal?: 'llamada', motivo?: '<máx 200>' },
  // origen lo fuerza el servidor a 'manual' — mandarlo no sirve de nada
  ..._dispoEnforcementBody(leadId),   // correctsAutoMarked / pendingCallId / telnyxCallMeta del pendiente
  telnyxCallMeta: { ... }             // meta FRESCA (pisa la del pendiente)
}
// → 200 { ok, lead, calendarEntry, resolvedPendingId }
// Sin `nextAction` válido el servidor asigna igual el default de +3 días (D-01).
```

<!-- Helpers del frontend que se reusan tal cual (verificados en public/app.js). -->
```javascript
window._dtPicker = { attach, sync, set, close };            // app.js:5007 (Phase 28)
function _dtPickerAttach(input, opts)                        // app.js:4962 — IDEMPOTENTE
function _dtPickerSync(input)                                // app.js:4988 — repinta el trigger
function _toDatetimeLocal(d)                                 // app.js:10849 — 'YYYY-MM-DDTHH:mm' local
function _dispoEnforcementBody(leadId)                       // app.js:8587
function _consumeTelnyxMeta(leadId)                          // app.js:~10039
function _dispoAfterSaved(leadId)                            // app.js:8532 — punto único post-guardado
function _leadStoreApply(id, patch)                          // app.js:5833 — escritura optimista única (#105)
function _refreshLeadPanels(leadId)                          // repinta lista + ficha modal de Hoy
async function _flushPendingTranscription(leadId, outcome)   // sube el audio a Whisper según el outcome
```

<!-- Los 2 call sites que llegan a call-disposition (PATTERNS §Gate frontend). -->
```javascript
window._handleCallDisposition = async (leadId, selectEl) => { ... }  // app.js:10617
window._pdHandleDisposition   = async function(leadId, selectEl) { ... } // app.js:7211
//   modalOpening (app.js:7214) = ['callback_later','scheduled_with_admin','answered_not_interested']
//   modalIds     (app.js:7234) = ['call-callback-modal','call-schedule-modal','call-objection-modal']
```
</interfaces>

<patron_cinturon_tiradores>
<!-- Fix #181b de CLAUDE.md. Copiar EXACTO en cualquier modal nuevo. -->
El modal se REUSA entre aperturas, así que el reset va en DOS lugares:
- **Cinturón**: al abrir, `btn.disabled = false` + `btn.textContent = '<label original>'`
  ANTES de `modal.classList.remove('hidden')`.
- **Tiradores**: `finally { btn.disabled = false; btn.textContent = '<label original>'; }`
  en cada intento de submit.
Sin los dos, el éxito esconde el modal con el botón en "Guardando…" y la
siguiente apertura muestra un botón muerto para el resto de la sesión.
Referencia viva: `openCallbackModal` (app.js:10811-10814 + 10842-10844) y
`openPlaceholderModal` (app.js:10735-10737 + 10761-10763).
</patron_cinturon_tiradores>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modal "Próximo paso" con la propuesta cargada (markup + helpers puros + apertura)</name>
  <files>public/index.html, public/app.js</files>
  <read_first>
    - `public/index.html` líneas 1540-1575 (el bloque de `#call-callback-modal`, que es el molde exacto de markup a copiar: `modal-overlay hidden`, `modal-card`, `modal-header`, quickpicks, input `datetime-local`, botones `pill-btn`/`btn-primary`).
    - `public/app.js` líneas 10768-10850 (`openCallbackModal` completo + `_toDatetimeLocal`) — es el analog de comportamiento (quickpicks + `_dtPickerAttach` + cinturón/tiradores).
    - `public/app.js` líneas 10994-11018 (`_buildCallbackQuickPicks`) — forma `{label, subtitle, date}` de los picks.
    - `public/app.js` líneas 4608-4700 (bloque `[28-01] DTPICKER-PURE`) — **el modelo del bloque puro con marcadores** que esta task tiene que replicar para que el test lo pueda extraer con `new Function`.
    - `.planning/phases/30-gate-proximo-paso/30-CONTEXT.md` (D-02, D-03).
  </read_first>
  <behavior>
    - `_gateInteresadoDefaultDate(now)` devuelve una `Date` exactamente 3 días después de `now` (259200000 ms), conservando la hora del día.
    - `_gateInteresadoPicks(now)` devuelve 4 picks `{label, subtitle, date}` con las duraciones de la cadencia de D-03 — día 1, día 3, día 7, día 15 — en orden ascendente, todas futuras respecto de `now`, y el pick de 3 días cae exactamente en la misma fecha/hora que `_gateInteresadoDefaultDate(now)`.
    - Ninguno de los dos helpers toca el DOM, `localStorage`, la red ni ninguna función de fuera del bloque (tienen que poder evaluarse aislados con `new Function`).
    - Abrir el modal dos veces seguidas deja el botón de confirmar habilitado y con su label original las dos veces.
  </behavior>
  <action>
**A. Markup en `public/index.html`** — insertar un bloque nuevo inmediatamente DESPUÉS del cierre de `#call-callback-modal` (después de la línea 1571), copiando su estructura:
   - Contenedor `<div id="call-next-modal" class="modal-overlay hidden" style="z-index:10000;">` con `modal-card` de `max-width:420px`.
   - `modal-header` con `<h3>Próximo paso</h3>` y la X que hace `classList.add('hidden')` sobre `call-next-modal` (mismo `onclick` inline que usa el modal de callback).
   - Párrafo explicativo: que el lead queda en **Hoy → Interesados** y que esta fecha es cuándo volver a hablarle. Sin nombre de empresa (regla anti-marca #119; igual esto no pasa por IA, pero el texto lo ve el usuario).
   - `<div id="call-next-quickpicks">` con el mismo grid de 2 columnas que `#call-cb-quickpicks`.
   - `<input type="datetime-local" id="call-next-fecha">` con los mismos estilos inline que `#call-cb-fecha`.
   - `<input type="text" id="call-next-motivo" maxlength="200">` opcional, placeholder tipo "Ej: pidió llamarlo cuando vuelva del congreso".
   - Botonera: "Cancelar" (`pill-btn` transparente, esconde el modal) y `<button class="btn-primary pill-btn" id="call-next-confirm">Guardar</button>`.
   - **NO agregar clases CSS nuevas**: todo con clases existentes + estilos inline, igual que el modal de callback. `public/style.css` no se toca en esta fase.

**B. Bloque puro en `public/app.js`** — insertarlo junto a `_buildCallbackQuickPicks` (~10994), delimitado por marcadores literales `// ─── [30-02] GATE-PURE: INICIO ───` y `// ─── [30-02] GATE-PURE: FIN ───` (mismo formato exacto que `[28-01] DTPICKER-PURE`, incluidos los guiones largos). Adentro, solo funciones puras y autocontenidas:
   - `GATE_INTERESADO_DELTA_MS = 3 * 24 * 60 * 60 * 1000` (espejo del backend; comentar que el servidor tiene su propia constante y que si una cambia hay que cambiar las dos).
   - `_gateInteresadoDefaultDate(now)` → `new Date(now.getTime() + GATE_INTERESADO_DELTA_MS)`.
   - `_gateInteresadoPicks(now)` → los 4 picks de D-03 (1/3/7/15 días) con `label` corto ("Mañana", "En 3 días", "En 1 semana", "En 15 días"), `subtitle` con día de semana + fecha + hora (mismo formato que `_buildCallbackQuickPicks`: `dayNames[d.getDay()] DD/M · HH:MM`) y `date`. Marcar cuál es el default (ej. campo `isDefault: true` en el de 3 días) para que la UI lo pueda resaltar al abrir.

**C. `openNextStepModal(leadId)` en `public/app.js`** — declararla junto a `openCallbackModal` (~10768) y exponerla como `window.openNextStepModal`. Estructura calcada de `openCallbackModal`:
   1. Resolver el lead con `_callsLeadsById.get(leadId)`; si no está, `return` (mismo guard que `openPlaceholderModal`).
   2. `fechaInput.value = _toDatetimeLocal(_gateInteresadoDefaultDate(new Date()))`.
   3. `_dtPickerAttach(fechaInput, { getLead: () => _callsLeadsById.get(leadId) })` — **usar exactamente esa forma de llamada** (el test `dtpicker-wiring` assertea el texto literal de cada call site).
   4. Renderizar `_gateInteresadoPicks(new Date())` en `#call-next-quickpicks` con la misma mecánica que los `.cb-quickpick`: botones con `data-iso`, listener que setea `fechaInput.value` + `_dtPickerSync(fechaInput)` + resalta el elegido. Resaltar de entrada el pick `isDefault`.
   5. Limpiar `#call-next-motivo`.
   6. **Cinturón** (ver `<patron_cinturon_tiradores>`): `confirmBtn.disabled = false; confirmBtn.textContent = 'Guardar';` ANTES de `modal.classList.remove('hidden')`.
   7. `confirmBtn.onclick` async: si `!fechaInput.value` → avisar con `window.showToast?.('Elegí cuándo volvés a hablarle — un interesado no puede quedar sin fecha.', { type:'warning' })` y `return` **sin** deshabilitar el botón (este es el "aviso claro" de GATE-01 / Success Criterion 1 del ROADMAP; usar toast y no `alert` para no bloquear el hilo durante una llamada). Después: `disabled = true`, `textContent = 'Guardando…'`, y en `finally` restaurar los dos.
   8. Body del POST: `{ outcome: 'answered_interested', nextAction: { dueAt: new Date(fechaInput.value).toISOString(), tipo: 'callback', canal: 'llamada', motivo: <valor de #call-next-motivo> }, ..._dispoEnforcementBody(leadId) }`; adjuntar `telnyxCallMeta` si `_consumeTelnyxMeta(leadId)` devuelve algo; y **leer `#pd-call-note`** igual que `_handleCallDisposition` (app.js:10659-10661): si tiene texto, mandarlo como `body.notes` recortado a 500 y vaciar el input. Sin esto, la nota tipeada en el Power Dialer se pierde al pasar este outcome por modal.
   9. Tras `resp.ok`: `_dispoAfterSaved(leadId)`, `_flushPendingTranscription(leadId, 'answered_interested').catch(...)`, parsear el JSON y `_leadStoreApply(leadId, data.lead)` (trae `estado`, `nextAction` y `callbackAt` frescos — el poller del Power Dialer los lee), esconder el modal, `_refreshLeadPanels(leadId)`, `renderCallsStats()`, `await loadCallsView()`. **No** agregar acá ningún toast de destino: eso lo universaliza el plan 30-03 desde `_dispoAfterSaved`.
   10. En `catch`, `alert('Error: ' + e.message)` (mismo idioma que los otros 3 modales) para que un fallo de red no pase desapercibido.
  </action>
  <verify>
    <automated>node --check public/app.js && npx vitest run tests/dtpicker-core.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check public/app.js` sale 0.
    - `grep -c "call-next-modal" public/index.html` devuelve ≥ 2 (contenedor + X de cierre) y existen los 4 ids `call-next-quickpicks`, `call-next-fecha`, `call-next-motivo`, `call-next-confirm` (1 hit cada uno).
    - `grep -c "class=\"dtp-\|--tile-accent\|seg-btn" public/style.css` sin cambios: `git diff --name-only` NO incluye `public/style.css`.
    - `grep -n "GATE-PURE: INICIO\|GATE-PURE: FIN" public/app.js` devuelve exactamente 2 líneas, en ese orden.
    - El bloque entre los marcadores no contiene `document.`, `window.`, `fetch(` ni `localStorage` (verificable con `sed -n` del rango + `grep -c`, debe dar 0 en los 4).
    - `grep -n "_dtPickerAttach(fechaInput, { getLead: () => _callsLeadsById.get(leadId) });" public/app.js` devuelve 2 líneas (la de `openCallbackModal` y la nueva).
    - En `openNextStepModal`, la línea que asigna `confirmBtn.textContent = 'Guardar'` aparece ANTES de la que hace `classList.remove('hidden')` (comparar números de línea con `grep -n`), y existe un bloque `finally` que restaura `disabled`/`textContent`.
  </acceptance_criteria>
  <done>El modal existe, se abre con la fecha de +3 días ya cargada, se puede editar con atajos o calendario, no deja guardar sin fecha, y su botón nunca queda muerto entre aperturas.</done>
</task>

<task type="auto">
  <name>Task 2: Rutear "Interesado" al paso nuevo en los dos caminos + no expulsar interesados del Power Dialer</name>
  <files>public/app.js</files>
  <read_first>
    - `public/app.js` líneas 10617-10695 (`_handleCallDisposition` completo: las 3 ramas que abren modal, la sección de outcomes directos, el manejo de `pd-call-note`).
    - `public/app.js` líneas 7206-7260 (`_pdHandleDisposition`: `modalOpening`, `_afterSaved`, la espera del modal con `modalIds`, `stillActionable`).
    - `public/app.js` líneas 6735-6750 (arranque de `_pdRender` con el bloque `holdCurrent` y las 2 expulsiones).
    - `public/app.js` líneas 6560-6575 (`_pdBuildQueueHoy`) y 6510-6545 (`_pdBuildQueue`) — para confirmar de dónde puede venir un lead `interesado` en la cola.
    - Si esos rangos ya quedaron en contexto por la Task 1, NO releerlos.
  </read_first>
  <action>
**A. `_handleCallDisposition`** (app.js:10617): agregar una rama para `answered_interested` junto a las otras tres, después del bloque de `answered_not_interested` (línea 10644-10649) y ANTES de la sección "Outcomes directos". Forma idéntica a las existentes: `openNextStepModal(leadId); selectEl.value = ''; selectEl.disabled = false; return;`.
   Reemplazar el comentario actual de las líneas 10638-10641 ("Interesado no abre la agenda… cae al flujo directo de abajo") por la explicación nueva: Interesado abre el paso de próximo paso (GATE-01/GATE-02) y **sigue sin abrir la agenda** — agendar es la acción aparte `scheduled_with_admin` (nota #63 de CLAUDE.md, intencional; no "arreglar" esto pensando que falta el modal de agenda).

**B. `_pdHandleDisposition`** (app.js:7211): agregar `'answered_interested'` al array `modalOpening` (línea 7214) y `'call-next-modal'` al array `modalIds` (línea 7234). Con eso el dialer espera a que el modal se cierre antes de decidir si avanza, en vez de disparar `_afterSaved` a los 600ms.
   Verificar (sin cambiar nada) que la evaluación de `stillActionable` (línea 7251) hace lo correcto con el estado nuevo: un interesado recién guardado tiene `callbackAt` futuro → `stillActionable === false` → corre `_afterSaved()` → con autopiloto avanza, sin autopiloto queda el banner "Resultado guardado" (#151). Si el usuario CANCELA el modal, no se guardó nada, `callbackAt` sigue vacío → `stillActionable === true` → el dialer se queda en el lead. Dejar constancia de esto en un comentario.

**C. `_pdRender`** (app.js:6744-6747) — **fix de regresión**: la expulsión por `callbackAt` futuro (línea 6746) echaría de la tarjeta a TODO interesado apenas tenga su próximo paso a +3 días, rompiendo el Power Dialer de "Hoy → Interesados" (nota #179: `_pdBuildQueueHoy` arma la cola con `_hoyState.interesadoIds`, sin mirar `callbackAt`). Agregar la excepción: no expulsar cuando `lead.estado === 'interesado'`.
   Comentar el razonamiento y la evidencia: un interesado solo puede estar en la cola del dialer vía la cola de Hoy — `_pdBuildQueue` (la cola de Llamadas, app.js:~6524) ya filtra `['descartado','agendado','interesado']`, así que esta excepción no puede colar un interesado en el dialer de Llamadas. La expulsión por estado terminal (línea 6745) queda intacta.

**NO TOCAR**: el gate de disposición obligatoria de la Phase 20 (`_dispoGateSet`/`_dispoGateClear`/`_dispoGateRestore`, `_autoMarkNoAnswer`, la cola de pendientes) — D-08, es otro mecanismo y coexiste. Tampoco los 8 outcomes restantes: siguen marcándose con un solo click/tecla.
  </action>
  <verify>
    <automated>node --check public/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check public/app.js` sale 0.
    - `grep -n "openNextStepModal(leadId)" public/app.js` devuelve una línea dentro del cuerpo de `_handleCallDisposition` (número de línea mayor que el de `window._handleCallDisposition = async` y menor que el de `function openPlaceholderModal`).
    - `grep -n "const modalOpening" public/app.js` muestra los 4 outcomes, incluido `answered_interested`.
    - `grep -n "const modalIds" public/app.js` muestra los 4 ids, incluido `call-next-modal`.
    - En `_pdRender`, la línea de la expulsión por `callbackAt` futuro contiene una condición que excluye `'interesado'` (verificable con `grep -n "estado !== 'interesado'" public/app.js` devolviendo ≥ 1 hit dentro del rango 6735-6760).
    - `grep -c "_dispoGateSet\|_dispoGateClear\|_dispoGateRestore" public/app.js` devuelve el MISMO número que antes del cambio (D-08: la Phase 20 no se toca). Registrar el número medido en el SUMMARY.
    - Los 9 botones del grid del Power Dialer (`pd-disposition-grid`, app.js:7045-7054) y las 9 opciones de `_dispoSelectHTML` (app.js:6036-6053) siguen existiendo sin cambios: `git diff public/app.js` no muestra ediciones en esos dos bloques.
  </acceptance_criteria>
  <done>Marcar Interesado desde la lista, desde Hoy o desde el Power Dialer abre el mismo paso de próximo paso; el dialer espera al modal y conserva la tarjeta del interesado en vez de expulsarla.</done>
</task>

<task type="auto">
  <name>Task 3: Tests del paso nuevo + actualizar dtpicker-wiring + cache-buster</name>
  <files>tests/gate-next-step-ui.test.js, tests/dtpicker-wiring.test.js, public/index.html</files>
  <read_first>
    - `tests/dtpicker-core.test.js` líneas 1-35 (el harness de extracción por marcadores con `new Function`) — es el patrón EXACTO a copiar para el bloque `[30-02] GATE-PURE`.
    - `tests/dtpicker-wiring.test.js` (COMPLETO) — es el archivo a editar: assertea que hay exactamente 5 llamadas a `_dtPickerAttach(`, y esta fase agrega la 6ta.
    - `public/index.html` líneas 1-20 y la línea del `<script type="module" src="/app.js?v=...">` (~3623) — los dos cache-busters.
    - `CLAUDE.md` sección "Cache-busting" (regla: bumpear en CADA edit a app.js/style.css, sin excepción).
  </read_first>
  <action>
**A. Crear `tests/gate-next-step-ui.test.js`.** No hay browser ni jsdom en el repo, así que se verifica en dos modos, ambos ya usados por el proyecto:

   1. **Bloque puro extraído** (patrón `tests/dtpicker-core.test.js`): leer `public/app.js`, cortar entre `// ─── [30-02] GATE-PURE: INICIO ───` y `// ─── [30-02] GATE-PURE: FIN ───`, evaluarlo con `new Function` devolviendo `{ _gateInteresadoDefaultDate, _gateInteresadoPicks, GATE_INTERESADO_DELTA_MS }`. Si los marcadores no están, tirar un error explicando cuáles faltan (mismo mensaje-guía que el test del dtpicker). Cubrir:
      - `GATE_INTERESADO_DELTA_MS === 259200000`.
      - `_gateInteresadoDefaultDate(new Date(2026, 7, 14, 15, 30))` cae el 17/08/2026 a las 15:30 (fecha local, comparar componentes con `getFullYear/getMonth/getDate/getHours/getMinutes` — **nunca** `toISOString`, que corre el día en AR: es el bug documentado en la decisión 21-04 de STATE.md).
      - `_gateInteresadoPicks(now)` devuelve 4 items, todos con `label`/`subtitle`/`date`, ordenados ascendente y todos `> now`.
      - Exactamente uno de los picks tiene `isDefault === true` y su `date` coincide al milisegundo con `_gateInteresadoDefaultDate(now)`.
      - Los picks cubren los 4 saltos de D-03 (1, 3, 7 y 15 días): verificar los deltas en ms contra `now`.

   2. **Aserciones de fuente** (patrón `tests/dtpicker-wiring.test.js` / `tests/app-version.test.js`), leyendo `public/app.js` e `public/index.html` como texto:
      - `index.html` contiene `id="call-next-modal"` y los 4 ids del modal.
      - `app.js` contiene `window.openNextStepModal` (la función está expuesta).
      - `_handleCallDisposition` rutea el outcome: el archivo contiene la rama con `openNextStepModal(leadId)`.
      - `modalOpening` incluye `'answered_interested'` y `modalIds` incluye `'call-next-modal'`.
      - **Anti-regresión del fix #181b**: dentro del cuerpo de `openNextStepModal` (cortar el texto desde `function openNextStepModal` hasta la siguiente declaración de función de nivel similar) aparece `textContent = 'Guardar'` al menos DOS veces (cinturón + tiradores) y existe la palabra clave `finally`.
      - **Anti-regresión del Power Dialer**: el archivo contiene la excepción de `'interesado'` en la expulsión por callback futuro.
      - `style.css` NO se tocó en esta fase: assertear que `public/style.css` no contiene ninguna clase con prefijo `call-next-` (la fase se hizo con clases existentes + inline).

**B. Editar `tests/dtpicker-wiring.test.js`**: cambiar el `expect(total - decl).toBe(5)` a `6` — actualizando también el comentario de cabecera del archivo ("los 5 campos de fecha reales" → 6) y el título del `it`. Agregar un `it` nuevo que assertee el call site nuevo con su texto literal, con el mismo estilo que los 5 existentes (`expect(appJs).toContain("_dtPickerAttach(fechaInput, { getLead: () => _callsLeadsById.get(leadId) });")` ya cubre dos call sites idénticos — el nuevo `it` debe verificar que ese literal aparece 2 veces usando `countOccurrences`, que el helper del archivo ya provee).

**C. Cache-buster** (`public/index.html`): bumpear el `<script type="module" src="/app.js?v=...">` de `v=20260814c` a **`v=20260814d`**. Si la fecha de ejecución no es el 2026-08-14, usar `v=<YYYYMMDD de hoy>a`. **No** tocar el de `style.css` (`v=20260814a`): esta fase no modifica la hoja de estilos.
  </action>
  <verify>
    <automated>npx vitest run tests/gate-next-step-ui.test.js tests/dtpicker-wiring.test.js tests/dtpicker-core.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/gate-next-step-ui.test.js` existe con ≥ 12 tests, todos verdes.
    - `tests/dtpicker-wiring.test.js` pasa con el conteo en 6 y sin ningún `it` eliminado (los 5 originales siguen).
    - `grep -n "app.js?v=" public/index.html` muestra un valor distinto de `20260814c`.
    - `grep -n "style.css?v=" public/index.html` sigue mostrando `20260814a` y `git diff --name-only` NO incluye `public/style.css`.
    - `npm test` termina con 0 fallos.
    - `npx vitest run tests/metrics-consistency.test.js` verde sin editar el archivo (D-09).
  </acceptance_criteria>
  <done>El paso nuevo tiene cobertura propia (helpers puros + cableado), el test del calendario refleja los 6 call sites reales, y el tab del usuario va a bajar el `app.js` nuevo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM del navegador → `openNextStepModal` | el `motivo` es texto libre del usuario que después se persiste y se re-renderiza |
| frontend → `POST /call-disposition` | la fecha propuesta viaja en el body (lo valida el servidor, plan 30-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05 | Tampering | `#call-next-motivo` (texto libre → `nextAction.motivo`) | mitigate | el servidor lo recorta a 200 chars (`_setNextAction`, index.js:10806); cuando el frontend lo muestre (plan 30-03) tiene que pasar por `escHtml`, nunca `innerHTML` crudo |
| T-30-06 | Spoofing | fecha del próximo paso construida en el cliente | accept | es una fecha de trabajo propia del SDR sobre su propio lead; el ownership del lead lo sigue validando el endpoint (index.js:11125-11134) |
| T-30-07 | Denial of Service | botón "Guardando…" muerto tras un error de red | mitigate | patrón cinturón+tiradores (fix #181b) obligatorio y verificado por aserción de fuente en la Task 3 |
| T-30-SC | Tampering | npm/pip/cargo installs | n/a | esta fase no instala paquetes; el modal usa solo clases y helpers existentes |
</threat_model>

<verification>
1. `node --check public/app.js` → OK.
2. `npm test` → 0 fallos.
3. Verificación en preview (patrón de las notas #124/#125: el screenshot puede colgarse, así que se inspecciona el DOM con el tool de JavaScript tras loguearse por fetch):
   - Abrir Llamadas, elegir "Interesado (sin agendar)" en el select de un lead → aparece `#call-next-modal` sin la clase `hidden`, con `#call-next-fecha` cargado a +3 días y el chip "En 3 días" resaltado.
   - Vaciar `#call-next-fecha` y clickear Guardar → no se dispara ningún POST y sale el aviso; el botón sigue habilitado.
   - Guardar con la fecha por defecto → el modal se cierra y el lead responde con `nextAction.dueAt` a +3 días.
   - Reabrir el modal sobre otro lead → `#call-next-confirm` está habilitado y dice "Guardar" (anti-regresión #181b).
   - Abrir el Power Dialer desde Hoy → Interesados y confirmar que la tarjeta del interesado NO se saltea sola.
</verification>

<success_criteria>
- Marcar "Interesado" nunca deja el lead sin fecha, y aceptar la propuesta cuesta un click.
- Los atajos de D-03 (1/3/7/15 días) y el calendario de la Phase 28 conviven en el mismo paso.
- El Power Dialer conserva la tarjeta del interesado tras marcarlo.
- Los otros 8 resultados se siguen marcando con un click/tecla, sin fricción nueva.
- Suite completa verde; `style.css` intacto; cache-buster bumpeado.
</success_criteria>

<output>
Create `.planning/phases/30-gate-proximo-paso/30-02-SUMMARY.md` when done.

Anotar en el SUMMARY: el número exacto de ocurrencias de `_dispoGate*` antes y
después (prueba de que D-08 no se tocó), el valor nuevo del cache-buster, y qué
se pudo verificar en preview vs qué queda pendiente de una llamada real
(el flujo con `telnyxCallMeta` fresca no se puede simular sin Telnyx).
</output>
