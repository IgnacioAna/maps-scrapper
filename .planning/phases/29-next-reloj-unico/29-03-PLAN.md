---
phase: 29-next-reloj-unico
plan: 03
type: execute
wave: 3
depends_on: ["29-01", "29-02"]
files_modified:
  - index.js
  - tests/next-action-followups.test.js
autonomous: true
requirements: [NEXT-03]
must_haves:
  truths:
    - "Tildar un paso de follow-up (24h/48h/72h/7d/15d) programa el MISMO próximo paso que cualquier otra vía: un nextAction con su fecha"
    - "Destildarlo apaga ese próximo paso"
    - "Ninguna vista deriva su verdad de `lead.followUps`: el campo queda en el JSON como registro muerto, sin borrar historia"
    - "Los 5 pasos siguen existiendo como plantillas de duración para elegir una fecha"
    - "Los reintentos automáticos de no-contacto NO se cuelan en la lista de follow-ups (siguen siendo plomería interna, criterio #150)"
  artifacts:
    - path: "index.js"
      provides: "PATCH /leads/:id/followup escribiendo nextAction; _computeFollowupsDue derivando de _leadNextAction"
      contains: "_leadNextAction"
    - path: "tests/next-action-followups.test.js"
      provides: "Cobertura del write-path de follow-up y del recorte de cadencia en la lista"
      min_lines: 90
  key_links:
    - from: "PATCH /api/setters/leads/:id/followup"
      to: "_setNextAction con NEXT_ACTION_TEMPLATES"
      via: "el step tildado se traduce a dueAt = ahora + deltaMs"
      pattern: "_setNextAction\\(lead"
    - from: "_computeFollowupsDue"
      to: "_leadNextAction"
      via: "única fuente de verdad; excluye origen 'cadencia'"
      pattern: "_leadNextAction\\(lead\\)"
    - from: "GET /api/setters/followups/today y /badge"
      to: "_computeFollowupsDue"
      via: "misma forma de respuesta que hoy (step/label/dueDate/startedAt/status/note)"
      pattern: "_computeFollowupsDue"
---

<objective>
Retirar `followUps` como reloj paralelo (NEXT-03 / D-04): sus 5 pasos pasan a
ser plantillas de `dueAt` y la vista de follow-ups deja de leer el campo.

Purpose: es la mitad menos riesgosa del "reloj único" en volumen (4 leads con
step activo en el snapshot local) pero la más importante conceptualmente: es
la que borra la posibilidad de que el sistema tenga dos respuestas distintas a
"¿cuándo vuelvo a este lead?".

Output: el write-path (`PATCH /leads/:id/followup`) programando `nextAction`,
el read-path (`_computeFollowupsDue`) derivando de `nextAction`, y la medición
del impacto real de ese cambio sobre los datos de producción.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-next-reloj-unico/29-CONTEXT.md
@.planning/phases/29-next-reloj-unico/29-PATTERNS.md
@.planning/phases/29-next-reloj-unico/29-01-SUMMARY.md
@.planning/phases/29-next-reloj-unico/29-02-SUMMARY.md
@CLAUDE.md

Notas de CLAUDE.md relevantes: **#175** (el toggle de follow-up se usa desde
la ficha de Hoy/Llamadas; es un RADIO, no checkboxes acumulativos: tildar uno
destilda los otros — comportamiento del endpoint, no bug), **#150** (los
reintentos automáticos no son una métrica del SDR).

**Backend-only.** El frontend sigue leyendo `followUps` para pintar el chip y
`callbackAt` para las colas: por eso este plan CONSERVA la escritura de los
flags (registro muerto) y NO toca `public/`.
</context>

<interfaces>
<!-- Creados en 29-01, usados acá tal cual -->
  NEXT_ACTION_TEMPLATES -> [{key:'24hs',label:'24h',deltaMs}, ... 5 items]
  _nextActionTemplateForDelta(deltaMs) -> template|null
  _setNextAction(lead, spec, nowIso) -> obj|null
  _clearNextAction(lead) -> void
  _leadNextAction(lead) -> obj|null

<!-- Forma de respuesta de _computeFollowupsDue que NO puede cambiar
     (la consumen /followups/today, /badge, team-performance y el frontend): -->
  [{ step, label, dueDate, startedAt, status, note }]
  status ∈ 'future' | 'dueToday' | 'dueYesterday' | 'overdue'
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Tildar un follow-up programa el próximo paso (write-path)</name>
  <files>index.js</files>
  <read_first>
    - index.js:9975-10017 (`PATCH /api/setters/leads/:id/followup` completo: guards de rol/visibilidad, semántica de radio, `followUpStartedAt`, y la forma exacta de la respuesta que el frontend consume)
    - index.js:11429-11497 (la sección de follow-ups y `_computeFollowupsDue`, para entender qué significaba cada campo)
    - index.js (bloque de helpers `NEXT_ACTION_*` agregado por 29-01)
    - public/app.js:7660-7690 (`_callsToggleFollowup` — SOLO LECTURA, para confirmar qué campos de la respuesta usa el frontend: `d.followUps` y `d.followUpStartedAt`. NO editar este archivo.)
  </read_first>
  <action>
El endpoint conserva su contrato externo COMPLETO (ruta, body `{step, value?}`,
validación del step contra los 5 válidos, guards de rol/visibilidad, y las
claves `followUps` / `followUpStartedAt` / `lead` en la respuesta). Lo que
cambia es qué se considera la verdad:

1) Rama "tildar" (`next === true`, hoy 9998-10001): además de la escritura
   actual de los flags y de `followUpStartedAt`, resolver la plantilla del
   step en `NEXT_ACTION_TEMPLATES` y programar el próximo paso con
   `_setNextAction`: `dueAt = new Date(Date.now() + template.deltaMs).toISOString()`,
   `tipo:'callback'`, `canal:'llamada'`, `motivo: 'follow-up ' + template.label`,
   `origen:'manual'`, `createdBy` = nombre del usuario autenticado
   (`req.auth?.user?.name || ''`). `nowIso` = el MISMO ISO que se escribe en
   `followUpStartedAt` (calcularlo una vez en una const y usarlo en los dos
   lados, para que `createdAt` y `followUpStartedAt` coincidan exacto — de eso
   depende que `_nextActionTemplateForDelta` pueda recuperar el `step`).

2) Rama "destildar" (`next === false`, hoy 10002-10007): además de apagar el
   flag y de poner `followUpStartedAt = null` cuando no queda ninguno activo,
   llamar `_clearNextAction(lead)` si el próximo paso vigente es el que este
   endpoint había programado. Criterio determinístico para "es el mío":
   `lead.nextAction` existe, `origen === 'manual'` y `motivo` empieza con
   `'follow-up '`. Si el próximo paso vigente vino de otra vía (un callback
   manual desde una disposición), NO se toca: destildar un checkbox no puede
   borrar un compromiso que el usuario pactó por teléfono.

3) Los flags `lead.followUps` y `lead.followUpStartedAt` SE SIGUEN
   ESCRIBIENDO (D-04: "el campo queda en el JSON como registro muerto"). Dejar
   un comentario de 3-4 líneas arriba de esa escritura diciendo exactamente
   eso: ya no es fuente de verdad de ninguna vista, se conserva porque el
   frontend todavía pinta el chip con él y porque borrar el campo sería borrar
   historia. Los lectores se migran en las fases 30-34.

4) Consecuencia esperada y BUSCADA (documentarla en el comentario del
   endpoint, no ocultarla): como `_setNextAction` espeja `callbackAt` (D-03),
   tildar un follow-up ahora TAMBIÉN saca el lead de la cola de Llamadas hasta
   esa fecha y lo hace aparecer en Hoy → Callbacks. Es el punto de la fase:
   antes tildar el checkbox no movía nada y el lead se perdía. Si esto no
   pasara, el reloj seguiría siendo doble.

El handler es SÍNCRONO (`loadSettersData` → mutar → `saveSettersData`, sin
`await` en el medio): NO envolverlo en `mutateSettersData` — la regla #19
aplica a handlers async, y convertirlo sería un cambio de forma innecesario.
  </action>
  <verify>
    <automated>node --check index.js && npx vitest run tests/followups.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check index.js` sale 0.
    - `npx vitest run tests/followups.test.js` verde SIN editar el archivo: sus 4 tests de `PATCH .../followup` siguen aseverando `followUps` y `followUpStartedAt` en la respuesta, que se siguen escribiendo.
    - En el handler, `grep` muestra una sola llamada a `_setNextAction` y una sola a `_clearNextAction`.
    - El ISO usado en `followUpStartedAt` y el `createdAt` del `nextAction` provienen de la MISMA const (verificable leyendo el diff: una sola llamada a `new Date().toISOString()` en la rama de tildado).
    - Prueba manual con supertest o curl contra el preview (registrar el JSON en el SUMMARY): tildar `72hs` sobre un lead devuelve `followUps['72hs'] === true` y deja el lead con `nextAction.motivo === 'follow-up 72h'` y `callbackAt === nextAction.dueAt` a ~72h.
    - `git diff --name-only` no incluye archivos bajo `public/`.
  </acceptance_criteria>
  <done>Tildar un paso de follow-up programa un próximo paso real y visible en las colas; destildarlo lo apaga sin pisar compromisos de otra vía.</done>
</task>

<task type="auto">
  <name>Task 2: La vista de follow-ups deja de leer lead.followUps (read-path)</name>
  <files>index.js</files>
  <read_first>
    - index.js:11429-11510 (`FOLLOWUP_HIDE_STATES`, `_isFollowupHidden`, `_computeFollowupsDue`, `_countFollowupsForBadge` — la implementación completa que se reemplaza)
    - index.js:11512-11620 (`GET /api/setters/followups/today` y `/badge`: qué campos del item consumen y cómo agrupan por status)
    - index.js:12060-12080 (team-performance: `followupsToday` vía `_countFollowupsForBadge` y `followupsOverdue` vía `_computeFollowupsDue`)
    - tests/followups.test.js (completo: los fixtures NO tienen `callbackAt`, así que la derivación legacy tiene que devolver exactamente los mismos items — este archivo es el contrato de regresión de esta task)
  </read_first>
  <action>
Reescribir `_computeFollowupsDue(lead, now = Date.now())` para que derive del
reloj único, conservando EXACTAMENTE la forma de retorno de `<interfaces>` y
el recorte por estado:

1) `_isFollowupHidden(lead)` se conserva tal cual y se sigue aplicando primero
   (agendado/descartado/cerrado + `interes === 'no'`, con el escape
   `followUpsReactivated`).
2) La fuente pasa a ser `_leadNextAction(lead)`. Sin objeto o sin `dueAt` →
   devuelve `[]`.
3) EXCLUIR `origen === 'cadencia'`: los reintentos automáticos de no-contacto
   nunca estuvieron en esta lista y son plomería interna (#150). Sin este
   recorte, los ~130 callbacks de cadencia del snapshot de producción
   inundarían el banner y las notificaciones de escritorio. Dejarlo escrito en
   un comentario con ese número medido.
4) `dueTs` = `Date.parse(na.dueAt)`; `baseTs` = `Date.parse(na.createdAt)` con
   fallback a `dueTs` cuando el `createdAt` falta (leads legacy). Los buckets
   de status se calculan con `_bizStartOfDay(now)` EXACTAMENTE como hoy
   (future / dueToday / dueYesterday / overdue): no cambiar esa aritmética,
   es la que ancló el flaky de medianoche descrito en la nota #163.
5) `step` y `label`: `_nextActionTemplateForDelta(dueTs - baseTs)`; si hay
   plantilla, `step`/`label` son los suyos (así un follow-up de 72h sigue
   reportando `step:'72hs'`, que es lo que asevera `tests/followups.test.js`);
   si no hay plantilla (un callback manual con fecha arbitraria), `step: ''` y
   `label: 'próximo paso'`.
6) `note`: pasa a ser `na.motivo || ''` (hoy es siempre `''`). Es aditivo.
7) `_countFollowupsForBadge` y los dos endpoints NO se tocan: siguen llamando
   a `_computeFollowupsDue` y agrupando igual.

Después de reescribirla, borrar el comentario de la sección que describe la
semántica vieja ("Reusa el data model existente: lead.followUps (flags por
step)...") y reemplazarlo por uno que diga que la sección quedó como VISTA
sobre `nextAction`, que `lead.followUps` ya no se lee, y que la migración de
los lectores de `callbackAt` es de las fases 30-34.

MEDICIÓN OBLIGATORIA (va al SUMMARY, no es opcional): antes y después del
cambio, correr un script Node de solo lectura sobre una COPIA de
`data/setters.json` (jamás sobre el archivo del repo) que cuente cuántos leads
caen en `dueToday`, `dueYesterday` y `overdue`. La lista pasa a incluir
también los callbacks MANUALES (no los de cadencia), así que el número va a
subir: reportar los dos conteos con su delta. Si el conteo de `overdue`
posterior superara los 60, PARAR y consultar antes de cerrar el plan — sería
señal de que el recorte de cadencia no está funcionando.
  </action>
  <verify>
    <automated>node --check index.js && npx vitest run tests/followups.test.js tests/team-performance.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/followups.test.js` verde SIN editar el archivo. Es la prueba dura de que la derivación legacy reproduce el comportamiento viejo: sus fixtures tienen solo `followUps` + `followUpStartedAt` y aseveran `step:'24hs'`, `step:'72hs'`, los buckets dueToday/dueYesterday/overdue/future y el conteo del badge.
    - Dentro del cuerpo de `_computeFollowupsDue`, `grep -c "lead.followUps"` devuelve 0 y `grep -c "_leadNextAction"` devuelve 1.
    - `grep -n "origen === 'cadencia'" index.js` muestra el recorte dentro de `_computeFollowupsDue`.
    - `npx vitest run tests/team-performance.test.js` verde (usa `followupsToday`/`followupsOverdue`).
    - En el SUMMARY quedan los conteos medidos antes/después sobre la copia de datos reales, con el delta explicado.
    - `grep -rn "lead.followUps\[" index.js` solo devuelve líneas del write-path (el endpoint PATCH y los resets), nunca de un lector.
  </acceptance_criteria>
  <done>Ninguna vista del backend deriva su verdad de `lead.followUps`; la lista de follow-ups es una vista del reloj único y su cambio de volumen está medido, no adivinado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests del retiro de followUps</name>
  <files>tests/next-action-followups.test.js</files>
  <read_first>
    - tests/followups.test.js (fixture y estilo; este archivo nuevo lo COMPLEMENTA, no lo reemplaza)
    - index.js (endpoint y `_computeFollowupsDue` ya modificados por las tasks 1 y 2)
  </read_first>
  <behavior>
    - Tildar `24hs` → `nextAction` con `motivo:'follow-up 24h'`, `origen:'manual'`, `dueAt` a ~24h, `callbackAt === dueAt`, y `followUps['24hs'] === true` (registro muerto, se sigue escribiendo).
    - Tildar `7d` después de `24hs` → un solo `nextAction` vigente, con `dueAt` a ~7 días (D-02: a lo sumo uno pendiente).
    - Destildar el step activo → `nextAction === null` y `callbackAt` vacío.
    - Destildar un step cuando el próximo paso vigente vino de una disposición `callback_later` → el `nextAction` NO se toca.
    - Un lead con `followUps` legacy activo y SIN `nextAction` aparece en `GET /api/setters/followups/today` con el `step` correcto (derivación, sin migración previa).
    - Un lead cuyo próximo paso es de cadencia (`no_answer`) NO aparece en `/followups/today` ni suma al `/badge`.
    - Un lead con callback MANUAL vencido SÍ aparece en `/followups/today` (bucket `overdue`) con `step:''` cuando la fecha no coincide con ninguna plantilla.
  </behavior>
  <action>
Crear `tests/next-action-followups.test.js` con el boilerplate estándar
(DATA_DIR temporal, API keys de IA a `""` por la regla #121, login admin,
teléfonos `+521...` de >= 7 dígitos).

El fixture necesita al menos 4 leads: uno virgen para tildar/destildar, uno
con `followUps` legacy activo (flags + `followUpStartedAt`, sin `nextAction`
ni `callbackAt`), uno que va a recibir `no_answer` (cadencia), y uno con
callback manual vencido (`callbackAt` en el pasado + un entry de callLog con
`outcome:'callback_later'`).

Cubrir uno por uno los casos de `<behavior>`. Para los que consultan
`/followups/today`, asegurarse de que el estado del lead no esté en
`FOLLOWUP_HIDE_STATES` (agendado/descartado/cerrado) ni con `interes:'no'`, o
el item se oculta por diseño y el test mentiría.
  </action>
  <verify>
    <automated>npx vitest run tests/next-action-followups.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/next-action-followups.test.js` pasa con >= 7 tests.
    - `npm test` completo verde (mismos criterios de flaky conocidos que en 29-02: verificar contra `main` limpio antes de atribuir un fallo a este plan).
    - El archivo NO contiene `delete process.env.`.
    - Verificación por mutación registrada en el SUMMARY: quitar el recorte `origen === 'cadencia'` de `_computeFollowupsDue` hace fallar el test del lead de cadencia. Restaurar después.
  </acceptance_criteria>
  <done>El retiro de `followUps` tiene red propia: write-path, read-path, derivación legacy y el recorte de cadencia quedan cubiertos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `PATCH /api/setters/leads/:id/followup` | Body `{step, value}`; guards de rol (setter solo sus leads) y de visibilidad (supervisor scoped) ya presentes en el handler |
| browser → `GET /api/setters/followups/today` \| `/badge` | Devuelve leads de OTROS setters si el rol lo permite; el filtro por `setterId` ya existe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09 | Tampering | `step` recibido en el body del PATCH | mitigate | La validación existente contra los 5 steps válidos se conserva (400 si no está); la plantilla se resuelve por lookup en `NEXT_ACTION_TEMPLATES`, nunca construyendo la duración desde el input |
| T-29-10 | Tampering | Destildar un checkbox borrando un compromiso pactado por teléfono | mitigate | `_clearNextAction` solo se llama si el próximo paso vigente es el que el endpoint creó (`origen:'manual'` + `motivo` con prefijo `follow-up `) |
| T-29-11 | Elevation of Privilege | Setter tocando follow-ups de leads ajenos | accept (ya mitigado) | Guards de ownership/visibilidad del handler intactos (index.js:9985-9988); `tests/followups.test.js` ya cubre el 403 |
| T-29-12 | Denial of Service | Inundación del banner/notificaciones de escritorio con reintentos de cadencia | mitigate | Recorte explícito `origen !== 'cadencia'` + medición obligatoria antes/después con umbral de parada (>60 overdue → consultar) |
| T-29-SC | Tampering | Instalación de paquetes (npm) | accept | Cero instalaciones en este plan |
</threat_model>

<verification>
1. `node --check index.js`.
2. `npx vitest run tests/followups.test.js tests/team-performance.test.js` — verde SIN editar esos archivos (contrato de regresión del retiro).
3. `npx vitest run tests/next-action-followups.test.js`.
4. `npm test` completo verde.
5. Conteos dueToday/dueYesterday/overdue medidos antes y después sobre una COPIA
   de `data/setters.json`, con el delta documentado.
</verification>

<success_criteria>
- `lead.followUps` no se lee en ningún lector del backend (grep lo prueba).
- Las 5 duraciones siguen vivas como plantillas y siguen devolviendo el `step`
  correcto en la respuesta de `/followups/today`.
- Tildar un follow-up programa un próximo paso real; destildarlo lo apaga sin
  pisar compromisos de otra vía.
- Los reintentos de cadencia siguen fuera de la lista de follow-ups.
- Suite completa verde.
</success_criteria>

<output>
Create `.planning/phases/29-next-reloj-unico/29-03-SUMMARY.md` when done.
Registrar ahí: los conteos antes/después de la lista de follow-ups sobre datos
reales, el JSON de la prueba manual del PATCH, y la verificación por mutación.
</output>
