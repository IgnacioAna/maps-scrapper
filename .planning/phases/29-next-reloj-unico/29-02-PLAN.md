---
phase: 29-next-reloj-unico
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - index.js
  - tests/next-action-disposition.test.js
  - tests/apply-call-outcome.test.js
autonomous: true
requirements: [NEXT-02, NEXT-04]
must_haves:
  truths:
    - "Marcar 'volver a llamar' con fecha deja UN nextAction manual con esa fecha, y la fecha visible es la misma que hoy"
    - "Un no_answer/voicemail deja UN nextAction de cadencia a +24h; el segundo seguido descarta el lead y lo deja SIN próximo paso (salvo interesados)"
    - "El tope de 2 cortes sigue descartando al 2do hung_up y deja el lead sin próximo paso"
    - "Marcar CUALQUIER resultado sobre un lead con próximo paso pendiente (vencido o no) lo reemplaza o lo apaga — nunca lo deja arrastrado"
    - "El webhook del agente de voz produce exactamente los mismos efectos que una disposición humana: un solo helper, sin bifurcación"
    - "El número de 'En seguimiento' (callbacks manuales del dueño) NO cambia ni un lead respecto de hoy"
  artifacts:
    - path: "index.js"
      provides: "_applyCallOutcome escribiendo el reloj único; manualCallbackByOwner leyendo nextAction.origen"
      contains: "_setNextAction"
    - path: "tests/next-action-disposition.test.js"
      provides: "Cobertura de cadencia, callback manual, consumo por disposición, tope de cortes e interesados"
      min_lines: 120
    - path: "tests/apply-call-outcome.test.js"
      provides: "Paridad humano↔agente de voz extendida a nextAction"
      contains: "nextAction"
  key_links:
    - from: "_applyCallOutcome (entrada)"
      to: "_clearNextAction"
      via: "consumo del compromiso pendiente en toda disposición (D-08 / NEXT-04)"
      pattern: "_clearNextAction\\(lead\\)"
    - from: "_applyCallOutcome (rama callback_later y rama cadencia)"
      to: "_setNextAction"
      via: "único punto de escritura del próximo paso"
      pattern: "_setNextAction\\(lead"
    - from: "GET /api/setters/leads/sin-wsp"
      to: "_leadNextAction"
      via: "manualCallbackByOwner deriva de origen==='manual' (D-09)"
      pattern: "manualCallbackByOwner"
---

<objective>
Que `_applyCallOutcome` escriba y consuma UN SOLO reloj (NEXT-02, NEXT-04),
preservando byte por byte el comportamiento visible de hoy: misma fecha de
cadencia, mismo descarte al 2do no-contacto, misma excepción de interesados,
mismo tope de cortes, mismos números de funnel.

Purpose: es la función que el usuario ejecuta decenas de veces por día y la
que comparte el webhook del agente de voz. Todo lo demás de la fase depende de
que acá el cambio sea de MODELO, no de comportamiento.

Output: `_applyCallOutcome` sobre `nextAction`, el lector de "En seguimiento"
migrado a `origen`, y dos suites (una nueva, una extendida) que lo prueban.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-next-reloj-unico/29-CONTEXT.md
@.planning/phases/29-next-reloj-unico/29-PATTERNS.md
@.planning/phases/29-next-reloj-unico/29-01-SUMMARY.md
@CLAUDE.md

Notas de CLAUDE.md que este plan NO puede romper: **#150** ("En seguimiento" =
SOLO callbacks manuales del dueño actual), **#171** (tope de 2 cortes),
**#182** (toda disposición consume el callback pendiente), **#183**
(los interesados NUNCA se auto-descartan por no-contacto).

Regla transversal del milestone: `tests/metrics-consistency.test.js` es la
garantía anti-regresión del funnel. Ningún número puede moverse.

**Backend-only.** Cero cambios en `public/`.
</context>

<interfaces>
<!-- Ya existen (creados en 29-01). Usarlos, no redefinirlos. -->
  _setNextAction(lead, {tipo, dueAt, canal, motivo, origen, createdBy}, nowIso) -> obj|null
  _clearNextAction(lead) -> void          // apaga nextAction Y callbackAt juntos
  _leadNextAction(lead) -> obj|null       // nextAction explícito, o derivación legacy
  NEXT_ACTION_TEMPLATES, NEXT_ACTION_TIPOS/CANALES/ORIGENES

<!-- Contrato vigente de _applyCallOutcome (index.js:10625). NO cambiar la firma: -->
  _applyCallOutcome(data, lead, logEntry, opts) -> { calendarEntry }
  opts = { leadId, nowIso, outcome, callbackAt, callbackShared, scheduled,
           cleanReason, doNotCall, actorSetterId, actorName, skipCalendarCreation }
<!-- Los DOS callers ya pasan nowIso y actorName: el handler humano
     (index.js:10975) y el webhook de Retell (index.js:17431). -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: _applyCallOutcome escribe y consume el reloj único</name>
  <files>index.js</files>
  <read_first>
    - index.js:10618-10800 (`_applyCallOutcome` ENTERA — incluido el bloque de helpers que agregó 29-01 justo arriba). Leerla completa antes de tocar una línea: cada comentario de esa función documenta un bug real de producción que no puede volver.
    - index.js:10840-11000 (el handler humano: cómo arma `logEntry`, el snapshot `preCadence`, y los `opts` que pasa)
    - index.js:17400-17450 (el webhook de Retell llamando al MISMO helper con `actorSetterId: VOICE_AGENT_SETTER_ID`)
    - .planning/phases/29-next-reloj-unico/29-PATTERNS.md (sección "_applyCallOutcome — core state machine", que cita los 5 sitios de escritura literalmente)
  </read_first>
  <action>
Reemplazar los 5 sitios donde `_applyCallOutcome` escribe el reloj. Ningún
otro cambio dentro de la función: el switch de outcomes, el bloque DNC, el
cálculo de `streak`, `cadenceStep`, `cadenceExhausted`, `autoDiscarded`,
`autoDiscardReason`, `estado` y el push del `logEntry` quedan IDÉNTICOS.

1) Entrada de la función (hoy index.js:10645, `lead.callbackAt = '';` con el
   comentario de 8 líneas del caso real del 2026-08-12): pasa a
   `_clearNextAction(lead);`. Conservar el comentario existente y agregarle una
   línea: la regla se generaliza del `callbackAt` a TODO el próximo paso
   (NEXT-04 / D-08) — cualquier compromiso pendiente muere cuando se vuelve a
   tocar el lead, y las ramas que programan uno nuevo lo hacen más abajo.

2) Rama `case 'callback_later'` (hoy 10686-10691): calcular la fecha en una
   const local con la EXPRESIÓN EXACTA de hoy
   (`callbackAt || new Date(Date.now() + 24*60*60*1000).toISOString()`) y
   pasarla a `_setNextAction` con `tipo:'callback'`, `canal:'llamada'`,
   `motivo:''`, `origen:'manual'`, `createdBy: opts.actorName || ''` y
   `opts.nowIso` como `nowIso`. La línea de `callbackShared` queda tal cual,
   DESPUÉS de la escritura. No asignar `lead.callbackAt` a mano: el espejo lo
   hace `_setNextAction`.

3) Tope de cortes, rama de descarte (hoy 10758-10763): la línea
   `lead.callbackAt = '';` pasa a `_clearNextAction(lead);`. El resto de la
   rama (`estado='descartado'`, `autoDiscarded`, `autoDiscardReason` con el
   template `cortes_${MAX_HUNG_UP}x`) intacto.

4) Cadencia, rama de descarte al 2do no-contacto (hoy 10781-10787): ídem,
   `lead.callbackAt = '';` → `_clearNextAction(lead);`. `cadenceExhausted`,
   `autoDiscarded` y `autoDiscardReason` (`sin_contacto_${MAX_NO_CONTACT}x`)
   intactos. La condición `streak >= MAX_NO_CONTACT && lead.estado !== 'interesado'`
   NO se toca: es la excepción de interesados de la nota #183.

5) Cadencia, rama de reintento (hoy 10788-10792): la asignación
   `lead.callbackAt = new Date(Date.now() + 24 * 3600000).toISOString();` pasa
   a `_setNextAction` con esa MISMA expresión como `dueAt`, `tipo:'cadencia'`,
   `canal:'llamada'`, `motivo:'reintento de no-contacto'`, `origen:'cadencia'`,
   `createdBy: opts.actorName || ''`. `lead.cadenceExhausted = false` queda
   después.

Prohibiciones explícitas de esta task:
- NO agregar parámetros nuevos a `opts` ni ramas condicionales por caller: el
  helper sigue siendo uno solo para la disposición humana y para el webhook de
  voz (D-10). Si algo "solo aplica al humano", está mal pensado.
- NO cambiar el orden relativo de los bloques (switch → DNC → tope de cortes →
  cadencia): el DNC puede setear `estado='descartado'` y las dos ramas de abajo
  chequean `!lead.doNotCall`.
- NO "mejorar" el cálculo de fechas (nada de `_bizStartOfDay`, nada de
  redondeos). Las expresiones de fecha se copian literales.
  </action>
  <verify>
    <automated>node --check index.js && npx vitest run tests/call-cadence.test.js tests/hangup-cap.test.js tests/disposition-dnc.test.js tests/funnel-close.test.js tests/disposition-enforcement.test.js tests/metrics-consistency.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check index.js` sale 0.
    - Dentro del cuerpo de `_applyCallOutcome` no queda NINGUNA asignación directa al reloj: `sed -n '<inicio>,<fin>p' index.js | grep -c "lead.callbackAt ="` devuelve 0 (reemplazar `<inicio>,<fin>` por las líneas reales de la función tras el edit).
    - En ese mismo rango, `grep -c "_clearNextAction(lead)"` devuelve 3 (entrada + los 2 descartes automáticos) y `grep -c "_setNextAction(lead"` devuelve 2 (callback manual + reintento de cadencia).
    - Las 6 suites del `<verify>` pasan SIN editar ninguna de ellas. En particular `tests/call-cadence.test.js` prueba, sin cambios, que la fecha de cadencia sigue cayendo entre 23 y 25 horas y que el interesado no se descarta.
    - `npx vitest run tests/metrics-consistency.test.js` verde: 0 tests fallando, 0 números de funnel modificados.
    - `grep -n "opts\." index.js` en el rango de la función no muestra ninguna clave nueva fuera de las listadas en `<interfaces>`.
  </acceptance_criteria>
  <done>Las disposiciones escriben `nextAction` (y `callbackAt` por espejo), consumen el pendiente al entrar, y las 6 suites de regresión del área siguen verdes sin tocarlas.</done>
</task>

<task type="auto">
  <name>Task 2: "En seguimiento" se lee del origen del próximo paso (D-09) + paridad con el agente de voz</name>
  <files>index.js, tests/apply-call-outcome.test.js</files>
  <read_first>
    - index.js:8300-8383 (`GET /api/setters/leads/sin-wsp` completo: el filtro de cola, el bloque de atribución `calledByOwner` y el cálculo de `manualCallbackByOwner` en 8372-8380)
    - index.js:9303-9339 (`_tariffRedButEngaged`, `_leadIsCallableNow`, `_leadPendingForOwner` — LECTORES de `callbackAt` que este plan NO toca, D-03)
    - tests/apply-call-outcome.test.js (completo: la lista `PARITY_FIELDS` en ~157 y el comparador con tolerancia de reloj en ~163-180)
    - tests/pipeline-attribution.test.js (qué número exacto protege "En seguimiento")
  </read_first>
  <action>
A) `manualCallbackByOwner` (index.js:8379). Hoy se decide con
   `l.callbackAt && ultimo entry.outcome === 'callback_later' && atribuido al dueño`.
   Pasa a decidirse con `_leadNextAction(l)`: es manual si el objeto existe,
   tiene `dueAt` no vacío y `origen === 'manual'`. La parte de ATRIBUCIÓN
   (`_callSetterId(_last, l, _swUserMap) === l.assignedTo`) queda EXACTAMENTE
   como está — sigue usando el último entry del callLog.
   Actualizar el comentario de 8372-8376 para que describa la regla nueva y
   deje escrito por qué el número no se mueve: la derivación legacy de
   `_leadNextAction` marca `origen:'manual'` sí y solo sí el último outcome es
   `callback_later`, que es el criterio literal de hoy (#150).
   NO tocar `calledByOwner`, ni el filtro de callbacks compartidos (8352-8356),
   ni `_leadIsCallableNow`: siguen leyendo `callbackAt` a propósito (D-03).

B) `tests/apply-call-outcome.test.js`: sumar `"nextAction"` a la lista de
   campos comparados entre la vía HTTP (handler humano) y la vía directa
   (helper puro, la que usa el webhook de voz). Como el objeto contiene un
   `dueAt` generado con `Date.now()` en la rama de cadencia, la comparación de
   `nextAction` necesita el MISMO tratamiento que ya tiene `callbackAt`:
   comparar `tipo`/`canal`/`origen`/`motivo` con igualdad estricta y `dueAt`
   con la tolerancia de reloj que el archivo ya implementa (`closeIso`),
   reutilizando el flag `looseCallbackAt` que el test ya usa para decidir
   cuándo aplicar tolerancia. Mantener el estilo y los comentarios del archivo.
  </action>
  <verify>
    <automated>node --check index.js && npx vitest run tests/apply-call-outcome.test.js tests/pipeline-attribution.test.js tests/callable-leads.test.js tests/shared-callback.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "manualCallbackByOwner" index.js` muestra el cálculo derivando de `_leadNextAction` y de `origen`, sin referencia a `outcome === 'callback_later'` en esa expresión.
    - `grep -n "callbackAt" index.js` sigue mostrando los lectores intactos en `_leadIsCallableNow` (~9325) y en el filtro de callbacks compartidos (~8356): este plan NO los migra.
    - `tests/pipeline-attribution.test.js` pasa sin editarlo (es el test que fija el número de "En seguimiento" para un lead heredado, uno de cadencia y uno manual).
    - `tests/apply-call-outcome.test.js` incluye `nextAction` entre los campos comparados y pasa.
    - Verificación de "el número no se movió" con datos reales, registrada en el SUMMARY: correr un script Node de solo-lectura sobre una COPIA de `data/setters.json` (nunca sobre el archivo del repo) que cuente los leads con `callbackAt` cuyo último outcome es `callback_later`, y comparar contra el conteo de los que `_leadNextAction` marca `origen:'manual'`. Los dos números tienen que ser IGUALES.
  </acceptance_criteria>
  <done>"En seguimiento" se lee del modelo nuevo, el número medido contra datos reales es idéntico al anterior, y la paridad humano↔agente cubre `nextAction`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tests de comportamiento del reloj único en disposiciones</name>
  <files>tests/next-action-disposition.test.js</files>
  <read_first>
    - tests/call-cadence.test.js (completo: es el modelo exacto de este archivo — fixture mínimo, helper `disp()`, helper `hoursFromNow()`)
    - tests/hangup-cap.test.js (fixture multi-lead y el idioma de aserción del tope de cortes)
    - index.js (`_applyCallOutcome` ya modificada por la Task 1)
  </read_first>
  <behavior>
    - `callback_later` con fecha explícita → `nextAction.tipo==='callback'`, `origen==='manual'`, `dueAt` === la fecha mandada, y `callbackAt` === `nextAction.dueAt`.
    - `callback_later` SIN fecha → `dueAt` a +24h (entre 23 y 25 horas) y `origen==='manual'`.
    - 1er `no_answer` → `nextAction.origen==='cadencia'`, `tipo==='cadencia'`, `dueAt` entre 23 y 25 horas, `cadenceStep===1`, lead no descartado.
    - 2do `no_answer` seguido → `estado==='descartado'`, `nextAction===null`, `callbackAt` vacío, `cadenceExhausted===true`.
    - Lead `interesado` con dos no-contactos seguidos → sigue `interesado` y CONSERVA un `nextAction` de cadencia a +24h (#183).
    - Consumo (NEXT-04): lead con callback manual a futuro + luego `wrong_number` → `nextAction===null` y `callbackAt` vacío.
    - Consumo con vencido: lead con callback manual VENCIDO + luego `hung_up` (1er corte) → `nextAction===null` (el caso real del 2026-08-12 que dejaba el lead clavado 1° en la cola).
    - 2do `hung_up` → `estado==='descartado'` y `nextAction===null`.
    - `answered_interested` y `scheduled_with_admin` → `nextAction===null` (la Phase 30 es la que va a exigir uno; hoy no).
    - Un lead que llega con `callbackAt` legacy y SIN `nextAction` (fixture escrito a mano) responde con próximo paso derivado en `GET /api/setters/leads/sin-wsp` (`manualCallbackByOwner` correcto) ANTES de cualquier migración.
  </behavior>
  <action>
Crear `tests/next-action-disposition.test.js` copiando el boilerplate de
`tests/call-cadence.test.js` (DATA_DIR temporal propio, API keys de IA a `""`
—regla #121—, login admin en `beforeAll`, helper `disp(leadId, body)`).

Usar un lead distinto por escenario (no encadenar estados entre tests salvo
donde el escenario ES la secuencia, como la cadencia): los teléfonos del
fixture con prefijo `+521` y >= 7 dígitos (regla #163), si no el lead nunca
entra en las colas.

Cada `it()` cubre uno de los casos de `<behavior>`, aseverando SIEMPRE las dos
caras del espejo (`nextAction.dueAt` y `callbackAt`) — si algún día alguien
rompe el espejo, este archivo es el que tiene que gritar.
  </action>
  <verify>
    <automated>npx vitest run tests/next-action-disposition.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/next-action-disposition.test.js` pasa con >= 10 tests.
    - `npm test` (suite completa) verde. Base de referencia: ~1181 tests al 2026-08-13 + los agregados por 29-01 y este plan. Si algo falla, verificar PRIMERO con `git stash` si ya fallaba en `main` limpio (los flaky históricos conocidos son `wa-campaign-engine` por hora/día, y `mercury-*` por red) y dejarlo documentado en el SUMMARY con esa evidencia.
    - El archivo NO contiene `delete process.env.` (regla #121).
    - Existe al menos un test que arranca de un fixture con `callbackAt` legacy y `nextAction` ausente (prueba la derivación en vivo, sin migración).
  </acceptance_criteria>
  <done>El comportamiento del reloj único queda fijado por tests propios y la suite completa del proyecto sigue verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `POST /api/setters/leads/:id/call-disposition` | El body trae `outcome`, `callbackAt`, `notes`, flags; el handler ya valida rol/ownership ANTES de invocar el helper |
| webhook Retell → `_applyCallOutcome` | Entrada externa firmada; usa el MISMO helper, con `actorSetterId: setter_agente_ia` |
| lectura `GET /api/setters/leads/sin-wsp` | Devuelve leads con scoping por setter/supervisor (Phase 18) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Tampering | `callbackAt` que llega del body y se convierte en `nextAction.dueAt` | mitigate | Se conserva la expresión de hoy (`callbackAt \|\| +24h`) y `_setNextAction` recorta/coerciona el resto de los campos; el `dueAt` nunca se ejecuta ni se interpola en HTML — solo se guarda y se compara como fecha |
| T-29-06 | Elevation of Privilege | Un setter marcando disposición sobre un lead ajeno | accept (ya mitigado) | El guard de ownership/visibilidad vive en el handler (index.js:10830-10847) y NO se toca en este plan; `_applyCallOutcome` sigue sin leer identidad (contrato T-24-01-01) |
| T-29-07 | Tampering | Bifurcación de lógica entre disposición humana y webhook de voz | mitigate | Prohibición explícita en la Task 1 de agregar ramas por caller; `tests/apply-call-outcome.test.js` compara los dos caminos campo por campo, ahora incluido `nextAction` |
| T-29-08 | Information Disclosure | `manualCallbackByOwner` expuesto en la respuesta de la cola | accept | Es un booleano derivado de datos que el mismo endpoint ya devuelve, y la respuesta ya está scopeada por setter/supervisor |
| T-29-SC | Tampering | Instalación de paquetes (npm) | accept | Cero instalaciones en este plan (`package.json`/`package-lock.json` sin cambios en el diff) |
</threat_model>

<verification>
1. `node --check index.js`.
2. `npx vitest run tests/next-action-disposition.test.js tests/apply-call-outcome.test.js`.
3. Regresión del corazón del sistema, sin editar ninguno de estos archivos:
   `npx vitest run tests/metrics-consistency.test.js tests/call-cadence.test.js tests/hangup-cap.test.js tests/disposition-dnc.test.js tests/disposition-enforcement.test.js tests/funnel-close.test.js tests/pipeline-attribution.test.js tests/shared-callback.test.js`
4. `npm test` completo verde.
5. Conteo comparativo de "En seguimiento" contra una COPIA de `data/setters.json`
   (viejo criterio vs `origen:'manual'`): mismo número.
</verification>

<success_criteria>
- `_applyCallOutcome` no asigna `callbackAt` a mano en ninguna rama.
- Toda disposición consume el próximo paso pendiente (NEXT-04).
- Cadencia, tope de cortes y excepción de interesados producen exactamente los
  mismos estados y fechas que antes del plan.
- `metrics-consistency` verde sin un solo número movido.
- El agente de voz y el humano siguen produciendo efectos idénticos.
</success_criteria>

<output>
Create `.planning/phases/29-next-reloj-unico/29-02-SUMMARY.md` when done.
Registrar ahí: el conteo comparativo de "En seguimiento" (criterio viejo vs
nuevo) sobre datos reales, y el resultado de `npm test` con el total de tests.
</output>
