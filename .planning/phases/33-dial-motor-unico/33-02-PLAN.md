---
phase: 33-dial-motor-unico
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - public/app.js
  - public/index.html
  - tests/dial-hold.test.js
autonomous: true
requirements: [DIAL-02]
must_haves:
  truths:
    - "Marcar cualquier resultado dentro del Power Dialer deja la tarjeta en pantalla con el banner '✓ Resultado guardado' hasta que el user aprieta Siguiente (o S)"
    - "El hold funciona igual en la cola de Llamadas y en las 3 colas de Hoy (completa, callbacks, interesados)"
    - "Los resultados que abren modal (callback, no interesado, agendar, interesado/próximo paso) también dejan el hold, no solo los directos"
    - "Cancelar un modal no deja hold ni avanza: el lead sigue siendo la tarjeta actual y accionable"
    - "Con el autopiloto encendido se mantiene el avance automático de siempre"
    - "El banner muestra el resultado marcado y a dónde se fue el lead (vista + cuándo vuelve), calculado por _dispoDestination"
  artifacts:
    - path: "public/app.js"
      provides: "_pdHold(leadId, outcome, opts) como único camino al hold + _pd.pendingSave escrito por _dispoAfterSaved"
      contains: "function _pdHold"
    - path: "tests/dial-hold.test.js"
      provides: "Suite de DIAL-02: universalidad del hold, consumo de pendingSave, autopiloto, banner"
  key_links:
    - from: "_dispoAfterSaved"
      to: "_pd.pendingSave"
      via: "señal determinística de 'esta disposición se confirmó', reemplaza la heurística stillActionable"
      pattern: "_pd\\.pendingSave"
    - from: "_pdHandleDisposition"
      to: "_pdHold"
      via: "único camino post-guardado del dialer, para outcomes directos y con modal"
      pattern: "_pdHold\\("
    - from: "_pdHold"
      to: "_dispoDestination"
      via: "holdMeta del banner cuando el aviso no pasó por _dispoAnnounce"
      pattern: "_dispoDestination\\("
---

<objective>
Marcar un resultado dentro del Power Dialer deja de expulsar la tarjeta:
el lead se queda hasta que el user decide avanzar, en TODAS las colas y para
TODOS los resultados —incluidos los que abren modal— y el banner dice qué se
guardó y a dónde se fue el lead (DIAL-02, reclamo textual: *"marco por ejemplo
volver a llamar y ese después se me desaparece"*).

Purpose: el hold existe desde 2026-07-22 (nota #151) pero nació antes que el
dialer de Hoy (#179, 2026-08-10) y su disparo depende de una heurística que
RE-DERIVA si el lead "todavía es accionable" en vez de saber si la disposición
se confirmó. Este plan lo vuelve universal por construcción: un solo helper
(`_pdHold`) y una señal determinística escrita en el único punto post-guardado
que la Phase 30 ya estableció (`_dispoAfterSaved`).

Output: `_pdHold(leadId, outcome, opts)`, `_pd.pendingSave`,
`_pdHandleDisposition` sin la heurística `stillActionable`, `_autoMarkNoAnswer`
ruteada por el helper, el banner con destino garantizado, y
`tests/dial-hold.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-dial-motor-unico/33-CONTEXT.md
@.planning/phases/33-dial-motor-unico/33-01-SUMMARY.md
@.planning/research/2026-08-13-requisitos-seguimiento-ignacio.md
@CLAUDE.md

Referencia canónica obligatoria — el aviso de destino y la regla "toda
disposición nueva pasa por `_dispoAfterSaved`" salen de acá, NO reimplementar:
@.planning/phases/30-gate-proximo-paso/30-03-SUMMARY.md
@.planning/phases/32-act-acciones/32-04-SUMMARY.md
</context>

<interfaces>
<!-- Contratos vigentes. Leer los rangos de <read_first> antes de editar. -->

Estado del hold (`public/app.js` ~6528, tal como quedó tras 30-03 y 33-01):
```js
holdCurrent: false,   // mientras es true, _pdRender NO expulsa la tarjeta
holdOutcome: null,    // outcome guardado, para el banner
holdMeta: null,       // { texto, vista, cuando, tono } — lo setea _dispoAnnounce (30-03 D-07)
forced: new Set(),    // 33-01
```

Punto único post-guardado (`public/app.js` ~9282) — lo llaman los 7 caminos de
disposición y es la regla establecida por 30-03:
```js
function _dispoAfterSaved(leadId, opts = {}) { /* opts = { lead, outcome, manual } */ }
```

Aviso de destino (`public/app.js` ~9504). Cuando `_pd.active` y NO hay
`forceToast`, en vez de tirar toast escribe `_pd.holdMeta`:
```js
function _dispoAnnounce(leadId, opts = {}) { ... }
function _dispoDestination(lead, now) -> { texto, vista, cuando, tono }  // bloque [30-03] DISPO-DEST
```

Post-guardado del dialer, HOY (`public/app.js` ~7341) — lo que este plan
reescribe:
```js
window._pdHandleDisposition = async function(leadId, selectEl) {
  const modalOpening = ['callback_later','scheduled_with_admin','answered_not_interested','answered_interested'].includes(outcome);
  ...
  const _afterSaved = () => {
    if (_pd.autopilot) { _pdAdvance(); return; }
    if (_pd.active && _pd.queue[_pd.currentIdx] === leadId) { _pd.holdCurrent = true; _pd.holdOutcome = outcome; _pdRender(); }
  };
  // rama modalOpening: espera a que cierren los 4 modales y decide con
  //   const stillActionable = !['descartado','agendado'].includes(lead.estado)
  //     && (!lead.callbackAt || new Date(lead.callbackAt).getTime() <= Date.now());
  //   if (!stillActionable) _afterSaved();
}
```

Auto-marca (`public/app.js` ~9584), que hoy replica el hold a mano:
```js
if (_pd.active && _pd.queue[_pd.currentIdx] === leadId) {
  if (_pd.autopilot) { _pdAdvance(); }
  else { _pd.holdCurrent = true; _pd.holdOutcome = 'No atendió (auto)'; _pdRender(); }
}
```

Banner del hold en `_pdRender` (~6981): `_holdDestLine` + `_holdBanner`, ya
integrados con `_pd.holdMeta` por 30-03 (D-06 ya está cubierto ahí; este plan
solo garantiza que `holdMeta` esté seteado en todos los caminos).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: _pdHold como único camino + _pd.pendingSave como señal determinística</name>

  <read_first>
    - `public/app.js` líneas 6515–6545 (declaración de `_pd`, ya con `forced` de 33-01)
    - `public/app.js` líneas 6815–6870 (`_pdAdvance` y los resets de estado)
    - `public/app.js` líneas 7460–7480 (`_pdBack` y sus resets)
    - `public/app.js` líneas 6975–6996 (`_holdDestLine` / `_holdBanner` en `_pdRender`)
    - `public/app.js` líneas 9278–9302 (`_dispoAfterSaved`)
    - `public/app.js` líneas 9493–9530 (`_dispoAnnounce`, incluido el early-return de `forceToast` de 32-03)
    - `.planning/phases/30-gate-proximo-paso/30-03-SUMMARY.md` (la tabla de los 8 textos de destino y la regla "toda disposición pasa por `_dispoAfterSaved`")
  </read_first>

  <action>
    Trabajar SOLO sobre `public/app.js`.

    1. **`_pd.pendingSave`**: agregar `pendingSave: null,` a la declaración de
       `_pd`. Shape: `{ leadId, outcome, at }`. Comentario explicando qué es:
       la señal de que una disposición se GUARDÓ de verdad (la escribe el
       único punto post-guardado), en reemplazo de re-derivar el estado del
       lead para adivinarlo.

    2. **`_dispoAfterSaved` la escribe**: al principio de la función, si
       `_pd.active`, setear
       `_pd.pendingSave = { leadId, outcome: opts.outcome || '', at: Date.now() }`.
       Nada más: NO llamar `_pdHold` desde acá. Motivo (documentarlo en un
       comentario): `_dispoAfterSaved` corre en el momento del guardado, que
       puede ser con un modal todavía abierto — avanzar ahí (autopiloto)
       dejaría el modal huérfano sobre la tarjeta siguiente. Quien sabe cuándo
       el flujo terminó es `_pdHandleDisposition`, y es quien consume.

    3. **`function _pdHold(leadId, outcome, opts = {})`** — declararla
       inmediatamente después de `_pdAdvance`:
       - `if (!_pd.active || _pd.queue[_pd.currentIdx] !== leadId) return false;`
       - `if (_pd.autopilot && opts.autoAdvance !== false) { _pdAdvance(); return true; }`
         (D-05: con autopiloto encendido se mantiene el avance automático — es
         su propósito explícito, no se toca).
       - `_pd.holdCurrent = true; _pd.holdOutcome = outcome || null;`
       - Si `!_pd.holdMeta`: intentar completarlo con
         `const l = opts.lead || _callsLeadsById.get(leadId); if (l) _pd.holdMeta = _dispoDestination(l, new Date());`
         envuelto en `try/catch` (nunca romper el render por el banner). Esto
         cubre los caminos que avisaron con `forceToast` (32-03/32-04) y por
         eso saltearon la rama que escribe `holdMeta`.
       - `_pdRender(); return true;`
       - NO exponerla en `window` (es interna del bloque del dialer).

    4. **Resets**: `_pd.pendingSave = null` en los MISMOS 3 lugares donde ya se
       resetean `holdCurrent`/`holdOutcome`/`holdMeta`: `_pdStart`,
       `_pdAdvance` y `_pdBack`. Sin esto, un pendingSave viejo podría
       holdear una tarjeta que nadie marcó.

    5. **`_autoMarkNoAnswer`** (~9584): reemplazar el bloque de 4 líneas que
       replica el hold a mano por
       `_pdHold(leadId, 'No atendió (auto)', { lead: data.lead });`.
       Comportamiento idéntico (el helper ya contempla autopiloto y el guard
       de tarjeta actual), una sola definición del hold.
  </action>

  <verify>
    <automated>node --check public/app.js && npx vitest run tests/gate-destination.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "function _pdHold" public/app.js` devuelve `1`.
    - `grep -c "_pd.pendingSave = null" public/app.js` devuelve `3` (los 3 resets).
    - El cuerpo de `_dispoAfterSaved` contiene `_pd.pendingSave = {` y NO contiene `_pdHold(`.
    - El cuerpo de `_pdHold` contiene `_pd.autopilot` y `_dispoDestination(`.
    - El cuerpo de `_autoMarkNoAnswer` contiene `_pdHold(` y NO contiene `_pd.holdCurrent = true`.
    - `grep -c "_pd.holdCurrent = true" public/app.js` devuelve `1` (solo dentro de `_pdHold`).
    - `npx vitest run tests/gate-destination.test.js` pasa sin editar el archivo (verifica por string match el literal `if (_pd.active) {` de `_dispoAnnounce`, que este plan NO toca).
  </acceptance_criteria>

  <done>
    Existe un único camino al hold, con una señal determinística de guardado, y
    la auto-marca ya lo usa. Los caminos con modal siguen usando la heurística
    vieja hasta la Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: _pdHandleDisposition universal — modales y las 3 colas de Hoy</name>

  <read_first>
    - `public/app.js` líneas 7335–7400 (`_pdHandleDisposition` completa, con la rama `modalOpening` y `stillActionable`)
    - `public/app.js` líneas 6660–6740 (`_pdBuildQueueHoy` y las ramas de modo de `_pdStart`)
    - `public/app.js` líneas 6841–6865 (guard de expulsión de `_pdRender`, con la excepción de `interesado` que dejó la Phase 30 y el `forced` de 33-01)
    - `public/app.js` líneas 11660–11690, 11835–11850, 11995–12005, 12145–12160, 12415–12455 (los call sites de `_dispoAfterSaved` en los modales: próximo paso, callback, objeción, agenda y su fallback)
  </read_first>

  <action>
    Reescribir la decisión post-guardado de `window._pdHandleDisposition`
    (`public/app.js` ~7341) usando `_pd.pendingSave`:

    1. Al entrar, capturar `const _startedAt = Date.now();` y limpiar
       `_pd.pendingSave = null` ANTES de `await window._handleCallDisposition(...)`
       — así lo que quede después es necesariamente de ESTE flujo.

    2. Definir el consumo:
       `const _consumeSaved = () => { const ps = _pd.pendingSave; if (!ps || ps.leadId !== leadId || ps.at < _startedAt) return null; _pd.pendingSave = null; return ps; };`

    3. Reemplazar `_afterSaved` por:
       `const _afterSaved = (ps) => _pdHold(leadId, (ps && ps.outcome) || outcome, {});`
       (el helper ya decide autopiloto vs. hold y ya valida la tarjeta actual).

    4. **Rama de outcomes directos** (no abren modal): en vez del `setTimeout`
       ciego de 600ms, esperar a que la señal aparezca con un pequeño polling
       acotado (cada 200ms, techo 6s): si aparece → `_afterSaved(ps)`; si se
       agota → NO holdear (el POST falló o el handler no confirmó; el lead
       sigue siendo la tarjeta actual y accionable, que es el comportamiento
       correcto ante un error). Nunca dejar el flujo colgado sin salida.

    5. **Rama `modalOpening`**: conservar la espera a que los 4 modales
       (`call-callback-modal`, `call-schedule-modal`, `call-objection-modal`,
       `call-next-modal`) queden `hidden`, y DESPUÉS:
       - `const ps = _consumeSaved();`
       - `if (ps) _afterSaved(ps);` → hubo guardado confirmado.
       - `else` → el user canceló: no holdear, no avanzar, no tocar `_pd`.
       - **ELIMINAR por completo** la heurística `stillActionable` (la
         constante y su `if`). Documentar en un comentario por qué se fue:
         re-derivaba "¿el lead sigue accionable?" desde el cache optimista para
         inferir si se había guardado — daba falsos negativos cuando la
         respuesta del server no traía `lead`, y obligaba a que cada
         resultado nuevo (Phase 34 en adelante) dejara al lead en estado
         terminal o con fecha futura para que el hold funcionara. La señal
         ahora la da el guardado mismo.
       - Conservar el caso `if (!lead) { _pdAdvance(); return; }` (lead borrado
         durante el flujo).

    6. **Cobertura de las 3 colas de Hoy (D-04)**: verificar y dejar asentado
       en comentario que ni `_pdHold` ni el guard de `_pdRender` miran
       `_pd.mode` ni `_pd.hoyFilter` — el hold es idéntico en `'calls'` y en
       las 3 variantes de Hoy. Si al leer el código aparece alguna rama que SÍ
       branchea por modo en el camino del hold, eliminarla (es exactamente el
       gap que D-04 manda cerrar). No agregar branching nuevo por modo.

    7. **FUERA DE ALCANCE, no tocar**: `window._actDiscard` sigue llamando
       `_pdAdvance()` y NO debe holdear. Es una decisión explícita de 32-04
       ("acá no se marcó ninguna disposición de llamada") y D-04 enumera
       exactamente qué se extiende: las colas y los outcomes que abren modal.
       Dejar un comentario de una línea en `_actDiscard` que apunte a esta
       decisión, para que la próxima fase no lo lea como un olvido.
  </action>

  <verify>
    <automated>node --check public/app.js && npx vitest run tests/gate-destination.test.js tests/act-ui-discard-material.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "stillActionable" public/app.js` devuelve `0`.
    - El cuerpo de `window._pdHandleDisposition` contiene `_pd.pendingSave = null` y `_pdHold(`.
    - El cuerpo de `window._pdHandleDisposition` NO contiene `_pd.holdCurrent` (el hold sale solo del helper).
    - El cuerpo de `window._pdHandleDisposition` sigue conteniendo los 4 ids de modal: `call-callback-modal`, `call-schedule-modal`, `call-objection-modal`, `call-next-modal`.
    - El cuerpo de `_pdHold` NO contiene `_pd.mode` ni `_pd.hoyFilter`.
    - El cuerpo de `_pdRender` no contiene `_pd.mode ===` dentro del bloque del guard de expulsión (el `_pd.mode` del texto de progreso, más abajo, no cuenta).
    - El cuerpo de `window._actDiscard` sigue conteniendo `_pdAdvance()` y NO contiene `_pdHold(`.
    - `npx vitest run tests/gate-destination.test.js tests/act-ui-discard-material.test.js` pasa sin editar esos archivos.
  </acceptance_criteria>

  <done>
    Todo resultado guardado dentro del dialer —directo o con modal, en
    cualquiera de las 4 colas— deja la tarjeta con el banner; cancelar no deja
    nada; el autopiloto sigue avanzando solo.
  </done>
</task>

<task type="auto">
  <name>Task 3: Suite de DIAL-02 + cache-buster</name>

  <read_first>
    - `tests/act-ui-discard-material.test.js` líneas 1–60 (helpers y molde de la suite)
    - `tests/gate-destination.test.js` (cómo 30-03 evalúa el bloque puro con `new Function` y con reloj fijo; y qué literales de `_dispoAnnounce` pinea, para no romperlos)
    - `public/index.html` (única línea con `app.js?v=`)
  </read_first>

  <action>
    Crear `tests/dial-hold.test.js` (mínimo 16 tests), mismo molde que las
    suites de la Phase 32: aserciones de fuente + evaluación aislada donde
    tenga sentido. Cobertura obligatoria:

    - **Un solo hold**: `_pd.holdCurrent = true` aparece exactamente 1 vez en
      todo `public/app.js`, y está dentro del cuerpo de `_pdHold`.
    - **Autopiloto (D-05)**: el cuerpo de `_pdHold` avanza cuando
      `_pd.autopilot` es true — extraer el cuerpo y assertar la presencia del
      branch `if (_pd.autopilot` seguido de `_pdAdvance()`.
    - **Guard de tarjeta actual**: `_pdHold` chequea
      `_pd.queue[_pd.currentIdx] !== leadId` y devuelve sin tocar estado.
    - **Sin branching por modo (D-04)**: ni `_pdHold` ni el guard de expulsión
      de `_pdRender` mencionan `_pd.mode`/`_pd.hoyFilter`.
    - **pendingSave**: `_dispoAfterSaved` lo escribe; `_pdHandleDisposition` lo
      limpia al entrar y lo consume validando `leadId` y `at`; los 3 resets
      existen.
    - **Heurística eliminada**: `stillActionable` no aparece en el archivo.
    - **Los 4 modales siguen esperados** por nombre de id.
    - **Cancelar no holdea**: el cuerpo de la rama de modales tiene una rama
      explícita para `!ps` que no llama `_pdHold` ni `_pdAdvance`.
    - **Banner (D-06)**: `_pdRender` sigue pintando `holdMeta` (la línea
      `_holdDestLine` con `hm.vista`/`hm.cuando`) y `_pdHold` completa
      `holdMeta` vía `_dispoDestination` cuando está vacío. Test de que el
      literal `✓ Resultado guardado` sigue existiendo exactamente 1 vez.
    - **Anti-regresión 32-04**: `_actDiscard` conserva `_pdAdvance()` y no
      holdea (la decisión de 32-04 queda protegida por test, no por memoria).
    - **Cache-buster**: por forma + monotonía contra el valor previo, nunca
      pineando el literal nuevo.

    Después: bumpear el cache-buster de `app.js` en `public/index.html`
    leyendo primero el valor REAL en disco (lo dejó 33-01; puede haberse
    movido). No tocar `style.css`.
  </action>

  <verify>
    <automated>npx vitest run tests/dial-hold.test.js tests/dial-start-at.test.js tests/gate-destination.test.js tests/act-ui-discard-material.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/dial-hold.test.js` pasa con al menos 16 tests y 0 fallos.
    - Las 3 suites previas del comando pasan sin editarlas (`git diff --stat` de esos 3 archivos vacío).
    - `grep -c "app.js?v=" public/index.html` devuelve `1` y el valor es estrictamente mayor (string compare) que el que dejó 33-01.
    - `git diff --stat public/style.css` no imprime nada.
    - `npm test` completo: 0 fallos nuevos respecto del baseline de 33-01.
    - `git diff --stat package.json package-lock.json` vacío.
  </acceptance_criteria>

  <done>
    DIAL-02 queda protegido por tests permanentes (incluida la decisión de
    32-04 sobre el descarte), el cache-buster está bumpeado y la suite completa
    sigue verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| respuesta del server → banner del dialer | `_dispoDestination` recibe el `lead` del body de la respuesta y su salida (`vista`, `cuando`) se pinta con `innerHTML` |
| flujo de disposición → estado del dialer | `_pd.pendingSave` es estado en memoria que decide si la tarjeta se retiene o avanza |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Tampering (XSS) | `_holdDestLine` en `_pdRender` pinta `hm.vista` / `hm.cuando` con `innerHTML` | mitigate | Ya vienen escapados con `escHtml` desde 30-03; este plan no cambia esas líneas. Acceptance criterion de la Task 3: el literal de `escHtml(hm.vista)` sigue presente |
| T-33-05 | Spoofing | Un `pendingSave` de otro lead podría holdear la tarjeta equivocada | mitigate | `_consumeSaved` valida `ps.leadId === leadId` **y** `ps.at >= _startedAt`, y `_pdHold` vuelve a validar `_pd.queue[_pd.currentIdx] === leadId` antes de tocar nada. Test dedicado |
| T-33-06 | Denial of Service | El polling de la rama directa podría quedar girando sin fin | mitigate | Techo duro: 200ms × 30 vueltas = 6s, y la rama de modales conserva su techo de 60s ya existente. Ambas tienen salida explícita que no holdea |
| T-33-07 | Repudiation | Un hold sin guardado real haría creer que el resultado quedó marcado | mitigate | El hold ahora depende de que `_dispoAfterSaved` haya corrido (o sea: el POST devolvió OK). Si el POST falla, no hay señal y no hay banner — el gate de disposición obligatoria de la Phase 20 sigue activo y el lead queda pendiente, que es el comportamiento correcto |
| T-33-SC | Tampering | instalación de paquetes npm | mitigate | Este plan NO instala paquetes. `git diff --stat package.json package-lock.json` vacío al cerrar |
</threat_model>

<verification>
1. `node --check public/app.js`.
2. `npx vitest run tests/dial-hold.test.js` — suite nueva verde.
3. `npx vitest run tests/gate-destination.test.js tests/act-ui-discard-material.test.js tests/dial-start-at.test.js` — sin editarlas.
4. `npm test` completo contra el baseline de 33-01.
5. `git diff --stat package.json package-lock.json` y `git diff --stat public/style.css` vacíos.
6. **Verificación por mutación**: romper temporalmente el guard
   `ps.leadId !== leadId` de `_consumeSaved` y confirmar que la suite nueva se
   pone en rojo en tests específicos y contables. Restaurar con
   `git checkout -- public/app.js` y confirmar `git diff public/app.js` vacío.
</verification>

<success_criteria>
- Marcar cualquier resultado dentro del dialer (directo, con modal, o la auto-marca) deja la tarjeta con el banner "✓ Resultado guardado · &lt;resultado&gt;" y la línea de destino.
- Ese comportamiento es idéntico en `mode:'calls'` y en las 3 colas de Hoy — no hay ninguna condición por modo en el camino del hold.
- Cancelar un modal no deja hold ni avanza.
- Con autopiloto ON el avance automático se mantiene tal cual (D-05).
- `stillActionable` ya no existe: el hold no re-deriva estado, lo recibe del único punto post-guardado.
- El descarte de 32-04 conserva su comportamiento (avanza, no holdea), ahora protegido por test.
- Cache-buster bumpeado; `style.css` intacto.
</success_criteria>

<output>
Crear `.planning/phases/33-dial-motor-unico/33-02-SUMMARY.md` al terminar.

Documentar explícitamente:
- Si al leer el código apareció (o no) una condición por modo en el camino del
  hold — la premisa de D-04 ("hoy funciona solo en el dialer de Llamadas") es
  histórica; decir con evidencia qué se encontró.
- El valor del cache-buster ANTES y DESPUÉS (leído de disco).
- El resultado de la verificación por mutación.
- Qué quedó SIN verificar en vivo: colgar una llamada real dentro del dialer de
  Hoy y confirmar que la grilla de resultado se queda; y el timing real del
  polling de la rama directa contra un backend lento.
</output>
