---
phase: 33-dial-motor-unico
plan: 04
type: execute
wave: 4
depends_on: ["33-03"]
files_modified:
  - index.js
  - public/app.js
  - public/index.html
  - tests/dial-history.test.js
autonomous: true
requirements: [DIAL-04]
must_haves:
  truths:
    - "Antes de discar y durante la llamada, el lead trabajado muestra arriba de todo qué pasó antes: última disposición y cuándo, quién la marcó, la última nota, el compromiso pendiente si hay, y cuántos intentos lleva"
    - "Llamar a un lead que ya trabajó otra vendedora no se ve ni se siente como una llamada en frío"
    - "Un lead que nunca fue trabajado NO muestra el bloque: cero cartel vacío ocupando la pantalla más importante"
    - "El bloque es un resumen para leer en 3 segundos: no muestra la transcripción expandida (esa sigue viviendo en el desplegable del histórico)"
    - "El nombre de quien marcó la disposición se resuelve de verdad, no se muestra un id crudo"
  artifacts:
    - path: "public/app.js"
      provides: "Bloque puro [33-04] HISTORY-PURE con _leadHistoryBrief + _leadHistoryHTML, cableado en las 3 superficies de llamada"
      contains: "[33-04] HISTORY-PURE"
    - path: "index.js"
      provides: "userNames (userId -> nombre) en la respuesta de GET /api/setters/leads/sin-wsp, solo para los ids referenciados"
      contains: "userNames"
    - path: "tests/dial-history.test.js"
      provides: "Suite de DIAL-04: bloque puro con reloj fijo, D-12 (sin historial no hay bloque), D-13 (sin transcript), orden D-11, y el mapa de nombres del backend"
  key_links:
    - from: "_pdRender"
      to: "_leadHistoryHTML"
      via: "bloque al tope de la tarjeta del Power Dialer, antes del ángulo/brief"
      pattern: "_leadHistoryHTML\\("
    - from: "window._leadFileHtml"
      to: "_leadHistoryHTML"
      via: "primera fila de la ficha del panel de llamada activa"
      pattern: "_leadHistoryHTML\\("
    - from: "_callsRenderExpandedPanel"
      to: "_leadHistoryHTML"
      via: "arriba del panel expandido (reusado por la ficha de Hoy)"
      pattern: "_leadHistoryHTML\\("
    - from: "GET /api/setters/leads/sin-wsp"
      to: "window.__userNames"
      via: "loadCallsView/loadHoyView guardan el mapa que resuelve callLog[].by"
      pattern: "__userNames"
---

<objective>
La ficha del lead pone al frente el historial de las vendedoras: quién lo
trabajó, qué anotó y en qué quedó — antes de que atiendan (DIAL-04, R1 y R6 del
relevamiento).

Purpose: el user dijo textualmente que *"las fichas están mal, son poco
prácticas o poco lindas para llamar al lead directamente"* y, sobre todo, que el
historial de Paula, Judith, Brenda, Teresa y Melissa **es un activo**: llamar a
un lead trabajado no es una llamada en frío y el sistema tiene que dejarlo claro
ANTES de discar, no enterrado en un panel. Hoy esa información existe
(`callLog`, `notes`, `commitment`) pero está repartida en bloques de abajo, en
formato timeline, y el nombre de quien marcó ni siquiera se puede resolver
desde el frontend (`callLog[].by` es un id de usuario crudo).

Output: el mapa `userNames` en la respuesta de la cola de llamadas, el bloque
puro `[33-04] HISTORY-PURE` (`_leadHistoryBrief` + `_leadHistoryHTML`) cableado
en las 3 superficies de llamada, y `tests/dial-history.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-dial-motor-unico/33-CONTEXT.md
@.planning/phases/33-dial-motor-unico/33-03-SUMMARY.md
@.planning/research/2026-08-13-requisitos-seguimiento-ignacio.md
@CLAUDE.md
@BRAND-SCM.md

Referencias canónicas — el compromiso que se muestra en el bloque sale de acá,
con su regla dura de estado ALMACENADO vs DERIVADO:
@.planning/phases/31-comm-compromisos/31-04-SUMMARY.md
@.planning/phases/30-gate-proximo-paso/30-03-SUMMARY.md
</context>

<interfaces>
<!-- Contratos vigentes. Leer los rangos de <read_first> antes de editar. -->

Backend — la cola de llamadas (`index.js` ~8863). Ya construye un mapa de
usuarios para la atribución; el mapa de NOMBRES se agrega al lado:
```js
app.get('/api/setters/leads/sin-wsp', requireAuth, (req, res) => {
  ...
  const _swUserMap = _buildUserSetterMap();   // ~8928
  for (const l of leads) { l.calledByOwner = ...; l.manualCallbackByOwner = ...; }
  res.json({ leads });                        // ~8948
});

function _buildUserSetterMap() {              // ~7897
  const m = {};
  try { (loadAuthData().users || []).forEach((u) => { if (u.id && u.setterId) m[u.id] = u.setterId; }); } catch {}
  return m;
}
```

Forma de una entry del callLog (`index.js` ~12417): `{ ts, outcome, by, notes }`
más campos opcionales (`duration`, `channel`, `cost`, `transcript`, …).
**`by` es el id del usuario, NO un nombre** — y algunos ids pertenecen a
usuarios ya borrados (nota #149 de CLAUDE.md).
Las notas (`lead.notes[]`) sí traen nombre: `{ text, by, date }` con `by` =
`req.auth.user.name` (nota #26).

Frontend — las 3 superficies donde va el bloque:
```js
function _pdRender()                                   // ~6841, tarjeta del Power Dialer
window._leadFileHtml = function(lead, opts = {})       // ~10425, ficha del panel de llamada ACTIVA
function _callsRenderExpandedPanel(l)                  // ~8300, panel expandido (Llamadas + ficha de Hoy)
function _renderCallHistory(lead)                      // ~10529, timeline del panel de llamada (NO tocar)
```

Helpers que el bloque puede usar (ya existen):
```js
function _commitmentLabel(tipo)                        // bloque [31-03] COMMITMENT-PURE
function _commitmentEffectiveEstado(c, nowMs)          // idem — derivado, NO usar para decidir abierto/cerrado
function callOutcomeLabel(outcome)                     // etiqueta de outcome con emoji al frente
function escHtml(s)
```

Regla dura heredada de 31-04 (Rule 1, documentada en su SUMMARY): para saber si
un compromiso sigue ABIERTO hay que mirar el estado **ALMACENADO**
(`lead.commitment.estado === 'pendiente'`), nunca el derivado — el derivado
devuelve `'vencido'` para dos situaciones de negocio distintas.

Anclas de inserción exactas:
- `_pdRender`: `main.innerHTML` arranca con `${_holdBanner}` y sigue con el
  `<!-- Bloque 1: Header del lead + acciones primarias -->`. El bloque nuevo va
  **entre el cierre del Bloque 1 y el `<!-- Bloque 1.5: Ángulo de apertura ... -->`**.
- `window._leadFileHtml`: el array `rows` arranca con la Pre-call. El bloque
  nuevo se agrega **primero de todo** en `rows`.
- `_callsRenderExpandedPanel`: en el `return`, la primera línea del
  `.call-detail-section` izquierdo es `${_expChips.length ? ... }` y la
  siguiente es `${_briefBlock}`. El bloque nuevo va **entre esas dos**.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backend — mapa de nombres para resolver quién marcó cada llamada</name>

  <read_first>
    - `index.js` líneas 7890–7935 (`_buildUserSetterMap` y `_callSetterId`, con su historia de usuarios borrados)
    - `index.js` líneas 8863–8949 (`GET /api/setters/leads/sin-wsp` completo: filtros, RBAC por `getEffectiveAuth` y `_visibleSetterIds`, y el `res.json`)
    - `index.js` líneas 12410–12430 (la construcción del `logEntry` de `call-disposition`, para confirmar que `by` es un id y que NO hay `byName`)
    - `tests/pipeline-attribution.test.js` (molde de un test que monta el server real contra `DATA_DIR` temporal y pega a `/leads/sin-wsp`)
  </read_first>

  <action>
    Un solo cambio en `index.js`, aditivo y acotado.

    1. Declarar `function _buildUserNameMap(ids)` inmediatamente después de
       `_buildUserSetterMap`:
       - Recibe un `Set` de ids de usuario.
       - Devuelve `{ [userId]: nombre }` leyendo la MISMA fuente que
         `_buildUserSetterMap` (`loadAuthData().users`), con el mismo
         `try/catch` defensivo.
       - **Solo incluye los ids que se le piden** (nada de volcar el padrón
         entero).
       - El nombre es `u.name` únicamente. **Nunca el email**: es un dato de
         contacto de un empleado y el bloque de historial no lo necesita. Si
         `u.name` está vacío, el id no entra al mapa (el frontend degrada solo).

    2. En `GET /api/setters/leads/sin-wsp`, dentro del `for (const l of leads)`
       que ya existe (~8929), juntar en un `Set` los `by` de cada entry de
       `l.callLog`. Después del loop, construir
       `const userNames = _buildUserNameMap(_swNameIds);` y devolverlo:
       `res.json({ leads, userNames });`
       — el shape existente NO cambia (`leads` sigue igual), solo se suma una
       clave. Comentario explicando para qué es: `callLog[].by` es un id y la
       ficha del lead necesita decir QUIÉN marcó la última disposición (R1: el
       historial de las vendedoras es un activo). Los ids de usuarios borrados
       simplemente no aparecen en el mapa.

    3. **No tocar** `_callSetterId`, ni la atribución, ni ninguna métrica: este
       plan agrega un dato de presentación, no cambia cómo se cuenta nada.

    4. Sumar los tests al archivo nuevo `tests/dial-history.test.js` (se crea
       en esta task y se completa en la Task 3), con el molde de
       `tests/pipeline-attribution.test.js`: fixture con `auth.json`
       pre-poblado ANTES de `import("../index.js")`, un lead con `callLog` que
       tiene un `by` de usuario existente y otro de usuario inexistente, y
       aserciones de que `userNames` trae el primero, no trae el segundo, y no
       contiene ningún `@` (prueba de que no viajan emails).
  </action>

  <verify>
    <automated>node --check index.js && npx vitest run tests/dial-history.test.js tests/pipeline-attribution.test.js tests/security-rbac.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` sale con código 0.
    - `grep -c "function _buildUserNameMap" index.js` devuelve `1`.
    - `grep -c "res.json({ leads, userNames })" index.js` devuelve `1`.
    - El cuerpo de `_buildUserNameMap` NO contiene `u.email` ni `email`.
    - `npx vitest run tests/pipeline-attribution.test.js tests/security-rbac.test.js` pasa sin editar esos archivos (el shape de la respuesta se extendió, no se cambió).
    - Los tests nuevos de esta task verifican: id existente presente en `userNames`, id de usuario borrado ausente, y `JSON.stringify(userNames)` sin `@`.
  </acceptance_criteria>

  <done>
    El frontend puede resolver el nombre de quien marcó cada llamada, sin
    exponer emails ni cambiar ninguna métrica.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bloque [33-04] HISTORY-PURE + cableado en las 3 superficies de llamada</name>

  <read_first>
    - `public/app.js` líneas 10526–10600 (`_renderCallHistory`: su `outcomeMap` y su `fmtAgo` — el bloque nuevo tiene que hablar el MISMO idioma)
    - `public/app.js` líneas 6996–7040 (Bloque 1 del `main.innerHTML` de `_pdRender` y el arranque del Bloque 1.5)
    - `public/app.js` líneas 10425–10460 (arranque de `window._leadFileHtml` y su array `rows`)
    - `public/app.js` líneas 8366–8400 y 8580–8600 (`_expChips` y el `return` de `_callsRenderExpandedPanel`)
    - `public/app.js` líneas 5928–5960 y 6280–6310 (dónde `loadHoyView` y `loadCallsView` parsean la respuesta, para guardar `window.__userNames`)
    - `.planning/phases/31-comm-compromisos/31-04-SUMMARY.md` (regla del estado ALMACENADO vs DERIVADO del compromiso)
    - `BRAND-SCM.md` (paleta, sin emojis, calma operativa)
  </read_first>

  <action>
    Trabajar SOLO sobre `public/app.js`.

    1. **Guardar el mapa de nombres**: en `loadCallsView` y en `loadHoyView`,
       donde se parsea la respuesta de `/leads/sin-wsp`, hacer
       `if (d.userNames) window.__userNames = { ...(window.__userNames || {}), ...d.userNames };`
       (merge, no reemplazo: las dos vistas traen poblaciones distintas).

    2. **Bloque puro `[33-04] HISTORY-PURE`**, declarado inmediatamente después
       del bloque `[31-03] COMMITMENT-PURE: FIN` (mismo criterio que 32-03 usó
       para `ACT-PURE`), con marcadores de inicio y fin como los bloques
       anteriores para que los tests lo extraigan y lo evalúen con
       `new Function`. Contiene:
       - `const HISTORY_OUTCOME_LABELS = { ... }` — el MISMO diccionario que
         usa `_renderCallHistory` (`answered_interested: 'Interesado'`, etc.),
         copiado literal. Un test de anti-deriva lo ata a ese `outcomeMap`
         (mismo patrón que 31-04 usó con los títulos de Hoy).
       - `function _historyWhenLabel(ts, nowMs)` — `'hoy'` / `'ayer'` /
         `'hace Nd'`, con la misma aritmética que el `fmtAgo` de
         `_renderCallHistory`, pero recibiendo el reloj por parámetro (los
         tests usan un reloj FIJO; nunca el reloj real de la corrida).
       - `function _leadHistoryBrief(lead, nowMs, opts = {})` — PURA: sin DOM,
         sin red, sin `localStorage`, sin `Date.now()` interno. `opts` acepta
         resolvers opcionales `{ userName, commitmentLabel }` que por defecto
         devuelven `''` (así el bloque queda autocontenido para `new Function`
         y la app le pasa los reales). Devuelve, en el orden que pide D-11:
         ```
         {
           has,                                  // false si nunca fue trabajado (D-12)
           last:   { outcome, label, when, by } | null,   // última disposición, cuándo, quién
           note:   { text, by, when } | null,             // última nota escrita
           commitment: { label, when, vencido } | null,   // SOLO si estado ALMACENADO === 'pendiente'
           attempts                                       // cantidad de intentos previos
         }
         ```
         Reglas:
         · `has` es `true` si hay al menos una entry de `callLog`, o al menos
           una nota, o un compromiso pendiente. Si no, `has:false` y el resto
           en `null`/`0` (D-12: sin historial NO hay bloque).
         · `last` sale de la ÚLTIMA entry de `callLog` (la de mayor `ts`, no
           asumir orden), con `label` de `HISTORY_OUTCOME_LABELS` y `by`
           resuelto por `opts.userName(entry.by)`; si no resuelve, `by: ''`.
         · `note` sale de la última de `lead.notes` (su `by` YA es un nombre).
         · `commitment` solo si `lead.commitment.estado === 'pendiente'`
           (estado ALMACENADO — regla dura de 31-04); `vencido` se deriva
           comparando `dueAt` contra `nowMs`.
         · `attempts` = `lead.callAttempts` si es un número, si no
           `callLog.length`.
         · Truncar textos largos (nota a 140 caracteres) DENTRO de la función
           pura, para que el HTML no tenga que decidirlo.
         · **NUNCA leer `entry.transcript`** (D-13): el bloque es un resumen
           para 3 segundos, no un archivo. La transcripción sigue viviendo en
           el `<details>` del histórico, intacta.

    3. **`function _leadHistoryHTML(lead)`** (NO pura, usa `escHtml`),
       declarada junto al builder `_actButtonsHTML` para convivir con los otros
       builders de superficie:
       - Llama `_leadHistoryBrief(lead, Date.now(), { userName: (id) => (window.__userNames || {})[id] || '', commitmentLabel: _commitmentLabel })`.
       - Si `!brief.has` devuelve `''` (D-12).
       - Devuelve un bloque compacto de 2–3 líneas, con encabezado que deje
         claro de qué se trata (p. ej. "Ya trabajado"), en el orden de D-11.
         Tokens de la paleta (`var(--bg-app)`, `var(--border-subtle)`,
         `var(--text-secondary)`, `var(--warning)` solo para el compromiso
         vencido). **Sin emojis** (BRAND-SCM). Toda interpolación de dato del
         lead pasa por `escHtml`.
       - La forma visual exacta es discreción de Claude (el CONTEXT lo dice),
         pero tiene que ser legible en 3 segundos y no empujar el botón
         "Llamar" fuera de la pantalla.

    4. **Cableado en las 3 superficies** (usar las anclas exactas del bloque
       `<interfaces>`):
       - `_pdRender`: entre el cierre del Bloque 1 (header + acciones) y el
         comentario del Bloque 1.5 (ángulo sugerido). Va arriba de todo el
         contenido, pero DEBAJO del header — el número y el botón "Llamar" no
         se corren de lugar. Documentar esa decisión en un comentario.
       - `window._leadFileHtml`: `rows.push(...)` como PRIMER elemento del
         array, antes de la Pre-call. Este es el panel de la llamada ACTIVA:
         literalmente "antes de que atiendan".
       - `_callsRenderExpandedPanel`: entre la línea de `_expChips` y
         `${_briefBlock}`. Cubre la lista de Llamadas y —por reuso— la ficha
         de Hoy (`window._hoyOpenFicha`).

    5. **No tocar** `_renderCallHistory` ni el `<details>` con la
       transcripción del histórico del Power Dialer.
  </action>

  <verify>
    <automated>node --check public/app.js && npx vitest run tests/commitment-hoy.test.js tests/act-ui-whatsapp.test.js tests/act-ui-discard-material.test.js tests/dial-sync.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "\[33-04\] HISTORY-PURE" public/app.js` devuelve `2` (marcador de inicio y de fin).
    - `grep -c "function _leadHistoryBrief" public/app.js` devuelve `1` y `grep -c "function _leadHistoryHTML" public/app.js` devuelve `1`.
    - `grep -c "_leadHistoryHTML(" public/app.js` devuelve `4` (declaración + 3 superficies).
    - El cuerpo de `_leadHistoryBrief` NO contiene `document`, `fetch`, `localStorage`, `Date.now(` ni `transcript`.
    - El cuerpo de `_leadHistoryBrief` contiene `=== 'pendiente'` (estado almacenado, regla de 31-04).
    - El cuerpo de `_leadHistoryHTML` contiene `escHtml(` y NO contiene ningún emoji.
    - El cuerpo de `_pdRender` contiene `_leadHistoryHTML(lead)` y la posición de ese literal es POSTERIOR a la del literal `Bloque 1: Header del lead` y ANTERIOR a la de `Bloque 1.5`.
    - El cuerpo de `window._leadFileHtml` contiene `_leadHistoryHTML(lead)`.
    - El cuerpo de `_callsRenderExpandedPanel` contiene `_leadHistoryHTML(l)` y su posición es ANTERIOR a la del literal `${_briefBlock}`.
    - `grep -c "window.__userNames" public/app.js` devuelve `>= 3`.
    - Las 4 suites del comando de verify pasan sin editarlas.
  </acceptance_criteria>

  <done>
    El historial de las vendedoras aparece al frente en las 3 superficies desde
    donde se llama, sale de un solo builder, y un lead virgen no muestra nada.
  </done>
</task>

<task type="auto">
  <name>Task 3: Suite de DIAL-04 + cache-buster</name>

  <read_first>
    - `tests/gate-destination.test.js` (cómo 30-03 evalúa un bloque `-PURE` con `new Function` y reloj fijo, y por qué no se hardcodean strings de `Intl`)
    - `tests/commitment-hoy.test.js` (test anti-deriva entre dos lugares que comparten literales)
    - `tests/dial-history.test.js` (lo que dejó la Task 1: los tests del backend)
    - `public/index.html` (única línea con `app.js?v=`)
  </read_first>

  <action>
    Completar `tests/dial-history.test.js` hasta un mínimo de 22 tests (los del
    backend de la Task 1 cuentan), con el molde de las suites de las fases
    30–32. Cobertura obligatoria:

    - **Bloque puro con reloj FIJO** (nunca el reloj real de la corrida):
      · lead sin `callLog`, sin notas y sin compromiso → `has:false` (D-12).
      · lead con una sola entry de `callLog` → `has:true`, `last.label`
        correcto, `attempts` correcto.
      · `last` toma la entry de mayor `ts` aunque el array venga desordenado.
      · `by` se resuelve con el resolver inyectado; con un id que no resuelve
        queda `''` y el bloque igual se arma (usuario borrado, nota #149).
      · última nota tomada correctamente y truncada a 140 caracteres.
      · compromiso con estado ALMACENADO `'pendiente'` → aparece; con
        `'cumplido'`/`'vencido'`/`'incumplido'` (cerrado explícito) → NO
        aparece. Es la regla dura de 31-04, protegida por test.
      · compromiso pendiente con `dueAt` pasado → `vencido:true`.
      · `_historyWhenLabel` para hoy / ayer / hace N días, con reloj fijo,
        incluido un borde de día (23:30).
    - **D-13**: el cuerpo de `_leadHistoryBrief` no menciona `transcript`, y
      `_leadHistoryHTML` no emite `<details`.
    - **Anti-deriva**: `HISTORY_OUTCOME_LABELS` cubre las mismas claves con los
      mismos valores que el `outcomeMap` de `_renderCallHistory` (extraer los
      dos del archivo y comparar). Si divergen, el test falla.
    - **Cableado**: las 3 superficies, con las aserciones de ORDEN (índice del
      literal) de los acceptance criteria de la Task 2.
    - **Sin emojis** en el HTML del bloque: regex sobre el cuerpo de
      `_leadHistoryHTML` que rechace el rango de pictogramas.
    - **Escapado**: toda interpolación de dato del lead dentro de
      `_leadHistoryHTML` viene precedida de `escHtml(` (mismo test que 32-03
      hace sobre su overlay).
    - **Cache-buster**: por forma + monotonía contra el valor previo, nunca
      pineando el literal nuevo.

    Después: bumpear el cache-buster de `app.js` en `public/index.html` leyendo
    primero el valor REAL en disco. No tocar `style.css`.
  </action>

  <verify>
    <automated>npx vitest run tests/dial-history.test.js tests/dial-sync.test.js tests/dial-hold.test.js tests/dial-start-at.test.js tests/commitment-hoy.test.js tests/gate-destination.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/dial-history.test.js` pasa con al menos 22 tests y 0 fallos.
    - Las 5 suites previas del comando pasan sin editarlas (`git diff --stat` de esos archivos vacío).
    - `grep -c "app.js?v=" public/index.html` devuelve `1`, con valor estrictamente mayor (string compare) al que dejó 33-03.
    - `git diff --stat public/style.css` no imprime nada.
    - `npm test` completo: 0 fallos nuevos respecto del baseline de 33-03.
    - `git diff --stat package.json package-lock.json` vacío.
    - `tests/dial-history.test.js` no contiene `Date.now()` como reloj de los casos del bloque puro (todos con timestamp fijo).
  </acceptance_criteria>

  <done>
    DIAL-04 queda cubierto por tests permanentes, incluidas las dos reglas que
    más fácil se rompen sin querer (estado almacenado del compromiso, y el
    diccionario de outcomes compartido con la timeline).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `auth.json` (datos de empleados) → respuesta de la cola de llamadas | El mapa `userNames` expone nombres de usuarios del sistema a cualquier rol autenticado que pida su cola |
| datos históricos del lead (notas escritas por personas) → `innerHTML` | El bloque nuevo pinta texto libre escrito por vendedoras y outcomes con `innerHTML` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-12 | Information Disclosure | `userNames` en `GET /leads/sin-wsp` podría filtrar el padrón de usuarios (y sus emails) a un rol setter | mitigate | El mapa incluye **solo** los ids referenciados en el `callLog` de los leads que ESE usuario ya recibió (o sea, gente que trabajó sus propios leads), y **solo** el campo `name` — nunca `email` (acceptance criterion con grep + test que verifica ausencia de `@`). Los nombres de SDR ya son visibles hoy vía `GET /api/setters` |
| T-33-13 | Tampering (XSS) | `_leadHistoryHTML` interpola notas escritas por personas y nombres de usuario dentro de `innerHTML` | mitigate | Todo pasa por `escHtml()`, con test de fuente que exige el prefijo `escHtml(` en cada interpolación (mismo test que 32-03 aplica a su overlay). El truncado ocurre en la función pura, antes del HTML |
| T-33-14 | Information Disclosure | El bloque podría exponer la transcripción de una llamada en una superficie donde antes no estaba | mitigate | D-13 lo prohíbe explícitamente y hay dos criterios verificables: `_leadHistoryBrief` no menciona `transcript` y `_leadHistoryHTML` no emite `<details`. La transcripción sigue solo en el desplegable del histórico, con el mismo RBAC de siempre |
| T-33-15 | Denial of Service | Construir el mapa de nombres en cada request de la cola | accept | Es un recorrido sobre el `callLog` de leads ya cargados en memoria y una lectura de `auth.json` que ya se hacía en el mismo handler (`_buildUserSetterMap`); el padrón de usuarios es de un puñado de registros |
| T-33-SC | Tampering | instalación de paquetes npm | mitigate | Este plan NO instala paquetes. `git diff --stat package.json package-lock.json` vacío al cerrar |
</threat_model>

<verification>
1. `node --check index.js && node --check public/app.js`.
2. `npx vitest run tests/dial-history.test.js` — suite nueva verde.
3. `npx vitest run tests/pipeline-attribution.test.js tests/security-rbac.test.js tests/commitment-hoy.test.js tests/gate-destination.test.js tests/dial-sync.test.js tests/dial-hold.test.js tests/dial-start-at.test.js` — sin editar ninguna.
4. `npm test` completo contra el baseline de 33-03.
5. `git diff --stat package.json package-lock.json` y `git diff --stat public/style.css` vacíos.
6. **Verificación por mutación**: cambiar temporalmente el chequeo del
   compromiso de `=== 'pendiente'` (almacenado) al derivado y confirmar que la
   suite nueva se pone en rojo en el test específico. Restaurar con
   `git checkout -- public/app.js` y confirmar diff vacío.
</verification>

<success_criteria>
- Antes de discar (tarjeta del Power Dialer), durante la llamada (ficha del panel) y en la ficha expandida (Llamadas y Hoy), un lead ya trabajado muestra arriba: última disposición y cuándo, quién la marcó, la última nota, el compromiso pendiente si hay, y cuántos intentos lleva.
- Un lead nunca trabajado no muestra nada (D-12).
- No aparece ninguna transcripción expandida en el bloque nuevo (D-13).
- El nombre de quien marcó se resuelve de verdad; los usuarios borrados degradan sin romper nada.
- El backend no expone emails ni el padrón completo de usuarios.
- Cero cambios en `_renderCallHistory`, en las métricas y en la atribución.
- Cache-buster bumpeado; `style.css` intacto.
</success_criteria>

<output>
Crear `.planning/phases/33-dial-motor-unico/33-04-SUMMARY.md` al terminar.

Documentar explícitamente:
- La forma final del bloque (qué líneas muestra y en qué orden), para que la
  próxima fase no lo reinvente.
- Cuántos ids quedaron sin resolver contra `userNames` en el fixture de test y
  cómo degrada la UI en ese caso.
- El valor del cache-buster ANTES y DESPUÉS (leído de disco).
- El resultado de la verificación por mutación.
- Qué quedó SIN verificar en vivo: cómo se ve el bloque en un browser real
  sobre un lead con historial largo de otra vendedora, si empuja o no el botón
  "Llamar" fuera de la vista en pantallas chicas, y el contraste del estado
  "compromiso vencido" contra el fondo de la tarjeta.
- **Cierre de fase**: marcar DIAL-01..04 en `.planning/REQUIREMENTS.md`,
  la Phase 33 como Complete (4/4) en `.planning/ROADMAP.md` (las 3 tablas que
  la mencionan) y actualizar `.planning/STATE.md` — **a mano con Edit, sin
  invocar `gsd-sdk query`**: el patrón de corrupción sobre el formato custom de
  esos archivos ya está confirmado 6 veces en este milestone (24-01, 29-02,
  29-03, 29-04, 31-03, 31-04).
</output>
