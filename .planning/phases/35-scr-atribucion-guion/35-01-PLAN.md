---
phase: 35-scr-atribucion-guion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.js
  - tests/script-attribution.test.js
autonomous: true
requirements: [SCR-01, SCR-03, SCR-04]
must_haves:
  truths:
    - "Una disposición marcada desde CUALQUIER superficie (sin telnyxCallMeta) puede atribuir guion: el backend acepta scriptIdsUsed en el nivel superior del body, no solo adentro de telnyxCallMeta"
    - "Un id de guion que no existe en el banco no se persiste — la atribución vale contra call_scripts.json, no contra lo que mande el cliente"
    - "Una llamada que nadie atendió no puede quedar atribuida a un guion: el gate de outcome es el mismo que ya usa callStage"
    - "Se distingue lo atribuido por el sistema (default) de lo elegido por una persona: scriptIdsAuto en el entry, igual que callStageAuto"
    - "Si la llamada usó más de un guion se guardan todos, deduplicados y en orden (D-03)"
    - "La vista de efectividad informa cuánta de su cobertura es automática y cuánta la eligió alguien — sin eso un 0% no se puede leer"
  artifacts:
    - path: "index.js"
      provides: "SCRIPT_RELEVANT_OUTCOMES + _sanitizeScriptIds + _knownScriptIds + persistencia unificada de scriptIdsUsed/scriptIdsAuto en call-disposition + split auto/manual en script-effectiveness"
      contains: "SCRIPT_RELEVANT_OUTCOMES"
    - path: "tests/script-attribution.test.js"
      provides: "Suite HTTP: aceptación desde el body, whitelist contra el banco, gate de outcome, flag auto, unión con telnyxCallMeta, herencia en corrección, cobertura del panel"
  key_links:
    - from: "POST /api/setters/leads/:id/call-disposition"
      to: "logEntry.scriptIdsUsed"
      via: "unión body.scriptIdsUsed + telnyxCallMeta.scriptIdsUsed, saneada contra el banco de guiones"
      pattern: "_sanitizeScriptIds"
    - from: "logEntry.scriptIdsAuto"
      to: "GET /api/telnyx/script-effectiveness"
      via: "coverage.withScriptsManual — la cobertura que registró una persona"
      pattern: "withScriptsManual"
---

<objective>
El contrato de datos de la atribución de guion. Hoy `scriptIdsUsed` solo se
persiste si viene DENTRO de `telnyxCallMeta` (index.js:12824-12829), es decir
solo si la llamada fue por WebRTC y el SDR además clickeó un guion en el panel
flotante. Ese camino existe desde Sprint 12 y dio **0 de 199 llamadas** al
17/08.

Este plan abre el contrato para que la atribución pueda viajar desde cualquier
superficie (que es lo que van a mandar 35-02 y 35-03), la valida contra el
banco real de guiones, la restringe a llamadas donde alguien atendió, y separa
lo que atribuyó el sistema de lo que eligió una persona — la misma distinción
que `callStage`/`callStageAuto` ya hizo el 16/08 y sin la cual un porcentaje no
se puede interpretar.

Purpose: sin este plan, el frontend puede mostrar todos los selectores que
quiera y el dato se pierde en el borde del servidor. Es el único cambio de
esta fase que toca `index.js`, y no toca `public/` (por diseño: sin frontend
no hay nada visible todavía — SCR-01/SCR-03 se cierran recién con 35-03).

Output: contrato aceptado y validado en `call-disposition`; cobertura
auto/manual expuesta en `script-effectiveness`; `tests/script-attribution.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-scr-atribucion-guion/CONTEXT.md
@CLAUDE.md

Precedente directo de este contrato (mismo idioma: whitelist-and-coerce,
flag `Auto` para lo derivado, gate por outcome):
@.planning/phases/34-hoy-vista-diaria/34-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos vigentes en index.js. Leer los rangos de <read_first> antes de editar. -->

Bloque de medición de guiones ya existente (index.js ~12679-12694) — el lugar
donde va lo nuevo, inmediatamente después:
```js
const CALL_STAGES = new Set(['contestador', 'recepcion', 'decisor']);
const _deriveCallStage = (outcome) => (outcome === 'voicemail' ? 'contestador' : '');
const CALL_STAGE_RELEVANT_OUTCOMES = new Set([
  'answered_interested', 'answered_not_interested', 'hung_up',
  'callback_later', 'scheduled_with_admin', 'placeholder_sent',
]);
globalThis.__voiceAgent.CALL_STAGES = CALL_STAGES;
```

Persistencia de la etapa dentro de `call-disposition` (index.js ~12797-12808) —
el patrón exacto a replicar (whitelist + gate de outcome + flag de derivado):
```js
const _cleanStage = (typeof callStage === 'string' && CALL_STAGES.has(callStage)
  && CALL_STAGE_RELEVANT_OUTCOMES.has(outcome)) ? callStage : '';
const _stage = _cleanStage || _deriveCallStage(outcome);
if (_stage) {
  logEntry.callStage = _stage;
  if (!_cleanStage) logEntry.callStageAuto = true;
}
```

Lo que HOY persiste guiones, y su única puerta de entrada (index.js
~12810-12833) — este bloque se vacía de guiones en la Task 1:
```js
if (telnyxCallMeta && typeof telnyxCallMeta === 'object') {
  ...
  if (Array.isArray(telnyxCallMeta.scriptIdsUsed) && telnyxCallMeta.scriptIdsUsed.length > 0) {
    logEntry.scriptIdsUsed = telnyxCallMeta.scriptIdsUsed
      .filter(id => typeof id === 'string').slice(0, 20).map(id => id.substring(0, 80));
  }
} else {
  logEntry.channel = 'manual';
}
```

Herencia del entry corregido (index.js ~12862-12868) — la lista de campos que
una corrección hereda de la llamada original:
```js
for (const f of ['duration', 'fromNumber', 'channel', 'cost', 'costCountry', 'costTariffKey', 'quickNote', 'scriptIdsUsed', 'callStage', 'callStageAuto']) {
  if (_correctedEntry[f] !== undefined) logEntry[f] = _correctedEntry[f];
}
```

Banco de guiones (index.js ~19540-19568). `loadCallScripts` es una *function
declaration* (hoisted) pero `CALL_SCRIPTS_FILE` es un `const` declarado en la
línea 19540: llamarla en tiempo de request es seguro, llamarla durante la
evaluación del módulo NO (TDZ):
```js
const CALL_SCRIPTS_FILE = path.join(DATA_DIR, "call_scripts.json");
function loadCallScripts() { /* lee el JSON; lazy-init desde scripts/seed/call-scripts.json */ }
```
Forma de un guion en `data/call_scripts.json` (30 en el seed):
`{ id, label, trigger, text, tags[], createdAt, createdBy }` — sin `active`,
sin `variant`. Triggers reales: `rules`(3), `before_call`, `gatekeeper`(5),
`opener`(2), `pitch`(2), `ask_meeting`(3), `confirm`(2), `objection_brushoff`(3),
`objection_real`(5), `callback`, `whatsapp_msg`, `email_template`(2).

Acumulador de `GET /api/telnyx/script-effectiveness` (index.js ~19875-19973),
donde entra el split auto/manual:
```js
const gl = { calls: 0, withStage: 0, withScripts: 0, contestador: 0, recepcion: 0, decisor: 0, connects: 0, interested: 0, scheduled: 0 };
const _blank = (scriptId) => ({ scriptId, label: ..., trigger: ..., variant: ..., used: 0, staged: 0, recepcion: 0, decisor: 0, reached: 0, interested: 0, scheduled: 0 });
...
const hasScripts = Array.isArray(c.scriptIdsUsed) && c.scriptIdsUsed.length > 0;
if (hasScripts) gl.withScripts++;
...
coverage: { calls, withStage, withStagePct, withScripts, withScriptsPct }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: contrato de atribución en call-disposition</name>

  <files>index.js</files>

  <read_first>
    - `index.js` líneas 12660-12720 (bloque `CALL_STAGES`/`_deriveCallStage`/`CALL_STAGE_RELEVANT_OUTCOMES` + exposición en `globalThis.__voiceAgent` + cabecera de `call-disposition` con el guard de RBAC y el destructuring del body)
    - `index.js` líneas 12780-12875 (armado de `logEntry`: objectionTags, callStage, bloque `telnyxCallMeta`, `disqualifyReason`, auto-marca, herencia de `_correctedEntry`)
    - `index.js` líneas 19535-19570 (`CALL_SCRIPTS_FILE`, `loadCallScripts`, `GET /api/telnyx/scripts`)
  </read_first>

  <action>
    Trabajar SOLO sobre `index.js`.

    1. Inmediatamente después del bloque de `CALL_STAGE_RELEVANT_OUTCOMES` y sus
       tres líneas de `globalThis.__voiceAgent` (~12694), agregar el bloque de
       constantes y helpers de atribución de guion, con comentario de decisión
       que explique: la atribución es una dimensión SEPARADA del resultado y de
       la etapa (D-02 del CONTEXT), si hubo más de un guion se guardan todos
       (D-03), y por qué el gate de outcome es el mismo que el de la etapa —
       en un `no_answer` nadie escuchó ningún guion, y contarlo inflaría el
       denominador de la vista de efectividad hacia abajo.
       - `SCRIPT_RELEVANT_OUTCOMES`: Set con los MISMOS seis outcomes que
         `CALL_STAGE_RELEVANT_OUTCOMES` (`answered_interested`,
         `answered_not_interested`, `hung_up`, `callback_later`,
         `scheduled_with_admin`, `placeholder_sent`). Declararlo como Set
         propio y NO como alias/referencia del de la etapa: son dos criterios
         que hoy coinciden y mañana pueden divergir (por ejemplo si algún día
         hay guion de buzón). Dejar eso escrito en el comentario.
       - `MAX_SCRIPT_IDS_PER_CALL = 20` y `MAX_SCRIPT_ID_LEN = 80` (los mismos
         topes que ya aplicaba el bloque viejo, ahora nombrados).
       - `_knownScriptIds()`: devuelve un `Set` con los `id` de
         `loadCallScripts().scripts`. Nunca lanza (el load ya es defensivo).
         Comentario: se llama SOLO en tiempo de request y SOLO cuando hay
         candidatos, nunca durante la evaluación del módulo (`CALL_SCRIPTS_FILE`
         es `const` declarado más abajo — TDZ).
       - `_sanitizeScriptIds(candidates, knownIds)`: PURA. Filtra a strings no
         vacíos, recorta a `MAX_SCRIPT_ID_LEN`, descarta los que no estén en
         `knownIds`, deduplica preservando el orden de aparición y corta en
         `MAX_SCRIPT_IDS_PER_CALL`. Devuelve array (vacío si no quedó nada).
       - Exponer `globalThis.__scriptAttr = { SCRIPT_RELEVANT_OUTCOMES,
         MAX_SCRIPT_IDS_PER_CALL, _sanitizeScriptIds }` siguiendo el patrón de
         `globalThis.__voiceAgent` de arriba (crear el objeto si no existe).

    2. Sumar `scriptIdsUsed` y `scriptIdsAuto` al destructuring del body de
       `call-disposition` (~12714, la línea que ya trae `callStage`).

    3. Inmediatamente después del bloque que persiste `callStage` (~12808),
       agregar el bloque unificado de atribución:
       - Candidatos = concatenación de `telnyxCallMeta.scriptIdsUsed` (si
         `telnyxCallMeta` es objeto y ese campo es array) seguido de
         `scriptIdsUsed` del nivel superior del body (si es array). El orden
         importa poco porque se deduplica, pero dejar la meta primero:
         representa lo que pasó durante la llamada.
       - Si no hay candidatos, o `SCRIPT_RELEVANT_OUTCOMES` no contiene el
         `outcome`, no se persiste nada (ni el campo vacío).
       - Sanear con `_sanitizeScriptIds(candidatos, _knownScriptIds())`. Llamar
         `_knownScriptIds()` SOLO dentro de esta rama, para no leer
         `call_scripts.json` en cada disposición que no atribuye guion.
       - Si quedó algo: `logEntry.scriptIdsUsed = <limpios>` y, cuando la
         atribución sea automática, `logEntry.scriptIdsAuto = true` (nunca
         `false`: campo ausente = la eligió una persona, mismo idioma que
         `callStageAuto`).
       - Resolución del flag: si el body trae `scriptIdsUsed` en el nivel
         superior, manda `scriptIdsAuto` del body; si no vino nada por el nivel
         superior, manda `telnyxCallMeta.scriptIdsAuto`. Solo el booleano
         estricto `true` cuenta como automático. Comentar por qué: las dos
         fuentes salen del mismo estado del frontend y coinciden, pero cuando
         una superficie manda su propia atribución (segunda oportunidad, SCR-03)
         esa es la más nueva y tiene que ganar.
       - Comentario obligatorio: por qué el whitelist es contra el banco y no
         contra una lista hardcodeada (los guiones los edita el admin desde
         Centralita), y que lo aceptado es superset de lo ofrecido (los guiones
         `trigger: 'rules'` no se ofrecen en el panel de flow pero se aceptan si
         existen).

    4. Vaciar de guiones el bloque viejo dentro de `if (telnyxCallMeta ...)`
       (~12824-12829): borrar ese `if (Array.isArray(telnyxCallMeta.scriptIdsUsed)...)`
       entero. Todo lo demás de ese bloque (duration, fromNumber, channel, cost,
       quickNote) queda intacto. Dejar un comentario de una línea diciendo a
       dónde se mudó y por qué (una sola puerta de entrada, la nota #156 de
       CLAUDE.md sobre builders duplicados aplica igual a la persistencia).

    5. Agregar `'scriptIdsAuto'` a la lista de campos que hereda el entry de
       corrección (~12865, la que ya incluye `'scriptIdsUsed'` y
       `'callStageAuto'`). Sin esto, corregir una disposición automática
       convertiría su atribución automática en "registrada por una persona" y
       la cobertura mentiría hacia arriba.
  </action>

  <verify>
    <automated>node --check index.js</automated>
  </verify>

  <done>
    `SCRIPT_RELEVANT_OUTCOMES`, `MAX_SCRIPT_IDS_PER_CALL`, `_knownScriptIds` y
    `_sanitizeScriptIds` existen y están expuestos en `globalThis.__scriptAttr`;
    `call-disposition` acepta `scriptIdsUsed`/`scriptIdsAuto` del nivel superior
    del body y los une con los de `telnyxCallMeta`; el bloque viejo dentro de
    `telnyxCallMeta` ya no persiste guiones; la corrección hereda
    `scriptIdsAuto`. `node --check index.js` limpio.
  </done>
</task>

<task type="auto">
  <name>Task 2: cobertura auto vs registrada por una persona en script-effectiveness</name>

  <files>index.js</files>

  <read_first>
    - `index.js` líneas 19850-19975 (`GET /api/telnyx/script-effectiveness` completo: acumulador `gl`, `_blank`, loop de callLog, armado de `coverage`)
  </read_first>

  <action>
    Trabajar SOLO sobre `index.js`, dentro de `script-effectiveness`. NO
    rehacer la vista ni cambiar la forma de ninguna métrica existente (non-goal
    del CONTEXT): esto es un campo más en la misma respuesta.

    1. En el acumulador global `gl`, agregar `withScriptsManual: 0`.
    2. En el loop, donde ya se calcula `hasScripts`, derivar
       `scriptsManual = hasScripts && c.scriptIdsAuto !== true` e incrementar
       `gl.withScriptsManual` cuando sea verdadero. Mantener `gl.withScripts`
       contando TODAS (auto incluidas): son dos números distintos y los dos
       hacen falta.
    3. En `_blank`, agregar `usedManual: 0`; dentro del loop por `scriptId`,
       incrementar `st.usedManual` cuando `scriptsManual` sea verdadero. Es una
       línea y evita que la próxima pregunta ("¿este guion lo eligió alguien o
       se lo atribuyó el sistema?") obligue a volver a tocar el endpoint.
    4. En `coverage`, agregar `withScriptsManual` y `withScriptsManualPct`
       (usando el helper `pct` que ya existe, sobre `gl.calls`).
    5. Comentario de decisión arriba de `coverage`, extendiendo el que ya está:
       con el default automático de la Fase 35 la cobertura total va a saltar a
       casi 100% de golpe; el número que dice si el equipo está midiendo de
       verdad es el manual. Un 100% total con 0% manual significa "todos los
       guiones atribuidos son el que estaba puesto por defecto", que sirve para
       comparar el guion oficial contra sí mismo pero NO para comparar dos
       guiones entre sí.

    Dejar explícitamente sin tocar el filtro `if (c.channel !== 'telnyx_webrtc') continue;`
    (los entries `manual` duplicarían conversaciones, ya documentado en el
    código). La divergencia contra el script de cobertura de 35-04 —que no
    filtra por canal— se resuelve allá, reportando el desglose.
  </action>

  <verify>
    <automated>node --check index.js</automated>
  </verify>

  <done>
    `GET /api/telnyx/script-effectiveness` devuelve
    `coverage.withScriptsManual` + `coverage.withScriptsManualPct` y
    `scripts[].usedManual`, sin cambiar ningún campo previo de la respuesta.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: suite HTTP del contrato de atribución</name>

  <files>tests/script-attribution.test.js</files>

  <behavior>
    - Superficie sin llamada WebRTC: `POST call-disposition` con
      `{ outcome: 'answered_interested', scriptIdsUsed: ['sc_opener'] }` y SIN
      `telnyxCallMeta` persiste `scriptIdsUsed: ['sc_opener']` en el último
      entry del callLog (y ese entry queda con `channel: 'manual'`, sin
      regresión del comportamiento previo).
    - Id que no está en `call_scripts.json` se descarta; si TODOS los ids son
      desconocidos, el entry queda SIN el campo `scriptIdsUsed`.
    - Gate de outcome: los mismos ids con `outcome: 'no_answer'` no se
      persisten. Con `voicemail` tampoco (hoy fuera del set, decisión explícita).
    - `scriptIdsAuto: true` persiste `scriptIdsAuto === true` en el entry; sin
      el flag (o con `false`) el campo NO existe en el entry.
    - Unión y dedupe: `telnyxCallMeta.scriptIdsUsed = ['sc_opener']` +
      `scriptIdsUsed: ['sc_opener','sc_gk']` en el nivel superior → el entry
      guarda exactamente `['sc_opener','sc_gk']` (sin repetir), y el flag lo
      decide el body.
    - Tope: 25 ids válidos distintos → se guardan 20.
    - Corrección de auto-marca (`correctsAutoMarked: true` dentro de la ventana)
      hereda `scriptIdsUsed` y `scriptIdsAuto` del entry corregido cuando el
      body de la corrección no trae `telnyxCallMeta` propio.
    - `GET /api/telnyx/script-effectiveness?range=all` como admin devuelve
      `coverage.withScripts` contando ambas y `coverage.withScriptsManual`
      contando solo las que no son auto, sobre entries del dialer.
    - RBAC sin cambios: un setter puede atribuir en su propio lead; el guard de
      lead ajeno sigue devolviendo 403 (no se relaja nada).
  </behavior>

  <action>
    Crear `tests/script-attribution.test.js` copiando el molde de setup de
    `tests/disposition-enforcement.test.js` (líneas 1-95): `NODE_ENV=test`,
    `DATA_DIR` en un tmpdir propio, `auth.json` + `setters.json` escritos ANTES
    del `await import("../index.js")`, API keys de IA en `""` (regla #121,
    NUNCA `delete`), helper `login()` que devuelve la cookie `gs_session`, y
    `afterAll` que borra el tmpdir.

    Escribir además un `call_scripts.json` FIJO en el tmpData antes del import,
    con tres guiones (`sc_opener` trigger `opener`, `sc_gk` trigger
    `gatekeeper`, `sc_rules` trigger `rules`) — sin fixture propio, el server
    haría lazy-init desde `scripts/seed/call-scripts.json` y los ids reales del
    seed acoplarían la suite al contenido del banco.

    Usar un lead por caso (no reciclar el mismo lead entre tests: cada
    disposición cambia estado/cadencia y el orden de ejecución dejaría de ser
    independiente). Para leer lo persistido, releer `setters.json` del tmpData
    con `fs.readFileSync` y mirar el último entry de `callLog` — es la
    verificación de persistencia real, no de lo que devuelve el endpoint.

    Para el caso de corrección: registrar primero una auto-marca
    (`{ outcome: 'no_answer', autoMarked: true, telnyxCallMeta: { durationSecs, scriptIdsUsed: ['sc_opener'], scriptIdsAuto: true } }`)
    y después la corrección con `correctsAutoMarked: true`. Ojo: el gate de
    outcome hace que la auto-marca `no_answer` NO persista guion — para probar
    la herencia hay que partir de un entry corregible que sí tenga atribución,
    así que armar ese caso corrigiendo un entry con outcome relevante, o
    inyectar el entry original directamente en el fixture de `setters.json`.
    Elegir el camino que quede más honesto y dejarlo comentado en el test.

    Para el caso de `script-effectiveness`, los entries tienen que ser
    `channel: 'telnyx_webrtc'` (el endpoint ignora los `manual` a propósito):
    mandar `telnyxCallMeta` en esas disposiciones.
  </action>

  <verify>
    <automated>npx vitest run tests/script-attribution.test.js</automated>
  </verify>

  <done>
    Suite verde, cubriendo los 9 comportamientos de `<behavior>`. Cada
    aserción de persistencia se hace releyendo `setters.json` del tmpdir.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → `POST /call-disposition` (`scriptIdsUsed`, `scriptIdsAuto`) | entrada NO confiable que se persiste en el `callLog` y alimenta métricas |
| `data/call_scripts.json` → whitelist | el banco lo edita el admin desde Centralita; es la autoridad de qué id es válido |
| `callLog[].scriptIdsAuto` → `script-effectiveness` | dato de medición: si miente, se compara mal un guion contra otro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | `scriptIdsUsed` del body se persiste en `lead.callLog` | mitigate | `_sanitizeScriptIds`: solo strings, recorte a 80 chars, whitelist contra `_knownScriptIds()`, dedupe y tope de 20 — un cliente no puede inyectar ids arbitrarios ni inflar el entry |
| T-35-02 | Repudiation / integridad de métrica | un cliente podría omitir `scriptIdsAuto` para que su atribución automática figure como registrada a mano | accept | El único perjudicado es el propio SDR sobre su propia métrica de proceso, no hay dinero ni acceso detrás; además el campo solo se persiste como `true` (ausencia = manual) y `coverage` publica los dos números para que la distorsión se vea |
| T-35-03 | Denial of Service | `loadCallScripts()` lee `call_scripts.json` de disco en cada disposición | mitigate | El load se invoca SOLO dentro de la rama que ya confirmó candidatos + outcome relevante; una disposición sin guion no toca el archivo |
| T-35-04 | Tampering (integridad de métrica) | atribuir guion a llamadas donde nadie atendió inflaría el denominador de efectividad | mitigate | Gate `SCRIPT_RELEVANT_OUTCOMES`, el mismo criterio ya aplicado a `callStage` |
| T-35-SC | Tampering | instalación de paquetes npm | mitigate | Este plan no instala nada. `git diff --stat package.json package-lock.json` vacío al cerrar |
</threat_model>

<verification>
1. `node --check index.js`.
2. `npx vitest run tests/script-attribution.test.js` — suite nueva verde.
3. `npx vitest run tests/disposition-enforcement.test.js tests/call-cadence.test.js tests/hangup-cap.test.js tests/funnel-close.test.js` — regresión de los vecinos de `call-disposition`.
4. `npm test` completo. Comparar contra el baseline previo al plan; los
   fallos ambientales conocidos (`wa-campaign-engine`, dependiente de hora/día
   — notas #93/#110/#171 de CLAUDE.md) se verifican con `git stash` antes de
   reportarlos como regresión.
5. `git diff --stat public/` vacío — este plan NO toca frontend, así que NO se
   bumpea el cache-buster.
6. **Verificación por mutación**: quitar temporalmente el gate
   `SCRIPT_RELEVANT_OUTCOMES.has(outcome)` y confirmar que el test de
   `no_answer` se pone en rojo. Restaurar con `git checkout -- index.js`.
</verification>

<success_criteria>
- Una disposición sin `telnyxCallMeta` puede atribuir guion.
- Ningún id fuera del banco llega al `callLog`.
- Ninguna llamada sin atención queda atribuida.
- `scriptIdsAuto` distingue lo automático de lo elegido, y sobrevive a una
  corrección.
- `script-effectiveness` publica cobertura total y manual por separado.
- Suite completa verde contra el baseline; `public/` intacto.
</success_criteria>

<output>
Crear `.planning/phases/35-scr-atribucion-guion/35-01-SUMMARY.md` al terminar.

Documentar explícitamente:
- El contrato final tal como quedó (nombres de campo del body, del entry y de
  `coverage`) — 35-02, 35-03 y 35-04 dependen de esos nombres exactos.
- Si se cambió algo del contrato propuesto acá, decirlo en primera línea: los
  otros tres planes lo dan por fijo.
- Qué queda SIN verificar en vivo: que una disposición real de producción
  llegue con `scriptIdsUsed` (nada en `public/` lo manda todavía — es 35-02).
- El baseline de `npm test` (números reales antes y después).
</output>
