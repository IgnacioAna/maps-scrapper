---
phase: 35-scr-atribucion-guion
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - public/app.js
  - public/index.html
  - tests/script-attribution-core.test.js
autonomous: true
requirements: [SCR-01, SCR-02]
must_haves:
  truths:
    - "Al arrancar una llamada desde el dialer, el lead queda con un guion atribuido sin que el SDR toque nada — el último que usó, o el opener oficial"
    - "El SDR VE qué guion se le está atribuyendo mientras habla, en el panel de llamada, y puede cambiarlo ahí sin abrir el panel de guiones"
    - "Cambiar de guion durante la llamada desde el panel de guiones SUMA (se usaron los dos, D-03); elegirlo desde un selector REEMPLAZA (es una corrección, no un segundo guion)"
    - "La atribución viaja en el body de call-disposition desde cualquier superficie, porque se inyecta en _dispoEnforcementBody — el mismo helper que ya comparten los 6 call sites"
    - "El guion atribuido es de UNA llamada: guardada la disposición se apaga, y la llamada siguiente vuelve a nacer con el default"
    - "Hay un solo estado de atribución en el frontend: _telnyxCallState.scriptIdsUsed deja de existir"
  artifacts:
    - path: "public/app.js"
      provides: "Bloque [35-02] SCR-ATTR (estado + builder + default + sync) y la captura automática al iniciar llamada"
      contains: "SCR-ATTR: INICIO"
    - path: "public/index.html"
      provides: "Selector de guion en el panel de llamada (#telnyx-call-script-wrap) + cache-buster bumpeado"
      contains: "telnyx-call-script-wrap"
    - path: "tests/script-attribution-core.test.js"
      provides: "Tests de fuente del cableado + tests de comportamiento del bloque SCR-ATTR aislado con new Function"
  key_links:
    - from: "_startTelnyxCall"
      to: "window._setCallScript(leadId, _scriptDefaultId(), { auto: true })"
      via: "siembra del default al iniciar la llamada — la causa raíz de la cobertura 0 (SCR-02)"
      pattern: "_scriptDefaultId\\(\\)"
    - from: "_dispoEnforcementBody"
      to: "body.scriptIdsUsed / body.scriptIdsAuto"
      via: "el contrato que aceptó 35-01, inyectado en el único helper compartido por los call sites"
      pattern: "body\\.scriptIdsUsed"
    - from: "_selectScript"
      to: "window._setCallScript(..., { append: true })"
      via: "el panel de guiones deja de escribir su propio array y escribe en el estado único"
      pattern: "append: true"
---

<objective>
Ataca la causa raíz medida en el CONTEXT: la única vía de captura exige tres
acciones opcionales (llamada por WebRTC + abrir el panel + clickear un guion) y
**cero de 199 llamadas** completó la cadena.

Este plan elimina dos de las tres: la llamada nace con guion atribuido y el
guion se ve (y se cambia) en el panel de llamada, sin abrir nada. Además unifica
el estado —hoy hay dos lugares donde vive la atribución: el array
`_telnyxCallState.scriptIdsUsed` y nada más; después de este plan hay uno solo,
compartido con las superficies de segunda oportunidad que cablea 35-03— y lo
inyecta en `_dispoEnforcementBody`, que es la puerta por la que ya pasan los 6
call sites de `call-disposition`.

NO entra en este plan (35-03): los selectores en la lista de Llamadas, Hoy,
Power Dialer y ficha modal, ni el banner de cobertura de la vista de guiones.

NO entra en esta fase, decisión explícita: abrir el panel de guiones
automáticamente al empezar la llamada. Con el default sembrado la atribución ya
no depende de que se abra, y un panel flotante que aparece solo tapa cosas.

Purpose: sin la siembra automática, cualquier cantidad de selectores nuevos
sigue dependiendo de que alguien se acuerde — que es exactamente el error que el
diagnóstico del 16/08 ya cometió una vez con `callStage`.

Output: bloque `[35-02] SCR-ATTR` en `public/app.js`; selector de guion en el
panel de llamada; `tests/script-attribution-core.test.js`; cache-buster bumpeado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-scr-atribucion-guion/CONTEXT.md
@.planning/phases/35-scr-atribucion-guion/35-01-SUMMARY.md
@CLAUDE.md

El precedente que este plan replica (builder único + estado por lead + sync que
mira `data-lead`), incluido su test de fuente:
@tests/call-stage-surfaces.test.js
</context>

<interfaces>
<!-- Contratos vigentes. Leer los rangos de <read_first> antes de editar. -->

Contrato que ya acepta el backend (35-01), en el body de
`POST /api/setters/leads/:id/call-disposition`:
```
scriptIdsUsed: string[]   // nivel superior del body (además del que viaja en telnyxCallMeta)
scriptIdsAuto: boolean    // true = lo atribuyó el sistema; ausente/false = lo eligió una persona
```
Solo se persiste si el outcome está en `SCRIPT_RELEVANT_OUTCOMES`
(`answered_interested`, `answered_not_interested`, `hung_up`, `callback_later`,
`scheduled_with_admin`, `placeholder_sent`) y si los ids existen en el banco.

Estado de la etapa, el molde EXACTO a espejar (public/app.js ~10336-10405):
```js
let _dispoStage = null; // { leadId, stage }
function _stageCurrentLeadId() {
  return _telnyxCallState?.leadId || (_pd.active ? _pd.queue[_pd.currentIdx] : '') || '';
}
function _stageFor(leadId) { return (_dispoStage && _dispoStage.leadId === leadId) ? _dispoStage.stage : ''; }
function _syncStageChips() { /* recorre .stage-chip y select.stage-select mirando dataset.lead */ }
window._setCallStage = function(leadId, stage, opts) { /* toggle; '' limpia */ };
function _stageChipsHTML(leadId, { variant = 'chips', fontSize = 12 } = {}) { /* 'chips' | 'select' */ }
function _clearCallStage() { _dispoStage = null; _syncStageChips(); }
function _dispoEnforcementBody(leadId) {
  const body = {};
  const _stage = _stageFor(leadId);
  if (_stage) body.callStage = _stage;
  ...
  return body;
}
```

Estado de la llamada activa y su meta (public/app.js 9919, 11295, 11582, ~11813):
```js
let _telnyxCallState = { leadId: null, fromNumber: null, startedAt: 0, timerInterval: null, muted: false, scriptIdsUsed: [] };
// _closeTelnyxCallPanel:  _telnyxCallState.scriptIdsUsed = [];
// _startTelnyxCall:       _telnyxCallState.scriptIdsUsed = []; // Sprint 12: tracking A/B
//                         if (_callScriptsCache.length === 0) _loadCallScripts();
// _onTelnyxCallEnded, armado de _metaObj:
const _metaObj = { durationSecs, fromNumber, startedAt, endedAt, quickNote, scriptIdsUsed: _telnyxCallState.scriptIdsUsed.slice() };
```

Banco de guiones en el frontend (public/app.js ~11990-12160):
```js
let _callScriptsCache = [];             // 11990 — `let`: OJO TDZ si se toca durante la evaluación del módulo
async function _loadCallScripts() { /* GET /api/telnyx/scripts → _callScriptsCache */ }
function _renderScriptPanel() {
  _renderScriptButtons(_callScriptsCache, '');
  const firstActionable = _callScriptsCache.find(s => s.trigger !== 'rules') || _callScriptsCache[0];
  if (firstActionable) _selectScript(firstActionable.id);   // ← hoy preselecciona el checklist "Antes de llamar"
}
function _renderScriptButtons(scripts, searchQuery) {
  const triggerOrder = ['before_call','gatekeeper','opener','pitch','ask_meeting','confirm','objection_brushoff','objection_real','callback','whatsapp_msg','email_template','first_call','objection','scheduling','voicemail','general'];
  const triggerLabels = { before_call:'Pre-call', gatekeeper:'Recepción', opener:'Apertura', pitch:'Pitch', ask_meeting:'Pedir reunión', confirm:'Confirmar', objection_brushoff:'Brush-off', objection_real:'Real', callback:'Callback', whatsapp_msg:'WhatsApp', email_template:'Email', first_call:'Apertura', objection:'Objeción', scheduling:'Cerrar', voicemail:'Buzón', general:'General' };
  ...
}
function _selectScript(scriptId) {
  ...
  if (_telnyx.activeCall && _telnyxCallState.startedAt > 0 && !_telnyxCallState.scriptIdsUsed.includes(scriptId)) {
    _telnyxCallState.scriptIdsUsed.push(scriptId);   // ← la única captura que existe hoy
  }
  ...
}
```

Forma de un guion tal como llega de `GET /api/telnyx/scripts`:
`{ id, label, trigger, text, tags[], createdAt, createdBy }`. En el banco real
hay 30, con `trigger: 'rules'` ×3 (meta, no se hablan) y `before_call` ×1
(checklist previo, tampoco se habla).

Panel de llamada, donde van los chips de etapa y va a ir el selector de guion
(public/index.html ~1443-1457):
```html
<div style="margin-bottom:10px;" class="tlx-stage">
  <div style="...">¿Con quién hablás?</div>
  <div style="display:flex; gap:6px;">
    <button type="button" class="stage-chip" data-stage="contestador" onclick="window._setCallStage(null,'contestador')">Contestador</button>
    ...
  </div>
</div>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: bloque SCR-ATTR — estado único, builder y default</name>

  <files>public/app.js</files>

  <read_first>
    - `public/app.js` líneas 10330-10420 (bloque completo de `callStage`: `_dispoStage`, `_stageCurrentLeadId`, `_stageFor`, `_syncStageChips`, `window._setCallStage`, `_stageChipsHTML`, `_clearCallStage`, `_dispoEnforcementBody`)
    - `public/app.js` líneas 10053-10095 (`_dispoAfterSaved`, incluido el apagado de la etapa)
    - `public/app.js` líneas 11985-12070 (`_callScriptsCache`, `_loadCallScripts`, `_interpolateScript`, `_renderScriptPanel`)
    - `public/app.js` líneas 12060-12125 (`_renderScriptButtons`: `triggerOrder`, `triggerLabels`, `triggerColors`)
  </read_first>

  <action>
    Trabajar SOLO sobre `public/app.js`.

    Crear un bloque contiguo, delimitado con los marcadores
    `// ─── [35-02] SCR-ATTR: INICIO ───` y `// ─── [35-02] SCR-ATTR: FIN ───`
    (misma convención que `[30-03] DISPO-DEST` y `[28-01] DTPICKER-PURE`),
    ubicado inmediatamente DESPUÉS de `_clearCallStage` y ANTES de
    `_dispoEnforcementBody`. El bloque tiene que ser evaluable aislado: sus
    únicas dependencias externas permitidas son `escHtml`, `_callScriptsCache`,
    `_loadCallScripts`, `_stageCurrentLeadId`, `document`, `window` y
    `localStorage` (el test de la Task 3 los stubea; si se agrega otra
    dependencia, el test se vuelve imposible de escribir).

    Contenido del bloque:

    1. `_SCRIPT_TRIGGER_ORDER` y `_SCRIPT_TRIGGER_LABELS`: mover acá los dos
       objetos/arrays que hoy son `const` locales dentro de `_renderScriptButtons`
       y hacer que esa función los consuma (borrar las copias locales). Un solo
       orden y un solo juego de etiquetas para el panel y para el selector: si
       divergen, el mismo guion aparece con dos nombres en dos pantallas.
       `triggerColors` queda como está, es solo del panel.

    2. `_SCRIPT_META_TRIGGERS`: Set con `'rules'` y `'before_call'`. Comentario:
       son referencia y checklist previo, no se le hablan a nadie — atribuir el
       checklist "Antes de llamar" a una conversación sería un dato falso
       (que es justo lo que hace hoy la preselección de `_renderScriptPanel`).

    3. `let _dispoScript = null; // { leadId, ids: string[], auto: boolean }`
       más los lectores puros `_scriptIdsFor(leadId)` (array, `[]` si no es de
       ese lead), `_scriptPrimaryFor(leadId)` (el ÚLTIMO id — el que muestra el
       selector) y `_scriptIsAuto(leadId)` (booleano).
       Reusar `_stageCurrentLeadId()` para resolver "el lead en foco": NO
       duplicar esa función.

    4. `window._setCallScript = function(leadId, scriptId, opts)`:
       - `id = leadId || _stageCurrentLeadId()`; sin id, no hace nada.
       - `scriptId` vacío → limpia la atribución de ESE lead (y solo de ese).
       - Si el estado actual es de otro lead, se descarta y arranca uno nuevo.
       - **Dos modos, y esto es la decisión central del plan**: por defecto
         REEMPLAZA (`ids = [scriptId]`) porque un selector afirma *cuál* fue el
         guion —es una corrección, no un segundo guion—; con `opts.append === true`
         SUMA si no estaba (`ids.push`), que es lo que corresponde al panel de
         guiones durante la llamada, donde abrir un segundo guion significa que
         se usaron los dos (D-03).
       - `auto`: queda `true` solo si `opts.auto === true`; cualquier elección
         humana lo baja a `false` de forma definitiva para esa llamada (incluso
         si después se agrega otro guion en modo append).
       - Cuando la elección NO es automática, persistir el id en
         `localStorage` bajo `scm_last_script_<userId>` (userId de
         `window.__CURRENT_USER__?.id`, string vacío si no hay) dentro de
         `try/catch` — es el insumo de `_scriptDefaultId`.
       - Termina llamando `_syncScriptControls()`.

    5. `_clearCallScript()`: `_dispoScript = null; _syncScriptControls();`.

    6. `_scriptDefaultId()`: devuelve el id a sembrar, o `''`.
       Precedencia, y dejarla comentada: (a) el último guion que ESTE SDR eligió
       a mano, si sigue existiendo en el banco y no es meta — es la mejor
       predicción disponible de lo que va a usar; (b) el primer guion con
       `trigger === 'opener'` — toda llamada abre con el opener, es el default
       honesto; (c) el primer guion no-meta del banco; (d) `''` si el banco está
       vacío. Nunca inventa un id que no esté en el cache: el backend lo
       descartaría igual (whitelist de 35-01).

    7. `_scriptOptionsHTML(selectedId)`: `<option value="">— Guion —</option>`
       más un `<optgroup>` por trigger en el orden de `_SCRIPT_TRIGGER_ORDER`
       (los triggers desconocidos van al final), excluyendo `_SCRIPT_META_TRIGGERS`.
       El `label` de cada option es `label` del guion. **Todo interpolado con
       `escHtml`**: el label y el id los edita el admin desde Centralita, así que
       son texto no confiable dentro de atributos y de un handler inline.

    8. `_scriptSelectHTML(leadId, { variant = 'row', fontSize = 12, minWidth = 150 } = {})`:
       builder ÚNICO (mismo criterio que `_stageChipsHTML`/`_dispoSelectHTML`/
       `_actButtonsHTML`: un builder, N call sites). `<select class="script-select"
       data-lead="...">` con `onchange="window._setCallScript('<id>', this.value)"`
       (sin `append`: desde un selector siempre reemplaza). `title` que explique
       en una línea que es el guion usado, que se atribuye solo al empezar a
       discar y que se puede corregir. Dos variantes de estilo, igual que
       `_actButtonsHTML`: `'row'` con la paleta de la app (`var(--bg-input)`,
       `var(--border-default)`, `var(--text-primary)`) y `'call'` con la paleta
       oscura del panel de llamada (fondo `rgba(255,255,255,0.05)`, borde
       `rgba(255,255,255,0.08)`, texto `#fff`) para no romper ese panel.

    9. `_syncScriptControls()`: recorre `document.querySelectorAll('select.script-select')`;
       para cada uno resuelve el lead con `dataset.lead || _stageCurrentLeadId()`,
       repuebla las `<option>` si la firma del banco cambió (guardar la firma en
       `dataset.optsig`, por ejemplo cantidad de guiones + id del primero) y
       recién DESPUÉS asigna `select.value = _scriptPrimaryFor(id)`. El orden
       importa: asignar un value que todavía no existe como option lo deja en
       vacío. Mismo criterio que `_syncStageChips`: mirar el lead de CADA
       control, nunca un valor global — con 50 filas en pantalla, comparar solo
       contra `_dispoScript.scriptId` prendería el mismo guion en todas.

    10. `_ensureCallScripts()`: carga el banco una sola vez (guardando la
        promesa en vuelo para que N llamadas concurrentes no disparen N fetch),
        y al resolver llama `_syncScriptControls()` para que los selectores ya
        renderizados se llenen. Devuelve una promesa siempre. Comentario TDZ:
        no invocarla durante la evaluación del módulo (`_callScriptsCache` es un
        `let` declarado más abajo) — solo desde paths de render o de llamada.

    Fuera del bloque, dos ediciones puntuales:

    11. `_dispoEnforcementBody(leadId)` (~10401): después de la inyección de
        `callStage`, agregar la de guion — si `_scriptIdsFor(leadId)` tiene
        elementos, `body.scriptIdsUsed = [...]` y, solo si `_scriptIsAuto(leadId)`,
        `body.scriptIdsAuto = true`. Comentario: el guion viaja desde CUALQUIER
        superficie por la misma razón que la etapa — es el helper que comparten
        los call sites.

    12. `_dispoAfterSaved(leadId, opts)` (~10053): junto al apagado de la etapa,
        apagar la atribución (`if (_dispoScript && _dispoScript.leadId === leadId) _clearCallScript();`).
        Mismo motivo, explicitado en comentario: el guion es de UNA llamada; si
        queda encendido, la próxima disposición de ese lead lo reenvía como si
        fuera nuevo.
  </action>

  <verify>
    <automated>node --check public/app.js</automated>
  </verify>

  <done>
    El bloque `[35-02] SCR-ATTR` existe entre marcadores, es contiguo y no
    depende de nada fuera de la lista permitida; `_renderScriptButtons` consume
    `_SCRIPT_TRIGGER_ORDER`/`_SCRIPT_TRIGGER_LABELS` en vez de sus copias
    locales; `_dispoEnforcementBody` inyecta `scriptIdsUsed`/`scriptIdsAuto` y
    `_dispoAfterSaved` los apaga. `node --check public/app.js` limpio.
  </done>
</task>

<task type="auto">
  <name>Task 2: captura automática en vivo — siembra al discar y selector en el panel de llamada</name>

  <files>public/app.js, public/index.html</files>

  <read_first>
    - `public/app.js` línea 9919 (declaración de `_telnyxCallState`)
    - `public/app.js` líneas 11285-11300 (`_closeTelnyxCallPanel`, reset de estado)
    - `public/app.js` líneas 11555-11595 (`_startTelnyxCall`: `_clearCallStage()`, seteo de `leadId`, reset de `scriptIdsUsed`, precarga del banco)
    - `public/app.js` líneas 11795-11825 (`_onTelnyxCallEnded`: armado de `_metaObj`)
    - `public/app.js` líneas 12036-12060 (`_renderScriptPanel`) y 12115-12160 (wire de clicks + `_selectScript`)
    - `public/index.html` líneas 1443-1470 (bloque de etapa + nota rápida del panel de llamada) y la línea del `<script src="/app.js?v=...">` al final
  </read_first>

  <action>
    1. **Un solo estado**: borrar `scriptIdsUsed` de la declaración de
       `_telnyxCallState` (9919) y sus dos reseteos (11295 en
       `_closeTelnyxCallPanel`, 11582 en `_startTelnyxCall`). El estado pasa a
       vivir solo en `_dispoScript`. Verificar con grep que no quede ninguna
       otra lectura de `_telnyxCallState.scriptIdsUsed` en el archivo.

    2. **Siembra al iniciar la llamada** (`_startTelnyxCall`, donde estaba el
       reset y la precarga del banco, ~11582-11589): reemplazar por
       `_clearCallScript()` seguido de la siembra del default con
       `{ auto: true }`, y `_ensureCallScripts()` con una re-siembra en el
       `.then` para el caso de que el banco todavía no estuviera en cache
       (primera llamada de la sesión) — re-sembrar SOLO si el lead sigue sin
       atribución, para no pisar una elección que el SDR haya hecho mientras
       cargaba. Comentario obligatorio explicando SCR-02: por qué la llamada
       tiene que nacer con guion y por qué se marca `auto` (para que la
       cobertura no se lea como si alguien lo hubiera elegido).

    3. **Selector visible durante la llamada**: en `public/index.html`, entre el
       bloque de etapa (`class="tlx-stage"`) y el de nota rápida, agregar un
       bloque con el mismo formato de rótulo (`Guion que estás usando`) y un
       contenedor vacío `<div id="telnyx-call-script-wrap"></div>`. El `<select>`
       lo pinta el JS porque las opciones salen del banco.
       En `_startTelnyxCall`, después de sembrar, poblar ese contenedor con
       `_scriptSelectHTML(leadId, { variant: 'call', ... })` y llamar
       `_syncScriptControls()`. En `_closeTelnyxCallPanel`, vaciarlo.

    4. **El panel de guiones escribe en el estado único**: en `_selectScript`,
       reemplazar el push a `_telnyxCallState.scriptIdsUsed` por una llamada a
       `window._setCallScript(<lead en foco>, scriptId, { append: true, auto: <lo que venga> })`,
       resolviendo el lead con `_stageCurrentLeadId()` y no haciendo nada si no
       hay lead. Extender la firma a `_selectScript(scriptId, opts)` para que la
       preselección pueda marcarse automática.
       El wire de clicks de `_renderScriptButtons` sigue llamando
       `_selectScript(btn.dataset.scriptId)` sin opts → elección humana.

    5. **Preselección honesta del panel**: en `_renderScriptPanel`, reemplazar
       la elección de `firstActionable` (que hoy cae en el checklist "Antes de
       llamar") por `_scriptDefaultId()`, y llamarla con `{ auto: true }`. Si
       `_scriptDefaultId()` devuelve vacío, caer al primer guion no-meta.
       Comentario: preseleccionar no es elegir — si se marcara como humana, la
       cobertura manual mentiría al 100%.

    6. **Meta de fin de llamada** (`_onTelnyxCallEnded`, ~11813): el `_metaObj`
       toma `scriptIdsUsed: _scriptIdsFor(leadId)` (copia) y suma
       `scriptIdsAuto: _scriptIsAuto(leadId)`. Comentario: esta meta se persiste
       en `localStorage` (`_telnyxMetaPersist`) y sobrevive un F5, así que es la
       red de seguridad de la atribución cuando `_dispoScript` se pierde por
       recarga antes de marcar el resultado.

    7. **Cache-buster**: bumpear el `?v=` de `app.js` en `public/index.html`
       (regla del repo, nota #48/#94/#123 de CLAUDE.md). `style.css` no se toca
       en este plan → su `?v=` queda igual. Dejar en el SUMMARY el valor previo
       y el nuevo, leídos de disco.
  </action>

  <verify>
    <automated>node --check public/app.js</automated>
  </verify>

  <done>
    `_telnyxCallState.scriptIdsUsed` no existe más en el archivo; iniciar una
    llamada siembra el default con `auto:true`; el panel de llamada muestra un
    `<select>` de guion poblado y sincronizado; `_selectScript` escribe en
    `_dispoScript` en modo append; la meta de cierre lleva
    `scriptIdsUsed`/`scriptIdsAuto`; cache-buster bumpeado.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests del núcleo (fuente + comportamiento aislado)</name>

  <files>tests/script-attribution-core.test.js</files>

  <behavior>
    Comportamiento del bloque `[35-02] SCR-ATTR` evaluado aislado con
    `new Function` (stubs: `escHtml` identidad, `_callScriptsCache` fijo,
    `_stageCurrentLeadId` que devuelve un id controlado, `document` con
    `querySelectorAll` → `[]`, `localStorage` con Map, `window` objeto vacío,
    `_loadCallScripts` async no-op):
    - `_scriptDefaultId` devuelve el último guion elegido a mano si sigue en el
      banco y no es meta.
    - Si ese último ya no existe (guion borrado), cae al primer `opener`.
    - Sin último guardado, devuelve el primer `opener` — nunca el `rules` ni el
      `before_call`, aunque estén primeros en el banco.
    - Con el banco vacío devuelve `''` (y sembrar con `''` no crea estado).
    - `_setCallScript` sin `append` REEMPLAZA (queda un solo id); con
      `append: true` SUMA sin duplicar.
    - `auto` nace `true` con `{auto:true}` y queda en `false` para siempre en
      esa llamada apenas hay una elección humana, incluso si después se agrega
      otro guion en append.
    - La atribución es por lead: marcar en `lead_a` no afecta a `lead_b`, y
      atribuir en `lead_b` descarta el estado de `lead_a`.
    - `_setCallScript(id, '')` limpia solo ese lead.
    - Una elección humana persiste el id en `localStorage`; una automática NO.
    - `_scriptOptionsHTML` emite la option vacía, agrupa por `<optgroup>` en el
      orden de `_SCRIPT_TRIGGER_ORDER`, excluye los triggers meta y llama
      `escHtml` sobre label e id (verificable con un guion cuyo label traiga
      comillas y `<`).
    - `_scriptSelectHTML` emite `class="script-select"`, `data-lead` con el lead
      y un `onchange` que llama `window._setCallScript`.

    Tests de FUENTE sobre `public/app.js` (mismo patrón que
    `tests/call-stage-surfaces.test.js`):
    - `_scriptSelectHTML` se declara UNA sola vez.
    - `_telnyxCallState.scriptIdsUsed` no aparece más en el archivo (regresión
      del estado duplicado).
    - `_dispoEnforcementBody` contiene `body.scriptIdsUsed`.
    - `_dispoAfterSaved` contiene `_clearCallScript()`.
    - `_startTelnyxCall` siembra: en el cuerpo aparece `_scriptDefaultId()` con
      `auto: true`.
    - `_selectScript` ya no hace push a un array propio y llama
      `window._setCallScript` con `append: true`.
    - `_renderScriptButtons` ya no declara `triggerOrder`/`triggerLabels`
      locales (usa las constantes del bloque).
    - `public/index.html` tiene `telnyx-call-script-wrap` y el `?v=` de `app.js`
      cambió respecto del valor previo al plan (asertar que existe el patrón
      `app.js?v=` y, si es simple hacerlo determinístico, que no es el valor
      viejo hardcodeado; si no, dejar solo la existencia y verificarlo a mano).
  </behavior>

  <action>
    Crear `tests/script-attribution-core.test.js` con dos `describe`: uno de
    fuente y otro de comportamiento.

    Para el aislamiento, copiar la técnica de `build()` de
    `tests/call-stage-surfaces.test.js` pero recortando por MARCADORES en vez de
    contar llaves: `appJs.indexOf('// ─── [35-02] SCR-ATTR: INICIO ───')` hasta
    el índice del marcador de FIN. Es más robusto que el balanceo de llaves y es
    exactamente para lo que se agregaron los marcadores.

    El `new Function` recibe los stubs como parámetros o los declara arriba del
    bloque, y devuelve al final un objeto con las funciones que hacen falta
    (`_scriptDefaultId`, `_scriptSelectHTML`, `_scriptOptionsHTML`,
    `_scriptIdsFor`, `_scriptPrimaryFor`, `_scriptIsAuto`, `setCall: window._setCallScript`,
    `setBanco: (arr) => { _callScriptsCache = arr; }`).

    Banco de prueba: al menos un `rules`, un `before_call`, dos `opener` y un
    `gatekeeper`, con un label que incluya `"` y `<` para el caso de escape.
  </action>

  <verify>
    <automated>npx vitest run tests/script-attribution-core.test.js</automated>
  </verify>

  <done>
    Suite verde. Los tests de comportamiento evalúan el bloque REAL extraído del
    archivo (no una copia), así que romper la precedencia del default o el modo
    append/replace los pone en rojo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `data/call_scripts.json` (editable por admin) → `innerHTML` del selector | `label` e `id` de cada guion se interpolan en atributos, en un `onchange` inline y en el texto de una `<option>` |
| `localStorage.scm_last_script_<userId>` → `_scriptDefaultId` | valor local, manipulable por quien tenga la consola del navegador |
| `_dispoScript` → body de `call-disposition` | lo que se manda como atribución sale de este estado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Tampering (XSS) | `_scriptOptionsHTML` / `_scriptSelectHTML` interpolan `label` e `id` de guiones en `innerHTML` | mitigate | Todo pasa por `escHtml`, incluido el id dentro del `onchange` inline (mismo criterio que `_stageChipsHTML` con `leadId`); test de fuente que exige la llamada a `escHtml` |
| T-35-06 | Spoofing / integridad de métrica | un id inventado en `localStorage` haría que `_scriptDefaultId` siembre basura | mitigate | `_scriptDefaultId` solo devuelve ids presentes en `_callScriptsCache`; y aunque pasara, el backend descarta lo que no está en el banco (whitelist de 35-01) |
| T-35-07 | Information disclosure | el selector expone la lista completa de guiones a cualquier rol con acceso a llamadas | accept | `GET /api/telnyx/scripts` ya es legible por admin y setter por diseño (el panel de guiones se lo muestra durante la llamada); no hay dato nuevo expuesto |
| T-35-08 | Denial of Service | `_ensureCallScripts` podría disparar un fetch por cada render de fila | mitigate | Promesa en vuelo compartida + corte temprano si el cache ya tiene guiones; el fetch es una sola vez por sesión |
| T-35-SC | Tampering | instalación de paquetes npm | mitigate | Este plan no instala nada. `git diff --stat package.json package-lock.json` vacío al cerrar |
</threat_model>

<verification>
1. `node --check public/app.js`.
2. `npx vitest run tests/script-attribution-core.test.js` — suite nueva verde.
3. `npx vitest run tests/call-stage-surfaces.test.js tests/script-attribution.test.js` — la etapa y el contrato de 35-01 sin regresión.
4. `npm test` completo contra el baseline de 35-01.
5. `git diff --stat public/style.css` vacío; el `?v=` de `app.js` en
   `public/index.html` cambió.
6. `grep -c "_telnyxCallState.scriptIdsUsed" public/app.js` → 0.
7. **Verificación por mutación**: invertir temporalmente la precedencia de
   `_scriptDefaultId` (poner el primer guion del banco antes que el `opener`) y
   confirmar que el test de "nunca el rules ni el before_call" se pone en rojo.
   Restaurar con `git checkout -- public/app.js`.
8. **Verificación en preview** (opcional pero recomendada, patrón del repo —
   login por fetch + inspección del DOM con javascript_tool, ver notas
   #124/#125 de CLAUDE.md): abrir Llamadas, confirmar que
   `document.querySelectorAll('select.script-select')` no rompe nada y que la
   consola queda limpia. La llamada Telnyx real NO se puede simular en preview.
</verification>

<success_criteria>
- Iniciar una llamada deja el lead con guion atribuido sin intervención, y el
  SDR lo ve en el panel de llamada.
- Hay un solo estado de atribución en el frontend.
- La atribución viaja en el body de `call-disposition` desde el helper
  compartido, y se apaga al guardar.
- El panel de guiones suma; los selectores reemplazan.
- Suite completa verde; cache-buster bumpeado; `style.css` intacto.
</success_criteria>

<output>
Crear `.planning/phases/35-scr-atribucion-guion/35-02-SUMMARY.md` al terminar.

Documentar explícitamente:
- El valor del cache-buster ANTES y DESPUÉS (leído de disco).
- La precedencia final de `_scriptDefaultId` tal como quedó, y si se desvió de
  la propuesta.
- El hash/fecha del commit de este plan: 35-04 lo necesita para fijar el
  `DEPLOY_ISO` del script de cobertura (antes de ese instante ninguna llamada
  pudo nacer con guion atribuido).
- Qué queda SIN verificar en vivo: la siembra durante una llamada Telnyx real
  (no hay WebRTC ni micrófono en el entorno de ejecución), y que el `<select>`
  del panel de llamada se vea bien sobre el fondo oscuro con el banco real de
  30 guiones.
</output>
