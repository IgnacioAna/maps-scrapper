---
phase: 20-disposicion-obligatoria
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: [tests/disposition-enforcement.test.js]
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03]

must_haves:
  truths:
    - "Los endpoints nuevos del enforcement tienen tests de regresión (registro pendiente, RBAC, auto-marca, corrección, auditoría, persistencia)"
    - "La suite completa queda verde — el enforcement no rompió ninguna métrica existente (metrics-consistency 19/19 incluida)"
  artifacts:
    - path: "tests/disposition-enforcement.test.js"
      provides: "regresión del backend de Phase 20"
      min_lines: 200
  key_links:
    - from: "tests/disposition-enforcement.test.js"
      to: "index.js"
      via: "supertest contra los endpoints con DATA_DIR temporal"
      pattern: "pending-calls"
---

<objective>
Tests backend del enforcement de disposición (success criterion 4 del ROADMAP:
"tests backend de cualquier endpoint nuevo") + suite completa verde antes de
cerrar la phase (regla transversal del milestone).

Purpose: los endpoints de 20-01 reescriben historia de métricas (corrección) y
miden el trabajo de personas reales (auditoría) — sin regresión automatizada,
cualquier refactor futuro los rompe en silencio.
Output: tests/disposition-enforcement.test.js (~16 tests) + suite completa verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-disposicion-obligatoria/20-CONTEXT.md
@.planning/phases/20-disposicion-obligatoria/20-01-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/disposition-enforcement.test.js</name>
  <files>tests/disposition-enforcement.test.js</files>
  <read_first>
    - tests/disposition-dnc.test.js (patrón de setup COMPLETO: DATA_DIR tmp + auth.json pre-populado ANTES de import("../index.js") + fixtures de leads + cookies por rol)
    - tests/call-cadence.test.js (fixtures de callLog/cadencia — la corrección interactúa con esto)
    - tests/metrics-consistency.test.js (solo el describe inicial — para confirmar que NO hay que tocarlo)
    - index.js: los 3 endpoints creados en 20-01 (leer el código REAL implementado, no asumir el plan)
    - CLAUDE.md notas #121 (API keys a "" NUNCA delete), #30 (timeouts/retry de vitest)
  </read_first>
  <action>
Crea `tests/disposition-enforcement.test.js` siguiendo el patrón exacto de
`tests/disposition-dnc.test.js`: `process.env.DATA_DIR = <tmpdir>`, `auth.json`
pre-populado con admin + 2 setters (cada uno con user vinculado y setterId) + 1
supervisor con `visibleSetterIds: [setterA]`, `setters.json` con leads fixture
(map, no array), API keys `process.env.OPENAI_API_KEY = ""` etc. (definidas
vacías, JAMÁS delete — regla #121), TODO antes de `await import("../index.js")`.
Login por endpoint real para obtener cookies por rol.

Tests (16, nombres en español descriptivos):

**Registro pendiente (D-01/D-02):**
1. `POST /api/setters/pending-calls` crea el registro; `GET` como dueño lo devuelve con `setterId` del auth (no del body).
2. Setter B haciendo POST sobre un lead de setter A → 403. Setter B haciendo GET no ve los pendientes de A.
3. Upsert: segundo POST con mismo leadId+startedAt actualiza (`endedAt`, `durationSecs`, `reachedActive`) sin duplicar — GET devuelve 1 solo registro.
4. `canceled:true` elimina solo si `endedAt` es null y el registro tiene <2 min; con `endedAt` seteado NO borra (registro sigue en GET).
5. Admin GET ve todos; `?setter=` filtra; supervisor scoped SOLO ve los del setter visible.

**Resolución (D-01):**
6. call-disposition con `pendingCallId` correcto resuelve el pendiente (GET vacío después). Con `pendingCallId` de OTRO lead → NO resuelve ese registro.
7. call-disposition con `telnyxCallMeta.startedAt` que matchea resuelve sin pendingCallId; sin meta ni id resuelve el MÁS RECIENTE del lead.

**Auto-marca y corrección (D-03):**
8. `{outcome:'no_answer', autoMarked:true}` → el callLog entry tiene `autoMarked === true` y `preCadence` con snapshot (estado/callbackAt/cadenceStep). Con `{outcome:'answered_interested', autoMarked:true}` el entry NO tiene autoMarked.
9. Corrección simple: auto-marca no_answer → `{outcome:'voicemail', correctsAutoMarked:true}` → `callLog.length` sigue en 1, outcome 'voicemail', `callAttempts` 1, `phoneStatus` 'voicemail', y el entry conserva `ts`/`duration` del original.
10. Corrección que revierte auto-descarte: lead con 1 no_answer previo real + auto-marca no_answer (streak 2 → `autoDiscarded:true`, estado 'descartado') → corrección a `answered_interested` → estado 'interesado', `autoDiscarded` false, `callbackAt` restaurado del snapshot, callLog.length 2.
11. `correctsAutoMarked` sin auto-marca previa (último entry manual) → 409 con `error` 'No hay auto-marca reciente para corregir.'.
12. `correctsAutoMarked` con auto-marca de hace >15 min (fabricar `ts` viejo en el fixture) → 409.

**Auditoría (D-06):**
13. RBAC: setter → 403; admin → 200; supervisor scoped ve SOLO su setter visible en `bySetter`.
14. Reglas de sospecha: no_answer con duration 45 → `longNoContact` 1; answered_interested con duration 5 → `shortConnect` 1; no_answer con duration 20 → 0 sospechosas; entry sin duration (channel manual) → 0 sospechosas.
15. `pctMarked`: fixture con 2 entries channel 'telnyx_webrtc' en rango + 1 pendiente sin resolver del mismo setter → `pctMarked` 67; setter sin dials ni pendientes NO aparece en bySetter.

**Persistencia (regla #21):**
16. `GET /api/admin/export-data` incluye bloque `pending_calls`; `POST /api/admin/import-data` con ese bloque lo restaura (round-trip: crear pendiente → export → borrar archivo → import → GET lo devuelve).

Notas de implementación: para fixtures de callLog con `duration`, crear los
entries vía el endpoint real con `telnyxCallMeta` (no escribir el JSON a mano)
salvo donde el test necesita `ts` viejo (ahí mutar el archivo con
fs.readFile/writeFile ANTES del request, como hace call-cadence.test.js).
Timestamps SIEMPRE relativos a `Date.now()` (nada de fechas hardcodeadas — la
suite corre en cualquier fecha/TZ, lección de metrics-timezone).
  </action>
  <verify>
    <automated>npx vitest run tests/disposition-enforcement.test.js 2>&1 | grep -Eq "1[6-9] passed|2[0-9] passed"</automated>
  </verify>
  <done>
    - El archivo tiene >= 16 tests y todos pasan.
    - Setup: `grep -n 'DATA_DIR' tests/disposition-enforcement.test.js` aparece ANTES del import de index.js; `grep -c 'API_KEY = ""' tests/disposition-enforcement.test.js` >= 1; ningún `delete process.env` en el archivo.
    - Cubre los 4 grupos: pendientes/RBAC, resolución, auto-marca/corrección, auditoría, export round-trip.
  </done>
</task>

<task type="auto">
  <name>Task 2: Suite completa verde</name>
  <files>tests/disposition-enforcement.test.js</files>
  <read_first>
    - CLAUDE.md nota #121 (suite 726+ estable, patrón de flakies resueltos) y STATE.md (base actual: 848/848 tras Phase 19)
  </read_first>
  <action>
Correr `npm test` (suite completa). Criterio: TODOS los tests verdes — base
848 de Phase 19 + los ~16 nuevos. Atención especial (deben pasar SIN
modificarlos):
- `tests/metrics-consistency.test.js` (19) — garantía de que la auditoría no divergió el funnel.
- `tests/disposition-dnc.test.js`, `tests/call-cadence.test.js`, `tests/shared-callback.test.js`, `tests/pipeline-attribution.test.js` — el endpoint call-disposition extendido no cambió la semántica existente.
- `tests/weekly-report.test.js` (12) — el bloque nuevo de export-data no rompió el shape.

Si algo falla: es un BUG de 20-01 (o del test nuevo) — arreglar el código del
enforcement o el test nuevo, JAMÁS adaptar un test pre-existente para que pase
(si un test viejo falla, la implementación rompió un contrato — corregir
index.js). Documentar en el SUMMARY el conteo final (ej. 864/864).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -20 | grep -Eq "Tests .*passed" && ! (npm test 2>&1 | grep -q " failed")</automated>
  </verify>
  <done>
    - `npm test` termina con 0 failed (conteo esperado: 848 base + >=16 nuevos).
    - Ningún test pre-existente fue modificado: `git diff --name-only tests/` muestra SOLO tests/disposition-enforcement.test.js.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests → index.js | los tests validan los guards de seguridad de 20-01 (son la evidencia de las mitigaciones) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | E | RBAC de los 3 endpoints | mitigate | tests 2, 5 y 13 verifican 403 de setter ajeno, scoping supervisor y setter en audit |
| T-20-10 | T | corrección/canceled como vía de manipulación | mitigate | tests 4, 11 y 12 verifican las ventanas (2 min / 15 min) y el 409 |
| T-20-11 | R | pérdida de pendientes en redeploy | mitigate | test 16 (export/import round-trip) |
</threat_model>

<verification>
- `npx vitest run tests/disposition-enforcement.test.js` → >=16 passed.
- `npm test` → suite completa verde, 0 failed.
- `git diff --name-only tests/` → solo el archivo nuevo.
</verification>

<success_criteria>
- Regresión completa del backend del enforcement: cualquier cambio futuro que rompa el registro pendiente, la corrección o la auditoría hace fallar la suite.
- Suite completa verde (base 848 + nuevos) — la phase no deja deuda de tests.
</success_criteria>

<output>
Al completar, crear `.planning/phases/20-disposicion-obligatoria/20-02-SUMMARY.md`
</output>
