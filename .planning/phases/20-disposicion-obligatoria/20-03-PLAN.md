---
phase: 20-disposicion-obligatoria
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: [public/app.js, public/index.html, public/style.css]
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03]

must_haves:
  truths:
    - "Con una llamada con contacto recién colgada sin marcar, NINGÚN punto de discado inicia otra llamada y un banner persistente dice qué llamada falta marcar (D-01)"
    - "Una llamada sin señal de atención (ni active ni voz sostenida) queda marcada No atendió automáticamente y el SDR puede corregirla en 1 gesto (D-03)"
    - "Al abrir el panel con llamadas sin marcar (tab cerrado/crash), aparece una franja NO bloqueante 'Tenés N llamadas sin marcar' con acceso directo a resolverlas — la SDR puede seguir discando (D-02)"
    - "El autopiloto y los atajos 1-9 del Power Dialer siguen funcionando (success criterion 3)"
    - "La ventana de 10 min del audio y la cadena de grabación quedan INTACTAS (D-04)"
    - "Admin/supervisor ven en Equipo la distribución de resultados por SDR + sospechosas + % marcada (D-06)"
  artifacts:
    - path: "public/app.js"
      provides: "gate _dispoGate en _startTelnyxCall, auto-marca, banner, franja de pendientes, auditoría en Equipo"
      contains: "_dispoGate"
    - path: "public/index.html"
      provides: "contenedores #dispo-pending-strip y #team-dispo-audit + cache-busters bumpeados"
      contains: "app.js?v=20260725b"
    - path: "public/style.css"
      provides: "estilos .dispo-gate-banner / .dispo-strip (minimalistas, sin emojis)"
      contains: "dispo-gate-banner"
  key_links:
    - from: "public/app.js _startTelnyxCall"
      to: "_dispoGate"
      via: "guard al inicio — cubre lista/Hoy/PD/autopiloto/tecla C/alt-contact/ad-hoc en UN punto"
      pattern: "if \\(_dispoGate\\)"
    - from: "public/app.js _onTelnyxCallEnded"
      to: "/api/setters/pending-calls"
      via: "upsert al colgar + auto-marca o gate según reachedContact"
      pattern: "pending-calls"
    - from: "los 5 POST de call-disposition"
      to: "_dispoAfterSaved"
      via: "clear del gate + refresh de la franja en cada disposición exitosa"
      pattern: "_dispoAfterSaved"
    - from: "public/app.js _teamLoad"
      to: "/api/setters/disposition-audit"
      via: "fetch con apiUrl() (regla #146 — jamás fetch crudo en vistas de supervisor)"
      pattern: "disposition-audit"
---

<objective>
Enforcement en el frontend: bloqueo de discado en vivo con banner (D-01),
auto-marca determinística de no-contactos con corrección (D-03), franja
recordatorio NO bloqueante de pendientes (D-02) y auditoría visible en Equipo
(D-06). Cache-buster bumpeado y verificación en preview.

Purpose: cierra la fuga — sin marcar no hay próxima llamada; lo que escapa al
vivo cae en la cola; el no-contacto se marca solo. La única traba dura es
"marcá la que acabás de cortar" (1 clic con memoria fresca — specifics del
CONTEXT).
Output: public/app.js + index.html + style.css con el enforcement completo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-disposicion-obligatoria/20-CONTEXT.md
@.planning/phases/20-disposicion-obligatoria/20-01-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Extraído del código real (2026-07-25). Líneas aproximadas de public/app.js. -->

```js
// 7939  window._startTelnyxCall = async (leadId, phoneOverride) => { ... }
//       — ÚNICO punto de inicio de llamada. Guard existente: if (_telnyx.activeCall) return (7948).
//       — 8065-8067: const call = _telnyx.client.newCall(_callOpts); _telnyx.activeCall = call; _telnyxCallState.startedAt = Date.now();
//       — catch (8096): startCall failed → _closeTelnyxCallPanel + toast.
// 8105  function _onTelnyxCallEnded(reason) — anti-doble-disparo con _telnyxCallState.ended.
//       — durationSecs/attemptedSecs calculados al inicio (8113-8114).
//       — setTimeout 500ms (8136): si attemptedSecs >= 1 → arma _pendingTelnyxCallMetadata[leadId] (8159) y foco al dropdown.
// 2446  _enterActiveOnce() → _telnyxCallState.enteredActive = true (señal 'active' del SDK).
// 2472-2476  early-media: _startRemoteVoiceWatch → _telnyxCallState.committedRemote = true (voz sostenida SIN 'active').
//       ⚠️ Comentario del código (8109): "este carrier no señaliza 'atendió' de forma fiable (todo viene por early-media)".
// 8237  const _pendingTelnyxCallMetadata = {}; — 8240 _consumeTelnyxMeta(leadId) devuelve y borra.
// 8255  _finalizeActiveCallBeforeDisposition(leadId) — cuelga si marca sin colgar.
// 8589  window._handleCallDisposition = async (leadId, selectEl) — POST directo en 8632; éxito: _leadStoreApply (8651) + renderCallsList + renderCallsStats.
// Los 5 POST a call-disposition: 8632 (directo), 8765 (modal callback), 8910 (modal objeción), 8978 (modal agendar), 9006 (fallback interesado del modal agendar).
// 6157  window._pdHandleDisposition — post-save: _pd.autopilot ? _pdAdvance() : (holdCurrent=true + holdOutcome + _pdRender()) (6169-6176).
// 6288  _pdKeyOutcomes = ['answered_interested','scheduled_with_admin','answered_not_interested','hung_up','no_answer','voicemail','callback_later','wrong_number','invalid_number'] — NO TOCAR (regla #156).
// 5446  _pdStartAutopilotCountdown() — 5466: if (!_telnyx?.activeCall) window._startTelnyxCall?.(lead.id);
// 6255-6272  discado ad-hoc: ghost lead con _isManualDial: true, id 'manual_...' → _startTelnyxCall(ghostId).
// 5200  async function loadCallsView() — boot de la vista Llamadas (donde engancha la franja).
// 14879 async function _teamLoad() — vista Equipo (donde engancha la auditoría). ⚠️ usar apiUrl() SIEMPRE (regla #146).
// 4933  _leadStoreApply(id, patch) — escritura única de estado de lead (regla #105).
// 7530  ventana de 10 min del buffer de audio (_pendingTranscribe) — NO TOCAR (D-04).
// index.html:14   style.css?v=20260724a   → bumpear a v=20260725a
// index.html:3382 app.js?v=20260725a      → bumpear a v=20260725b
```

Endpoints de 20-01 (leer el SUMMARY para el shape final):
```
POST /api/setters/pending-calls   { leadId, startedAt, endedAt?, durationSecs?, reachedActive?, canceled? } → { ok, id }
GET  /api/setters/pending-calls   → { pending: [{ id, leadId, leadName, setterId, startedAt, endedAt, durationSecs, fromNumber, reachedActive }] }
POST /api/setters/leads/:id/call-disposition  body extra: autoMarked, correctsAutoMarked, pendingCallId
GET  /api/setters/disposition-audit?period=today|7d|30d|all&setter=  → { bySetter: [...], totals }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Gate en vivo + auto-marca de no-contactos + corrección (D-01 + D-03)</name>
  <files>public/app.js</files>
  <read_first>
    - public/app.js:7939-8101 (_startTelnyxCall completo — dónde va el guard y el registro del pendiente)
    - public/app.js:8105-8232 (_onTelnyxCallEnded — dónde se bifurca auto-marca vs gate)
    - public/app.js:2442-2478 (enteredActive vs committedRemote — la señal de contacto)
    - public/app.js:8589-8660 (_handleCallDisposition) y 6157-6206 (_pdHandleDisposition post-save)
    - public/app.js:7521-7534 (buffer 10 min — SOLO leer, PROHIBIDO modificar: D-04, saga Whisper)
    - 20-CONTEXT.md D-01/D-03 completos
  </read_first>
  <action>
Todo en public/app.js, dentro del scope del módulo de llamadas (junto a
`_telnyxCallState`). UI minimalista SIN emojis decorativos.

1. **Estado del gate**:
   ```js
   let _dispoGate = null; // { leadId, leadName, startedAt } | null
   let _lastAutoMark = null; // { leadId, at } — habilita correctsAutoMarked 15 min
   ```
   Helpers:
   - `_dispoGateSet(leadId, leadName, startedAtIso)`: setea `_dispoGate`, lo persiste en `localStorage['scm_dispo_gate_' + (currentUser?.id || 'anon')]` (JSON), y muestra el banner.
   - `_dispoGateClear(leadId)`: si `_dispoGate?.leadId === leadId` (o leadId null = forzar), limpia estado + localStorage + oculta banner.
   - `_dispoAfterSaved(leadId)`: `_dispoGateClear(leadId)`; si `_lastAutoMark?.leadId === leadId` limpiar `_lastAutoMark`; refrescar la franja de pendientes (`_dispoLoadPendingStrip?.()` fire-and-forget, definida en Task 2 — usar optional call para que Task 1 funcione solo).
   - `_dispoEnforcementBody(leadId)`: devuelve `{}` o `{ correctsAutoMarked: true }` si `_lastAutoMark?.leadId === leadId && Date.now() - _lastAutoMark.at < 15*60*1000`. (Task 2 lo extiende con pendingCallId de la franja.)

2. **Banner persistente** (D-01: "banner persistente señala QUÉ llamada falta
   marcar; SIN modal que tape la pantalla"): div creado una sola vez y appendeado
   a `document.body`, id `dispo-gate-banner`, clase `.dispo-gate-banner`
   (estilos en Task 2), `position:fixed; bottom:18px; left:50%;
   transform:translateX(-50%); z-index:9000`. Contenido:
   `Marcá el resultado de la llamada a {leadName} para seguir discando` +
   botón `Ir a marcar`. El botón: click en `[data-target="view-calls"]`,
   `_callsForceShow.add(leadId)`, `renderCallsList()`, y scroll+focus a
   `.call-row[data-id="{leadId}"] select` (mismo patrón de retry que
   `_focusDispositionRow` de _onTelnyxCallEnded: reintentar hasta 12 veces cada
   400ms). Helper `_dispoGateFlashBanner()`: pulso visual (box-shadow accent
   400ms, como el flash de la call-row).

3. **Guard en `_startTelnyxCall`** (después del check `_telnyx.activeCall`,
   línea ~7951):
   ```js
   if (_dispoGate) {
     window.showToast?.('Marcá el resultado de la llamada a ' + _dispoGate.leadName + ' antes de discar otra', { type: 'warn', duration: 4000 });
     _dispoGateFlashBanner();
     return;
   }
   ```
   Bloquea TODO discado (incluido re-discar el mismo lead — D-01: "no se puede
   iniciar OTRA llamada"; marcar toma 1 clic). Un solo guard cubre lista, Hoy,
   Power Dialer, autopiloto, tecla C, alt-contact, Ctrl+K y ad-hoc porque TODOS
   pasan por `_startTelnyxCall` (verificado: call sites en 5146, 5466, 5845,
   5984, 6272, 6301, 6761, 7147).
   Además en `_pdStartAutopilotCountdown` (~5446), primera línea:
   `if (_dispoGate) return;` (no armar countdown que va a chocar con el guard).

4. **Registro del pendiente al INICIAR** (D-02 — sobrevive crash del tab):
   inmediatamente después de `_telnyxCallState.startedAt = Date.now();` (~8067),
   si el lead NO es ad-hoc (`!lead._isManualDial`):
   ```js
   fetch(apiUrl('/api/setters/pending-calls'), { method:'POST', credentials:'include', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ leadId, startedAt: new Date(_telnyxCallState.startedAt).toISOString() }) }).catch(() => {});
   ```
   En el `catch` de startCall (~8096), si ya se había registrado: mismo POST con
   `canceled: true` (la llamada nunca existió). Los ad-hoc no crean pendiente ni
   disposición (no hay lead real — resolución simple coherente con D-01: el
   gate igual les aplica al INICIAR porque pasan por _startTelnyxCall).

5. **Bifurcación al colgar** — en `_onTelnyxCallEnded`, dentro del `setTimeout`
   de 500ms existente, DESPUÉS de armar `_pendingTelnyxCallMetadata` (~8160).
   Calcular ANTES del setTimeout (con `_telnyxCallState` aún vivo):
   `const reachedContact = !!(_telnyxCallState.enteredActive || _telnyxCallState.committedRemote);`
   Para leads reales (`!_callsLeadsById.get(leadId)?._isManualDial`):
   - Si `attemptedSecs < 1`: POST pending-calls `{ leadId, startedAt, canceled: true }` (llamada fallida — hoy tampoco pide disposición) y NADA más.
   - Si no: POST pending-calls upsert `{ leadId, startedAt, endedAt, durationSecs, reachedActive: reachedContact }` (fire-and-forget). Luego:
     * `reachedContact === false` → `_autoMarkNoAnswer(leadId)` (punto 6). ⚠️ La condición usa `enteredActive || committedRemote`, NO solo 'active': el comentario de 8109 y la rama early-media (2472) confirman que este carrier entrega llamadas ATENDIDAS sin pasar por 'active' — auto-marcar esas como no_answer corrompería el dato (contra el espíritu de D-06). D-03 se implementa conservador: solo se auto-marca cuando NO hubo ni 'active' ni voz sostenida. Documentar esto en comentario.
     * `reachedContact === true` → `_dispoGateSet(leadId, leadName, startedAtIso)` (hubo contacto real: el SDR marca a mano, y hasta que marque no disca — D-01). El flujo existente de foco al dropdown queda como está.

6. **`_autoMarkNoAnswer(leadId)`** (async, D-03):
   - `const telnyxMeta = _consumeTelnyxMeta(leadId);` body
     `{ outcome:'no_answer', autoMarked:true, ...(telnyxMeta && { telnyxCallMeta: telnyxMeta }) }` →
     POST call-disposition (credentials include). El backend resuelve el
     pendiente por startedAt del meta.
   - **NO llamar `_flushPendingTranscription`** (D-04: el buffer conserva su
     timer propio de 10 min; si el SDR corrige a un outcome con conversación,
     el flujo normal de corrección lo flushea — dejar comentario explícito).
   - Éxito: `data.lead && _leadStoreApply(leadId, data.lead)` (regla #105);
     `_lastAutoMark = { leadId, at: Date.now() }`; `renderCallsList();
     renderCallsStats();` toast `'No atendió — marcado automático. Corregilo si hubo contacto (ej. Buzón).'`
     Si `_pd.active && _pd.queue[_pd.currentIdx] === leadId`: replicar el
     post-save de `_pdHandleDisposition` (6169-6176): autopilot → `_pdAdvance()`;
     sino `_pd.holdCurrent = true; _pd.holdOutcome = 'No atendió (auto)'; _pdRender();`.
     El foco al dropdown existente queda (sirve para corregir con las teclas 1-9
     del shortcut de 30s).
   - Error (red caída): fallback a `_dispoGateSet(...)` — la llamada no queda
     en limbo, el SDR la marca a mano.

7. **Corrección + clear en los 5 POST de call-disposition**: en los bodies de
   los 5 call sites (8632 directo, 8765 callback, 8910 objeción, 8978 agendar,
   9006 fallback interesado) mergear `..._dispoEnforcementBody(leadId)`. En los
   5 success paths (tras `resp.ok`), llamar `_dispoAfterSaved(leadId)`.
   El backend, ante `correctsAutoMarked`, reemplaza la auto-marca (mismo dial)
   — no hace falta lógica extra acá; ante 409 (auto-marca vieja) el flujo sigue
   normal porque el 409 solo ocurre si el flag viajó fuera de ventana, y
   `_dispoEnforcementBody` ya respeta los 15 min client-side (defensa doble).

8. **Restore tras refresh**: en el boot del módulo de llamadas (cerca de la
   primera `loadCallsView()`), leer el localStorage del gate; si existe,
   validar contra `GET /api/setters/pending-calls`: si hay un pendiente con ese
   leadId + startedAt sin resolver → re-armar `_dispoGate` (banner incluido);
   si no (ya se marcó en otro tab / se resolvió) → limpiar localStorage.

PROHIBIDO en este task: tocar `_pdKeyOutcomes` (regla #156), el shortcutMap
1-9, `_syncCallRecording`/`_recBindChannel`/buffer de transcripción (D-04,
saga Whisper), y `_autoDispositionLLM` (es la sugerencia IA, NO la auto-marca
determinística — D-03 nota explícita).
  </action>
  <verify>
    <automated>node --check public/app.js && test $(grep -c "_dispoGate" public/app.js) -ge 8 && grep -q "_autoMarkNoAnswer" public/app.js && grep -q "committedRemote" public/app.js && test $(grep -c "_dispoAfterSaved" public/app.js) -ge 6 && grep -c "_pdKeyOutcomes = \[" public/app.js | grep -qx "1"</automated>
  </verify>
  <done>
    - Guard `if (_dispoGate)` presente en `_startTelnyxCall` (entre el check de activeCall y el fetch de config) y `_pdStartAutopilotCountdown` retorna temprano con gate activo.
    - La condición de auto-marca es `!(enteredActive || committedRemote)` — grep muestra `committedRemote` en la bifurcación de `_onTelnyxCallEnded`.
    - `_dispoAfterSaved` invocado en los 5 success paths de call-disposition + en `_autoMarkNoAnswer` (>= 6 ocurrencias del identificador incluyendo la definición → grep >= 6, definición + 6 calls = 7).
    - `_autoMarkNoAnswer` NO contiene `_flushPendingTranscription` (grep del cuerpo de la función).
    - Cero cambios en las líneas 7521-7534 (buffer 10 min): `git diff public/app.js` no toca ese rango.
    - `_pdKeyOutcomes` intacto (1 sola definición, mismo array de 9).
  </done>
</task>

<task type="auto">
  <name>Task 2: Franja de pendientes NO bloqueante + estilos (D-02)</name>
  <files>public/app.js, public/index.html, public/style.css</files>
  <read_first>
    - public/app.js:5200-5260 (loadCallsView — dónde engancha la carga de la franja)
    - public/index.html: estructura de view-calls (buscar id="view-calls" — dónde insertar el contenedor, arriba de filtros/lista)
    - 20-CONTEXT.md D-02 COMPLETO — cita del user: "el criterio lo tiene que manejar el SDR... si no se va a ver muy trabado". PROHIBIDO convertir la franja en bloqueo.
    - CLAUDE.md memoria "UI minimalista sin emojis"
  </read_first>
  <action>
1. **Contenedor en index.html**: `<div id="dispo-pending-strip" class="dispo-strip hidden"></div>`
   dentro de view-calls, arriba de la zona de filtros/lista (primer hijo después
   del header de la vista).

2. **`_dispoLoadPendingStrip()`** en app.js (async, fire-and-forget): GET
   `apiUrl('/api/setters/pending-calls')`; filtrar client-side a los del user
   (`p.setterId === currentUser?.setterId` — para admin/supervisor la franja
   también muestra SOLO los propios; los ajenos se ven en la auditoría de
   Equipo, Task 3). EXCLUIR el pendiente cubierto por el gate activo
   (`_dispoGate` con mismo leadId+startedAt) — ese ya tiene banner. Si quedan 0
   → ocultar franja. Si N>0 → render:
   - Línea principal: `Tenés N llamada(s) sin marcar` + botón `Resolver` que
     expande/colapsa la lista inline.
   - Item por pendiente: `{leadName} · {hora local de startedAt} · {durationSecs formateado m:ss o '—'}` + botón `Marcar` →
     `_dispoStripGoResolve(pendingId)`.
   - SIN lógica de bloqueo: la franja NO deshabilita nada, NO intercepta
     `_startTelnyxCall`, NO abre modales solos (D-02 es recordatorio; comentario
     en el código citando la decisión del user).
3. **`_dispoStripGoResolve(pendingId)`**: busca el record en el cache de la
   franja; stashea
   `_dispoStripPending = { pendingId, leadId, meta: { durationSecs, fromNumber, startedAt, endedAt } }`;
   `_callsForceShow.add(leadId)`; si la vista actual no es Llamadas, click a
   `[data-target="view-calls"]`; `renderCallsList()`; scroll+focus al select de
   `.call-row[data-id]` (mismo retry-pattern del banner de Task 1). El SDR marca
   con el dropdown NORMAL (reusa el 100% del flujo existente, modales incluidos).
   Extender `_dispoEnforcementBody(leadId)` (Task 1): si
   `_dispoStripPending?.leadId === leadId`, agregar `pendingCallId` y, SOLO si
   `_consumeTelnyxMeta` no aportó meta fresca (el body del call site no trae
   `telnyxCallMeta`), agregar `telnyxCallMeta: _dispoStripPending.meta` (así el
   callLog de una llamada vieja conserva duration/fromNumber reales y la
   auditoría D-06 puede cruzar duración). Limpiar `_dispoStripPending` en
   `_dispoAfterSaved`.
   Nota: `_dispoEnforcementBody` se invoca en los 5 call sites (ya cableado en
   Task 1) — acá solo se extiende su retorno.
4. **Enganches de carga**: llamar `_dispoLoadPendingStrip()` al final de
   `loadCallsView()` (fire-and-forget, no bloquea el render de la lista) y en
   `_dispoAfterSaved` (refresh del contador tras cada disposición).
5. **Estilos en style.css** (bloque nuevo al final, comentario `/* Phase 20:
   disposición obligatoria */`):
   - `.dispo-gate-banner`: fondo `var(--bg-surface)`, borde 1px `var(--accent)`,
     border-radius 10px, padding 12px 18px, sombra suave, texto
     `var(--text-primary)` 13px, botón con acento. Sin animaciones pesadas, sin
     backdrop-filter (regla #6 de bugs Telnyx).
   - `.dispo-strip`: franja full-width, fondo ámbar sutil
     (`rgba(240,180,60,0.10)`), borde 1px `rgba(240,180,60,0.35)`,
     border-radius 8px, padding 10px 14px, margin-bottom 12px.
   - `.dispo-strip-item`: fila con nombre/hora/duración + botón chico.
   - CERO emojis en todos los textos.
  </action>
  <verify>
    <automated>node --check public/app.js && grep -c "dispo-pending-strip" public/index.html | grep -qx "1" && grep -q "_dispoLoadPendingStrip" public/app.js && grep -q "_dispoStripPending" public/app.js && grep -q "dispo-gate-banner" public/style.css && grep -q "dispo-strip" public/style.css</automated>
  </verify>
  <done>
    - La franja se carga en `loadCallsView()` y se refresca en `_dispoAfterSaved` (grep de `_dispoLoadPendingStrip` >= 3: definición + 2 llamadas).
    - `_dispoStripGoResolve` NO contiene POST a call-disposition (delegación total al flujo existente — grep dentro de la función).
    - En la franja no existe ninguna llamada a `_startTelnyxCall` ni disabled de botones de discado (no bloqueante — leer el código).
    - Ningún emoji en los strings nuevos de banner/franja (`grep -nE "Tenés .* sin marcar" public/app.js` muestra texto plano).
  </done>
</task>

<task type="auto">
  <name>Task 3: Auditoría en Equipo + cache-buster + verificación en preview (D-06)</name>
  <files>public/app.js, public/index.html</files>
  <read_first>
    - public/app.js:14879-14990 (_teamLoad — período seleccionado y render de la vista Equipo)
    - public/index.html: estructura de view-team (dónde insertar #team-dispo-audit) y líneas 14 + 3382 (cache-busters)
    - CLAUDE.md notas #146 (apiUrl SIEMPRE en vistas visibles para supervisor — 2 veces mordió), #48 (cache-buster obligatorio), #15 (preview con DATA_DIR=tmp/preview-data)
  </read_first>
  <action>
1. **Contenedor**: `<div id="team-dispo-audit"></div>` en index.html al final de
   view-team (después de la tabla/alertas existentes).

2. **Render en `_teamLoad`**: tras el render existente, fetch
   `apiUrl('/api/setters/disposition-audit?period=' + p)` donde `p` mapea el
   período que ya usa la vista Equipo (day→today, week→7d, month→30d; si Equipo
   usa otros literales, leer `_teamLoad` y mapear a los que acepta
   `_ccResolveRange`). ⚠️ `apiUrl()` OBLIGATORIO (regla #146: fetch crudo rompe
   "Ver como" — ya pasó dos veces).
   Sección "Auditoría de disposiciones" (título simple, sin emojis):
   - Tabla por SDR: columnas `SDR · Llamadas · % marcada · Distribución · Sospechosas`.
     Distribución compacta: top 3 outcomes con label humano (`callOutcomeLabel`
     ya existe en app.js) y count, ej. `No atendió 45 · Buzón 12 · Interesado 3`.
     `% marcada`: `pctMarked` del endpoint; `null` → `—`.
   - Sospechosas: count total; si > 0, `<details>` nativo con los samples
     (`{leadName} · {fecha corta} · {label outcome} · {duration}s · {regla}`),
     regla en humano: `no-contacto >30s` / `contacto <10s`.
   - Si `bySetter` viene vacío → no renderizar la sección (nada de tablas
     vacías).
   - Nota al pie chica (text-muted): `Las llamadas del canal manual no tienen duración y no entran al cruce.` (transparencia del sesgo, mismo criterio que REP-10).

3. **Cache-buster** (regla #48 — SIN excepción): en index.html,
   `app.js?v=20260725a` → `app.js?v=20260725b` (línea 3382) y
   `style.css?v=20260724a` → `style.css?v=20260725a` (línea 14; style.css se
   tocó en Task 2). `/api/version` lee el buster de app.js al boot → el banner
   de versión (#152) avisa solo a los tabs viejos.

4. **Verificación en preview** (documentada — el frontend no tiene framework de
   tests): levantar el preview (DATA_DIR=tmp/preview-data per nota #15 de
   CLAUDE.md; copiar data/*.json a tmp/preview-data si hace falta y resetear
   pass admin AHÍ, jamás en data/). Checklist (ejecutar via browser/DOM o
   javascript_tool según el workaround del pane conocido en #114/#125):
   a. Login admin → view-calls carga sin errores de consola.
   b. Consola: `window._leadStoreApply` existe; simular gate:
      setear `_dispoGate` via el flujo (o exponer `window.__dispoTest = { set: _dispoGateSet, clear: _dispoGateClear }` TEMPORAL — quitar antes de commitear, o dejarlo solo si es inocuo y documentarlo) → banner visible con el texto correcto → `_startTelnyxCall('<leadId real>')` retorna con toast de bloqueo (0 llamadas iniciadas).
   c. Sembrar `tmp/preview-data/pending_calls.json` con 2 registros del setter del admin → recargar → franja "Tenés 2 llamadas sin marcar" visible, expandible, botón Marcar navega y foca el dropdown de la row. Marcar una → POST sale con `pendingCallId` (Network tab / log del server) → franja pasa a 1.
   d. Equipo: sembrar callLog con un no_answer duration 45 y un answered_interested duration 5 (editar tmp/preview-data/setters.json) → sección auditoría muestra 2 sospechosas con las reglas correctas y % marcada coherente.
   e. Power Dialer abre, atajos 1-9 y S/C/B responden, autopiloto togglea (A) — success criterion 3.
   f. `fetch('/app.js?v=20260725b')` devuelve el archivo nuevo (cache-buster efectivo).
   Registrar los resultados del checklist en el SUMMARY (qué se verificó y cómo).

5. **Nota para el deploy** (va en el SUMMARY, no en código): recordar al user
   que las SDRs deben recargar el tab UNA vez (el banner de versión avisa);
   la regla arranca de cero — llamadas viejas sin marcar no aparecen (D-05).
  </action>
  <verify>
    <automated>node --check public/app.js && grep -c "app.js?v=20260725b" public/index.html | grep -qx "1" && grep -c "style.css?v=20260725a" public/index.html | grep -qx "1" && grep -c "team-dispo-audit" public/index.html | grep -qx "1" && grep -q "disposition-audit" public/app.js && grep -n "disposition-audit" public/app.js | head -1</automated>
  </verify>
  <done>
    - El fetch de disposition-audit usa `apiUrl(` (grep de la línea lo confirma — NUNCA fetch crudo).
    - Cache-busters bumpeados: app.js `v=20260725b`, style.css `v=20260725a` (exactamente 1 ocurrencia de cada uno en index.html).
    - La sección de auditoría no se renderiza con bySetter vacío (leer el guard).
    - Checklist de preview ejecutado y documentado en el SUMMARY (items a-f con resultado).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM/console del SDR → gate client-side | el bloqueo en vivo es client-side (bypasseable por consola) |
| view-team (supervisor) → disposition-audit | datos de rendimiento de otras personas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-12 | E | gate _dispoGate client-side | accept | un SDR con DevTools puede saltar el gate, pero la llamada igual crea un pendiente server-side → queda visible en la cola y baja su pctMarked en la auditoría — el server es la fuente de verdad, el gate es UX |
| T-20-13 | I | franja de pendientes | mitigate | client-side filtra a los propios y el backend (20-01) ya scopea por setterId — doble capa |
| T-20-14 | I | auditoría en Equipo | mitigate | endpoint 403 para setters; fetch con apiUrl() respeta "Ver como" y scoping supervisor |
| T-20-15 | T | meta de la franja (telnyxCallMeta del record) | accept | el SDR podría alterar duration via consola; el CDR real de Telnyx (reconcile-costs) y la auditoría de patrón por SDR acotan el impacto — mismo nivel de confianza que el telnyxCallMeta actual |
</threat_model>

<verification>
- `node --check public/app.js` pasa (ESM, package.json type:module).
- Gates grep de los 3 tasks pasan.
- Preview checklist a-f documentado en el SUMMARY.
- `npm test` sigue verde (el frontend no toca tests, smoke de sanidad).
</verification>

<success_criteria>
- D-01: con gate activo, ningún punto de discado inicia llamada; banner dice qué llamada falta.
- D-02: la franja aparece con pendientes, resuelve con acceso directo y JAMÁS bloquea.
- D-03: no-contacto real (ni active ni voz sostenida) queda auto-marcado no_answer, corregible 15 min sin duplicar el dial.
- D-04: rango 7521-7534 de app.js y cadena de grabación sin un solo diff.
- D-06: Equipo muestra distribución + sospechosas + % marcada por SDR.
- Power Dialer: autopiloto y atajos 1-9 intactos (verificado en preview).
- Cache-busters bumpeados (app.js v=20260725b, style.css v=20260725a).
</success_criteria>

<output>
Al completar, crear `.planning/phases/20-disposicion-obligatoria/20-03-SUMMARY.md`
</output>
