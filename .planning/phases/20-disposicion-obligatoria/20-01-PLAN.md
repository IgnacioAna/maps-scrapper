---
phase: 20-disposicion-obligatoria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [index.js, scripts/pre-deploy.js]
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03]

must_haves:
  truths:
    - "El server sabe qué llamadas existen sin disposición: cada llamada iniciada crea un registro pendiente que solo el endpoint de disposición resuelve"
    - "Una auto-marca no_answer se puede corregir dentro de 15 min sin duplicar el dial ni dejar efectos de cadencia fantasma"
    - "Admin/supervisor pueden ver distribución de resultados por SDR + llamadas sospechosas (duración vs resultado) + % de llamadas marcadas"
    - "pending_calls.json sobrevive redeploys de Railway (export/import/pre-deploy/seed/backup)"
    - "El registro arranca vacío: cero reconstrucción de llamadas históricas (D-05)"
  artifacts:
    - path: "index.js"
      provides: "POST/GET /api/setters/pending-calls, extensiones de call-disposition (autoMarked/correctsAutoMarked/pendingCallId), GET /api/setters/disposition-audit"
      contains: "app.post('/api/setters/pending-calls'"
    - path: "scripts/pre-deploy.js"
      provides: "descarga de pending_calls.json en el pre-deploy"
      contains: "pending_calls"
  key_links:
    - from: "POST /api/setters/leads/:id/call-disposition"
      to: "pending_calls.json"
      via: "resolución del registro pendiente (pendingCallId → startedAt → más reciente)"
      pattern: "pendingCallId"
    - from: "GET /api/setters/disposition-audit"
      to: "globalThis.__callCore"
      via: "_ccCollectCalls + _ccResolveRange (JAMÁS re-implementar el funnel)"
      pattern: "_ccCollectCalls"
    - from: "logEntry.autoMarked"
      to: "corrección correctsAutoMarked"
      via: "snapshot preCadence en el entry auto-marcado"
      pattern: "preCadence"
---

<objective>
Backend de la disposición obligatoria: registro server-side de llamadas
pendientes (la fuente de verdad de "esta llamada existió y no está marcada"),
extensiones del endpoint de disposición para auto-marca D-03 con corrección, y
el endpoint de auditoría pasiva D-06.

Purpose: habilita D-01 (el gate en vivo del frontend necesita saber qué llamada
falta marcar), D-02 (la cola de pendientes sobrevive crash del tab porque el
registro se crea al INICIAR la llamada, server-side), D-03 (la auto-marca es
corregible sin duplicar dials) y D-06 (auditoría + medición del % de
disposición = success criterion 2 del ROADMAP).
Output: 3 endpoints nuevos + call-disposition extendido + persistencia en los
5 lugares, todo en index.js + scripts/pre-deploy.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-disposicion-obligatoria/20-CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<interfaces>
<!-- Extraído del código real (2026-07-25). El executor usa esto directo. -->

De index.js (patrón de persistencia a espejar — Phase 19 reports.json):
```js
// index.js:1746  getReportsFile() → path.join(dir, 'reports.json')
// index.js:1748  loadReportsState() — load sync con try/catch
// index.js:4034  seedVolumeFromRepo: lista ['history.json', ..., 'reports.json']
// index.js:4087  BACKUP_FILES = ['setters.json', ..., 'reports.json']
// index.js:2089  GET /api/admin/export-data — bloque reports en ~2122
// index.js:2147  POST /api/admin/import-data — bloques espejo
// scripts/pre-deploy.js:186  ['reports', 'reports.json'] en la lista de descargas
```

De index.js — call-disposition (endpoint a extender, SYNC — sin awaits):
```js
// index.js:8525  app.post('/api/setters/leads/:id/call-disposition', requireAuth, (req, res) => {
// index.js:8532-8541  guard de ownership: _isSetter && !_isOwner && !_isSharedDue → 403
// index.js:8543  const { outcome, notes, callbackAt, scheduled, telnyxCallMeta, objectionTags, disqualifyReason, doNotCall, callbackShared } = req.body || {};
// index.js:8674-8712  logEntry = { ts, outcome, by, notes } + telnyxCallMeta → duration/fromNumber/channel/cost/costCountry/costTariffKey/quickNote/scriptIdsUsed; sin meta → channel:'manual'
// index.js:8712-8716  lead.callLog.push(logEntry); cap 500; lead.callAttempts += 1; lead.lastContactAt = now
// index.js:8723-8788  switch(outcome) — efectos por outcome (estado, phoneStatus, callbackAt, calendar)
// index.js:8800-8835  cadencia: _NO_CONTACT = Set(['no_answer','voicemail']); MAX_NO_CONTACT = 2; streak desde el callLog; streak>=2 → estado='descartado' + autoDiscarded=true + autoDiscardReason + cadenceExhausted; sino callbackAt=+24h
// index.js:8837-8838  saveSettersData(data); res.json({ ok: true, lead, calendarEntry });
```

De index.js — CALL METRICS CORE y visibilidad (para el audit endpoint):
```js
// index.js:5523  COLD_CALL_CONNECT_OUTCOMES = Set(['answered_interested','answered_not_interested','scheduled_with_admin','callback_later','hung_up'])
// index.js:5655  _ccCollectCalls(data, { setterId='', visibleSet=null, channel='' }) — aplana callLog PRE-ATRIBUIDO por _callSetterId
// index.js:5744  _ccResolveRange(period, {from,to}) — 'today'|'7d'|'30d'|'thismonth'|'all' en TZ negocio
// index.js:5787  globalThis.__callCore = { _ccCollectCalls, _ccFunnelAggregate, _ccResolveRange, _ccFunnelSeries, _bizDateStrToTs }
// index.js:5624  _filterSettersVisible(setters, visibleSet)
// index.js:9699-9700  patrón team-performance: const visibleSet = _visibleSetterIds(req.auth.user); const scopedSetters = _filterSettersVisible(data.setters || [], visibleSet);
// index.js:8496  CALL_OUTCOMES (Set de los 9 outcomes válidos)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Registro de llamadas pendientes (pending_calls.json + endpoints + persistencia)</name>
  <files>index.js, scripts/pre-deploy.js</files>
  <read_first>
    - index.js:1740-1760 (getReportsFile/loadReportsState — patrón de helpers de archivo a copiar)
    - index.js:4030-4100 (seedVolumeFromRepo lista + BACKUP_FILES)
    - index.js:2089-2200 (export-data/import-data — dónde agregar el bloque pending_calls)
    - index.js:8525-8545 (guard de ownership de call-disposition — se REUSA el mismo criterio)
    - scripts/pre-deploy.js:180-195 (lista de descargas donde está ['reports','reports.json'])
    - CLAUDE.md notas #19 (mutex solo para handlers async), #21/#128 (persistencia en los lugares)
  </read_first>
  <action>
Implementa el registro server-side de llamadas pendientes (mecanismo elegido en
Claude's Discretion del CONTEXT: registro al INICIAR la llamada — sobrevive
crash del tab y habilita la medición del % de disposición). Todo per D-01/D-02.

1. **Helpers de archivo** (junto a los helpers de reports, ~index.js:1746):
   - `getPendingCallsFile()` → `path.join(DATA_DIR-aware dir, 'pending_calls.json')` (mismo patrón que getReportsFile).
   - `loadPendingCalls()` → `{ pending: [] }` si no existe o parse falla (try/catch, sync).
   - `savePendingCalls(state)` → antes de escribir: (a) prune de registros con `createdAt` más viejo que 14 días, (b) cap FIFO 300 (conservar los más nuevos). Escritura sync (writeFileSync). Los handlers que los usan son SYNC → atómicos, sin mutex (regla #19).

2. **Shape del registro** (documentar en comentario):
   ```js
   { id: 'pc_<leadId>_<startedAtMs>', leadId, leadName, setterId, userId,
     startedAt (ISO), endedAt (ISO|null), durationSecs (num), fromNumber (str),
     reachedActive (bool), createdAt (ISO), updatedAt (ISO) }
   ```
   `setterId`/`userId` SIEMPRE derivados de `req.auth.user` (atribución al que LLAMA, jamás del body — coherente con `_callSetterId` y evita que un SDR infle/ensucie el % de otro). `leadName` se lee de `data.leads[leadId].name` server-side.

3. **`POST /api/setters/pending-calls`** (requireAuth, handler SYNC `(req, res) => {}`, colocar cerca del endpoint call-disposition ~8520):
   - Body: `{ leadId, startedAt, endedAt?, durationSecs?, reachedActive?, canceled? }`.
   - Validaciones: `leadId` existe en `data.leads` → sino 404 `{error:'Lead no encontrado.'}`. `startedAt` parseable con `Date.parse` → sino 400. Ownership: mismo criterio exacto que call-disposition (setter: `lead.assignedTo === req.auth.user.setterId` O callback compartido vencido) → sino 403. Admin/supervisor pasan (el admin cold-callea con setter_ignacio).
   - `id = 'pc_' + leadId + '_' + Date.parse(startedAt)`. UPSERT por id: si existe, mergear solo `endedAt/durationSecs/reachedActive` (+`updatedAt`); si no, crear con `createdAt`.
   - **`canceled: true`** (caso llamada que nunca llegó a existir — newCall falló o cortó en <1s): ELIMINA el registro SOLO si `endedAt == null` Y `createdAt` tiene menos de 2 minutos. Fuera de esa ventana se ignora (mitiga que se borren pendientes para inflar el % marcado — threat T-20-02). Responder `{ok:true, removed:bool}`.
   - `durationSecs` clamp 0..3600, `reachedActive` coerción `!!`.
   - Respuesta: `{ ok: true, id }`.

4. **`GET /api/setters/pending-calls`** (requireAuth, sync):
   - Setter → SOLO sus registros (`p.setterId === req.auth.user.setterId`).
   - Admin/supervisor → todos los visibles: `const visibleSet = _visibleSetterIds(req.auth.user);` y si visibleSet, filtrar `visibleSet.has(p.setterId)`. Query opcional `?setter=<id>` filtra a ese setter.
   - Orden: `startedAt` desc. Respuesta `{ pending: [...] }`.
   - Ruta estática, no colisiona con `/api/setters/leads/:id` — igual colocarla ANTES de cualquier ruta `/api/setters/:param` si existiera (regla #3 de CLAUDE.md).

5. **Persistencia en los 5 lugares** (espejo EXACTO de cómo está `reports.json` — grep `reports.json` en index.js da 3 hits: 1746/4034/4087, más los bloques export/import y pre-deploy):
   - `seedVolumeFromRepo` lista (index.js:4034): agregar `'pending_calls.json'`.
   - `BACKUP_FILES` (index.js:4087): agregar `'pending_calls.json'`.
   - `GET /api/admin/export-data` (~2122): bloque `pending_calls` con `loadPendingCalls()` (try/catch como reports). El export pasa de 14 a 15 bloques.
   - `POST /api/admin/import-data`: si `payload.pending_calls` viene, `savePendingCalls` (espejo del bloque reports).
   - `scripts/pre-deploy.js` (~186): agregar `['pending_calls', 'pending_calls.json']` a la lista.

6. **NO crear `data/pending_calls.json` en el repo ni backfillear nada**: el registro arranca vacío en el primer boot post-deploy (D-05 — la regla aplica solo a llamadas nuevas; las viejas sin marcar no se reconstruyen). `loadPendingCalls()` ya tolera ausencia del archivo.
  </action>
  <verify>
    <automated>node --check index.js && node --check scripts/pre-deploy.js && grep -c "app.post('/api/setters/pending-calls'" index.js | grep -qx "1" && grep -c "app.get('/api/setters/pending-calls'" index.js | grep -qx "1" && test $(grep -c "pending_calls.json" index.js) -ge 4 && grep -q "pending_calls" scripts/pre-deploy.js</automated>
  </verify>
  <done>
    - `grep "app.post('/api/setters/pending-calls', requireAuth, (req" index.js` matchea (handler sync, NO async).
    - `pending_calls.json` aparece en: helpers (getPendingCallsFile), lista seedVolumeFromRepo (línea ~4034), BACKUP_FILES (~4087) → `grep -c "pending_calls.json" index.js` >= 4.
    - Bloques `pending_calls` presentes en export-data e import-data (`grep -n "pending_calls" index.js` muestra hits entre las líneas 2089-2250).
    - `savePendingCalls` contiene el cap `300` y el prune de `14` días (grep -n "300" y "14" en el cuerpo de la función).
    - No existe `data/pending_calls.json` en el repo (`ls data/pending_calls.json` falla).
  </done>
</task>

<task type="auto">
  <name>Task 2: call-disposition — autoMarked, corrección y resolución de pendientes</name>
  <files>index.js</files>
  <read_first>
    - index.js:8525-8840 (endpoint call-disposition COMPLETO — switch de outcomes + bloque de cadencia, es lo que se extiende)
    - index.js:8496-8523 (CALL_OUTCOMES + COLD_CALL_CONNECT gaps)
    - 20-CONTEXT.md sección D-03 (auto-marca corregible) y D-04 (ventana 10 min INTACTA — este task NO toca nada de grabación/Whisper)
  </read_first>
  <action>
Extiende `POST /api/setters/leads/:id/call-disposition` (index.js:8525) con 3
capacidades. El handler SIGUE siendo sync (cero awaits — regla #19). NO tocar
la tabla de tarifas, el switch de outcomes existente ni el bloque de cadencia
salvo lo indicado.

1. **Destructurar 3 campos nuevos del body** (línea 8543): `autoMarked`,
   `correctsAutoMarked`, `pendingCallId`.

2. **Auto-marca (D-03)**: si `autoMarked === true` Y `outcome === 'no_answer'`
   (SOLO esa combinación — ignorar el flag con cualquier otro outcome, para que
   nadie pueda etiquetar de "automático" un connect):
   - `logEntry.autoMarked = true;`
   - ANTES de aplicar el switch y la cadencia, snapshotear en el entry:
     `logEntry.preCadence = { estado: lead.estado, callbackAt: lead.callbackAt || '', cadenceStep: lead.cadenceStep || 0, cadenceExhausted: !!lead.cadenceExhausted, autoDiscarded: !!lead.autoDiscarded, autoDiscardReason: lead.autoDiscardReason || '', phoneStatus: lead.phoneStatus || '', lastContactAt: lead.lastContactAt || '' }`.
     (El snapshot vive en el entry; si nunca se corrige queda como dato inerte —
     el cap 500 del callLog lo poda con el tiempo.)
   - Después el flujo sigue NORMAL: no_answer cuenta como dial, avanza cadencia,
     puede auto-descartar al 2do no-contacto. Eso es intencional: la auto-marca
     ES una disposición real (D-03).

3. **Corrección (D-03: "el SDR puede corregir la auto-marca")**: si
   `correctsAutoMarked === true` (ignora `autoMarked` si vinieron juntos):
   - Buscar el ÚLTIMO entry de `lead.callLog`. Requisitos: `entry.autoMarked === true` Y `Date.now() - Date.parse(entry.ts) <= 15*60*1000`. Si no se cumplen → 409 `{ error: 'No hay auto-marca reciente para corregir.' }` y NO tocar nada.
   - Si se cumplen: (a) `lead.callLog.pop()` (sacar el entry auto-marcado);
     (b) restaurar en el lead TODOS los campos de `entry.preCadence` (estado,
     callbackAt, cadenceStep, cadenceExhausted, autoDiscarded, autoDiscardReason,
     phoneStatus, lastContactAt); (c) `lead.callAttempts = Math.max(0, (lead.callAttempts || 1) - 1);`
     (d) continuar con el flujo NORMAL del endpoint para el `outcome` nuevo del
     body (crea un entry nuevo que REEMPLAZA al auto-marcado → 1 llamada = 1
     entry, los dials no se duplican);
     (e) el entry nuevo HEREDA del entry popped: `ts` (la llamada ocurrió a esa
     hora — las métricas bucketean por ts), `duration`, `fromNumber`, `channel`,
     `cost`, `costCountry`, `costTariffKey`, `quickNote`, `scriptIdsUsed` —
     SALVO que el body traiga su propio `telnyxCallMeta` (raro en corrección),
     en cuyo caso gana el body. Implementación: capturar el popped entry antes
     del pop lógico y, después de construir `logEntry` por el camino normal,
     copiar los campos faltantes.
   - La corrección NO crea ni resuelve pendientes (la auto-marca original ya
     resolvió el suyo).

4. **Resolución de pendientes**: después de `lead.callLog.push(logEntry)` y
   ANTES de `res.json`, resolver (eliminar) EXACTAMENTE UN registro de
   `pending_calls.json` para este lead, en este orden de prioridad:
   (1) `pendingCallId` del body, si existe un registro con ese id Y
   `pending.leadId === req.params.id` (validar el leadId — que no se pueda
   resolver el pendiente de OTRO lead);
   (2) registro cuyo `startedAt === telnyxCallMeta.startedAt` (si vino meta);
   (3) el registro MÁS RECIENTE (startedAt máximo) de este lead.
   Si no hay ninguno → silencioso, no es error. Load/save sync del archivo
   dentro del handler (sigue siendo atómico). Excepción: en el flujo de
   corrección (punto 3) NO resolver nada.

5. **Respuesta**: sin cambios de shape (`{ ok: true, lead, calendarEntry }`) —
   los 5 call sites del frontend ya la consumen así. Opcional: agregar
   `resolvedPendingId` (string|null) al JSON para debugging; no lo consume nadie.
  </action>
  <verify>
    <automated>node --check index.js && test $(grep -c "correctsAutoMarked" index.js) -ge 2 && grep -q "preCadence" index.js && grep -q "pendingCallId" index.js && grep -q "No hay auto-marca reciente" index.js</automated>
  </verify>
  <done>
    - El destructure de la línea ~8543 incluye `autoMarked, correctsAutoMarked, pendingCallId`.
    - `logEntry.autoMarked` solo se setea cuando `outcome === 'no_answer'` (leer el código y confirmar el guard).
    - El branch de corrección devuelve 409 con el mensaje exacto `No hay auto-marca reciente para corregir.` cuando el último entry no es auto-marcado o pasó la ventana de 15 min.
    - El entry de corrección hereda `ts` y `duration` del entry popped (leer el código).
    - `grep -n "await" index.js | awk -F: '$1 > 8520 && $1 < 8900'` no muestra awaits nuevos dentro del handler (sigue sync).
  </done>
</task>

<task type="auto">
  <name>Task 3: GET /api/setters/disposition-audit — auditoría pasiva D-06</name>
  <files>index.js</files>
  <read_first>
    - index.js:5640-5800 (_ccCollectCalls, _ccResolveRange, __callCore — la ÚNICA fuente permitida para derivar métricas de llamadas)
    - index.js:9690-9780 (endpoint team-performance — patrón de visibilidad a ESPEJAR: _visibleSetterIds + _filterSettersVisible)
    - .planning/ROADMAP.md reglas transversales (jamás re-implementar el funnel; tests/metrics-consistency.test.js es la garantía)
  </read_first>
  <action>
Crea `GET /api/setters/disposition-audit` (requireAuth, sync). Es la auditoría
PASIVA de D-06: cero fricción al marcar, todo se deriva de datos ya existentes.

1. **RBAC**: `req.auth.user.role` debe ser `admin` o `supervisor` → sino 403
   `{ error: 'Solo admin/supervisor.' }`. Visibilidad espejo EXACTO de
   team-performance: `const visibleSet = _visibleSetterIds(req.auth.user);` +
   `_filterSettersVisible(data.setters || [], visibleSet)` para la lista de
   setters a reportar. Query `?setter=<id>` filtra a uno (validar que esté en
   los visibles → sino 403).

2. **Rango**: `?period=today|7d|30d|all` (default `7d`) resuelto con
   `_ccResolveRange(period)` del CALL METRICS CORE. Aceptar también los alias
   que `_ccResolveRange` ya soporta (week→7d, month→30d).

3. **Datos** — derivar TODO de `_ccCollectCalls(data, { visibleSet })`
   (entries pre-atribuidas por quién llamó; leer la función para el shape real
   de cada entry). Filtrar por ts dentro del rango. Por cada setter visible:
   - `dials`: total de entries del setter en rango (todos los canales).
   - `byOutcome`: objeto `{ outcome: count }` (la distribución de D-06).
   - `suspicious` (umbrales fijados acá, Claude's discretion del CONTEXT):
     * `longNoContact`: entries con `outcome ∈ {'no_answer','voicemail'}` y `duration >= 31` (segundos) — "llamada >30s marcada como no atendió".
     * `shortConnect`: entries con `outcome ∈ {'answered_interested','scheduled_with_admin'}` y `duration > 0 && duration < 10` — contacto imposible de tan corto.
     * `samples`: hasta 10 items `{ leadId, leadName, ts, outcome, duration, rule }` (leadName resuelto de `data.leads[leadId]?.name || ''`). `total = longNoContact + shortConnect`.
     * Entries sin `duration` (channel 'manual') NO cuentan como sospechosas (no hay dato para cruzar — no inventar).
   - `pctMarked` (success criterion 2 del ROADMAP): `telnyxDials = entries del setter con channel === 'telnyx_webrtc' en rango`; `pendingCount = registros de pending_calls.json del setter con startedAt en rango` (cargar con loadPendingCalls); `pctMarked = telnyxDials + pendingCount === 0 ? null : Math.round(100 * telnyxDials / (telnyxDials + pendingCount))`. (Los pendientes son las ÚNICAS llamadas sin marcar que el sistema conoce: la auto-marca cubre no-contactos y el gate cubre el flujo en vivo.)
4. **Respuesta**:
   ```js
   { period, from, to,
     bySetter: [ { setterId, name, dials, byOutcome, suspicious: { longNoContact, shortConnect, total, samples }, pendingCount, pctMarked } ],
     totals: { dials, pending, pctMarked } }
   ```
   Ordenar bySetter por dials desc. Setters con 0 dials y 0 pendientes en el
   rango se OMITEN de bySetter (nada de filas en cero — regla del milestone).

5. **PROHIBIDO**: re-implementar lógica de funnel/connect inline (usar solo lo
   que dan las entries de `_ccCollectCalls`), tocar `_ccFunnelAggregate`, o
   modificar cualquier función existente del CORE. `tests/metrics-consistency.test.js`
   debe seguir verde sin cambios (se corre en el plan 20-02).
  </action>
  <verify>
    <automated>node --check index.js && grep -c "app.get('/api/setters/disposition-audit'" index.js | grep -qx "1" && npx vitest run tests/metrics-consistency.test.js 2>&1 | grep -q "19 passed"</automated>
  </verify>
  <done>
    - El endpoint usa `_ccCollectCalls` y `_ccResolveRange` (grep dentro del handler) y NO contiene una re-implementación del funnel (no aparece un Set nuevo de outcomes de connect).
    - Umbrales exactos en el código: `31` (longNoContact) y `10` (shortConnect).
    - Un setter (role setter) recibe 403 — verificable por lectura del guard.
    - `tests/metrics-consistency.test.js` pasa 19/19 sin modificaciones.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser SDR → /api/setters/pending-calls | input no confiable: leadId/startedAt/canceled del body |
| browser SDR → call-disposition (autoMarked/correctsAutoMarked) | flags que reescriben historia de métricas |
| supervisor → disposition-audit | scoping de visibilidad entre SDRs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | S | POST pending-calls | mitigate | setterId/userId SIEMPRE de req.auth, jamás del body — un SDR no puede crear pendientes atribuidos a otro |
| T-20-02 | T | canceled:true | mitigate | solo borra registros con endedAt null y <2 min de creados — no se puede vaciar la cola para inflar pctMarked |
| T-20-03 | T | correctsAutoMarked | mitigate | solo corrige el ÚLTIMO entry, solo si autoMarked===true, solo dentro de 15 min; el entry nuevo hereda duration/ts (no se puede fabricar historia) |
| T-20-04 | T | autoMarked flag | mitigate | solo se acepta con outcome no_answer — no se puede etiquetar un connect como "automático" |
| T-20-05 | I | GET pending-calls / disposition-audit | mitigate | setter solo ve lo suyo; audit 403 para setter; supervisor filtrado con _visibleSetterIds/_filterSettersVisible (espejo team-performance) |
| T-20-06 | T | pendingCallId en disposition | mitigate | se valida pending.leadId === req.params.id antes de resolver |
| T-20-07 | D | pending_calls.json growth | mitigate | cap FIFO 300 + prune 14 días en savePendingCalls |
| T-20-08 | R | dos requests concurrentes al file | accept | handlers sync (load→modify→save sin awaits) = atómicos por event loop; mismo criterio que el resto de los JSON (regla #19) |
</threat_model>

<verification>
- `node --check index.js` y `node --check scripts/pre-deploy.js` pasan.
- Los 3 endpoints existen (grep de las 3 rutas devuelve exactamente 1 cada una).
- `pending_calls` presente en export-data, import-data, seedVolumeFromRepo, BACKUP_FILES y pre-deploy.js.
- `npx vitest run tests/metrics-consistency.test.js` → 19 passed (el CORE no se tocó).
- `npx vitest run tests/disposition-dnc.test.js tests/call-cadence.test.js` → verdes (el switch/cadencia existentes no se rompieron).
</verification>

<success_criteria>
- El server registra llamadas pendientes con atribución segura y las resuelve SOLO vía disposición (o cancelación acotada a 2 min).
- La auto-marca no_answer es corregible 15 min sin duplicar dials ni dejar cadencia fantasma.
- disposition-audit devuelve distribución + sospechosas + pctMarked por SDR, derivado del CALL METRICS CORE, con RBAC admin/supervisor y scoping Phase 18.
- Cero cambios de comportamiento para requests de disposición que no usan los campos nuevos (retrocompatibilidad total con el frontend actual).
</success_criteria>

<output>
Al completar, crear `.planning/phases/20-disposicion-obligatoria/20-01-SUMMARY.md`
</output>
