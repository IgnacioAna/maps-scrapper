---
phase: 32-act-acciones
plan: 03
type: execute
wave: 3
depends_on: ["32-01"]
files_modified:
  - public/app.js
  - public/index.html
  - tests/act-ui-whatsapp.test.js
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03]

must_haves:
  truths:
    - "Desde la lista de Llamadas, el Power Dialer, la ficha del lead y Hoy hay un botón de WhatsApp — el mismo componente, no cuatro"
    - "Un click abre el chat en una pestaña nueva con el mensaje ya escrito y deja el envío anotado, sin navegar a otra vista"
    - "Se puede mandar a otro número tipeado en el momento, y opcionalmente guardarlo como contacto secundario"
    - "Después de mandar, el aviso dice exactamente en qué sección vuelve a aparecer el lead"
    - "Mandar desde el Power Dialer no expulsa la tarjeta que el SDR está trabajando"
  artifacts:
    - path: "public/app.js"
      provides: "bloque [32-03] ACT-PURE con las 3 plantillas, _actButtonsHTML, window._actWhatsApp y la rama de destino post-envío"
      contains: "[32-03] ACT-PURE"
    - path: "tests/act-ui-whatsapp.test.js"
      provides: "bloque puro + paridad con el backend + cableado de las 4 superficies + anti-deriva del título de Hoy"
      min_lines: 150
  key_links:
    - from: "renderCallsList / _pdRender / _callsRenderExpandedPanel / _hoyRenderSection"
      to: "_actButtonsHTML"
      via: "un único builder con variante por superficie (patrón _dispoSelectHTML)"
      pattern: "_actButtonsHTML\\("
    - from: "window._actWhatsApp"
      to: "POST /api/setters/leads/:id/whatsapp-send"
      via: "fetch + window.open del whatsappUrl devuelto"
      pattern: "whatsapp-send"
    - from: "window._actWhatsApp"
      to: "_dispoAnnounce"
      via: "aviso de destino con forceToast (nunca un showToast de destino a mano)"
      pattern: "forceToast"
---

<objective>
El botón de WhatsApp en las 4 superficies (D-01) con su flujo completo: elegir
plantilla, revisar el mensaje, elegir a qué número, abrir el chat en pestaña
nueva (D-02) y que el envío quede anotado en el mismo acto (D-03), con el aviso
diciendo dónde vuelve a aparecer el lead.

Purpose: ACT-01/02/03 son la mitad visible de la fase — el user nombró este
botón primero. El backend (32-01) ya devuelve el link y hace el registro; falta
la superficie.
Output: bloque puro `[32-03] ACT-PURE` con las 3 plantillas de D-06,
`_actButtonsHTML` cableado en las 4 vistas, `window._actWhatsApp`, una rama
nueva de destino y la suite que ata todo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-act-acciones/32-CONTEXT.md
@.planning/phases/32-act-acciones/32-PATTERNS.md
@.planning/phases/32-act-acciones/32-01-SUMMARY.md
@.planning/phases/31-comm-compromisos/31-03-SUMMARY.md
@.planning/phases/31-comm-compromisos/31-04-SUMMARY.md
@.planning/phases/30-gate-proximo-paso/30-03-SUMMARY.md
@CLAUDE.md

Reglas del proyecto que aplican:

- **Cache-buster (regla dura):** este plan toca `public/app.js` → hay que
  bumpear `app.js?v=` en `public/index.html`. **Leer el valor REAL del archivo
  antes de bumpear** (al escribir este plan era `20260815g`, línea 3678, y ya
  pasó 3 veces en este milestone que una sesión paralela lo bumpeara primero —
  ver 31-03/31-04). `style.css` NO se toca en este plan: todo el markup nuevo
  reusa clases existentes o estilos inline, igual que `_callsAltContact`.
- **z-index (bug ya pagado en esta fase del proyecto):** `.modal-overlay` vive
  en 10000/10010 y el date-picker propio en 10050 (comentario en
  `public/style.css:4938-4945`). El overlay nuevo se abre TAMBIÉN desde la ficha
  de Hoy, que es un `.modal-overlay` → tiene que ir por encima: **10060**.
  `_callsAltContact` usa 9999 y por eso queda tapado cuando se abre desde la
  ficha (bug preexistente, no lo arregla este plan, pero no hay que copiarlo).
- **`_leadStoreApply` (nota #105):** toda mutación optimista pasa por ahí, con el
  `lead` COMPLETO de la respuesta, nunca con un patch armado a mano.
- **`_refreshLeadPanels` en `finally` (nota #175):** siempre, también en el
  camino de error — si no, la ficha queda mostrando estado viejo.
- **Aviso de destino (30-03):** nunca un `showToast` propio para "a dónde se fue
  el lead". Se usa `_dispoAnnounce` y, si falta un caso, se EXTIENDE
  `_dispoDestination` dentro de los marcadores `[30-03] DISPO-DEST`.
- **Restricción dura del bloque DISPO-DEST:** el test lo evalúa aislado con
  `new Function` — la rama nueva solo puede usar literales y campos del `lead`
  (nada de `_commitmentLabel`, `COMMITMENT_UI_TIPOS`, etc.).
</context>

<interfaces>
<!-- Lo que este plan deja para 32-04 (que suma el botón de descartar al MISMO
     builder, sin volver a tocar las 4 superficies). -->

function _actButtonsHTML(leadId, opts = {})
  opts.variant: 'row' | 'pd' | 'ficha' | 'hoy'   (default 'row')
  Devuelve el HTML de los botones de acción del lead para esa superficie.
  Punto de extensión único: 32-04 agrega acá el botón de descartar.

window._actWhatsApp(leadId)   — abre el overlay de envío.
window._actSendWhatsAppNow(leadId, payload)  — (opcional, si el executor separa
  el submit del overlay) hace el POST y el window.open.

_dispoDestination(lead, now)  — rama NUEVA: compromiso propio 'cumplido' +
  nextAction {origen:'compromiso', tipo:'esperar_respuesta'} →
  vista 'en Hoy → Esperando del prospecto'.
_dispoAnnounce(leadId, opts)  — opts.forceToast:true tira el toast aunque el
  Power Dialer esté activo (acción explícita del SDR, no una disposición).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Plantillas (bloque puro), builder único de botones y cableado en las 4 superficies</name>

  <read_first>
    - `public/app.js` líneas 11684-11803 — bloque `[31-03] COMMITMENT-PURE`
      COMPLETO: el formato exacto de marcadores, el estilo de comentarios y
      dónde termina (el bloque nuevo va inmediatamente después).
    - `public/app.js` líneas 6085-6104 — `_dispoSelectHTML(leadId, {minWidth, fontSize})`:
      el patrón "un builder, 4 call sites, una perilla de tamaño por contexto"
      que hay que replicar, incluido el comentario que explica por qué duplicarlo
      ya mordió una vez.
    - `public/app.js` líneas 8489-8554 — la fila de `renderCallsList`: el botón
      `+ contacto` (8510) con su `event.stopPropagation()` es el molde de la
      variante `'row'`.
    - `public/app.js` líneas 7033-7040 — el bloque Quick-links de `_pdRender`,
      incluido el `<a>` que usa `lead.whatsappUrl` + `window._waBtnClick`.
    - `public/app.js` líneas 8205-8222 — `.call-action-row` de
      `_callsRenderExpandedPanel`.
    - `public/app.js` líneas 6119-6136 — la fila de `_hoyRenderSection` con
      `hoy-ficha-btn` / `hoy-call-btn`.
    - `public/app.js` líneas 1336-1425 — `_waBtnClick`/`_waMultiClick`: **leer
      para NO usarlos** (feature parkeada de wa-multi, gateada por
      `_waMultiEnabled()`, lee el campo legacy `lead.whatsappUrl`).
    - `public/app.js` líneas 10593-10625 — `_interpolateScript(text, lead)`: el
      vocabulario de variables que D-06 pide reusar.
    - `public/style.css` líneas 4229-4255 — `.call-action-btn` y el hook
      `.call-action-btn.is-wsp` (definido y HOY sin uso: esta fase es su primer
      consumidor).
    - `.planning/phases/32-act-acciones/32-01-SUMMARY.md` — los 3 ids de
      `ACT_WA_TEMPLATE_IDS` tal como quedaron en el backend.
  </read_first>

  <files>public/app.js</files>

  <action>
**A. Bloque puro `[32-03] ACT-PURE`.**
Insertarlo inmediatamente después de `// ─── [31-03] COMMITMENT-PURE: FIN ───`
(app.js:11803), con marcadores del mismo formato:
`// ─── [32-03] ACT-PURE: INICIO ───` / `// ─── [32-03] ACT-PURE: FIN ───`.
Contenido (autocontenido: sin DOM, sin red, sin `window`, para que el test lo
evalúe con `new Function`):

- `const ACT_WA_TEMPLATES = [...]` con EXACTAMENTE 3 entradas
  `{ key, label, body }`, y los `key` idénticos a `ACT_WA_TEMPLATE_IDS` del
  backend (`post_llamada`, `envio_info`, `reconfirmar_reunion`).
  Reglas duras del contenido de `body` (D-06/D-08 + estilo del proyecto):
    - Usa solo variables del vocabulario de `_interpolateScript`: `{name}`,
      `{city}`, `{setterName}` (y ninguna otra).
    - **Nunca** nombra la empresa ni ninguna marca (nota #119). El test lo
      verifica por grep.
    - **Sin `¿` ni `¡` de apertura** — es texto que ve el prospecto y el
      proyecto ya lo sanea así en todo lo que sale hacia afuera
      (`sanitizeMercuryStyle`). El test lo verifica.
    - Máximo 2 párrafos separados por `\n\n`, tono del guión de llamada
      (presentación post-llamada / envío de información / reconfirmación de
      reunión, los 3 casos que nombra D-06).
- `function _actTemplateById(key)` → la plantilla cuyo `key` coincide, o la
  primera del array si no coincide ninguna (nunca `undefined`).
- Comentario de cabecera: estas 3 claves ESPEJAN `ACT_WA_TEMPLATE_IDS` de
  `index.js` (si cambia una, cambian las dos); el test lee los dos archivos como
  texto para verificarlo, mismo criterio que la paridad de `COMMITMENT_UI_TIPOS`.

**B. `_actButtonsHTML(leadId, opts = {})`.**
Declararla como `function` (no `const`: hay call sites por encima de su posición
y las declaraciones de función sí hoistean) inmediatamente después de
`_dispoSelectHTML` (app.js:6104). Devuelve el HTML de los botones de acción
para la superficie indicada por `opts.variant`:
  - `'row'` (lista de Llamadas): pill chico con estilos inline calcados del
    botón `+ contacto` (8510), incluido `event.stopPropagation();` al principio
    del `onclick` — la fila tiene su propio manejo de click.
  - `'pd'` (Power Dialer): `class="pd-quick-link"`.
  - `'ficha'` (panel expandido): `class="call-action-btn is-wsp"` — el hook CSS
    que ya existe sin uso.
  - `'hoy'`: `class="hoy-ficha-btn"` (mismo tratamiento visual que el botón
    Ficha de esa fila).
El único botón que emite este plan es el de WhatsApp
(`onclick="window._actWhatsApp('<id escapado>')"`, `title` explicando que abre
el chat con el mensaje cargado y deja el envío anotado).
Dejar un comentario explícito: **este es el punto de extensión único; 32-04
agrega acá el botón de descartar en vez de tocar las 4 superficies otra vez**.
Todo `leadId` interpolado pasa por `escHtml`.

**C. Cableado en las 4 superficies (D-01: un componente, no cuatro).**
  1. `renderCallsList` (~8510): agregar `${_actButtonsHTML(l.id, { variant: 'row' })}`
     inmediatamente después del botón `+ contacto`, dentro del mismo contenedor
     de badges.
  2. `_pdRender` (7034-7040): agregar `${_actButtonsHTML(lead.id, { variant: 'pd' })}`
     dentro del bloque Quick-links **y eliminar el `<a>` que usa
     `lead.whatsappUrl` + `window._waBtnClick`** (línea 7039) junto con
     `lead.whatsappUrl` de la condición de visibilidad del bloque (7034).
     Razón a dejar en comentario: ese link es la feature parkeada de wa-multi y
     lee un campo legacy armado en tiempo de import desde el `openMessage` viejo
     de Setteo — dos botones de WhatsApp con comportamientos distintos en la
     misma tarjeta es peor que uno. **No** tocar `_waBtnClick` ni el badge
     "ver chat" de `renderCallsList` (8507), que es otra cosa (abrir una
     conversación YA existente).
  3. `_callsRenderExpandedPanel` (8205-8222): agregar
     `${_actButtonsHTML(l.id, { variant: 'ficha' })}` como primer elemento de
     `.call-action-row`.
  4. `_hoyRenderSection` (~6134): agregar `${_actButtonsHTML(l.id, { variant: 'hoy' })}`
     inmediatamente ANTES del botón `hoy-ficha-btn`.

**No tocar** `_dispoSelectHTML`, `_waBtnClick`, `_waMultiClick` ni ningún otro
render.
  </action>

  <verify>
    <automated>node --check public/app.js &amp;&amp; npx vitest run tests/commitment-ui.test.js tests/commitment-hoy.test.js tests/gate-destination.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "\[32-03\] ACT-PURE" public/app.js` devuelve exactamente 2
      (marcador de inicio y de fin).
    - `grep -c "_actButtonsHTML(" public/app.js` devuelve exactamente 5 (la
      declaración + los 4 call sites).
    - Cada una de estas funciones contiene una llamada a `_actButtonsHTML(`
      dentro de su cuerpo: `renderCallsList`, `_pdRender`,
      `_callsRenderExpandedPanel`, `_hoyRenderSection` (verificable acotando por
      los números de línea de cada declaración).
    - `grep -c "lead.whatsappUrl" public/app.js` bajó en 2 respecto de `HEAD`
      (el `<a>` del quick-link y su condición de visibilidad).
    - `grep -c "_waBtnClick" public/app.js` bajó exactamente en 1 respecto de
      `HEAD` (solo se sacó el uso del Power Dialer; el de `renderCallsList`
      sigue).
    - Ninguna de las 3 plantillas contiene la marca de la empresa, ni `¿`, ni `¡`
      (verificable por grep sobre el bloque `[32-03] ACT-PURE`).
    - Las 3 suites del comando de verify siguen verdes (31-03/31-04/30-03 no se
      rompen por el markup nuevo).
  </acceptance_criteria>

  <done>
Las 4 superficies muestran el botón de WhatsApp del mismo componente, y existe
un catálogo de 3 plantillas espejado con el backend y libre de marca.
  </done>
</task>

<task type="auto">
  <name>Task 2: window._actWhatsApp (overlay de envío) + rama de destino post-envío</name>

  <read_first>
    - `public/app.js` líneas 11974-12041 — `window._callsAltContact` COMPLETA: el
      molde del overlay (div en `document.body`, cierre por click afuera/Cancelar,
      `.remove()`, keypad, `doSave` con `fetch` + `_leadStoreApply` +
      `_refreshLeadPanels`). **Ojo con su `z-index:9999`** — el overlay nuevo va
      en 10060 (ver `<context>`).
    - `public/app.js` líneas 7777-7832 — `_callsSetCommitment` /
      `_callsCloseCommitment`: la secuencia canónica fetch → `!r.ok` con
      `d.error` → `_leadStoreApply(leadId, d.lead)` → toast → `finally`
      `_refreshLeadPanels(leadId)`.
    - `public/app.js` líneas 8839-8993 — bloque `[30-03] DISPO-DEST` COMPLETO,
      con la rama de compromiso PENDIENTE (8921-8942) que la rama nueva
      acompaña, y la rama genérica `esperar_respuesta` (8968-8983) que hoy
      atrapa mal este caso.
    - `public/app.js` líneas 9006-9021 — `_dispoAnnounce` (la rama
      `if (_pd.active)` que hay que poder saltear con `forceToast`).
    - `public/app.js` líneas 11776-11802 — `_commitmentHoyBucket`: el 3er bucket
      ("ya mandé, espero respuesta") cuya condición hay que replicar EXACTA en la
      rama nueva de destino, o el aviso va a mentir.
    - `public/app.js` líneas 6793-6814 — `_pdRender` y su expulsión por
      `callbackAt` futuro (6813): la razón por la que este handler NO llama
      `_pdRender()`.
    - `public/app.js` líneas 10593-10625 — `_interpolateScript`.
    - `public/style.css` líneas 5094-5099 y 5176-5182 — `.scm-phone` (D-11) y
      `.btn-accent` (verde de marca con label oscuro; nunca blanco sobre verde).
  </read_first>

  <files>public/app.js</files>

  <action>
**A. Rama nueva en `_dispoDestination` (dentro de los marcadores DISPO-DEST).**
Insertarla INMEDIATAMENTE DESPUÉS de la rama de compromiso pendiente
(termina en 8942) y ANTES de `if (lead && lead.estado === 'interesado')` (8943).
Condición, replicando literal el 3er bucket de `_commitmentHoyBucket`:
`lead.commitment.estado === 'cumplido'` && `lead.commitment.parte === 'yo'` &&
`lead.nextAction.origen === 'compromiso'` && `lead.nextAction.tipo === 'esperar_respuesta'`.
Devuelve `vista: 'en Hoy → Esperando del prospecto'`, `tono: 'info'`, `cuando`
calculado con `_dispoNextAt`/`_dispoWhenLabel` (ya están en el bloque), y un
`texto` que distingue el canal leyendo `lead.commitment.canal`:
  - `whatsapp` → `«<nombre>» — anotado: le abriste el chat para mandarle info. Vuelve <cuando> en Hoy → Esperando del prospecto.`
  - cualquier otro (email) → `«<nombre>» — anotado: le mandaste el material. Vuelve <cuando> en Hoy → Esperando del prospecto.`
Sin `cuando`, la misma frase sin la parte de "Vuelve …".
Comentario obligatorio: sin esta rama el caso caía en el genérico
"a la cola de Llamadas" (8968-8983), que es MENTIRA — el lead vive en
`Hoy → Esperando del prospecto` (`_commitmentHoyBucket`, 3er bucket). El título
está duplicado a propósito entre `loadHoyView` y este bloque (el `new Function`
del test no tiene el resto del archivo en scope) y hay un test que ata los dos.
**Solo literales y campos del lead** en esta rama.

**B. `forceToast` en `_dispoAnnounce` (9006).**
Agregar `opts.forceToast`: cuando es `true`, saltea la rama
`if (_pd.active) { _pd.holdMeta = …; return; }` y tira el toast igual.
Comentario: el descarte y el envío de material son acciones EXPLÍCITAS del SDR,
no el cierre de una llamada — el banner "✓ Resultado guardado" del dialer no las
representa (diría que se marcó un resultado que no se marcó), así que en esos
dos casos el aviso va como toast aunque el dialer esté abierto.

**C. `window._actWhatsApp(leadId)`.**
Declararla junto a los otros handlers de acción del lead (después de
`window._callsCloseCommitment`, ~7832). Comportamiento:
1. Resolver el lead con `_callsLeadsById.get(leadId)` y fallback a
   `callsLeadsCache`; si no está, toast de error y `return`.
2. Montar un overlay `#act-wa-overlay` en `document.body` con
   `z-index:10060` (ver `<context>`), cierre por click afuera, botón Cancelar y
   Escape. Contenido, TODO visible a la vez (nada de wizard):
   - Selector de plantilla: los 3 `label` de `ACT_WA_TEMPLATES` como botones o
     `<select>`; el default es `envio_info`.
   - `<textarea id="act-wa-msg">` con el cuerpo YA interpolado por
     `_interpolateScript(_actTemplateById(key).body, lead)` — editable. Cambiar
     de plantilla reescribe el textarea.
   - Destino (ACT-03/D-09/D-10): opciones `Principal` (`lead.phone`),
     `Alternativo` (`lead.altPhone`, solo si existe, con su `altPhoneLabel`) y
     `Otro número` con un `<input type="tel">`. Los números se muestran con
     `class="scm-phone"` (D-11: mono + cifras tabulares).
     Con "Otro número": checkbox `Guardar como contacto secundario` + input de
     etiqueta (ambos deshabilitados/ocultos si no es esa opción).
   - Link secundario "editar contacto secundario" que llama
     `window._callsAltContact(leadId)` (reuso del editor completo, no una UI
     paralela).
   - CTA principal `class="btn-accent"`: **Abrir WhatsApp**.
3. Al confirmar: `POST` a `/api/setters/leads/<id>/whatsapp-send` vía
   `apiUrl(...)` con `{ templateId, message, phone, saveAsAltPhone, altPhoneLabel }`
   (`phone` solo si la opción elegida no es la principal).
   - `!r.ok` → `window.showToast?.(d.error || ('Error mandando WhatsApp: HTTP ' + r.status), { type:'error' })` y `return` (el overlay se cierra igual en el `finally`, ver 5).
   - OK → `window.open(d.whatsappUrl, '_blank', 'noopener')` (D-02: pestaña
     nueva, jamás un envío automático) → `_leadStoreApply(leadId, d.lead)` →
     `_dispoAnnounce(leadId, { lead: d.lead, forceToast: true })`.
     Si `window.open` devuelve `null` (bloqueador de pop-ups), mostrar además un
     toast con el link para abrirlo a mano — el registro ya quedó hecho y el SDR
     tiene que poder mandar igual.
4. **NO llamar `_pdRender()`** en ningún caso. Comentario obligatorio: el envío
   programa `nextAction` a +48h y `_pdRender` expulsa la tarjeta cuando hay
   `callbackAt` futuro (app.js:6813) — re-renderizar le sacaría de la pantalla
   el lead que está trabajando. Tampoco setear `_pd.holdCurrent` (pintaría
   "✓ Resultado guardado", que sería falso: no se marcó ninguna disposición y el
   gate de la Phase 20 sigue abierto).
5. `finally`: cerrar el overlay y `_refreshLeadPanels(leadId)` (nota #175).
6. Todo dato del lead interpolado en el markup pasa por `escHtml`.

**No tocar** `_dispoAfterSaved`, `_dispoGateClear`, `_pdRender` ni
`_callsAltContact`.
  </action>

  <verify>
    <automated>node --check public/app.js &amp;&amp; npx vitest run tests/gate-destination.test.js tests/commitment-hoy.test.js tests/commitment-ui.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "window._actWhatsApp" public/app.js` devuelve ≥ 2 (declaración +
      al menos el call site de `_actButtonsHTML`).
    - `grep -n "Esperando del prospecto" public/app.js` devuelve al menos 3
      líneas: la de `loadHoyView`, la de la rama de compromiso pendiente y la de
      la rama nueva.
    - La rama nueva está entre las líneas de
      `if (lead && lead.commitment && lead.commitment.estado === 'pendiente')` y
      `if (lead && lead.estado === 'interesado')`.
    - `grep -c "forceToast" public/app.js` devuelve ≥ 2 (la lectura en
      `_dispoAnnounce` y el uso en `_actWhatsApp`).
    - El cuerpo de `window._actWhatsApp` contiene `_leadStoreApply(`,
      `window.open(`, `_dispoAnnounce(` y `_refreshLeadPanels(` dentro de un
      `finally`, y **no** contiene `_pdRender(` ni `_pd.holdCurrent`.
    - El overlay declara `z-index:10060` (o mayor) — `grep -n "act-wa-overlay" public/app.js`
      y verificar el `cssText` asociado.
    - `npx vitest run tests/gate-destination.test.js` sigue verde (las 8 ramas
      viejas no cambiaron de comportamiento).
  </acceptance_criteria>

  <done>
Un click abre el chat con el mensaje cargado, deja el envío anotado y avisa en
qué sección vuelve el lead; funciona igual desde el Power Dialer sin expulsar la
tarjeta.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests del bloque puro, de la paridad y del cableado + cache-buster</name>

  <read_first>
    - `tests/commitment-ui.test.js` líneas 1-70 — el extractor por marcadores con
      `new Function`, `extractObjectLiteral`, `countOccurrences` y el criterio de
      reloj FIJO. Es el molde exacto.
    - `tests/commitment-hoy.test.js` — el test anti-deriva T-31-13 (título de la
      sección de Hoy atado al bloque DISPO-DEST): el nuevo va en la misma línea.
    - `tests/gate-destination.test.js` — cómo se evalúa el bloque DISPO-DEST
      aislado y por qué la rama nueva no puede referenciar helpers externos.
    - `public/index.html` línea 3678 — el cache-buster REAL de `app.js` (leerlo,
      no asumirlo).
    - El código escrito en las Tasks 1 y 2.
  </read_first>

  <files>tests/act-ui-whatsapp.test.js, public/index.html</files>

  <action>
**A. Crear `tests/act-ui-whatsapp.test.js`** con el molde de
`tests/commitment-ui.test.js` (leer `public/app.js`, `public/index.html` e
`index.js` como texto; extraer el bloque `[32-03] ACT-PURE` por marcadores y
evaluarlo con `new Function` devolviendo `{ ACT_WA_TEMPLATES, _actTemplateById }`).
Cubrir:

**Bloque puro (D-06/D-08)**
1. `ACT_WA_TEMPLATES` tiene exactamente 3 entradas, cada una con `key`, `label`
   y `body` no vacíos.
2. Los 3 `key` son exactamente `post_llamada`, `envio_info`,
   `reconfirmar_reunion`.
3. Ningún `body` contiene la marca de la empresa (chequear la sigla en
   mayúsculas como palabra completa, mismo criterio que `_stripBrandMentions`).
4. Ningún `body` contiene `¿` ni `¡`.
5. Cada `body` usa solo variables del vocabulario permitido (`{name}`, `{city}`,
   `{setterName}`): extraer con regex `\{[a-zA-Z]+\}` y verificar que el set
   resultante está contenido en el permitido.
6. `_actTemplateById('envio_info')` devuelve esa plantilla;
   `_actTemplateById('no-existe')` devuelve la primera (nunca `undefined`).

**Paridad con el backend**
7. Los 3 `key` aparecen dentro de la declaración de `ACT_WA_TEMPLATE_IDS` en
   `index.js` (extraerla por regex del texto del archivo).

**Cableado de las 4 superficies (D-01)**
8. `_actButtonsHTML` está declarada una sola vez y se llama exactamente 4 veces.
9. Cada uno de los 4 cuerpos (`renderCallsList`, `_pdRender`,
   `_callsRenderExpandedPanel`, `_hoyRenderSection`) contiene
   `_actButtonsHTML(` — extraer el cuerpo por conteo de llaves balanceado
   (**ojo**: `_hoyRenderSection(..., opts = {})` tiene un `{}` de default param
   antes del cuerpo — usar el helper corregido de `commitment-hoy.test.js`).
10. La variante `'row'` incluye `event.stopPropagation()`.
11. `_pdRender` ya no menciona `lead.whatsappUrl` (el quick-link legacy se fue) y
    `_waBtnClick` sigue existiendo en el archivo (no se borró la feature, solo
    su uso en el dialer).

**Destino (GATE-04 + anti-deriva)**
12. Extraer el bloque `[30-03] DISPO-DEST`, evaluarlo con `new Function` y, con
    un reloj FIJO, verificar que un lead con `commitment {estado:'cumplido',
    parte:'yo', canal:'whatsapp'}` + `nextAction {origen:'compromiso',
    tipo:'esperar_respuesta', dueAt: +48h}` devuelve
    `vista === 'en Hoy → Esperando del prospecto'`.
13. El mismo lead con `canal:'email'` devuelve la variante de texto de material.
14. **Anti-deriva:** el literal del título que usa `loadHoyView` para esa sección
    aparece también dentro del bloque DISPO-DEST (mismo estilo que T-31-13 — si
    alguien renombra una sección, este test cae).
15. Las 8 ramas previas siguen devolviendo lo mismo: correr
    `tests/gate-destination.test.js` como parte del verify (no duplicar sus
    casos acá).

**Handler**
16. El cuerpo de `window._actWhatsApp` contiene `whatsapp-send`, `window.open(`,
    `_leadStoreApply(`, `forceToast` y `_refreshLeadPanels(`; y NO contiene
    `_pdRender(`.
17. Todo lo que se interpola del lead en el markup del overlay pasa por
    `escHtml(` (misma aserción que T-31-04: buscar ocurrencias de
    `lead.name`/`lead.phone`/`lead.altPhone` dentro del cuerpo del handler y
    verificar que están precedidas por `escHtml(`).

**Cache-buster**
18. `public/index.html` referencia `app.js?v=` con un valor que **cambió**
    respecto del que tenía al abrir este plan. Usar un chequeo de FORMA (regex
    `app\.js\?v=\d{8}[a-z]`), **no** un valor pineado — un pin exacto se rompe
    con cualquier edición legítima y ajena de otra sesión (lección de 31-03,
    commit `613256f`).

**B. Bumpear el cache-buster.** Leer `public/index.html`, tomar el valor REAL de
`app.js?v=` y subirlo a la siguiente letra libre (si el disco dice `20260815g`,
va `20260815h`). **No tocar** `style.css?v=` (este plan no edita `style.css`).

**Verificación por mutación (obligatoria):** romper temporalmente una de las 4
llamadas a `_actButtonsHTML` (por ejemplo, la de `_hoyRenderSection`), correr la
suite, anotar cuántos tests se ponen en rojo, y restaurar con
`git checkout -- public/app.js`. Confirmar `git diff public/app.js` vacío y
dejar el conteo en el SUMMARY.
  </action>

  <verify>
    <automated>npx vitest run tests/act-ui-whatsapp.test.js tests/gate-destination.test.js tests/commitment-ui.test.js tests/commitment-hoy.test.js tests/dtpicker-wiring.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/act-ui-whatsapp.test.js` pasa con al menos 18 tests.
    - `grep -n "app.js?v=" public/index.html` muestra un valor distinto al que
      tenía el archivo al comenzar este plan, y `git diff public/style.css` está
      vacío.
    - `grep -c "app.js?v=" public/index.html` devuelve 1 (no se duplicó la etiqueta).
    - Los tests del cache-buster usan un regex de forma, no un literal pineado
      (`grep -c "20260815" tests/act-ui-whatsapp.test.js` devuelve 0).
    - `npm test` completo verde salvo los flaky ambientales ya documentados
      (CLAUDE.md #93/#110/#130), re-corridos en aislado y anotados en el SUMMARY.
    - El SUMMARY documenta el conteo de rojos de la verificación por mutación y
      que `git diff public/app.js` quedó vacío al restaurar.
  </acceptance_criteria>

  <done>
El catálogo, el cableado de las 4 superficies y el aviso de destino quedan
protegidos por tests que fallan si alguien los desconecta, y los browsers van a
bajar el `app.js` nuevo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos del lead → markup del overlay | nombre/teléfono/etiqueta del lead se interpolan por `innerHTML` |
| overlay → `window.open(url)` | la URL viene del backend y se abre en una pestaña nueva |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-12 | Tampering | interpolación de `lead.name`/`altPhoneLabel` en el overlay | mitigate | `escHtml(` en toda interpolación; aserción de fuente en el test (mismo criterio que T-31-04) |
| T-32-13 | Tampering | `leadId` dentro de un `onclick="…('<id>')"` | mitigate | `escHtml(leadId)` en `_actButtonsHTML`, igual que el resto de los botones generados (y los ids del proyecto son `[a-z0-9_]`) |
| T-32-14 | Information Disclosure | `window.open` con `_blank` | mitigate | flag `noopener` en la llamada (evita `window.opener` desde la pestaña abierta) |
| T-32-15 | Repudiation | registro del envío | transfer | el registro lo hace el backend (32-01) con `req.auth.user`; el frontend no manda `by`/`byName` |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | esta fase NO instala paquetes: `git diff package.json package-lock.json` vacío |
</threat_model>

<verification>
- `node --check public/app.js` en cada task.
- `npx vitest run tests/act-ui-whatsapp.test.js` ≥ 18 tests verdes.
- `tests/gate-destination.test.js`, `tests/commitment-ui.test.js` y
  `tests/commitment-hoy.test.js` verdes SIN editarlos.
- `git diff public/style.css` vacío; `app.js?v=` bumpeado en `public/index.html`.
- `git diff package.json package-lock.json` vacío.
</verification>

<success_criteria>
- El botón de WhatsApp existe y funciona desde las 4 superficies con un solo
  componente.
- Un click abre `wa.me` en pestaña nueva con el mensaje cargado y deja el lead
  en "Hoy → Esperando del prospecto" con el aviso diciéndolo.
- Se puede mandar a otro número tipeado en el momento y guardarlo como contacto
  secundario sin tocar el principal.
- El Power Dialer no expulsa la tarjeta al mandar.
</success_criteria>

<output>
Crear `.planning/phases/32-act-acciones/32-03-SUMMARY.md` al terminar, con: la
firma final de `_actButtonsHTML` (32-04 la extiende), el valor REAL del
cache-buster que dejó este plan, los 3 `key`/`label` de las plantillas, el
resultado de la verificación por mutación y el checklist de lo que quedó SIN
verificar en vivo (no hay browser en el entorno).
</output>
