---
phase: 32-act-acciones
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - index.js
  - tests/act-discard-email.test.js
autonomous: true
requirements: [ACT-04, ACT-05]

must_haves:
  truths:
    - "El SDR (no solo el admin) puede descartar un lead propio desde un endpoint, con o sin razón"
    - "Descartar apaga el reloj y cierra el compromiso pendiente — el lead no puede quedar colgado en Hoy después de descartarlo"
    - "Una razón que implica 'no me llames más' descarta Y marca DNC, igual que hoy hace la disposición"
    - "Mandar material por email deja el MISMO evento que mandarlo por WhatsApp"
    - "El email no lleva ningún mecanismo de tracking de apertura"
  artifacts:
    - path: "index.js"
      provides: "POST /api/setters/leads/:id/discard + POST /api/setters/leads/:id/send-material + attachments opcionales en _sendPlaceholderEmail"
      contains: "/api/setters/leads/:id/discard"
    - path: "tests/act-discard-email.test.js"
      provides: "cobertura HTTP del descarte (RBAC, razón, DNC, compromiso, reloj) y del material por email"
      min_lines: 150
  key_links:
    - from: "POST /api/setters/leads/:id/discard"
      to: "_closeCommitment / _clearNextAction"
      via: "cierre del compromiso pendiente y apagado del reloj al pasar a terminal"
      pattern: "_clearNextAction\\(lead\\)"
    - from: "POST /api/setters/leads/:id/send-material"
      to: "_actRegisterSendEvent"
      via: "mismo modelo de evento que WhatsApp, canal 'email', dentro de mutateSettersData"
      pattern: "_actRegisterSendEvent\\("
    - from: "POST /api/setters/leads/:id/send-material"
      to: "_sendPlaceholderEmail"
      via: "envío por Resend sin adjunto .ics"
      pattern: "_sendPlaceholderEmail\\("
---

<objective>
Las dos acciones que faltan del lado del servidor: **descartar** un lead desde
cualquier vista sin ser admin (ACT-04, cerrando el hueco de RBAC que dejó el
endpoint `bulk`) y **mandar material por email** dejando exactamente el mismo
evento que el WhatsApp (ACT-05/D-17), sin ningún tracking de apertura (D-18).

Purpose: hoy el único descarte de un lead suelto vive en `POST /leads/bulk`, que
es `requireRole('admin')` — un SDR recibiría 403 desde las 4 superficies que
pide D-12. Y el único envío por email que existe está acoplado al `.ics` del
hold de calendario.
Output: `POST /api/setters/leads/:id/discard`, `POST /api/setters/leads/:id/send-material`,
`_sendPlaceholderEmail` con adjunto opcional, y una suite HTTP.

**Este plan NO toca `public/`.** Cero bump de cache-buster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-act-acciones/32-CONTEXT.md
@.planning/phases/32-act-acciones/32-PATTERNS.md
@.planning/phases/32-act-acciones/32-01-SUMMARY.md
@.planning/phases/31-comm-compromisos/31-01-SUMMARY.md
@CLAUDE.md

Reglas del proyecto que aplican:

- **Mutex (regla #19): SÍ hace falta en `send-material`** — la rama de envío
  tiene un `await` (Resend) ANTES de la mutación, exactamente el caso que
  documenta el comentario de `send-placeholder` (`index.js:12234-12236`). El
  endpoint de descarte, en cambio, es síncrono (mismo criterio que `bulk` y
  `reactivate`).
- **Orden de rutas Express (regla #3):** las dos rutas nuevas tienen sufijo
  estático después de `:id`; no compiten con `/leads/sin-wsp` ni con `/leads/bulk`.
- **D-13 y el hueco de RBAC (decisión ya tomada por este plan, no re-abrir):**
  el CONTEXT dice "reusa el endpoint que ya existe", pero `POST /leads/bulk` es
  `requireRole('admin')` (`index.js:10435`) y aflojarlo cambiaría una superficie
  de mutación masiva (7 acciones, hasta 500 leads) para todos los roles. Se
  implementa la opción 2 del PATTERNS: un endpoint de **un solo lead** que copia
  las 2 líneas del `case 'discard'` (`index.js:10489-10492`) y agrega el guard de
  dueño. El `bulk` queda **intacto y admin-only**.
- Regla #121 (tests): env vars de IA a `""`, **nunca** `delete`. Idem
  `RESEND_API_KEY`.
- Regla #163 (fixtures): teléfonos `+521…` de ≥7 dígitos.
</context>

<interfaces>
<!-- Contrato que consume 32-04 desde el frontend. -->

POST /api/setters/leads/:id/discard    (auth: requireAuth)
  body:
    reason?    string  — id de DISQUALIFY_REASONS. Fuera de la whitelist o
                         ausente → '' (D-14: pide razón pero NO bloquea).
    doNotCall? boolean — fuerza DNC además del descarte.
  respuestas:
    200 { ok:true, lead:{id, ...lead}, commitmentClosed:boolean, doNotCall:boolean }
    403 { error } — setter que no es dueño / lead fuera de visibilidad
    404 { error } — lead inexistente

POST /api/setters/leads/:id/send-material    (auth: requireAuth)
  body:
    via?        'resend' | 'mailto'  — default 'resend'. Fuera de whitelist → 'resend'.
    email?      string  — destino. Default lead.email. Formato inválido → 400.
    subject?    string  — asunto (cap 140, _stripBrandMentions).
    message?    string  — cuerpo en texto plano (cap 900, _stripBrandMentions).
    templateId? string  — D-07, misma whitelist que WhatsApp.
  respuestas:
    200 { ok:true, sent:boolean, via, sentTo, mailtoUrl, commitment, nextAction,
          lead:{id, ...lead} }
    400 { error } — email ausente o inválido
    409 { error, resendUnavailable:true, mailtoUrl } — via='resend' sin
          RESEND_API_KEY configurada. NO registra nada.
    502 { error, detail } — Resend respondió error. NO registra nada.
    403 / 404 — iguales que arriba
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: POST /api/setters/leads/:id/discard (ACT-04, D-12..D-16)</name>

  <read_first>
    - `index.js` líneas 10432-10540 — `POST /api/setters/leads/bulk` COMPLETO:
      el `case 'discard'` (10489-10492, las 2 líneas de estado que hay que
      copiar), el `case 'mark_dnc'` (10493-10499) y la entry de auditoría
      (10529-10536). **Este endpoint no se modifica.**
    - `index.js` líneas 10542-10588 — `POST /leads/:id/reactivate`: la acción
      inversa. Es el molde de posición (el endpoint nuevo va justo después),
      de `previousState` en la entry de auditoría y del uso de `_clearNextAction`.
    - `index.js` líneas 10380-10387 — los 2 guards de auth de
      `PATCH .../commitment` (copiar literales).
    - `index.js` líneas 11584-11592 — el bloque DNC de `_applyCallOutcome`:
      los nombres EXACTOS de los 4 campos (`doNotCall`, `doNotCallReason`,
      `doNotCallAt`, `doNotCallBy`) y el criterio `DNC_REASONS.has(cleanReason)`.
    - `index.js` líneas 11773-11785 — `DISQUALIFY_REASONS` y `DNC_REASONS`.
    - `index.js` línea 11427 — `_closeCommitment(lead, estado, nowIso, closedBy)`:
      qué cierres acepta (`COMMITMENT_CIERRES`) y que solo apaga el reloj si
      `nextAction.origen === 'compromiso'`.
    - `public/app.js` líneas 5983-5986 — las 2 secciones de compromiso de
      `loadHoyView` NO filtran por estado terminal: es la razón técnica por la
      que este endpoint TIENE que cerrar el compromiso pendiente.
  </read_first>

  <files>index.js</files>

  <action>
Agregar `app.post('/api/setters/leads/:id/discard', requireAuth, (req, res) => {...})`
inmediatamente después del cierre de `POST /api/setters/leads/:id/reactivate`
(~línea 10588) y antes de `PUT .../precall-note` (10592).

Comentario de cabecera obligatorio, con las 3 razones de existir: (1) el
descarte de un lead suelto solo existía en `bulk`, que es admin-only, y D-12
lo pide desde las 4 superficies donde trabaja el SDR; (2) copia el `case
'discard'` del bulk en vez de aflojar ese endpoint (que mueve hasta 500 leads
con 7 acciones distintas); (3) es síncrono, así que no necesita el mutex de
escrituras async (regla #19).

Cuerpo, en este orden:

1. `loadSettersData()` → lead → 404 si no existe.
2. Los 2 guards de auth copiados de `index.js:10384-10387`.
3. `ensureLeadDefaults(lead);`
4. `const { reason, doNotCall } = req.body || {};`
   `const cleanReason = (typeof reason === 'string' && DISQUALIFY_REASONS.has(reason)) ? reason : '';`
   Comentario: whitelist-and-coerce, **nunca 400 por la razón** (D-14: un
   descarte sin motivo es mejor que un lead que sigue en la cola porque el
   formulario era largo).
5. **Estado terminal** (copiado literal del `case 'discard'`):
   `lead.estado = 'descartado'; lead.interes = 'no';`
   Y si `cleanReason`: `lead.disqualifyReason = cleanReason;` (campo que ya
   existe en `ensureLeadDefaults`, línea 685).
6. **DNC** (mismo criterio que `_applyCallOutcome`, 11586-11592):
   si `doNotCall === true || DNC_REASONS.has(cleanReason)` → setear los 4
   campos `doNotCall/doNotCallReason/doNotCallAt/doNotCallBy` con
   `cleanReason || 'manual'`, `nowIso` y `req.auth?.user?.name || ''`.
7. **Cerrar el compromiso pendiente y apagar el reloj** — la parte que NO existe
   en el bulk y que ACT-04 exige:
   `const commitmentClosed = !!(lead.commitment && lead.commitment.estado === 'pendiente' && _closeCommitment(lead, 'vencido', nowIso, req.auth?.user?.name || ''));`
   seguido SIEMPRE de `_clearNextAction(lead);`.
   Comentario obligatorio con las dos razones: (a) `'vencido'` es el cierre de
   "ya no aplica" para un compromiso propio (31-01), y es el único que no
   dispara el seguimiento post-envío de +48h que sí dispara `'cumplido'`;
   (b) `_clearNextAction` va aparte y siempre porque `_closeCommitment` solo
   apaga el reloj cuando `origen === 'compromiso'`, y un lead terminal nunca
   puede llevar `nextAction` (regla dura de la Phase 30,
   `GATE_TERMINAL_ESTADOS`).
8. **Auditoría**: push en `lead.interactions` de
   `{ action: 'discard', reason: cleanReason, by: req.auth?.user?.id || '', byName: req.auth?.user?.name || req.auth?.user?.email || '', createdAt: nowIso }`
   + recorte a 200. **Sin `setterId`** (misma razón que en 32-01: `_perfAggregate`
   lo lee para atribuir "lead trabajado") y **sin entry en `callLog`** (misma
   razón: `_ccCollectCalls` cuenta cada entry como un dial; el `bulk` tampoco
   escribe callLog en el caso `discard`).
9. `saveSettersData(data);` +
   `res.json({ ok: true, lead: { id: req.params.id, ...lead }, commitmentClosed, doNotCall: !!lead.doNotCall });`

**No tocar** `POST /leads/bulk` (sigue `requireRole('admin')`), ni
`reactivate`, ni `_applyCallOutcome`.
  </action>

  <verify>
    <automated>node --check index.js &amp;&amp; npx vitest run tests/disposition-dnc.test.js tests/security-rbac.test.js tests/commitment-endpoints.test.js tests/metrics-consistency.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` sale con código 0.
    - `grep -n "app.post('/api/setters/leads/:id/discard'" index.js` devuelve
      exactamente 1 línea, con número entre el de
      `app.post('/api/setters/leads/:id/reactivate'` y el de
      `app.put('/api/setters/leads/:id/precall-note'`.
    - `grep -n "requireRole('admin')" index.js` en la línea de
      `app.post('/api/setters/leads/bulk'` SIGUE presente (el bulk no se aflojó);
      `git diff index.js` no muestra cambios entre las líneas 10432-10540.
    - El endpoint nuevo contiene literalmente `lead.estado = 'descartado';` y
      `lead.interes = 'no';` (paridad con el `case 'discard'`).
    - `_clearNextAction(lead)` aparece dentro del cuerpo del endpoint nuevo
      (`grep -n "_clearNextAction(lead)" index.js` incluye una línea dentro de su
      rango).
    - El bloque del endpoint nuevo NO contiene la substring `callLog` ni
      `setterId`.
    - Las 4 suites del comando de verify pasan sin fallos.
  </acceptance_criteria>

  <done>
Un SDR puede descartar un lead propio en un request; el lead queda terminal, sin
reloj y sin compromiso pendiente, y el endpoint masivo de admin quedó intacto.
  </done>
</task>

<task type="auto">
  <name>Task 2: Material por email — adjunto opcional + POST .../send-material (ACT-05, D-17/D-18)</name>

  <read_first>
    - `index.js` líneas 12141-12170 — `_sendPlaceholderEmail` COMPLETA: es la
      función a la que hay que hacerle el adjunto opcional. Nótese que hoy
      SIEMPRE arma `attachments` con `icsContent`.
    - `index.js` líneas 12172-12266 — `POST .../send-placeholder` COMPLETO: el
      único caller actual de esa función, el guard de auth, la validación de
      email (12189-12192), el `htmlBody` y el bloque `mutateSettersData`
      (12234-12264) con su comentario sobre por qué el mutex es obligatorio acá.
      El endpoint nuevo va INMEDIATAMENTE DESPUÉS de este.
    - El bloque `ACCIONES (Phase 32)` de 32-01 (`_actSanitizeMessage`,
      `_actRegisterSendEvent`, `ACT_WA_TEMPLATE_IDS`).
    - `index.js` líneas 925-932 — `_stripBrandMentions`.
    - `.planning/phases/32-act-acciones/32-01-SUMMARY.md` — la firma REAL de
      `_actRegisterSendEvent` (manda sobre lo que dice este plan).
  </read_first>

  <files>index.js</files>

  <action>
**A. Adjunto opcional en `_sendPlaceholderEmail` (index.js:12141).**
Cambiar el `body: JSON.stringify({...})` para que la clave `attachments` solo se
incluya cuando `icsContent` es un string no vacío. Implementarlo armando el
payload en una variable previa y agregando la clave con un `if` (no con un
spread condicional inline, para que el diff sea legible). **Cero cambios de
comportamiento para `send-placeholder`**, que siempre pasa `icsContent`.
Comentario: la función pasa a servir a los dos envíos (hold de calendario con
`.ics` y material sin adjunto) — un solo call site de Resend en todo el archivo.

**B. `_actEmailHtml(text)`** en el bloque `ACCIONES (Phase 32)`: helper puro que
escapa `&`, `<`, `>` y convierte los saltos de línea en `<br>`, devolviendo el
cuerpo listo para el `htmlBody`. Exponerlo en `globalThis.__voiceAgent` junto a
los otros helpers de la fase. Comentario: el texto lo tipea el SDR y termina
dentro de un HTML que abre el prospecto — escapar es obligatorio.

**C. `app.post('/api/setters/leads/:id/send-material', requireAuth, async (req, res) => {...})`**
inmediatamente después del cierre de `send-placeholder` (~línea 12266).

Cabecera con las 3 decisiones: mismo modelo de evento que el WhatsApp (D-17, se
reusa `_actRegisterSendEvent`); **cero tracking de apertura** (D-18 — ni pixel,
ni link de redirección, ni webhook: Apple MPP volvió ruido esa métrica); y dos
vías, porque `RESEND_API_KEY` puede no estar configurada en producción.

Cuerpo:
1. `loadSettersData()` → lead → 404 → los 2 guards de auth → `ensureLeadDefaults`.
2. Destructuring `{ via, email, subject, message, templateId }`.
   `const cleanVia = (via === 'mailto') ? 'mailto' : 'resend';` (whitelist-and-coerce).
3. Destino: `const toEmail = String(email || lead.email || '').trim();`
   validar con la MISMA regex que `send-placeholder` (12190); inválido/ausente →
   `400 { error: 'Falta o es inválido el email del prospect.' }`.
4. Texto: `const cleanSubject = _stripBrandMentions(String(subject || '').trim()).slice(0, 140) || 'La información que te prometí';`
   y `const cleanMessage = _actSanitizeMessage(message);`
   (si `cleanMessage` queda vacío → `400 { error: 'El mensaje no puede estar vacío.' }`).
5. `const mailtoUrl = 'mailto:' + encodeURIComponent(toEmail) + '?subject=' + encodeURIComponent(cleanSubject) + '&body=' + encodeURIComponent(cleanMessage);`
   Se devuelve en TODAS las respuestas (200, 409) para que el frontend pueda
   ofrecer la salida manual sin recalcular nada.
6. **Rama `resend`:**
   - Si no hay `process.env.RESEND_API_KEY` → `409 { error: 'El envío por el sistema no está configurado. Podés abrirlo en tu cliente de mail.', resendUnavailable: true, mailtoUrl }` y **cortar sin registrar**.
   - Si hay: `const htmlBody = ...` armado con el mismo esqueleto `<div style="font-family:sans-serif; max-width:520px; …">` de `send-placeholder` (12214-12220) pero con `_actEmailHtml(cleanMessage)` como cuerpo y la firma `— ${_actEmailHtml(u.name || 'Equipo')}`. **Sin `<img>` de ningún tipo** (D-18).
   - `const result = await _sendPlaceholderEmail({ toEmail, toName, subject: cleanSubject, htmlBody, fromOverride: process.env.PLACEHOLDER_FROM_EMAIL || undefined });`
     (sin `icsContent` → sin adjunto).
   - Si `!result.sent` → `502 { error: 'No se pudo enviar el mail.', detail: result.reason }` y **cortar sin registrar** (nada salió, el registro no puede decir que sí).
   - `sent = true`.
7. **Rama `mailto`:** no se intenta enviar nada. `sent = false`. Comentario:
   el registro es igual de honesto que el del WhatsApp (D-04) — se anota "abrí
   el mail para mandar X", no "lo recibió".
8. **Registro dentro del mutex** (regla #19: la rama `resend` tiene un `await`
   antes; se usa el mismo camino para las dos para no tener dos escrituras
   distintas). Copiar la forma de `send-placeholder` (12236-12264):
   `const updated = await mutateSettersData((fresh) => { const l = fresh.leads?.[req.params.id]; if (!l) return null; ensureLeadDefaults(l); _actRegisterSendEvent(l, { canal: 'email', templateId, to: toEmail, byId: u.id || '', byName: u.name || '' }, nowIso); return l; });`
9. Respuesta:
   `res.json({ ok: true, sent, via: cleanVia, sentTo: toEmail, mailtoUrl, commitment: (updated || lead).commitment, nextAction: (updated || lead).nextAction, lead: { id: req.params.id, ...(updated || lead) } });`

**No tocar** `send-placeholder`, `_buildPlaceholderICS` ni `_icsEscape`.
  </action>

  <verify>
    <automated>node --check index.js &amp;&amp; npx vitest run tests/gate-next-action.test.js tests/commitment-endpoints.test.js tests/act-whatsapp.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` sale con código 0.
    - `grep -c "api.resend.com/emails" index.js` devuelve exactamente 1 (sigue
      habiendo un solo call site de Resend en todo el archivo).
    - `grep -n "app.post('/api/setters/leads/:id/send-material'" index.js`
      devuelve 1 línea, con número mayor al de
      `app.post('/api/setters/leads/:id/send-placeholder'`.
    - El cuerpo del endpoint nuevo contiene `mutateSettersData(` y
      `_actRegisterSendEvent(` (el registro va dentro del mutex).
    - El `htmlBody` del endpoint nuevo NO contiene la substring `<img` ni la
      palabra `pixel` (D-18).
    - `git diff index.js` no muestra cambios dentro de
      `POST .../send-placeholder` salvo, a lo sumo, ninguno: el diff en esa zona
      debe estar vacío.
    - `npx vitest run tests/gate-next-action.test.js` sigue verde (contiene la
      aserción de fuente de `send-placeholder`).
  </acceptance_criteria>

  <done>
Mandar material por email deja el mismo compromiso cumplido + seguimiento a 48h
que el WhatsApp, funciona con Resend o abriendo el cliente de mail del SDR, y no
mide aperturas.
  </done>
</task>

<task type="auto">
  <name>Task 3: Suite HTTP del descarte y del material por email</name>

  <read_first>
    - `tests/act-whatsapp.test.js` (creado en 32-01) — el setup a calcar y los
      helpers ya escritos (`lead(n, extra)`, `hoursFromNow`, cookies).
    - `tests/disposition-dnc.test.js` — cómo se asertan hoy los campos de DNC y
      la salida de la cola (`GET /leads/sin-wsp`).
    - `tests/commitment-endpoints.test.js` líneas 1-95 — fixture base.
    - Los dos endpoints creados en las Tasks 1 y 2.
  </read_first>

  <files>tests/act-discard-email.test.js</files>

  <action>
Crear `tests/act-discard-email.test.js` con fixture propio (admin + `s_owner` +
`s_other`, leads `+521…`). **`process.env.RESEND_API_KEY = ''`** en el bloque de
env vars (regla #121: `""`, nunca `delete` — con `delete`, `dotenv` lo repone
desde `.env` y el test intentaría mandar un email real).

**Descarte (ACT-04)**
1. `s_owner` descarta su lead sin body → 200, `lead.estado === 'descartado'`,
   `lead.interes === 'no'`, `lead.disqualifyReason === ''` (D-14: sin razón
   también descarta).
2. Con `{reason:'no_es_icp'}` → `lead.disqualifyReason === 'no_es_icp'`,
   `lead.doNotCall === false`.
3. Con `{reason:'no_contactar'}` → `doNotCall === true`, `doNotCallReason ===
   'no_contactar'`, `doNotCallBy` con el nombre del user autenticado (nunca del
   body).
4. Con `{doNotCall:true}` y sin razón → `doNotCall === true`,
   `doNotCallReason === 'manual'`.
5. Con `{reason:'basura'}` → 200 y `disqualifyReason === ''` (nunca 4xx).
6. Lead con compromiso PENDIENTE (crearlo antes vía `PATCH .../commitment`) →
   tras descartar: `commitment.estado === 'vencido'`, `commitment.closedAt`
   seteado, `lead.nextAction` vacío/null y `lead.callbackAt === ''`
   (`commitmentClosed:true` en la respuesta).
7. Lead SIN compromiso → `commitmentClosed:false` y `nextAction` igualmente
   apagado.
8. `lead.callLog` conserva la MISMA longitud que antes del descarte (no infla el
   funnel) y la última entry de `interactions` es `action:'discard'` sin clave
   `setterId`.
9. `GET /api/setters/leads/sin-wsp` como `s_owner` ya no incluye el lead
   descartado por DNC (usar el criterio que ya asertan los tests de DNC).

**RBAC (el hueco que cierra este plan)**
10. `s_other` sobre un lead de `s_owner` → 403.
11. Sin cookie → 401. Lead inexistente → 404.
12. **El bulk sigue siendo admin-only**: `POST /api/setters/leads/bulk` con
    `{leadIds:[…], action:'discard'}` y la cookie de `s_owner` → 403 (regresión
    explícita: este plan no aflojó esa superficie).

**Material por email (ACT-05)**
13. `via:'mailto'` con `{message:'te paso la info'}` sobre un lead con email →
    200, `sent:false`, `mailtoUrl` empieza con `mailto:` y contiene el subject
    y el body URL-encodeados.
14. Tras ese envío: `lead.commitment.tipo === 'enviar_info'`,
    `canal === 'email'`, `estado === 'cumplido'`, y `lead.nextAction.tipo ===
    'esperar_respuesta'` con `dueAt` a 47-49 horas.
15. La entry de `interactions` es `action:'material_sent'` con `canal:'email'`.
16. `via:'resend'` sin `RESEND_API_KEY` → 409 con `resendUnavailable:true` y
    `mailtoUrl`, y el lead **no cambió** (`commitment` sigue como estaba).
17. Sin email en el lead ni en el body → 400.
18. `message` vacío → 400.
19. `subject` con la marca → el `mailtoUrl` no la contiene (D-08 vía
    `_stripBrandMentions`).
20. Aserción de fuente (D-18): leer `index.js` y verificar que el bloque del
    endpoint `send-material` no contiene `<img` (sin pixel de tracking).

**Verificación por mutación (obligatoria):** comentar temporalmente la línea
`_clearNextAction(lead);` del endpoint de descarte, correr la suite, anotar
cuántos tests se ponen en rojo (esperado: los de los puntos 6 y 7), y restaurar
con `git checkout -- index.js`. Confirmar `git diff index.js` vacío y dejar el
conteo en el SUMMARY.
  </action>

  <verify>
    <automated>npx vitest run tests/act-discard-email.test.js tests/act-whatsapp.test.js tests/disposition-dnc.test.js tests/metrics-consistency.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/act-discard-email.test.js` pasa con al menos 20 tests.
    - `grep -c "RESEND_API_KEY = ''" tests/act-discard-email.test.js` devuelve 1 y
      `grep -c "delete process.env" tests/act-discard-email.test.js` devuelve 0.
    - El test 12 (bulk sigue admin-only) está presente y pasa.
    - `npx vitest run tests/metrics-consistency.test.js` verde sin editar el
      archivo.
    - `npm test` completo queda verde salvo los flaky ambientales ya documentados
      (CLAUDE.md #93/#110/#130), re-corridos en aislado y anotados en el SUMMARY.
    - El SUMMARY documenta el conteo de rojos de la verificación por mutación.
  </acceptance_criteria>

  <done>
Los dos endpoints están probados en camino feliz, rechazos y RBAC, con una
prueba explícita de que descartar apaga el reloj y de que el bulk de admin no se
aflojó.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `POST /leads/:id/discard` | mutación de estado terminal disponible ahora para rol `setter` |
| browser → `POST /leads/:id/send-material` | asunto/cuerpo/destinatario controlados por el cliente, salen a un tercero (Resend) y llegan al prospecto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Elevation of Privilege | `POST .../discard` | mitigate | endpoint de UN lead con los 2 guards de dueño/visibilidad; el `bulk` masivo sigue `requireRole('admin')` — test 12 lo verifica como regresión |
| T-32-07 | Tampering | `body.reason` | mitigate | whitelist `DISQUALIFY_REASONS`, desconocido → `''` (nunca 4xx, D-14) |
| T-32-08 | Information Disclosure | `subject`/`message` del email | mitigate | `_stripBrandMentions` + caps (140/900) + `_actEmailHtml` escapando `& < >` antes de meterlo en el HTML |
| T-32-09 | Repudiation | `doNotCallBy` / `byName` | mitigate | se toman de `req.auth.user`, nunca del body (mismo criterio que la nota #26) |
| T-32-10 | Spoofing | `body.email` arbitrario | accept | el SDR puede mandar material al email que le pasaron por teléfono; se valida formato y se registra a quién se mandó (`to` en la entry de auditoría). Riesgo bajo: el remitente es la cuenta Resend del proyecto y el volumen es de unidades por día |
| T-32-11 | Denial of Service | envío por Resend en loop | accept | endpoint autenticado, 1 lead por request, volumen real 5-10/día (fuera de alcance de esta fase un rate-limit propio) |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | esta fase NO instala paquetes: `git diff package.json package-lock.json` vacío |
</threat_model>

<verification>
- `node --check index.js` en cada task.
- `npx vitest run tests/act-discard-email.test.js` ≥ 20 tests verdes.
- `git diff index.js` sin cambios en el rango del endpoint `bulk` (10432-10540)
  ni dentro de `send-placeholder`.
- `grep -c "api.resend.com/emails" index.js` === 1.
- `git diff package.json package-lock.json` vacío.
</verification>

<success_criteria>
- Un SDR descarta su propio lead en un request; el lead sale de toda cola y no
  queda ningún compromiso ni reloj colgado.
- Mandar material por email produce el mismo `lead.commitment` + `nextAction`
  que mandarlo por WhatsApp.
- El endpoint masivo de admin quedó exactamente como estaba.
- `public/` sin tocar: cero bump de cache-buster en este plan.
</success_criteria>

<output>
Crear `.planning/phases/32-act-acciones/32-02-SUMMARY.md` al terminar, con: el
contrato HTTP final de los 2 endpoints (32-04 lo consume), qué decidió el
executor sobre la rama `resend` cuando la key no está configurada, el resultado
de la verificación por mutación y el conteo de la suite completa.
</output>
