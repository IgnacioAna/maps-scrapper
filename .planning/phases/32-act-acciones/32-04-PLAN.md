---
phase: 32-act-acciones
plan: 04
type: execute
wave: 4
depends_on: ["32-02", "32-03"]
files_modified:
  - public/app.js
  - public/index.html
  - tests/act-ui-discard-material.test.js
autonomous: true
requirements: [ACT-04, ACT-05]

must_haves:
  truths:
    - "Hay un botón de descartar en las mismas 4 vistas donde está el de WhatsApp, y descartar saca el lead de Llamadas, del Power Dialer y de Hoy de una sola vez"
    - "Descartar pide la razón pero no la exige — un descarte sin motivo se completa igual"
    - "El descarte se confirma en un solo paso y el aviso dice a dónde fue el lead"
    - "Un lead descartado se ve atenuado en gris, nunca en rojo de alarma"
    - "Mandar material por email deja el mismo evento que el WhatsApp y se ve en el mismo historial del lead"
  artifacts:
    - path: "public/app.js"
      provides: "window._actDiscard, botón de descartar dentro de _actButtonsHTML, filtro terminal en las secciones de compromiso de Hoy, window._actSendMaterial"
      contains: "window._actDiscard"
    - path: "tests/act-ui-discard-material.test.js"
      provides: "cableado del descarte en las 4 superficies, estado bloqueado, filtro de Hoy y flujo de material por email"
      min_lines: 140
  key_links:
    - from: "_actButtonsHTML"
      to: "window._actDiscard"
      via: "segundo botón en el mismo builder — las 4 superficies no se vuelven a tocar"
      pattern: "window._actDiscard\\("
    - from: "window._actDiscard"
      to: "POST /api/setters/leads/:id/discard"
      via: "fetch + _leadStoreApply + _dispoAnnounce(forceToast)"
      pattern: "/discard"
    - from: "loadHoyView"
      to: "terminal(l)"
      via: "las 2 secciones de compromiso dejan de mostrar leads descartados/agendados"
      pattern: "!terminal\\(l\\)"
---

<objective>
Cerrar la fase con las dos acciones que faltan en la superficie: **descartar**
desde las mismas 4 vistas (D-12..D-16) —y que el lead desaparezca de todas de
una sola vez— y **mandar material por email** desde la ficha, con el mismo
evento y el mismo historial que el WhatsApp (ACT-05).

Purpose: ACT-04 es el segundo botón que el user nombró textualmente ("un botón
en todos lados para descartar el lead, que desaparezca de las listas de todos
lados"). El backend está listo desde 32-02 y el builder de botones desde 32-03.
Output: `window._actDiscard`, el botón sumado al builder único, el estado
bloqueado de marca, el filtro que saca los descartados de las secciones de
compromiso de Hoy, `window._actSendMaterial` en la ficha, y la suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-act-acciones/32-CONTEXT.md
@.planning/phases/32-act-acciones/32-PATTERNS.md
@.planning/phases/32-act-acciones/32-02-SUMMARY.md
@.planning/phases/32-act-acciones/32-03-SUMMARY.md
@.planning/phases/31-comm-compromisos/31-04-SUMMARY.md
@.planning/phases/30-gate-proximo-paso/30-03-SUMMARY.md
@CLAUDE.md

Reglas del proyecto que aplican:

- **Cache-buster:** este plan toca `public/app.js` → bumpear `app.js?v=` en
  `public/index.html`. **Leer el valor real que dejó 32-03** (no asumirlo).
  `style.css` NO se toca: `.scm-chip-blocked`, `.scm-row-blocked`, `.scm-phone`,
  `.btn-accent` y `.call-action-btn` ya existen
  (`public/style.css:4229-4255` y `5094-5182`).
- **z-index 10060** para el overlay nuevo, por la misma razón que en 32-03 (se
  puede abrir desde la ficha de Hoy, que es un `.modal-overlay` en 10000).
- **Aviso de destino (30-03):** `_dispoAnnounce(leadId, { lead, forceToast:true })`,
  nunca un `showToast` de destino a mano. La rama de `estado === 'descartado'`
  ya existe (`public/app.js:8892-8905`) — no hay que escribir texto nuevo.
- **`_dispoAfterSaved` NO** se usa acá: libera el gate de disposición obligatoria
  de la Phase 20 y un descarte no marca el resultado de ninguna llamada (mismo
  criterio que tomó 30-03 con el hold de calendario).
- **D-16 (marca):** el estado descartado se comunica en gris atenuado con ícono
  + etiqueta (`.scm-chip-blocked`), **nunca en rojo**: "un número que no se puede
  marcar no necesita alarma, necesita ausencia de acción"
  (`public/style.css:5140-5141`).
</context>

<interfaces>
<!-- Consumido por: nada más dentro de esta fase (es el último plan). -->

window._actDiscard(leadId)      — overlay de un paso: razón opcional + DNC opcional.
window._actSendMaterial(leadId) — overlay de material por email (2 vías).

_actButtonsHTML(leadId, opts)   — sin cambio de firma: resuelve el lead por
  `_callsLeadsById` para decidir si muestra el botón de descartar o el chip de
  estado bloqueado. Los 4 call sites de 32-03 NO se tocan.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Botón de descartar en el builder único + overlay de un paso + Hoy sin descartados</name>

  <read_first>
    - `public/app.js` — `_actButtonsHTML` tal como la dejó 32-03 (buscar por
      nombre, no por número de línea) y sus 4 call sites.
    - `public/app.js` líneas 7856-7883 — `window._callsReactivate`: la acción
      inversa y el molde exacto (fetch → `_leadStoreApply` →
      `_callsRenderCountryChips()` → `_refreshLeadPanels` → `renderCallsStats()`
      → toast).
    - `public/app.js` líneas 11520-11532 — `DISQUALIFY_REASONS_UI` (la lista a
      reusar) y ~11589, el one-liner `reasonSel.innerHTML = DISQUALIFY_REASONS_UI.map(...)`
      de `openObjectionModal`.
    - `public/app.js` líneas 8880-8905 — la rama `estado === 'descartado'` de
      `_dispoDestination` (ya escrita, no se toca) y `_dispoAnnounce` con
      `forceToast` (agregado en 32-03).
    - `public/app.js` líneas 5960-5999 — `loadHoyView`: el helper `terminal(l)`
      (5964) y las 2 líneas de filtro de compromisos (5983-5986) que hay que
      completar.
    - `public/app.js` líneas 8475-8520 — `renderCallsList`: `isDiscarded`,
      `discardedBadge`, `cardBorder` y el `<span>` del teléfono con estilos mono
      inline (8513).
    - `public/app.js` líneas 6130 — el `<span>` del teléfono en la fila de Hoy.
    - `public/app.js` líneas 6793-6814 y 7416-7418 — `_pdRender` (expulsión) y
      `_pdAdvance` (resetea `holdCurrent`/`holdOutcome`/`holdMeta`).
    - `public/style.css` líneas 5140-5172 — `.scm-chip-blocked` y
      `.scm-row-blocked` (incluido el guard `[data-action="call"]`, que este plan
      deliberadamente NO activa — ver acción).
  </read_first>

  <files>public/app.js</files>

  <action>
**A. `window._actDiscard(leadId)`** — declararla junto a `window._actWhatsApp`.
1. Resolver el lead (`_callsLeadsById` con fallback a `callsLeadsCache`); si no
   está, toast de error y `return`.
2. Overlay `#act-discard-overlay` en `document.body`, `z-index:10060`, cierre por
   click afuera / Cancelar / Escape. **Un solo paso (D-15)**: todo visible a la
   vez, sin pantalla de confirmación aparte:
   - Título con el nombre del lead (`escHtml`).
   - `<select id="act-discard-reason">` poblado con
     `DISQUALIFY_REASONS_UI.map(...)` — **reusar el array existente, no escribir
     una lista nueva**. La opción por defecto es la primera
     (`{ key:'', label:'Sin razón específica' }`), que es la que hace literal el
     D-14 ("pide razón, pero no bloquea").
   - Checkbox `No volver a llamar (DNC)`, sin marcar por defecto. Nota inline
     aclarando que elegir la razón "Pidió NO ser contactado (DNC)" ya lo marca
     sola (el backend lo deriva de `DNC_REASONS`).
   - CTA `Descartar` + `Cancelar`. El CTA **no** usa rojo (D-16): botón neutro
     con borde, mismo lenguaje que `.call-action-btn`.
3. Al confirmar: `POST` a `/api/setters/leads/<id>/discard` vía `apiUrl(...)` con
   `{ reason, doNotCall }`.
   - `!r.ok` → toast con `d.error` y `return`.
   - OK → `_leadStoreApply(leadId, d.lead)` →
     `_dispoAnnounce(leadId, { lead: d.lead, forceToast: true })` →
     `_callsRenderCountryChips?.()` y `renderCallsStats?.()` (mismo cierre que
     `_callsReactivate`).
   - Si el Power Dialer está activo y el lead descartado es el de la tarjeta
     actual (`_pd.active && _pd.queue[_pd.currentIdx] === leadId`) →
     `_pdAdvance()`. Comentario: la tarjeta dejó de corresponder; `_pdAdvance`
     ya resetea `holdCurrent`/`holdOutcome`/`holdMeta` y renderiza el siguiente.
     **No** setear `_pd.holdCurrent` (pintaría "✓ Resultado guardado", que sería
     falso: no se marcó ninguna disposición).
   - Si la vista Hoy está visible (`document.querySelector('#view-hoy:not(.hidden)')`)
     → `loadHoyView()`; mismo guard que usa `_dispoAfterSaved` (8833-8835).
4. `finally`: cerrar el overlay y `_refreshLeadPanels(leadId)` (nota #175).

**B. Botón de descartar dentro de `_actButtonsHTML`** (el punto de extensión que
dejó 32-03; **los 4 call sites no se tocan**).
- La función resuelve el lead internamente: `const lead = _callsLeadsById?.get?.(leadId)`
  (defensivo: si no está, se comporta como lead no-descartado).
- Si `lead.estado === 'descartado'`: en vez del botón, emitir un chip de estado
  `class="scm-chip-blocked"` con ícono + la etiqueta `Descartado` (D-16: gris,
  nunca rojo; la forma y el texto llevan la información, no el color).
- Si no: botón `Descartar` con `onclick="window._actDiscard('<id escapado>')"`,
  con `event.stopPropagation();` primero en la variante `'row'` y las mismas
  clases por variante que el botón de WhatsApp
  (`pd-quick-link` / `call-action-btn` / `hoy-ficha-btn` / pill inline).

**C. Estado bloqueado en la fila de Llamadas (D-16).**
- En `renderCallsList`, agregar la clase `scm-row-blocked` a la `call-row`
  cuando `isDiscarded` (junto a `is-expanded`/`is-selected`).
- Agregar `class="scm-phone"` al `<span>` del teléfono de `renderCallsList`
  (8513) y al de `_hoyRenderSection` (6130), **conservando los estilos inline
  actuales** — es lo que hace que `.scm-row-blocked .scm-phone` tache el número
  y lo que vuelve literal D-11.
- **NO** agregar `data-action="call"` al botón Llamar. La regla
  `.scm-row-blocked [data-action="call"] { display:none !important }` existe y
  está lista, pero activarla ocultaría el botón de llamar de todo lead
  descartado visible con el toggle "ver descartados" — un cambio de
  comportamiento que ACT-04 no pide. Dejarlo anotado en un comentario como hook
  disponible para una fase futura.

**D. Hoy: las secciones de compromiso dejan de mostrar terminales.**
En `loadHoyView` (5983-5986), agregar `!terminal(l)` a los dos filtros
(`misCompromisos` y `compromisosProspecto`). Comentario obligatorio: 32-02 hace
que el descarte NUEVO cierre el compromiso, pero un lead descartado por otras
vías (el `bulk` de admin, un `answered_not_interested`) conserva su compromiso
pendiente y hoy se quedaría en estas secciones para siempre — este filtro es la
red que hace verdadera la promesa de ACT-04 ("sale de todas las listas de una").

**No tocar** `_dispoDestination`, `_dispoAfterSaved`, `_pdRender`,
`openObjectionModal` ni `_callsReactivate`.
  </action>

  <verify>
    <automated>node --check public/app.js &amp;&amp; npx vitest run tests/act-ui-whatsapp.test.js tests/commitment-hoy.test.js tests/gate-destination.test.js tests/commitment-ui.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "window._actDiscard" public/app.js` devuelve ≥ 2 (declaración +
      uso en `_actButtonsHTML`).
    - `grep -c "_actButtonsHTML(" public/app.js` sigue devolviendo 5 (los 4 call
      sites NO se multiplicaron: el botón nuevo entró en el builder).
    - `grep -c "scm-chip-blocked" public/app.js` devuelve ≥ 1 y
      `grep -c "scm-row-blocked" public/app.js` devuelve ≥ 1.
    - `grep -c "no_es_icp" public/app.js` sigue devolviendo 1 (se reusó
      `DISQUALIFY_REASONS_UI`, no se duplicó la lista).
    - Las 2 líneas de filtro de compromisos de `loadHoyView` contienen
      `!terminal(l)` (verificable con `grep -n "_commitmentHoyBucket(l, nowMsHoy)" public/app.js`
      y leyendo esas 2 líneas).
    - `grep -c "data-action=\"call\"" public/app.js` devuelve 0 (no se activó el
      guard que oculta el botón de llamar).
    - El cuerpo de `window._actDiscard` contiene `forceToast`, `_leadStoreApply(`,
      `_pdAdvance(` y `_refreshLeadPanels(` dentro de un `finally`; y **no**
      contiene `_dispoAfterSaved(`.
    - Las 4 suites del comando de verify siguen verdes.
  </acceptance_criteria>

  <done>
Descartar está disponible en las 4 vistas, se resuelve en un solo paso con razón
opcional, avisa a dónde fue el lead y lo saca de Llamadas, del dialer y de Hoy.
  </done>
</task>

<task type="auto">
  <name>Task 2: Material por email desde la ficha + canal en el historial del lead (ACT-05)</name>

  <read_first>
    - `.planning/phases/32-act-acciones/32-02-SUMMARY.md` — el contrato REAL de
      `POST /api/setters/leads/:id/send-material` (las dos vías, el 409 de
      `resendUnavailable`, la forma de `mailtoUrl`). Manda sobre este plan.
    - `public/app.js` líneas 8205-8222 — `.call-action-row` de
      `_callsRenderExpandedPanel`, incluido el `<a href="mailto:…" class="call-action-btn">Mandar mail</a>`
      (8209-8212) que este plan reemplaza.
    - `public/app.js` — `window._actWhatsApp` tal como la dejó 32-03: el overlay
      nuevo es su gemelo (mismo z-index, mismo cierre, mismo
      `_leadStoreApply`/`_dispoAnnounce(forceToast)`/`finally`).
    - `public/app.js` — `_renderCallHistory` y su `kind: 'commitment'` (agregado
      por 31-04; buscar por `kind` dentro de la función).
    - `public/app.js` — bloque `[32-03] ACT-PURE` (`ACT_WA_TEMPLATES`,
      `_actTemplateById`) y `_interpolateScript` (10593).
  </read_first>

  <files>public/app.js</files>

  <action>
**A. `window._actSendMaterial(leadId)`** — overlay gemelo del de WhatsApp
(mismo `z-index:10060`, mismo patrón de cierre, `escHtml` en toda interpolación):
- Campo `Para` con `lead.email` precargado y editable (si el lead no tiene, queda
  vacío y el SDR lo tipea — el backend acepta `body.email`).
- Campo `Asunto` con un default corto y sin marca.
- `<textarea>` con el cuerpo interpolado desde `ACT_WA_TEMPLATES`
  (`_actTemplateById` + `_interpolateScript`), editable. Comentario: se reusa el
  mismo catálogo de D-06 para que el material diga lo mismo por los dos canales.
- Dos CTAs, ambos visibles (D-15, un solo paso):
  - **Mandar por el sistema** → POST con `via:'resend'`.
  - **Abrir mi cliente de mail** → POST con `via:'mailto'` y, con la respuesta OK,
    `window.open(d.mailtoUrl, '_blank', 'noopener')`.
- Manejo de respuestas:
  - `409` con `d.resendUnavailable` → toast explicando que el envío por el
    sistema no está configurado y que use el segundo botón (el `mailtoUrl` ya
    viene en la respuesta). **No** registrar nada del lado del cliente.
  - `502` → toast con `d.detail`.
  - `200` → `_leadStoreApply(leadId, d.lead)` +
    `_dispoAnnounce(leadId, { lead: d.lead, forceToast: true })`
    (la rama de destino que 32-03 agregó ya distingue el canal `email`).
- `finally`: cerrar overlay + `_refreshLeadPanels(leadId)`. **Sin `_pdRender()`**
  (misma razón que en 32-03: el envío programa `nextAction` a +48h).

**B. Cablearlo en la ficha.** En `.call-action-row` de
`_callsRenderExpandedPanel`, **reemplazar** el `<a href="mailto:…">Mandar mail</a>`
por un `<button class="call-action-btn" onclick="window._actSendMaterial('<id>')">Mandar material</button>`.
Comentario: el link viejo abría el cliente de mail con el cuerpo vacío y **sin
dejar ningún registro** — que es exactamente el problema que ACT-05 cierra. El
botón nuevo se renderiza siempre (con o sin `lead.email`), porque el overlay
permite tipear el destinatario.

**C. Canal en el historial del lead.** En `_renderCallHistory`, la línea del
`kind: 'commitment'` (31-04) hoy dice "Compromiso cumplido: `<etiqueta>`" sin
distinguir por dónde se mandó. Agregar el canal cuando el compromiso lo tiene
(`por WhatsApp` / `por email`), con `escHtml`. Es lo que hace verdadera la frase
de ACT-05 ("mismo timeline del lead") sin necesitar una vista nueva.
**Cuidado (lección de 31-04):** hay tests que extraen bloques por comentarios
literales — no reescribir la primera línea de ningún comentario de cabecera
existente; agregar líneas nuevas si hace falta.

**No tocar** `_dispoDestination`, `send-placeholder` ni el bloque
`[32-03] ACT-PURE`.
  </action>

  <verify>
    <automated>node --check public/app.js &amp;&amp; npx vitest run tests/commitment-hoy.test.js tests/commitment-ui.test.js tests/act-ui-whatsapp.test.js tests/gate-destination.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "window._actSendMaterial" public/app.js` devuelve ≥ 2.
    - El cuerpo de `window._actSendMaterial` contiene `send-material`,
      `'mailto'`, `'resend'`, `resendUnavailable`, `_leadStoreApply(`,
      `forceToast` y `_refreshLeadPanels(` en un `finally`; y NO contiene
      `_pdRender(`.
    - `grep -c "class=\"call-action-btn\">Mandar mail" public/app.js` devuelve 0
      (el link plano se reemplazó) y existe el botón `Mandar material`.
    - `_renderCallHistory` menciona `por WhatsApp` y `por email` (o el literal que
      elija el executor) dentro de la rama `commitment`.
    - `npx vitest run tests/commitment-hoy.test.js` sigue verde (los tests de
      31-04 sobre `_renderCallHistory` no se rompen).
  </acceptance_criteria>

  <done>
Mandar material por email se hace desde la ficha, deja el mismo evento que el
WhatsApp y se lee en el mismo historial diciendo por qué canal salió.
  </done>
</task>

<task type="auto">
  <name>Task 3: Tests del descarte y del material + cache-buster + cierre de fase</name>

  <read_first>
    - `tests/act-ui-whatsapp.test.js` (32-03) — el molde de extracción por
      marcadores/cuerpos de función y las aserciones de cableado.
    - `tests/commitment-hoy.test.js` — el helper `extractFunctionBody` corregido
      (conteo de llaves balanceado, tolerante a `opts = {}`) y el test anti-deriva.
    - `public/index.html` — el cache-buster REAL que dejó 32-03.
    - Todo lo escrito en las Tasks 1 y 2.
  </read_first>

  <files>tests/act-ui-discard-material.test.js, public/index.html</files>

  <action>
**A. Crear `tests/act-ui-discard-material.test.js`** (aserciones de fuente sobre
`public/app.js` + `public/index.html`, mismo estilo que 31-03/31-04/32-03).
Cubrir:

**Descarte en las 4 superficies (ACT-04/D-12)**
1. `_actButtonsHTML` está declarada 1 vez y se llama exactamente 4 veces (el
   botón nuevo NO multiplicó los call sites).
2. El cuerpo de `_actButtonsHTML` contiene `window._actDiscard(` y
   `window._actWhatsApp(` (los dos botones salen del mismo builder).
3. Los 4 cuerpos (`renderCallsList`, `_pdRender`, `_callsRenderExpandedPanel`,
   `_hoyRenderSection`) siguen conteniendo `_actButtonsHTML(`.

**Razón y confirmación (D-14/D-15)**
4. `_actDiscard` referencia `DISQUALIFY_REASONS_UI` y `no_es_icp` aparece 1 sola
   vez en todo `public/app.js` (no se duplicó la lista).
5. El overlay no tiene un segundo paso: el cuerpo de `_actDiscard` no contiene
   `confirm(` ni una segunda pantalla (`step`), y el `POST` sale del mismo
   handler del CTA.
6. El `POST` manda `reason` y `doNotCall` en el body.

**Destino y sincronización de vistas**
7. `_actDiscard` llama `_dispoAnnounce(` con `forceToast` y **no** llama
   `_dispoAfterSaved(` (no toca el gate de la Phase 20).
8. `_actDiscard` llama `_pdAdvance(` dentro de un guard que compara
   `_pd.queue[_pd.currentIdx]` con el `leadId`.
9. `_actDiscard` llama `loadHoyView(` bajo el guard de `#view-hoy:not(.hidden)`.
10. `_refreshLeadPanels(` está dentro de un `finally`.

**Marca (D-16/D-11)**
11. El chip de estado descartado usa `scm-chip-blocked` y el bloque nuevo no
    introduce ningún color rojo hardcodeado (`#f85149`, `#F47272`, `rgba(248,81,73`)
    dentro del cuerpo de `_actButtonsHTML` ni del de `_actDiscard`.
12. `renderCallsList` agrega `scm-row-blocked` cuando el lead está descartado.
13. Los `<span>` de teléfono de `renderCallsList` y `_hoyRenderSection` llevan
    `class="scm-phone"`.

**Hoy (ACT-04, criterio 4)**
14. Las 2 líneas de filtro de `_commitmentHoyBucket` en `loadHoyView` contienen
    `!terminal(l)`.

**Material por email (ACT-05/D-17/D-18)**
15. `window._actSendMaterial` existe, pega a `send-material` y contempla las dos
    vías (`'resend'` y `'mailto'`) + `resendUnavailable`.
16. La ficha ya no tiene el `<a href="mailto:` como acción de la
    `.call-action-row`, y sí tiene el botón `Mandar material`.
17. `_renderCallHistory` distingue el canal del compromiso cerrado.
18. El overlay de material usa `z-index:10060` (o mayor).

**Cache-buster**
19. `app.js?v=` cambió respecto del valor con el que arrancó este plan, con
    chequeo de FORMA (regex), nunca un literal pineado (lección de 31-03).

**B. Bumpear el cache-buster** de `app.js` en `public/index.html` a la siguiente
letra libre a partir del valor REAL en disco. **No tocar** `style.css?v=`.

**C. Cierre de fase.** Correr `npm test` completo y dejar el conteo en el
SUMMARY (baseline de referencia: 1512 al cerrar la Phase 31, más lo que sumaron
32-01/02/03). Actualizar a mano `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`
(ACT-01..05 en `[x]`) y `.planning/STATE.md` (Phase 32 COMPLETE, 4/4 planes).
**No invocar `gsd-sdk query` para esto**: el patrón de corrupción del formato
custom de esos archivos está confirmado 6 veces en este milestone (24-01, 29-02,
29-03, 29-04, 31-03, 31-04) — editar con `Edit`.

**Verificación por mutación (obligatoria):** romper temporalmente el
`!terminal(l)` de una de las 2 líneas de Hoy, correr la suite, anotar los rojos,
y restaurar con `git checkout -- public/app.js`. Confirmar `git diff` vacío y
dejar el conteo en el SUMMARY.
  </action>

  <verify>
    <automated>npx vitest run tests/act-ui-discard-material.test.js tests/act-ui-whatsapp.test.js tests/act-discard-email.test.js tests/act-whatsapp.test.js tests/commitment-hoy.test.js tests/gate-destination.test.js tests/metrics-consistency.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/act-ui-discard-material.test.js` pasa con al menos 19
      tests.
    - `grep -c "20260815" tests/act-ui-discard-material.test.js` devuelve 0 (sin
      cache-buster pineado).
    - `grep -n "app.js?v=" public/index.html` muestra un valor distinto al que
      dejó 32-03; `git diff public/style.css` vacío.
    - `npm test` completo verde salvo los flaky ambientales ya documentados
      (CLAUDE.md #93/#110/#130), re-corridos en aislado y anotados en el SUMMARY.
    - `npx vitest run tests/metrics-consistency.test.js` verde **sin que la fase
      haya editado ese archivo** en ninguno de sus 4 planes.
    - `.planning/REQUIREMENTS.md` tiene ACT-01..ACT-05 en `[x]` y
      `.planning/ROADMAP.md` marca la Phase 32 como completa con 4 planes.
  </acceptance_criteria>

  <done>
La fase queda cerrada con tests que fallan si alguien desconecta un botón, el
cache-buster bumpeado y el estado del proyecto actualizado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `POST /leads/:id/discard` | mutación a estado terminal disparada desde 4 superficies |
| browser → `POST /leads/:id/send-material` | asunto/cuerpo/destinatario tipeados por el SDR salen hacia el prospecto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-16 | Tampering | interpolación del lead en los 2 overlays nuevos | mitigate | `escHtml(` en toda interpolación; aserción de fuente en el test |
| T-32-17 | Elevation of Privilege | descarte desde el frontend | transfer | la autorización la hace el backend (32-02: guard de dueño + visibilidad); el cliente no manda `by`/`byName` |
| T-32-18 | Denial of Service | descarte accidental masivo | accept | es de a un lead, con razón visible y reversible desde `Reactivar lead` (`_callsReactivate`); el aviso de destino dice dónde quedó |
| T-32-19 | Information Disclosure | `mailtoUrl` con el cuerpo del mensaje | accept | se abre en el cliente de mail del propio SDR; el contenido es el que él mismo escribió |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | esta fase NO instala paquetes: `git diff package.json package-lock.json` vacío |
</threat_model>

<verification>
- `node --check public/app.js` en cada task.
- `npx vitest run tests/act-ui-discard-material.test.js` ≥ 19 tests verdes.
- `npm test` completo con el conteo documentado en el SUMMARY.
- `git diff public/style.css` vacío; `app.js?v=` bumpeado.
- `git diff package.json package-lock.json` vacío.
</verification>

<success_criteria>
- El botón de descartar está en las 4 vistas y saca el lead de Llamadas, del
  Power Dialer y de Hoy en una sola acción.
- Descartar sin razón funciona; con razón `no_contactar` marca DNC.
- Un lead descartado se muestra en gris atenuado con etiqueta, nunca en rojo.
- Mandar material por email deja el mismo evento y se ve en el historial del
  lead con su canal.
- ACT-01..ACT-05 quedan cerrados y la fase marcada como completa.
</success_criteria>

<output>
Crear `.planning/phases/32-act-acciones/32-04-SUMMARY.md` al terminar, con: el
cache-buster final, el conteo de la suite completa, el resultado de la
verificación por mutación, y el **checklist de verificación en vivo** (no hay
browser en el entorno) que debe incluir como mínimo:
1. Botón de WhatsApp visible y funcionando en las 4 vistas (lista, dialer,
   ficha, Hoy), incluida la ficha abierta DESDE Hoy (prueba del z-index).
2. Mandar a un número tipeado en el momento + guardarlo como contacto
   secundario; confirmar que el teléfono principal no cambió.
3. Confirmar que el lead aparece después en `Hoy → Esperando del prospecto` con
   el badge "info mandada · fecha".
4. Mandar desde el Power Dialer y confirmar que la tarjeta NO se va.
5. Descartar desde cada una de las 4 vistas y confirmar que desaparece de las 3
   colas, con el aviso de destino correcto.
6. Descartar con razón "Pidió NO ser contactado" y confirmar el chip de DNC.
7. Ver un lead descartado con el toggle "ver descartados": fila atenuada, chip
   gris, teléfono tachado — nada en rojo.
8. Mandar material por email por las dos vías (o confirmar el 409 con el aviso
   correcto si `RESEND_API_KEY` no está configurada en Railway).
</output>
