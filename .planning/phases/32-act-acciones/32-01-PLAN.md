---
phase: 32-act-acciones
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.js
  - tests/act-whatsapp.test.js
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03]

must_haves:
  truths:
    - "Un solo request abre el chat de WhatsApp Y deja el envío registrado — nunca puede quedar el link abierto sin registro ni el registro sin link"
    - "El link se arma con la misma normalización de teléfono que ya resolvió los casos raros (US con paréntesis, México frontera, móvil argentino)"
    - "Se puede mandar a un número alternativo cargado en el momento sin que el lead pierda su número principal"
    - "El envío deja al lead esperando respuesta a +48h — el mismo mecanismo del compromiso, no un registro paralelo"
    - "Un envío de WhatsApp NO suma una llamada a ninguna métrica del funnel"
  artifacts:
    - path: "index.js"
      provides: "bloque ACCIONES (Phase 32) con _actRegisterSendEvent + endpoint POST /api/setters/leads/:id/whatsapp-send"
      contains: "/api/setters/leads/:id/whatsapp-send"
    - path: "tests/act-whatsapp.test.js"
      provides: "cobertura HTTP del endpoint (URL, número alternativo, registro, RBAC, no-inflación de métricas)"
      min_lines: 150
  key_links:
    - from: "POST /api/setters/leads/:id/whatsapp-send"
      to: "buildWhatsAppUrl"
      via: "armado del wa.me server-side con el país del lead"
      pattern: "buildWhatsAppUrl\\("
    - from: "_actRegisterSendEvent"
      to: "_setCommitment / _closeCommitment"
      via: "compromiso enviar_info propio creado y cerrado como cumplido en el mismo request"
      pattern: "_closeCommitment\\(lead"
    - from: "_actRegisterSendEvent"
      to: "lead.interactions"
      via: "entry de auditoría sin setterId (no toca la atribución del embudo legacy)"
      pattern: "action: 'material_sent'"
---

<objective>
El backend que hace que "mandar WhatsApp" sea UN SOLO ACTO (D-03): un endpoint
que arma el `wa.me` con la normalización de teléfono ya probada, acepta un
número alternativo cargado en el momento (ACT-03) y deja el envío registrado
como compromiso propio ya cumplido — que a su vez programa el seguimiento a
+48h (D-05).

Purpose: ACT-01/02/03 necesitan que el link y el registro viajen en el mismo
request. Si fueran dos llamadas separadas, la segunda no se hace nunca — que es
exactamente el problema que el user describió.
Output: bloque `ACCIONES (Phase 32)` en `index.js` con `_actRegisterSendEvent`,
`POST /api/setters/leads/:id/whatsapp-send`, y una suite HTTP que prueba el
camino feliz, el número alternativo, los rechazos y que las métricas no se
mueven.

**Este plan NO toca `public/`.** Cero bump de cache-buster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-act-acciones/32-CONTEXT.md
@.planning/phases/32-act-acciones/32-PATTERNS.md
@.planning/phases/31-comm-compromisos/31-01-SUMMARY.md
@.planning/phases/31-comm-compromisos/31-02-SUMMARY.md
@CLAUDE.md

Reglas del proyecto que aplican:

- **Mutex (regla #19): NO hace falta en este plan.** El endpoint nuevo es
  100% síncrono (`loadSettersData` → mutar → `saveSettersData`, sin ningún
  `await` en el medio) porque `buildWhatsAppUrl` es puro. Es el mismo criterio
  documentado en el comentario de cabecera de `PATCH .../commitment`
  (`index.js:10375-10379`). Si alguna vez se agrega trabajo `await` antes de la
  mutación, ENTONCES sí hay que envolverla en `mutateSettersData`.
- **Orden de rutas Express (regla #3):** la ruta nueva tiene sufijo estático
  después de `:id` (`/whatsapp-send`), igual que `/commitment` y `/followup` —
  no compite con `/leads/sin-wsp` (GET) ni con `/leads/bulk` (POST sin sufijo).
- **TDZ de `const`:** `COMMITMENT_*`, `GATE_TERMINAL_ESTADOS` y
  `DISQUALIFY_REASONS` se declaran DESPUÉS (líneas 11095-11785) del lugar donde
  van los endpoints (~10430). Eso ya pasa hoy con `PATCH .../commitment`, que
  usa `_setCommitment` y `COMMITMENT_CIERRES` sin problema: el cuerpo de un
  handler corre en tiempo de request, no de evaluación del módulo. **No mover
  ninguna declaración de lugar** (ver 24-02-SUMMARY: mover consts a un bloque
  "conceptualmente mejor" ya rompió el boot una vez).
- Regla #121 (tests): env vars de IA a `""`, **nunca** `delete`.
- Regla #163 (fixtures): teléfonos de ≥7 dígitos y prefijo `+521` (México, no
  bloqueado por `_expensiveTariffLabel`) o el lead nunca entra en las colas.
</context>

<interfaces>
<!-- Contrato que este plan expone y que consumen 32-02 (reusa el helper) y
     32-03 (lo consume desde el frontend). -->

POST /api/setters/leads/:id/whatsapp-send    (auth: requireAuth)
  body:
    templateId?     string  — id de la plantilla usada (D-07). Fuera de
                              ACT_WA_TEMPLATE_IDS → se guarda '' (nunca 4xx).
    message?        string  — cuerpo YA interpolado por el frontend. Se trimea,
                              se corta a 900 chars y pasa por _stripBrandMentions
                              (D-08). Vacío → el wa.me sale sin ?text=.
    phone?          string  — destino alternativo (ACT-03). Default: lead.phone.
    saveAsAltPhone? boolean — persiste ese número en lead.altPhone (D-10: NUNCA
                              en lead.phone).
    altPhoneLabel?  string  — etiqueta del contacto secundario (máx 60).
  respuestas:
    200 { ok:true, whatsappUrl, sentTo, templateId, commitment, nextAction,
          lead:{id, ...lead} }
    400 { error } — phone con formato inválido, o no se pudo armar un wa.me
                    confiable (buildWhatsAppUrl devolvió ''). NO registra nada.
    403 { error } — setter que no es dueño / lead fuera de visibilidad
    404 { error } — lead inexistente

_actRegisterSendEvent(lead, spec, nowIso)  → { commitment, nextAction, terminal }
  spec = { canal:'whatsapp'|'email', templateId, to, byId, byName }
  Efectos: push en lead.interactions (cap 200) + compromiso enviar_info/parte
  'yo' creado y cerrado como 'cumplido' en el mismo acto → nextAction
  'esperar_respuesta' a +48h. En lead terminal (GATE_TERMINAL_ESTADOS) NO crea
  compromiso: solo la entry de auditoría, y devuelve terminal:true.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Bloque ACCIONES (Phase 32) — whitelists y _actRegisterSendEvent</name>

  <read_first>
    - `index.js` líneas 11280-11460 — bloque COMPROMISOS completo:
      `COMMITMENT_DEFAULT_CANAL` (11285), `_sanitizeCommitment` (11370),
      `_setCommitment` (11387) y `_closeCommitment` (11427). El helper nuevo
      compone estos dos últimos sin modificarlos.
    - `index.js` líneas 11095-11100 — `GATE_PLACEHOLDER_DELTA_MS` y
      `GATE_TERMINAL_ESTADOS`.
    - `index.js` líneas 11736-11756 — el objeto `globalThis.__voiceAgent` que
      hay que extender.
    - `index.js` líneas 10529-10536 — la entry de auditoría que escribe el bulk
      (`{action, by, byName, createdAt}`): la forma EXACTA a copiar.
    - `index.js` líneas 12694-12712 — `_perfAggregate` con `attr`: lee
      `interactions[].setterId`. Es la razón por la que la entry nueva NO puede
      llevar esa clave.
    - `index.js` líneas 7759-7800 — `_ccCollectCalls`/`_ccFunnelAggregate`:
      **cada entry de `callLog` cuenta como un dial**. Es la razón por la que
      este helper no escribe en `callLog`.
    - `index.js` líneas 925-932 — `_stripBrandMentions`.
    - `.planning/phases/31-comm-compromisos/31-01-SUMMARY.md` — la superficie
      real de los helpers de compromiso (los nombres mandan sobre este plan).
  </read_first>

  <files>index.js</files>

  <action>
Agregar un bloque nuevo `ACCIONES (Phase 32)` en `index.js`, **inmediatamente
después del cierre de `_closeCommitment`** (~línea 11460) y **antes** de
`globalThis.__voiceAgent` (11739), con este contenido:

**A. Whitelists.**
- `ACT_WA_TEMPLATE_IDS = new Set(['post_llamada', 'envio_info', 'reconfirmar_reunion'])`
  — los 3 ids de plantilla de D-06. El frontend (32-03) espeja estas 3 claves;
  el test de 32-03 verifica la paridad leyendo los dos archivos como texto.
- `ACT_SEND_CANALES = new Set(['whatsapp', 'email'])` — canales por los que se
  puede registrar un envío de material. Ambos ya están en `NEXT_ACTION_CANALES`.
- `ACT_MESSAGE_MAX = 900` — tope del cuerpo del mensaje antes de meterlo en la
  URL. Comentario: `wa.me` acepta más, pero un texto largo en un query param es
  un vector de abuso y el mensaje real del SDR nunca pasa de un par de párrafos.

**B. `_actSanitizeMessage(raw)`** — devuelve string (nunca lanza, nunca null):
no-string → `''`; si no, `trim()` → `slice(0, ACT_MESSAGE_MAX)` →
`_stripBrandMentions(...)` → `trim()` de nuevo. Comentario citando la nota #119:
el texto lo ve el PROSPECTO, así que la marca nunca puede colarse aunque el
cliente mande un cuerpo arbitrario.

**C. `_actRegisterSendEvent(lead, spec, nowIso)`** — el registro compartido por
WhatsApp (este plan) y email (32-02). Firma y comportamiento EXACTOS:
  - `canal` = `spec.canal` si está en `ACT_SEND_CANALES`, si no `'whatsapp'`.
  - `templateId` = `spec.templateId` si está en `ACT_WA_TEMPLATE_IDS`, si no `''`
    (D-07: se guarda cuál se usó; un id desconocido no invalida el envío).
  - `to` = `String(spec.to || '').slice(0, 40)`.
  - Push en `lead.interactions` (inicializar el array si no existe) de
    `{ action: 'material_sent', canal, templateId, to, by: spec.byId || '',
       byName: spec.byName || '', createdAt: nowIso }` y recorte a los últimos
    200 (`if (lead.interactions.length > 200) lead.interactions = lead.interactions.slice(-200)`),
    igual que `index.js:10248`.
    **Comentario obligatorio en el código, con las dos razones:** (1) esta entry
    NO lleva `setterId` a propósito — `_perfAggregate` atribuye "lead trabajado"
    por `interactions[].setterId` y agregarlo movería el embudo legacy;
    (2) el evento NO va a `callLog` a propósito — `_ccCollectCalls` cuenta CADA
    entry de `callLog` como un dial y esto no es una llamada.
  - Si `GATE_TERMINAL_ESTADOS.has(lead.estado)`: no crear compromiso; devolver
    `{ commitment: lead.commitment || null, nextAction: lead.nextAction || null, terminal: true }`.
    Comentario: un lead descartado/agendado/cerrado nunca lleva `nextAction`
    (regla dura de la Phase 30) — el envío igual queda auditado.
  - Si no es terminal: `_setCommitment(lead, { tipo: 'enviar_info', parte: 'yo',
    canal, motivo, createdBy: spec.byName || '', callId: '' }, nowIso)` seguido de
    `_closeCommitment(lead, 'cumplido', nowIso, spec.byName || '')`.
    `motivo` = `'mandé info por WhatsApp'` cuando `canal==='whatsapp'` y
    `'mandé material por email'` cuando `canal==='email'`.
    Comentario obligatorio: crear-y-cerrar en el mismo acto es lo que traduce
    D-04 ("abrí el chat para mandar X", no "el mensaje fue entregado") al modelo
    de la Phase 31 — `_closeCommitment` es el que programa solo el
    `esperar_respuesta` a +48h (`COMMITMENT_ENVIAR_INFO_DELTA_MS`). Documentar
    también la consecuencia asumida: como hay UN compromiso por lead (D-01 de la
    Phase 31), esto REEMPLAZA un compromiso pendiente previo — el envío es el
    hecho más nuevo.
  - Devolver `{ commitment: lead.commitment, nextAction: lead.nextAction, terminal: false }`.

**D. Exposición.** Agregar al objeto `globalThis.__voiceAgent` (11739-11756) un
bloque comentado `// Phase 32: acciones desde cualquier vista (ACT-01..05).` con
`_actRegisterSendEvent`, `_actSanitizeMessage`, `ACT_WA_TEMPLATE_IDS`,
`ACT_SEND_CANALES`.

**No tocar** `_setCommitment`, `_closeCommitment` ni ninguna constante existente.
  </action>

  <verify>
    <automated>node --check index.js &amp;&amp; npx vitest run tests/commitment-model.test.js tests/commitment-endpoints.test.js tests/metrics-consistency.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` sale con código 0.
    - `grep -n "function _actRegisterSendEvent" index.js` devuelve exactamente 1
      línea, y su número está DESPUÉS del de `grep -n "function _closeCommitment" index.js`
      y ANTES del de `grep -n "globalThis.__voiceAgent = {" index.js`.
    - `grep -c "action: 'material_sent'" index.js` devuelve exactamente 1.
    - `grep -n "setterId" index.js` NO devuelve ninguna línea dentro del rango del
      bloque `ACCIONES (Phase 32)` (verificable acotando con los números de línea
      de inicio/fin del bloque).
    - `grep -c "callLog" index.js` devuelve el MISMO número que antes de esta task
      (medirlo con `git stash` o contra `git show HEAD:index.js | grep -c callLog`):
      el bloque nuevo no menciona `callLog` en ninguna forma.
    - `node -e "process.env.NODE_ENV='test';"` no aplica; en su lugar:
      `npx vitest run tests/metrics-consistency.test.js` pasa **sin haber editado
      ese archivo** (`git diff --name-only` no lo lista).
    - Las 3 suites del comando de verify pasan sin fallos.
  </acceptance_criteria>

  <done>
Existe un único registro de "mandé material" que compone los helpers de
compromiso de la Phase 31, escribe una entry de auditoría inocua para las
métricas, y está expuesto para tests puros.
  </done>
</task>

<task type="auto">
  <name>Task 2: POST /api/setters/leads/:id/whatsapp-send (ACT-01/02/03)</name>

  <read_first>
    - `index.js` líneas 1493-1563 — `buildWhatsAppUrl` COMPLETA, incluidos los
      comentarios de los bugs históricos que resuelve (US con paréntesis
      1498-1507, alias de país 1511-1528, móvil argentino 1543, "sin prefijo
      conocido no inventar +1" 1545-1561). Este endpoint la reusa TAL CUAL —
      duplicar esa lógica en el frontend reintroduciría esos bugs.
    - `index.js` líneas 10369-10430 — `PATCH /api/setters/leads/:id/commitment`
      COMPLETO: es el molde de auth (los 2 guards de 10384-10387),
      `ensureLeadDefaults`, mutación síncrona, `saveSettersData` y forma de
      respuesta `lead: { id, ...lead }`. El endpoint nuevo va INMEDIATAMENTE
      DESPUÉS de este (antes del comentario `// Sprint 31: Bulk operations`
      de la línea 10432).
    - `index.js` líneas 10613-10639 — `PUT .../alt-contact`: la normalización
      EXACTA del teléfono (`replace(/[^\d+]/g,'')`, regex `/^\+?\d{6,15}$/`,
      prefijo `+` forzado) y del label (trim + `slice(0,60)`) que hay que copiar.
    - El bloque `ACCIONES (Phase 32)` creado en la Task 1.
  </read_first>

  <files>index.js</files>

  <action>
Agregar `app.post('/api/setters/leads/:id/whatsapp-send', requireAuth, (req, res) => {...})`
inmediatamente después del cierre de `PATCH /api/setters/leads/:id/commitment`
(~línea 10430), con un comentario de cabecera que explique: es UN SOLO REQUEST
que devuelve el link Y deja el registro (D-03), la URL se arma server-side para
reusar `buildWhatsAppUrl` (D-02), y por qué es síncrono (mismo criterio que el
endpoint de arriba — sin `await` no hace falta el mutex, regla #19).

Cuerpo, en este orden exacto:

1. `const data = loadSettersData();` + `const lead = data.leads[req.params.id];`
   + 404 si no existe.
2. Los 2 guards de auth copiados literales de `index.js:10384-10387` (dueño
   `role === 'setter'` + `_visibleSetterIds`/`_setterIsVisible`).
3. `ensureLeadDefaults(lead);`
4. Destructuring del body: `{ templateId, message, phone, saveAsAltPhone, altPhoneLabel }`.
5. **Resolución del destino (ACT-03/D-09/D-10).**
   - `const rawTo = (typeof phone === 'string' ? phone : '').trim();`
   - Si `rawTo` existe: normalizar con `replace(/[^\d+]/g, '')` y validar con
     `/^\+?\d{6,15}$/`; si no valida → `400 { error: 'Teléfono inválido. Formato E.164: +5491112345678' }`
     (mismo texto que `alt-contact` — un solo mensaje para el mismo error).
     El valor final lleva `+` al frente si no lo tenía (copiar de alt-contact).
   - `const sentTo = <normalizado> || lead.phone;` Si queda vacío →
     `400 { error: 'El lead no tiene teléfono y no pasaste uno alternativo.' }`.
   - **Nunca escribir `lead.phone`** (D-10). Comentario explícito.
6. **Armado de la URL.**
   - `const cleanMessage = _actSanitizeMessage(message);`
   - `const whatsappUrl = buildWhatsAppUrl(sentTo, lead.country, cleanMessage);`
   - Si `whatsappUrl` es `''` → `400 { error: 'No pude armar un link de WhatsApp confiable con ese número. Cargá el número con código de país (ej: +5491112345678).' }`
     y **cortar antes de registrar nada**: si no se abrió ningún chat, no puede
     quedar anotado que se mandó info (D-04, el registro no puede mentir).
7. **Persistencia opcional del alternativo (D-09).** Si `saveAsAltPhone === true`
   y el `sentTo` normalizado es distinto de `lead.phone`: `lead.altPhone = sentTo;`
   `lead.altPhoneLabel = (typeof altPhoneLabel === 'string' ? altPhoneLabel : '').trim().slice(0, 60);`
   Comentario: se escribe el MISMO campo que usa el modal de contacto secundario
   (nota #156) para que "número alternativo" tenga una sola fuente de verdad.
8. **Registro.** `const nowIso = new Date().toISOString();` +
   `const reg = _actRegisterSendEvent(lead, { canal: 'whatsapp', templateId, to: sentTo, byId: req.auth?.user?.id || '', byName: req.auth?.user?.name || '' }, nowIso);`
9. `saveSettersData(data);` y respuesta
   `res.json({ ok: true, whatsappUrl, sentTo, templateId: reg.templateId ?? undefined, commitment: lead.commitment, nextAction: lead.nextAction, lead: { id: req.params.id, ...lead } })`.
   Si `_actRegisterSendEvent` no devuelve `templateId`, devolver en su lugar el
   `templateId` saneado que el helper aplicó — el frontend lo usa para el toast;
   ajustar el helper para devolverlo si hace falta (y actualizar el `<interfaces>`
   de este plan en el SUMMARY si cambia la forma).

**No tocar** `buildWhatsAppUrl`, `alt-contact`, `bulk` ni `call-disposition`.
  </action>

  <verify>
    <automated>node --check index.js &amp;&amp; npx vitest run tests/commitment-endpoints.test.js tests/security-rbac.test.js tests/hardening.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` sale con código 0.
    - `grep -n "whatsapp-send" index.js` devuelve exactamente 1 línea de ruta, y su
      número está entre el de `app.patch('/api/setters/leads/:id/commitment'` y el
      de `app.post('/api/setters/leads/:id/bulk'`… (en su defecto, entre
      `.../commitment` y `// Sprint 31: Bulk operations`).
    - `git diff index.js` no muestra ningún cambio dentro del cuerpo de
      `buildWhatsAppUrl` (líneas 1493-1563) ni de `PUT .../alt-contact`.
    - `grep -c "lead.phone = " index.js` devuelve el MISMO valor que en `HEAD`
      (el endpoint nuevo nunca escribe el teléfono principal — D-10).
    - Arrancando el server de test e invocando el endpoint con
      `{message:'hola'}` sobre un lead `+5215551000001` país `Mexico`, la respuesta
      trae `whatsappUrl` que empieza con `https://wa.me/521` y contiene
      `?text=hola`.
    - El mismo endpoint con `{phone:'(305) 555-1234'}` devuelve una URL que empieza
      con `https://wa.me/1305` (prueba de que se está usando `buildWhatsAppUrl` y
      no una normalización nueva).
    - Con `{phone:'123'}` responde 400 y `lead.commitment` NO cambia.
    - Las 3 suites del comando de verify pasan sin fallos.
  </acceptance_criteria>

  <done>
Un request devuelve el `wa.me` listo para abrir y, en la misma escritura, deja
el compromiso cumplido + el seguimiento a 48h; el número alternativo funciona y
nunca pisa el principal.
  </done>
</task>

<task type="auto">
  <name>Task 3: Suite HTTP del envío por WhatsApp</name>

  <read_first>
    - `tests/commitment-endpoints.test.js` líneas 1-95 — el setup COMPLETO a
      calcar: `tmpData`, env vars (incluida la regla #121 con `""`), fixture de
      `auth.json` con admin + 2 setters, helper `lead(n, extra)` con teléfono
      `+521…` de ≥7 dígitos (regla #163), import dinámico de `../index.js`,
      login por cookie.
    - `tests/metrics-consistency.test.js` — NO se edita; solo se corre.
    - El endpoint y el helper creados en las Tasks 1 y 2.
  </read_first>

  <files>tests/act-whatsapp.test.js</files>

  <action>
Crear `tests/act-whatsapp.test.js` con el setup calcado de
`tests/commitment-endpoints.test.js` (fixture propio en `os.tmpdir()`, admin +
`s_owner` + `s_other`, leads con teléfono `+521555100NNNN`). Cubrir como mínimo:

**URL (ACT-01)**
1. Lead `Mexico` + `+5215551000001` + `{message:'hola doctor'}` → 200, `whatsappUrl`
   empieza con `https://wa.me/521` y termina con el texto URL-encodeado.
2. `{phone:'(305) 555-1234'}` sobre el mismo lead → `https://wa.me/1305…`
   (delegación real en `buildWhatsAppUrl`, incluye el caso frontera de México).
3. Lead sin país y con teléfono corto sin prefijo → 400 con `error` y
   `lead.commitment` sin cambios (nada se registró).
4. `message` vacío/ausente → URL sin `?text=`.

**Registro (ACT-02 / D-04 / D-05)**
5. Tras un envío OK: `lead.commitment.tipo === 'enviar_info'`, `parte === 'yo'`,
   `canal === 'whatsapp'`, `estado === 'cumplido'`, `closedAt` seteado.
6. `lead.nextAction.tipo === 'esperar_respuesta'`, `origen === 'compromiso'`, y
   `dueAt` entre 47 y 49 horas en el futuro (helper `hoursFromNow`).
7. `lead.interactions` termina con una entry `action:'material_sent'` con
   `canal:'whatsapp'`, `templateId` y `to`; y esa entry **no** tiene la clave
   `setterId` (`expect('setterId' in entry).toBe(false)`).
8. `lead.callLog` conserva EXACTAMENTE la misma longitud que antes del envío
   (el envío no es una llamada — protege el funnel).
9. `templateId` fuera de whitelist (`'basura'`) → 200 y la entry queda con
   `templateId: ''` (nunca 4xx, D-07/whitelist-and-coerce).
10. Envío sobre un lead `estado:'descartado'` → 200, `whatsappUrl` presente,
    `lead.nextAction` sigue sin fecha (un terminal nunca lleva reloj) y la entry
    de auditoría igual se escribió.

**Número alternativo (ACT-03 / D-10)**
11. `{phone:'+5215559999999', saveAsAltPhone:true, altPhoneLabel:'Encargado'}` →
    `lead.altPhone === '+5215559999999'`, `lead.altPhoneLabel === 'Encargado'` y
    `lead.phone` INTACTO.
12. Mismo envío con `saveAsAltPhone` ausente → `lead.altPhone` sigue vacío pero
    `sentTo` de la respuesta es el número alternativo.

**Marca y tamaño (D-08)**
13. `message` que contiene la marca → el `?text=` de la URL no la contiene
    (comparar contra el resultado de `_stripBrandMentions` vía
    `globalThis.__voiceAgent`).
14. `message` de 3000 chars → la entry/URL no supera el tope (`ACT_MESSAGE_MAX`).

**Seguridad**
15. `s_other` (setter no dueño) → 403.
16. Sin cookie → 401.
17. Lead inexistente → 404.

**Verificación por mutación (obligatoria antes de commitear):** desactivar
temporalmente la llamada a `_closeCommitment` dentro de `_actRegisterSendEvent`,
correr la suite, anotar cuántos tests se ponen en rojo (esperado: los de
`esperar_respuesta`/`cumplido`), y restaurar con `git checkout -- index.js`
(NO deshacer a mano: `core.autocrlf=true` + `sed` deja terminadores LF y
ensucia el diff — lección de 31-04). Confirmar `git diff index.js` vacío antes
de seguir y dejar el número de rojos en el SUMMARY.
  </action>

  <verify>
    <automated>npx vitest run tests/act-whatsapp.test.js tests/metrics-consistency.test.js tests/commitment-endpoints.test.js tests/commitment-model.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/act-whatsapp.test.js` pasa con al menos 17 tests.
    - `grep -c "delete process.env" tests/act-whatsapp.test.js` devuelve 0 y
      `grep -c "API_KEY = ''" tests/act-whatsapp.test.js` devuelve ≥ 3 (regla #121).
    - `npx vitest run tests/metrics-consistency.test.js` pasa y
      `git diff --name-only` NO lista `tests/metrics-consistency.test.js`.
    - `npm test` completo queda verde (o los únicos rojos son los flaky
      ambientales ya documentados en CLAUDE.md #93/#110/#130 — en ese caso
      re-correrlos en aislado y dejar constancia en el SUMMARY).
    - El SUMMARY documenta el conteo de tests rojos de la verificación por
      mutación y que `git diff index.js` quedó vacío al restaurar.
  </acceptance_criteria>

  <done>
El endpoint está probado en su camino feliz, sus rechazos, su RBAC y —lo más
importante— con una prueba explícita de que no infla ninguna métrica.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `POST /leads/:id/whatsapp-send` | body con teléfono, mensaje libre y flags de persistencia, controlado por el cliente |
| backend → `wa.me` (URL devuelta) | el texto del mensaje termina en una URL que abre el navegador del SDR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | `body.phone` | mitigate | normalización `replace(/[^\d+]/g,'')` + regex `/^\+?\d{6,15}$/` copiadas de `alt-contact`; nunca se escribe `lead.phone` (D-10) |
| T-32-02 | Elevation of Privilege | endpoint completo | mitigate | los 2 guards de `PATCH .../commitment` (dueño por `setterId` + `_visibleSetterIds`) verificados por test con `s_other` → 403 |
| T-32-03 | Information Disclosure | `body.message` → `?text=` | mitigate | `_stripBrandMentions` + cap `ACT_MESSAGE_MAX` (D-08, nota #119) |
| T-32-04 | Repudiation | registro del envío | mitigate | entry `material_sent` con `by`/`byName` del `req.auth.user`, nunca del body (mismo criterio que la nota #26 de CLAUDE.md) |
| T-32-05 | Denial of Service | `interactions` sin tope | mitigate | recorte a 200 entries, igual que `index.js:10248` |
| T-32-SC | Tampering | npm/pip/cargo installs | mitigate | esta fase NO instala paquetes: `git diff package.json package-lock.json` debe quedar vacío |
</threat_model>

<verification>
- `node --check index.js` en cada task.
- `npx vitest run tests/act-whatsapp.test.js` ≥ 17 tests verdes.
- `npx vitest run tests/metrics-consistency.test.js` verde SIN editar el archivo.
- `git diff --name-only` al cerrar el plan lista exactamente `index.js` y
  `tests/act-whatsapp.test.js` (más los archivos de planificación).
- `git diff package.json package-lock.json` vacío.
</verification>

<success_criteria>
- Un `POST` devuelve el `wa.me` correcto para MX/AR/US-formato y, en la misma
  escritura, deja `lead.commitment` cumplido y `lead.nextAction` a +48h.
- Un número alternativo se puede usar y opcionalmente guardar sin tocar
  `lead.phone`.
- Ningún dashboard cambia de número: `metrics-consistency` verde sin editar.
- `public/` sin tocar: cero bump de cache-buster en este plan.
</success_criteria>

<output>
Crear `.planning/phases/32-act-acciones/32-01-SUMMARY.md` al terminar, con: la
forma FINAL del contrato HTTP (para que 32-03 lo consuma sin adivinar), la
superficie agregada a `globalThis.__voiceAgent`, el resultado de la verificación
por mutación, y el conteo de la suite completa antes/después.
</output>
