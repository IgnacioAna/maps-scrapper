---
phase: 28-quick-alivio-inmediato
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - public/app.js
  - public/index.html
  - tests/dtpicker-wiring.test.js
autonomous: true
requirements: [GATE-03]

must_haves:
  truths:
    - "Los 5 campos de fecha de la app usan el MISMO componente de calendario (D-01)"
    - "Los atajos rápidos existentes (#call-cb-quickpicks y #call-ph-quickpicks) siguen eligiendo fecha con un solo click, sin abrir el calendario (D-05)"
    - "Los handlers de guardado siguen leyendo input.value sin cambios: ninguna firma ni ningún POST se modificó"
    - "Al elegir una hora se ve la equivalencia en el huso del lead, en texto plano y sin ningún aviso de color (D-06)"
    - "Cada día del mes muestra cuántos compromisos ya tiene: callbacks manuales + reuniones agendadas, sin contar los reintentos automáticos de cadencia (D-07)"
    - "Si el lead no tiene país mapeado, o el punto de entrada no tiene lead, no se muestra nada de hora local (D-06)"
  artifacts:
    - path: "public/app.js"
      provides: "5 llamadas a _dtPickerAttach (una por input), 3 llamadas a _dtPickerSync tras escrituras programáticas, _leadLocalTimeAt/_leadTimeAtTz y el cargador de carga por día"
      contains: "[28-02] LEADTIME-AT: INICIO"
    - path: "tests/dtpicker-wiring.test.js"
      provides: "Aserciones de código fuente sobre el cableado de los 5 inputs + tests puros de hora local por zona"
    - path: "public/index.html"
      provides: "Cache-buster de app.js bumpeado a v=20260814b"
  key_links:
    - from: "openCallbackModal / openPlaceholderModal / openScheduleModal / openAgendarModal / _openLeadModal"
      to: "window._dtPicker.attach"
      via: "llamada a _dtPickerAttach inmediatamente después de asignar el valor por defecto del input"
      pattern: "_dtPickerAttach\\("
    - from: "popover del calendario"
      to: "/api/setters/calendar"
      via: "fetch cacheado con TTL para contar reuniones por día (D-07)"
      pattern: "api/setters/calendar"
    - from: "popover del calendario"
      to: "_callsLeadsById"
      via: "conteo local de callbacks manuales por día (manualCallbackByOwner)"
      pattern: "manualCallbackByOwner"
---

<objective>
Enchufar el componente construido en 28-01 a los 5 campos de fecha reales de
la app, y darle al popover la información que el user pidió: la hora local
del lead y cuántos compromisos ya tiene cada día.

Purpose: sin este plan el calendario existe pero nadie lo usa. Con este plan,
programar una fecha deja de ser contar días a mano en los 5 lugares donde hoy
aparece un `datetime-local`.
Output: los 5 modales usando el calendario, hora local del lead en el popover,
badge de carga por día.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-quick-alivio-inmediato/28-CONTEXT.md
@.planning/phases/28-quick-alivio-inmediato/28-PATTERNS.md
@.planning/phases/28-quick-alivio-inmediato/28-01-SUMMARY.md

<interfaces>
<!-- Producido por 28-01. Ya existe en public/app.js — no re-implementar. -->

```javascript
// API pública del picker (funciones hoisted + espejo en window._dtPicker)
_dtPickerAttach(input, opts)  // opts = { getLead?: () => object|null, load?: boolean }
_dtPickerSync(input)          // repinta el trigger leyendo input.value
_dtPickerSet(input, date)     // escribe el valor y sincroniza
_dtPickerClose()

// Helpers puros disponibles (bloque DTPICKER-PURE)
_dtpFormatValue(date)                  // 'YYYY-MM-DDTHH:mm'
_dtpParseValue(str)                    // Date | null
_dtpDayKey(date)                       // 'YYYY-MM-DD' local
_dtpBuildMonthGrid(year, monthIndex)   // 42 celdas [{date, inMonth, key}]
_dtpFullLabel(date, now)               // 'Martes 18/08 · en 4 días · 10:00'
_dtpCountByDay(leads, calendarEntries) // Map('YYYY-MM-DD' → number)

// Slots del popover que este plan tiene que llenar
// .dtp-leadtime  → D-06 (hora local del lead)
// .dtp-load      → D-07 (badge de carga dentro de cada .dtp-day)
```

Contrato de `/api/setters/calendar` (index.js:12777-12785, GET, requireAuth):
devuelve `{ calendar: [...] }` ya filtrado por setter/scope del usuario
autenticado. Cada entry tiene `fecha` (ISO string) y `calendarioEstado`.

Campo `manualCallbackByOwner` (index.js:8379): viaja en cada lead de
`GET /api/setters/leads/sin-wsp`. Es `true` sólo cuando el callback pendiente
lo puso una persona (`outcome === 'callback_later'`) — exactamente el criterio
que D-07 pide para excluir los reintentos automáticos de cadencia.
</interfaces>

<call_sites>
Los 5 puntos de entrada, con su lead disponible en scope (verificado en código):

| Input | Función | Línea del default | Lead disponible |
|---|---|---|---|
| `#call-cb-fecha` | `openCallbackModal(leadId)` | app.js:10170 | `_callsLeadsById.get(leadId)` |
| `#call-ph-fecha` | `openPlaceholderModal(leadId)` | app.js:10112 | `lead` (resuelto en 10102) |
| `#call-sched-fecha` | `openScheduleModal(leadId, opts)` | app.js:10422 | `lead` (resuelto en 10416) |
| `#agendar-fecha` | `openAgendarModal(lead)` | app.js:17874 | `_agendarLead` (asignado en 17868) |
| `#schedule-datetime` | `_openLeadModal` (hidratación) | app.js:4554 | sin lead confiable → `getLead: () => null` |

Escrituras programáticas que NO son el default y necesitan `_dtPickerSync`:
- app.js:10188 — click de `.cb-quickpick` (D-05: el atajo sigue igual, sólo se repinta el trigger)
- app.js:10123 — click de `.ph-quickpick`
- app.js:4625 — click de `[data-schedule-preset]`
</call_sites>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cablear los 5 campos de fecha al componente</name>
  <files>public/app.js, public/index.html, tests/dtpicker-wiring.test.js</files>
  <read_first>
    - `public/app.js` líneas 4543-4562 — hidratación de `#schedule-datetime` dentro de `_openLeadModal`
    - `public/app.js` líneas 4605-4630 — presets `[data-schedule-preset]` y el bloque del picker insertado por 28-01
    - `public/app.js` líneas 10101-10135 — `openPlaceholderModal` (default + quickpicks)
    - `public/app.js` líneas 10165-10240 — `openCallbackModal` completo, incluido el fix del botón "Guardando…" (2026-08-11, nota #181b): ese reset NO se puede romper
    - `public/app.js` líneas 10412-10425 — `openScheduleModal`
    - `public/app.js` líneas 17866-17895 — `openAgendarModal`
    - `.planning/phases/28-quick-alivio-inmediato/28-01-SUMMARY.md` — nombres finales y ubicación del componente
  </read_first>
  <action>
    Agregar exactamente 5 llamadas a `_dtPickerAttach`, una por input, SIEMPRE inmediatamente después de la línea que asigna el valor por defecto (así el primer `attach` ya pinta el trigger con esa fecha y no hace falta un `sync` extra):

    1. `openCallbackModal` — después de `fechaInput.value = _toDatetimeLocal(m);` (app.js:10170): `_dtPickerAttach(fechaInput, { getLead: () => _callsLeadsById.get(leadId) });`
    2. `openPlaceholderModal` — después de `fechaIn.value = _toDatetimeLocal(m);` (app.js:10112): `_dtPickerAttach(fechaIn, { getLead: () => lead });`
    3. `openScheduleModal` — después de la asignación de `call-sched-fecha` (app.js:10422): `_dtPickerAttach(document.getElementById('call-sched-fecha'), { getLead: () => lead });`
    4. `openAgendarModal` — después de `document.getElementById('agendar-fecha').value = defaultIso;` (app.js:17874): `_dtPickerAttach(document.getElementById('agendar-fecha'), { getLead: () => _agendarLead });`
    5. `_openLeadModal` — dentro del `try` de hidratación, después de asignar `dtInput.value` (app.js:4554): `_dtPickerAttach(dtInput, { getLead: () => null });`

    Agregar exactamente 3 llamadas a `_dtPickerSync`, una por cada escritura programática que NO pasa por el picker (los atajos quedan funcionalmente intactos — D-05 — sólo se repinta la etiqueta del trigger):

    - dentro del handler de click de `.cb-quickpick` (app.js:10188), después de `fechaInput.value = _toDatetimeLocal(new Date(iso));`
    - dentro del handler de click de `.ph-quickpick` (app.js:10123), después de la asignación equivalente
    - dentro del handler de `[data-schedule-preset]` (app.js:4625), después de `dtInput.value = _scheduleFormatDatetimeLocal(target);`

    Restricciones duras:
    - NO tocar ninguna otra línea de esos handlers. Los `onclick` de confirmar (`#call-cb-confirm`, `#call-ph-confirm`, `#call-sched-confirm`, `#agendar-confirm`, `#schedule-submit-btn`) siguen leyendo `.value` tal cual. El reset del botón de `openCallbackModal` (`confirmBtn.disabled = false; confirmBtn.textContent = 'Programar';`) y su `finally` quedan idénticos.
    - NO cambiar el `type` de ningún input en `public/index.html`: siguen siendo 5 `datetime-local`. El componente los oculta y pone su trigger al lado; el elemento sigue siendo el mismo y `.value` sigue siendo la fuente de verdad.
    - NO borrar ni modificar `_toDatetimeLocal` ni `_scheduleFormatDatetimeLocal`.

    Bumpear el cache-buster de app.js en `public/index.html` (línea 3611) a `v=20260814b`.

    Crear `tests/dtpicker-wiring.test.js`: lee `public/app.js` como texto y assertea el cableado (es una prueba de código fuente, no de DOM — no hay jsdom en el proyecto). Debe verificar: exactamente 5 ocurrencias de `_dtPickerAttach(`; que cada uno de los 5 ids (`call-cb-fecha` vía la variable `fechaInput`, `call-ph-fecha`, `call-sched-fecha`, `agendar-fecha`, `schedule-datetime`) queda cubierto; al menos 3 ocurrencias de `_dtPickerSync(`; que `function _toDatetimeLocal` y `function _scheduleFormatDatetimeLocal` siguen existiendo una vez cada una; y que `public/index.html` sigue teniendo 5 `type="datetime-local"` y los 3 ids de quickpicks/preset intactos (`call-cb-quickpicks`, `call-ph-quickpicks`, `data-schedule-preset`).
  </action>
  <verify>
    <automated>npx vitest run tests/dtpicker-wiring.test.js tests/dtpicker-core.test.js && node --check public/app.js</automated>
    <human-check>En el preview: abrir "Volver a llamar" y comprobar que (a) los atajos "Mañana 9am"/"En 2 días" siguen cargando la fecha con un click y la etiqueta del campo se actualiza sola, (b) tocar el campo abre el calendario, (c) confirmar guarda igual que antes (el lead sale de la cola con el toast de destino).</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "_dtPickerAttach(" public/app.js` devuelve exactamente `5` (sin contar la declaración `function _dtPickerAttach`, que el test debe excluir por el prefijo `function `).
    - `grep -c "_dtPickerSync(" public/app.js` devuelve al menos `3` además de sus usos internos dentro del componente.
    - `grep -c 'type="datetime-local"' public/index.html` devuelve `5`.
    - `grep -c "function _toDatetimeLocal" public/app.js` = `1` y `grep -c "function _scheduleFormatDatetimeLocal" public/app.js` = `1`.
    - `grep -c "call-cb-quickpicks" public/index.html` ≥ `1` y `grep -c "cb-quickpick" public/app.js` ≥ `1` (los atajos de D-05 siguen ahí).
    - `grep -o 'app\.js?v=[0-9a-z]*' public/index.html` devuelve `app.js?v=20260814b`.
    - `npx vitest run tests/dtpicker-wiring.test.js` exit code 0.
    - `git diff public/app.js` no muestra ningún cambio dentro de los bloques `confirmBtn.onclick`, `phBtn.onclick`, `call-sched-confirm`, `agendar-confirm` ni `schedule-submit-btn`.
  </acceptance_criteria>
  <done>Los 5 campos de fecha abren el calendario propio, los atajos siguen funcionando y ningún handler de guardado cambió.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hora local del lead (D-06) y carga por día (D-07) dentro del popover</name>
  <files>public/app.js, tests/dtpicker-wiring.test.js</files>
  <read_first>
    - `public/app.js` líneas 7078-7103 — `_LEAD_TZ` y `_leadLocalTime` (la base a extender; el mapa país→IANA se reusa tal cual)
    - `public/app.js` líneas 6470-6495 — el chip 🕐 del Power Dialer, que SÍ usa el semáforo verde/ámbar: D-06 dice explícitamente que en el calendario NO se pinta nada de eso
    - `public/app.js` líneas 5412-5420 — `callsLeadsCache` y `_callsLeadsById` (fuente en memoria para los callbacks manuales)
    - `public/app.js` líneas 15834-15855 — `_mypLoadPipeline`: cómo se combinan hoy los leads en memoria con `GET /api/setters/calendar`
    - `index.js` líneas 12777-12790 — el endpoint `/api/setters/calendar` y su filtrado por scope
    - El bloque del picker en `public/app.js` (insertado por 28-01) — los nodos `.dtp-leadtime` y `.dtp-load` que hay que llenar
  </read_first>
  <behavior>
    - `_leadTimeAtTz('America/Mexico_City', new Date(Date.UTC(2026, 7, 18, 15, 0)))` devuelve `'09:00'` (CDMX es UTC-6 todo el año desde 2022) — el resultado no depende de la zona horaria del runner porque el Date se construye en UTC.
    - `_leadTimeAtTz('America/Argentina/Buenos_Aires', new Date(Date.UTC(2026, 7, 18, 15, 0)))` devuelve `'12:00'`.
    - `_leadTimeAtTz('Zona/Inexistente', d)` devuelve `null` en vez de lanzar.
    - `_leadLocalTimeAt(null, d)` y `_leadLocalTimeAt({ country: 'Marte' }, d)` devuelven `null` (D-06: sin país mapeado no se muestra nada).
    - `_leadLocalTimeAt({ country: 'México' }, new Date(Date.UTC(2026,7,18,15,0)))` devuelve un objeto con `time === '09:00'` y el `tz` IANA.
    - `_dtpCountByDay` (ya testeado en 28-01) alimenta los badges: un día con 2 callbacks manuales y 1 reunión muestra `3`; un día cuyo único lead tiene `callbackAt` pero `manualCallbackByOwner` falso no muestra badge.
  </behavior>
  <action>
    **D-06 — hora local del lead.** Insertar en `public/app.js`, inmediatamente después de `window._leadLocalTime = _leadLocalTime;` (app.js:7102), un bloque delimitado por `// ─── [28-02] LEADTIME-AT: INICIO ───` y `// ─── [28-02] LEADTIME-AT: FIN ───` con dos `function` declarations (hoisted, así el componente del picker —que está más arriba en el archivo— las puede llamar):

    - `_leadTimeAtTz(tzId, date)`: puro salvo por `Intl`. Devuelve `'HH:MM'` usando `date.toLocaleTimeString('es-AR', { timeZone: tzId, hour: '2-digit', minute: '2-digit', hour12: false })` dentro de try/catch; `null` ante zona inválida o argumentos faltantes.
    - `_leadLocalTimeAt(lead, date)`: busca `_LEAD_TZ[(lead && lead.country || '').trim()]`; si no hay match devuelve `null`; si hay, devuelve `{ time, tz }`. **No** devuelve el flag `ok` de `_leadLocalTime`: D-06 prohíbe el semáforo en este contexto.
    - Exponer `window._leadLocalTimeAt = _leadLocalTimeAt;` (convención del proyecto, igual que `window._leadLocalTime`).

    En el popover, al renderizar/actualizar el draft, llenar `.dtp-leadtime` con el texto `= HH:MM para el lead` (más el nombre de país si está disponible), escrito con `textContent`, sin ninguna clase ni color de alerta. Si `opts.getLead()` devuelve `null` o `_leadLocalTimeAt` devuelve `null`, el nodo queda vacío y sin altura visible.

    **D-07 — carga por día.** Agregar dentro del bloque del componente (junto al resto del estado del picker de 28-01):

    - `let _dtpCalCache = { at: 0, entries: [] };` — cache del fetch de calendario con TTL de 120000 ms.
    - `async function _dtpFetchCalendar()`: si el cache está fresco devuelve `_dtpCalCache.entries`; si no, hace `fetch(apiUrl('/api/setters/calendar'), { credentials: 'include' })`, guarda `j.calendar || []` y devuelve. Ante cualquier error, loguea con `console.warn` y devuelve `[]` — un fallo de red NO puede romper la elección de fecha.
    - `function _dtpLocalCallbackLeads()`: devuelve `Array.from(_callsLeadsById.values())` si el Map existe y tiene datos; si no, `[]`. Nunca lanza.
    - `function _dtpPaintLoad(counts)`: recorre las celdas `.dtp-day` ya renderizadas y, para las que tienen `counts.get(key) > 0`, inserta/actualiza un `span.dtp-load` con el número (`textContent`). Las celdas sin compromisos no muestran nada.

    Flujo al abrir el popover y al navegar de mes: renderizar la grilla PRIMERO (sin latencia perceptible — requisito de Claude's Discretion del CONTEXT) y disparar el conteo en paralelo. Cuando resuelve, si el popover sigue abierto y sigue mostrando el mismo mes (`_dtpState.view`), pintar los badges con `_dtpCountByDay(_dtpLocalCallbackLeads(), entries)`. Si `opts.load === false`, saltear todo el bloque.

    Agregar los casos de `_leadTimeAtTz`/`_leadLocalTimeAt` a `tests/dtpicker-wiring.test.js` extrayendo el bloque `LEADTIME-AT` por marcadores y evaluándolo con `new Function`, inyectando un `_LEAD_TZ` mínimo (`{'México':'America/Mexico_City','Argentina':'America/Argentina/Buenos_Aires'}`) como parámetro del wrapper — el bloque real lee la constante del closure, así que el wrapper debe declararla antes del código extraído.
  </action>
  <verify>
    <automated>npx vitest run tests/dtpicker-wiring.test.js tests/dtpicker-core.test.js && node --check public/app.js</automated>
    <human-check>En el preview, abrir el calendario desde un lead con país cargado (ej. México): al elegir una hora aparece "= HH:MM para el lead" en texto neutro (sin ámbar), y los días que ya tienen callbacks o reuniones muestran su numerito.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "LEADTIME-AT: INICIO" public/app.js` = `1` y `grep -c "window._leadLocalTimeAt" public/app.js` ≥ `1`.
    - `grep -c "function _leadLocalTime\b" public/app.js` = `1` (la función original quedó intacta; el chip del Power Dialer sigue usándola con su semáforo).
    - El bloque `LEADTIME-AT` no contiene la subcadena `ok:` — verificable con `sed -n '/LEADTIME-AT: INICIO/,/LEADTIME-AT: FIN/p' public/app.js | grep -c "ok:"` → `0` (D-06: sin semáforo).
    - `grep -c "manualCallbackByOwner" public/app.js` ≥ `2` (el uso existente de `_mypLoadPipeline` más el conteo nuevo, que llega vía `_dtpCountByDay`).
    - `grep -c "api/setters/calendar" public/app.js` aumentó en 1 respecto del estado previo al plan (el fetch cacheado del picker).
    - `npx vitest run tests/dtpicker-wiring.test.js` pasa e incluye los 5 casos de comportamiento de hora local descritos arriba.
    - `npm test` completo en verde.
  </acceptance_criteria>
  <done>El popover muestra la hora del lead sin avisos de color y el numerito de compromisos por día, sin agregar latencia perceptible al abrir.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/api/setters/calendar` → popover | respuesta del servidor renderizada en el DOM (sólo se usan conteos numéricos) |
| `lead.country` (dato scrapeado) → `Intl.DateTimeFormat` | string no confiable usado como clave de un mapa cerrado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering (XSS) | badges `.dtp-load` y texto `.dtp-leadtime` | mitigate | ambos se escriben con `textContent`; del calendario sólo se usa un conteo numérico, nunca un campo de texto de la entry |
| T-28-05 | Information Disclosure | conteo por día en el popover | accept | `/api/setters/calendar` ya filtra por setter/`visibleSetterIds` en el backend (index.js:12777-12785); el popover no puede ver más de lo que la vista Reuniones ya muestra al mismo usuario |
| T-28-06 | Denial of Service (auto-infligido) | fetch del calendario al abrir el popover | mitigate | cache con TTL 120s, render de la grilla sin esperar la red, y `catch` que devuelve `[]` — un backend caído deja el calendario sin badges pero perfectamente usable |
| T-28-07 | Tampering | `lead.country` como clave de zona horaria | mitigate | `_LEAD_TZ` es un mapa cerrado; una clave desconocida devuelve `null` y no se muestra nada; `_leadTimeAtTz` envuelve `Intl` en try/catch |
| T-28-SC | Tampering | instalación de paquetes | mitigate | esta fase no instala nada: `git diff --stat package.json package-lock.json` vacío al cerrar el plan |
</threat_model>

<verification>
- `npm test` completo verde (backend intacto).
- Los 5 inputs siguen siendo `datetime-local` con los mismos ids.
- Los quickpicks de `#call-cb-quickpicks` y `#call-ph-quickpicks` siguen presentes y funcionales (D-05).
- Cache-buster de app.js en `v=20260814b`.
</verification>

<success_criteria>
- Los 5 campos de fecha abren el calendario propio (D-01).
- Los atajos rápidos siguen siendo un click directo (D-05).
- El popover muestra la hora local del lead en texto plano (D-06).
- Cada día muestra su carga de callbacks manuales + reuniones (D-07).
- Ningún handler de guardado ni ningún endpoint cambió.
</success_criteria>

<output>
Create `.planning/phases/28-quick-alivio-inmediato/28-02-SUMMARY.md` when done
</output>
