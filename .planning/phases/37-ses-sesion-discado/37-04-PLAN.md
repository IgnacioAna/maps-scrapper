---
phase: 37-ses-sesion-discado
plan: 04
type: execute
wave: 4
depends_on: ["37-02", "37-03"]
files_modified:
  - public/app.js
  - public/index.html
  - tests/dial-session-myperf-ui.test.js
autonomous: true
requirements: [SES-03, SES-04]
must_haves:
  truths:
    - "En Mi rendimiento el SDR ve sus últimas sesiones de discado y puede comparar la de hoy con la de ayer: cuándo, cuánto duró, marcadas, atendieron, conversaciones"
    - "La columna de estado hace visible lo que se respondió al cerrar: sin eso la pregunta sería write-only y no serviría para explicar por qué una semana rinde 8× más que otra"
    - "Una sesión que se cerró sola (pestaña cerrada, recarga) se distingue de una que el SDR cerró: no se lee como una jornada que terminó"
    - "Sin sesiones todavía, la sección dice qué falta hacer — nunca una tabla vacía ni un 0 desnudo"
    - "En modo Ver como SDR y con el selector de SDR de la vista, la tabla muestra las sesiones de ESE vendedor, no las del que está logueado"
  artifacts:
    - path: "public/index.html"
      provides: "sección #myp-sessions en view-myperf + cache-buster"
      contains: "myp-sessions"
    - path: "public/app.js"
      provides: "_mypLoadSessions + render de la tabla, colgado de la misma cadena que el resto de Mi rendimiento"
      contains: "_mypLoadSessions"
    - path: "tests/dial-session-myperf-ui.test.js"
      provides: "Suite: render puro de filas, agrupación por día, setter efectivo, apiUrl, cache-buster"
  key_links:
    - from: "_mypLoad"
      to: "_mypLoadSessions(effectiveSetter)"
      via: "misma cadena y mismo setter efectivo que _mypLoadPipeline — no bloquea el resto de la vista"
      pattern: "_mypLoadSessions\\(effectiveSetter\\)"
    - from: "_mypLoadSessions"
      to: "GET /api/setters/dial-sessions"
      via: "apiUrl() + ?setter= explícito cuando corresponde (reglas #135/#146)"
      pattern: "dial-sessions"
    - from: "session.mood"
      to: "columna de estado"
      via: "SES_MOOD_LABELS, el mismo mapa que usan los chips de la pantalla de cierre"
      pattern: "SES_MOOD_LABELS"
---

<objective>
Cierra la fase: el historial de sesiones se ve. Una tabla en Mi rendimiento
con las últimas sesiones del SDR — cuándo, cuánto duró, marcadas, atendieron,
conversaciones y cómo la remó.

Purpose: SES-03. Hasta 37-03 el SDR ve el resultado de la sesión que acaba de
cerrar y nada más; la partida de ayer no existe en ninguna pantalla. Y SES-04
queda cerrado recién acá: el estado del operador se guarda desde 37-02/37-03,
pero si no se lee en algún lado es un campo write-only.

Output: sección "Sesiones de discado" en `view-myperf`, cargada por la misma
cadena que el resto de la vista.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-ses-sesion-discado/CONTEXT.md
@.planning/phases/37-ses-sesion-discado/37-02-PLAN.md
@.planning/phases/37-ses-sesion-discado/37-03-PLAN.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contratos vigentes. Leer los rangos de <read_first> antes de editar. -->

Cadena de carga de Mi rendimiento (app.js ~18829-18862) — el precedente exacto
a copiar, incluido el manejo del setter efectivo en modo "Ver como":
```js
async function _mypLoad() {
  ...
  const u = window.__CURRENT_USER__;
  const isViewAsSetter = u?.realRole === 'admin' && u?.role === 'setter' && u?.setterId;
  const setterFilter = document.getElementById('myp-setter')?.value || '';
  const effectiveSetter = setterFilter || (isViewAsSetter ? u.setterId : '');
  if (effectiveSetter) params.set('setter', effectiveSetter);
  const r = await fetch(apiUrl(`/api/setters/cold-call-metrics?${params.toString()}`), { credentials: 'include' });
  ...
  _mypRenderAssigned(d); _ccmRender(d); _mypRenderChart(d);
  _mypLoadPipeline(effectiveSetter).catch((e) => console.warn('[myp-pipeline]', e?.message));
  _mypPopulateSetters();
}
```

Endpoint que dejó 37-02:
```
GET /api/setters/dial-sessions?setter=&limit=&all=
  200 → { setterId, sessions: [ { id, setterId, by, startedAt, endedAt, durationS,
          mode, hoyFilter, filtro, queueSize, processed, mood, closedBy,
          counters: { dials, connects, conversations, appointments, deals,
                      totalDurationS, avgConvDurationS, leads, byOutcome } } ], total }
```

Puro que dejó 37-03 (se reusa, no se reimplementa):
```js
// ─── [37-03] SESSION-PURE: INICIO ───
function _sesDurationLabel(seconds) { /* '0 min' | 'N min' | 'Xh MM' */ }
function _sesClosingModel(payload, opts) { ... }
// ─── [37-03] SESSION-PURE: FIN ───
window.__ses = { _sesDurationLabel, _sesClosingModel };
```

Estructura de `view-myperf` en `public/index.html` (~2364-2440): toolbar de
período → `#myp-assigned` → "Mi cartera" `#myp-pipeline` → "Funnel de llamadas"
→ "Evolución" (`#myp-chart`) → `#myp-empty`. Las etiquetas de sección usan
`<div class="myp-section-label">`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Sección "Sesiones de discado" en Mi rendimiento</name>
  <files>public/app.js, public/index.html</files>

  <read_first>
    - `public/index.html` líneas 2364-2440 (`view-myperf` completo: toolbar, secciones y sus etiquetas)
    - `public/app.js` líneas 18829-18870 (`_mypLoad` y cómo cuelga `_mypLoadPipeline`)
    - `public/app.js` líneas 18871-18950 (`_mypLoadPipeline`: molde de loader de sección — fetch con `apiUrl`, render, estado vacío)
    - `public/app.js`: el bloque `[37-03] SESSION-PURE` y el HTML de los chips de estado de `_pdShowClosing` (localizar con `grep -n "SESSION-PURE\|data-mood" public/app.js`)
  </read_first>

  <action>
    1. **`public/index.html`** — agregar al final de `view-myperf`, después del
       bloque de "Evolución" y antes de `#myp-empty`:
       `<div class="myp-section-label">Sesiones de discado</div>` y
       `<section id="myp-sessions" style="margin-top:4px;"></section>`.
       Reusar las clases que ya usa la vista (`myp-section-label`,
       `myp-tile`/`seg-control` si hacen falta); **no crear CSS nuevo en
       `style.css`** — esta fase no toca la hoja de estilos.

    2. **`SES_MOOD_LABELS`** — mover/definir el mapa de estados
       (`bien` → `'Bien'`, `normal` → `'Normal'`, `costo` → `'Me costó'`,
       `pesimo` → `'Pésima'`) DENTRO del bloque `[37-03] SESSION-PURE`, y hacer
       que los chips de la pantalla de cierre (37-03) lo consuman en vez de
       tener las etiquetas escritas a mano. Un solo mapa, dos consumidores: si
       mañana se agrega un estado, no queda la mitad de la app llamándolo
       distinto. El bloque sigue siendo autocontenido (un objeto de strings no
       rompe la pureza).

    3. **`_mypLoadSessions(effectiveSetter)`** en `public/app.js`, junto a
       `_mypLoadPipeline`:
       - `const el = document.getElementById('myp-sessions'); if (!el) return;`
       - `GET` a `/api/setters/dial-sessions` con `limit=20`, vía `apiUrl()` y
         `credentials: 'include'`. `?setter=` solo cuando hay
         `effectiveSetter` y el rol es `admin`/`supervisor` (mismo criterio que
         `_mypLoadPipeline`: un SDR real nunca manda setter).
       - Si falla: dejar la sección con un texto discreto ("No se pudo cargar
         el historial de sesiones") y `console.warn('[myp-sessions]', …)`. No
         romper el resto de la vista (por eso se cuelga con `.catch` de
         `_mypLoad`, igual que el pipeline).

    4. **Render de la tabla** — tabla simple, con las clases que ya existen en
       la vista. Columnas: **Cuándo · Duración · Marcadas · Atendieron ·
       Conversaciones · Cómo la remó · Cola**.
       - *Cuándo*: hora de inicio (`toLocaleTimeString` corto). Las filas se
         agrupan por día con un encabezado por jornada: `Hoy`, `Ayer` o la
         fecha; **sin totales por día** (decisión de 37-02: un total diario
         propio sería un segundo número de "hoy" que puede no coincidir con el
         canónico del funnel que está más arriba en la MISMA pantalla). El
         agrupador compara contra la fecha local del browser, que es lo que el
         SDR tiene en la cabeza.
       - *Duración*: `_sesDurationLabel(durationS)` — el mismo helper de la
         pantalla de cierre.
       - *Marcadas / Atendieron / Conversaciones*: `counters.dials`,
         `counters.connects`, `counters.conversations`. Números tal cual del
         servidor, `font-variant-numeric: tabular-nums` como el resto de la
         vista. **Marcadas es la columna que manda visualmente** (D-01): en
         negrita, las otras dos en tono secundario.
       - *Cómo la remó*: `SES_MOOD_LABELS[mood]`; si está vacío, un guion
         discreto (no "sin datos" en rojo: no responder es una opción válida
         por D-03).
       - *Cola*: modo legible (`Llamadas` / `Hoy` + la sección de `hoyFilter`
         si la hay) y el `queueSize` con el que arrancó, en tono secundario.
       - Una sesión con `closedBy === 'auto'` lleva una marca discreta
         ("cerrada sola") con `title` explicando que la pestaña se cerró o se
         recargó: sin eso se lee como una jornada que terminó cuando en
         realidad se cortó.
       - Una sesión ABIERTA (`!endedAt`) se muestra como "en curso", sin
         números (todavía no tiene `counters`).
       - Todo lo que venga del servidor se pinta con `escHtml`.
       - **Estado vacío**: si no hay sesiones, la sección dice
         "Todavía no registraste ninguna sesión de discado — abrí el Power
         Dialer y al salir vas a ver acá el resultado". Nunca una tabla con
         encabezados y nada abajo.

    5. **Cablear** en `_mypLoad`, al lado de `_mypLoadPipeline`:
       `_mypLoadSessions(effectiveSetter).catch((e) => console.warn('[myp-sessions]', e?.message));`
       No agregar un período propio ni un selector nuevo: el historial de
       sesiones no depende del rango de arriba (son las últimas 20 y punto).
       Dejarlo dicho en un comentario para que no se "arregle" después
       enganchándolo al `_mypState.period`, que traería de vuelta el problema
       del total diario.

    6. **Cache-buster** en `public/index.html`: bumpear `app.js?v=` por encima
       del que dejó 37-03. `style.css?v=` NO se toca.
  </action>

  <verify>
    <automated>node --check public/app.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "myp-sessions" public/index.html` devuelve `1` y `grep -c "_mypLoadSessions" public/app.js` es `>= 2` (definición + llamada en `_mypLoad`).
    - La llamada dentro de `_mypLoad` pasa `effectiveSetter` y tiene `.catch(`.
    - El fetch de `_mypLoadSessions` usa `apiUrl(` (nunca `fetch('/api/...')` crudo).
    - `SES_MOOD_LABELS` se declara exactamente 1 vez, DENTRO del bloque `[37-03] SESSION-PURE`, y las 4 etiquetas con acentos NO aparecen escritas a mano en el HTML de los chips de cierre.
    - `git diff public/style.css` vacío.
    - En `public/index.html`, `app.js?v=` matchea `/\d{8}[a-z]/`, aparece una sola vez, y es estrictamente mayor que el valor que dejó 37-03.
    - `git diff --stat package.json package-lock.json` vacío.
  </acceptance_criteria>

  <done>
    El SDR entra a Mi rendimiento y ve sus últimas sesiones, con la de hoy
    arriba de la de ayer, y con la respuesta de estado a la vista.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suite de SES-03 en el frontend + cache-buster</name>
  <files>tests/dial-session-myperf-ui.test.js</files>

  <behavior>
    - El armador de filas agrupa por día y etiqueta Hoy / Ayer / fecha.
    - Una sesión sin `mood` no rompe ni muestra "undefined".
    - Una sesión `closedBy: 'auto'` sale marcada; una abierta sale "en curso" sin números.
    - Sin sesiones → estado vacío con texto, nunca tabla pelada.
  </behavior>

  <read_first>
    - `tests/dial-session-close-ui.test.js` (el que dejó 37-03: reusar el `extractFunctionBody` y la forma de evaluar el bloque puro)
    - `tests/hoy-sections.test.js` o `tests/commitment-ui.test.js` (cómo este repo testea render de secciones sin jsdom)
  </read_first>

  <action>
    Crear `tests/dial-session-myperf-ui.test.js` (mínimo 12 tests), molde de
    las suites de UI de esta familia (aserciones de fuente + evaluación de lo
    puro con `new Function`).

    Para que las filas sean testeables sin browser, el armado del modelo de
    cada fila tiene que ser una función pura dentro del bloque
    `[37-03] SESSION-PURE` — `_sesHistoryRows(sessions, nowMs)` — que devuelva
    `[{ tipo: 'dia', label } | { tipo: 'sesion', hora, duracion, dials,
    connects, conversations, mood, cola, auto, enCurso }]`. El HTML lo arma el
    render afuera. Cobertura:

    **Puro (`_sesHistoryRows`)**
    - Sesiones de hoy y de ayer → aparecen 2 encabezados de día, con `Hoy` y `Ayer`, y las de hoy primero.
    - Una sesión de hace 5 días → encabezado con la fecha, no "Ayer".
    - `duracion` sale de `_sesDurationLabel` (mismo texto para el mismo `durationS`).
    - `mood: ''` → el modelo devuelve `''`, nunca `undefined` ni `'undefined'`.
    - `closedBy: 'auto'` → `auto: true`; `closedBy: 'user'` → `auto: false`.
    - `endedAt: null` → `enCurso: true` y sin números.
    - `counters` ausente (sesión vieja o abierta) → no tira, devuelve 0/`null` según corresponda.
    - Lista vacía → `[]` (el estado vacío lo decide el render).
    - Orden: entra desordenado, sale por `startedAt` descendente.
    - `_sesHistoryRows` NO suma nada por día (aserción explícita: ningún row de tipo `dia` trae contadores) — la decisión de no inventar un total diario, clavada en un test.

    **Cableado (aserciones de fuente)**
    - `_mypLoadSessions` se declara 1 vez y se llama dentro de `_mypLoad` con `effectiveSetter`.
    - El fetch usa `apiUrl(`.
    - El render usa `escHtml` sobre los campos del servidor.
    - `SES_MOOD_LABELS` está dentro del bloque `[37-03] SESSION-PURE` y lo consumen los dos lugares (chips de cierre e historial).
    - El texto del estado vacío existe en el archivo.

    **Cache-buster**
    - Forma `/\d{8}[a-z]/`, una sola ocurrencia, estrictamente mayor que el baseline real leído en disco al empezar el plan (documentarlo en el test como comentario, sin pinear el valor nuevo).
  </action>

  <verify>
    <automated>npx vitest run tests/dial-session-myperf-ui.test.js tests/dial-session-close-ui.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/dial-session-myperf-ui.test.js` pasa con al menos 12 tests y 0 fallos.
    - `tests/dial-session-close-ui.test.js` sigue verde SIN editarlo salvo por el cambio de `SES_MOOD_LABELS` (si hubo que ajustarlo, documentar exactamente qué aserción cambió y por qué en el SUMMARY).
    - `npm test` completo: 0 fallos nuevos respecto del baseline real.
  </acceptance_criteria>

  <done>
    El historial queda protegido por tests, incluido el criterio de agrupar por
    día sin inventar totales diarios.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| respuesta de `GET /dial-sessions` → tabla de Mi rendimiento | `mood`, `filtro`, `hoyFilter` y demás campos persistidos se pintan en el DOM |
| selector de SDR / modo "Ver como" → `?setter=` | quién decide de quién son las sesiones que se piden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-16 | Tampering | inyección al pintar campos persistidos (`mood`, `filtro`, `mode`) en la tabla | mitigate | `escHtml` en todo lo que viene del servidor; `mood` además pasa por `SES_MOOD_LABELS` (mapa cerrado, un valor desconocido cae a guion) |
| T-37-17 | Information Disclosure | un supervisor scoped viendo sesiones de un SDR fuera de su visibilidad al elegirlo en el selector | mitigate | El filtrado real es del backend (37-02, `_visibleSetterIds` → 403); el frontend solo puebla el selector con los SDRs que ya le devuelve la API |
| T-37-18 | Repudiation | leer una sesión cerrada sola como una jornada que terminó bien | mitigate | `closedBy: 'auto'` se muestra explícito como "cerrada sola", con `title` que lo explica |
| T-37-SC | Tampering | instalación de paquetes npm | mitigate | Este plan no instala nada |
</threat_model>

<verification>
1. `node --check public/app.js`.
2. `npx vitest run tests/dial-session-myperf-ui.test.js tests/dial-session-close-ui.test.js` — verdes.
3. `npm test` completo contra el baseline real.
4. Verificación en preview con datos reales: abrir y cerrar 2 sesiones cortas desde el Power Dialer (una desde Llamadas, otra desde una sección de Hoy), responder el estado en una y saltearlo en la otra, entrar a Mi rendimiento y confirmar que las dos aparecen con sus números, la duración, el estado de la que se respondió y el guion discreto en la otra. Confirmar también en modo "Ver como SDR" que la tabla trae las de ESE vendedor.
5. `git diff --stat package.json package-lock.json` vacío y `git diff public/style.css` vacío.
</verification>

<success_criteria>
- Existe historial de sesiones visible con fecha, duración, marcadas, atendieron y conversaciones (SES-03).
- La respuesta de estado del operador se puede leer, no solo escribir (SES-04 cerrado de punta a punta).
- La tabla respeta el setter efectivo en modo "Ver como" y con el selector de la vista.
- No aparece ningún total diario que compita con el funnel canónico de la misma pantalla.
</success_criteria>

<output>
Crear `.planning/phases/37-ses-sesion-discado/37-04-SUMMARY.md` al terminar.

Documentar explícitamente:
- El baseline real de `npm test` y el cache-buster final de la fase.
- Qué se verificó en preview.
- Estado final de los 5 criterios SES-01..SES-05 de la fase, uno por uno, con
  el archivo/test que los respalda.
</output>
