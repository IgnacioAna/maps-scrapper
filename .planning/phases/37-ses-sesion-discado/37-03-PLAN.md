---
phase: 37-ses-sesion-discado
plan: 03
type: execute
wave: 3
depends_on: ["37-01", "37-02"]
files_modified:
  - public/app.js
  - public/index.html
  - tests/dial-session-close-ui.test.js
autonomous: true
requirements: [SES-01, SES-02, SES-04]
must_haves:
  truths:
    - "Salir del Power Dialer SIEMPRE muestra el resultado de esa sesión antes de cerrar el panel — con la cola vacía o con 300 leads sin tocar, con Esc, con el botón Salir o navegando por el menú"
    - "El número grande de la pantalla de cierre es MARCADAS y sube aunque la sesión no haya dejado un solo interesado; el desglose comercial va abajo"
    - "Los números que se muestran vienen del servidor: el frontend no cuenta llamadas por su cuenta"
    - "Al cerrar se ofrece una sola vez decir cómo la remó; si el SDR no responde, la sesión se cierra igual"
    - "Si el backend no responde, el SDR igual ve una pantalla de cierre y puede salir: nunca queda encerrado en el dialer"
    - "En la pantalla de cierre las teclas de resultado (1-9) no marcan nada: ya no hay tarjeta abajo"
  artifacts:
    - path: "public/app.js"
      provides: "_pdSession (ciclo de vida) + bloque puro [37-03] SESSION-PURE + pantalla de cierre + chips de estado"
      contains: "[37-03] SESSION-PURE"
    - path: "public/index.html"
      provides: "cache-buster de app.js bumpeado"
      contains: "app.js?v="
    - path: "tests/dial-session-close-ui.test.js"
      provides: "Suite: modelo puro de la pantalla, cableado del ciclo de vida en las 3 salidas, guards, cache-buster"
  key_links:
    - from: "window._pdStart"
      to: "POST /api/setters/dial-sessions"
      via: "se abre la sesión cuando el dialer abre de verdad (después de los early-return de cola vacía), fire-and-forget"
      pattern: "_pdSessionOpen"
    - from: "window._pdExit"
      to: "_pdShowClosing → POST /dial-sessions/:id/close"
      via: "primera salida muestra el cierre; la segunda cierra el panel de verdad"
      pattern: "_pdShowClosing"
    - from: "_pdAdvance (fin de cola)"
      to: "_pdShowClosing"
      via: "la pantalla de '¡Cola completa!' pasa a ser una variante de la MISMA pantalla de cierre, no un HTML aparte"
      pattern: "cola_completa"
    - from: "chips de estado"
      to: "PATCH /api/setters/dial-sessions/:id"
      via: "window._pdSessionMood, opcional, no bloquea el cierre (D-03)"
      pattern: "_pdSessionMood"
---

<objective>
El Power Dialer empieza a abrir y cerrar sesiones de verdad, y salir del
dialer pasa a tener pantalla de cierre SIEMPRE.

Purpose: SES-02 y SES-04. Hoy el resumen (app.js:7378-7387) vive DENTRO de
`if (_pd.currentIdx >= _pd.queue.length)` — o sea solo si el SDR agotó la cola
entera, que con colas de decenas de leads no pasa nunca — y cuando aparece
dice solo *"Procesaste N leads en esta sesión"*, sin desglose. La partida se
juega y no devuelve marcador.

D-01 manda la jerarquía visual: **la victoria definida es marcar**. El número
grande es marcadas y sube aunque el resultado comercial haya sido cero; el
desglose comercial (atendieron / conversaciones / agendadas) va abajo.

Output: ciclo de vida de la sesión cableado en el dialer, pantalla de cierre
única, chips de estado, cache-buster.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-ses-sesion-discado/CONTEXT.md
@.planning/phases/37-ses-sesion-discado/37-01-PLAN.md
@.planning/phases/37-ses-sesion-discado/37-02-PLAN.md
@CLAUDE.md
</context>

<interfaces>
<!-- Contratos vigentes en public/app.js. Leer los rangos de <read_first>. -->

Estado del dialer (app.js ~6915) — se le suma UN flag (`closing`); el resto
del objeto no se toca:
```js
const _pd = {
  active: false, mode: 'calls', hoyFilter: null, queue: [], currentIdx: 0,
  processed: 0, autopilot: false, autopilotArmed: false, autopilotTimer: null,
  holdCurrent: false, holdOutcome: null, holdMeta: null,
  forced: new Set(), pendingSave: null,
};
```

Fin de cola actual (app.js ~7378-7387, dentro de `_pdAdvance`) — el HTML que
este plan reemplaza por la pantalla única:
```js
if (_pd.currentIdx >= _pd.queue.length) {
  document.getElementById('pd-current-content').innerHTML = `... ¡Cola completa! ...
    Procesaste ${_pd.processed} leads en esta sesión. ...`;
  document.getElementById('pd-queue').innerHTML = '';
  document.getElementById('pd-progress').textContent = `${_pd.processed} procesadas · completado`;
  return;
}
```

Salida actual (app.js ~7280):
```js
window._pdExit = function() {
  if (_telnyx?.activeCall) { if (!confirm('Hay una llamada activa...')) return; try { _telnyx.activeCall.hangup?.(); } catch {} }
  _pdCancelAutopilot();
  _pd.active = false;
  document.getElementById('power-dialer').style.display = 'none';
  document.body.style.overflow = '';
  if (_pd.mode === 'hoy') loadHoyView(); else loadCallsView();
};
```

Los 3 caminos que hoy llaman a `_pdExit`:
- `#pd-exit-btn` (app.js ~8030)
- Escape (app.js ~8117, `if (e.key === 'Escape') { if (_pd.autopilotTimer) {...} else { window._pdExit(); } return; }`)
- delegación del sidebar (app.js ~7304-7310): cualquier click en `.menu-item[data-target]` con `_pd.active`

Etiquetas de resultado YA existentes (app.js ~9870) — se REUSAN, no se
duplica el mapa:
```js
function callOutcomeLabel(o) { /* answered_interested → 'Interesado', no_answer → 'No atendió', … */ }
```

Bloque marcador puro, precedente exacto a copiar (app.js 13225-13342):
```js
// ─── [33-04] HISTORY-PURE: INICIO ───
//   …funciones sin document/localStorage/fetch/Date.now, dependencias inyectadas…
// ─── [33-04] HISTORY-PURE: FIN ───
```

Respuesta del cierre (la deja 37-01) — el frontend NO calcula nada de esto:
```
{ session: { id, startedAt, endedAt, durationS, mode, hoyFilter, queueSize,
             processed, mood, closedBy,
             counters: { dials, connects, conversations, appointments, deals,
                         totalDurationS, avgConvDurationS, leads, byOutcome } },
  previous: { id, startedAt, endedAt, durationS, counters } | null,
  alreadyClosed?: true }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bloque puro [37-03] SESSION-PURE — el modelo de la pantalla de cierre</name>
  <files>public/app.js</files>

  <behavior>
    - `_sesDurationLabel(0)` → `'0 min'`; `(95)` → `'1 min'`; `(4320)` → `'1h 12'`.
    - `_sesClosingModel` con `counters.dials = 34` → `big.value = 34`, `big.label` habla de marcadas.
    - Con `dials = 0` → devuelve igual un modelo mostrable (no null, no excepción).
    - El desglose sale ordenado de mayor a menor y su suma es `dials`.
    - Con `previous` → línea de comparación con el signo correcto; sin `previous` → `null`, no una comparación contra 0.
    - Con `error: true` (el backend no respondió) → modelo de fallback con el `processed` local y `canMood: false`.
  </behavior>

  <read_first>
    - `public/app.js` líneas 13225-13342 (bloque `[33-04] HISTORY-PURE`: cómo se delimita, cómo se inyectan dependencias, qué NO puede referenciar)
    - `public/app.js` líneas 9870-9884 (`callOutcomeLabel` — el mapa que se reusa desde AFUERA del bloque)
    - `public/app.js` líneas 7314-7366 (`_pdTodayStats` / `_pdRenderToday`: leerlo para NO reusarlo — ver la nota de la acción)
    - `tests/dial-history.test.js` líneas 150-215 (cómo se extrae y evalúa un bloque marcador con `new Function` en este repo)
  </read_first>

  <action>
    Trabajar SOLO sobre `public/app.js`. Crear el bloque
    `// ─── [37-03] SESSION-PURE: INICIO ───` / `// ─── [37-03] SESSION-PURE: FIN ───`
    cerca del bloque del Power Dialer (antes de `_pdAdvance`), autocontenido:
    sin `document`, sin `localStorage`, sin `fetch(`, sin `Date.now(`, sin
    `window.` — todo lo variable entra por parámetro.

    1. **`_sesDurationLabel(seconds)`** — pura. `< 60` → `'0 min'`; `< 3600` →
       `'N min'`; si no → `'Xh MM'` (minutos con 2 dígitos). Nunca devuelve
       `NaN` ni negativos (clampear a 0).

    2. **`_sesClosingModel(payload, opts)`** — pura, el modelo completo de la
       pantalla. `payload` = `{ session, previous, error }` tal cual lo
       devuelve el cierre (37-01), más `opts` = `{ processedLocal, reason }`
       donde `reason` ∈ `'salida' | 'cola_completa'`. Devuelve:
       - `title`: `'¡Cola completa!'` si `reason === 'cola_completa'`, si no
         `'Sesión terminada'`.
       - `big`: `{ value, label, sub }` — **`value` es SIEMPRE
         `counters.dials`** (D-01: la victoria es marcar), `label` = `'marcadas'`
         (singular si es 1), `sub` = duración vía `_sesDurationLabel` más, si
         `counters.leads` difiere de `dials`, cuántos leads distintos fueron
         (un lead marcado dos veces son 2 marcadas y 1 lead: decirlo evita que
         el número parezca inflado).
       - `secondary`: array ordenado y fijo `[atendieron, conversaciones,
         agendadas]` tomando `connects`, `conversations`, `appointments` — cada
         uno `{ key, label, value }`. Va ABAJO del número grande (D-01).
       - `breakdown`: `Object.entries(counters.byOutcome)` como
         `[{ outcome, n }]`, ordenado por `n` descendente y, a igualdad, por
         `outcome` alfabético (determinístico para los tests). Sin etiquetas:
         las pone el render con `callOutcomeLabel`.
       - `comparison`: `null` si no hay `previous`; si hay,
         `{ prevDials, diff, when }` con `diff = dials - previous.counters.dials`
         y `when` = el `startedAt` de la anterior en crudo (el formateo de
         fecha es del render, que sí puede usar `toLocaleString`).
       - `canMood`: `true` solo si hay `session.id` y no hubo `error` — sin id
         no hay a qué colgarle el estado.
       - `mood`: el ya guardado (`session.mood`) o `''`.
       - `degraded`: `true` cuando `error` es truthy. En ese caso `big.value`
         cae a `opts.processedLocal` y `big.label` dice `'leads pasados'` —
         nunca se muestra un 0 de marcadas que sería mentira, y se acompaña con
         `note` explicando que el resumen no se pudo guardar.
       - Nunca tira: con `payload` `null`/`{}` devuelve el modelo degradado.

    3. **NO reusar `_pdTodayStats`** (app.js:7314) para nada de esto, y dejarlo
       escrito en un comentario dentro del bloque: esa función tiene su PROPIA
       definición de conversación (`answered_interested`,
       `answered_not_interested`, `scheduled_with_admin`), cuenta solo sobre
       `callsLeadsCache` y corta el día con la medianoche del browser. Es el
       chip de meta diaria (fuera de alcance por D-04, no se toca) y no es el
       canon. Los números de la sesión vienen del servidor, que los deriva del
       CALL METRICS CORE (SES-05).

    4. Exponer los dos puros en `window.__ses` (patrón `globalThis.__X` del
       repo) para que la suite los pueda evaluar sin browser.
  </action>

  <verify>
    <automated>node --check public/app.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - Los marcadores `[37-03] SESSION-PURE: INICIO` y `: FIN` aparecen exactamente 1 vez cada uno.
    - `_sesDurationLabel` y `_sesClosingModel` se declaran DENTRO del rango entre los marcadores.
    - El texto entre los marcadores NO contiene: `document`, `localStorage`, `fetch(`, `Date.now(`, `window.`, `_pdTodayStats`, `callsLeadsCache`.
    - El bloque no define un mapa propio de etiquetas de outcome (`grep` de `answered_interested:` dentro del bloque devuelve 0 — las etiquetas son de `callOutcomeLabel`).
  </acceptance_criteria>

  <done>
    Existe un modelo puro y testeable de la pantalla de cierre, con la
    jerarquía de D-01 clavada en el código y sin ninguna definición propia de
    métricas.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ciclo de vida de la sesión + pantalla de cierre única + chips de estado</name>
  <files>public/app.js, public/index.html</files>

  <read_first>
    - `public/app.js` líneas 6913-6950 (objeto `_pd` completo)
    - `public/app.js` líneas 7125-7231 (`window._pdStart` entero, incluidos los early-return de cola vacía)
    - `public/app.js` líneas 7278-7311 (`window._pdExit` y la delegación del sidebar)
    - `public/app.js` líneas 7366-7392 (`_pdAdvance`, con el fin de cola a reemplazar)
    - `public/app.js` líneas 7423-7460 (arranque de `_pdRender` y sus expulsiones)
    - `public/app.js` líneas 8110-8135 (handler de teclado del dialer)
    - `public/app.js` líneas 10053-10090 (`_dispoAfterSaved`: el único punto post-guardado, para entender el timing de la disposición contra el cierre)
    - `public/index.html` líneas 1305-1340 (DOM del `#power-dialer`: `pd-current-content`, `pd-queue`, `pd-progress`)
  </read_first>

  <action>
    Trabajar sobre `public/app.js` (y `public/index.html` solo para el
    cache-buster).

    1. **Estado de sesión** — objeto `_pdSession` a nivel del bloque del dialer
       (separado de `_pd` a propósito: `_pd` se resetea entero en `_pdStart` y
       hay tests que cuentan literales sobre él):
       `{ id: null, startedAt: null, opening: null /* promesa */, closing: false,
          payload: null, error: false }`.
       En `_pd` se agrega UN solo flag: `closing: false` (lo consultan
       `_pdRender`, `_pdAdvance` y el teclado).

    2. **`_pdCurrentFilters()`** — arma el `filtro` con el que se armó la cola,
       leyendo los selects que existan en la vista (si un id no está, se omite
       la clave; el servidor sanitiza igual):
       - modo `calls`: `calls-country-filter`, `calls-sort-select`,
         `calls-setter-select`, y `busqueda: '1'|''` según `calls-search` tenga
         texto (el texto NO se manda: es dato del lead, no del filtro).
       - modo `hoy`: `seccion` = `_pd.hoyFilter || 'todas'` y
         `hoy-country-filter`.

    3. **`_pdSessionOpen()`** — POST a `/api/setters/dial-sessions` vía
       `apiUrl()` con `credentials: 'include'`, body
       `{ mode, hoyFilter, filtro, queueSize }`. Guarda la promesa en
       `_pdSession.opening` y, al resolver, `_pdSession.id`/`startedAt`. **Es
       fire-and-forget: nunca bloquea la apertura del dialer ni muestra error
       al SDR** — si falla, `_pdSession.error = true` y se sigue discando (una
       sesión no registrada es un dato perdido; un dialer que no abre es un día
       perdido). Loguear con `console.warn('[ses]', ...)`.
       Se llama desde `window._pdStart` DESPUÉS de todos los early-return (los
       de cola vacía y el toast de "No hay leads cargados"), junto a
       `_pd.active = true` — abrir el dialer y no abrirlo tienen que ser lo
       mismo para la sesión. En el mismo lugar resetear
       `_pd.closing = false` y `_pdSession` a su estado inicial.

    4. **`_pdSessionClose({ reason })`** — async:
       - `await _pdSession.opening` (si el POST de apertura sigue en vuelo, sin
         esto el cierre no tendría id).
       - **Gracia de 250 ms antes del POST** (`await new Promise(r => setTimeout(r, 250))`),
         con comentario explicando por qué: la disposición que el SDR acaba de
         marcar puede estar todavía en vuelo, y el servidor calcula los
         contadores con SU reloj al recibir el cierre — 250 ms alcanzan para
         que esa marca ya esté persistida y entre en el resumen. Es
         imperceptible en una pantalla que el usuario acaba de pedir.
       - Si no hay `_pdSession.id` → devolver `{ error: true }` sin llamar a
         nada.
       - POST a `/api/setters/dial-sessions/${id}/close` con
         `{ processed: _pd.processed }`. Si la respuesta no es ok o tira,
         devolver `{ error: true }`.
       - Devolver el payload del servidor.

    5. **`_pdShowClosing(reason)`** — el único renderizador de cierre:
       - Setea `_pd.closing = true` y `_pdSession.closing = true`, cancela el
         autopiloto, vacía `#pd-queue`.
       - Pinta inmediatamente el estado "calculando" en `#pd-current-content`
         (título según `reason` y un botón **Salir** ya funcional desde el
         primer frame: el SDR nunca queda esperando a la red para poder irse).
       - `const payload = await _pdSessionClose({ reason })`, arma el modelo con
         `_sesClosingModel(payload, { processedLocal: _pd.processed, reason })`
         y re-pinta.
       - HTML: número grande = `model.big` (marcadas, D-01), abajo la fila
         `model.secondary`, después el desglose `model.breakdown` con
         `callOutcomeLabel(outcome)` y `escHtml`, después
         `model.comparison` como una línea ("vs tu sesión anterior: N
         marcadas", con la fecha formateada acá), y al final los chips de
         estado + el botón Salir.
       - Chips de estado (SES-04): 4 botones con `data-mood` `bien` / `normal`
         / `costo` / `pesimo` y etiquetas en castellano ("Bien", "Normal", "Me
         costó", "Pésima"), bajo una pregunta corta. Solo si `model.canMood`.
         **Una sola vez, al cierre, nunca por llamada** (D-03/SES-04). Si el
         SDR no toca ninguno, no pasa nada: el botón Salir cierra igual.
       - Si `model.degraded`, mostrar `model.note` en tono discreto (no un
         error rojo: la sesión se perdió, la jornada no).
       - `#pd-progress` pasa a `'sesión cerrada'`.

    6. **`window._pdSessionMood(mood)`** — PATCH a
       `/api/setters/dial-sessions/${id}` con `{ mood }`, marca visualmente el
       chip elegido y muestra un toast corto. Si falla, toast discreto y nada
       más: **no puede impedir salir**. Re-clickear el mismo chip lo deja como
       está (no hace falta implementar el borrado desde la UI, aunque el
       endpoint lo acepte).

    7. **`window._pdExit` en dos fases** — conservando intacto el confirm de
       llamada activa (BUG-A2) al principio:
       - Si `!_pd.closing` → `_pdShowClosing('salida')` y **return** (el panel
         NO se esconde todavía). Esto vale para los 3 caminos que ya llaman a
         `_pdExit` (botón, Esc, delegación del sidebar) sin tocar ninguno de
         ellos.
       - Si `_pd.closing` → salida real: extraer el cuerpo actual a
         `_pdExitFinal()` (cancelar autopiloto, `_pd.active = false`,
         `_pd.closing = false`, resetear `_pdSession`, esconder
         `#power-dialer`, restaurar `document.body.style.overflow` y refrescar
         la vista de origen — ese refresh explícito se conserva tal cual, ver
         el comentario de DIAL-03 que ya está en el código).
       - Caso navegación por el sidebar: la vista de atrás ya cambió mientras
         se ve la pantalla de cierre; el segundo click (o Salir, o Esc)
         descubre la vista nueva. Dejarlo documentado en un comentario.

    8. **Fin de cola unificado** (SES-02): en `_pdAdvance`, reemplazar el HTML
       de "¡Cola completa!" por `_pdShowClosing('cola_completa')`. La pantalla
       es la misma; solo cambia el título. Ya no se pierde el desglose cuando
       el SDR sí agota la cola.

    9. **Guards** (para no crear bugs nuevos con la pantalla encima):
       - Al principio de `_pdRender` y de `_pdAdvance`: `if (_pd.closing) return;`.
       - En el handler de teclado, inmediatamente después de
         `if (!_pd.active) return;`, agregar — **APROBADO por el user el
         2026-08-21**, tras plantearle la alternativa del overlay aparte:
         se eligió esta línea porque no cambia ningún atajo, solo los
         desactiva cuando no hay tarjeta —
         `if (_pd.closing && e.key !== 'Escape') return;` con un comentario: en
         la pantalla de cierre ya no hay tarjeta, así que 1-9 no pueden marcar
         un resultado sobre el último lead. **Los atajos no cambian de
         significado ni se agregan teclas nuevas** (non-goal explícito de la
         fase): solo se los limita a cuando hay una tarjeta abajo. Esc sigue
         funcionando y en la pantalla de cierre hace la salida real.
       - NO tocar `_pdHold`, `holdCurrent`, `holdMeta`, el autopiloto ni
         `_pdKeyOutcomes`.

    10. **Cache-buster** en `public/index.html`: bumpear `app.js?v=` (baseline
        actual `20260817d`; usar la fecha del día con letra). `style.css?v=` NO
        se toca (este plan no toca CSS: los estilos van inline en el HTML
        generado, como el resto del dialer).
  </action>

  <verify>
    <automated>node --check public/app.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check public/app.js` sale con código 0.
    - `grep -c "_pdShowClosing" public/app.js` es `>= 3` (definición + `_pdExit` + fin de cola).
    - `grep -c "Procesaste " public/app.js` devuelve `0` (el resumen viejo de fin de cola ya no existe).
    - `grep -c "_pd.closing" public/app.js` es `>= 5` (set en `_pdShowClosing`, reset en `_pdStart` y `_pdExitFinal`, guards en `_pdRender`, `_pdAdvance` y teclado).
    - `window._pdSessionMood` y `_pdSessionOpen` existen; `_pdSessionClose` NO se expone en `window` (interna del bloque, mismo criterio que `_pdHold`).
    - En `public/index.html`, `app.js?v=` matchea `/\d{8}[a-z]/` y es estrictamente mayor que `20260817d`; aparece una sola vez; `style.css?v=` sin cambios.
    - `git diff public/app.js` NO toca: `_pdHold`, `_pdKeyOutcomes`, `_pdToggleAutopilot`, `_pdStartAutopilotCountdown`, `_pdTodayStats`, `_pdGetGoal`, `_pdRenderToday`, `_pdBuildQueue`, `_pdBuildQueueHoy`.
    - `git diff --stat package.json package-lock.json` vacío.
  </acceptance_criteria>

  <done>
    Abrir el dialer abre una sesión, salir muestra el marcador de esa sesión y
    recién después cierra el panel, la cola agotada usa la misma pantalla, y el
    estado del operador se puede responder o saltear sin consecuencias.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Suite de SES-02/SES-04 (modelo puro + cableado + cache-buster)</name>
  <files>tests/dial-session-close-ui.test.js</files>

  <behavior>
    - El modelo puro responde lo que dice el `<behavior>` de la Task 1, evaluado con `new Function` sin browser.
    - Las aserciones de fuente prueban que hay UN solo renderizador de cierre y que los 3 caminos de salida pasan por él.
    - El cache-buster tiene forma válida y subió.
  </behavior>

  <read_first>
    - `tests/dial-hold.test.js` completo (molde exacto: `extractFunctionBody`, `countOccurrences`, aserciones de fuente, cache-buster por FORMA nunca pineado a un literal)
    - `tests/dial-history.test.js` líneas 150-215 (evaluación de un bloque marcador con `new Function`)
  </read_first>

  <action>
    Crear `tests/dial-session-close-ui.test.js` (mínimo 20 tests), molde de
    `tests/dial-hold.test.js` (sin jsdom: aserciones de fuente + evaluación del
    bloque puro).

    **Bloque puro** (extraer `[37-03] SESSION-PURE` y evaluarlo con
    `new Function`, como hace `dial-history.test.js`):
    - `_sesDurationLabel`: 0, 59, 60, 95, 3599, 3600, 4320, negativo, `NaN`.
    - `_sesClosingModel` con un payload completo: `big.value === counters.dials`
      (no `connects`, no `processed`) — este test es D-01 escrito en código.
    - `dials: 0` → devuelve modelo mostrable, no null ni excepción.
    - `secondary` en el orden fijo atendieron → conversaciones → agendadas.
    - `breakdown` ordenado desc y `sum(n) === dials`.
    - `breakdown` sin etiquetas propias (los items tienen `outcome`, no `label`).
    - `comparison` con `previous` (diff positivo, negativo y cero) y `null` sin `previous`.
    - `canMood: false` cuando `error` o cuando falta `session.id`.
    - `degraded: true` → `big.value === opts.processedLocal` y hay `note`.
    - `reason: 'cola_completa'` cambia el título y NADA más (mismo `big`, mismo `breakdown`).
    - `payload` `null` / `{}` / `{session:null}` → no tira.

    **Cableado (aserciones de fuente)**
    - `_pdShowClosing` se declara exactamente 1 vez.
    - Dentro del cuerpo de `window._pdExit` aparece `_pdShowClosing` y el early `return`, y el confirm de `_telnyx?.activeCall` sigue estando ANTES.
    - Dentro de `_pdAdvance` aparece `_pdShowClosing('cola_completa')` y ya NO aparece `'¡Cola completa!'` como HTML propio ni `Procesaste `.
    - `_pdRender` y `_pdAdvance` arrancan con el guard `if (_pd.closing) return;`.
    - El handler de teclado tiene el guard de `_pd.closing` DESPUÉS de `if (!_pd.active) return;` y ANTES del bloque de `e.key >= '1'`.
    - `_pdKeyOutcomes` sigue teniendo exactamente los 9 outcomes de siempre, en el mismo orden (regresión: la fase no toca atajos).
    - `_pdSessionOpen` se llama dentro de `window._pdStart` y DESPUÉS del último `return` de cola vacía (verificable por índice de posición dentro del cuerpo extraído).
    - `_pdSessionClose` contiene `await _pdSession.opening` y el `setTimeout` de gracia.
    - Los 3 fetch nuevos usan `apiUrl(` (regla #135/#146: nunca `fetch` crudo en vistas con `viewAs`).
    - `_pdTodayStats` NO aparece dentro de `_pdShowClosing` ni del bloque puro (SES-05: los números de la sesión no salen del chip de meta diaria).
    - Los 4 moods `bien|normal|costo|pesimo` aparecen como `data-mood` en el HTML de la pantalla, y `_pdSessionMood` hace `PATCH`.

    **Cache-buster**
    - `app.js?v=` tiene forma `/\d{8}[a-z]/` y es estrictamente mayor que `20260817d` (nunca pinear el literal nuevo: otra sesión puede bumpearlo por un motivo ajeno).
    - `app.js?v=` aparece una sola vez; `style.css?v=` sigue teniendo forma válida.
  </action>

  <verify>
    <automated>npx vitest run tests/dial-session-close-ui.test.js tests/dial-hold.test.js tests/dial-start-at.test.js tests/gate-destination.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/dial-session-close-ui.test.js` pasa con al menos 20 tests y 0 fallos.
    - `tests/dial-hold.test.js`, `tests/dial-start-at.test.js`, `tests/dial-sync.test.js` y `tests/gate-destination.test.js` siguen verdes SIN editarlos (son los que cuentan literales del dialer: si el cableado rompió alguna invariante del hold/gate, saltan acá).
    - `npm test` completo: 0 fallos nuevos respecto del baseline real.
  </acceptance_criteria>

  <done>
    La pantalla de cierre y su cableado quedan protegidos: si alguien vuelve a
    esconder el resumen detrás de "cola agotada", si el número grande deja de
    ser marcadas, o si las teclas vuelven a marcar sobre la pantalla de cierre,
    la suite falla.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| respuesta del servidor → HTML de la pantalla de cierre | `byOutcome`, `mood` y demás campos se inyectan en el DOM del dialer |
| SDR → cierre del dialer | el cierre no puede depender de que la red ande: si depende, el SDR queda encerrado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-12 | Tampering | inyección al pintar `byOutcome`/`mood`/nombres en la pantalla de cierre | mitigate | Todo lo que viene del servidor se pinta con `escHtml` (helper ya usado en todo el dialer); las etiquetas salen de `callOutcomeLabel`, mapa cerrado |
| T-37-13 | Denial of Service | el SDR queda atrapado en el dialer porque el cierre espera una respuesta que no llega | mitigate | El botón Salir se pinta en el primer frame, antes del POST; si el cierre falla se muestra el modelo degradado y se sale igual |
| T-37-14 | Repudiation | una disposición marcada 100 ms antes de salir no entra en el resumen y el SDR ve un número más bajo del que hizo | mitigate | 250 ms de gracia + el servidor cierra con SU reloj. Límite conocido y aceptado: el número canónico del día sigue siendo el de Mi rendimiento |
| T-37-15 | Tampering | las teclas 1-9 marcando un resultado sobre el último lead mientras se ve la pantalla de cierre | mitigate | Guard `_pd.closing` en el handler de teclado (solo limita el alcance; no cambia ningún atajo) |
| T-37-SC | Tampering | instalación de paquetes npm | mitigate | Este plan no instala nada |
</threat_model>

<verification>
1. `node --check public/app.js`.
2. `npx vitest run tests/dial-session-close-ui.test.js` — suite nueva verde.
3. `npx vitest run tests/dial-hold.test.js tests/dial-start-at.test.js tests/dial-sync.test.js tests/gate-destination.test.js` — sin editarlos.
4. `npm test` completo contra el baseline real.
5. Verificación en preview (login real, sin mocks): abrir el Power Dialer desde Llamadas, salir con Esc → aparece la pantalla de cierre con números; salir de nuevo → cierra y refresca la vista. Repetir desde una sección de Hoy. Confirmar en `DATA_DIR/setters.json` que quedó la sesión con `endedAt`, `counters` y (si se clickeó un chip) `mood`. Consola sin errores nuevos.
6. `git diff --stat package.json package-lock.json` vacío.
</verification>

<success_criteria>
- Siempre hay pantalla de cierre al salir del Power Dialer, con marcadas arriba y el desglose comercial abajo (SES-02, D-01).
- Los números los calcula el servidor desde el CORE; el frontend solo los muestra (SES-05).
- La pregunta de estado se ofrece una sola vez al cierre y saltearla no bloquea nada (SES-04, D-03).
- El hold, el autopiloto y los atajos siguen exactamente como estaban (non-goals de la fase).
- Con el backend caído el SDR ve una pantalla degradada y puede salir.
</success_criteria>

<output>
Crear `.planning/phases/37-ses-sesion-discado/37-03-SUMMARY.md` al terminar.

Documentar explícitamente:
- El baseline real de `npm test` y el cache-buster nuevo.
- Qué se verificó en preview y qué no se pudo (por ejemplo: una llamada Telnyx
  real dentro del dialer no se puede simular).
- El límite conocido de la gracia de 250 ms.
- Qué queda para 37-04 (la tabla de historial en Mi rendimiento: hasta acá el
  SDR ve la sesión que acaba de cerrar, pero todavía no la de ayer).
</output>
