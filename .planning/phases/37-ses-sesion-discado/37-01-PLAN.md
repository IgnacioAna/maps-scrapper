---
phase: 37-ses-sesion-discado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.js
  - tests/dial-session-model.test.js
autonomous: true
requirements: [SES-01, SES-05]
must_haves:
  truths:
    - "Una sesión de discado existe como objeto persistido: se abre al abrir el Power Dialer y se cierra al salir, con startedAt, endedAt, quién marcó, modo, filtro con el que se armó la cola y tamaño de esa cola"
    - "Los contadores de la sesión los calcula el servidor desde el CALL METRICS CORE sobre la ventana de la sesión — el cliente nunca manda un número que termine guardado como contador"
    - "Una sesión que quedó abierta (el SDR cerró la pestaña, recargó, se le cayó el browser) se cierra sola en la próxima apertura, anclada a su última llamada real, y no se traga las horas muertas"
    - "Un usuario sin SDR vinculado NO puede abrir sesión: sin ese guard, los contadores se calcularían sin filtro de setter y una sola sesión se llevaría las llamadas de todo el equipo"
    - "Nadie puede cerrar ni leer la sesión de otro: la sesión se busca siempre dentro del scope del que la pide, nunca por id suelto"
  artifacts:
    - path: "index.js"
      provides: "modelo dialSessions dentro de setters.json + helpers puros + POST /api/setters/dial-sessions (abrir) + POST /api/setters/dial-sessions/:id/close (cerrar)"
      contains: "app.post('/api/setters/dial-sessions'"
    - path: "tests/dial-session-model.test.js"
      provides: "Suite HTTP + pura: apertura/cierre, auto-cierre de huérfanas, RBAC/IDOR, coerción de input, y el invariante de SES-05 (contadores == CALL METRICS CORE sobre la misma ventana)"
  key_links:
    - from: "POST /api/setters/dial-sessions/:id/close"
      to: "_ccCollectCalls + _ccFunnelAggregate"
      via: "los 4 números canónicos de la sesión salen del CORE sobre [startedAt, ahora] filtrado por el setter de la sesión — SES-05"
      pattern: "_ccFunnelAggregate\\(inWindow"
    - from: "data.dialSessions"
      to: "setters.json"
      via: "key más del objeto que loadSettersData/saveSettersData redondean completo — cero wiring de export/pre-deploy/seed/backup (regla #21)"
      pattern: "dialSessions"
    - from: "session.setterId"
      to: "req.auth.user.setterId (el REAL, no el impersonado)"
      via: "las llamadas se atribuyen por callLog[].by → _callSetterId, así que la sesión tiene que pertenecer a quien marca de verdad"
      pattern: "_dialSessionActor"
---

<objective>
Crear la entidad que hoy no existe: `dialSession`. El Power Dialer abre una,
la cierra al salir, y el servidor le pega los contadores derivados del CALL
METRICS CORE sobre la ventana de esa sesión.

Este plan es SOLO backend — el frontend no consume nada de esto todavía (eso
es 37-03). Corre solo y se verifica solo.

Purpose: SES-01. Hoy `_pd` (public/app.js:6915) es estado efímero de cliente
(`queue`, `currentIdx`, `processed`) y `_pdExit()` (app.js:7280) esconde el
panel sin guardar nada — verificado 21/08: grep de `dialSession` /
`dial_session` / `sesionDiscado` en `index.js`, `public/app.js` y `src/` da
cero resultados. Sin objeto persistido no hay pantalla de cierre confiable
(37-03) ni historial (37-02/37-04).

Output: `data.dialSessions` dentro de `setters.json`, los helpers puros, y
los 2 endpoints del ciclo de vida (abrir / cerrar) con su suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-ses-sesion-discado/CONTEXT.md
@CLAUDE.md

Precedente directo de persistencia sin archivo nuevo (una key más dentro de
`setters.json`), con su RBAC por scope y su poda:
@.planning/phases/34-hoy-vista-diaria/34-01-PLAN.md
</context>

<interfaces>
<!-- Contratos vigentes. Leer los rangos de <read_first> antes de editar. -->

CALL METRICS CORE (index.js ~8156-8292, expuesto en `globalThis.__callCore`).
Definiciones canónicas — **no se re-implementan, se llaman**:
```js
// Aplana todos los callLog en entries pre-atribuidas por quién llamó
// (callLog[].by → _callSetterId), nunca por dueño actual del lead.
function _ccCollectCalls(data, { setterId = '', visibleSet = null, channel = '' } = {}) { ... }
// → [{ ts, outcome, duration, setterId, leadId, disqualifyReason }]
// ⚠️ setterId:'' = SIN FILTRO (devuelve las llamadas de TODO el equipo).

// Única definición del funnel. Filtra por [fromTs, toTs) internamente.
function _ccFunnelAggregate(calls, calendar, fromTs, toTs, { setterId = '', visibleSet = null } = {}) { ... }
// → { dials, connects, conversations, appointments, deals, revenue,
//     totalDurationS, byReason, avgConvDurationS, rates:{...} }
//   connect      = COLD_CALL_CONNECT_OUTCOMES (incluye hung_up y callback_later)
//   conversation = connect && (duration >= COLD_CALL_CONV_MIN_S || appointment)
//   appointment  = COLD_CALL_APPOINTMENT_OUTCOMES
```

Persistencia de `setters.json` (index.js ~7455-7495). Handler 100% SYNC no
necesita `mutateSettersData` (regla #19: el mutex es para handlers con `await`
entre el load y el save):
```js
function loadSettersData() { ... }   // devuelve el objeto completo (cacheado por mtime)
function saveSettersData(data) { ... }
async function mutateSettersData(mutator) { ... } // solo si hay await entre load y save
```

Auth/scope ya establecido (index.js ~558-576 y el patrón de `cold-call-metrics`
~8300-8325):
```js
function getEffectiveAuth(req) { ... } // { role, setterId, isImpersonating, visibleSetterIds }
function _visibleSetterIds(user) { ... } // Set | null (null = sin restricción)
```

Helpers de fecha en TZ de negocio (index.js ~7872-7886, `globalThis.__metricsAudit`):
```js
function _bizStartOfDay(ts = Date.now()) { ... }
function _bizDayStr(ts) { ... } // 'YYYY-MM-DD'
```

**Contrato que este plan crea** (lo consume 37-03; 37-02 agrega lectura y estado):

```
POST /api/setters/dial-sessions            → abrir
  body: { mode: 'calls'|'hoy', hoyFilter: string|null, filtro: {…}, queueSize: number }
  200 → { session: { id, setterId, by, startedAt, endedAt:null, mode, hoyFilter,
                     filtro, queueSize, processed:0, mood:'', closedBy:'' } }
  400 → { error } si el usuario no tiene SDR vinculado
  403 → { error } si el rol no puede discar

POST /api/setters/dial-sessions/:id/close  → cerrar
  body: { processed?: number }
  200 → { session: {…, endedAt, durationS, closedBy:'user', counters:{…} },
          previous: { id, startedAt, endedAt, durationS, counters:{…} } | null,
          alreadyClosed?: true }
  404 → { error } si el id no existe DENTRO del scope del que pide

counters (claves en inglés, espejo EXACTO del CORE — cualquier renombre acá
sería un lugar donde los números se pueden separar del canon):
  { dials, connects, conversations, appointments, deals,
    totalDurationS, avgConvDurationS, leads, byOutcome: { outcome: n } }
  invariante: sum(byOutcome) === dials
```

Colisión de nombres a evitar (verificado 21/08): `data.sessions` YA EXISTE en
`setters.json` — son las "sesiones de setteo" de la era WhatsApp
(`{id, setter, startedAt, endedAt, summary, aiSummary}`, endpoints
`/api/setters/sessions/start|end`, index.js ~14679-14800, métricas derivadas de
`interactions[]`, con llamada a la IA y todo). Está viva pero colgada de
`view-crm`, que está PARKEADA, y tiene 1 registro de abril de un setter que ya
no existe. **No se toca, no se reusa, no se extiende.** La entidad nueva es
`data.dialSessions` y sus rutas son `/api/setters/dial-sessions`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Modelo dialSessions + abrir y cerrar sesión</name>
  <files>index.js</files>

  <read_first>
    - `index.js` líneas 8130-8292 (CALL METRICS CORE completo: comentario de las definiciones canónicas, `_ccCollectCalls`, `_ccFunnelAggregate`, `_ccResolveRange`, `globalThis.__callCore`)
    - `index.js` líneas 8300-8430 (`cold-call-metrics`: el patrón de scope/RBAC y dónde termina)
    - `index.js` líneas 8429-8515 (`_hoyHygieneScope` + `POST /hoy-hygiene-snapshot`: el precedente de persistir dentro de `setters.json` con poda, coerción de input y guard de setter mal configurado)
    - `index.js` líneas 550-590 (`getEffectiveAuth`, `ensureSetterProfile`)
    - `index.js` líneas 7455-7500 (`loadSettersData` final, `saveSettersData`, `mutateSettersData` y su comentario sobre cuándo NO hace falta)
    - `index.js` líneas 14679-14800 (las sesiones de setteo LEGACY — leerlas para NO confundirlas ni tocarlas)
    - `index.js` líneas 7420-7440 (dónde `loadSettersData` normaliza `raw.sessions` — este plan NO agrega normalización ahí, ver acción)
  </read_first>

  <action>
    Trabajar SOLO sobre `index.js`. Todo el bloque nuevo va inmediatamente
    después del endpoint `POST /api/setters/hoy-hygiene-snapshot` (~línea 8515),
    antes de `app.get('/api/setters/team/:id/calls-today', ...)`, con un
    comentario de cabecera que diga: Fase 37 (SES-01/SES-05), qué es una sesión
    de discado y por qué NO es `data.sessions`.

    1. **Constantes de módulo**, junto al bloque nuevo:
       `DIAL_SESSION_MODES` (Set: `calls`, `hoy`), `DIAL_SESSION_HOY_FILTERS`
       (Set: `compromisos`, `esperando`, `callbacks`, `interesados`,
       `reintentos` — los 5 sub-filtros que ya maneja `_pdBuildQueueHoy`),
       `DIAL_SESSION_CAP_PER_SETTER = 90` (poda; ~3 meses a una sesión por
       día) y `DIAL_SESSION_MAX_FILTER_KEYS = 8`.

    2. **`_dialSessionActor(req)`** — quién es el dueño de la sesión que se
       escribe. Devuelve `{ setterId, userId, error }`.
       - Rol permitido: `admin`, `supervisor` o `setter` según
         `getEffectiveAuth(req).role`; si no, `error: 'rol'`.
       - `setterId` sale SIEMPRE de `req.auth.user.setterId` — el setter REAL
         del usuario logueado, NUNCA `eff.setterId`. Motivo (documentarlo en
         un comentario): las llamadas se atribuyen por `callLog[].by` →
         `_callSetterId`, o sea al usuario real. Si un admin discando en modo
         "Ver como SDR · Paula" abriera la sesión como `setter_paula`, los
         contadores derivados del CORE darían 0 (sus llamadas se atribuyen a
         SU setter). La sesión pertenece a quien marca.
       - Si `setterId` queda vacío → `error: 'sin_setter'`. Este guard es
         crítico y no es cosmético: `_ccCollectCalls(data, { setterId: '' })`
         NO filtra nada y devolvería las llamadas de todo el equipo — una
         sesión sin setter se llevaría los números de todos.

    3. **`_dialSessionSanitize(body)`** — pura, whitelist-and-coerce (mismo
       idioma que el resto del repo: un body raro nunca tira 4xx, se clampea).
       Devuelve `{ mode, hoyFilter, filtro, queueSize }`:
       - `mode`: el valor si está en `DIAL_SESSION_MODES`, si no `'calls'`.
       - `hoyFilter`: el valor si está en `DIAL_SESSION_HOY_FILTERS`, si no
         `null`.
       - `queueSize`: entero, mínimo 0, tope 100000.
       - `filtro`: objeto de contexto (con qué filtros se armó la cola). Se
         copian como mucho `DIAL_SESSION_MAX_FILTER_KEYS` claves propias, cada
         clave recortada a 24 caracteres y cada valor coercionado con `String()`
         y recortado a 60. Nada de objetos anidados, arrays ni funciones. Si el
         body no manda objeto, `{}`.

    4. **`_dialSessionCounters(data, session, endTs)`** — el ÚNICO lugar donde
       se calculan números de sesión (SES-05). Pasos exactos:
       - Si `!session.setterId`, devolver todos los contadores en 0 con
         `byOutcome` vacío (defensa en profundidad del guard del punto 2).
       - `fromTs` = `startedAt` en ms; si `endTs <= fromTs`, usar `fromTs`.
       - `calls` = `_ccCollectCalls(data, { setterId: session.setterId })`.
       - `inWindow` = las entries con `ts >= fromTs && ts < toTs`.
       - `agg` = `_ccFunnelAggregate(inWindow, data.calendar, fromTs, toTs,
         { setterId: session.setterId })` — pasarle la lista YA filtrada es un
         no-op (el agregado re-aplica el mismo predicado), y garantiza que
         `byOutcome` se tallee EXACTAMENTE sobre las entries que el agregado
         contó como `dials`.
       - `byOutcome`: conteo crudo del campo `outcome` de `inWindow`. Es un
         tally sin clasificación propia: cero sets de outcomes, cero umbrales
         de duración. Toda interpretación (qué es "atendida", qué es
         "conversación") viene de `agg`.
       - `leads`: cantidad de `leadId` distintos en `inWindow`.
       - Devolver las claves del contrato de `<interfaces>` tomando
         `dials/connects/conversations/appointments/deals/totalDurationS/
         avgConvDurationS` directo de `agg`, sin recalcular ni renombrar.

    5. **`_dialSessionPrune(list, setterId)`** — pura. Deja como mucho
       `DIAL_SESSION_CAP_PER_SETTER` sesiones de ese setter (las más recientes
       por `startedAt`), sin tocar las de los demás. Devuelve la lista podada.

    6. **`_dialSessionFinalize(data, session, { closedBy, endTs, processed })`**
       — pura sobre `data`, sin I/O. Setea `endedAt` (ISO), `durationS`
       (entero, `>= 0`), `closedBy`, `processed` (entero `>= 0` si vino) y
       `counters` = `_dialSessionCounters(data, session, endTs)`. La usan el
       cierre explícito y el auto-cierre.

    7. **`POST /api/setters/dial-sessions`** (abrir), `requireAuth`, handler
       100% SYNC:
       - `_dialSessionActor(req)`; `error: 'rol'` → 403; `error: 'sin_setter'`
         → 400 con mensaje explícito ("Tu cuenta no tiene un SDR vinculado: no
         se puede registrar la sesión de discado.").
       - `data = loadSettersData()`; `data.dialSessions` se crea lazy si no es
         array (NO agregar normalización en `loadSettersData` — es hot path y
         no hace falta: se inicializa en el handler, igual que
         `hoyHygieneSnapshots`).
       - **Auto-cierre de huérfanas**: toda sesión de ESE setter con
         `!endedAt` se finaliza antes de crear la nueva, con
         `closedBy: 'auto'` y `endTs` = el `ts` de su ÚLTIMA llamada dentro de
         `[startedAt, ahora]` (usando `_ccCollectCalls` una sola vez para todo
         el handler), o `startedAt` si no hubo ninguna. Motivo a documentar:
         anclar a la última llamada evita que una pestaña cerrada el viernes
         convierta la sesión en una de 60 horas.
       - Crear la sesión: `id` = `dsess_${Date.now()}_${random base36 de 6}`
         (patrón del repo), `setterId`, `by` = userId, `startedAt` = reloj del
         SERVIDOR (nunca el del cliente — ver amenaza T-37-03), `endedAt: null`,
         `mode`, `hoyFilter`, `filtro`, `queueSize`, `processed: 0`, `mood: ''`,
         `closedBy: ''`, `counters: null`.
       - Push, `_dialSessionPrune`, `saveSettersData`, responder `{ session }`.

    8. **`POST /api/setters/dial-sessions/:id/close`** (cerrar), `requireAuth`,
       handler 100% SYNC:
       - Mismo `_dialSessionActor` (403/400 igual).
       - Buscar la sesión por `id` **y** `setterId` del actor en el mismo
         `find` — nunca por id suelto (T-37-01). Si no aparece → 404.
       - Si ya tiene `endedAt`: NO recalcular nada; responder la sesión
         guardada con `alreadyClosed: true` (idempotencia — dos pestañas, o un
         reintento de red, no pueden reescribir el resultado ya mostrado).
       - Si está abierta: `_dialSessionFinalize` con `closedBy: 'user'`,
         `endTs = Date.now()` (reloj del servidor) y el `processed` coercionado
         del body.
       - `previous`: la última sesión CERRADA de ese setter, distinta de la
         actual, con `counters.dials > 0`, ordenada por `startedAt` — o `null`.
         Se devuelve proyectada (`id, startedAt, endedAt, durationS, counters`),
         no el objeto entero.
       - `saveSettersData` y responder `{ session, previous }`.

    9. Exponer los puros en `globalThis.__dialSessions`
       (`_dialSessionSanitize`, `_dialSessionCounters`, `_dialSessionPrune`,
       `_dialSessionFinalize`), patrón `globalThis.__callCore` /
       `globalThis.__hoyHygiene`, para que la Task 2 los pueda testear aislados.

    NO tocar: `_ccCollectCalls`, `_ccFunnelAggregate`, `_ccResolveRange` ni
    ninguna otra función del CORE (agregarles un campo cambiaría el archivo más
    protegido del repo sin necesidad: `byOutcome` se calcula afuera, sobre las
    mismas entries). NO tocar `data.sessions` ni sus 3 endpoints legacy. NO
    tocar `loadSettersData`.
  </action>

  <verify>
    <automated>node --check index.js</automated>
  </verify>

  <acceptance_criteria>
    - `node --check index.js` sale con código 0.
    - `grep -c "app.post('/api/setters/dial-sessions'" index.js` devuelve `1` y `grep -c "/api/setters/dial-sessions/:id/close" index.js` devuelve `1`.
    - `grep -c "globalThis.__dialSessions" index.js` devuelve `1`.
    - Dentro del cuerpo de `_dialSessionCounters` aparecen `_ccCollectCalls` y `_ccFunnelAggregate`, y NO aparece ninguno de estos literales: `COLD_CALL_CONNECT_OUTCOMES`, `COLD_CALL_CONV_MIN_S`, `COLD_CALL_APPOINTMENT_OUTCOMES` (los umbrales y sets viven en el CORE, no acá).
    - `git diff index.js` no toca las líneas de `_ccCollectCalls`/`_ccFunnelAggregate`/`_ccResolveRange`/`_ccFunnelSeries` ni las de `/api/setters/sessions/start|end`.
    - Ningún handler nuevo usa `await` (por lo tanto ninguno usa `mutateSettersData`): `grep -c "await" ` sobre los 2 cuerpos nuevos devuelve `0`.
    - `git diff --stat package.json package-lock.json` vacío.
  </acceptance_criteria>

  <done>
    Abrir el Power Dialer puede crear una sesión y salir puede cerrarla con
    contadores derivados del CORE. Una sesión huérfana se cierra sola en la
    próxima apertura. Nadie sin SDR vinculado abre sesión, y nadie cierra la
    de otro.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Suite de SES-01/SES-05 (modelo, ciclo de vida y el invariante contra el CORE)</name>
  <files>tests/dial-session-model.test.js</files>

  <behavior>
    - Abrir → la sesión existe con `startedAt` del servidor, `setterId` del usuario real y `queueSize` del body coercionado.
    - Cerrar → `counters.dials` == `_ccFunnelAggregate` sobre la MISMA ventana; `sum(byOutcome) === dials`.
    - Cerrar dos veces → la segunda no cambia los números (`alreadyClosed`).
    - Un SDR no puede cerrar la sesión de otro (404, no 403 mudo ni éxito).
    - Un usuario sin `setterId` no abre sesión (400) y no queda basura en `data.dialSessions`.
    - Sesión huérfana → se cierra sola al abrir la siguiente, anclada a la última llamada.
  </behavior>

  <read_first>
    - `tests/hoy-hygiene-snapshot.test.js` completo (molde de suite HTTP de esta familia: fixture inline en `tmpData`, cookies, RBAC por scope, lectura del `setters.json` de disco para verificar side-effects)
    - `tests/metrics-consistency.test.js` líneas 1-60 y 200-240 (cómo se accede a `globalThis.__callCore` desde un test y cómo se comparan números entre endpoints)
    - `tests/hangup-cap.test.js` líneas 1-60 (teléfonos de 7+ dígitos en fixtures — nota #163 — y el patrón `process.env.X_API_KEY = ""`, nunca `delete`, nota #121)
  </read_first>

  <action>
    Crear `tests/dial-session-model.test.js` (mínimo 20 tests). Fixture inline
    en `tmpData` con: un admin, dos SDRs (`s_a` con user `u_a`, `s_b` con user
    `u_b`), un usuario `setter` SIN `setterId` (cuenta mal configurada), un
    supervisor scoped que solo ve a `s_a`, y leads con `callLog` de outcomes
    variados repartidos en el tiempo (algunos DENTRO de la ventana que va a
    tener la sesión, otros claramente antes y después; al menos uno de `s_b`
    para probar que no se cuela).

    Cobertura obligatoria:

    **Apertura**
    - 200 con `session.id`, `setterId` del usuario, `startedAt` ISO, `endedAt: null`, `counters: null`.
    - `startedAt` lo pone el servidor: mandar `startedAt` en el body con una fecha de 2020 y verificar que la sesión NO lo toma.
    - `mode` fuera de la whitelist → `'calls'`; `hoyFilter` inventado → `null`; `queueSize: -3` o `'muchos'` → `0`.
    - `filtro` con 30 claves, valores largos y un objeto anidado → como mucho 8 claves, todas strings recortadas, sin anidados.
    - Usuario `setter` sin `setterId` → 400 y `data.dialSessions` sigue sin ninguna sesión suya (leer el archivo de `tmpData`).
    - Sin auth → 401/403 (patrón estándar del repo).

    **Cierre y SES-05 (el invariante que justifica la fase)**
    - Con llamadas del SDR dentro de la ventana: `counters.dials/connects/conversations/appointments` son EXACTAMENTE los que devuelve `globalThis.__callCore._ccFunnelAggregate` corriendo en el test sobre `_ccCollectCalls(data, {setterId})` con `fromTs = startedAt` y `toTs = endedAt`. Este test es la garantía de SES-05: si alguien re-implementa el funnel inline, falla.
    - `sum(Object.values(byOutcome)) === counters.dials`.
    - Una llamada de `s_b` en la misma ventana NO entra en los contadores de la sesión de `s_a` (atribución por quién llamó).
    - Una llamada del MISMO SDR anterior a `startedAt` no entra.
    - `durationS` coherente con `endedAt - startedAt` (tolerancia de segundos).
    - Cerrar dos veces: la segunda responde `alreadyClosed: true` y los `counters` no cambian aunque en el medio se agregue una llamada nueva al fixture.
    - `previous`: la primera sesión cerrada del día devuelve `previous: null`; la segunda devuelve la primera (con `counters`), y una sesión sin llamadas NO se ofrece como `previous`.

    **RBAC / IDOR**
    - `u_b` cerrando la sesión de `u_a` (id válido, dueño distinto) → 404 y la sesión de `u_a` sigue abierta.
    - `:id` inexistente → 404.
    - Usuario sin `setterId` intentando cerrar → 400.

    **Auto-cierre de huérfanas**
    - Sembrar en el fixture una sesión abierta de `s_a` con `startedAt` de hace 3 horas y llamadas suyas hasta hace 2 horas. Abrir una nueva → la vieja quedó con `endedAt` ≈ el `ts` de esa última llamada, `closedBy: 'auto'` y `counters` calculados; la nueva arranca limpia.
    - Sesión huérfana SIN ninguna llamada → `endedAt === startedAt`, `durationS: 0`, `counters.dials: 0` (no se traga las horas muertas).
    - Nunca quedan 2 sesiones abiertas del mismo setter después de abrir.

    **Poda**
    - Sembrar 95 sesiones cerradas de `s_a` y 3 de `s_b`; abrir una de `s_a` → `s_a` queda con `DIAL_SESSION_CAP_PER_SETTER` como máximo y las 3 de `s_b` intactas.

    **Puros** (vía `globalThis.__dialSessions`)
    - `_dialSessionSanitize` con `null`/`undefined`/string → no revienta y devuelve defaults.
    - `_dialSessionCounters` con `setterId: ''` → todo en 0 (no devuelve las llamadas del equipo).
  </action>

  <verify>
    <automated>npx vitest run tests/dial-session-model.test.js tests/metrics-consistency.test.js</automated>
  </verify>

  <acceptance_criteria>
    - `npx vitest run tests/dial-session-model.test.js` pasa con al menos 20 tests y 0 fallos.
    - `tests/metrics-consistency.test.js` sigue en 18/18 SIN editarlo (`git diff --stat tests/metrics-consistency.test.js` vacío).
    - `npm test` completo: 0 fallos nuevos respecto del baseline REAL leído en disco antes de empezar (documentarlo en el SUMMARY, no asumir un número).
  </acceptance_criteria>

  <done>
    El modelo de sesión queda protegido por tests permanentes, incluido el
    invariante de SES-05 (los contadores de la sesión y el CALL METRICS CORE
    dan el mismo número sobre la misma ventana) y el guard del usuario sin SDR
    vinculado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → `POST /dial-sessions` | body `{mode, hoyFilter, filtro, queueSize}` es input NO confiable |
| cliente → `POST /dial-sessions/:id/close` | el `:id` es input NO confiable: apunta a un objeto de otro usuario si no se acota el scope |
| `data.dialSessions` → pantalla de cierre e historial (37-02/37-03/37-04) | lo que se guarde acá es lo que después se muestra como "tu resultado" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Elevation of Privilege | `:id` de otro SDR en el cierre (IDOR) | mitigate | La sesión se busca con `id` **y** `setterId` del actor en el mismo `find`; si no matchea, 404 |
| T-37-02 | Tampering | contadores inflados desde el cliente | mitigate | Los contadores NUNCA vienen del body: se derivan server-side del `callLog` vía CORE. Del cliente solo entra `processed` (informativo, separado de `counters`) |
| T-37-03 | Tampering | `startedAt`/`endedAt` del cliente para agrandar la ventana y absorber llamadas de otro rango | mitigate | Ambos los pone el reloj del servidor; el body no tiene forma de moverlos |
| T-37-04 | Elevation of Privilege | usuario sin `setterId` abriendo sesión → `_ccCollectCalls` sin filtro devuelve las llamadas de TODO el equipo | mitigate | Guard 400 en `_dialSessionActor` + defensa en profundidad: `_dialSessionCounters` devuelve 0 si `setterId` está vacío |
| T-37-05 | Tampering | `filtro` como vector de basura (objetos anidados, claves infinitas, strings gigantes) inflando `setters.json` | mitigate | Whitelist-and-coerce: ≤8 claves, claves ≤24 chars, valores `String()` ≤60 chars, sin anidados |
| T-37-06 | Denial of Service | crecimiento sin fin de `dialSessions` (una sesión por apertura del dialer) | mitigate | `_dialSessionPrune` a `DIAL_SESSION_CAP_PER_SETTER` por setter en cada apertura |
| T-37-07 | Repudiation | dos pestañas cerrando la misma sesión y reescribiendo el resultado ya mostrado | mitigate | Cierre idempotente: con `endedAt` ya seteado no se recalcula, se devuelve lo guardado con `alreadyClosed` |
| T-37-SC | Tampering | instalación de paquetes npm | mitigate | Este plan no instala nada; `git diff --stat package.json package-lock.json` vacío al cerrar |
</threat_model>

<verification>
1. `node --check index.js`.
2. `npx vitest run tests/dial-session-model.test.js` — suite nueva verde.
3. `npx vitest run tests/metrics-consistency.test.js` — 18/18 sin editarlo.
4. `npm test` completo contra el baseline real leído en disco.
5. `git diff --stat package.json package-lock.json` vacío.
</verification>

<success_criteria>
- Existe `dialSession` persistida con `startedAt`, `endedAt`, `by`, `setterId`, `mode`, `hoyFilter`, `filtro`, `queueSize` y contadores por resultado (SES-01).
- Los contadores salen del CALL METRICS CORE sobre la ventana de la sesión, con un test que lo compara número por número (SES-05).
- Cero archivos de datos nuevos: todo vive dentro de `setters.json` (regla #21 satisfecha por construcción).
- Una sesión huérfana se cierra sola, anclada a su última llamada real.
- IDOR cerrado: la sesión de otro SDR no se puede cerrar ni leer por id.
</success_criteria>

<output>
Crear `.planning/phases/37-ses-sesion-discado/37-01-SUMMARY.md` al terminar.

Documentar explícitamente:
- El baseline REAL de `npm test` leído en disco antes de empezar.
- La decisión de persistencia con su fundamento (key en `setters.json` vs
  archivo nuevo) y por qué `data.sessions` legacy no se reusó.
- Qué queda para 37-02 (lectura + estado del operador) y 37-03 (el frontend
  todavía NO abre ni cierra ninguna sesión: los endpoints existen sin
  consumidor).
</output>
