---
phase: 24-integracion-backend-retell
plan: 05
type: execute
wave: 5
depends_on: ["24-04"]
files_modified:
  - index.js
  - tests/retell-webhook-process.test.js
autonomous: true
requirements: [VOICE-05]

must_haves:
  truths:
    - "D-24-08: el transcript de Retell se guarda en `logEntry.transcript.segments` con el shape de Whisper (`speaker:'setter'|'lead'`, `start`, `end`, `text`) y aparece en la biblioteca de Entrenamiento IA sin tocar una línea de esa vista"
    - "D-24-07: el logEntry lleva `by:''` y `channel:'retell'` → `_callSetterId` cae a `assignedTo` = `setter_agente_ia` y la llamada entra en funnel/Comando/Equipo y queda FUERA de Centralita, sin código de métricas nuevo"
    - "D-24-05: el webhook es el ÚNICO que escribe callLog para una llamada del agente, aplica la cascada con `_applyCallOutcome` dentro de `mutateSettersData`, y usa `skipCalendarCreation` cuando `/book` ya creó la cita"
    - "D-24-05: la extracción de Retell persiste donde corresponde — `nota_seguimiento` en notes[] firmada 'Agente IA', `callback_fecha_hora` validado (futuro, ≤90 días) en callbackAt, `doctor_name` en lead.doctor solo si estaba vacío"
    - "Research §2.3: el webhook maneja `call_ended` Y `call_analyzed` — las llamadas que nunca conectaron se resuelven sin esperar un análisis que puede no llegar"
    - "Un reintento de Retell (3 intentos, research §5.3) nunca duplica el callLog: la idempotencia por `retellCallId` se chequea y se escribe dentro del MISMO mutator"
  artifacts:
    - path: "index.js"
      provides: "Mapeo de transcript, tabla de `disconnection_reason`, decisión de outcome, pipeline de procesamiento del webhook"
      contains: "RETELL_DISCONNECT_OUTCOME"
    - path: "tests/retell-webhook-process.test.js"
      provides: "End-to-end del success criterion 1 + idempotencia + los 3 caminos de outcome + extracción"
      min_lines: 260
  key_links:
    - from: "_retellProcessCallEvent"
      to: "_applyCallOutcome (creado en 24-01)"
      via: "llamada dentro de mutateSettersData con opts.actorSetterId = 'setter_agente_ia'"
      pattern: "_applyCallOutcome\\("
    - from: "call.transcript_object"
      to: "logEntry.transcript.segments"
      via: "mapeo agent→setter / user→lead con start/end derivados de words[]"
      pattern: "transcript_object"
    - from: "_pendingBooked (creado en 24-04)"
      to: "outcome scheduled_with_admin con skipCalendarCreation"
      via: "el webhook no vuelve a crear la cita que /book ya creó"
      pattern: "skipCalendarCreation"
---

<objective>
El cierre del circuito: convertir un evento de Retell en exactamente la misma
huella que deja una llamada de una SDR humana — entry de callLog con transcript,
outcome canónico aplicado con toda la cascada, y los datos que el agente sacó de
la conversación en su lugar.

Propósito: es el requisito que define la fase. Si esto funciona, el agente es una
SDR más en Equipo, en el Comando, en el funnel y en la biblioteca de
Entrenamiento IA, sin una sola línea de métricas nueva.

Output: helpers puros de mapeo/decisión + `_retellProcessCallEvent` enganchado al
webhook de 24-04 + tests end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-integracion-backend-retell/24-CONTEXT.md
@.planning/phases/24-integracion-backend-retell/24-RESEARCH.md
@CLAUDE.md
@.planning/phases/24-integracion-backend-retell/24-01-SUMMARY.md
@.planning/phases/24-integracion-backend-retell/24-04-SUMMARY.md

Reglas duras:
- **Regla #19**: TODA escritura de este plan va por `mutateSettersData`. El
  chequeo de idempotencia y el push del logEntry ocurren dentro del MISMO
  mutator — si se separan, dos reintentos concurrentes de Retell pasan los dos.
- **Regla del milestone**: cero circuito paralelo de métricas. El outcome se
  aplica con `_applyCallOutcome`, jamás seteando campos del lead a mano.
- **Regla #9**: `data.leads` es MAP, `data.calendar` es ARRAY.
- **Regla #163**: fixtures con teléfonos de ≥7 dígitos.
- **Cache-buster**: backend puro.

### Los tres unknowns del research que este plan resuelve por diseño, no por supuesto

1. **El mapeo `disconnection_reason` → outcome es ASSUMED** (research §2.3/§6.2:
   propuesta razonada, no verificada contra llamadas reales). Tratamiento: se
   implementa como **una tabla explícita de módulo**, no como lógica dispersa; el
   `disconnection_reason` crudo se persiste en el logEntry para poder auditar el
   mapeo con datos reales; los valores desconocidos se loguean con `console.warn`
   y caen a un default conservador. La revisión con datos reales es un ítem del
   piloto de Phase 26, anotado en el SUMMARY como deferred — no un supuesto
   escondido en el código.
2. **`call_analyzed` puede no llegar, o llegar con `call_analysis` vacío**
   (research §2.3/§6.3, evidencia community). Tratamiento: el pipeline maneja los
   DOS eventos y además tiene una red de seguridad temporizada (abajo) — funciona
   igual en los dos escenarios, sin necesidad de confirmar cuál es.
3. **Cuál API key tiene el "webhook badge"** (research §6.1): resuelto por el
   fallback `webhookSecret → apiKey` que armó 24-02. La confirmación contra el
   dashboard real es un ítem de Phase 26.
</context>

<interfaces>
<!-- Extraído del código real (HEAD d973e9f) y del research §2.3/§2.4/§5.3. -->

Helpers a REUSAR (todos existen ya):
- `_applyCallOutcome(data, lead, logEntry, opts)` — creado en 24-01. Recibe el
  `logEntry` SIN pushear (lo pushea la función). `opts`: `leadId`, `outcome`,
  `nowIso`, `callbackAt`, `callbackShared`, `scheduled`, `cleanReason`,
  `doNotCall`, `actorSetterId`, `actorName`, `skipCalendarCreation`.
- `_estimateTelnyxCost(destinationPhone, durationSecs)` — hoisteado en 24-01.
  Devuelve `{cost, country, tariffKey, source}`.
- `_autoDispositionLLM(segments)` — index.js:16232-16264. Recibe segments con
  `{speaker, text}`, devuelve `{outcome, reason}` o `null`. Timeout interno de
  15s, whitelist `_AUTO_DISP_OUTCOMES` (index.js:16231).
- `mutateSettersData(mutator)` — index.js:6703-6713.
- `_pendingBooked`, `_pendingRetellCalls` — creados en 24-04 y 24-03.
- `VOICE_AGENT_SETTER_ID` — creado en 24-02.
- `CALL_OUTCOMES` — index.js:10272-10283, la whitelist canónica.

Shape del logEntry que produce el dialer humano, a espejar — index.js:10480-10515:
`{ts, outcome, by, notes, channel, duration, fromNumber, cost, costCountry,
costTariffKey}`. El transcript se guarda aparte, como `entry.transcript`
(index.js:16725-16732): `{segments, transcribedAt, whisperModel, language}`.

Shape del transcript de Retell (research §2.4): `transcript_object` es un array
de **turnos**, `{role:'agent'|'user'|'transfer_target', content, words:[{word,start,end}]}`.
**No hay `start`/`end` a nivel del turno** — se derivan de `words[0].start` y
`words[words.length-1].end`, con `0` de fallback.

Shape de `call_analysis` (solo en `call_analyzed`): `{call_summary, in_voicemail,
user_sentiment, call_successful, custom_analysis_data}`. `custom_analysis_data`
trae las claves que Phase 26 configure en la Post Call Data Extraction: `atendio`,
`doctor_name`, `recepcionista_nombre`, `interes`, `objecion_principal`,
`callback_fecha_hora`, `email`, `agendo`, `nota_seguimiento`.

Catálogo completo de `disconnection_reason` (research §2.3, 32 valores):
`user_hangup` · `agent_hangup` · `call_transfer` · `voicemail_reached` ·
`ivr_reached` · `inactivity` · `max_duration_reached` · `concurrency_limit_reached` ·
`no_concurrency_fallback` · `no_valid_payment` · `scam_detected` · `dial_busy` ·
`dial_failed` · `dial_no_answer` · `invalid_destination` ·
`telephony_provider_permission_denied` · `telephony_provider_unavailable` ·
`sip_routing_error` · `marked_as_spam` · `user_declined` ·
`error_llm_websocket_open` · `error_llm_websocket_lost_connection` ·
`error_llm_websocket_runtime` · `error_llm_websocket_corrupt_payload` ·
`error_no_audio_received` · `error_asr` · `error_retell` · `error_unknown` ·
`error_user_not_joined` · `registered_call_timeout` · `transfer_bridged` ·
`transfer_cancelled` · `manual_stopped` · `call_take_over`

Shape de la nota — index.js:10023: `{id, text, by, date}`, cap 100 (index.js:10025).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Helpers puros — transcript, tabla de disconnection, decisión de outcome</name>
  <files>index.js</files>
  <read_first>
    - index.js:16700-16745 — cómo se persiste hoy un transcript (`lead.callLog[idx].transcript = {segments, transcribedAt, whisperModel, language}`) y qué consume la biblioteca
    - index.js:16228-16264 — `_autoDispositionLLM` y su whitelist `_AUTO_DISP_OUTCOMES`
    - index.js:10272-10283 — `CALL_OUTCOMES`, la whitelist canónica de outcomes
    - .planning/phases/24-integracion-backend-retell/24-RESEARCH.md §2.3 (catálogo + propuesta de mapeo) y §2.4 (shape del transcript)
  </read_first>
  <action>
Crear tres helpers puros en scope de módulo, junto al bloque de Retell, y
exponerlos en `globalThis.__voiceAgent` (para poder testearlos sin levantar el
webhook entero).

**`_retellTranscriptToSegments(call)`** (D-24-08). Mapea `call.transcript_object`
al shape de Whisper: `role === 'agent'` → `speaker: 'setter'`, cualquier otro
(`user`, y `transfer_target` que no aplica en v1) → `speaker: 'lead'`;
`start` = `words?.[0]?.start ?? 0`; `end` = `words?.[words.length-1]?.end ?? 0`;
`text` = `content || ''`. Descartar turnos sin texto. Si `transcript_object` no
existe pero sí el string plano `call.transcript`, devolver `[]` (no intentar
parsear el formato humano — mejor un transcript vacío que uno inventado) y
loguear un warn.

**`RETELL_DISCONNECT_OUTCOME`** — objeto/Map de módulo, tabla explícita con
UNA entrada por cada uno de los 32 valores del catálogo. Cada entrada mapea a
`'no_answer'`, `'voicemail'`, `'hung_up'` o `null` (= la llamada conectó y hubo
conversación real → el outcome lo decide la extracción, no el motivo de corte).
Punto de partida (research §2.3, marcado ASSUMED — es una decisión de diseño de
este plan, revisable con datos reales del piloto de Phase 26):
- → `voicemail`: `voicemail_reached`, `ivr_reached`
- → `no_answer`: `dial_no_answer`, `dial_busy`, `dial_failed`,
  `invalid_destination`, `registered_call_timeout`,
  `telephony_provider_permission_denied`, `telephony_provider_unavailable`,
  `sip_routing_error`, `error_no_audio_received`, `error_user_not_joined`,
  `error_llm_websocket_open`, `error_llm_websocket_lost_connection`,
  `error_llm_websocket_runtime`, `error_llm_websocket_corrupt_payload`,
  `error_asr`, `error_retell`, `error_unknown`, `concurrency_limit_reached`,
  `no_concurrency_fallback`, `no_valid_payment`, `scam_detected`,
  `marked_as_spam`
- → `hung_up`: `user_declined`
- → `null` (conectó): `user_hangup`, `agent_hangup`, `inactivity`,
  `max_duration_reached`, `call_transfer`, `transfer_bridged`,
  `transfer_cancelled`, `manual_stopped`, `call_take_over`
Un valor que no esté en la tabla → `console.warn` con el valor crudo y default
`null` (dejar que la extracción o el LLM decidan; es más conservador que
inventar un no-contacto que dispararía la cadencia).

**`_retellReasonIsNoConnection(reason)`** — devuelve `true` cuando la tabla mapea
a `'no_answer'` o `'voicemail'`. Es el predicado que decide si `call_ended` puede
resolver la llamada solo, sin esperar `call_analyzed` (research §2.3).

**`_retellParseCallbackAt(raw, nowMs)`** — parsea el `callback_fecha_hora` de la
extracción a ISO. Devuelve `''` si no parsea, si es pasado, o si supera los 90
días (D-24-05). Mismo criterio de validación que usó `/book` en 24-04.

**`_retellDecideOutcome({ call, extraction, booked, segments })`** — devuelve
`{ outcome, source, callbackAt, cleanReason }` con `outcome` siempre dentro de
`CALL_OUTCOMES` o `null` (= hace falta el fallback LLM). Orden de decisión
(D-24-05):
1. `booked === true` (hay `_pendingBooked` vigente para ese call_id, o
   `extraction.agendo === true`) → `scheduled_with_admin`, `source:'book'`.
2. `RETELL_DISCONNECT_OUTCOME[disconnection_reason]` no nulo → ese outcome,
   `source:'disconnect'`.
3. `extraction.callback_fecha_hora` que pasa `_retellParseCallbackAt` →
   `callback_later` con ese `callbackAt`, `source:'extraction'`.
4. `extraction.interes` con valor afirmativo (aceptar `true`, `'si'`, `'sí'`,
   `'yes'`, `'alto'` en minúsculas) → `answered_interested`, `source:'extraction'`.
5. `extraction.interes` con valor negativo explícito → `answered_not_interested`,
   `source:'extraction'`.
6. `extraction.atendio === false` sin más datos → `no_answer`,
   `source:'extraction'`.
7. Nada de lo anterior → `null` (el pipeline decidirá si llama al LLM).
El `cleanReason` solo se setea si `extraction.objecion_principal` matchea
exactamente uno de los valores de `DISQUALIFY_REASONS` (index.js:10287-10298);
si no matchea, se deja `''` y el texto crudo se guarda en el logEntry como
`retellObjection` (no forzar un valor de la whitelist con un texto libre del LLM).
  </action>
  <verify>
    <automated>node --check index.js && npx vitest run tests/metrics-consistency.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "RETELL_DISCONNECT_OUTCOME" index.js` devuelve la declaración de módulo
    - La tabla contiene las 32 claves del catálogo del research (verificable contando las claves con un `node -e` que importe `globalThis.__voiceAgent.RETELL_DISCONNECT_OUTCOME` desde un test)
    - `grep -n "function _retellTranscriptToSegments" index.js` devuelve 1 línea y su cuerpo contiene `'setter'` y `'lead'`
    - `grep -n "function _retellDecideOutcome" index.js` devuelve 1 línea
    - `grep -n "_retellParseCallbackAt" index.js` aparece en el helper y en su uso desde `_retellDecideOutcome`
    - Los 4 helpers están en `globalThis.__voiceAgent`
    - `node --check index.js` sale 0
  </acceptance_criteria>
  <done>Toda la traducción Retell→SCM vive en helpers puros y testeables, con el mapeo de motivos de corte en UNA tabla auditable en vez de repartido en ifs.</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline `_retellProcessCallEvent` enganchado al webhook</name>
  <files>index.js</files>
  <read_first>
    - index.js — el handler `POST /api/retell/webhook` y el marcador `// [24-05] punto de inserción del procesamiento de la llamada` que dejó 24-04 (ubicación exacta en `24-04-SUMMARY.md`)
    - index.js:16745-16761 — el patrón fire-and-forget de `_autoDispositionLLM` DESPUÉS de responder, en el endpoint de transcribe
    - index.js:10480-10515 — el logEntry del dialer humano, campo por campo
    - index.js:10015-10027 — cómo se pushea una nota con `{id, text, by, date}` y el cap 100
    - index.js:6703-6713 — `mutateSettersData`
    - .planning/phases/24-integracion-backend-retell/24-RESEARCH.md §4 (patrón de idempotencia por `retellCallId`), §5.3 (10s / 3 reintentos) y §5.6 (concurrencia)
  </read_first>
  <action>
Crear `async function _retellProcessCallEvent(event, call)` en scope de módulo y
llamarla desde el marcador que dejó 24-04, **sin awaitear** antes de responder
(el `res.json` del shell no se mueve): `_retellProcessCallEvent(...).catch(() => {})`.
Retell corta a los 10s y `_autoDispositionLLM` puede tardar 15 (research §5.3).

Flujo:

**a) Filtro de evento.** Solo `call_ended` y `call_analyzed` se procesan; el
resto (`call_started`, `transcript_updated`, `transfer_*`) se ignoran en
silencio (el shell ya los persistió en el log de eventos).

**b) Guard de doble procesamiento en vuelo.** Set de módulo `_retellProcessing`
con los `call_id` que están siendo procesados. Si el id ya está, salir. Sacarlo
en un `finally`. Es la única defensa contra un reintento que llega mientras el
primer procesamiento todavía no escribió el callLog.

**c) Resolver el lead.** `call.metadata?.leadId` → `call.retell_llm_dynamic_variables?.leadId`
→ `_pendingRetellCalls.get(call.call_id)?.leadId` (research §2.5, tres vías
redundantes). Sin leadId resoluble → `console.warn` con el `call_id` y salir.

**d) Decidir si este evento resuelve la llamada.**
- `call_analyzed` → siempre resuelve.
- `call_ended` → resuelve SOLO si `_retellReasonIsNoConnection(disconnection_reason)`
  (la llamada nunca conectó; `call_analyzed` puede no llegar nunca para estos
  casos — research §2.3). Si conectó, registrar el `call_id` en
  `_retellAwaitingAnalysis` (Map `call_id → {event, call, at}`) y salir a esperar
  el análisis.
- Red de seguridad: un timer de módulo que cada 5 minutos recorre
  `_retellAwaitingAnalysis` y procesa con los datos de `call_ended` todo lo que
  lleve más de 10 minutos esperando (el `call_ended` de Retell YA trae
  `transcript_object` completo — lo único que falta es `call_analysis`, así que
  el camino del fallback LLM funciona igual). El `setInterval` se registra solo
  si `NODE_ENV !== 'test'`; los tests invocan la función de barrido directo por
  `globalThis.__voiceAgent`.

**e) Armar el logEntry** (D-24-07), espejando el del dialer humano:
`ts` = ISO del `end_timestamp` de la llamada si viene, si no `new Date().toISOString()`;
`outcome` (se completa en el paso f); `by: ''` (**vacío a propósito**: `_callSetterId`
cae a `lead.assignedTo` = `setter_agente_ia`, criterio #149 — NO inventar un
user sintético); `notes` = el `call_summary` de la extracción recortado a 500
chars, o `''`; `channel: 'retell'` (match exacto que lo deja fuera de Centralita,
index.js:7318 y 15707 — intencional); `duration` = segundos derivados de
`duration_ms` o de `end_timestamp - start_timestamp`, clampeado 0..3600;
`fromNumber` = `call.from_number`; `cost`/`costCountry`/`costTariffKey` de
`_estimateTelnyxCost(lead.phone, duration)`; `retellCallId` = `call.call_id`;
`retellAgentId`; `disconnectionReason` (crudo, para poder auditar la tabla del
paso 1 con datos reales); `transcript` = `{segments, transcribedAt: <ISO>,
source: 'retell'}` (D-24-08) cuando hay segmentos;
`retellObjection` si la extracción trajo una objeción que no matchea la whitelist.

**f) Decidir el outcome.** `booked` = hay entrada vigente en `_pendingBooked` para
ese `call_id` **o** `extraction.agendo === true`. Llamar `_retellDecideOutcome`.
Si devuelve `null` y hay segmentos, llamar `await _autoDispositionLLM(segments)`
y usar su outcome si está en `CALL_OUTCOMES`, guardando además
`logEntry.aiSuggestedOutcome` / `aiSuggestedReason` (mismos nombres de campo que
el flujo humano, index.js:16755-16756). Si todo falla → `answered_not_interested`
como último recurso (D-24-05), con `logEntry.outcomeSource = 'fallback'`.
Registrar siempre el origen de la decisión en `logEntry.outcomeSource`
(`book` / `disconnect` / `extraction` / `llm` / `fallback`) — es el dato que va a
permitir evaluar la calidad del agente en el piloto.

**g) Escribir, todo dentro de UN `mutateSettersData`:**
1. Releer el lead del snapshot fresco. Si no existe → salir.
2. **Idempotencia**: si `lead.callLog` ya tiene una entry con
   `retellCallId === call.call_id`, salir sin escribir. Este chequeo y el push
   TIENEN que estar en el mismo mutator; separarlos abre la ventana que un
   reintento de Retell aprovecha.
3. `_applyCallOutcome(fresh, lead, logEntry, { leadId, outcome, nowIso,
   callbackAt, scheduled, cleanReason, doNotCall: false,
   actorSetterId: VOICE_AGENT_SETTER_ID, actorName: 'Agente IA',
   skipCalendarCreation: booked })`. **`skipCalendarCreation: booked`** es lo que
   evita la segunda cita cuando `/book` ya creó una (D-24-05, §5.4 Opción A).
   `doNotCall` siempre `false`: el agente no marca DNC por su cuenta; si el
   prospecto lo pide, el dato queda en la extracción y lo revisa una persona.
4. Persistir la extracción (D-24-05):
   - `nota_seguimiento` no vacío → push a `lead.notes` con
     `{id: 'note_...', text: <recortado a 500>, by: 'Agente IA', date: <ISO>}`
     y cap 100 (mismo shape y cap que index.js:10023-10027).
   - `doctor_name` → `lead.doctor` **solo si estaba vacío**.
   - `recepcionista_nombre` → una nota adicional firmada 'Agente IA' (nunca pisa
     `lead.doctor`).
   - `email` con formato válido → `lead.email` **solo si estaba vacío**.
   - `objecion_principal` crudo → ya quedó en el logEntry.
   Ninguno de estos campos pisa un dato cargado a mano — misma política que el
   enrichment (nota #111).
5. Loguear a stdout una línea con `call_id`, `leadId`, `outcome` y
   `outcomeSource`.

**h) Limpieza.** Sacar el `call_id` de `_pendingBooked`, `_pendingRetellCalls`,
`_retellAwaitingAnalysis` y `_retellProcessing`.

Exponer `_retellProcessCallEvent` y la función de barrido de la red de seguridad
en `globalThis.__voiceAgent`.
  </action>
  <verify>
    <automated>node --check index.js && npx vitest run tests/metrics-consistency.test.js tests/training-privacy.test.js tests/call-cadence.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "async function _retellProcessCallEvent" index.js` devuelve 1 línea
    - `grep -n "_retellProcessCallEvent(" index.js` aparece en el handler del webhook seguido de `.catch(`
    - El handler del webhook sigue teniendo su `res` ANTES de la llamada al procesamiento (verificable por orden de líneas)
    - `grep -n "retellCallId" index.js` aparece tanto en el armado del logEntry como en el chequeo de idempotencia
    - `grep -n "channel: 'retell'" index.js` devuelve 1 línea
    - `grep -n "by: ''" index.js` aparece en el logEntry del agente con el comentario que explica el criterio #149
    - `grep -n "skipCalendarCreation: booked" index.js` devuelve 1 línea
    - `grep -n "setInterval" index.js` — el timer de la red de seguridad está guardado por `NODE_ENV !== 'test'`
    - `node --check index.js` sale 0 y las 3 suites del verify pasan
  </acceptance_criteria>
  <done>Un evento de Retell produce exactamente una entry de callLog con transcript, la cascada canónica aplicada y los datos de la conversación en su lugar — con la misma forma que deja una llamada humana.</done>
</task>

<task type="auto">
  <name>Task 3: tests/retell-webhook-process.test.js + gate del success criterion 1</name>
  <files>tests/retell-webhook-process.test.js</files>
  <read_first>
    - tests/retell-webhook.test.js (creado en 24-04) — el helper `signRetellBody` y el patrón de mandar el body como string crudo
    - tests/training-privacy.test.js — cómo se consulta la biblioteca de Entrenamiento IA (`GET /api/training/calls` y su detalle) y su RBAC
    - tests/call-cadence.test.js — asserts de cadencia tras `no_answer`
    - index.js:16266-16268 — `TRAINING_EXCLUDED_SETTERS`: confirmar que `setter_agente_ia` NO está excluido de la biblioteca
    - .planning/phases/24-integracion-backend-retell/24-RESEARCH.md §5.1 — patrón de comparación campo por campo
  </read_first>
  <action>
Crear `tests/retell-webhook-process.test.js`. Fixture: `DATA_DIR` temporal,
`retell_config.json` con `apiKey` conocido, `setters.json` con
`setter_agente_ia` y varios leads asignados a él (teléfonos mexicanos completos,
≥7 dígitos — regla #163), `auth.json` con admin. Env de IA y de Retell a `""`
(regla #121) para que `AI_AVAILABLE` sea `false` y el fallback LLM devuelva `null`
de forma determinística (los casos que necesitan probar la rama del LLM invocan
`_autoDispositionLLM` mockeado o testean el fallback final).

Los webhooks se mandan firmados con `signRetellBody` y el body como string crudo.
Como el procesamiento es fire-and-forget, cada test espera a que termine con un
polling corto sobre `setters.json` (o exponiendo la promesa del procesamiento en
`globalThis.__voiceAgent` para poder awaitearla — preferible, elegir esa vía si
no complica el handler).

**Gate del success criterion 1 del ROADMAP (el test más importante del plan).**
Un `call_analyzed` firmado, con `transcript_object` de 4 turnos alternados,
`disconnection_reason: 'user_hangup'`, y `custom_analysis_data` con
`interes: 'si'` y `nota_seguimiento`, produce:
- exactamente 1 entry nueva en `lead.callLog`, con `channel === 'retell'`,
  `by === ''`, `duration > 0`, `cost > 0`, `retellCallId` correcto
- `entry.transcript.segments` con 4 elementos, `speaker` en `['setter','lead']`,
  y el primero mapeado desde `role:'agent'` → `'setter'`
- `lead.estado === 'interesado'` (la cascada de `answered_interested` corrió)
- una nota en `lead.notes` con `by === 'Agente IA'` y el texto de
  `nota_seguimiento`
- `GET /api/training/calls` como admin devuelve esa llamada, y su detalle
  devuelve los segmentos (la biblioteca la consume sin cambios — D-24-08)
- `GET /api/setters/cold-call-metrics?setter=setter_agente_ia` la cuenta como
  dial y como connect (D-24-07: la atribución cae sola)

Resto de la cobertura:

Idempotencia (research §5.3)
- El MISMO `call_analyzed` mandado dos veces deja UNA sola entry.
- `call_ended` de una llamada no conectada seguido de un `call_analyzed` tardío
  del mismo `call_id` deja UNA sola entry.

Los tres caminos de outcome (D-24-05)
- `call_ended` con `disconnection_reason: 'dial_no_answer'` → `no_answer`
  aplicado de inmediato, sin esperar `call_analyzed`, con la cadencia corriendo
  (`cadenceStep === 1`, `callbackAt` a ~24h).
- Dos `dial_no_answer` seguidos sobre el mismo lead → `estado === 'descartado'`
  y `autoDiscardReason === 'sin_contacto_2x'` (la cadencia del helper de 24-01
  funciona igual para el agente que para un humano).
- `call_ended` con `voicemail_reached` → `voicemail` + `phoneStatus`.
- `call_ended` con `user_hangup` (conectó) → NO escribe nada todavía; queda
  esperando el análisis. Invocar la función de barrido de la red de seguridad con
  un reloj adelantado y verificar que ahí sí resuelve.
- `call_analyzed` con `callback_fecha_hora` a 3 días → `callback_later` con
  `lead.callbackAt` en esa fecha. Con `callback_fecha_hora` en el pasado o a 200
  días → NO se usa (cae al camino siguiente).

Booking (D-24-05 — la prueba de que no se duplica la cita)
- Llamar `/book` (endpoint de 24-04) con un `call_id`, y después mandar el
  `call_analyzed` de ese mismo `call_id`: `lead.estado === 'agendado'`,
  `data.calendar` tiene **exactamente 1** entry (no 2), y hay 1 sola entry de
  callLog.
- Un `call_analyzed` con `agendo: true` pero SIN `/book` previo → sí crea la cita
  (el camino de respaldo de D-24-05).

Extracción
- `doctor_name` se escribe en `lead.doctor` si estaba vacío y NO lo pisa si ya
  tenía valor.
- `email` inválido no se persiste.
- `objecion_principal` con un valor fuera de `DISQUALIFY_REASONS` queda en
  `logEntry.retellObjection` y `lead.disqualifyReason` NO se ensucia.

Robustez
- Evento sin `leadId` resoluble por ninguna de las 3 vías → no rompe, no escribe,
  el webhook igual devolvió 200.
- `call_analyzed` con `call_analysis` ausente (research §6.3) → no rompe; cae al
  fallback.
- `disconnection_reason` desconocido (inventar uno) → no rompe, loguea, y no
  dispara la cadencia por su cuenta.
- Evento `call_started` / `transcript_updated` → se persiste en el log de eventos
  pero NO escribe callLog.

Cerrar corriendo la suite completa.
  </action>
  <verify>
    <automated>npx vitest run tests/retell-webhook-process.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/retell-webhook-process.test.js` sale 0 con al menos 20 tests
    - Existe el test del success criterion 1 que assertea, en un solo caso: `channel==='retell'`, 4 segmentos de transcript, `estado==='interesado'`, nota con `by==='Agente IA'`, y presencia en `GET /api/training/calls`
    - Existe un test que manda el mismo `call_analyzed` dos veces y assertea `callLog.length === 1`
    - Existe un test que combina `/book` + `call_analyzed` y assertea `data.calendar.length === 1`
    - Existe un test que assertea `cold-call-metrics?setter=setter_agente_ia` con `dials >= 1`
    - Ningún `delete process.env.` en el archivo
    - `npm test` sale 0 y `git diff --name-only -- tests/` no lista ningún test preexistente modificado
  </acceptance_criteria>
  <done>El circuito completo está probado de punta a punta: un webhook firmado entra y sale como una llamada de SDR indistinguible en callLog, biblioteca, cascada y métricas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| payload verificado de Retell → escritura sobre `setters.json` | La firma prueba QUIÉN mandó el evento, no que su CONTENIDO sea sensato: el `leadId` y la extracción vienen de un LLM |
| extracción del LLM de Retell → campos del lead | Texto generado que termina en `lead.doctor`, `lead.email`, `notes[]` y `callbackAt` |
| procesamiento async → mutex de `setters.json` | Corre en paralelo con dispatches y con el dialer humano |

## STRIDE Threat Register

| Threat ID | Categoría | Componente | Disposición | Mitigación |
|-----------|-----------|------------|-------------|------------|
| T-24-05-01 | Tampering | Un evento firmado con un `leadId` de otro lead (agente mal configurado, o un actor con el secret) aplicando una cascada sobre un lead ajeno | mitigate | El `leadId` se resuelve preferentemente de `metadata` (que lo puso NUESTRO dispatch, no el LLM) y hay una tercera vía interna (`_pendingRetellCalls`). El daño posible está acotado a los outcomes de `CALL_OUTCOMES` — nunca se ejecuta nada derivado del texto. La superficie previa (firma) es la barrera principal (T-24-04-01) |
| T-24-05-02 | Tampering | Duplicación del callLog por los 3 reintentos de Retell → dials inflados en el funnel | mitigate | Idempotencia por `retellCallId` **dentro del mismo mutator** que hace el push (research §4/§5.6) + Set `_retellProcessing` para los reintentos que llegan mientras el primero está en vuelo. Dos tests dedicados |
| T-24-05-03 | Tampering | Doble creación de la cita (una en `/book`, otra en el switch de `_applyCallOutcome`) | mitigate | `skipCalendarCreation: booked` (D-24-05, §5.4 Opción A) + test que assertea `data.calendar.length === 1` |
| T-24-05-04 | Tampering | El LLM de Retell inventa un `doctor_name`/`email` y pisa un dato real cargado a mano | mitigate | Los campos de la extracción se escriben **solo si estaban vacíos**; misma política que el enrichment (nota #111). `objecion_principal` que no matchea la whitelist NO se fuerza a `disqualifyReason` |
| T-24-05-05 | Tampering | `callback_fecha_hora` alucinado dejando un lead escondido de la cola por años | mitigate | `_retellParseCallbackAt` valida futuro y ≤90 días (D-24-05); fuera de rango se descarta y el outcome cae al camino siguiente. Test con fecha pasada y con 200 días |
| T-24-05-06 | Denial of Service | `_autoDispositionLLM` (15s) bloqueando la respuesta del webhook (presupuesto 10s) y disparando reintentos | mitigate | El procesamiento entero corre fire-and-forget DESPUÉS del `res` (patrón ya usado en el transcribe, index.js:16748-16759); el reintento que llegue entre medio lo frena el Set `_retellProcessing` |
| T-24-05-07 | Denial of Service | Escrituras concurrentes (webhook + dispatch + dialer humano) pisándose el snapshot | mitigate | Toda escritura por `mutateSettersData` (regla #19, research §5.6): el mutex encadena promesas, así que la serialización es automática con solo usar el wrapper |
| T-24-05-08 | Repudiation | Un outcome del agente indistinguible de uno humano en una auditoría | mitigate | `channel:'retell'`, `retellCallId`, `disconnectionReason` y `outcomeSource` quedan en el logEntry; las notas se firman `by:'Agente IA'`. Se puede reconstruir exactamente por qué el sistema decidió cada outcome |
| T-24-05-09 | Information Disclosure | Transcripts del agente visibles para SDRs que no hicieron esa llamada | accept | La biblioteca ya filtra por atribución (nota #118): las llamadas del agente pertenecen a `setter_agente_ia`, así que solo admin/supervisor las ven. Es el comportamiento deseado (el agente no tiene sesión propia) |
| T-24-05-SC | Tampering | instalación de paquetes npm | accept | Este plan NO instala dependencias. `package.json` no se modifica |
</threat_model>

<verification>
1. `node --check index.js` sale 0 y `npm test` verde, sin editar ningún test preexistente.
2. Un `call_analyzed` firmado produce 1 entry de callLog con transcript visible en `GET /api/training/calls`.
3. El mismo evento repetido no duplica nada (callLog, calendario, notas).
4. `/book` + `call_analyzed` dejan exactamente 1 cita.
5. `GET /api/setters/cold-call-metrics?setter=setter_agente_ia` cuenta la llamada sin que se haya tocado una línea del CALL METRICS CORE (`git diff` sobre el rango 7248-7450 de index.js vacío).
6. `git diff --stat public/` vacío.
</verification>

<success_criteria>
- Un webhook `call_analyzed` simulado produce en el lead: callLog `channel:'retell'` con transcript en la biblioteca, outcome canónico con la cascada idéntica a la humana, y nota de seguimiento en notes[] (success criterion 1 del ROADMAP).
- Las llamadas del agente aparecen en Equipo/Comando/funnel atribuidas a `setter_agente_ia` sin código de métricas nuevo (success criterion 5, cerrado en conjunto con 24-02).
- Ningún reintento de Retell duplica una llamada.
</success_criteria>

<output>
Crear `.planning/phases/24-integracion-backend-retell/24-05-SUMMARY.md` al terminar.
Documentar explícitamente para Phase 26: (a) la tabla `RETELL_DISCONNECT_OUTCOME`
tal como quedó, marcada como ASSUMED y pendiente de revisión con los
`disconnectionReason` reales del piloto; (b) las claves exactas que el pipeline
espera en `custom_analysis_data` — Phase 26 tiene que configurar la Post Call Data
Extraction con ESOS nombres o el agente devuelve datos que nadie lee; (c) qué se
observó (si algo) sobre si `call_analyzed` llega para llamadas sin conexión.
</output>
