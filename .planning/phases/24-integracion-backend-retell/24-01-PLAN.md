---
phase: 24-integracion-backend-retell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.js
  - tests/apply-call-outcome.test.js
autonomous: true
requirements: [VOICE-02]

must_haves:
  truths:
    - "D-24-02: la cascada de dispositions vive en un helper puro `_applyCallOutcome(data, lead, logEntry, opts)` que el handler humano llama, sin que ningún número de las métricas cambie"
    - "El webhook del agente (plan 24-05) puede aplicar la MISMA cascada sin tener `req`, `req.auth` ni `req.params` en scope"
    - "`_estimateTelnyxCost` se puede llamar desde cualquier punto del archivo (hoy está anidada dentro del handler humano y es inalcanzable)"
    - "El case `scheduled_with_admin` puede aplicarse SIN crear una segunda cita cuando la cita ya la creó `/book` (D-24-05, §5.4 Opción A)"
    - "La suite completa queda verde y `tests/metrics-consistency.test.js` no cambió ningún assert"
  artifacts:
    - path: "index.js"
      provides: "`_applyCallOutcome` en scope de módulo + helpers de costo hoisteados + export `globalThis.__voiceAgent`"
      contains: "function _applyCallOutcome"
    - path: "tests/apply-call-outcome.test.js"
      provides: "Test de paridad doble-vía (handler humano vs helper directo) sobre los 8 outcomes"
      min_lines: 150
  key_links:
    - from: "app.post('/api/setters/leads/:id/call-disposition')"
      to: "_applyCallOutcome"
      via: "llamada directa con opts.actorSetterId / opts.actorName / opts.leadId / opts.nowIso"
      pattern: "_applyCallOutcome\\(data, lead, logEntry"
    - from: "globalThis.__voiceAgent"
      to: "tests/apply-call-outcome.test.js"
      via: "acceso al helper puro desde el test de paridad"
      pattern: "globalThis\\.__voiceAgent"
---

<objective>
Extraer la cascada de disposición de llamada del handler humano a un helper puro
reutilizable, y subir a scope de módulo los helpers de costo que hoy están
encerrados dentro de ese mismo handler.

Propósito: sin esto, el webhook de Retell (plan 24-05) tendría que duplicar 125
líneas de lógica de negocio (estados, DNC, cadencia, calendario) — el circuito
paralelo de métricas que TODO el milestone prohíbe. Resuelve además la deuda M1
del audit 2026-06-20.

Output: `_applyCallOutcome(data, lead, logEntry, opts)` + `_estimateTelnyxCost` /
`_detectCountryAndType` / `TELNYX_RATES_USD_PER_MIN` en scope de módulo +
`globalThis.__voiceAgent` + test de paridad doble-vía.

**Esto es EXTRACCIÓN, no reescritura (D-24-02).** El cuerpo del código se mueve
verbatim. Cualquier "mejora", renombre de variable, reordenamiento de ifs o
simplificación que se te ocurra en el camino: NO. El gate de la fase es que la
suite existente no cambie un solo número.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-integracion-backend-retell/24-CONTEXT.md
@.planning/phases/24-integracion-backend-retell/24-RESEARCH.md
@CLAUDE.md

Reglas duras que aplican a este plan:
- **Regla #19 (mutex)**: el handler humano de `call-disposition` es SÍNCRONO y
  sigue usando `loadSettersData()` / `saveSettersData()` directo. Este refactor
  NO debe convertirlo en async ni meterlo en `mutateSettersData`. El helper
  extraído es síncrono y puro sobre los objetos que recibe; quien lo llama desde
  un handler async (el webhook, plan 24-05) es el responsable de envolverlo en
  `mutateSettersData`.
- **Cache-buster**: este plan no toca `public/app.js` ni `public/style.css` →
  NO se bumpea nada.
- **Nota MEMORY `user-commits-in-parallel`**: correr `git status` / `git log`
  antes de empezar. El árbol puede haberse movido.
</context>

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraídos del código real, HEAD d973e9f. -->

Referencias externas que HOY existen dentro del rango a extraer (10552-10675) y
que se convierten en parámetros de `opts`:

| Referencia actual | Línea | Pasa a ser |
|---|---|---|
| `req.params.id` (en `calendarEntry.leadId`) | 10613 | `opts.leadId` |
| `now` (ISO del handler, en `lastContactAt` y `doNotCallAt`) | 10557, 10635 | `opts.nowIso` |
| `req.auth?.user?.role === 'setter' ? req.auth.user.setterId : (lead.assignedTo \|\| '')` | 10619 | `opts.actorSetterId` |
| `req.auth?.user?.name \|\| ''` (en `doNotCallBy`) | 10636 | `opts.actorName` |
| `outcome`, `callbackAt`, `callbackShared`, `scheduled`, `cleanReason`, `doNotCall` | varias | `opts.*` (mismos nombres) |

⚠️ `opts.actorName` NO está en 24-CONTEXT.md ni en 24-RESEARCH.md — se detectó
leyendo el código durante la planificación (línea 10636, `lead.doNotCallBy`). Sin
él, todo lead marcado DNC por el handler humano perdería la firma del audit trail.

Constantes que YA son de módulo y el helper usa directo (no se mueven, no se
pasan por opts): `DNC_REASONS` (index.js:10299).

Constantes que hoy son locales del handler y viven SOLO dentro del rango
extraído → se declaran dentro del helper: `_NO_CONTACT` (10644),
`MAX_NO_CONTACT` (10656).

Firma destino:
`function _applyCallOutcome(data, lead, logEntry, opts)` → devuelve
`{ calendarEntry }` (`null` cuando el outcome no crea cita).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Subir los helpers de costo a scope de módulo</name>
  <files>index.js</files>
  <read_first>
    - index.js:10269-10301 — dónde arrancan `CALL_OUTCOMES` / `DISQUALIFY_REASONS` / `DNC_REASONS` y el handler; es el punto de inserción
    - index.js:10373-10479 — el bloque anidado completo: comentario + `TELNYX_RATES_USD_PER_MIN` (10378-10397) + `_detectCountryAndType` (10401-10452) + `_estimateTelnyxCost` (10454-10478)
    - index.js:10492-10515 — el ÚNICO call site actual (`_estimateTelnyxCost(lead.phone, dur)` en la rama `telnyxCallMeta`)
    - index.js:14030-14050 — `_telnyxRateForNumber`, de quien `_estimateTelnyxCost` depende
    - index.js:7445 — patrón de export `globalThis.__callCore = { ... }`
  </read_first>
  <action>
Mover al scope de módulo, con el cuerpo VERBATIM (mismos comentarios, mismos
valores, mismo orden de ifs), las tres declaraciones que hoy viven anidadas
dentro del handler `POST /api/setters/leads/:id/call-disposition`:
`TELNYX_RATES_USD_PER_MIN`, `_detectCountryAndType` y `_estimateTelnyxCost`.

Destino: scope de módulo inmediatamente ANTES de la const `CALL_OUTCOMES`
(index.js:10272), para que queden adyacentes a su consumidor actual. Borrar las
copias anidadas del handler.

`_telnyxRateForNumber` (index.js:14030) NO se mueve: es una function declaration
de módulo, el hoisting la deja accesible desde la posición nueva. `TELNYX_RATES_USD_PER_MIN`
es un `const` de módulo que se evalúa en el boot, muy antes de cualquier request,
así que no hay TDZ posible en el camino del handler.

Agregar el comentario de una línea que explique POR QUÉ subieron: el webhook de
Retell (VOICE-05) necesita estimar el costo de una llamada que nunca pasó por
el dialer humano, y anidadas eran inalcanzables.

No tocar la rama `telnyxCallMeta` del handler: sigue llamando
`_estimateTelnyxCost(lead.phone, dur)` exactamente igual.
  </action>
  <verify>
    <automated>node --check index.js && npx vitest run tests/disposition-dnc.test.js tests/call-cadence.test.js tests/metrics-consistency.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check index.js` sale con exit code 0
    - `grep -n "^function _estimateTelnyxCost" index.js` devuelve exactamente 1 línea (columna 0 = scope de módulo)
    - `grep -n "^function _detectCountryAndType" index.js` devuelve exactamente 1 línea
    - `grep -n "^const TELNYX_RATES_USD_PER_MIN" index.js` devuelve exactamente 1 línea
    - `grep -n "  function _estimateTelnyxCost" index.js` no devuelve ninguna línea (ya no hay copia indentada/anidada)
    - `grep -n "_estimateTelnyxCost(lead.phone, dur)" index.js` sigue devolviendo 1 línea dentro del handler humano
    - `npx vitest run tests/disposition-dnc.test.js tests/call-cadence.test.js tests/metrics-consistency.test.js` sale 0
  </acceptance_criteria>
  <done>Los tres helpers de costo son invocables desde cualquier punto de index.js; el handler humano calcula el costo exactamente igual que antes.</done>
</task>

<task type="auto">
  <name>Task 2: Extraer `_applyCallOutcome` (extracción verbatim + flag skipCalendarCreation)</name>
  <files>index.js</files>
  <read_first>
    - index.js:10552-10675 — EL RANGO A EXTRAER, línea por línea: push+cap500 (10552-10555), attempts/lastContactAt/conexion (10556-10559), `let calendarEntry = null` (10561), switch de 8 casos (10563-10628) con el shape del calendarEntry (10611-10621), DNC (10630-10638), cadencia (10640-10675)
    - index.js:10677-10707 — lo que queda DESPUÉS y NO se extrae: resolución de `pendingCallId` (Phase 20) + `saveSettersData` + `res.json({ ok, lead, calendarEntry, resolvedPendingId })`
    - index.js:10319-10337 — de dónde salen `outcome`, `cleanReason`, `callbackAt`, `scheduled`, `doNotCall`, `callbackShared` en el handler
    - index.js:10480-10486 — construcción del `logEntry` (queda en el handler; el helper recibe el objeto YA armado y SIN pushear)
    - index.js:10299 — `DNC_REASONS`, constante de módulo que el helper usa directo
    - .planning/phases/24-integracion-backend-retell/24-RESEARCH.md §3.1 y §5.4
  </read_first>
  <action>
Crear `function _applyCallOutcome(data, lead, logEntry, opts)` en scope de módulo,
inmediatamente después del bloque de helpers de costo de la Task 1 (antes de
`CALL_OUTCOMES`). Su cuerpo es el rango index.js:10552-10675 movido tal cual,
con estas cinco sustituciones mecánicas y ninguna otra:

1. `req.params.id` → `opts.leadId`
2. `now` → `opts.nowIso`
3. la expresión ternaria de `calendarEntry.setterId` → `opts.actorSetterId`
4. `req.auth?.user?.name || ''` de `lead.doNotCallBy` → `opts.actorName || ''`
5. `outcome` / `callbackAt` / `callbackShared` / `scheduled` / `cleanReason` /
   `doNotCall` → destructurados de `opts` al inicio de la función

`_NO_CONTACT` y `MAX_NO_CONTACT` se declaran dentro del helper (hoy son locales
del handler y solo se usan en el rango extraído). `DNC_REASONS` se usa directo
del scope de módulo.

El helper declara `let calendarEntry = null` al inicio y devuelve
`{ calendarEntry }`.

Agregar el flag `opts.skipCalendarCreation` (default `false`, §5.4 Opción A):
en el case `scheduled_with_admin`, cuando el flag es `true` NO se construye el
objeto de cita ni se hace `data.calendar.push(...)`, pero SÍ se aplican los
cuatro side-effects de estado del case (`respondio`, `calificado`, `interes`,
`estado`). El handler humano NUNCA pasa este flag → comportamiento idéntico.
El consumidor es `/book` + webhook (D-24-05: una sola cita por llamada).

En el handler humano, reemplazar el rango 10552-10675 por una sola llamada que
destructura `calendarEntry` del resultado, pasando:
`leadId: req.params.id`, `nowIso: now`, `outcome`, `callbackAt`, `callbackShared`,
`scheduled`, `cleanReason`, `doNotCall`, `actorSetterId` = la ternaria original
de la línea 10619 calculada en el handler, `actorName: req.auth?.user?.name || ''`.
Todo lo de arriba (auth, parseo, `correctsAutoMarked`, construcción del logEntry,
`telnyxCallMeta`, `preCadence`) y todo lo de abajo (`pendingCallId`,
`saveSettersData`, `res.json`) queda EXACTAMENTE donde está.

Exponer `globalThis.__voiceAgent = { _applyCallOutcome, _estimateTelnyxCost,
_detectCountryAndType }` siguiendo el patrón de `globalThis.__callCore`
(index.js:7445). Los planes 24-03/24-04/24-05 van a sumar más claves a este
mismo objeto.
  </action>
  <verify>
    <automated>node --check index.js && npx vitest run tests/disposition-dnc.test.js tests/call-cadence.test.js tests/funnel-close.test.js tests/metrics-consistency.test.js tests/disposition-enforcement.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^function _applyCallOutcome(data, lead, logEntry, opts)" index.js` devuelve 1 línea
    - `grep -n "_applyCallOutcome(data, lead, logEntry" index.js` devuelve al menos 2 líneas (la declaración y la llamada del handler humano)
    - `grep -n "req.params.id" index.js` NO devuelve ninguna línea dentro del cuerpo de `_applyCallOutcome`
    - `grep -n "req.auth" index.js` NO devuelve ninguna línea dentro del cuerpo de `_applyCallOutcome`
    - `grep -n "globalThis.__voiceAgent" index.js` devuelve 1 línea con `_applyCallOutcome` y `_estimateTelnyxCost` entre sus claves
    - `grep -n "skipCalendarCreation" index.js` devuelve al menos 1 línea dentro del case `scheduled_with_admin`
    - El handler humano conserva `res.json({ ok: true, lead, calendarEntry, resolvedPendingId })` sin cambios
    - `npx vitest run tests/disposition-dnc.test.js tests/call-cadence.test.js tests/funnel-close.test.js tests/metrics-consistency.test.js tests/disposition-enforcement.test.js` sale 0
  </acceptance_criteria>
  <done>El handler humano delega toda la cascada al helper; el helper no referencia `req` en ninguna forma; la suite de disposición/cadencia/DNC/funnel/métricas pasa sin editar un solo assert.</done>
</task>

<task type="auto">
  <name>Task 3: Test de paridad doble-vía + gate de suite completa</name>
  <files>tests/apply-call-outcome.test.js</files>
  <read_first>
    - tests/disposition-dnc.test.js — patrón de fixture (DATA_DIR temporal, auth pre-poblada, `await import("../index.js")` DESPUÉS de escribir los fixtures) y de login del admin/setter
    - tests/call-cadence.test.js — cómo se ejercita la cadencia `no_answer` repetida y qué campos se assertan
    - .planning/phases/24-integracion-backend-retell/24-RESEARCH.md §4 (patrón de test) y §5.1 (estrategia de paridad)
    - index.js:10272-10283 — los 8 outcomes de `CALL_OUTCOMES` que el test tiene que cubrir
  </read_first>
  <action>
Crear `tests/apply-call-outcome.test.js` con el fixture doble-vía de §5.1:

Setup: `DATA_DIR` temporal, `auth.json` con un admin y un setter, `setters.json`
con pares de leads IDÉNTICOS en estado inicial (`a1`/`b1`, `a2`/`b2`, …), uno por
escenario. Teléfonos de **≥7 dígitos** y de país NO bloqueado por tarifa —
usar prefijo `+521` mexicano con número completo, ej. `+525550000001`
(regla #163: `_leadIsCallableNow` exige ≥7 dígitos, y `_expensiveTariffLabel`
bloquea ES fijo / UY / EC / BO / PE fijo). Env vars de IA a `""`, nunca `delete`
(regla #121).

Por cada escenario: aplicar el outcome al lead A vía
`POST /api/setters/leads/:id/call-disposition` (handler humano, con supertest) y
al lead B vía `globalThis.__voiceAgent._applyCallOutcome` llamado directo sobre
el objeto de `setters.json` con los mismos `opts` equivalentes; después comparar
lead A vs lead B campo por campo.

Escenarios mínimos (uno por par de leads):
- `answered_interested`
- `answered_not_interested` con `disqualifyReason: 'no_es_icp'`
- `answered_not_interested` con `disqualifyReason: 'no_contactar'` (dispara DNC)
- `no_answer` una vez (cadencia paso 1: `callbackAt` a +24h, `cadenceStep` 1)
- `no_answer` dos veces seguidas (cadencia agotada: `estado='descartado'`,
  `cadenceExhausted`, `autoDiscarded`, `autoDiscardReason`)
- `voicemail` (`phoneStatus`)
- `wrong_number` / `invalid_number`
- `callback_later` con `callbackAt` explícito y `callbackShared: true`
- `scheduled_with_admin` con `scheduled.fecha` (verifica el shape completo del
  calendarEntry: `calendarioEstado:'pendiente'`, `valorProyecto:0`, `comision:0`,
  `sourceCall:true`, `setterId`, `leadId`, `nombre`)
- `scheduled_with_admin` con `skipCalendarCreation: true` → los cuatro campos de
  estado del lead se aplican igual, `data.calendar` NO crece, el helper devuelve
  `calendarEntry: null`

Campos a comparar en cada par: `estado`, `respondio`, `calificado`, `interes`,
`callbackAt`, `callbackShared`, `doNotCall`, `doNotCallReason`, `doNotCallBy`,
`phoneStatus`, `cadenceStep`, `cadenceExhausted`, `autoDiscarded`,
`autoDiscardReason`, `callAttempts`, `conexion`, `disqualifyReason` y el largo de
`callLog`. Diferencias intencionales que el test debe documentar y excluir:
`logEntry.by` y `logEntry.channel` (el handler humano firma con el user de sesión
y `channel:'manual'`; el agente firmará `by:''` / `channel:'retell'`), y
`lastContactAt` si los dos caminos usaron `nowIso` distintos — pasar el MISMO
`nowIso` al helper para que también coincida.

Sumar un caso que pruebe que `globalThis.__voiceAgent._estimateTelnyxCost` es
invocable desde fuera del handler y devuelve `{cost, country, tariffKey}` con
`cost > 0` para 90 segundos a un número mexicano (prueba de la Task 1 desde el
lado del comportamiento, no del texto).

Cerrar con el gate: correr la suite COMPLETA y confirmar que ningún test
existente fue editado.
  </action>
  <verify>
    <automated>npx vitest run tests/apply-call-outcome.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/apply-call-outcome.test.js` sale 0 con al menos 11 tests
    - `npm test` sale 0 (suite completa)
    - `git diff --name-only -- tests/` lista ÚNICAMENTE `tests/apply-call-outcome.test.js` (ningún test preexistente fue modificado para que pase)
    - El test contiene un caso que pasa `skipCalendarCreation: true` y assertea que `data.calendar.length` no cambió
    - El test contiene un caso donde dos `no_answer` seguidos dejan `estado === 'descartado'` y `autoDiscardReason === 'sin_contacto_2x'` en AMBAS vías
    - Ningún `delete process.env.` aparece en el archivo (regla #121); las env vars de IA se setean a `""`
  </acceptance_criteria>
  <done>La paridad handler↔helper está probada por test, la suite completa pasa y no se tocó ningún test viejo — el success criterion 2 del ROADMAP queda cerrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| sesión HTTP → `call-disposition` | Ya existente. Este plan NO cambia el auth ni el ownership check del handler (10305-10317) |
| futuro webhook público → `_applyCallOutcome` | Se abre en 24-05. Este plan define el contrato: el helper NO lee identidad de ningún lado, la recibe por `opts` |

## STRIDE Threat Register

| Threat ID | Categoría | Componente | Disposición | Mitigación |
|-----------|-----------|------------|-------------|------------|
| T-24-01-01 | Elevation of Privilege | `_applyCallOutcome` como superficie compartida humano/agente | mitigate | El helper NO contiene ningún control de acceso: los checks de auth y ownership quedan ARRIBA, en el handler (10305-10317). Cada caller es responsable de autorizar antes de llamar. Documentarlo en el comentario de cabecera del helper para que 24-05 no asuma que el helper lo cubre |
| T-24-01-02 | Repudiation | `lead.doNotCallBy` firmado por el actor | mitigate | `opts.actorName` es obligatorio en el contrato; el handler humano sigue pasando `req.auth.user.name` (nunca el body — regla #26). El agente pasará una identidad fija. Un `opts` sin `actorName` deja `''`, nunca un nombre arbitrario |
| T-24-01-03 | Tampering | Regresión silenciosa de la cascada durante la extracción | mitigate | Gate de paridad de la Task 3: doble-vía campo por campo + suite completa sin editar ningún test viejo (verificable con `git diff --name-only -- tests/`) |
| T-24-01-04 | Tampering | Doble creación de cita cuando `/book` ya creó una | mitigate | `opts.skipCalendarCreation` (§5.4 Opción A), default `false`, con test dedicado que assertea que `data.calendar` no crece |
| T-24-01-SC | Tampering | instalación de paquetes npm | accept | Este plan NO instala ninguna dependencia. `package.json` no se modifica |
</threat_model>

<verification>
1. `node --check index.js` sale 0.
2. `npm test` verde sin haber editado ningún test preexistente.
3. `grep -n "req\." index.js` no matchea dentro del cuerpo de `_applyCallOutcome`.
4. `globalThis.__voiceAgent` expone `_applyCallOutcome`, `_estimateTelnyxCost` y `_detectCountryAndType`.
5. `git diff --stat public/` está vacío (backend puro, sin cache-buster).
</verification>

<success_criteria>
- El handler humano de `call-disposition` produce exactamente los mismos efectos que antes del refactor (probado campo por campo).
- Un caller sin sesión HTTP puede aplicar la cascada completa pasando `opts`.
- `tests/metrics-consistency.test.js` pasa sin un solo cambio de assert (success criterion 2 del ROADMAP).
- `_estimateTelnyxCost` es invocable desde el scope de módulo.
</success_criteria>

<output>
Crear `.planning/phases/24-integracion-backend-retell/24-01-SUMMARY.md` al terminar.
Documentar explícitamente: (a) si apareció alguna referencia externa dentro del
rango extraído que no esté en la tabla de `<interfaces>`, (b) el resultado exacto
del conteo de la suite completa antes y después.
</output>
