---
phase: 24-integracion-backend-retell
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - index.js
  - scripts/pre-deploy.js
  - tests/retell-config.test.js
autonomous: true
requirements: [VOICE-01, VOICE-06]
user_setup:
  - service: retell
    why: "El agente de voz llama a la API de Retell y verifica la firma de sus webhooks con la misma credencial"
    env_vars:
      - name: RETELL_API_KEY
        source: "Dashboard de Retell → API Keys (la misma key firma los webhooks — research §2.1)"
      - name: RETELL_TOOL_SECRET
        source: "Valor inventado por el user; se carga IGUAL en Railway y en el Custom Header 'x-scm-tool-secret' del function node de Retell (Phase 26)"
      - name: RETELL_WEBHOOK_SECRET
        source: "OPCIONAL — solo si el dashboard de Retell muestra un valor distinto del API key para el 'webhook badge'. Vacío = se usa RETELL_API_KEY"
    dashboard_config:
      - task: "Apuntar la Webhook URL del agente a https://<railway-domain>/api/retell/webhook"
        location: "Dashboard de Retell → Agent → Webhook settings (Phase 26)"

must_haves:
  truths:
    - "D-24-01: los secrets de Retell viven en env vars de Railway, ganan sobre el JSON, y el PUT del panel los rechaza con 409 cuando vienen de env"
    - "D-24-01: `retell_config.json` y `retell_events.json` sobreviven un redeploy (BACKUP_FILES + export-data + import-data + seedVolumeFromRepo + pre-deploy)"
    - "D-24-01 corregido por research §2.1: `webhookSecret` es OPCIONAL y cae a `apiKey` cuando está vacío — Retell firma con el mismo API key, no con un signing secret aparte"
    - "D-24-09: `setter_agente_ia` existe como SDR normal (name 'Agente IA', sin user vinculado, NO hidden) y aparece en Equipo/Comando/Distribución sin excepciones"
    - "El GET de config nunca devuelve el valor de ningún secret, solo flags `hasX` + `envSourced` (patrón `_publicTelnyxConfig`)"
    - "`npm run pre-deploy` no deja ningún secret de Retell escrito en `data/retell_config.json`"
  artifacts:
    - path: "index.js"
      provides: "Módulo de config Retell (env>JSON), endpoints GET/PUT admin, log FIFO de eventos, seed del pseudo-SDR"
      contains: "RETELL_ENV_FIELDS"
    - path: "scripts/pre-deploy.js"
      provides: "Stripper de secrets de Retell + persistencia de los 2 archivos nuevos"
      contains: "retell_config.json"
    - path: "tests/retell-config.test.js"
      provides: "RBAC, no-leak de secrets, 409 env-sourced, self-healing, round-trip export/import, pseudo-SDR visible"
      min_lines: 180
  key_links:
    - from: "GET /api/retell/config"
      to: "_publicRetellConfig"
      via: "sanitizador que nunca expone apiKey/webhookSecret/toolSecret"
      pattern: "_publicRetellConfig"
    - from: "BACKUP_FILES"
      to: "retell_config.json"
      via: "array de archivos del snapshot cada 6h"
      pattern: "retell_config\\.json"
    - from: "ensureSetterProfile('Agente IA')"
      to: "setter_agente_ia"
      via: "seed idempotente en el boot"
      pattern: "VOICE_AGENT_SETTER_NAME"
---

<objective>
Dar al agente de voz su configuración persistente y su identidad dentro del
sistema: `retell_config.json` con el patrón env>JSON de Telnyx, el log FIFO de
eventos, los endpoints admin de lectura/escritura, y el pseudo-SDR
`setter_agente_ia`.

Propósito: los planes 24-03/24-04/24-05 necesitan un lugar de donde leer el API
key, el `agentId`, el `dailyCap` y el índice de rotación de caller ID — y un
`assignedTo` al que pertenezcan los leads del agente. Sin la regla #21 completa,
un redeploy de Railway borra todo.

Output: config + eventos + 2 endpoints + persistencia en 5 lugares + pseudo-SDR +
tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-integracion-backend-retell/24-CONTEXT.md
@.planning/phases/24-integracion-backend-retell/24-RESEARCH.md
@CLAUDE.md
@.planning/phases/24-integracion-backend-retell/24-01-SUMMARY.md

Reglas duras:
- **Regla #21**: los 5 lugares. El research §5.2 avisa que el precedente de
  Telnyx está INCOMPLETO (`seedVolumeFromRepo` no lo incluye) — **no clonar ese
  hueco**.
- **Regla #3**: las rutas nuevas no tienen `:id`, no hay riesgo de colisión.
- **Regla #121**: en tests, env vars a `""`, nunca `delete`.
- **Cache-buster**: backend puro, no se bumpea.
- Este plan va en wave 2 porque toca `index.js`, igual que 24-01: la dependencia
  es de SERIALIZACIÓN de archivo, no funcional (no usa `_applyCallOutcome`).
</context>

<interfaces>
<!-- Extraído del código real, HEAD d973e9f. El ejecutor no debe explorar. -->

Patrón Telnyx a clonar (leerlo, no reinventarlo):
- `TELNYX_CONFIG_FILE` / `TELNYX_EVENTS_FILE` — index.js:13996-13997
- `TELNYX_ENV_FIELDS` — index.js:14070-14076
- `_telnyxEnvSourced()` — index.js:14080-14086
- `loadTelnyxConfig()` (lazy-init + overlay env>JSON) — index.js:14088-14119
- `saveTelnyxConfig()` — index.js:14121-14124
- `_publicTelnyxConfig()` (nunca expone secrets, sí `envSourced`) — index.js:14129-14142
- `GET /api/telnyx/config` — index.js:14722-14730
- `PUT /api/telnyx/config` — index.js:14740-14815, con el 409 de env-sourced en
  14751-14757 y el self-healing (limpia el JSON cuando la env var manda) en 14793-14797
- `loadTelnyxEvents()` — index.js:15502
- `_telnyxWebhookRejects` (contador en memoria) — index.js:15044

Puntos de la regla #21:
- `BACKUP_FILES` — index.js:5732 (array plano de nombres de archivo)
- `seedVolumeFromRepo()` — index.js:5671-5687 (el `for` de la línea 5679)
- `GET /api/admin/export-data` — index.js:3668-3723 (`telnyxConfig`/`telnyxEvents`
  como claves del payload)
- `POST /api/admin/import-data` — index.js:3768-3814 (validación de shape +
  restore)
- `scripts/pre-deploy.js` — array `SENSITIVE` (líneas 164-171) + lista `extras`
  (líneas 173-190, tuplas `[key, filename]`)

Pseudo-SDR:
`ensureSetterProfile(name)` — index.js:575-584. Idempotente (chequea por id o por
nombre), NO requiere user vinculado, el shape que crea (`{id, name,
activeVariantId:'', createdAt}`) NO incluye `hidden` → visible por default.
`ensureSetterProfile('Agente IA')` produce el id `setter_agente_ia`.
Depende de `loadSettersData` (index.js:6586) y `SETTERS_FILE` (index.js:6561) →
la llamada de boot NO puede ir antes de la línea 6674.

`ADMIN_ONLY_SETTER_IDS` — index.js:7257 — es `Set(['setter_ignacio',
'setter_paula_kroff'])`. `setter_agente_ia` NO está ahí y NO debe agregarse.

Test que hoy assertea la lista de bloques del export: `tests/export-data-full.test.js`
línea 73-80 (chequea `toHaveProperty` por clave, no un set exacto → sumar claves
nuevas no lo rompe, pero el título del test dice "12 bloques" y hay que
actualizarlo si se agregan).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Módulo de config Retell + log de eventos + pseudo-SDR</name>
  <files>index.js</files>
  <read_first>
    - index.js:13996-14154 — el bloque Telnyx completo a clonar: file consts, ENV_FIELDS, envSourced, load, save, public, setter-view
    - index.js:15495-15515 — `loadTelnyxEvents` / `saveTelnyxEvents`
    - index.js:15044 — `_telnyxWebhookRejects`, el contador en memoria a espejar
    - index.js:575-584 — `ensureSetterProfile`
    - index.js:6674-6715 — `saveSettersData` y `mutateSettersData`: el punto más temprano donde el seed del pseudo-SDR puede correr
    - index.js:5663-5691 — `seedVolumeFromRepo` y por qué el seed del setter NO puede ir ahí
    - .planning/phases/24-integracion-backend-retell/24-RESEARCH.md §2.1 (por qué webhookSecret cae a apiKey) y §4 (esqueleto de config)
  </read_first>
  <action>
Crear el bloque de configuración de Retell en scope de módulo, adyacente al
bloque de Telnyx (después de `_setterTelnyxConfig`, index.js:14154) para que la
coherencia del archivo se mantenga.

Contenido:

`RETELL_CONFIG_FILE` = `retell_config.json` y `RETELL_EVENTS_FILE` =
`retell_events.json`, ambos en `DATA_DIR`.

`RETELL_ENV_FIELDS` con TRES campos:
`apiKey: 'RETELL_API_KEY'`, `webhookSecret: 'RETELL_WEBHOOK_SECRET'`,
`toolSecret: 'RETELL_TOOL_SECRET'`.
`toolSecret` es una EXTENSIÓN de D-24-01, no una contradicción: VOICE-04 exige
un header secreto `x-scm-tool-secret` para `/book`, y un secret que no siga el
patrón env>JSON del proyecto sería la única credencial del sistema viviendo en
el volumen. Documentarlo en un comentario.

`_defaultRetellConfig()` con los campos NO secretos:
`agentId: ''`, `fromNumberId: ''` (vacío = round-robin, D-24-01),
`dailyCap: 50`, `enabled: false`, `rotationIdx: 0`, `whatsappReturn: ''`
(el número de retorno que se inyecta como variable dinámica, ver Specifics de
CONTEXT), `updatedAt`, `updatedBy: 'system_seed'`.
Los tres campos secretos arrancan en `''`.

`_retellEnvSourced()`, `loadRetellConfig()` (lazy-init + normalización de tipos +
overlay env>JSON con la env var ganando SIEMPRE) y `saveRetellConfig()`: clones
directos de sus equivalentes Telnyx.

`_retellWebhookSecret(cfg)`: devuelve `cfg.webhookSecret` si tiene contenido, si
no `cfg.apiKey`, si no `''`. **Esta es la corrección de research §2.1**: Retell
NO tiene un signing secret separado — firma el webhook con HMAC-SHA256 usando el
MISMO API key (verificado leyendo el source de `retell-sdk@5.53.0`,
`lib/webhook_auth.mjs`). El campo `webhookSecret` se conserva solo por si la
cuenta del user resultara tener un valor distinto con el "webhook badge";
nunca es obligatorio. Comentar esto en el código, no solo acá.

`_retellToolSecret(cfg)`: devuelve `cfg.toolSecret` o `''` (sin fallback — un
tool secret ausente debe ser detectable, no silenciosamente igual al API key).

`_publicRetellConfig(cfg)`: devuelve `hasApiKey`, `hasWebhookSecret`
(true si `_retellWebhookSecret` tiene contenido), `hasToolSecret`, `envSourced`,
`agentId`, `fromNumberId`, `dailyCap`, `enabled`, `rotationIdx`,
`whatsappReturn`, `updatedAt`, `updatedBy`, y un objeto `webhook` con la salud
del endpoint (`rejects`, `lastReject`, `lastEventAt`, `eventCount`) — D-24-06
pide la health en el GET de config, no en un endpoint aparte. NUNCA devolver el
valor de ningún secret.

**Este plan DECLARA el shape del objeto `webhook`, completo y ya enchufado a sus
fuentes reales**: `rejects`/`lastReject` salen de `_retellWebhookRejects` (el
contador en memoria que se declara más abajo) y `lastEventAt`/`eventCount` de
`loadRetellEvents()`. Hasta que exista el webhook del plan 24-04 esas fuentes
están vacías, así que el GET devuelve `total: 0`, `lastReject: null`,
`lastEventAt: null` y `eventCount: 0` — valores legítimos derivados de las
fuentes, **no placeholders hardcodeados**. El plan 24-04 no redeclara nada de
esto: solo hace que las fuentes se pueblen.

`loadRetellEvents()` / `saveRetellEvents()`: clones de los de Telnyx, shape
`{ events: [] }`.

`_retellWebhookRejects = { total: 0, last: null, since: <ISO del boot> }` —
contador en memoria, espejo de `_telnyxWebhookRejects` (index.js:15044). Lo
incrementa el plan 24-04; acá solo se declara y se lee en la health.

Pseudo-SDR (D-24-09): declarar las constantes de módulo
`VOICE_AGENT_SETTER_NAME = 'Agente IA'` y `VOICE_AGENT_SETTER_ID =
'setter_agente_ia'`, y llamar `ensureSetterProfile(VOICE_AGENT_SETTER_NAME)` una
sola vez en el boot, inmediatamente después de la definición de
`mutateSettersData` (index.js ~6713), guardado por
`if (process.env.NODE_ENV !== 'test')`. El guard es deliberado: sin él, cada
fixture de test ganaría un setter extra y varios tests que cuentan filas de
Equipo/Distribución cambiarían de número — exactamente la clase de regresión que
esta fase no puede permitirse. Los tests que necesiten el agente lo ponen en su
propio fixture de `setters.json`.

Sumar `loadRetellConfig`, `_publicRetellConfig`, `_retellWebhookSecret`,
`_retellToolSecret`, `VOICE_AGENT_SETTER_ID` al objeto `globalThis.__voiceAgent`
que creó 24-01 (extender el objeto existente, no crear uno nuevo).
  </action>
  <verify>
    <automated>node --check index.js && npx vitest run tests/team-performance.test.js tests/pool-distribution.test.js tests/command-metrics.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "RETELL_ENV_FIELDS" index.js` devuelve la declaración con las 3 claves `apiKey`, `webhookSecret`, `toolSecret`
    - `grep -n "function _publicRetellConfig" index.js` devuelve 1 línea y su cuerpo NO contiene `cfg.apiKey` fuera de una expresión booleana (`!!`)
    - `grep -n "VOICE_AGENT_SETTER_ID = 'setter_agente_ia'" index.js` devuelve 1 línea
    - `grep -n "ensureSetterProfile(VOICE_AGENT_SETTER_NAME)" index.js` devuelve 1 línea, precedida por un guard `NODE_ENV !== 'test'`
    - `grep -n "setter_agente_ia" index.js` NO aparece dentro de `ADMIN_ONLY_SETTER_IDS`
    - `node --check index.js` sale 0
    - `npx vitest run tests/team-performance.test.js tests/pool-distribution.test.js tests/command-metrics.test.js` sale 0 (el seed del pseudo-SDR no cambió ningún conteo de setters en los tests)
  </acceptance_criteria>
  <done>La config de Retell se carga con overlay env>JSON, el sanitizador público no filtra secrets, y el pseudo-SDR existe en producción al bootear sin ensuciar los fixtures de test.</done>
</task>

<task type="auto">
  <name>Task 2: Endpoints GET/PUT de config + regla #21 en los 5 lugares</name>
  <files>index.js, scripts/pre-deploy.js</files>
  <read_first>
    - index.js:14722-14815 — `GET`/`PUT /api/telnyx/config` completos: RBAC, detección de campos bloqueados con 409, lectura del JSON SIN overlay antes de guardar, self-healing, respuesta pública
    - index.js:5732 — `BACKUP_FILES`
    - index.js:5679 — el `for` de `seedVolumeFromRepo`
    - index.js:3665-3730 — armado del payload de `/api/admin/export-data`
    - index.js:3760-3820 — validación y restore de `/api/admin/import-data`
    - scripts/pre-deploy.js:154-193 — el bloque `SENSITIVE` de Telnyx y el array `extras`
    - tests/export-data-full.test.js:70-82 — el test que enumera los bloques del export
    - .planning/phases/24-integracion-backend-retell/24-RESEARCH.md §5.2 — el hueco real de `seedVolumeFromRepo` que NO hay que clonar
  </read_first>
  <action>
**Endpoints.** Montar `GET /api/retell/config` y `PUT /api/retell/config` junto al
bloque de rutas de Telnyx.

`GET`: `requireAuth` + `requireRole('admin')`. Devuelve `_publicRetellConfig(loadRetellConfig())`.
A diferencia de Telnyx, NO hay vista reducida para el SDR: ningún rol que no sea
admin tiene nada que hacer con la config del agente (el panel de VOICE-07 es
admin-only). Un supervisor recibe 403.

`PUT`: `requireAuth` + `requireRole('admin')`. Acepta `apiKey`, `webhookSecret`,
`toolSecret`, `agentId`, `fromNumberId`, `dailyCap`, `enabled`, `whatsappReturn`.
Campos omitidos NO se tocan (mismo criterio que Telnyx: no borrar secrets sin
querer). Reglas:
- Si un campo secreto viene en el body Y está env-sourced → responder **409**
  con el detalle de qué campos y el hint de editarlos en Railway (clon de
  index.js:14751-14757).
- Leer el JSON crudo SIN pasar por `loadRetellConfig()` antes de guardar, para
  que el overlay de env no quede persistido en disco (index.js:14759-14767).
- Self-healing: después de aplicar los cambios, todo campo de `RETELL_ENV_FIELDS`
  que esté env-sourced se limpia a `''` en el JSON (index.js:14793-14797).
- Validar `dailyCap` como entero finito entre 0 y 500; `enabled` como boolean;
  `agentId`/`fromNumberId`/`whatsappReturn` como strings recortados con cap de
  200 chars.
- `rotationIdx` NO es editable desde el panel (lo administra el dispatch del plan
  24-03). Ignorarlo si viene en el body.
- Sellar `updatedAt` / `updatedBy` con el email del admin.
- Responder `_publicRetellConfig(loadRetellConfig())`.

**Regla #21, los 5 lugares** — para `retell_config.json` Y `retell_events.json`:
1. `BACKUP_FILES` (index.js:5732): agregar los 2 nombres de archivo.
2. `seedVolumeFromRepo` (index.js:5679): agregar los 2 al array del `for`.
   Research §5.2 documenta que los archivos de Telnyx NO están ahí — es deuda
   preexistente, **no la clones**. Dejar los de Telnyx como están (arreglarlos
   está fuera del alcance de esta fase; anotarlo en el SUMMARY como deferred).
3. `/api/admin/export-data`: sumar `retellConfig` (vía `loadRetellConfig()` en
   try/catch, igual que Telnyx) y `retellEvents` (vía `loadRetellEvents()`).
4. `/api/admin/import-data`: sumar la validación de shape (`retellConfig` objeto,
   `retellEvents.events` array), sumar las 2 claves a la lista del chequeo de
   "payload vacío", y el restore con `saveRetellConfig` / `saveRetellEvents`.
5. `scripts/pre-deploy.js`: replicar el bloque de stripping de Telnyx para
   `data.retellConfig` con `SENSITIVE = ['apiKey','webhookSecret','toolSecret']`
   (el API key de Retell es tan pusheable a GitHub como el de Telnyx — ver
   memoria `predeploy-telnyx-secret-leak`), y sumar a `extras` las tuplas
   `['retellConfig','retell_config.json']` y `['retellEvents','retell_events.json']`.

Actualizar el título del test `tests/export-data-full.test.js` (línea 70) y su
`EXPECTED_KEYS` para reflejar los bloques nuevos — es el único test preexistente
que este plan puede tocar, y solo para SUMAR claves, nunca para relajar un assert.
  </action>
  <verify>
    <automated>node --check index.js && node --check scripts/pre-deploy.js && npx vitest run tests/export-data-full.test.js tests/security-rbac.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "api/retell/config" index.js` devuelve exactamente 2 rutas (GET y PUT), ambas con `requireRole("admin")`
    - `grep -n "retell_config.json" index.js` aparece en la definición del path, en `BACKUP_FILES` y en el array de `seedVolumeFromRepo` (3 apariciones mínimo)
    - `grep -n "retell_events.json" index.js` aparece en las mismas 3 listas
    - `grep -n "retellConfig" index.js` aparece en el payload del export, en la validación del import y en el restore
    - `grep -n "retellConfig\|retell_config.json" scripts/pre-deploy.js` devuelve el bloque `SENSITIVE` y la tupla de `extras`
    - `node --check scripts/pre-deploy.js` sale 0
    - `npx vitest run tests/export-data-full.test.js tests/security-rbac.test.js` sale 0
  </acceptance_criteria>
  <done>La config de Retell se lee y escribe desde el panel con lock de env vars, y los 2 archivos nuevos sobreviven un redeploy por las 5 vías.</done>
</task>

<task type="auto">
  <name>Task 3: tests/retell-config.test.js</name>
  <files>tests/retell-config.test.js</files>
  <read_first>
    - tests/security-rbac.test.js — patrón de asserts de RBAC por rol (admin / supervisor / setter / anónimo)
    - tests/export-data-full.test.js — patrón de round-trip export→import
    - .planning/phases/24-integracion-backend-retell/24-RESEARCH.md §4 — fixture con `process.env.RETELL_API_KEY = ""`
  </read_first>
  <action>
Crear `tests/retell-config.test.js`. Fixture: `DATA_DIR` temporal, `auth.json`
con admin + supervisor + setter, `setters.json` con `setter_agente_ia` ya
presente en el array de setters (el seed de boot está apagado en test — así el
test prueba la coexistencia, no el seed), env vars de Retell a `""` (regla #121,
NUNCA `delete`).

Cobertura mínima:

RBAC y no-leak
- `GET /api/retell/config` sin sesión → 401.
- Con setter → 403. Con supervisor → 403. Con admin → 200.
- La respuesta del admin NO contiene las strings de ningún secret: escribir un
  `apiKey` por PUT y verificar que un `JSON.stringify` de la respuesta del GET
  no lo incluye, y que sí incluye `hasApiKey: true`.

Env-sourced (D-24-01)
- Con `RETELL_API_KEY` seteada en `process.env`, el PUT que manda `apiKey`
  responde **409** y el body nombra el campo bloqueado.
- El PUT que manda solo `agentId` con `RETELL_API_KEY` activa responde 200
  (bloquear un campo no bloquea los demás).
- `envSourced.apiKey === true` en la respuesta del GET cuando la env var está.
- Self-healing: pre-poblar `retell_config.json` con un `apiKey` viejo en el JSON,
  setear la env var, hacer un PUT de `agentId`, y verificar leyendo el archivo de
  disco que `apiKey` quedó en `""`.
- Overlay: con la env var seteada, el valor efectivo que usa el server es el de
  env, no el del JSON (verificable indirectamente por `hasApiKey`).

webhookSecret con fallback (research §2.1)
- Sin `webhookSecret` y con `apiKey` cargado → `hasWebhookSecret` es `true`
  (cae al apiKey).
- Sin ninguno de los dos → `hasWebhookSecret` es `false`.

Validaciones del PUT
- `dailyCap` fuera de rango (negativo, >500, no numérico) no se persiste.
- `enabled` no-boolean se ignora.
- Un campo omitido conserva su valor anterior.
- `rotationIdx` mandado por el body NO cambia el valor persistido.

Persistencia (regla #21)
- `GET /api/admin/export-data` como admin incluye `retellConfig` y `retellEvents`.
- El export NO incluye el valor del `apiKey` cuando viene de env (viene del JSON
  limpiado por el self-healing) — assertear explícitamente.
- Round-trip: `POST /api/admin/import-data` con un `retellConfig` que trae
  `agentId` y `dailyCap` restaura esos valores (leer con el GET).

Pseudo-SDR (D-24-09)
- `globalThis.__voiceAgent.VOICE_AGENT_SETTER_ID === 'setter_agente_ia'`.
- `globalThis.__phase18._filterSettersVisible` (o el endpoint de setters que use
  el panel) devuelve al agente para el admin — el agente NO está oculto.
- `ADMIN_ONLY_SETTER_IDS` no lo contiene: verificable pidiendo la lista de
  setters como supervisor sin restricción y encontrando `setter_agente_ia`.
- Con un lead asignado a `setter_agente_ia` y una entry en su `callLog` con
  `by: ''` y `channel: 'retell'`, `GET /api/setters/team-performance` como admin
  devuelve una fila para el agente con esa llamada contada (D-24-07 anticipado:
  la atribución cae sola por `assignedTo`, sin código de métricas nuevo).
  Teléfono del lead de ≥7 dígitos (regla #163), ej. `+525550000123`.
  </action>
  <verify>
    <automated>npx vitest run tests/retell-config.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/retell-config.test.js` sale 0 con al menos 16 tests
    - El archivo contiene un assert de que la respuesta del GET no incluye el valor literal del apiKey de prueba
    - El archivo contiene un assert de status `409` para el PUT de un campo env-sourced
    - El archivo contiene un assert de que `team-performance` devuelve una fila con `setterId === 'setter_agente_ia'`
    - Ningún `delete process.env.` en el archivo
    - `npm test` sale 0
  </acceptance_criteria>
  <done>La config está probada por el lado de seguridad (RBAC + no-leak + lock de env), de persistencia (round-trip) y de identidad (el agente ya figura como fila comparable en Equipo).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descripción |
|----------|-------------|
| navegador admin → `PUT /api/retell/config` | Entrada de credenciales desde una sesión con cookie |
| `index.js` → Railway Volume (`retell_config.json`) | Los secrets no deben tocar disco cuando vienen de env |
| Volume → repo git (vía `npm run pre-deploy`) | Todo lo que baja el pre-deploy se commitea; GitHub Push Protection rechaza API keys |

## STRIDE Threat Register

| Threat ID | Categoría | Componente | Disposición | Mitigación |
|-----------|-----------|------------|-------------|------------|
| T-24-02-01 | Information Disclosure | `GET /api/retell/config` | mitigate | `_publicRetellConfig` devuelve solo flags `hasX` + `envSourced`; test que assertea que el valor literal del apiKey no aparece en el body. Admin-only (supervisor y setter → 403) |
| T-24-02-02 | Information Disclosure | `data/retell_config.json` commiteado al repo | mitigate | Stripper de `apiKey`/`webhookSecret`/`toolSecret` en `scripts/pre-deploy.js` (mismo patrón y mismo motivo que la memoria `predeploy-telnyx-secret-leak`) + self-healing que deja el JSON vacío cuando el secret vive en env |
| T-24-02-03 | Tampering | Un admin (o una sesión robada) cambia el API key desde el panel para redirigir el gasto | mitigate | Con la env var seteada en Railway el PUT responde 409 y el JSON se limpia solo: el valor efectivo no es editable desde ninguna superficie web. Es la razón operativa de D-24-01 |
| T-24-02-04 | Elevation of Privilege | Supervisor scoped leyendo la config del agente | mitigate | `requireRole('admin')` en GET y PUT (a diferencia de Telnyx, que le da vista reducida al supervisor). Test explícito de 403 |
| T-24-02-05 | Denial of Service | `dailyCap` seteado a un número enorme desde el panel → gasto descontrolado en 24-03 | mitigate | Validación de rango 0..500 en el PUT. El cap real de gasto lo aplica el dispatch (plan 24-03), esto es la primera barrera |
| T-24-02-06 | Repudiation | Cambio de config sin rastro | mitigate | `updatedAt` + `updatedBy` (email del admin) sellados en cada PUT, igual que Telnyx |
| T-24-02-07 | Denial of Service | Redeploy de Railway borra la config del agente | mitigate | Regla #21 en los 5 lugares, incluido `seedVolumeFromRepo` — que el precedente de Telnyx NO cubre (research §5.2) |
| T-24-02-SC | Tampering | instalación de paquetes npm | accept | Este plan NO instala dependencias. `package.json` no se modifica |
</threat_model>

<verification>
1. `node --check index.js` y `node --check scripts/pre-deploy.js` salen 0.
2. `npm test` verde.
3. `grep -rn "retell_config.json" index.js scripts/pre-deploy.js` aparece en las 5 superficies de la regla #21.
4. Un `GET /api/retell/config` como admin con `RETELL_API_KEY` seteada devuelve `envSourced.apiKey === true` y ningún secret.
5. `git diff --stat public/` vacío.
</verification>

<success_criteria>
- Los secrets de Retell viven en env vars, ganan sobre el JSON y no son editables desde el panel (success criterion 6 del ROADMAP).
- `retell_config.json` y `retell_events.json` están en las 5 superficies de persistencia.
- `setter_agente_ia` existe, es visible y ya aparece como fila en Equipo con sus llamadas atribuidas (success criterion 5 del ROADMAP).
</success_criteria>

<output>
Crear `.planning/phases/24-integracion-backend-retell/24-02-SUMMARY.md` al terminar.
Documentar: (a) el shape final de `retell_config.json`, (b) las env vars que el
user tiene que cargar en Railway (`RETELL_API_KEY`, `RETELL_TOOL_SECRET`, y
`RETELL_WEBHOOK_SECRET` solo si su dashboard le da un valor distinto), (c) que el
hueco de `seedVolumeFromRepo` para los archivos de Telnyx sigue abierto a
propósito.
</output>
