node_modules/
.env
# data/ ya NO se ignora — se sube al repo para persistir entre deploys
# pero los .bak* sí: son safety nets locales, no se trackean
data/*.bak
data/*.bak-*
# Backups automáticos (cron 6 hs) — viven solo en disco local del server
data/backups/
# Logs y state local, nunca commitear
data/error.log
data/error.log.old
data/reports.json
server.out.log
server.err.log
tmp/
wa1.html
wa2.html
debug.js
test_qwen.js
.env.local
# Archivo personal con notas/credenciales scratch — nunca subirlo
Scapper.txt
# Dump viejo de leads (9MB con PII) que se coló en un commit de UI — destrackeado.
# Sigue en disco local; nunca volver a commitearlo.
paula-old.json
# Snapshots/dumps sueltos de datos en la raíz (mismo criterio que paula-old).
*-old.json
# Script one-shot de armado de xlsx — no es parte del runtime.
build_xlsx.js
.claude/
# Carpeta de assets del desktop WAMULTI — no pertenece a este repo
wa-multi/
# Atajo local de pre-deploy (lleva URL + email, NO contraseña). No commitear.
pre-deploy.bat
